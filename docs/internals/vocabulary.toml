version = 1
complete_public_api_crates = ["fava-simple-groups"]

# Established Nostr vocabulary. These meanings come from the cited protocol,
# not from Fava.

[[term]]
name = "Event"
source = "nostr"
protocol = "NIP-01"
meaning = "A signed Nostr event."
owner = "nostr"
symbols = []
crates = []

[[term]]
name = "EventId"
source = "nostr"
protocol = "NIP-01"
meaning = "The SHA-256 identifier computed from a Nostr event's serialized body."
owner = "nostr"
symbols = []
crates = []

[[term]]
name = "PublicKey"
source = "nostr"
protocol = "NIP-01"
meaning = "The public key identifying a Nostr event author."
owner = "nostr"
symbols = []
crates = []

[[term]]
name = "Kind"
source = "nostr"
protocol = "NIP-01"
meaning = "The integer declaring a Nostr event's protocol meaning and replacement behavior."
owner = "nostr"
symbols = []
crates = []

[[term]]
name = "Tag"
source = "nostr"
protocol = "NIP-01"
meaning = "One ordered array in a Nostr event's tags field."
owner = "nostr"
symbols = []
crates = []

[[term]]
name = "Filter"
source = "nostr"
protocol = "NIP-01"
meaning = "A Nostr relay filter selecting events for a subscription."
owner = "nostr"
symbols = []
crates = []

[[term]]
name = "RelayUrl"
source = "nostr"
protocol = "NIP-01"
meaning = "The URL identifying a Nostr relay endpoint."
owner = "nostr"
symbols = []
crates = []

[[term]]
name = "Subscription"
source = "nostr"
protocol = "NIP-01"
meaning = "Relay work opened by a REQ message and identified until CLOSE."
owner = "nostr"
symbols = []
crates = []

[[term]]
name = "Coordinate"
source = "nostr"
protocol = "NIP-01"
meaning = "The kind, author, and optional identifier that address a replaceable event."
owner = "nostr"
symbols = []
crates = []

[[term]]
name = "ClientMessage"
source = "nostr"
protocol = "NIP-01"
meaning = "A Nostr protocol message sent from a client to a relay."
owner = "fava-wire"
symbols = []
crates = ["fava-wire"]
spec_symbols = ["ClientMessage"]
spec_crates = ["fava-wire"]

[[term]]
name = "RelayMessage"
source = "nostr"
protocol = "NIP-01"
meaning = "A Nostr protocol message sent from a relay to a client."
owner = "fava-wire"
symbols = []
crates = []
spec_symbols = ["RelayMessage"]

[[term]]
name = "ContactList"
source = "nostr"
protocol = "NIP-02"
meaning = "A replaceable kind 3 event describing an author's contacts and relay hints."
owner = "fava-nip02"
nearest_nostr = "NIP-02 contact list"
distinction = "The protocol event is raw; fava_nip02::ContactList is the immutable validated projection that exposes valid rows and complete typed row evidence without owning query or publication work."
counterexample = "An arbitrary kind 3 tag walk that drops malformed p rows is not a ContactList projection, and a QuerySnapshot remains query-owned rather than protocol-owned."
lifecycle = "Callers construct the value from one EventValue; it owns immutable decoded fields and is dropped without state, acquisition, observation, or publication effects."
forcing_requirement = "NIP-02 applications must read one author-scoped list without raw tag walking while retaining every uninterpreted row and refusing invalid event boundaries."
falsifier = "The contact_list tests must refuse wrong-kind or invalid events and must fail if valid empty kind-3 events do not decode."
symbols = ["fava_nip02::ContactList"]
crates = ["fava-nip02"]
spec_crates = ["fava-nip02"]

[[term]]
name = "Follow"
source = "fava"
meaning = "One fully valid first-occurrence NIP-02 p row with its source index, public key, optional relay hint, and optional petname."
owner = "fava-nip02"
nearest_nostr = "NIP-02 p tag"
distinction = "A p tag is an untyped ordered string row; Follow exists only after every understood column validates and preserves absent versus present-empty petname meaning."
counterexample = "A malformed relay row or later valid duplicate is ContactListRowEvidence, not a Follow, and cannot reserve a target before validation completes."
lifecycle = "ContactList::from_event creates immutable Follow values; they borrow no event storage and disappear with their owning ContactList."
forcing_requirement = "Applications need ordered typed public keys, relay hints, and exact petnames without duplicating NIP-02 parsing or accepting partially valid rows."
falsifier = "The invalid_contact_rows_do_not_reserve_duplicate_targets test must fail if an invalid earlier row becomes a Follow or poisons the later valid row."
symbols = ["fava_nip02::Follow"]
crates = []

[[term]]
name = "ContactListRowEvidence"
source = "fava"
meaning = "Exact owned evidence for one malformed, duplicate, or uninterpreted NIP-02 p row."
owner = "fava-nip02"
nearest_nostr = "NIP-02 p tag"
distinction = "NIP-02 defines valid contact rows but not a typed conservation result for rows a client cannot safely interpret."
counterexample = "A parser warning without source index and exact raw columns cannot prove that every p row was retained and is not ContactListRowEvidence."
lifecycle = "ContactList::from_event creates one immutable evidence value for each non-valid p row; the ContactList owns it until dropped."
forcing_requirement = "Shared kind-3 documents may contain malformed, duplicate, or future rows, and applications must not silently lose data another client may understand."
falsifier = "The nip02_accounts_for_every_p_row test must fail when parsing uses filter_map to retain only valid rows."
symbols = ["fava_nip02::ContactListRowEvidence"]
crates = []

[[term]]
name = "ContactListError"
source = "fava"
meaning = "A typed refusal for an invalid event-level boundary before NIP-02 row decoding begins."
owner = "fava-nip02"
nearest_nostr = "NIP-02 kind 3 event"
distinction = "Row evidence represents a valid document's uninterpretable contacts; ContactListError instead refuses a wrong-kind, unfinalized, unverifiable, or over-bound whole event."
counterexample = "An invalid relay hint inside an otherwise valid kind 3 is row evidence and must not become ContactListError."
lifecycle = "ContactList::from_event returns the value synchronously and retains no failure state after the caller drops it."
forcing_requirement = "Invalid event identity, signature, kind, or bounds must be rejected once before any typed row result can be trusted."
falsifier = "The invalid_contact_list_events_are_refused_before_rows test must fail if wrong-kind, missing-id, tampered, or over-bound events produce a ContactList."
symbols = ["fava_nip02::ContactListError"]
crates = []

[[term]]
name = "IntoContactAuthors"
source = "fava"
meaning = "The sealed one-or-many public-key input accepted by the NIP-02 contact-list query builder."
owner = "fava-nip02"
nearest_nostr = "NIP-02 contact-list author"
distinction = "Nostr identifies one event author; IntoContactAuthors permits the same ordinary kind-3 query builder to retain a concrete author axis for either one key or a finite caller-owned key collection."
counterexample = "An unconstrained kind-3 query or an application-defined conversion that can erase an empty author axis is not IntoContactAuthors."
lifecycle = "The caller supplies one supported owned or borrowed key shape; contact_list consumes its finite iterator immediately into Query selection and retains no conversion or observation state."
forcing_requirement = "Applications must compose both first-hop and two-hop contact-list reads without synonym functions, while empty author input must remain a present-empty match-nothing axis."
falsifier = "The one_many_and_empty_contact_list_queries_keep_a_concrete_author_axis and external public_api tests must fail if one and borrowed-many inputs do not reach the same builder or empty input becomes unconstrained."
symbols = ["fava_nip02::IntoContactAuthors"]
crates = []

[[term]]
name = "BookmarkList"
source = "nostr"
protocol = "NIP-51"
meaning = "A replaceable event carrying an author's public or private bookmarks."
owner = "fava-bookmarks"
symbols = []
crates = ["fava-bookmarks"]
spec_crates = ["fava-bookmarks"]

[[term]]
name = "SimpleGroup"
source = "nostr"
protocol = "NIP-29"
nearest_nostr = "NIP-29 relay-based group identified by an h or d tag value"
meaning = "One non-empty opaque simple-group id plus a normalized non-empty sequence of application-selected relays."
owner = "fava-simple-groups"
distinction = "A raw id or relay URL alone cannot lower both exact group selection and the complete caller-selected relay sequence."
counterexample = "A globally canonical group id or a value that silently chooses one relay is not this SimpleGroup."
lifecycle = "Constructed synchronously from a complete finite owned RelayUrl vector after its id and relay selection validate; immutable thereafter and owns no query or publication work."
forcing_requirement = "Applications must reuse one exact id and non-empty relay selection across content queries and concrete EventBuilder composition without a shared numeric bound or a new relay owner."
falsifier = "Construction and composition tests fail if an empty id or empty relay vector succeeds, a non-empty opaque id changes, first-occurrence relay order changes, or group composition omits an id or any selected relay."
symbols = ["fava_simple_groups::SimpleGroup"]
crates = ["fava-simple-groups"]

[[term]]
name = "SimpleGroupEventBuilder"
source = "fava"
nearest_nostr = "NIP-29 h tag and relay URL"
meaning = "The fava-simple-groups extension trait that adds one selected SimpleGroup's exact h context and relays to a concrete EventBuilder."
owner = "fava-simple-groups"
distinction = "SimpleGroup is an immutable group value and EventBuilder owns generic event and route construction; SimpleGroupEventBuilder alone owns fluent NIP-29 group-tag composition without a wrapper, publisher, signer, route store, or receipt."
counterexample = "A wrapper around EventBuilder, a global group registry, or a helper that accepts a signed Event is not SimpleGroupEventBuilder."
lifecycle = "An application imports the trait and calls simple_group on an EventBuilder before publication; each call returns the same concrete builder or the existing WriteIntentError from generic route accumulation, and the trait retains no state."
forcing_requirement = "One unsigned event must carry several selected NIP-29 contexts and the exact bounded route union while protocol-neutral EventBuilder remains independent of NIP-29 and fluent builder methods remain available."
falsifier = "Public API, composition, and facade tests fail if simple_group changes the concrete builder type, adds a duplicate owned exact h tag, translates the route-bound error, serializes relays, or leaves a selected relay out of the route union."
symbols = ["fava_simple_groups::SimpleGroupEventBuilder"]
crates = []

[[term]]
name = "SimpleGroupConstructionError"
source = "fava"
nearest_nostr = "NIP-29 simple group id plus application-selected relay sequence"
meaning = "The exact EmptyId or EmptyRelays refusal to construct a SimpleGroup from an empty id or empty finite relay vector."
owner = "fava-simple-groups"
distinction = "It attributes only invalid reusable group construction input; relay URL parsing, query bounds, write bounds, and event decoding retain their existing error owners."
counterexample = "An invalid RelayUrl, a QueryError from lowering, or a SimpleGroupDecodeError from one state event is not a SimpleGroupConstructionError."
lifecycle = "Created synchronously when new refuses exactly one of its two input invariants; it owns no retained state or work."
forcing_requirement = "Applications must distinguish an unusable group id from a missing relay selection before a reusable SimpleGroup exists without moving those invariants into query or write operations."
falsifier = "Public constructor tests fail if empty ids and empty relay vectors do not return their distinct variants or if any other non-empty id is refused."
symbols = ["fava_simple_groups::SimpleGroupConstructionError"]
crates = []

[[term]]
name = "SimpleGroupStateEventKind"
source = "nostr"
protocol = "NIP-29"
nearest_nostr = "Relay-generated group state kinds 39000 through 39005"
meaning = "The closed selector mapping Metadata→39000, Admins→39001, Members→39002, Roles→39003, LivekitParticipants→39004, and Pins→39005."
owner = "fava-simple-groups"
distinction = "It names the exact six-family domain without owning an observation or arbitrary kind policy."
counterexample = "An arbitrary event Kind or content kind selected through h is not a SimpleGroupStateEventKind."
lifecycle = "An inert copyable query input consumed synchronously by meta_events."
forcing_requirement = "Callers need a type-safe subset, including the query owner's match-nothing empty set, without copying raw NIP-29 kind numbers."
falsifier = "The ALL and state-event query tests fail if any kind is missing, duplicated, reordered, or lowered to another number."
symbols = ["fava_simple_groups::SimpleGroupStateEventKind"]
crates = []

[[term]]
name = "SimpleGroupMetadata"
source = "nostr"
protocol = "NIP-29"
nearest_nostr = "Kind-39000 group metadata event"
meaning = "The semantic metadata fields decoded from one kind-39000 event."
owner = "fava-simple-groups"
distinction = "It retains first singleton values, presence flags, value-local supported kinds, and ordered child entries without assigning trust."
counterexample = "A metadata object merged across events or relays is not SimpleGroupMetadata."
lifecycle = "Decoded synchronously from one EventValue and immutable thereafter."
forcing_requirement = "Applications need typed metadata semantics without raw tag walking or losing malformed siblings."
falsifier = "Metadata decoder tests fail if kind/d boundaries loosen, later singleton tags win, or malformed values erase valid siblings."
symbols = ["fava_simple_groups::SimpleGroupMetadata"]
crates = []

[[term]]
name = "SimpleGroupAdmins"
source = "nostr"
protocol = "NIP-29"
nearest_nostr = "Kind-39001 group administrators event"
meaning = "The ordered administrator entries decoded from one kind-39001 event."
owner = "fava-simple-groups"
distinction = "Each p tag retains one public key, every role value, or its own parse failure."
counterexample = "A membership set, relay-authority projection, or merged admin state is not SimpleGroupAdmins."
lifecycle = "Decoded synchronously from one EventValue and immutable thereafter."
forcing_requirement = "Applications need all administrator entries and entry-local failures in source order."
falsifier = "People decoder tests fail if repetitions collapse, roles reorder, or one malformed p tag removes valid siblings."
symbols = ["fava_simple_groups::SimpleGroupAdmins"]
crates = []

[[term]]
name = "SimpleGroupMembers"
source = "nostr"
protocol = "NIP-29"
nearest_nostr = "Kind-39002 group members event"
meaning = "The ordered member entries decoded from one kind-39002 event."
owner = "fava-simple-groups"
distinction = "Each p tag remains a public key or its own parse failure without set canonicalization."
counterexample = "An inferred membership absence or member set merged across events is not SimpleGroupMembers."
lifecycle = "Decoded synchronously from one EventValue and immutable thereafter."
forcing_requirement = "Applications need typed member entries without raw tag walking or sibling erasure."
falsifier = "People decoder tests fail if repeated members collapse or malformed entries erase later valid members."
symbols = ["fava_simple_groups::SimpleGroupMembers"]
crates = []

[[term]]
name = "SimpleGroupRoles"
source = "nostr"
protocol = "NIP-29"
nearest_nostr = "Kind-39003 group roles event"
meaning = "The ordered role names and optional descriptions decoded from one kind-39003 event."
owner = "fava-simple-groups"
distinction = "Each role tag retains its first name, optional description, or local missing-value failure."
counterexample = "Administrator role assignments on kind 39001 are not SimpleGroupRoles."
lifecycle = "Decoded synchronously from one EventValue and immutable thereafter."
forcing_requirement = "Applications need role definitions separately from administrator assignments."
falsifier = "Role decoder tests fail if repetitions collapse, descriptions move, or missing names erase valid siblings."
symbols = ["fava_simple_groups::SimpleGroupRoles"]
crates = []

[[term]]
name = "SimpleGroupLivekitParticipants"
source = "nostr"
protocol = "NIP-29"
nearest_nostr = "Kind-39004 LiveKit participants event"
meaning = "The ordered exact-lowercase participant public keys decoded from one kind-39004 event."
owner = "fava-simple-groups"
distinction = "Participant tags use their own lowercase-hex rule rather than member p-tag semantics."
counterexample = "Kind-39002 members or case-normalized public keys are not SimpleGroupLivekitParticipants."
lifecycle = "Decoded synchronously from one EventValue and immutable thereafter."
forcing_requirement = "Applications need the protocol's participant-specific tag and key rules without raw decoding."
falsifier = "LiveKit decoder tests fail if uppercase hex succeeds, valid lowercase keys fail, or malformed siblings erase valid entries."
symbols = ["fava_simple_groups::SimpleGroupLivekitParticipants"]
crates = []

[[term]]
name = "SimpleGroupPins"
source = "nostr"
protocol = "NIP-29"
nearest_nostr = "Kind-39005 pinned items event"
meaning = "The source-interleaved event-id and address-coordinate pins decoded from one kind-39005 event."
owner = "fava-simple-groups"
distinction = "Existing EventCoordinate represents both e and a targets while ordered Result entries retain malformed pins."
counterexample = "A grouped pair of event-id and address lists or pins gathered from content events is not SimpleGroupPins."
lifecycle = "Decoded synchronously from one EventValue and immutable thereafter."
forcing_requirement = "Applications need typed pin targets without losing e/a interleaving or entry failures."
falsifier = "Pin decoder tests fail if e/a entries regroup, invalid coordinates disappear, or repetitions collapse."
symbols = ["fava_simple_groups::SimpleGroupPins"]
crates = []

[[term]]
name = "SimpleGroupDecodeError"
source = "fava"
nearest_nostr = "NIP-29 group state event and semantic tag"
meaning = "A wrong event boundary or one semantic tag-value parse failure while decoding kinds 39000 through 39005."
owner = "fava-simple-groups"
distinction = "Its variants share the single-event semantic decode boundary and retain exact source positions."
counterexample = "Signature, event-id verification, relay provenance, replacement, query, and publication failures are not SimpleGroupDecodeError."
lifecycle = "Created synchronously for a whole-event refusal or retained inside one decoded entry Result."
forcing_requirement = "Malformed semantic input must remain attributable without widening into generic event validity."
falsifier = "Decoder tests fail if wrong kinds or missing d values pass, entry indexes are lost, or one entry failure aborts valid siblings."
symbols = ["fava_simple_groups::SimpleGroupDecodeError"]
crates = []

[[term]]
name = "SavedSimpleGroup"
source = "nostr"
protocol = "NIP-29"
nearest_nostr = "Kind-10009 group tag"
meaning = "One valid saved group entry containing opaque id, relay URL, and optional display name."
owner = "fava-simple-groups"
distinction = "The relay is inert saved-list data and the display name belongs to this exact entry."
counterexample = "A SimpleGroup relay sequence or active routing destination is not SavedSimpleGroup."
lifecycle = "Decoded as an immutable entry inside one SavedGroupList."
forcing_requirement = "Applications need typed group-list entries without mistaking saved relays for active policy."
falsifier = "Saved-list tests fail if id, relay, name, repetition, or source order is lost."
symbols = ["fava_simple_groups::SavedSimpleGroup"]
crates = []

[[term]]
name = "SavedGroupList"
source = "nostr"
protocol = "NIP-29"
nearest_nostr = "Kind-10009 Simple Group List event"
meaning = "One decoded kind-10009 event with author and ordered saved-group and relay entry results."
owner = "fava-simple-groups"
distinction = "One value corresponds to one event and retains both tag families, repetitions, and local failures."
counterexample = "Entries merged across authors or replacement candidates are not one SavedGroupList."
lifecycle = "Decoded synchronously from one EventValue and immutable thereafter."
forcing_requirement = "Applications need one-event list semantics without raw tags or hidden replacement policy."
falsifier = "Saved-list tests fail if wrong kinds pass, authors change, entry families are rescanned, or malformed siblings erase valid entries."
symbols = ["fava_simple_groups::SavedGroupList"]
crates = []

[[term]]
name = "SavedGroupListMaterializer"
source = "fava"
nearest_nostr = "Kind-10009 Simple Group List replacement"
meaning = "The private implementation that verifies a signed or unsigned current kind-10009 source and applies this crate's saved-list edit codec through ReplaceableEventMaterializer."
owner = "fava-simple-groups"
distinction = "The public function returns the neutral contract, while this concrete type owns decoding private edit bytes, exact source author/kind/time qualification, source verification, and deterministic rematerialization."
counterexample = "A signer, author selector, router, write store, publication owner, or public provider contract is not SavedGroupListMaterializer."
lifecycle = "Constructed fresh behind Arc, called by Fava for one materialization, and retains no mutable state."
forcing_requirement = "ReplaceableEventEdit requires one concrete neutral-contract implementation to rematerialize against the current kind-10009 source."
falsifier = "Saved edit tests fail if another edit kind is accepted, an invalid or mismatched signed/unsigned source passes, output time does not advance, source material is lost, or repeated calls retain state."
symbols = []
crates = []

[[term]]
name = "SavedGroupListDecodeError"
source = "fava"
nearest_nostr = "Kind-10009 Simple Group List event and entry"
meaning = "A wrong kind or one missing or invalid saved-list entry value."
owner = "fava-simple-groups"
distinction = "It retains exact entry positions and the saved-list-specific relay URL failure."
counterexample = "Generic event verification, replacement, query, or edit failures are not SavedGroupListDecodeError."
lifecycle = "Returned for the whole event or retained inside one SavedGroupList entry Result."
forcing_requirement = "Malformed saved-list entries must remain attributable without erasing valid sibling entries."
falsifier = "Saved-list tests fail if wrong kinds pass, indexes disappear, invalid relay URLs succeed, or one malformed entry aborts the event."
symbols = ["fava_simple_groups::SavedGroupListDecodeError"]
crates = []

[[term]]
name = "fava_simple_groups"
source = "fava"
nearest_nostr = "NIP-29 relay-based groups and NIP-51 kind-10009 Simple Group Lists"
meaning = "The pure public crate boundary for simple-group queries, event preparation, state decoding, saved-list queries, and saved-list edits."
owner = "fava-simple-groups"
distinction = "It composes protocol semantics over neutral query and write contracts without owning observation, provenance, routing, signing, storage, publication, or lifecycle work."
counterexample = "A provider, group-state projection, relay authority, or runtime facade is not the fava_simple_groups crate boundary."
lifecycle = "Compiled as one stateless library boundary; every public operation is synchronous and returns neutral Fava values or typed refusals."
forcing_requirement = "Applications need one coherent public home for NIP-29 and kind-10009 semantics without making a NIP document or provider lifecycle the crate boundary."
falsifier = "The complete-public-API vocabulary test fails if any compiler-rendered fava_simple_groups identity has no exact owning term."
symbols = ["fava_simple_groups"]
crates = []

[[term]]
name = "save_simple_group"
source = "fava"
nearest_nostr = "Kind-10009 group tag"
meaning = "A pure edit constructor ensuring one saved `group` tag for every normalized relay of a SimpleGroup."
owner = "fava-simple-groups"
distinction = "It keeps the first exact id/relay match, removes later matches, appends missing relays in group order, and carries an optional display name without selecting publication routes."
counterexample = "Publishing, signing, routing, or mutating a stored event directly is not save_simple_group."
lifecycle = "Encodes one bounded ReplaceableEventEdit synchronously and retains no state."
forcing_requirement = "Applications must save every selected group relay through the neutral rematerialization contract while preserving unrelated source material."
falsifier = "Saved-list edit tests fail if a selected relay is omitted, duplicates survive, existing order changes, or the display name is applied outside matching group entries."
symbols = ["fava_simple_groups::save_simple_group"]
crates = []

[[term]]
name = "remove_saved_simple_group"
source = "fava"
nearest_nostr = "Kind-10009 group tag"
meaning = "A pure edit constructor removing every saved `group` tag matching the SimpleGroup id and any of its normalized relays."
owner = "fava-simple-groups"
distinction = "It removes saved-list entries only; the group, relay data, routing, and published group events remain untouched."
counterexample = "Deleting a group, removing content events, or removing an inert saved `r` relay tag is not remove_saved_simple_group."
lifecycle = "Encodes one bounded ReplaceableEventEdit synchronously and retains no state."
forcing_requirement = "Applications need exact id/relay removal across all selected relays without rebuilding or normalizing unrelated kind-10009 tags."
falsifier = "Saved-list edit tests fail if a matching group tag survives or an unrelated, malformed, trailing, or foreign tag changes."
symbols = ["fava_simple_groups::remove_saved_simple_group"]
crates = []

[[term]]
name = "rename_saved_simple_group"
source = "fava"
nearest_nostr = "Kind-10009 group tag display name"
meaning = "A pure edit constructor setting one display name for every selected SimpleGroup id/relay entry."
owner = "fava-simple-groups"
distinction = "It retains the first matching position, removes later matches, appends absent selected relays, and preserves unused trailing values on retained tags."
counterexample = "Changing the opaque group id, relay selection, group metadata name, or unrelated saved entries is not rename_saved_simple_group."
lifecycle = "Encodes one bounded ReplaceableEventEdit synchronously and retains no state."
forcing_requirement = "Applications need consistent display-name edits across a multi-relay saved group without conflating display data with group identity."
falsifier = "Saved-list edit tests fail if any selected id/relay entry has the old name, absent relays are not appended, duplicates survive, or trailing values disappear."
symbols = ["fava_simple_groups::rename_saved_simple_group"]
crates = []

[[term]]
name = "save_relay"
source = "fava"
nearest_nostr = "Kind-10009 r tag"
meaning = "A pure edit constructor ensuring one inert saved `r` tag for an exact RelayUrl."
owner = "fava-simple-groups"
distinction = "It keeps the first exact relay match, removes later matches, and appends an absent relay without granting routing or acquisition authority."
counterexample = "Adding a relay to SimpleGroup, Query acquisition, or an explicit publication route is not save_relay."
lifecycle = "Encodes one bounded ReplaceableEventEdit synchronously and retains no state."
forcing_requirement = "Applications need saved relay preferences to remain inert kind-10009 data while duplicates normalize predictably."
falsifier = "Saved-list edit tests fail if duplicate exact r tags survive, an absent relay is not appended, or unrelated tags or content change."
symbols = ["fava_simple_groups::save_relay"]
crates = []

[[term]]
name = "remove_saved_relay"
source = "fava"
nearest_nostr = "Kind-10009 r tag"
meaning = "A pure edit constructor removing every inert saved `r` tag for an exact RelayUrl."
owner = "fava-simple-groups"
distinction = "It removes only exact saved relay entries and does not change SimpleGroup relay selection, query acquisition, or publication routing."
counterexample = "Removing a group entry or disconnecting a live relay session is not remove_saved_relay."
lifecycle = "Encodes one bounded ReplaceableEventEdit synchronously and retains no state."
forcing_requirement = "Applications need exact relay-preference removal without parsing or rewriting unrelated kind-10009 material."
falsifier = "Saved-list edit tests fail if an exact r tag survives or any nonmatching, malformed, trailing, foreign, or content value changes."
symbols = ["fava_simple_groups::remove_saved_relay"]
crates = []

[[term]]
name = "saved_group_list_materializer"
source = "fava"
nearest_nostr = "Kind-10009 Simple Group List replacement"
meaning = "A constructor returning a fresh stateless kind-10009 materializer that verifies and deterministically edits the exact qualified signed or unsigned source."
owner = "fava-simple-groups"
distinction = "It exposes only the neutral contract, accepts this crate's private edit bytes, verifies bounded source structure and ids/signatures, requires matching author/kind and a later timestamp, preserves opaque content and unrelated tags, and reports WriteIntentError."
counterexample = "A public materializer type, signer, author selector, router, store, publisher, or retained service is not saved_group_list_materializer."
lifecycle = "Allocates one fresh Arc-backed stateless implementation; Fava invokes it synchronously for rematerialization and may then drop it."
forcing_requirement = "Neutral replaceable edits require a concrete codec owner without exposing private edit representation or a parallel provider contract."
falsifier = "Materializer tests fail if the returned contract has another kind, accepts an unrelated edit or mismatched/unverified source, permits a non-advancing timestamp, loses source content/tags, or retains mutable state across calls."
symbols = ["fava_simple_groups::saved_group_list_materializer"]
crates = []

[[term]]
name = "saved_group_lists"
source = "fava"
nearest_nostr = "Kind-10009 Simple Group List event"
meaning = "A query constructor selecting kind 10009 for the exact supplied author set, with empty input matching nothing."
owner = "fava-simple-groups"
distinction = "It returns an ordinary Query, delegates author bounds and refusal to fava-query, and owns no observation or replacement selection."
counterexample = "A decoded SavedGroupList, relay-specific query, observation handle, or unconstrained kind-10009 filter is not saved_group_lists."
lifecycle = "Consumes the finite author iterator synchronously into one Query or returns the exact QueryError; it retains no work."
forcing_requirement = "Applications need one query for both saved-group and saved-relay entries without synonym functions or a match-all empty-author accident."
falsifier = "Query tests fail if kind 10009 is absent, authors change, empty input broadens the query, or QueryError is translated."
symbols = ["fava_simple_groups::saved_group_lists"]
crates = []

[[term]]
name = "create_group"
source = "nostr"
protocol = "NIP-29"
meaning = "A constructor for an unsigned kind-9007 request to create the exact simple group identified by its h tag."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::create_group"]
crates = []

[[term]]
name = "delete_event"
source = "nostr"
protocol = "NIP-29"
meaning = "A constructor for an unsigned kind-9005 request to delete one exact event from one simple group."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::delete_event"]
crates = []

[[term]]
name = "delete_group"
source = "nostr"
protocol = "NIP-29"
meaning = "A constructor for an unsigned kind-9008 request to delete one exact simple group."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::delete_group"]
crates = []

[[term]]
name = "edit_metadata"
source = "nostr"
protocol = "NIP-29"
meaning = "A constructor for an unsigned kind-9002 request replacing the supplied metadata fields of one exact simple group."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::edit_metadata"]
crates = []

[[term]]
name = "invite"
source = "nostr"
protocol = "NIP-29"
meaning = "A constructor for an unsigned kind-9009 invitation naming one simple group, invitee, and relay."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::invite"]
crates = []

[[term]]
name = "join_request"
source = "nostr"
protocol = "NIP-29"
meaning = "A constructor for an unsigned kind-9021 request by its author to join one exact simple group."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::join_request"]
crates = []

[[term]]
name = "leave_group"
source = "nostr"
protocol = "NIP-29"
meaning = "A constructor for an unsigned kind-9022 request by its author to leave one exact simple group."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::leave_group"]
crates = []

[[term]]
name = "put_user"
source = "nostr"
protocol = "NIP-29"
meaning = "A constructor for an unsigned kind-9000 request assigning one user and supplied roles to one exact simple group."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::put_user"]
crates = []

[[term]]
name = "remove_user"
source = "nostr"
protocol = "NIP-29"
meaning = "A constructor for an unsigned kind-9001 request removing one user from one exact simple group."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::remove_user"]
crates = []

[[term]]
name = "RelayList"
source = "nostr"
protocol = "NIP-65"
meaning = "A replaceable kind 10002 event declaring an author's read and write relays."
owner = "fava-nip65"
symbols = ["fava_nip65::RelayList", "fava_nip65::RelayListError"]
crates = ["fava-nip65"]
spec_crates = ["fava-nip65"]

[[term]]
name = "Repost"
source = "nostr"
protocol = "NIP-18"
meaning = "A Nostr event reposting another Event."
owner = "fava-nip18"
symbols = []
crates = []
spec_crates = ["fava-nip18"]

[[term]]
name = "Comment"
source = "nostr"
protocol = "NIP-22"
meaning = "A Nostr comment event with explicit root, parent, and scoped references."
owner = "fava-nip22"
symbols = []
crates = []
spec_crates = ["fava-nip22"]

[[term]]
name = "Reaction"
source = "nostr"
protocol = "NIP-25"
meaning = "A Nostr reaction event referring to another Event."
owner = "fava-nip25"
symbols = []
crates = []
spec_crates = ["fava-nip25"]

# Fava vocabulary. Each term documents why protocol vocabulary alone is not
# enough.

[[term]]
name = "Fava"
source = "fava"
meaning = "One assembled Fava engine instance and its public facade."
owner = "fava"
nearest_nostr = "none"
distinction = "Nostr defines protocol messages, not an application's assembled client lifecycle."
symbols = [
    "fava::Fava",
    "fava::FavaBuilder",
    "fava::BuildError",
    "fava_runtime::Runtime",
    "fava_runtime::RuntimeConfig",
    "fava_runtime::RuntimeError",
    "fava_runtime::TaskName",
    "fava_runtime::TaskHandle",
    "fava_runtime::TaskFailure",
    "fava_runtime::OperationName",
    "fava_runtime::ProviderCompletion",
    "fava_runtime::CancellationToken",
    "fava_runtime::Sender",
    "fava_runtime::Receiver",
    "fava_runtime::SendRefused",
    "fava_runtime::SendRefusal",
]
crates = ["fava", "fava-runtime"]
spec_crates = ["fava-ffi", "fava-standard"]

[[term]]
name = "Query"
source = "fava"
meaning = "A valid declarative request for events with acquisition, relay access, freshness, ordering, and result rules."
owner = "fava-query"
nearest_nostr = "Filter"
distinction = "A Filter is relay wire demand; a Query also governs local sources, relay acquisition, evidence authority, ordering, and bounds."
symbols = [
    "fava_query::FilterSelection",
    "fava_query::QueryAcquisition",
    "fava_query::ResultAuthority",
    "fava_query::QuerySourcePolicy",
    "fava_query::Freshness",
    "fava_query::QueryOrdering",
    "fava_query::Query",
    "fava_query::QueryBounds",
    "fava_query::QueryBranchId",
    "fava_query::QueryError",
]
crates = ["fava-query"]

[[term]]
name = "QuerySource"
source = "fava"
meaning = "A local owner that supplies one complete current event contribution and later replacement snapshots for a Query."
owner = "fava-query"
nearest_nostr = "Subscription"
distinction = "A Nostr Subscription is relay work; a QuerySource exposes independently owned local state such as an event cache or write store."
symbols = [
    "fava_query::SourceKind",
    "fava_query::SourceRevision",
    "fava_query::SourceEvent",
    "fava_query::SourceSnapshot",
    "fava_query::SourceStatus",
    "fava_query::SourceTerminationCause",
    "fava_query::SourceChangeFuture",
    "fava_query::SourceChanges",
    "fava_query::OpenedQuerySource",
    "fava_query::QuerySource",
    "fava_query::QuerySourceError",
    "fava_query::QuerySourceClosed",
]
crates = []

[[term]]
name = "EventRecord"
source = "fava"
meaning = "An event together with the relay and local-publication evidence Fava currently knows about it."
owner = "fava-query"
nearest_nostr = "Event"
distinction = "An Event is the protocol object; EventRecord adds local evidence without changing the event."
symbols = ["fava_query::EventRecord"]
crates = []

[[term]]
name = "QuerySnapshot"
source = "fava"
meaning = "One complete immutable answer delivered for an open Query revision, including typed result and live-retention shortfalls."
owner = "fava-query"
nearest_nostr = "EVENT and EOSE messages"
distinction = "Relay messages are source facts; a QuerySnapshot is the merged application-visible current answer."
symbols = [
    "fava_query::QueryEvidence",
    "fava_query::SourceEvidence",
    "fava_query::RelayQueryEvidence",
    "fava_query::RelaySourceState",
    "fava_query::RelayDeadline",
    "fava_query::RelayWithdrawal",
    "fava_query::RelayShortfall",
    "fava_query::AuthenticationState",
    "fava_query::RouteOrigin",
    "fava_query::DesiredPlanEvidence",
    "fava_query::QueryShortfall",
    "fava_query::BoundedText",
    "fava_query::QueryRevision",
    "fava_query::QuerySnapshot",
]
crates = []

[[term]]
name = "QueryEvaluator"
source = "fava"
meaning = "The replaceable algorithm that evaluates one Query over complete current QuerySource snapshots."
owner = "fava-query"
nearest_nostr = "Filter matching"
distinction = "Nostr filter matching covers relay events; query evaluation also merges independent local sources and evidence."
symbols = [
    "fava_query::QueryEvaluator",
    "fava_query::QueryEvaluationError",
    "fava_query_standard::StandardQueryEvaluator",
]
crates = ["fava-query-standard"]

[[term]]
name = "Observation"
source = "fava"
meaning = "The lifecycle and application handle of one opened Query."
owner = "fava-observe"
nearest_nostr = "Subscription"
distinction = "An Observation merges local and relay sources and delivers current QuerySnapshots; it is not one relay subscription."
symbols = [
    "fava_observe::Observer",
    "fava_observe::Observation",
    "fava_observe::ObserveError",
    "fava_observe::ObservationClosed",
    "fava_query::ObservationId",
    "fava_query::OperationGeneration",
]
crates = ["fava-observe"]

[[term]]
name = "RelayAccess"
source = "fava"
meaning = "The stable authorization identity under which relay work occurs."
owner = "fava-relay"
nearest_nostr = "NIP-42 authentication"
distinction = "NIP-42 defines authentication messages; RelayAccess identifies the isolated authorization used by queries and relay sessions."
symbols = ["fava_relay::RelayAccess"]
crates = ["fava-relay"]

[[term]]
name = "RelaySessionKey"
source = "fava"
meaning = "The stable logical pair of one relay URL and one RelayAccess authority."
owner = "fava-relay"
nearest_nostr = "RelayUrl and NIP-42 authentication"
distinction = "A URL alone cannot distinguish public and authenticated work at the same endpoint."
symbols = ["fava_relay::RelaySessionKey"]
crates = []

[[term]]
name = "RelayOccurrence"
source = "fava"
meaning = "One admitted event occurrence under one exact relay-access session."
owner = "fava-state"
nearest_nostr = "EVENT message"
distinction = "An EVENT message carries an event; RelayOccurrence preserves which attributed exact session actually supplied it and when it was admitted locally."
symbols = ["fava_state::RelayOccurrence"]
crates = ["fava-state"]

[[term]]
name = "RelayOccurrences"
source = "fava"
meaning = "The event-id-bound finite aggregate of exact qualifying RelayOccurrence values."
owner = "fava-state"
nearest_nostr = "Repeated EVENT messages carrying one EventId"
distinction = "A free occurrence collection could combine different event ids; RelayOccurrences refuses that mismatch."
symbols = ["fava_state::RelayOccurrences"]
crates = []

[[term]]
name = "RelayEvent"
source = "fava"
meaning = "One signed Nostr event plus exactly one admitted RelayOccurrence."
owner = "fava-state"
nearest_nostr = "EVENT message"
distinction = "The wire event does not retain the exact logical session and local admission time after decoding."
symbols = ["fava_state::RelayEvent"]
crates = []

[[term]]
name = "EventCoordinate"
source = "fava"
meaning = "The identity used to choose one immutable event or the current event at a replaceable coordinate."
owner = "fava-state"
nearest_nostr = "EventId and Coordinate"
distinction = "Nostr uses EventId for immutable events and Coordinate for replaceable events; query merging needs one ordered key over both."
symbols = ["fava_state::EventCoordinate"]
crates = []

[[term]]
name = "EventStateMutation"
source = "fava"
meaning = "One atomic exact-session upsert or caused retraction decided by Nostr event-state rules."
owner = "fava-state"
nearest_nostr = "Event"
distinction = "Nostr defines events; EventStateMutation carries the local atomic contribution change an EventCache may retain, while RetractionCause preserves why an exact contribution was removed."
counterexample = "A bare event id is not an EventStateMutation because it cannot identify the exact relay-access contribution or distinguish deletion, supersession, expiration, and eviction."
lifecycle = "fava-state decides the batch from current state; the EventCache provider commits it atomically and never persists the cause beyond the commit."
forcing_requirement = "A bounded EventCache must apply a NIP-09 deletion at capacity, so it must distinguish the retractions it must always apply from the insertions it may refuse."
falsifier = "cache_lifecycle::a_full_cache_still_applies_a_deletion and event_state_model fail when a retraction carries no exact session or cause."
symbols = ["fava_state::EventStateMutation"]
crates = []

[[term]]
name = "RetractionCause"
source = "fava"
meaning = "The exact protocol or provider reason an event contribution was removed."
owner = "fava-state"
nearest_nostr = "NIP-09 deletion, NIP-40 expiration, and NIP-01 replacement"
distinction = "Absence alone cannot distinguish deletion, supersession, expiration, and provider eviction."
symbols = ["fava_state::RetractionCause"]
crates = []

[[term]]
name = "EventCache"
source = "fava"
meaning = "Optional retention for admitted atomic RelayEvent contributions and their exact-session state mutations."
owner = "fava-event-cache"
nearest_nostr = "Event"
distinction = "Nostr defines events but not client retention, eviction, persistence, or coherent local observation."
symbols = [
    "fava_event_cache::EventCache",
    "fava_event_cache::EventCacheError",
    "fava_event_cache_memory::MemoryEventCache",
]
crates = ["fava-event-cache", "fava-event-cache-memory"]
spec_crates = ["fava-event-cache-redb", "fava-event-cache-testkit"]

[[term]]
name = "Write"
source = "fava"
meaning = "An accepted local publication obligation, its application handle, and its current event and receipt facts."
owner = "fava owns the application handle; fava-write and fava-publication own the referenced custody and receipt facts"
nearest_nostr = "EVENT and OK messages"
distinction = "Nostr defines publication messages; Write identifies accepted local responsibility across signing, routing, delivery, cancellation, and restart without making network progress part of acceptance."
counterexample = "An EVENT sent to a relay without durable local custody is not a Write, and an AcceptedWrite is a store result rather than the application handle."
lifecycle = "Fava creates the handle only after fava-publication atomically accepts custody; the handle reads receipt facts while neutral owners retain and remove them."
forcing_requirement = "WRITE-004 and WRITE-005 require atomic durable acceptance and immediate query visibility, while WRITE-013 forbids routing or network progress from delaying the acceptance result."
falsifier = "The publication_door test publish_returns_after_local_acceptance must fail when the facade waits for a blocked downstream receipt before returning."
symbols = [
    "fava::Write",
    "fava::PublishError",
    "fava_write::WriteId",
    "fava_write::ReceiptId",
    "fava_write::EventValue",
    "fava_write::SignatureState",
    "fava_write::PublicationEvidence",
    "fava_write::LocalWriteEvent",
    "fava_write::InvalidEventValue",
    "fava_write::Receipt",
    "fava_write::ReceiptOutcome",
    "fava_write::RelayDeliveryOutcome",
    "fava_publication::Publication",
    "fava_publication::PublicationError",
]
crates = ["fava-write", "fava-publication"]
spec_crates = ["fava-publication"]

[[term]]
name = "all_terminal"
source = "fava"
meaning = "A receipt predicate that accepts only after routing settles and every currently desired destination has an exact terminal fact."
owner = "fava"
nearest_nostr = "NIP-01 OK response"
distinction = "Terminal destination evidence includes acknowledgement, rejection, exhausted delivery, and ambiguous handoff, so it proves completion rather than publication success."
counterexample = "A settled receipt whose desired destinations are all rejected satisfies all_terminal even though no relay acknowledged the event."
lifecycle = "The stateless predicate reads one current Receipt revision and retains no work or evidence."
forcing_requirement = "Applications must be able to await complete attributable destination evidence without misrepresenting rejection or ambiguity as acknowledgement."
falsifier = "write_settlement::all_terminal_and_custom_predicates_receive_complete_mixed_revisions fails if a mixed acknowledged, rejected, and ambiguous receipt does not satisfy all_terminal."
symbols = ["fava::all_terminal"]
crates = []

[[term]]
name = "all_acknowledged"
source = "fava"
meaning = "A receipt predicate that accepts only when a nonempty settled current route has exact acknowledgement evidence for every desired destination."
owner = "fava"
nearest_nostr = "NIP-01 OK response"
distinction = "It evaluates exact current desired destination identity, not aggregate historical acknowledgement counts or generic terminal completion."
counterexample = "An acknowledgement retained for a withdrawn destination cannot compensate for rejection at a destination still desired by the current route."
lifecycle = "The stateless predicate reads one current Receipt revision and retains no work or evidence."
forcing_requirement = "A caller claiming complete publication success must distinguish every-current-destination acknowledgement from a terminal, empty, or historically acknowledged receipt."
falsifier = "write_settlement::withdrawn_acknowledgement_cannot_mask_current_rejection fails if aggregate acknowledgement counts satisfy a rejected current destination."
symbols = ["fava::all_acknowledged"]
crates = []

[[term]]
name = "WriteStore"
source = "fava"
meaning = "The authority for accepted local publication obligations, receipts, current materialized events, coordinate-bound bounded reservations, and exact-generation ordered edit custody whose complete applied sequence gates each successor."
owner = "fava-write-store"
nearest_nostr = "EVENT and OK messages"
distinction = "Relay messages do not define durable local custody, ordered same-coordinate edit composition, or recovery of publication obligations."
symbols = [
    "fava_write_store::AcceptedWrite",
    "fava_write_store::WriteStore",
    "fava_write_store::WriteStoreError",
    "fava_write_store_memory::MemoryWriteStore",
    "fava_write_store_redb::RedbWriteStore",
]
crates = [
    "fava-write-store",
    "fava-write-store-memory",
    "fava-write-store-redb",
]
spec_crates = ["fava-write-store-redb", "fava-write-store-testkit"]

[[term]]
name = "PublishAs"
source = "fava"
meaning = "An inert borrowed application scope that selects the exact author for one replaceable-event edit publication."
owner = "fava facade"
nearest_nostr = "Event pubkey and signature"
distinction = "Nostr events carry an author after construction; PublishAs exists before an edit payload is named, accepts only ReplaceableEventEdit, and owns no signing or publication lifecycle."
counterexample = "Dropping fava.by(author) creates no write, receipt, provider call, or relay work; an accepted Write always has durable custody."
lifecycle = "Fava::by creates the borrowed handle; PublishAs::publish or PublishAs::to consumes it, otherwise it is dropped without effects; it is never stored or serialized."
forcing_requirement = "WRITE-003 forbids a parallel author field from contradicting event identity, so unsigned and presigned events must be excluded from signer scope by the Rust type surface."
falsifier = "The PublishAs compile-fail doctests must fail if signer-scoped publication accepts UnsignedEvent or Event."
symbols = ["fava::PublishAs"]
crates = []

[[term]]
name = "PublishTo"
source = "fava"
meaning = "An inert borrowed application scope that validates and selects an exact ordered explicit relay route before one publication payload is named."
owner = "fava facade"
nearest_nostr = "Relay URL"
distinction = "WriteRouting is the durable routing fact of an accepted intent; PublishTo is a pre-payload narrowing that owns no custody, receipt, route acquisition, or transport work."
counterexample = "Dropping fava.to([relay]) leaves the write store and every provider untouched; accepting through the handle creates a Write whose receipt retains the selected route."
lifecycle = "Fava::to validates and creates the borrowed handle; PublishTo::publish or PublishTo::by consumes it, otherwise it is dropped without effects; it is never stored or serialized."
forcing_requirement = "WRITE-011 requires ordinary automatic routing plus an exact explicit mode without placing routing syntax on every publish call or introducing an options bag."
falsifier = "The publication_scopes_are_inert_before_valid_payload test must observe zero custody and provider effects when a PublishTo is dropped or its relay input is invalid."
symbols = ["fava::PublishTo"]
crates = []

[[term]]
name = "WriteIntent"
source = "fava"
meaning = "A neutral checked request carrying a publication payload and durable routing fact between publication and write-store contracts."
owner = "fava-write"
nearest_nostr = "EVENT message"
distinction = "An EVENT message contains a signed event; WriteIntent coordinates neutral providers after the application has used Fava's payload-oriented publish door, and is not an application-facing facade input."
counterexample = "An application calling fava.publish(edit) receives Write without constructing WriteIntent; WriteRouting remains facade-visible only because Receipt::routing exposes that durable fact."
lifecycle = "The fava facade constructs WriteIntent privately after resolving its publication scope and any EventBuilder-carried route; fava-publication and write-store providers consume and reconstruct it until the accepted obligation ends."
forcing_requirement = "Publication, rematerialization, preview, and recovery require one provider-neutral payload and route contract without exposing provider custody vocabulary as an alternate application door."
falsifier = "The facade_has_no_write_intent_compatibility_door test must fail if fava re-exports WriteIntent or WritePayload or accepts WriteIntent through Fava::publish."
symbols = [
    "fava_write::WriteIntent",
    "fava_write::WritePayload",
    "fava_write::WriteRouting",
]
crates = []
spec_symbols = ["WriteIntent"]

[[term]]
name = "WriteIntentError"
source = "fava"
meaning = "The neutral refusal algebra returned while constructing a write intent or materializing its unsigned event body, preserving raw route input, distinct destinations, tag cardinality, serialized bytes, encoding, verification, and expiry as distinct concerns."
owner = "fava-write"
nearest_nostr = "NIP-01 event construction and verification, NIP-40 expiration, and client-selected relay destinations"
distinction = "EventBuildError is the generic EventBuilder refusal; WriteIntentError is the outward error contract returned by complete intent validation and ReplaceableEventMaterializer before or after custody, but it owns no publication lifecycle attribution and does not survive the current publication boundary."
counterexample = "A route with 1,025 duplicate relay occurrences returns TooManyRawExplicitRelays before normalization, while 257 distinct relays returns TooManyExplicitRelays; duplicates within the raw bound do not consume destination capacity."
lifecycle = "Constructed during pre-custody intent validation or converted from EventBuildError during initial or post-custody materialization. Intent validation can return it directly; current fava-publication converts a materializer result to PublicationError::Routing(error.to_string()), so the typed value does not survive custody-side publication. Issue 0025 owns structured lifecycle attribution."
forcing_requirement = "EventBuilder distinguishes tag cardinality, serialized bytes, and encoding, and every materializer must expose those distinctions exactly at its return boundary without duplicating conversion policy; current publication erasure is explicitly outside this contract."
falsifier = "Mutate only the EventBuildError::TooManyTags conversion to TooLarge and run the fava-write owner test, all three shipped materializer tests, four internal fixture tests, and the external semantic-capability conversion test; every exact TooManyTags assertion must fail."
symbols = ["fava_write::WriteIntentError"]
crates = []

[[term]]
name = "ReplaceableEventEdit"
source = "fava"
meaning = "A durable protocol-owned change carrying kind and optional identifier, but no author, encoding version, or stored inverse; accepted custody applies its bounded ordered sequence to the newest event at the resolved-author coordinate or to defined empty state."
owner = "fava-write"
nearest_nostr = "replaceable Event and Coordinate"
distinction = "Nostr defines replacement events; ReplaceableEventEdit preserves one intended change and its position in one active coordinate's ordered sequence while the accepted write separately freezes the author used for every materialization generation."
symbols = ["fava_write::ReplaceableEventEdit"]
crates = []
spec_symbols = ["ReplaceableEventEdit"]

[[term]]
name = "ReplaceableEventMaterializer"
source = "fava"
meaning = "A replaceable protocol provider that applies one durable semantic edit with the accepted write's frozen author to qualified signed or unsigned source or defined empty state and produces an unsigned replacement event; universal publication invokes the exact refreshed durable sequence in order and attests that complete sequence at successor installation."
owner = "fava-write"
nearest_nostr = "replaceable Event and Coordinate"
distinction = "Nostr defines replacement events but not a client-side provider contract for replaying durable ordered intentions across changing source state without owning custody, queues, or publication."
symbols = ["fava_write::ReplaceableEventMaterializer"]
crates = []
spec_symbols = ["ReplaceableEventMaterializer"]

[[term]]
name = "MaterializationId"
source = "fava"
meaning = "The exact identity of one immutable event materialization generation under a stable accepted write and receipt, including generations produced by appending a pre-signature same-coordinate edit; live signing, routing, and replay cannot advance across a failed generation-qualified custody refresh."
owner = "fava-write"
nearest_nostr = "EventId"
distinction = "An EventId identifies one completed event body; MaterializationId correlates every effect and late completion to the exact generation that authorized it."
symbols = ["fava_write::MaterializationId"]
crates = []
spec_symbols = ["MaterializationId"]

[[term]]
name = "UnsignedEvent"
source = "fava"
meaning = "A complete Nostr event body and id that lacks only its signature."
owner = "fava-write"
nearest_nostr = "Event"
distinction = "An Event contains a valid signature; an UnsignedEvent is the exact body presented to a signer."
symbols = []
crates = []
spec_symbols = ["UnsignedEvent"]

[[term]]
name = "EventBuilder"
source = "fava"
meaning = "A generic constructor for an UnsignedEvent from author, kind, content, timestamp, validated Tags, and bounded neutral local publication routing."
owner = "fava-write"
nearest_nostr = "Event"
distinction = "Nostr defines Event fields but not local delivery intent; EventBuilder provides checked construction and route accumulation without interpreting any event kind. Its route is neither serialized nor signed."
lifecycle = "An application or protocol crate composes fields, tags, and an optional local route; each route addition consumes a finite owned vector and increments a transient raw-input count before normalization; Fava consumes the event and normalized route into WriteIntent."
forcing_requirement = "One fluent concrete builder must retain a selected route, refuse cumulative raw routing work before normalization, and keep duplicate occurrences distinct from destination capacity without learning protocol group meaning."
falsifier = "Builder and facade tests fail if raw route inputs can exceed 1,024 occurrences, duplicates consume the 256-destination capacity, event-only construction drops an attached route, or Fava mishandles the attached route."
symbols = [
    "fava_write::EventBuilder",
]
crates = []
spec_symbols = ["EventBuilder"]

[[term]]
name = "EventBuildError"
source = "fava"
meaning = "The generic checked-event construction refusal that distinguishes tag cardinality, serialized event bytes, and exact encoding failure."
owner = "fava-write's EventBuilder"
nearest_nostr = "NIP-01 event serialization and tag array validity"
distinction = "WriteIntentError is the neutral outward materializer and intent-validation error contract; EventBuildError names only why EventBuilder could not produce one bounded UnsignedEvent and owns no publication attribution."
counterexample = "Building an event with 2,001 tags returns EventBuildError::TooManyTags { actual: 2001, maximum: 2000 }; that count is neither serialized bytes nor an encoding string."
lifecycle = "Created by EventBuilder after fields and tag order are fixed, either before custody or inside initial or post-custody materialization, then returned directly or converted immediately to WriteIntentError. Current fava-publication subsequently erases a materializer result to PublicationError::Routing(error.to_string()); neither typed error enters WriteStore."
forcing_requirement = "The global boundedness gate and replaceable materializer contract require one protocol-agnostic constructor whose three refusal categories remain exact through the EventBuildError-to-WriteIntentError caller boundary; they make no typed-survival promise beyond current publication."
falsifier = "Remove or mis-map the tag-count branch and run cargo test -p fava-write --test event_builder plus the shipped, internal, Redb restart, and external conversion falsifiers; the 2,001-tag assertions must fail independently."
symbols = ["fava_write::EventBuildError"]
crates = []

[[term]]
name = "RouteRequest"
source = "fava"
meaning = "The query or event facts for which automatic relay destinations are requested."
owner = "fava-routing"
nearest_nostr = "RelayUrl"
distinction = "Nostr identifies relays but does not define client-side relay selection for query or publication work."
symbols = ["fava_routing::RouteRequest"]
crates = []
spec_symbols = ["RouteRequest"]

[[term]]
name = "RouteTarget"
source = "fava"
meaning = "One author, recipient, referenced event, coordinate, or whole request whose relay coverage is tracked."
owner = "fava-routing"
nearest_nostr = "PublicKey, EventId, and Coordinate"
distinction = "Protocol identifiers name Nostr objects; RouteTarget identifies the object for which a router claims relay coverage."
symbols = ["fava_routing::RouteTarget"]
crates = []
spec_symbols = ["RouteTarget"]

[[term]]
name = "Router"
source = "fava"
meaning = "One independently selectable policy that contributes relay destinations for automatic routing."
owner = "fava-routing"
nearest_nostr = "RelayUrl"
distinction = "Nostr defines relay endpoints but does not define a client's destination-selection policy or its lifecycle."
symbols = [
    "fava_routing::Router",
    "fava_routing::RouterSession",
    "fava_routing::RouterError",
    "fava_router_app_relays::AppRelayRouter",
    "fava_router_fallback_relays::FallbackRelayRouter",
    "fava_router_hints::HintRouter",
    "fava_router_outbox::OutboxRouter",
    "fava_router_testkit::DelayedRouter",
]
crates = [
    "fava-routing",
    "fava-router-app-relays",
    "fava-router-fallback-relays",
    "fava-router-hints",
    "fava-router-outbox",
    "fava-router-testkit",
]
spec_symbols = ["Router", "RouterSession"]
spec_crates = [
    "fava-router-app-relays",
    "fava-router-fallback-relays",
    "fava-router-hints",
    "fava-router-outbox",
    "fava-router-testkit",
    "fava-routing",
]

[[term]]
name = "RoutePlan"
source = "fava"
meaning = "The merged current relay destinations, target coverage, unresolved needs, and shortfalls for one RouteRequest."
owner = "fava-routing"
nearest_nostr = "RelayUrl"
distinction = "A relay URL names one endpoint; RoutePlan is Fava's current attributed result after ordered Router contributions."
symbols = [
    "fava_routing::CoverageState",
    "fava_routing::RouteContribution",
    "fava_routing::RouteDestination",
    "fava_routing::PlannedRelay",
    "fava_routing::RoutePlan",
]
crates = []
spec_symbols = [
    "CoverageState",
    "PlannedRelay",
    "RouteContribution",
    "RouteDestination",
    "RoutePlan",
]

[[term]]
name = "SubscriptionPlanner"
source = "fava"
meaning = "The replaceable algorithm mapping Query demand for one relay session into equivalent Nostr subscriptions."
owner = "fava-subscriptions"
nearest_nostr = "Subscription and Filter"
distinction = "Nostr defines subscriptions and filters; the planner groups application demand within relay limits while retaining exact attribution."
symbols = [
    "fava_subscriptions::AttributedSubscription",
    "fava_subscriptions::DeclaredLimit",
    "fava_subscriptions::DemandId",
    "fava_subscriptions::EoseCompleteness",
    "fava_subscriptions::InstalledSubscription",
    "fava_subscriptions::InstalledSubscriptions",
    "fava_subscriptions::PlanConformanceError",
    "fava_subscriptions::PlanRevision",
    "fava_subscriptions::PlannedSubscription",
    "fava_subscriptions::RelayDemand",
    "fava_subscriptions::RelayReadConstraints",
    "fava_subscriptions::ShortfallReason",
    "fava_subscriptions::SubscriptionAttribution",
    "fava_subscriptions::SubscriptionPlan",
    "fava_subscriptions::SubscriptionPlanError",
    "fava_subscriptions::SubscriptionPlanner",
    "fava_subscriptions::SubscriptionShortfall",
    "fava_subscriptions::WithdrawalReason",
    "fava_subscriptions::WithdrawnSubscription",
    "fava_subscriptions_standard::StandardSubscriptionPlanner",
    "fava_subscriptions_testkit::DifferentialReport",
    "fava_subscriptions_testkit::PlannerScenario",
]
crates = [
    "fava-subscriptions",
    "fava-subscriptions-no-grouping",
    "fava-subscriptions-standard",
    "fava-subscriptions-testkit",
]
spec_symbols = ["RelayDemand", "SubscriptionPlan", "SubscriptionPlanner"]
spec_crates = [
    "fava-subscriptions",
    "fava-subscriptions-no-grouping",
    "fava-subscriptions-standard",
    "fava-subscriptions-testkit",
]

[[term]]
name = "RelaySessionGeneration"
source = "fava"
meaning = "The transport-minted identity of one physical relay connection generation."
owner = "fava-transport"
nearest_nostr = "client-relay connection"
distinction = "NIP-01 identifies relay endpoints and messages but does not identify successive physical connections or prevent stale completions from crossing reconnect and reacquisition boundaries."
symbols = ["fava_transport::RelaySessionGeneration"]
crates = ["fava-transport"]

[[term]]
name = "Transport"
source = "fava"
meaning = "The replaceable owner of relay connections, byte handoff, reconnect generations, and correlated inbound messages."
owner = "fava-transport"
nearest_nostr = "client-relay connection"
distinction = "NIP-01 defines messages over a WebSocket but not Fava's connection lifecycle, bounds, or handoff evidence."
symbols = [
    "fava_transport::BoundedReason",
    "fava_transport::HandoffCorrelation",
    "fava_transport::HandoffFuture",
    "fava_transport::HandoffOutcome",
    "fava_transport::LeaseRelease",
    "fava_transport::OpenRelaySession",
    "fava_transport::RelayInbound",
    "fava_transport::RelayInboundFuture",
    "fava_transport::RelayMessageStream",
    "fava_transport::RelaySession",
    "fava_transport::RelaySessionFuture",
    "fava_transport::RelaySessionIdentity",
    "fava_transport::RelaySessionLease",
    "fava_transport::ReleaseFuture",
    "fava_transport::ReleaseOutcome",
    "fava_transport::Transport",
    "fava_transport::TransportAmbiguity",
    "fava_transport::TransportBounds",
    "fava_transport::TransportDeadlines",
    "fava_transport::TransportError",
    "fava_transport::TransportFailure",
    "fava_transport::TransportShutdownFuture",
    "fava_transport_testkit::FakeRelay",
    "fava_transport_testkit::FakeTransport",
    "fava_transport_websocket::WebSocketTransport",
]
crates = [
    "fava-transport",
    "fava-transport-testkit",
    "fava-transport-websocket",
]
spec_symbols = ["HandoffOutcome", "RelaySession", "Transport"]
spec_crates = [
    "fava-relay-lab",
    "fava-transport",
    "fava-transport-testkit",
    "fava-transport-websocket",
]

[[term]]
name = "Publisher"
source = "fava"
meaning = "The replaceable mechanism performing one publication attempt for one exact Event and relay session."
owner = "fava-publisher"
nearest_nostr = "EVENT and OK messages"
distinction = "Nostr defines publication messages; Publisher owns one correlated attempt without choosing retry or destination policy."
symbols = [
    "fava_publisher::PublishAttempt",
    "fava_publisher::PublishOutcome",
    "fava_publisher::Publisher",
    "fava_publisher_nip01::Nip01Publisher",
]
crates = ["fava-publisher", "fava-publisher-nip01"]
spec_symbols = ["PublishAttempt", "PublishOutcome", "Publisher"]
spec_crates = ["fava-publisher", "fava-publisher-nip01", "fava-publisher-testkit"]

[[term]]
name = "DeliveryPolicy"
source = "fava"
meaning = "The replaceable policy deciding whether and when current durable delivery facts authorize another publication attempt."
owner = "fava-delivery"
nearest_nostr = "EVENT and OK messages"
distinction = "Nostr defines request and acknowledgement messages but not client retry scheduling, attempt ceilings, or give-up decisions."
symbols = [
    "fava_delivery::DeliveryDecision",
    "fava_delivery::DeliveryFacts",
    "fava_delivery::DeliveryPolicy",
    "fava_delivery_standard::StandardDeliveryPolicy",
]
crates = ["fava-delivery", "fava-delivery-standard"]
spec_symbols = ["DeliveryDecision", "DeliveryFacts", "DeliveryPolicy"]
spec_crates = ["fava-delivery", "fava-delivery-standard"]

[[term]]
name = "Signer"
source = "fava"
meaning = "A replaceable provider that signs an exact UnsignedEvent for its author."
owner = "fava-signer"
nearest_nostr = "Event signature"
distinction = "Nostr defines signature verification; Signer defines the application-owned signing operation and availability lifecycle."
symbols = [
    "fava_signer::Signer",
    "fava_signer::SignerAvailability",
    "fava_signer::SignerError",
    "fava_signer_local::LocalSigner",
]
crates = ["fava-signer", "fava-signer-local"]
spec_symbols = ["Signer"]
spec_crates = [
    "fava-signer",
    "fava-signer-local",
    "fava-signer-nip46",
    "fava-signer-testkit",
]

[[term]]
name = "Session"
source = "fava"
meaning = "The runtime authority for which signer provider is currently attached to each exact author public key."
owner = "fava-session"
nearest_nostr = "Event author public key"
distinction = "A Nostr public key identifies an author and a Signer executes one provider operation; Session owns the mutable runtime attachment between those facts without owning accepted writes."
counterexample = "An accepted Alice write awaiting a signer begins signing in the same running Fava instance when Alice's signer is attached, while attaching Bob's signer leaves Alice's write parked."
lifecycle = "Created with Fava, mutated through bounded add, explicit replace, and remove operations, observed by publication, and closed during Fava shutdown; accepted writes remain independently durable."
forcing_requirement = "WRITE-007, WRITE-008, ID-001, ID-005, and ID-006 require runtime signer attachment, exact missing-signer parking and wakeup, and separation from durable write custody."
falsifier = "Accept an unsigned Alice write without a signer, attach Alice's signer without rebuilding Fava, and require the same write and receipt to continue; suppressing the exact session-change wakeup must fail."
symbols = ["fava_session::Session", "fava_session::SessionError"]
crates = ["fava-session"]

[[term]]
name = "FetchCache"
source = "fava"
meaning = "Optional storage for opaque entries whose validity and freshness remain owned by the fetching protocol crate."
owner = "fava-fetch-cache"
nearest_nostr = "none"
distinction = "Nostr event storage does not cover HTTP-fetched NIP-05 or NIP-11 data and must not determine that data's validity."
symbols = []
crates = []
spec_symbols = ["FetchCache"]
spec_crates = [
    "fava-fetch-cache",
    "fava-fetch-cache-memory",
    "fava-fetch-cache-redb",
]

[[term]]
name = "Nip05Resolver"
source = "fava"
meaning = "A replaceable provider that resolves and validates one NIP-05 identifier."
owner = "fava-nip05"
nearest_nostr = "NIP-05 identifier"
distinction = "NIP-05 defines the identifier and verification procedure; the resolver owns bounded acquisition and result delivery."
symbols = []
crates = []
spec_symbols = ["Nip05Resolver"]
spec_crates = ["fava-nip05", "fava-nip05-http"]

[[term]]
name = "RelayInformationFetcher"
source = "fava"
meaning = "A replaceable provider that fetches and validates a relay's NIP-11 information document."
owner = "fava-nip11"
nearest_nostr = "NIP-11 relay information document"
distinction = "NIP-11 defines the document; the fetcher owns bounded acquisition and result delivery."
symbols = []
crates = []
spec_symbols = ["RelayInformationFetcher"]
spec_crates = ["fava-nip11", "fava-nip11-http"]

[[term]]
name = "Authentication"
source = "fava"
meaning = "The lifecycle of accepting, refusing, or failing one NIP-42 challenge for an exact relay session."
owner = "fava-auth"
nearest_nostr = "NIP-42 AUTH"
distinction = "NIP-42 defines challenge and AUTH messages; Fava owns correlation, application policy, signing, and session-scoped outcomes."
symbols = []
crates = []
spec_crates = ["fava-auth"]

[[term]]
name = "RelayIngest"
source = "fava"
meaning = "Admission, attribution, verification, and ordered cache commit for relay-supplied Events."
owner = "fava-ingest"
nearest_nostr = "EVENT message"
distinction = "An EVENT message carries an Event; RelayIngest decides whether the message belongs to current work and may enter the EventCache."
symbols = ["fava_ingest::RelayIngestError"]
crates = ["fava-ingest"]
spec_crates = ["fava-ingest"]

[[term]]
name = "Diagnostics"
source = "fava"
meaning = "Bounded current facts and exact shortfall reasons reported by Fava owners."
owner = "fava-diagnostics"
nearest_nostr = "NOTICE and CLOSED messages"
distinction = "Relay messages report relay facts; Diagnostics also reports scoped local source, routing, signing, storage, and resource facts."
symbols = [
    "fava_diagnostics::BoundKind",
    "fava_diagnostics::Diagnostics",
    "fava_diagnostics::DiagnosticsSnapshot",
    "fava_diagnostics::DroppedFacts",
    "fava_diagnostics::LimitDiagnostic",
    "fava_diagnostics::LimitScope",
    "fava_diagnostics::LogicalDemandDiagnostic",
    "fava_diagnostics::ObservationWireBinding",
    "fava_diagnostics::ProviderDiagnostic",
    "fava_diagnostics::ProviderKind",
    "fava_diagnostics::ProviderOperation",
    "fava_diagnostics::ProviderOperationState",
    "fava_diagnostics::QueryDiagnostic",
    "fava_diagnostics::RelayDiagnostic",
    "fava_diagnostics::RelaySessionState",
    "fava_diagnostics::WireSubscriptionDiagnostic",
    "fava_diagnostics::WriteDiagnostic",
    "fava_diagnostics::WriteStall",
]
crates = ["fava-diagnostics"]
spec_crates = ["fava-diagnostics"]

[[term]]
name = "ParsedContent"
source = "fava"
meaning = "Structured values parsed from Nostr event content without rendering or acquisition behavior."
owner = "fava-content"
nearest_nostr = "Event content"
distinction = "Nostr Events carry strings; ParsedContent exposes bounded recognized structure while retaining the original content."
symbols = []
crates = []
spec_crates = ["fava-content"]

[[term]]
name = "GroupAccess"
source = "nostr"
protocol = "NIP-29"
meaning = "The open/closed access policy of a NIP-29 simple group."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::GroupAccess"]
crates = ["fava-simple-groups"]

[[term]]
name = "GroupVisibility"
source = "nostr"
protocol = "NIP-29"
meaning = "The public/private visibility policy of a NIP-29 simple group."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::GroupVisibility"]
crates = []

[[term]]
name = "MetadataEdit"
source = "nostr"
protocol = "NIP-29"
meaning = "A partial metadata update for a NIP-29 simple group, expressed as a kind-9002 management event body."
owner = "fava-simple-groups"
symbols = ["fava_simple_groups::MetadataEdit"]
crates = []
