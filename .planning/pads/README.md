# Exported pads

Snapshots of two `pad` (hash-out) sessions from the 2026-08-23 remediation,
exported so they travel with the repo. The live sessions are on the origin
machine under `~/pad/fava/` and are not otherwise reachable.

- `remediation-changelog.md` — every change made and why. The action log.
- `architecture-decision-frontier.md` — the open decisions, with recommendations.
- `*-notes.md` — the private meta-notes trail behind each, including corrections.

These are point-in-time exports. Re-export with `pad read --md` if the live
sessions move on.
