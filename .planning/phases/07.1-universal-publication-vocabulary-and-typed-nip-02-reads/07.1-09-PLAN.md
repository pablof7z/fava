---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 09
type: execute
wave: 7
depends_on: [07.1-06]
files_modified:
  - crates/fava/tests/semantic_write_failures/transient_reads.rs
  - crates/fava/tests/semantic_write_failures/validation.rs
  - crates/fava/tests/semantic_write_failures/reservation.rs
  - crates/fava/tests/semantic_write_failures/route_revision.rs
  - crates/fava/tests/semantic_write_failures/source_isolation.rs
  - falsifiers/external-semantic-capability/src/capability.rs
  - falsifiers/external-semantic-capability/src/lib.rs
  - falsifiers/external-semantic-capability/tests/public_capability.rs
  - falsifiers/external-semantic-capability/tests/support/mod.rs
  - falsifiers/external-semantic-capability/tests/support/waits.rs
  - crates/fava/src/lib.rs
  - crates/fava/tests/facade_surface.rs
  - crates/fava/BUILD.bazel
  - docs/internals/vocabulary.toml
autonomous: true
requirements: [R1, R2, R3, R4, R9]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Remaining validation/reservation/route/source-isolation tests keep exact pre-effect and current-generation behavior."
    - "The independent external semantic capability consumes the new public facade and still proves N+1 plus arbitrary future-kind publication."
    - "The facade retains the application-visible WriteRouting type required by Receipt::routing, while removing WriteIntent, WritePayload, AcceptedWrite, old publish(WriteIntent), and facade-level terminal-wait compatibility."
  prohibitions:
    - descriptor: facade_has_no_write_intent_compatibility_door
      statement: "No alias, overload, re-export, example, or public method preserves the old application-facing `WriteIntent` publication path."
  artifacts:
    - path: crates/fava/tests/facade_surface.rs
      provides: "Compile/source/API evidence that old application publication vocabulary is absent"
    - path: falsifiers/external-semantic-capability/tests/public_capability.rs
      provides: "Independent outside-workspace consumer of universal publish/Write"
  key_links:
    - from: falsifiers/external-semantic-capability/tests/public_capability.rs
      to: crates/fava/src/publication.rs
      via: "outside workspace publishes custom capability and future kind only through public facade"
      pattern: "\.publish\("
---

<objective>
Finish caller migration, prove independent N+1 consumption, and remove the old facade compatibility surface.

Purpose: Make the adopted vocabulary the sole application door while neutral internal contracts remain available to providers.
Output: Remaining migrated tests, external falsifier migration, and enforced R9 facade surface.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-SPEC.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-07-SUMMARY.md
@docs/issues/0014-publish-door-ergonomics.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate remaining semantic validation and isolation modules</name>
  <read_first>
    @crates/fava/tests/semantic_write_failures/transient_reads.rs
    @crates/fava/tests/semantic_write_failures/validation.rs
    @crates/fava/tests/semantic_write_failures/reservation.rs
    @crates/fava/tests/semantic_write_failures/route_revision.rs
    @crates/fava/tests/semantic_write_failures/source_isolation.rs
  </read_first>
  <files>crates/fava/tests/semantic_write_failures/transient_reads.rs, crates/fava/tests/semantic_write_failures/validation.rs, crates/fava/tests/semantic_write_failures/reservation.rs, crates/fava/tests/semantic_write_failures/route_revision.rs, crates/fava/tests/semantic_write_failures/source_isolation.rs</files>
  <behavior>
    - Invalid payload/scope, reservation failure, transient read, stale route revision, and source isolation remain refused or inert at their exact boundary.
  </behavior>
  <action>Run the relevant failure target red, migrate only facade call sites to payload/scopes/Write, and preserve neutral fault injection plus every zero-residue/current-identity assertion. Keep tests at the owner boundary; do not replace them with repository greps.</action>
  <acceptance_criteria>All five modules compile and pass with their original exact refusal/isolation assertions.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test semantic_write_failures</automated>
  </verify>
  <done>All remaining owner failure cases consume the new door without losing attribution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the external semantic capability and arbitrary-kind consumer</name>
  <read_first>
    @falsifiers/external-semantic-capability/src/capability.rs
    @falsifiers/external-semantic-capability/src/lib.rs
    @falsifiers/external-semantic-capability/tests/public_capability.rs
    @falsifiers/external-semantic-capability/tests/support/mod.rs
    @falsifiers/external-semantic-capability/tests/support/waits.rs
  </read_first>
  <files>falsifiers/external-semantic-capability/src/capability.rs, falsifiers/external-semantic-capability/src/lib.rs, falsifiers/external-semantic-capability/tests/public_capability.rs, falsifiers/external-semantic-capability/tests/support/mod.rs, falsifiers/external-semantic-capability/tests/support/waits.rs</files>
  <behavior>
    - Outside-workspace protocol code uses `fava.publish`/`by`/`to` and returned `Write` without normal dependencies on internal/provider crates.
    - Raw kind 50001 remains unchanged and no universal kind switch appears.
  </behavior>
  <action>Run the external workspace red for old facade calls. Migrate the public application path while retaining `fava` as its sole normal Fava dependency, exact future-kind bytes, source successor flow, and stale-completion assertions. Any test-only providers remain dev dependencies.</action>
  <acceptance_criteria>The outside workspace compiles/tests, Cargo metadata preserves its normal-dependency boundary, and arbitrary future-kind evidence stays exact.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path falsifiers/external-semantic-capability/Cargo.toml --all-targets</automated>
  </verify>
  <done>A genuinely external N+1 capability receives the universal publication vocabulary without a core change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove and falsify the old facade publication surface</name>
  <read_first>
    @crates/fava/src/lib.rs
    @crates/fava/src/publication.rs
    @docs/internals/vocabulary.toml
  </read_first>
  <files>crates/fava/src/lib.rs, crates/fava/tests/facade_surface.rs, crates/fava/BUILD.bazel, docs/internals/vocabulary.toml</files>
  <behavior>
    - Application imports of `fava::WriteIntent`, `WritePayload`, and `AcceptedWrite` fail; `fava::WriteRouting` remains available because `Receipt::routing` returns it.
    - `Fava::wait_terminal` and an old `publish(WriteIntent)` call do not exist.
    - Neutral crates still expose their internal contracts to stores/providers.
  </behavior>
  <action>Add compile-fail rustdoc/API checks and a bounded allowlist source check named `facade_has_no_write_intent_compatibility_door` before removing re-exports/methods. Retain the facade `WriteRouting` re-export because `Receipt::routing` is application-visible. Remove only facade exports of `WriteIntent`, `WritePayload`, and `AcceptedWrite`, the facade-level terminal wait, and any adapter/overload accepting application `WriteIntent`; update vocabulary so `WriteIntent` remains neutral internal cross-crate vocabulary only. Deliberately restore the old overload or one removed re-export, require the descriptor to fail for that exact symbol, restore exact bytes, and rerun.</action>
  <acceptance_criteria>Removed facade imports/calls fail, `fava::WriteRouting` and `Receipt::routing` compile for applications, neutral contract imports compile, repository allowlist finds no compatibility door, and the restore-old-overload deliberate break fails causally.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test facade_surface &amp;&amp; cargo test -p fava --doc &amp;&amp; python3 tools/check_vocabulary.py</automated>
  </verify>
  <done>R9's compatibility prohibition is wired and the new vocabulary is the only application publication door.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public symbols/functions/fields: none; this plan retains facade `WriteRouting` for `Receipt::routing` and removes only facade `WriteIntent`, `WritePayload`, `AcceptedWrite`, old `publish(WriteIntent)`, and `wait_terminal` compatibility.
- New test file: `crates/fava/tests/facade_surface.rs`.
- Migrated files: five remaining failure modules and five external-falsifier modules.

Mechanical-migration cohesion rationale: Tasks 1 and 2 each update one bounded five-file caller corpus with no new behavior, and Task 3 performs the single facade-deletion gate those migrations unblock. Each task is a focused commit with its own target; separating them into more plans would duplicate the same deletion dependency without reducing per-task context or file ownership.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External capability -> facade | An outside consumer must not gain private/provider access or protocol-specific privilege. |
| Public API inventory -> callers | Compatibility aliases could preserve contradictory ownership indefinitely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-071-23 | Elevation of privilege | external capability dependency graph | high | mitigate | Cargo metadata asserts `fava` is the sole normal Fava dependency. |
| T-071-24 | Tampering | facade compatibility surface | high | mitigate | Compile-fail imports plus bounded allowlist and restore-overload mutation. |
</threat_model>

<verification>
Run failure, external workspace, facade surface, rustdoc and vocabulary suites; inspect the compatibility deliberate-break record.
</verification>

<success_criteria>
No application compatibility path bypasses the adopted universal door, while provider-neutral intent vocabulary remains intact.
</success_criteria>

<output>
Create `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-09-SUMMARY.md` when done.
</output>
