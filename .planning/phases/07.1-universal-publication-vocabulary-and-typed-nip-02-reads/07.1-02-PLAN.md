---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 02
type: execute
wave: 2
depends_on: [07.1-01]
files_modified:
  - crates/fava/src/publication.rs
  - crates/fava/src/lib.rs
  - crates/fava/tests/publication_scopes.rs
  - crates/fava/BUILD.bazel
  - docs/internals/vocabulary.toml
  - crates/fava-write/src/lib.rs
  - crates/fava-write/src/routing.rs
  - crates/fava-write-store-memory/src/model.rs
  - crates/fava-write-store-redb/src/lifecycle.rs
  - crates/fava-write-store-redb/src/validation.rs
  - crates/fava-write-store-redb/tests/semantic_write_store/recovery.rs
autonomous: true
requirements: [R2, R4, R9]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Edge 3: `by().to()` and `to().by()` both compile and signer scope remains edit-only."
    - "Edge 4: an empty explicit route is refused before any instrumented custody, provider, or acquisition count changes."
    - "Edge 5: `[r1, r2, r1]` normalizes to `[r1, r2]` in caller first-occurrence order."
    - "Edge 6: equivalent duplicate relay identities create one durable lane and one attempt."
    - "Edge 7: concurrent scope handles freeze independent author and route selections at their own acceptance boundaries."
  prohibitions:
    - descriptor: publication_scopes_are_inert_before_valid_payload
      statement: "Invalid or dropped scopes cannot create custody, receipts, provider calls, route acquisition, or transport work."
  artifacts:
    - path: crates/fava/src/publication.rs
      provides: "Borrowed edit-only PublishAs and payload-general PublishTo typestate"
      exports: [PublishAs, PublishTo]
    - path: crates/fava/tests/publication_scopes.rs
      provides: "Compile/runtime scope, ordering, inertness, bound, and concurrency evidence"
    - path: crates/fava-write-store-redb/tests/semantic_write_store/recovery.rs
      provides: "Ordered explicit-route restart evidence"
  key_links:
    - from: crates/fava/src/publication.rs
      to: crates/fava-write/src/lib.rs
      via: "validated ordered explicit route becomes internal WriteRouting::Explicit"
      pattern: "WriteRouting::Explicit"
    - from: crates/fava-write/src/routing.rs
      to: crates/fava-write-store-redb/src/lifecycle.rs
      via: "serde-stable first-occurrence route survives custody and reopen"
      pattern: "Explicit\(relays\)"
---

<objective>
Deliver inert signer/relay publication scopes and ordered explicit-route persistence.

Purpose: Make invalid signer combinations unrepresentable and preserve the caller's exact bounded destination selection across memory and durable providers.
Output: R2 scope typestate, ordered neutral routing, compile-fail evidence, and restart proof.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-SPEC.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-RESEARCH.md
@docs/issues/0015-publish-scope-vocabulary.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red-first borrowed scope typestate through the public facade</name>
  <reversibility rating="costly">The scope types are public application vocabulary consumed by NIP-02 and simple groups; their locked shape deliberately prevents invalid signer use.</reversibility>
  <read_first>
    @crates/fava/src/publication.rs
    @crates/fava/src/lib.rs
    @docs/issues/0015-publish-scope-vocabulary.md
  </read_first>
  <files>crates/fava/src/publication.rs, crates/fava/src/lib.rs, crates/fava/tests/publication_scopes.rs, crates/fava/BUILD.bazel, docs/internals/vocabulary.toml</files>
  <behavior>
    - `Fava::by` returns `#[must_use] PublishAs`; its `publish` method accepts only `ReplaceableEventEdit`.
    - `Fava::to` returns a fallible `#[must_use] PublishTo`; both composition orders preserve scopes.
    - Dropping either scope is effect-free and signer-scoped unsigned/pre-signed publication is a compile error.
  </behavior>
  <action>Add compile-pass/runtime tests and rustdoc `compile_fail` examples first; run them red before implementation. Implement borrowed private-field handles with no lifecycle/provider fields. `PublishAs::publish` accepts the concrete edit type; `PublishTo::publish` uses the sealed payload path; both composition orders carry immutable scope selections. Validate empty and over-bound relay collections before publication. Register `PublishAs`/`PublishTo` using the approved seven-part distinctions. Deliberately move validation after acceptance and require `publication_scopes_are_inert_before_valid_payload` to detect residue, then restore exact bytes.</action>
  <acceptance_criteria>Both orders compile, signer misuse cannot compile, handles are `#[must_use]`, and empty/dropped/invalid scopes cause zero effects before and after the named deliberate break experiment.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test publication_scopes &amp;&amp; cargo test -p fava --doc &amp;&amp; python3 tools/check_vocabulary.py</automated>
  </verify>
  <done>The facade expresses the two locked axes without an options bag, compatibility overload, or runtime signer contradiction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve ordered explicit routes in the neutral model and memory custody</name>
  <read_first>
    @crates/fava-write/src/lib.rs
    @crates/fava-write-store-memory/src/model.rs
  </read_first>
  <files>crates/fava-write/src/lib.rs, crates/fava-write/src/routing.rs, crates/fava-write-store-memory/src/model.rs</files>
  <behavior>
    - Explicit routes retain normalized first-occurrence relay order in `Receipt::routing` in neutral and memory custody.
    - Destination maps derive one lane per relay identity without redefining caller route order.
    - Empty and more than 256 normalized destinations refuse before custody without truncation.
  </behavior>
  <action>Write the memory route-order cases red, then move routing representation/normalization from the 497-line `fava-write/src/lib.rs` into cohesive `fava-write/src/routing.rs` and re-export the unchanged public vocabulary from `lib.rs`. Replace the order-destroying `BTreeSet` payload with a serde-stable ordered neutral representation. Normalize by exact relay identity using temporary membership state, persist first occurrences, and let keyed lane maps remain independent derived facts. Keep both code files below the 500-line soft limit. Add concurrency assertions that independently created scopes do not share mutable selection state.</action>
  <acceptance_criteria>Memory exposes `[r1,r2]` after accepting `[r1,r2,r1]`; duplicate identity creates one lane; bounds refuse rather than truncate; concurrent scopes remain isolated.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-write &amp;&amp; cargo test -p fava-write-store-memory &amp;&amp; cargo test -p fava --test publication_scopes</automated>
  </verify>
  <done>Ordered explicit publication scope is a bounded neutral fact with memory custody and one-attempt deduplication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist ordered explicit routes through redb reopen</name>
  <read_first>
    @crates/fava-write-store-redb/src/lifecycle.rs
    @crates/fava-write-store-redb/src/validation.rs
    @crates/fava-write-store-redb/tests/semantic_write_store/recovery.rs
  </read_first>
  <files>crates/fava-write-store-redb/src/lifecycle.rs, crates/fava-write-store-redb/src/validation.rs, crates/fava-write-store-redb/tests/semantic_write_store/recovery.rs</files>
  <behavior>
    - Redb reopen returns the exact normalized first-occurrence route order and one lane per identity.
    - Unsound stored route shapes receive an exact typed/schema refusal rather than silent reordering.
  </behavior>
  <action>Write the redb route-order/restart cases red, then update reconstruction validation for the ordered neutral representation. Change schema compatibility only if old stored values cannot be decoded soundly; test any refusal exactly and preserve existing lifecycle/recovery assertions.</action>
  <acceptance_criteria>Redb exposes `[r1,r2]` after reopening accepted `[r1,r2,r1]`, derives one lane per identity, and handles any incompatible stored shape by the exact tested refusal.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-write-store-redb --test semantic_write_store recovery</automated>
  </verify>
  <done>Ordered explicit routing survives durable custody and reopen without semantic reordering.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public symbols: `fava::PublishAs<'a>`, `fava::PublishTo<'a>`, `Fava::by`, `Fava::to`, `PublishAs::publish`, `PublishAs::to`, `PublishTo::publish`, `PublishTo::by`.
- Changed public field shape: `fava_write::WriteRouting::Explicit` carries an ordered deduplicated relay sequence rather than a set.
- New cohesive implementation file: `crates/fava-write/src/routing.rs`; `crates/fava-write/src/lib.rs` re-exports `WriteRouting` and both remain below 500 lines.
- New tests: `publication_scopes` compile/runtime suite and redb ordered-route recovery cases.
- New file: `crates/fava/tests/publication_scopes.rs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Relay input -> scope normalization | Untrusted URLs and unbounded duplicates become durable route facts. |
| Scope value -> custody | A dropped or invalid partial request must remain inert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-071-04 | Denial of service | relay fan-out | high | mitigate | Normalize identity and enforce empty/256 bounds before any custody/provider call. |
| T-071-05 | Tampering | signer typestate | high | mitigate | Concrete edit-only `PublishAs::publish` plus compile-fail unsigned/pre-signed examples. |
| T-071-06 | Repudiation | persisted route order | medium | mitigate | Restart test compares exact ordered route and one destination lane per identity. |
</threat_model>

<verification>
Run facade scopes, memory provider and redb reopen suites plus vocabulary checks; inspect deliberate-break restoration hashes.
</verification>

<success_criteria>
Every explicit publication scope is inert until a valid payload, bounded before effects, and durably preserves exact normalized order.
</success_criteria>

<output>
Create `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-02-SUMMARY.md` when done.
</output>
