---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 12
type: execute
wave: 9
depends_on: [07.1-11]
files_modified:
  - apps/canary/src/croissant.rs
  - apps/canary/src/croissant_tests.rs
  - apps/canary/src/croissant_nip02.rs
  - apps/canary/src/croissant_nip02_tests.rs
  - apps/canary/src/lib.rs
  - apps/canary/src/main.rs
  - apps/canary/Cargo.toml
  - apps/canary/scenarios.json
  - apps/canary/README.md
  - .planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-VALIDATION.md
autonomous: true
requirements: [R1, R2, R3, R4, R5, R6, R7, R8, R9]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Edge 27: two controlled Croissant runs expose distinct scenario-seed hashes and public group coordinates and both pass; prior-run data cannot satisfy the second run."
    - "Edge 28: local observation, exact wire echo, typed decode, and settlement correlate one run's exact write, receipt, materialization, and event identities."
    - "The canary publishes a kind-9007 NIP-29 group-create through the universal door without a universal protocol switch, then completes the README NIP-02 publish-observe-decode-settle flow."
  prohibitions:
    - descriptor: universal_publication_is_kind_blind
      statement: "Universal facade/write/publication/store/runtime owners cannot branch on NIP-02 or NIP-29 event-kind meaning."
  artifacts:
    - path: apps/canary/src/croissant.rs
      provides: "Bounded kill-on-drop Croissant child supervisor with provenance and teardown"
    - path: apps/canary/src/croissant_nip02.rs
      provides: "Public-API group-bootstrap plus NIP-02 read/edit/settlement scenario"
      exports: [CroissantNip02Options, CroissantNip02Outcome, run_croissant_nip02_scenario, verify_croissant_run_pair]
    - path: apps/canary/src/croissant_nip02_tests.rs
      provides: "Automated controlled-process canary target"
  key_links:
    - from: apps/canary/src/croissant_nip02.rs
      to: crates/fava/src/publication.rs
      via: "kind-9007 and kind-3 payloads use the same public publish/to/by/Write path"
      pattern: "\.publish\("
    - from: apps/canary/src/croissant_nip02.rs
      to: crates/fava-nip02/src/contact_list.rs
      via: "relay echo enters ordinary observation then typed ContactList decoding"
      pattern: "ContactList::from_event"
---

<objective>
Prove the complete Phase 07.1 application flow twice against the controlled Croissant relay and close the full validation matrix.

Purpose: Supply independent process/wire evidence for universal kind-blind publication, immediate local NIP-02 state, exact readback, preservation and settlement.
Output: Bounded Croissant supervisor/scenario, two archived unique runs, all eight prohibition mutations, and full phase validation.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-SPEC.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-RESEARCH.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-VALIDATION.md
@apps/canary/src/relay.rs
@apps/canary/src/artifacts.rs
@apps/canary/src/wire.rs
@apps/canary/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a bounded Croissant process supervisor with exact provenance</name>
  <read_first>
    @apps/canary/src/relay.rs
    @apps/canary/src/artifacts.rs
    @/Users/pablofernandez/Work/croissant/README.md
    @/Users/pablofernandez/Work/croissant/go.mod
  </read_first>
  <files>apps/canary/src/croissant.rs, apps/canary/src/croissant_tests.rs</files>
  <behavior>
    - Supervisor resolves `/Users/pablofernandez/Work/croissant/croissant`, records SHA-256 separately from checkout HEAD, and never modifies relay source.
    - Each run uses loopback HOST, reserved PORT, fresh temporary DATAPATH, bounded stdout/stderr, readiness deadline, kill-on-drop and bounded teardown.
    - Early exit, readiness timeout, log overflow and teardown failure are typed/attributed scenario failures.
    - Raw signer/scenario seed exists only in process memory; artifacts, stdout/stderr, errors, reports and manifests retain only `scenario_seed_sha256`, public coordinates and author pubkey.
  </behavior>
  <action>Add process-contract tests red first, including a fake early-exit executable, bounded log fixtures, and a sentinel raw-seed test that scans every retained artifact and captured stdout/stderr/error. Implement a Croissant-specific supervisor rather than a false abstraction over `nostr-rs-relay`. Probe the exact local executable; if absent, build it in the Croissant checkout with its declared Go toolchain without editing source. Record executable hash, source HEAD, PID, endpoint, data/log paths, readiness and teardown facts. Hash the scenario seed in memory and retain only `scenario_seed_sha256`; never serialize or log the raw seed/private key.</action>
  <acceptance_criteria>Supervisor tests prove isolated data, loopback readiness, bounded logs/deadlines, early-exit attribution, kill-on-drop and completed teardown with binary/source provenance; sentinel raw seed/private key bytes are absent from all retained/logged surfaces.</acceptance_criteria>
  <verify>
    <automated>test -d /Users/pablofernandez/Work/croissant/.git &amp;&amp; cargo test --manifest-path apps/canary/Cargo.toml croissant::</automated>
  </verify>
  <done>A repeatable Croissant child can be started and stopped without source modification, process leakage, unbounded logs, or ambiguous provenance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the public kind-blind and NIP-02 Croissant scenario</name>
  <read_first>
    @apps/canary/src/croissant.rs
    @apps/canary/src/publication_support.rs
    @apps/canary/src/wire.rs
    @crates/fava-nip02/README.md
    @apps/canary/scenarios.json
  </read_first>
  <files>apps/canary/src/croissant_nip02.rs, apps/canary/src/croissant_nip02_tests.rs, apps/canary/src/lib.rs, apps/canary/src/main.rs, apps/canary/scenarios.json</files>
  <behavior>
    - A unique ephemeral signer/group id publishes kind 9007 with exact `h` through `to([croissant])`, then awaits `all()`.
    - A baseline kind 3 with the same `h`, `['t','nostr']`, an unknown one-column tag, nonempty content and no `p` rows observes/decodes as valid empty.
    - `follow_with` publishes through `by(author).to([croissant])`; local observation precedes acknowledgement, settlement completes, relay echo decodes exact target/hint/petname, and all foreign bytes remain.
    - The run manifest contains `scenario_seed_sha256`, public group coordinate and author pubkey but no raw seed/private key field or value.
  </behavior>
  <action>Add the scenario test red using only public `fava`/`fava-nip02` application APIs and independent wire/process witnesses. Implement the exact 11-step research sequence, including Croissant group bootstrap required for non-kind-0 events. Correlate author public key, group id, write/receipt/materialization/event ids, observation revisions, bounded raw frames, decoded contact list, terminal receipt and teardown. Add exact public `CroissantNip02Options`, `CroissantNip02Outcome`, `run_croissant_nip02_scenario`, and `verify_croissant_run_pair`; the pair verifier accepts one persistent pair root containing exactly two completed scenario manifests and checks distinct seed hashes/group/event/write/receipt identities, old-data exclusion, executable/source/artifact hashes, declared byte/count/time bounds, and closed PID/port teardown facts. Register one enabled `croissant-nip02-public-flow` scenario plus `verify-croissant-pair --runs-dir PATH` CLI dispatch. Deliberately add a kind-3 switch to a universal publication owner; require `universal_publication_is_kind_blind` plus the kind-9007 control to fail, restore exact bytes, and rerun.</action>
  <acceptance_criteria>The automated scenario passes once with exact public/local/wire/typed/settlement evidence, all foreign data preserved, no retained raw seed/private key, and the protocol-switch mutation fails causally; pair-verifier unit tests fail on reused identities, old data, missing hashes/bounds, leaked child state and raw seed fields/values.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path apps/canary/Cargo.toml croissant_nip02</automated>
  </verify>
  <done>The complete README application flow and an unrelated NIP-29 payload cross the same real-relay public lifecycle.</done>
</task>

<task type="auto">
  <name>Task 3: Execute two unique canaries and close every phase gate</name>
  <read_first>
    @apps/canary/src/croissant_nip02.rs
    @apps/canary/README.md
    @.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-VALIDATION.md
    @docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
  </read_first>
  <files>apps/canary/Cargo.toml, apps/canary/README.md, .planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-VALIDATION.md</files>
  <action>Refresh `git worktree list` and preserve every other worktree, especially dirty `/Users/pablofernandez/Work/nnn-m8`. Run the complete focused/workspace/format/Clippy/Bazel/vocabulary matrix, treating `apps/canary` truthfully as a Cargo-owned downstream workspace rather than claiming a Bazel target. Create one persistent unique pair root under `apps/canary/runs/`, generate two distinct in-memory seeds, and execute `croissant-nip02-public-flow` twice against `/Users/pablofernandez/Work/croissant/croissant` into that root; never echo or retain the raw seeds and do not delete the evidence. Run `verify-croissant-pair` over exactly those two manifests to prove different seed hashes/group/event/write/receipt ids, run-two old-data exclusion, exact foreign tags/content, complete receipt facts, executable/source/artifact hashes, declared byte/count/time bounds, and no live child/PID/port after teardown. Record the persistent pair root and verifier result in `07.1-VALIDATION.md`. Execute and record all eight named deliberate breaks from Plans 01-09/12, each with exact source hash restoration and green controls. Mark Nyquist/wave-zero complete only after every command and both manifests pass.</action>
  <acceptance_criteria>Workspace and all focused checks pass; two unique Croissant manifests independently prove the exact flow; all eight deliberate breaks fail their owning descriptors; sources restore byte-exactly; no process/worktree leakage remains.</acceptance_criteria>
  <verify>
    <automated>cargo test --workspace &amp;&amp; cargo test -p fava-nip02 --all-targets &amp;&amp; cargo test -p fava-nip02 --doc &amp;&amp; cargo test --manifest-path apps/canary/Cargo.toml --all-targets &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; bazel test //... &amp;&amp; bazel test //... --config=clippy &amp;&amp; bazel test //... --config=fmt-check &amp;&amp; mkdir -p apps/canary/runs &amp;&amp; pair_root="$(mktemp -d apps/canary/runs/phase-07.1-pair.XXXXXX)" &amp;&amp; seed_one="$(openssl rand -hex 32)" &amp;&amp; seed_two="$(openssl rand -hex 32)" &amp;&amp; test "$seed_one" != "$seed_two" &amp;&amp; cargo run --manifest-path apps/canary/Cargo.toml -- run croissant-nip02-public-flow --relay-bin /Users/pablofernandez/Work/croissant/croissant --seed "$seed_one" --runs-dir "$pair_root" &amp;&amp; cargo run --manifest-path apps/canary/Cargo.toml -- run croissant-nip02-public-flow --relay-bin /Users/pablofernandez/Work/croissant/croissant --seed "$seed_two" --runs-dir "$pair_root" &amp;&amp; unset seed_one seed_two &amp;&amp; cargo run --manifest-path apps/canary/Cargo.toml -- verify-croissant-pair --runs-dir "$pair_root"</automated>
  </verify>
  <done>Phase 07.1 has complete deterministic, mutation, workspace and two-run independent Croissant evidence with exact cleanup.</done>
</task>

</tasks>

## Artifacts this phase produces

- New canary public symbols: `CroissantNip02Options`, `CroissantNip02Outcome`, `run_croissant_nip02_scenario`, and `verify_croissant_run_pair`.
- New public fields: `CroissantNip02Options::{relay_binary, scenario_seed, runs_directory}` and `CroissantNip02Outcome::run_directory`; raw `scenario_seed` is process-memory input only.
- New canary evidence fields: executable SHA-256, Croissant source HEAD, PID/endpoint, `scenario_seed_sha256`, group id, author public key, write/receipt/materialization/event ids, observation revisions, bounded frames, decoded list, terminal receipt, teardown; no raw seed/private key field or value.
- New tests: `croissant_tests` supervisor cases and `croissant_nip02_tests` public-flow/process/wire case.
- New files: `apps/canary/src/croissant.rs`, `apps/canary/src/croissant_tests.rs`, `apps/canary/src/croissant_nip02.rs`, `apps/canary/src/croissant_nip02_tests.rs`.
- New scenario/CLI entries: `croissant-nip02-public-flow` and `verify-croissant-pair --runs-dir PATH`.
- New retained artifacts: two unique bounded canary run directories recorded in `07.1-VALIDATION.md`.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plans | Status |
|---|---|---|---|---|
| GOAL | — | README-level read/discover/edit/publish/await NIP-02 through universal payload vocabulary | 01-06, 11-12 | COVERED |
| REQ | R1 | Universal synchronous publication door | 01, 07-10, 12 | COVERED |
| REQ | R2 | Typed inert signer/relay scopes and ordered bounded routes | 02, 07-10, 12 | COVERED |
| REQ | R3 | Write-attached settlement and complete receipt evidence | 03, 07-10, 12 | COVERED |
| REQ | R4 | Universal/protocol ownership boundary | 01-06, 09, 12 | COVERED |
| REQ | R5 | Total typed ContactList row accounting | 04, 12 | COVERED |
| REQ | R6 | Ordinary contact-list/follower queries and pure projection | 05, 12 | COVERED |
| REQ | R7 | Lossless follow/unfollow/follow_with and conversions | 06, 12 | COVERED |
| REQ | R8 | Truthful README and controlled real-relay proof | 10-12 | COVERED |
| REQ | R9 | Current authoritative vocabulary/docs and no compatibility door | 01-06, 09, 11-12 | COVERED |
| RESEARCH | PUB | Borrowed facade typestate and ordered persisted routes | 01-03 | COVERED |
| RESEARCH | NIP02 | Total parser, ordinary queries, pure projection, lossless edits | 04-06 | COVERED |
| RESEARCH | MIGRATION | Existing facade/falsifier/canary callers retain behavioral proof | 07-10 | COVERED |
| RESEARCH | CANARY | Croissant supervisor, group bootstrap, exact evidence, two runs | 12 | COVERED |
| CONTEXT | publication | Sync payload door, edit-only by, general to, frozen scopes | 01-02 | COVERED |
| CONTEXT | settlement | all/at_least/custom predicates and terminal NotReached receipt | 03 | COVERED |
| CONTEXT | reads | Typed evidence, empty list, exact queries and pure projection | 04-05 | COVERED |
| CONTEXT | edits | String inputs, follow_with, first-row and foreign-byte preservation | 06 | COVERED |
| CONTEXT | proof | Engine-free ownership, README truth, compile/mutation/live evidence | 05, 09, 11-12 | COVERED |

Deferred Phase 07.1.1 simple-groups behavior, ValueSet, native wrappers, runtime capture and router-chain build refusal are excluded per CONTEXT/SPEC and are not source-audit gaps.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local test process -> Croissant child | External executable/process/filesystem/port behavior crosses the Fava test boundary. |
| Fava -> real WebSocket relay | Exact frames and acknowledgements must be independently witnessed and bounded. |
| Run one -> run two | Persistent/fixture state must not satisfy a later unique run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-071-29 | Spoofing | Croissant executable/source provenance | high | mitigate | Record executable SHA-256 and checkout HEAD separately before launch. |
| T-071-30 | Tampering | wire/readback identity | high | mitigate | Correlate exact group/event/write/receipt/materialization ids across proxy frames, observation and receipt. |
| T-071-31 | Information disclosure | ephemeral signer/logs | high | mitigate | Hash seed in memory; retain only `scenario_seed_sha256`, public coordinates and author pubkey; sentinel tests scan every artifact/stdout/stderr/error for raw seed/private-key bytes. |
| T-071-32 | Denial of service | child process and waits | high | mitigate | Loopback only, hard readiness/operation/teardown deadlines, bounded logs, kill-on-drop. |
| T-071-33 | Repudiation | two-run evidence | medium | mitigate | Distinct seed hashes/public coordinates, hashed manifests, explicit old-data exclusion and retained artifact paths. |
</threat_model>

<verification>
Run the complete phase matrix, both direct Croissant executions, all eight deliberate breaks, artifact validation, process cleanup and worktree preservation checks.
</verification>

<success_criteria>
Every R1-R9 behavior, Edge 1-28 and all eight prohibitions have current executable proof, including two exact independent real-relay runs.
</success_criteria>

<output>
Create `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-12-SUMMARY.md` when done.
</output>
