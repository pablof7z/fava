---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
verified: 2026-08-22T05:35:46Z
status: passed
score: "9/9 phase requirements verified"
plan_truths: "42/42 verified"
edges: "28/28 verified"
prohibitions: "8/8 test-enforced"
behavior_unverified: 0
overrides_applied: 0
requirements: [R1, R2, R3, R4, R5, R6, R7, R8, R9]
gaps: []
human_verification: []
evidence:
  croissant_pair: apps/canary/runs/phase-07.1-pair.9EyxBY
  fava_revision: d0b300ac5cd3ac8ecd559ffad6ff2a87dd524786
  croissant_revision: 9c4c93e84852bd9aa6824060b74c56ab2ce812c2
  croissant_executable_sha256: 6dbf7e868641f1c763bdccb4b66a53f15d419940f4af862549dc57a61ba625bb
---

# Phase 07.1 Verification Report

**Phase goal:** An application can read, discover, edit, publish, and await NIP-02 contact-list changes through the README-level typed Rust API, while the same universal `publish`/`by`/`to` vocabulary accepts every write payload without exposing `WriteIntent` ceremony.

**Verdict:** Passed. Current code, independent tests, public API, vocabulary registry, and the retained two-run Croissant evidence establish the phase goal. No blocking gap or human-only check remains.

## Goal Achievement

| Requirement | Status | Code and behavioral evidence |
|---|---|---|
| R1 — universal synchronous publication door | VERIFIED | `Fava::publish` accepts the sealed payload forms and returns application `Write`; all four `publication_door` tests pass. `Publication::accept` validates, reserves, materializes, applies the initial route, and commits before asynchronous publication starts. |
| R2 — typed signer and relay scopes | VERIFIED | `PublishAs` and `PublishTo` are borrowed, `#[must_use]`, inert scopes; signer scope exposes only edit publication. `publication_scopes` proves both composition orders, payload coverage, first-occurrence normalization, independent custody, invalid/abandoned-scope inertness, and frozen selections. Compile-fail doctests reject signer-scoped events. |
| R3 — awaitable settlement | VERIFIED | `Write::{id,receipt_id,receipt,settled}`, `all`, and `at_least` exist. All six `write_settlement` tests pass, covering zero refusal, exact first satisfying revision, custom predicates, mixed outcomes/counts, terminal `NotReached` with the complete receipt, and durable reload after lagged/redundant notifications. |
| R4 — ownership and dependency direction | VERIFIED | `fava-nip02` owns pure values, parsing, queries, projections, edits, and materializer selection only. Both architecture tests pass; source search finds no NIP-02 kind branch in universal owners. Kind 9007 and kind 3 use the same public canary door. |
| R5 — typed complete contact-list decoding | VERIFIED | `ContactList`, `Follow`, entry errors, and error types are substantive and exported. Six decoder tests cover finalized boundaries, valid empty/ordered lists, total `p`-entry accounting, invalid-entry duplicate behavior, exact petname UTF-8/presence, and invalid event-level refusal. |
| R6 — typed reads and discovery | VERIFIED | `contact_list`, `follows_of`, and `followers_of` use ordinary `Query`/`QuerySnapshot`. Four query tests cover one/many/present-empty author axes, newest-per-author replacement, exact lowercase canonical `p`, deterministic repeated projection, and independent two-hop composition. External API coverage typechecks the surface. |
| R7 — complete lossless edit helpers | VERIFIED | `follow`, `unfollow`, `follow_with`, and `materializer` are public. Tests prove key/string inputs, metadata, hostile formatting bounds, inverse operations, empty/newer-source rebasing, structural bounds, idempotence, existing-first-entry authority, duplicate removal, and exact preservation. `nip02_preserves_foreign_kind3_bytes` checks `h`, `t`, one-value, malformed `p`, extra-value, and content behavior through follow and unfollow. |
| R8 — README-driven Rust and live relay proof | VERIFIED | README contains only delivered APIs; signatures match exports and are mirrored by external API, facade, unit, doctest, and canary tests. The independently replayed Croissant pair proves public publish, local observation, signing, typed readback, exact relay echo, settlement, preservation, and teardown twice. |
| R9 — authoritative vocabulary and documentation | VERIFIED | Vocabulary entries include owners, nearest Nostr concepts, distinctions, counterexamples, lifecycles, forcing requirements, symbols, and falsifiers. Checks pass. Search finds no facade `WriteIntent` re-export/overload, `AcceptedWrite` export, old `accept_event`/`preview_write_routes`, or facade terminal-wait compatibility door. |

**Score:** 9/9 requirements; 42/42 plan-frontmatter truths.

## Edge Coverage

Every SPEC edge has executable evidence, in SPEC order.

| Edge | Requirement | Status | Owning evidence |
|---:|---|---|---|
| 1 idempotency | R1 | VERIFIED | distinct custody identities; semantic edit idempotence suites |
| 2 atomic concurrency | R1 | VERIFIED | blocked-progress local acceptance; atomic store/provider tests |
| 3 scope adjacency | R2 | VERIFIED | both composition orders; compile-fail doctests |
| 4 empty route | R2 | VERIFIED | inert invalid scope; routing bounds |
| 5 route ordering | R2 | VERIFIED | duplicate normalization; `routing_order` |
| 6 route idempotence | R2 | VERIFIED | one lane/attempt per normalized identity |
| 7 scope concurrency | R2 | VERIFIED | independent authored edits and frozen scopes |
| 8 threshold boundary | R3 | VERIFIED | zero refusal; terminal `NotReached` |
| 9 receipt precision | R3 | VERIFIED | complete mixed revisions and derived counts |
| 10 notification idempotence | R3 | VERIFIED | durable reload after lag/redundancy |
| 11 racing revisions | R3 | VERIFIED | first satisfying revision/terminality tests |
| 12 entry adjacency | R5 | VERIFIED | total `p`-entry accounting |
| 13 valid empty list | R5 | VERIFIED | valid empty decoder fixture |
| 14 encoding / UTF-8 | R5 | VERIFIED | invalid-entry errors; exact petname bytes/presence |
| 15 entry ordering | R5 | VERIFIED | source indexes and first-occurrence assertions |
| 16 query-axis adjacency | R6 | VERIFIED | one/many and exact lowercase `p` tests |
| 17 empty query/result | R6 | VERIFIED | present-empty author axis and empty snapshots |
| 18 query/projection ordering | R6 | VERIFIED | newest results and ordered projection |
| 19 repeat projection | R6 | VERIFIED | repeated equality assertions |
| 20 independent composition | R6 | VERIFIED | independent two-hop projection; no protocol state |
| 21 edit adjacency | R7 | VERIFIED | first target, duplicates, malformed, foreign fixtures |
| 22 empty/no-source edit | R7 | VERIFIED | empty and first-value materializer tests |
| 23 edit encoding/refusal | R7 | VERIFIED | strings, metadata bounds, hostile `Display`, typed errors |
| 24 edit ordering | R7 | VERIFIED | byte/position-sensitive follow/unfollow fixture |
| 25 edit idempotence | R7 | VERIFIED | repeated/adjacent edits; existing-entry authority |
| 26 deterministic rebase | R7 | VERIFIED | newer qualified-source materialization |
| 27 repeatable controlled proof | R8 | VERIFIED | two distinct retained Croissant runs |
| 28 exact completion correlation | R8 | VERIFIED | signed identities/revisions/terminal claims, exact echo, cross-run exclusion |

## Test-Tier Prohibitions

| Prohibition | Status | Wired enforcement |
|---|---|---|
| No foreign tag/content deletion, rewrite, normalization, or reordering | VERIFIED | `nip02_preserves_foreign_kind3_bytes`; canary preservation claims |
| No silent malformed/duplicate entry omission | VERIFIED | `nip02_accounts_for_every_p_entry` |
| No engine/provider lifecycle in `fava-nip02` | VERIFIED | `nip02_remains_engine_provider_free` |
| No NIP-02 meaning in universal owners | VERIFIED | kind-blind architecture test; kind-9007 control |
| No work from invalid/dropped scopes | VERIFIED | `publication_scopes_are_inert_before_valid_payload` |
| No routing/network wait before accepted return | VERIFIED | blocked-progress publication test |
| No collapsed mixed facts or hidden terminal receipt | VERIFIED | mixed count and terminal-not-reached tests |
| No old facade `WriteIntent` door | VERIFIED | facade source test, seven compile-fail doctests, repository search |

All 8/8 prohibitions have wired passing tests; none is accepted by judgment alone.

## Artifacts, Wiring, and Data Flow

| Owner | Status | Wiring/data flow |
|---|---|---|
| Facade and `publication.rs` | VERIFIED | `Fava::{publish,by,to}` -> sealed payload -> neutral intent -> `Publication::accept` -> durable store -> `Write` receipt/settlement. |
| `fava-publication` | VERIFIED | Injected signer/router/store contracts; asynchronous provider work starts only after atomic acceptance. Query visibility flows through the independent write-store source. |
| `fava-nip02` decoder/query/edit owners | VERIFIED | Public exports reach typed parsing, ordinary queries/snapshots, and neutral edits/materializer without engine/provider dependency. |
| Memory/redb semantic stores | VERIFIED | Same complete route-effect equality and atomic acceptance; redb retains terminal source provenance across reopen while excluding terminal records from recovery. |
| README/public API | VERIFIED | README claims match exported signatures and mirrored tests; no aspirational compatibility/status narration. |
| Vocabulary/specs | VERIFIED | Complete registry approvals/falsifiers and passing checker. |
| Croissant evidence owners | VERIFIED | CLI -> public facade/NIP-02 APIs -> loopback Croissant -> wire witness -> sealed artifacts -> independent pair verifier. |

No required artifact is missing, stubbed, orphaned, hollow, or compatibility-routed. Automated artifact-query false negatives for module-owned/re-exported symbols were resolved by direct source and external compile-surface inspection.

## Iteration-3 Adversarial Checks

| Risk | Status | Evidence |
|---|---|---|
| Secret-bearing refusal is not promoted or retained | VERIFIED | Owner-private sibling staging, drop cleanup, promotion only after post-manifest scan; persistent-parent injection test proves no retained run, staging entry, or injected bytes at both refusal boundaries. |
| Readiness cannot publish a transient/exited child | VERIFIED | Child checked before connection, after it, after sealed 100 ms stability, and after a second connection. Opens-port-then-exits test passed in the full canary suite and 20/20 exact reruns. |
| Terminal receipt with source survives redb restart | VERIFIED | Source-backed terminal receipt/provenance survives reopen and remains absent from active recovery. |
| Initial-route equality covers the complete effect | VERIFIED | Memory/redb negatives vary only shortfall or settled-absent coverage and prove refusal/no rewrite/no notification; equality includes outcome, revision, settlement, shortfalls, desired set, attempts, and lanes. |
| All verifier-consumed claims are signed | VERIFIED | Author-signed digest covers the canonical manifest except its seal; mutation tests cover every identity, completion, terminal, bound, and teardown field. |
| All supported secret encodings are scanned | VERIFIED | 19 classes cover seed, raw key bytes, lower/upper hex, NSEC, and NIP-21 scheme/payload combinations for both keys; direct scanner tests pass. |
| Foreign kind-3 data survives | VERIFIED | Unit and both canary runs prove `h`, `t`, one-value unknowns, malformed/unrelated entries, extra values, decomposed petname UTF-8, and legacy content. |

## Retained Croissant Pair

```text
cargo run --quiet --manifest-path apps/canary/Cargo.toml -- verify-croissant-pair --runs-dir apps/canary/runs/phase-07.1-pair.9EyxBY
verified Croissant NIP-02 pair
```

The `0700` pair root has exactly two run directories/manifests.

| Fact | Run `565144...` | Run `4d6a...` |
|---|---|---|
| seed SHA-256 | `8e082948...bd92` | `d3a6dbc8...17ac` |
| group id | `3cfa7c27...50a8` | `53b7542c...10d6` |
| group event | `19daa0f7...ef5a` | `63eda3fd...f471` |
| baseline kind-3 | `2a0d06c2...5463` | `35919c81...ba09` |
| edited kind-3 | `19c66ce4...fbe0` | `d842131e...00f5` |
| write / receipt | `...565144...:3` / same | `...4d6a...:3` / same |
| terminal | Complete; 1/1 acknowledged | Complete; 1/1 acknowledged |
| wire / bound | 7,564 / 1,048,576 bytes | 7,564 / 1,048,576 bytes |
| PID / port | 92704 / 55544 | 92738 / 55567 |

The verifier proves distinct seed/group/event/write/receipt identities and scans both directions for the other run's four data identities. Both manifests have a valid author seal, complete artifact hashes, 19 secret classes, exact typed/preservation claims, 30 s operation, 10 s readiness, 100 ms stability, 5 s teardown, 1 MiB log/wire bounds, and closed teardown. Live checks reconfirmed PIDs 92704/92738 absent, ports closed, and no phase staging directory. Croissant HEAD/executable hash still match. Unrelated pre-phase PID 75649 remains live and was excluded.

## Verification Commands

| Command | Result |
|---|---|
| `cargo test --workspace` | PASS; full workspace, including NIP-02, publication/scopes/settlement, restart/process-kill, and compile-fail doctests |
| `cargo test --manifest-path apps/canary/Cargo.toml --all-targets` | PASS; 32/32 |
| transient-readiness test, 20 consecutive exact runs | PASS; 20/20 |
| retained pair verifier | PASS |
| `cargo fmt --all -- --check`; `git diff --check` | PASS |
| vocabulary checker plus vocabulary/publication/NIP-02 Python tests | PASS; 12/12 |
| `bazel test //...` | PASS; 42/42 targets, cache-backed after the uncached Rust run |
| decision coverage | tool skipped: CONTEXT has no trackable decision checklist |

## Architecture Gates

| Gate | Status | Evidence |
|---|---|---|
| Ownership | VERIFIED | Facade owns application vocabulary; neutral contracts own lifecycle; NIP-02 owns protocol values/transformations. |
| Dependency direction | VERIFIED | Protocol crate depends on neutral query/state/write crates; universal owners do not import NIP-02. |
| Replaceability | VERIFIED | Publication/store/signer/router/publisher/materializer/query paths are injected contracts; memory/redb parity is exercised. |
| Failure isolation | VERIFIED | Validation precedes custody; release failures surface; atomic route application has no partial visibility; exact generation/source identity rejects stale work; process/log/teardown failures are typed/scoped. |
| Boundedness | VERIFIED | Relay/event/tag/evidence/projection, terminal predicate, log/wire/readiness/operation/teardown inputs and outputs have bounds or typed refusal; no truncation/silent shortfall found. |
| Behavioral proof | VERIFIED | Owner tests, external compile tests, process-kill/restart tests, causal mutations, and two real relay runs cover public promises. |

## Anti-Patterns and Repository State

No unreferenced `TBD`/`FIXME`/`XXX`, disabled phase test, placeholder, compatibility shim, universal NIP-02 kind switch, or code-size violation was found in phase source/tests. Facade is 475 lines; redb semantic owners are 386 and 167.

Main source is clean; only pre-existing planning artifacts are untracked. Dirty sibling `nnn-m8` was not modified by verification. It advanced concurrently, ending at `e928c633` on `worktree-agent-m8-p03`; its dirty set was preserved. Other worktrees were inspected only.

## Human Verification

None. Every required state transition, cleanup, restart, ordering, settlement, and live-relay claim has a passing executable test or independently replayed retained evidence.

## Gaps Summary

No blocking or deferred Phase 07.1 gap remains. Phase 07.1.1 can consume this verified universal foundation without treating NIP-29 behavior as part of Phase 07.1.

---

_Verified: 2026-08-22T05:35:46Z_  
_Verifier: independent gsd-verifier agent_
