---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 10
type: execute
wave: 7
depends_on: [07.1-06]
files_modified:
  - apps/canary/src/automatic_publication.rs
  - apps/canary/src/publication.rs
  - apps/canary/src/publication_child.rs
  - apps/canary/src/publication_support.rs
  - apps/canary/src/semantic_write_store.rs
  - apps/canary/src/semantic_write_support.rs
  - apps/canary/src/semantic_writes.rs
  - apps/canary/src/semantic_n_plus_one.rs
autonomous: true
requirements: [R1, R2, R3, R4]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All existing M5-M7 canary publication scenarios consume the adopted public vocabulary and retain exact wire/process/receipt evidence."
    - "Existing arbitrary-kind and external N+1 canary controls remain green before adding the Croissant NIP-02 scenario."
  prohibitions: []
  artifacts:
    - path: apps/canary/src/publication.rs
      provides: "Existing real-relay publication scenarios migrated to Write settlement"
    - path: apps/canary/src/semantic_writes.rs
      provides: "Existing semantic canaries migrated without private engine access"
  key_links:
    - from: apps/canary/src/publication.rs
      to: crates/fava/src/publication.rs
      via: "ordinary downstream app uses publish/to and Write::settled"
      pattern: "settled"
---

<objective>
Migrate the existing downstream canary publication corpus before adding the new Croissant scenario.

Purpose: Preserve independent wire/process proof across the universal API replacement and keep a green control corpus for R8.
Output: Eight migrated canary modules with unchanged scenario evidence.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/canary/README.md
@apps/canary/scenarios.json
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-SPEC.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate durable and automatic real-relay publication scenarios</name>
  <read_first>
    @apps/canary/src/automatic_publication.rs
    @apps/canary/src/publication.rs
    @apps/canary/src/publication_child.rs
    @apps/canary/src/publication_support.rs
  </read_first>
  <files>apps/canary/src/automatic_publication.rs, apps/canary/src/publication.rs, apps/canary/src/publication_child.rs, apps/canary/src/publication_support.rs</files>
  <behavior>
    - Explicit/automatic optimistic visibility, mixed outcomes, cancellation, crash recovery and route expansion retain exact artifacts.
    - Scenario code uses only public facade payload/scopes/Write, while support providers remain neutral.
  </behavior>
  <action>Run the relevant scenario tests red for old calls, migrate application flows to `publish`/`to` and `Write::settled`, and import neutral types only inside support/provider code. Preserve independent wire frames, process kill markers, receipt identities, deadlines, artifact bounds and scenario names.</action>
  <acceptance_criteria>All existing M5/M6 canary library tests compile/pass and scenario artifact assertions are unchanged or stronger.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path apps/canary/Cargo.toml publication</automated>
  </verify>
  <done>Existing durable and automatic real-relay publication controls use the adopted API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate semantic canary storage/support/N+1 flows</name>
  <read_first>
    @apps/canary/src/semantic_write_store.rs
    @apps/canary/src/semantic_write_support.rs
    @apps/canary/src/semantic_writes.rs
    @apps/canary/src/semantic_n_plus_one.rs
  </read_first>
  <files>apps/canary/src/semantic_write_store.rs, apps/canary/src/semantic_write_support.rs, apps/canary/src/semantic_writes.rs, apps/canary/src/semantic_n_plus_one.rs</files>
  <behavior>
    - First value, reapplication, opposing operations, and N+1 scenarios retain exact identities/evidence through Write.
    - Raw future-kind publication remains unchanged and kind-blind.
  </behavior>
  <action>Run semantic canary tests red, migrate public calls and terminal waits, and keep exact protocol/store internals in their owning support modules. Preserve seed redaction, artifact hashes, resource deadlines, external package graph checks, and raw event bytes. Perform the named `canary_write_receipt_identity_break` by mismatching one expected write/receipt identity in the controlled scenario, require the scenario test to fail, restore the exact file, and rerun.</action>
  <acceptance_criteria>All four enabled M7 scenarios pass through the new facade and the raw future-kind control remains exact.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path apps/canary/Cargo.toml semantic</automated>
  </verify>
  <done>The downstream canary is green before the new NIP-02/Croissant capstone lands, after observed red migration evidence and the restored `canary_write_receipt_identity_break`.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public symbols/functions/fields: none.
- New tests/files: none; eight downstream canary modules are migrated.
- Preserved evidence: M5-M7 scenario IDs, wire/process artifacts, exact identities, bounds, and resource deadlines.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Fava diagnostics -> independent canary witnesses | Canary must not treat Fava's own belief as proof of wire/process effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-071-25 | Repudiation | migrated canary evidence | high | mitigate | Preserve proxy frames, relay logs, process markers, exact IDs and artifact hashes. |
| T-071-26 | Information disclosure | canary seeds/keys | medium | mitigate | Retain seed redaction and record public keys only. |
</threat_model>

<verification>
Run downstream canary publication/semantic library tests; compare scenario registry and artifact fields for silent loss.
</verification>

<success_criteria>
The independent existing canary corpus remains a trustworthy control for the new R8 scenario.
</success_criteria>

<output>
Create `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-10-SUMMARY.md` when done.
</output>
