---
phase: 07.1
fixed_at: 2026-08-22T04:46:12Z
review_path: .planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-REVIEW.md
iteration: 2
findings_in_scope: 6
fixed: 6
skipped: 0
status: all_fixed
---

# Phase 07.1: Code Review Fix Report

**Fixed at:** 2026-08-22T04:46:12Z
**Source review:** `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-REVIEW.md`
**Iteration:** 2

**Summary:**

- Findings in scope: 6
- Fixed: 6
- Skipped: 0

## Fixed Issues

### CR-01: A terminal redb receipt with a source cannot survive restart

**Files modified:** `crates/fava-write-store-redb/src/lib.rs`, `crates/fava-write-store-redb/src/lifecycle.rs`, `crates/fava-write-store-redb/src/schema.rs`, `crates/fava-write-store-redb/src/semantic.rs`, `crates/fava-write-store-redb/src/validation.rs`, `crates/fava-write-store-redb/tests/semantic_write_store/admission.rs`
**Commit:** `405fad8`
**Status:** fixed: requires human verification
**Applied fix:** Retained the bounded semantic tuple as durable terminal provenance while removing its active coordinate and filtering terminal rows from recovery. A signed-source restart test proves the receipt and `revision_source` survive reopen while `recover_applied_edits()` remains empty.

### CR-02: Idempotent atomic acceptance treats materially different route plans as equal

**Files modified:** `crates/fava-write-store-memory/src/semantic.rs`, `crates/fava-write-store-redb/src/semantic.rs`, `crates/fava-write-store-redb/tests/semantic_write_store/admission.rs`, `crates/fava/tests/semantic_write_store.rs`
**Commit:** `8f007c1`
**Status:** fixed: requires human verification
**Applied fix:** Both stores now derive the candidate initial route effect through the neutral application helper and compare the complete persisted effect: outcome, revision, settlement, shortfalls including settled-absent evidence, desired destinations, attempts, and destination lanes. Provider-parity negatives vary only shortfall or settled-absent evidence and prove refusal without rewriting or notifying the first receipt.

### CR-03: The seal does not cover the identities and completion facts used as old-data proof

**Files modified:** `apps/canary/src/croissant_nip02.rs`, `apps/canary/src/croissant_nip02_evidence.rs`, `apps/canary/src/croissant_nip02_tests.rs`
**Commit:** `bcc63ca`
**Status:** fixed: requires human verification
**Applied fix:** The author signature now covers the canonical manifest claim set, including the embedded artifact hashes, before the verifier consumes any claim. Direct mutation negatives cover every identity, completion boolean, terminal field, bound, and teardown field with the exact `signed manifest claims` refusal.

### CR-04: Uppercase NIP-19 secret keys evade the retained-secret scan

**Files modified:** `apps/canary/src/croissant_nip02_evidence.rs`, `apps/canary/src/croissant_nip02_tests.rs`
**Commit:** `1295941`
**Status:** fixed: requires human verification
**Applied fix:** The exact 19-class inventory now scans the seed plus raw, lower/upper hex, lower/upper `nsec`, and every supported lower/upper NIP-21 scheme and payload combination for author and target keys. Direct scanner tests inject every representation.

### WR-01: The derived-secret negative is satisfied by a stale signature, not the secret scanner

**Files modified:** `apps/canary/src/croissant_nip02.rs`, `apps/canary/src/croissant_nip02_tests.rs`
**Commit:** `407259e`
**Status:** fixed: requires human verification
**Applied fix:** Removed the post-hoc stale-seal negative. An explicit generation-only hook now injects derived raw secret material immediately before the scanner at both pre-seal and post-manifest stages; both live Croissant negatives fail with the exact scanner refusal.

### WR-02: The redb semantic owner crossed the soft limit without a cohesion rationale

**Files modified:** `crates/fava-write-store-redb/BUILD.bazel`, `crates/fava-write-store-redb/src/lib.rs`, `crates/fava-write-store-redb/src/semantic.rs`, `crates/fava-write-store-redb/src/semantic_acceptance.rs`, `crates/fava-write-store-redb/tests/semantic_write_store.rs`
**Commit:** `d2de1b6`
**Applied fix:** Split atomic acceptance into a private `semantic_acceptance` owner and added measured guards for both production files. `semantic.rs` is 386 lines and `semantic_acceptance.rs` is 167; the only long-function rationale is scoped to the single-lock, single-commit transaction.

## Verification

All verification ran in the main checkout, as explicitly required by the fixer assignment.

- Focused RED/GREEN evidence reproduced each causal gap before its fix: terminal source reopen refusal; memory/redb route-evidence mismatch acceptance; mutable group identity accepted under the old seal; 9-versus-19 secret inventory; and the stale-seal generation negative. Each owning focused suite passed after its fix.
- `cargo test --workspace` — passed.
- `cargo test -p fava-nip02 --all-targets` — passed: 24 unit, 2 architecture, 1 public-API test.
- `cargo test --manifest-path apps/canary/Cargo.toml --all-targets` — passed: 31 tests.
- `cargo fmt --all -- --check` and `git diff --check` — passed.
- `python3 tools/check_vocabulary.py` and `python3 -m unittest tools.tests.test_vocabulary_check` — passed; 6 Python tests.
- `bazel test //...`, `bazel test //... --config=clippy`, and `bazel test //... --config=fmt-check` — passed: all 42 tests in each configuration.
- Fresh exact two-run Croissant gate — passed at `apps/canary/runs/phase-07.1-pair.1gstDm`; exactly two signed manifests verify, PIDs 42502/42583 are absent, and ports 61876/61896 are closed.
- Superseded evidence is recoverably retained at `apps/canary/runs/superseded/2026-08-22-review-iteration-2/`. The stronger verifier rejects the two old roots for omitted or incomplete claim sealing.
- Validation evidence was refreshed in commit `5112efe`.

## Skipped Issues

None.

---

_Fixed: 2026-08-22T04:46:12Z_
_Fixer: the agent (gsd-code-fixer)_
_Iteration: 2_
