---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 11
type: execute
wave: 8
depends_on: [07.1-07, 07.1-08, 07.1-09, 07.1-10]
files_modified:
  - docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md
  - docs/spec/ARCHITECTURE.md
  - docs/spec/FAVA_REWRITE_IMPLEMENTATION_PLAN.md
  - docs/spec/partial-spec-api-semantics.md
  - crates/fava-nip02/README.md
  - docs/internals/vocabulary.toml
  - .planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-VALIDATION.md
  - docs/issues/0013-edit-author-at-taker.md
  - docs/issues/0014-publish-door-ergonomics.md
  - docs/issues/0015-publish-scope-vocabulary.md
autonomous: true
requirements: [R1, R2, R3, R4, R5, R6, R7, R8, R9]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Authoritative specifications describe the delivered `publish`/`by`/`to`/`Write` and typed NIP-02 model as current behavior, not a proposal or migration."
    - "The NIP-02 README's app flow, read/discovery/edit examples and preservation claims compile against the delivered public Rust surface."
    - "Every approved public noun has one registry owner/lifecycle/distinction/counterexample/forcing requirement/reason/falsifier, and superseded facade symbols are application-internal only."
  prohibitions: []
  artifacts:
    - path: crates/fava-nip02/README.md
      provides: "Truthful delivered application-DX North Star with compiling examples"
    - path: docs/internals/vocabulary.toml
      provides: "Closed approved publication and NIP-02 public vocabulary"
    - path: docs/spec/ARCHITECTURE.md
      provides: "Current owner/lifecycle/application-door architecture"
  key_links:
    - from: crates/fava-nip02/README.md
      to: crates/fava-nip02/tests/public_api.rs
      via: "README examples are doctested or mirrored exactly by public API tests"
      pattern: "contact_list|follow_with|settled"
---

<objective>
Replace superseded documentation with the delivered universal publication and typed NIP-02 model.

Purpose: Make authoritative behavior, ownership, vocabulary, issues, validation, and crate DX agree with executable code.
Output: R9 current-model docs and a truthful executable NIP-02 README.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-SPEC.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-RESEARCH.md
@crates/fava-nip02/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace authoritative spec vocabulary and walkthroughs in place</name>
  <read_first>
    @docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md
    @docs/spec/ARCHITECTURE.md
    @docs/spec/FAVA_REWRITE_IMPLEMENTATION_PLAN.md
    @docs/spec/partial-spec-api-semantics.md
  </read_first>
  <files>docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md, docs/spec/ARCHITECTURE.md, docs/spec/FAVA_REWRITE_IMPLEMENTATION_PLAN.md, docs/spec/partial-spec-api-semantics.md</files>
  <action>Update only current behavioral/application vocabulary: the primary workload remains a durable write obligation internally, while applications call synchronous `publish` with optional `by`/`to` scopes and receive `Write`; replace `AcceptedWrite`/facade `WriteIntent`/facade `wait_terminal` examples accordingly. Describe `WriteIntent` and `AcceptedWrite` as legitimate neutral/internal owner vocabulary where the authorities explain custody, not as application doors. Scope the negative docs check to concrete application signatures/call examples so those internal ownership descriptions remain. Correct NIP-02 typed reads/entry errors/lossless edits and simple-groups examples to consume the universal door without implementing Phase 07.1.1 behavior. Remove superseded/proposed/transition narration rather than preserving parallel signatures. Keep issues 0016/0017 and ValueSet/native boundaries unpromised.</action>
  <acceptance_criteria>All four authorities agree on ownership, sync acceptance, exact scopes, settlement, kind-blind protocol composition and NIP-02 preservation; no contradictory application signature remains, while legitimate internal `WriteIntent`/`AcceptedWrite` ownership descriptions remain intact.</acceptance_criteria>
  <verify>
    <automated>python3 tools/check_vocabulary.py &amp;&amp; ! rg -n 'Fava::publish\([^\n]*WriteIntent|fava\.publish\((WriteIntent|intent)|Fava::wait_terminal\(|fava\.wait_terminal\(|let [^\n]*: AcceptedWrite = fava\.' docs/spec</automated>
  </verify>
  <done>Authoritative docs describe one delivered current model and retain all locked ownership/boundedness behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the NIP-02 README executable delivered documentation</name>
  <read_first>
    @crates/fava-nip02/README.md
    @crates/fava-nip02/tests/public_api.rs
    @crates/fava-nip02/src/lib.rs
    @docs/internals/vocabulary.toml
    @.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-VALIDATION.md
  </read_first>
  <files>crates/fava-nip02/README.md, docs/internals/vocabulary.toml, .planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-VALIDATION.md</files>
  <behavior>
    - README follow/unfollow/follow_with, typed reads, two-hop discovery, follower query, scopes and settlement examples compile.
    - README states exact malformed-entry errors and foreign-tag/content preservation contracts.
    - No delivered-versus-target, prerequisite-already-landed, inverse-field, actor-field, edit-format, or old explicit-intent narration remains.
  </behavior>
  <action>Rewrite the README in place as delivered app documentation. Use the actual public signatures and ordinary `observe`/`QuerySnapshot` behavior, correct the stale global `limit(1)` claim, show `Write::settled(all())`, and preserve the app-first flow from the whiteboard. Mirror every nontrivial code block in public API/integration tests or make it a valid doctest. Complete the registry for only actually exported symbols and update `07.1-VALIDATION.md` task mappings/status commands to the delivered test names.</action>
  <acceptance_criteria>README examples compile, every claim has named evidence, all approved vocabulary entries match exported symbols, and no prospective disclaimer remains.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-nip02 --doc &amp;&amp; cargo test -p fava-nip02 --test public_api &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check</automated>
  </verify>
  <done>The README is the truthful delivered DX North Star and the validation map names its executable proof.</done>
</task>

<task type="auto">
  <name>Task 3: Close focused architecture issues as adopted current state</name>
  <read_first>
    @docs/issues/0013-edit-author-at-taker.md
    @docs/issues/0014-publish-door-ergonomics.md
    @docs/issues/0015-publish-scope-vocabulary.md
  </read_first>
  <files>docs/issues/0013-edit-author-at-taker.md, docs/issues/0014-publish-door-ergonomics.md, docs/issues/0015-publish-scope-vocabulary.md</files>
  <action>Mark 0013/0014/0015 accepted and completed at the exact implementation commits produced by execution. Replace proposed API sketches and stale implementation-status claims with the delivered signatures, owner map, exit-gate commands, and deliberate-break evidence. Preserve focused decision rationale where it explains the current distinction, but remove claims that approval or implementation is pending.</action>
  <acceptance_criteria>All three issues identify adopted owners/current signatures/executable evidence and contain no awaiting-approval or implementation-in-progress status.</acceptance_criteria>
  <verify>
    <automated>! rg -n 'proposed|awaiting Pablo approval|not yet built|implementation in progress|broader target' docs/issues/0013-edit-author-at-taker.md docs/issues/0014-publish-door-ergonomics.md docs/issues/0015-publish-scope-vocabulary.md</automated>
  </verify>
  <done>The focused architecture record and delivered code/docs tell the same current story.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public symbols/functions/fields: none; this plan documents exactly those delivered by Plans 01-06.
- Updated documentation files: all four relevant authorities, `crates/fava-nip02/README.md`, vocabulary registry, validation strategy, and issues 0013-0015.
- New tests/files: none; README examples are covered by existing doctest/public API targets.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Documentation -> application implementation | Stale signatures can cause callers to construct unsafe or unsupported paths. |
| Vocabulary registry -> public API | Missing or parallel nouns can conceal ownership drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-071-27 | Tampering | authoritative API examples | high | mitigate | Doctest/public API compile checks and negative searches for superseded signatures. |
| T-071-28 | Repudiation | vocabulary ownership | medium | mitigate | Seven-part entries plus executable falsifiers and checker unit corpus. |
</threat_model>

<verification>
Run README doctests, public API, vocabulary checker/tests, focused negative searches and issue-status checks.
</verification>

<success_criteria>
R9 is complete and every documentation surface describes the delivered model only.
</success_criteria>

<output>
Create `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-11-SUMMARY.md` when done.
</output>
