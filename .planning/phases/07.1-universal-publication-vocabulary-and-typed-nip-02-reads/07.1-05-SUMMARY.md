---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 05
subsystem: nip02
tags: [rust, nip02, query, projection, architecture, tdd]
requires:
  - phase: 07.1-04
    provides: Total kind-3 ContactList decoder with ordered valid follows and exact entry errors
provides:
  - Ordinary kind-3 queries over one or many concrete author keys without a global result limit
  - Exact lowercase-p follower discovery using canonical subject hex
  - Pure deterministic follow projection in snapshot and source-entry order
  - Executable engine/provider dependency and universal kind-blind boundaries
affects: [07.1-06, 07.1-10, 07.1-11, 07.1-12, 07.1.1]
actuals:
  tokens: 5850
  tasks: 2
  commits: 5
tech-stack:
  added: [neutral fava-query dependency, fava-query-standard development oracle]
  patterns: [sealed one-or-many query input, present-empty match-nothing selection, pure snapshot projection]
key-files:
  created:
    - crates/fava-nip02/src/query.rs
    - crates/fava-nip02/src/tests/query.rs
    - crates/fava-nip02/tests/architecture.rs
  modified:
    - crates/fava-nip02/src/lib.rs
    - crates/fava-nip02/src/tests.rs
    - crates/fava-nip02/tests/public_api.rs
    - crates/fava-nip02/Cargo.toml
    - crates/fava-nip02/BUILD.bazel
    - Cargo.lock
    - MODULE.bazel.lock
    - docs/internals/vocabulary.toml
key-decisions:
  - "IntoContactAuthors is sealed and supports exact owned or borrowed one/many key shapes; every shape feeds the same present author axis."
  - "contact_list applies no global limit because ordinary replacement evaluation selects one newest kind-3 event independently for every author coordinate."
  - "followers_of uses SingleLetterTag::LOWERCASE_P with canonical lowercase public-key hex; follows_of owns no state and preserves snapshot then entry order."
patterns-established:
  - "Protocol query helpers return inert neutral Query values; acquisition, observation, evaluation, provenance, and lifecycle remain universal owners."
  - "Pure protocol projections decode each ordinary snapshot record locally and allocate only the ordered valid keys they return."
requirements-completed: [R4, R6, R9]
coverage:
  - id: D1
    description: One, many, duplicate, and empty author inputs produce exact kind-3 author selection without a global limit, and ordinary evaluation returns each author's newest list.
    requirement: R6
    verification:
      - kind: unit
        ref: crates/fava-nip02/src/tests/query.rs#one_many_and_empty_contact_list_queries_keep_a_concrete_author_axis
        status: pass
      - kind: unit
        ref: crates/fava-nip02/src/tests/query.rs#ordinary_evaluation_keeps_each_authors_newest_contact_list
        status: pass
    human_judgment: false
  - id: D2
    description: Follower discovery uses exact lowercase p and pure projection remains ordered, repeatable, empty-safe, two-hop composable, and concurrency-independent.
    requirement: R6
    verification:
      - kind: unit
        ref: crates/fava-nip02/src/tests/query.rs#follower_discovery_uses_only_exact_lowercase_p_and_canonical_hex
        status: pass
      - kind: unit
        ref: crates/fava-nip02/src/tests/query.rs#follow_projection_is_ordered_repeatable_and_safe_for_two_hop_concurrency
        status: pass
    human_judgment: false
  - id: D3
    description: The public typed query surface compiles externally while Cargo, Bazel, and source checks reject engine/provider ownership and NIP-02 switches in universal owners.
    requirement: R4
    verification:
      - kind: integration
        ref: crates/fava-nip02/tests/public_api.rs#external_surface_uses_only_approved_functions_and_types
        status: pass
      - kind: integration
        ref: crates/fava-nip02/tests/architecture.rs#nip02_remains_engine_provider_free
        status: pass
      - kind: other
        ref: forbidden fava-observe normal-dependency mutation failed the named descriptor and exact Cargo.toml bytes were restored
        status: pass
    human_judgment: false
  - id: D4
    description: IntoContactAuthors has approved ownership, lifecycle, distinction, forcing requirement, counterexample, and executable falsifier vocabulary.
    requirement: R9
    verification:
      - kind: unit
        ref: python3 tools/check_vocabulary.py && python3 -m unittest tools.tests.test_vocabulary_check
        status: pass
    human_judgment: false
duration: 10min
completed: 2026-08-22
status: complete
---

# Phase 07.1 Plan 05: Typed NIP-02 Reads and Follow Projection Summary

**Applications can now query one or many contact-list authors, discover exact lowercase-p followers, and compose deterministic typed follow projections through ordinary Fava query semantics.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-08-22T00:43:09Z
- **Completed:** 2026-08-22T00:52:59Z
- **Tasks:** 2
- **Files modified:** 11

## Accomplishments

- Added sealed one/many `contact_list` construction with a concrete author axis, match-nothing empty inputs, no global limit, and ordinary newest-per-coordinate evaluation.
- Added exact lowercase `p` follower discovery plus pure snapshot/entry-ordered `follows_of` projection proven repeatable, empty-safe, two-hop composable, and concurrency-independent.
- Locked the external public surface and normal Cargo/Bazel dependency set while scanning protocol sources for forbidden lifecycle edges and universal owners for NIP-02 kind switches.
- Registered only the plan-approved `IntoContactAuthors` noun and proved its private seal through compile-fail rustdoc evidence.

## Task Commits

1. **Task 1 RED: Add failing typed NIP-02 query evidence** - `455710b` (test)
2. **Task 1 GREEN: Add typed NIP-02 query builders** - `ab72705` (feat)
3. **Task 2 RED: Add failing NIP-02 boundary evidence** - `d224b6f` (test)
4. **Task 2 GREEN: Lock NIP-02 query boundary** - `ebf8015` (feat)
5. **Task 2 verification fix: Keep borrowed query API proof lint-clean** - `d602f1d` (fix)

## Files Created/Modified

- `crates/fava-nip02/src/query.rs` - Sealed author inputs, ordinary contact-list/follower queries, and pure follow projection.
- `crates/fava-nip02/src/tests/query.rs` - One/many/empty/newest/exact-p/order/repeat/two-hop/concurrency evidence.
- `crates/fava-nip02/tests/architecture.rs` - Named dependency/lifecycle boundary and independent universal kind-blind source scan.
- `crates/fava-nip02/tests/public_api.rs` - External compile and README-style borrowed first-hop composition proof.
- `crates/fava-nip02/src/lib.rs`, `crates/fava-nip02/src/tests.rs` - Public exports, private-seal compile failure, and cohesive test registry.
- `crates/fava-nip02/Cargo.toml`, `crates/fava-nip02/BUILD.bazel`, `Cargo.lock`, `MODULE.bazel.lock` - Neutral query dependencies and reproducible Cargo/Bazel metadata.
- `docs/internals/vocabulary.toml` - Complete approval record for `IntoContactAuthors`.

## Decisions Made

- Supported exact `PublicKey`, borrowed key, owned/borrowed vector, slice, and array shapes rather than an externally extensible conversion trait; every input is consumed immediately into ordinary query selection.
- Retained empty authors as `Some(empty)` instead of omitting the axis, so empty social-graph hops cannot broaden into an unfiltered kind-3 query.
- Used the upstream `SingleLetterTag::LOWERCASE_P` constant and canonical `PublicKey::to_hex`, keeping uppercase `P` and author selection as independent axes.

## Deliberate Break Evidence

- Added forbidden normal dependency `fava-observe.workspace = true` to `fava-nip02`.
- `nip02_remains_engine_provider_free` failed causally with dependency set `{fava-observe, fava-query, fava-state, fava-write}` instead of the exact allowlist.
- Reversed the patch directly and restored `Cargo.toml` byte-for-byte; SHA-256 before and after was `f6fb54e2fbb0e4241efb8c2e81faf20e78fc44a1b98d504b2c82db808ae23441`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added neutral query test infrastructure before Task 1 GREEN**
- **Found during:** Task 1 RED/GREEN
- **Issue:** The planned query implementation could not compile until Task 2's already-approved normal `fava-query` edge existed, while newest-replacement evidence required the neutral standard evaluator as a development oracle.
- **Fix:** Added both as RED test infrastructure, then moved `fava-query` to normal dependencies in Task 1 GREEN; Task 2 independently locked the exact Cargo and Bazel allowlists.
- **Files modified:** `crates/fava-nip02/Cargo.toml`, `Cargo.lock`
- **Verification:** The causal RED failed only on absent query symbols; all focused query and architecture suites pass.
- **Committed in:** `455710b`, `ab72705`

**2. [Rule 1 - Bug] Made borrowed-vector public evidence strict-Clippy clean**
- **Found during:** Overall verification
- **Issue:** Clippy considered an unconstrained borrowed generic call needless even though the test intentionally proved README-style `&Vec<PublicKey>` composition.
- **Fix:** Explicitly selected the borrowed-vector trait implementation at the external generic boundary.
- **Files modified:** `crates/fava-nip02/tests/public_api.rs`
- **Verification:** Strict Clippy and the external public API suite pass.
- **Committed in:** `d602f1d`

---

**Total deviations:** 2 auto-fixed (1 blocking plan-order issue, 1 evidence bug).
**Impact on plan:** Both fixes were required to prove the approved surface without adding compatibility paths, lifecycle ownership, or unapproved vocabulary.

## Issues Encountered

- Bazel cannot execute the repository-wide universal-source scan from a sandboxed crate test without exporting unrelated packages' source files. The architecture test remains Cargo-owned and still inspects the Bazel library allowlist; Bazel independently compiles and passes the crate unit and external public-API targets.
- The workspace-wide Cargo compile debt in legacy facade call sites remains owned by the scheduled Wave 7 migrations; this plan's complete crate, rustdoc, Clippy, Cargo, Bazel, and vocabulary gates are green.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 06 can build lossless NIP-02 edit helpers on the typed decoder and ordinary query surface.
- Later caller migrations can use the sealed one/many builder without protocol-owned observation state.

## Self-Check: PASSED

- All eleven implementation, evidence, manifest, lock, and vocabulary files plus this summary exist.
- Task commits `455710b`, `ab72705`, `d224b6f`, `ebf8015`, and `d602f1d` resolve in repository history.
- Focused query, external API, architecture, complete crate, rustdoc, strict Clippy, formatting, Bazel, vocabulary, file-size, and deliberate-break gates pass.
- No retained forbidden dependency, NIP-02 universal-owner switch, known stub, or code file above 500 lines remains.

---
*Phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads*
*Completed: 2026-08-22*
