---
phase: 07.1
fixed_at: 2026-08-22T04:04:08Z
review_path: .planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-REVIEW.md
iteration: 1
findings_in_scope: 8
fixed: 8
skipped: 0
status: all_fixed
---

# Phase 07.1: Code Review Fix Report

**Fixed at:** 2026-08-22T04:04:08Z  
**Source review:** `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-REVIEW.md`  
**Iteration:** 1

**Summary:**

- Findings in scope: 8
- Fixed: 8
- Skipped: 0

## Fixed Issues

### CR-01: The application facade still exposes neutral custody and route-preview doors

**Files modified:** `crates/fava/src/lib.rs`, `crates/fava/BUILD.bazel`, facade tests/support, and Cargo-owned canary callers  
**Commits:** `64c4db2`, `6a21ec9`  
**Applied fix:** Removed `Fava::accept_event` and `Fava::preview_write_routes` without aliases, moved provider/canary use to the existing neutral contracts, added compile/source prohibitions, and declared the exact Bazel provider dependencies used by the migrated tests.

### CR-02: Publication silently ignores reservation-release and initial-route mutation failures

**Files modified:** `crates/fava-publication/src/lib.rs`, the neutral write-store contract, memory/redb semantic owners, fault fixtures, and semantic publication/store tests  
**Commits:** `61901ad`, `45610ae`  
**Status:** fixed: requires human verification  
**Applied fix:** Made the initial automatic route part of the acceptance transaction, preserved both preparation and release failure context in an existing bounded typed refusal, returned no custody on mutation failure, released immediately terminal semantic coordinates, and retained redb terminal bounds across restart.

### CR-03: Redb recovery accepts explicit receipts whose durable relay lanes do not match the route

**Files modified:** `crates/fava-write-store-redb/src/validation.rs`, `crates/fava-write-store-redb/tests/semantic_write_store/recovery.rs`  
**Commit:** `9d128c0`  
**Status:** fixed: requires human verification  
**Applied fix:** Reconstructed exact public relay-session lanes from the ordered explicit route and refused missing, extra, and substituted destination or desired-destination identities.

### CR-04: The two-run verifier self-asserts old-data exclusion and checks only the group id

**Files modified:** `apps/canary/src/croissant_nip02.rs`  
**Commit:** `b4ddd8d`  
**Status:** fixed: requires human verification  
**Applied fix:** Removed the manifest-authored exclusion claim, required distinct group, group-event, baseline-event, and final-event identities, and derived cross-run exclusion by scanning both evidence bundles in both directions.

### CR-05: Secret evidence scans only the raw scenario seed, not derived private keys

**Files modified:** `apps/canary/src/croissant_nip02.rs`, `apps/canary/src/croissant_nip02_evidence.rs`, `apps/canary/src/croissant_nip02_tests.rs`, `apps/canary/src/lib.rs`  
**Commit:** `f059976`  
**Status:** fixed: requires human verification  
**Applied fix:** Derived author and target keys only in process memory, scanned raw key bytes, lower/upper hex, and `nsec` encodings plus the raw seed before and after manifest creation, retained only scan classes, and signed the exact artifact-hash set so post-run hash refresh cannot conceal injected secrets.

### CR-06: Public NIP-02 helpers allocate unbounded input before typed refusal

**Files modified:** `crates/fava-nip02/src/edit.rs`, `crates/fava-nip02/src/tests/edit.rs`  
**Commit:** `57e71bf`  
**Status:** fixed: requires human verification  
**Applied fix:** Replaced unbounded `ToString` conversion with a 69-byte capped `fmt::Write` sink covering hex, `npub`, and NIP-21 forms; formatting stops on byte 70. Metadata encoded length is refused before cloning petnames or allocating the encoded edit.

### WR-01: The old-data negative test is satisfied by hash mismatch, not the old-data check

**Files modified:** `apps/canary/src/croissant_nip02_tests.rs`  
**Commit:** `5e90af9`  
**Applied fix:** Replaced the hash-mismatch sentinel with direct causal negatives for every old-data identity in both scan directions and asserted the exact field-specific refusal.

### WR-02: The facade root crossed the repository soft limit without a cohesion rationale

**Files modified:** `crates/fava/tests/facade_surface.rs`  
**Commit:** `2749711`  
**Applied fix:** The facade root is now 475 lines, and a durable source check requires it to remain below 500 or split the next cohesive owner.

## Verification

All gates ran in the main checkout, as explicitly required by the fixer assignment.

- `cargo test --workspace` — passed.
- `cargo test -p fava-nip02 --all-targets` — passed: 24 unit, 2 architecture, 1 public-API test.
- `cargo test -p fava-nip02 --doc` — passed: 3 compile-fail doctests.
- `cargo test --manifest-path apps/canary/Cargo.toml --all-targets` — passed: 28 tests. An earlier run exposed the existing early-exit timing test once; its immediate full rerun passed.
- `python3 tools/check_vocabulary.py` and `python3 -m unittest tools.tests.test_vocabulary_check` — passed.
- `bazel test //...`, `bazel test //... --config=clippy`, and `bazel test //... --config=fmt-check` — passed: all 42 tests.
- `cargo fmt --all -- --check` and `git diff --check` — passed.
- Exact two-run Croissant gate — passed at `apps/canary/runs/phase-07.1-pair.nrSPUU`; verifier accepted exactly two distinct sealed manifests with all four event/group identities excluded cross-run and both child PID/ports closed.

## Skipped Issues

None.

---

_Fixed: 2026-08-22T04:04:08Z_  
_Fixer: the agent (gsd-code-fixer)_  
_Iteration: 1_
