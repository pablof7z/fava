---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 07
subsystem: publication-testing
tags: [rust, publication, facade, write, semantic-writes, migration]
requires:
  - phase: 07.1-03
    provides: Facade Write receipt inspection and settlement predicates
  - phase: 07.1-06
    provides: Universal publication and complete NIP-02 edit prerequisites
provides:
  - Primary explicit, automatic, bounded, and semantic facade callers using publish, by, to, and returned Write
  - Race-sensitive semantic publication coverage with exact author, generation, route, capacity, and winner attribution preserved
  - Causal facade_migration_write_identity_break with byte-identical restoration
affects: [07.1-08, 07.1-09, 07.1-10, 07.1-12, 07.1.1]
actuals:
  tokens: 8130
  tasks: 3
  commits: 3
tech-stack:
  added: []
  patterns: [payload publication at the facade, neutral intent imports only for contract fixtures, Write-owned receipt identity and settlement]
key-files:
  created: []
  modified:
    - crates/fava/tests/automatic_publication.rs
    - crates/fava/tests/explicit_publication.rs
    - crates/fava/tests/write_bounds.rs
    - crates/fava/tests/semantic_write_capabilities.rs
    - crates/fava/tests/semantic_write_publication.rs
    - crates/fava/tests/semantic_write_publication/author.rs
    - crates/fava/tests/semantic_write_publication/interleavings.rs
    - crates/fava/tests/semantic_write_publication/winner_order.rs
    - crates/fava/tests/semantic_write_publication/route_revision.rs
    - crates/fava/tests/semantic_write_publication/shared_capacity.rs
key-decisions:
  - "Facade tests publish payloads through publish, by, and to; neutral WriteIntent and WriteRouting remain direct fava-write imports only for preview and store-contract fixtures."
  - "Plan 08 retains sole ownership of shared capability support modules, so their known compile debt was recorded rather than hidden with a compatibility adapter."
patterns-established:
  - "Returned Write identities drive receipt inspection, settlement, cancellation coordinates, route revision, and capacity assertions without replacing exact facts with counts."
requirements-completed: [R1, R2, R3, R4]
coverage:
  - id: D1
    description: Automatic, explicit, bounded, and neutral contract regressions use the adopted application vocabulary without losing existing tests or assertions.
    requirement: R1
    verification:
      - kind: integration
        ref: cargo test -p fava --test automatic_publication
        status: pass
      - kind: integration
        ref: cargo test -p fava --test explicit_publication
        status: pass
      - kind: integration
        ref: cargo test -p fava --test write_bounds
        status: pass
      - kind: integration
        ref: cargo test -p fava --test semantic_write_contract
        status: pass
    human_judgment: false
  - id: D2
    description: The owned semantic capability caller uses by and to, while its shared lifecycle and protocol harness awaits the next migration plan.
    requirement: R4
    verification:
      - kind: integration
        ref: cargo test -p fava --test semantic_write_capabilities
        status: fail
    human_judgment: true
    rationale: The remaining compiler errors are exclusively in Plan 08-owned semantic_write_capability_lifecycle.rs and semantic_write_capability_protocol.rs.
  - id: D3
    description: All 19 semantic publication cases preserve exact author, receipt, revision, generation, route, capacity, winner, and interleaving facts through returned Write.
    requirement: R3
    verification:
      - kind: integration
        ref: cargo test -p fava --test semantic_write_publication
        status: pass
      - kind: other
        ref: facade_migration_write_identity_break failed with WriteId(2) versus WriteId(1), then exact source SHA was restored
        status: pass
    human_judgment: false
duration: 18min
completed: 2026-08-22
status: complete
---

# Phase 07.1 Plan 07: Primary Facade Caller Migration Summary

**Primary publication and race-sensitive semantic regressions now consume payload scopes and returned `Write` identities while preserving every existing test and assertion.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-08-22T01:09:00Z
- **Completed:** 2026-08-22T01:27:00Z
- **Tasks:** 3
- **Files modified:** 10

## Accomplishments

- Migrated automatic and explicit event publication to `publish(payload)` and `to(relays).publish(payload)`, with receipt inspection and terminal waiting attached to returned `Write`.
- Migrated semantic edit publication to explicit `by(author)` and appropriate automatic or explicit route scopes without weakening author freeze, generation, winner, route-revision, or capacity evidence.
- Preserved all 39 tests and every prior assertion across the eleven planned modules; the shared-capacity case gained one exact facade-write-to-receipt identity assertion.
- Proved the named identity sensitivity break: substituting a second returned `WriteId(2)` for the controlled receipt's `WriteId(1)` failed, and `semantic_write_publication.rs` restored byte-identically to SHA-256 `daedd49f095b73e939c5dcdfaa1613d34ff9aa98d6ed3ab5e5e950fc25f05ad5`.

## Task Commits

1. **Task 1: Migrate explicit, automatic, bounds, contract, and capability callers** - `c4b033e` (test)
2. **Task 2: Migrate semantic publication interleavings without losing exact attribution** - `8ca2ea9` (test)
3. **Task 3: Migrate route-revision and shared-capacity publication callers** - `ff54ba9` (test)

## Files Created/Modified

- `crates/fava/tests/automatic_publication.rs` - Payload publication, Write receipt inspection, and Write settlement for expanding automatic routes.
- `crates/fava/tests/explicit_publication.rs` - Explicit route scopes and Write-owned identities across unsigned, presigned, cancellation, and missing-signer cases.
- `crates/fava/tests/write_bounds.rs` - Direct neutral contract imports and ordered explicit route shape.
- `crates/fava/tests/semantic_write_capabilities.rs` - Public edit calls use by/to; neutral preview/support intent construction imports from fava-write.
- `crates/fava/tests/semantic_write_publication.rs` - Public semantic publication harness uses scoped edits and returned Write evidence.
- `crates/fava/tests/semantic_write_publication/author.rs` - Frozen author selection and addressable edit scopes.
- `crates/fava/tests/semantic_write_publication/interleavings.rs` - Neutral store interleavings use contract-crate imports and ordered routes.
- `crates/fava/tests/semantic_write_publication/winner_order.rs` - Winner-order publication uses by/to and Write receipt inspection.
- `crates/fava/tests/semantic_write_publication/route_revision.rs` - Automatic route revision remains attached to one returned Write identity.
- `crates/fava/tests/semantic_write_publication/shared_capacity.rs` - Shared capacity refusal retains provider-effect and exact accepted identity evidence.

## Decisions Made

- Preview and direct store tests keep `WriteIntent` as neutral contract vocabulary imported from `fava-write`; no application caller imports it from `fava` or publishes it through the facade.
- The Plan 08-owned shared capability lifecycle/protocol modules were not edited out of sequence and no compatibility adapter was introduced to conceal their remaining compile debt.

## Deviations from Plan

None - caller migration followed the approved ownership split. Sequential execution exposed the already-planned cross-plan compile interval described below.

## Issues Encountered

- The RED baseline failed for automatic, explicit, bounds, capabilities, and semantic publication callers. `semantic_write_contract` was already green because it intentionally tests the neutral contract directly.
- `cargo test -p fava --test semantic_write_capabilities` remains compile-red only in `support/semantic_write_capability_lifecycle.rs` and `support/semantic_write_capability_protocol.rs`, both owned by 07.1-08. The four other Task 1 targets and all 19 semantic publication tests pass.
- `cargo test -p fava --all-targets --no-run` additionally remains red in Plan 08/09-owned semantic failure modules. No Plan 07-owned migrated file appears in the remaining compiler-error paths.
- The first break-output grep used unquoted backticks and emitted a harmless zsh diagnostic; the captured test still failed on exact `WriteId(2)` versus `WriteId(1)`, restoration checksum matched, and the restored test passed.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 07.1-08 can migrate the shared capability lifecycle/protocol harness and semantic store/failure owners, clearing the remaining `semantic_write_capabilities` gate.
- 07.1-09 must clear the remaining semantic-failure callers and remove the temporary facade compatibility surface before the workspace-wide gate.

## Self-Check: PASSED

- Summary and all three task commits exist.
- All 39 original tests and all prior assertions remain; shared capacity adds one exact identity assertion.
- The named deliberate-break source checksum matches after restoration and the restored focused test passes.
- Passing and remaining compile-red gates above match the latest executed commands and exact compiler paths.

---
*Phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads*
*Completed: 2026-08-22*
