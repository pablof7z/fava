---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 07
type: execute
wave: 7
depends_on: [07.1-06]
files_modified:
  - crates/fava/tests/automatic_publication.rs
  - crates/fava/tests/explicit_publication.rs
  - crates/fava/tests/write_bounds.rs
  - crates/fava/tests/semantic_write_contract.rs
  - crates/fava/tests/semantic_write_capabilities.rs
  - crates/fava/tests/semantic_write_publication.rs
  - crates/fava/tests/semantic_write_publication/author.rs
  - crates/fava/tests/semantic_write_publication/interleavings.rs
  - crates/fava/tests/semantic_write_publication/winner_order.rs
  - crates/fava/tests/semantic_write_publication/route_revision.rs
  - crates/fava/tests/semantic_write_publication/shared_capacity.rs
autonomous: true
requirements: [R1, R2, R3, R4]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Existing explicit, automatic, bounded, semantic, and interleaving behaviors execute through the new payload/scope/Write vocabulary without weaker assertions."
    - "Semantic author freeze, generation attribution, route revision, and capacity tests still prove the same neutral owner contracts after caller migration."
  prohibitions: []
  artifacts:
    - path: crates/fava/tests/automatic_publication.rs
      provides: "Automatic publication regression coverage through `publish(payload)` and `Write`"
    - path: crates/fava/tests/semantic_write_publication.rs
      provides: "Semantic publication corpus migrated without protocol-specific facade paths"
  key_links:
    - from: crates/fava/tests/semantic_write_publication/author.rs
      to: crates/fava/src/publication.rs
      via: "explicit edit author uses `by(author).publish(edit)` and freezes at acceptance"
      pattern: "\.by\("
---

<objective>
Migrate primary facade publication regressions to the adopted public vocabulary without weakening Phase 5-7 guarantees.

Purpose: Keep the new door honest against durable, automatic, bounded, author-freeze, generation, and interleaving evidence.
Output: Eleven migrated facade integration files using `publish`/`by`/`to`/`Write::settled`.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-SPEC.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-03-SUMMARY.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate explicit, automatic, bounds, contract, and capability callers</name>
  <read_first>
    @crates/fava/tests/automatic_publication.rs
    @crates/fava/tests/explicit_publication.rs
    @crates/fava/tests/write_bounds.rs
    @crates/fava/tests/semantic_write_contract.rs
    @crates/fava/tests/semantic_write_capabilities.rs
  </read_first>
  <files>crates/fava/tests/automatic_publication.rs, crates/fava/tests/explicit_publication.rs, crates/fava/tests/write_bounds.rs, crates/fava/tests/semantic_write_contract.rs, crates/fava/tests/semantic_write_capabilities.rs</files>
  <behavior>
    - Existing test intent payload/routing distinctions map one-for-one to `publish`, `to`, or `by` calls.
    - Existing receipt assertions use returned `Write` identities/current receipt/settlement without losing exact facts.
  </behavior>
  <action>First run each target against the changed facade and record its expected compile failure. Replace only application-side intent construction: automatic unsigned/pre-signed payloads call `publish`; explicit payloads call `to`; edits requiring an author call `by`; terminal waits attach to `Write`. Import neutral intent/routing types directly from `fava-write` only where a test is intentionally testing the internal contract rather than the facade. Preserve every existing assertion and deadline.</action>
  <acceptance_criteria>All five integration targets compile and pass with the same behavioral assertions and no application-side facade `WriteIntent` use.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test automatic_publication &amp;&amp; cargo test -p fava --test explicit_publication &amp;&amp; cargo test -p fava --test write_bounds &amp;&amp; cargo test -p fava --test semantic_write_contract &amp;&amp; cargo test -p fava --test semantic_write_capabilities</automated>
  </verify>
  <done>Primary publication and contract regressions use only the adopted application vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate semantic publication interleavings without losing exact attribution</name>
  <read_first>
    @crates/fava/tests/semantic_write_publication.rs
    @crates/fava/tests/semantic_write_publication/author.rs
    @crates/fava/tests/semantic_write_publication/interleavings.rs
    @crates/fava/tests/semantic_write_publication/winner_order.rs
  </read_first>
  <files>crates/fava/tests/semantic_write_publication.rs, crates/fava/tests/semantic_write_publication/author.rs, crates/fava/tests/semantic_write_publication/interleavings.rs, crates/fava/tests/semantic_write_publication/winner_order.rs</files>
  <behavior>
    - Author selection is explicit at the taker and persists through source successor/recovery.
    - Retired completions, lane generations, winner order, and terminal facts remain exactly attributed.
  </behavior>
  <action>Run the target red for old call-site compilation, then migrate the harness, author/interleaving modules, and `winner_order.rs` to returned `Write` identities and appropriate `by`/`to` scopes. Replace `winner_order.rs`'s exact `publish(intent(...))` application call with the corresponding payload/scope call while preserving every winner, receipt, revision, event, route, attempt, and ordering assertion. Do not replace exact identity assertions with counts or booleans. Keep internal store/provider fault fixtures on neutral crate imports and preserve controlled barriers. Perform the named `facade_migration_write_identity_break` by substituting a different returned write identity in the controlled interleaving assertion, require the focused target to fail, restore the exact file, and rerun.</action>
  <acceptance_criteria>The migrated semantic publication modules, including `winner_order.rs`, pass unchanged behavioral names and exact write/receipt/revision/event/route/attempt/order assertions.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test semantic_write_publication</automated>
  </verify>
  <done>The most race-sensitive Phase 7 corpus proves the new door without compatibility paths, including observed red compilation before migration and the restored `facade_migration_write_identity_break`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate route-revision and shared-capacity publication callers</name>
  <read_first>
    @crates/fava/tests/semantic_write_publication/route_revision.rs
    @crates/fava/tests/semantic_write_publication/shared_capacity.rs
  </read_first>
  <files>crates/fava/tests/semantic_write_publication/route_revision.rs, crates/fava/tests/semantic_write_publication/shared_capacity.rs</files>
  <behavior>
    - Route revision and shared-capacity cases use returned `Write` identity without weakening revision, lane, capacity, or terminal assertions.
  </behavior>
  <action>Observe the old facade calls fail to compile, then mechanically migrate only those calls to payload/scopes/Write. Preserve the controlled barriers and every exact route-revision, lane-generation, capacity, receipt, attempt, and terminal assertion.</action>
  <acceptance_criteria>Both modules compile and the complete semantic publication target retains exact route and capacity evidence.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test semantic_write_publication route_revision &amp;&amp; cargo test -p fava --test semantic_write_publication shared_capacity</automated>
  </verify>
  <done>The remaining race-sensitive publication callers use the adopted facade with their original proof strength.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public symbols/functions/fields: none; this plan consumes the Phase 07.1 facade surface.
- New test files: none; eleven existing integration modules are migrated.
- Modified tests: explicit/automatic/bounds/semantic contract/capability/publication author/interleaving/route/capacity cases.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Old regression corpus -> new facade | Mechanical caller changes could silently weaken exact distributed-system assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-071-19 | Repudiation | migrated receipt assertions | high | mitigate | Preserve exact identity/outcome assertions and existing deterministic deadlines; review diff for removed assertions. |
| T-071-20 | Tampering | author scope migration | high | mitigate | Use `by(author)` at the public taker and retain Alice/Bob freeze tests. |
</threat_model>

<verification>
Run every named integration target independently; compare test lists before/after and require no removed case.
</verification>

<success_criteria>
Primary historical publication guarantees survive the API replacement through the public door.
</success_criteria>

<output>
Create `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-07-SUMMARY.md` when done.
</output>
