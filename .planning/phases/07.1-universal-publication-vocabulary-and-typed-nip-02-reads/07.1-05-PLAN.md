---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 05
type: execute
wave: 5
depends_on: [07.1-04]
files_modified:
  - crates/fava-nip02/src/query.rs
  - crates/fava-nip02/src/lib.rs
  - crates/fava-nip02/src/tests.rs
  - crates/fava-nip02/tests/public_api.rs
  - crates/fava-nip02/tests/architecture.rs
  - crates/fava-nip02/Cargo.toml
  - crates/fava-nip02/BUILD.bazel
  - docs/internals/vocabulary.toml
autonomous: true
requirements: [R4, R6, R9]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Edge 16: author selection and subject exact-`p` discovery remain distinct axes and never broaden one another."
    - "Edge 17: empty author input, empty snapshot, and empty result fabricate no public key or unfiltered query."
    - "Edge 18: query results use ordinary evaluator order and `follows_of` preserves snapshot then row order."
    - "Edge 19: repeated projection over the same snapshot returns equal ordered keys and evidence."
    - "Edge 20: independent observations and two-hop projections share no mutable protocol state."
  prohibitions:
    - descriptor: nip02_remains_engine_provider_free
      statement: "The protocol crate cannot own or depend on facade, observation, signer, routing, publication, receipt, store, runtime, socket, acquisition, or provider lifecycles."
  artifacts:
    - path: crates/fava-nip02/src/query.rs
      provides: "Ordinary exact contact-list and follower queries plus pure follow projection"
      exports: [IntoContactAuthors, contact_list, follows_of, followers_of]
    - path: crates/fava-nip02/tests/architecture.rs
      provides: "Dependency and lifecycle-owner negative checks"
  key_links:
    - from: crates/fava-nip02/src/query.rs
      to: crates/fava-query/src/selection.rs
      via: "kind/authors and exact lowercase `p` tag-value axes"
      pattern: "tag_values"
    - from: crates/fava-nip02/src/query.rs
      to: crates/fava-nip02/src/contact_list.rs
      via: "pure projection parses snapshot records then retains source/row order"
      pattern: "ContactList::from_event"
---

<objective>
Deliver ordinary NIP-02 contact-list reads, follower discovery, and pure follow projection.

Purpose: Let applications compose first-hop and two-hop social graph reads using the existing query/observation lifecycle.
Output: R6 builders/projection, exact tag-axis use, empty/order/concurrency evidence, and protocol-boundary checks.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-SPEC.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-RESEARCH.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-03-SUMMARY.md
@crates/fava-nip02/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red-first one/many reads, exact follower discovery, and pure projection</name>
  <read_first>
    @crates/fava-query/src/selection.rs
    @crates/fava-query-standard/src/lib.rs
    @crates/fava-nip02/src/contact_list.rs
    @crates/fava-nip02/src/tests.rs
  </read_first>
  <files>crates/fava-nip02/src/query.rs, crates/fava-nip02/src/lib.rs, crates/fava-nip02/src/tests.rs</files>
  <behavior>
    - Single/multiple authors become kind-3 plus a concrete author axis without global `limit(1)`.
    - Empty authors match nothing; `followers_of` uses canonical lowercase hex under exact lowercase `p`.
    - `follows_of` is pure, deterministic, ordered, and safe for independent/two-hop calls.
  </behavior>
  <action>Add the full R6 tests first and run red. Implement `contact_list` over one/many concrete authors without a global limit, `followers_of` through `SingleLetterTag::lowercase(Alphabet::P)` or the existing exact constructor, and `follows_of` as an allocation-bounded snapshot/row walk over `ContactList`. Use a sealed `IntoContactAuthors` only because one-and-many ergonomics force it; do not add synonym functions or an observation wrapper. Preserve decoder evidence while projecting only valid public keys.</action>
  <acceptance_criteria>One/many/newest/empty/exact-p/order/repeat/concurrency/two-hop tests pass and no query axis broadens when its input is empty.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-nip02 query</automated>
  </verify>
  <done>Applications construct ordinary NIP-02 reads and compose typed keys without private live state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the external query API and engine-free dependency boundary</name>
  <read_first>
    @crates/fava-nip02/Cargo.toml
    @crates/fava-nip02/BUILD.bazel
    @crates/fava-nip02/tests/public_api.rs
    @docs/internals/vocabulary.toml
  </read_first>
  <files>crates/fava-nip02/Cargo.toml, crates/fava-nip02/BUILD.bazel, crates/fava-nip02/tests/public_api.rs, crates/fava-nip02/tests/architecture.rs, docs/internals/vocabulary.toml</files>
  <behavior>
    - Normal dependencies add only neutral `fava-query` and existing protocol/domain crates.
    - External tests compile exact public functions/types and reject lifecycle/provider imports/dependencies.
    - Universal-owner source scans remain free of NIP-02 kind switches.
  </behavior>
  <action>Mirror the neutral query dependency in Cargo and Bazel. Add a bounded architecture allowlist/source check named `nip02_remains_engine_provider_free`; register the exported sealed `IntoContactAuthors` trait. Compile external API bindings for all functions and types. Deliberately add a forbidden `fava` or provider dependency and require the descriptor to fail on that exact edge, restore, and rerun. Keep the universal kind-blind scan as an independent control for the final canary.</action>
  <acceptance_criteria>The crate compiles with only neutral dependencies, public types/functions are reachable externally, forbidden dependency/lifecycle edges fail the descriptor, and source scans find no protocol switch.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-nip02 --test public_api &amp;&amp; cargo test -p fava-nip02 --test architecture &amp;&amp; python3 tools/check_vocabulary.py</automated>
  </verify>
  <done>R6 composes ordinary query primitives and R4's protocol boundary is executable.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public symbols: sealed `IntoContactAuthors`, `contact_list`, `follows_of`, `followers_of`.
- New dependency: normal `fava-query` dependency in `fava-nip02` Cargo/Bazel manifests; no engine/provider dependency.
- New tests: one/many/newest/exact-p/empty/order/repeated/two-hop/concurrent projection cases and architecture descriptors.
- New files: `crates/fava-nip02/src/query.rs`, `crates/fava-nip02/tests/architecture.rs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Application author set -> query selection | Empty or malformed selection could accidentally become an unfiltered network query. |
| Query snapshot -> typed keys | Malformed rows and concurrent calls must not leak mutable protocol state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-071-13 | Information disclosure | empty author selection | high | mitigate | Preserve present-empty author axis and assert match-nothing behavior. |
| T-071-14 | Tampering | exact follower filter | high | mitigate | Use the landed generic exact lowercase `p` axis and canonical target hex. |
| T-071-15 | Elevation of privilege | protocol dependency direction | high | mitigate | Cargo/Bazel allowlist and source checks reject engine/provider ownership. |
</threat_model>

<verification>
Run query/public API/architecture/vocabulary suites and inspect the forbidden-dependency mutation record.
</verification>

<success_criteria>
R6 reads and discovery are ordinary inert queries/pure projections and `fava-nip02` remains lifecycle-free.
</success_criteria>

<output>
Create `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-05-SUMMARY.md` when done.
</output>
