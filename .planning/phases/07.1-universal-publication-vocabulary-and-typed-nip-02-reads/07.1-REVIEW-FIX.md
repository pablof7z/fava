---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
fixed_at: 2026-08-22T05:17:33Z
review_path: .planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-REVIEW.md
iteration: 3
findings_in_scope: 4
fixed: 4
skipped: 0
status: all_fixed
---

# Phase 07.1: Code Review Fix Report

**Fixed at:** 2026-08-22T05:17:33Z  
**Source review:** `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-REVIEW.md`  
**Iteration:** 3

**Summary:**
- Findings in scope: 4
- Fixed: 4
- Skipped: 0

## Fixed Issues

### CR-01: Rejected secret evidence remains in the persistent runs directory

**Files modified:** `apps/canary/src/artifacts.rs`, `apps/canary/src/croissant_nip02.rs`, `apps/canary/src/croissant_nip02_tests.rs`  
**Commit:** 6f96373  
**Applied fix:** Croissant NIP-02 evidence is generated in an owner-private sibling staging directory. A `RunArtifacts` drop owner removes staging on every refusal, and the final run is atomically renamed beneath the persistent pair root only after the post-manifest scan. Persistent-parent tests inject a derived private key at both scan boundaries and prove that the retained root, staging directory, and injected bytes are absent.

### WR-01: Croissant readiness can accept a child that is already exiting

**Files modified:** `apps/canary/src/croissant.rs`, `apps/canary/src/croissant_tests.rs`, `apps/canary/src/croissant_nip02.rs`, `apps/canary/src/croissant_nip02_tests.rs`  
**Commit:** 4013b3f  
**Status:** fixed: requires human verification  
**Applied fix:** Readiness checks child status before the first connection, after it, after a bounded 100 ms stability window, and after a second successful connection. The deterministic opens-port-then-exits fixture is causally red on the old ordering and passed 20 consecutive green runs. The stability duration is retained in sealed manifest bounds.

### WR-02: Validation claims an evidence field that neither manifest contains

**Files modified:** `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-VALIDATION.md`  
**Commit:** b566c3d  
**Applied fix:** Validation now states that pair verification derives bidirectional old-data exclusion from the two sealed identity sets instead of claiming a nonexistent per-manifest boolean. Commit `306af30` refreshes the retained sign-off pair and records the staged-promotion and readiness-stability evidence.

### WR-03: Authoritative composition example does not compile against the current API

**Files modified:** `docs/spec/partial-spec-api-semantics.md`  
**Commit:** d0b300a  
**Applied fix:** The authoritative example now awaits both `observe` calls, projects with `first.current().as_ref()`, and supplies `first_hop.as_slice()` to `contact_list`, matching the implemented signatures.

## Verification

The full gates ran in the isolated review-fix worktree `/Users/pablofernandez/Work/nnn/.claude/worktrees/rf-07.1-68646-1787375033`; retained canary artifacts were written to the persistent main-checkout evidence root named below.

- RED: the persistent-parent secret-injection test failed because the refused run remained under the persistent root.
- GREEN: the same test passed at both pre-seal and post-manifest boundaries.
- RED: the opens-port-then-exits fixture returned a ready `CroissantProcess` under the old ordering.
- GREEN: all five Croissant supervisor tests passed; the transient-listener fixture then passed 20 consecutive executions.
- `cargo test --manifest-path apps/canary/Cargo.toml --all-targets`: 32 passed.
- `cargo test --workspace`: passed.
- `python3 tools/check_vocabulary.py`: passed.
- `python3 -m unittest tools.tests.test_vocabulary_check`: 6 passed.
- `cargo fmt --all -- --check` and `git diff --check`: passed.
- `bazel test //...`: 42 passed.
- `bazel test //... --config=clippy`: 42 passed.
- `bazel test //... --config=fmt-check`: 42 passed.
- Fresh pair: `/Users/pablofernandez/Work/nnn/apps/canary/runs/phase-07.1-pair.9EyxBY`; exactly two manifests; `verify-croissant-pair` passed after all gates.
- Pair PIDs 92704 and 92738 are absent; ports `127.0.0.1:55544` and `127.0.0.1:55567` are closed; no owner-private staging directory remains.
- Unrelated pre-phase PID 75649 remained live and untouched.
- After fast-forwarding to `main`, `verify-croissant-pair`, `cargo fmt --all -- --check`, and `git diff --check` passed again in the main checkout.

---

_Fixed: 2026-08-22T05:17:33Z_  
_Fixer: the agent (gsd-code-fixer)_  
_Iteration: 3_
