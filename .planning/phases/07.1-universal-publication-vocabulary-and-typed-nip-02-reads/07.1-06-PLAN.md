---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 06
type: execute
wave: 6
depends_on: [07.1-05]
files_modified:
  - crates/fava-nip02/src/edit.rs
  - crates/fava-nip02/src/lib.rs
  - crates/fava-nip02/src/tests.rs
  - crates/fava-nip02/src/tests/edit.rs
  - crates/fava-nip02/tests/public_api.rs
autonomous: true
requirements: [R4, R7]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Edge 21: the first matching row, later duplicates, foreign tags, and surrounding tags receive distinct owned/preserved treatment."
    - "Edge 22: add/remove against no source or an empty list produces a bounded valid kind-3 successor."
    - "Edge 23: invalid key/relay strings refuse with typed errors while preserved bytes remain unchanged."
    - "Edge 24: the first row and every foreign tag retain their exact relative positions except owned removed duplicate rows."
    - "Edge 25: repeated add/remove is semantically stable and `follow_with` never overwrites existing first-row metadata."
    - "Edge 26: rebase over a newer qualified source preserves unrelated concurrent changes."
  prohibitions:
    - descriptor: nip02_preserves_foreign_kind3_bytes
      statement: "Follow/unfollow cannot delete, rewrite, normalize, or reorder foreign tags, preserved columns, or legacy content."
  artifacts:
    - path: crates/fava-nip02/src/edit.rs
      provides: "Fallible target conversion, metadata edit codec, and lossless materialization"
      exports: [follow, unfollow, follow_with, materializer]
    - path: crates/fava-nip02/src/tests/edit.rs
      provides: "String/key, metadata, inverse, preservation, bounds, idempotency, and rebase tests"
  key_links:
    - from: crates/fava-nip02/src/edit.rs
      to: fava_write::ReplaceableEventEdit
      via: "private bounded change codec with authorless kind-3 coordinate"
      pattern: "ReplaceableEventEdit::new"
    - from: crates/fava-nip02/src/edit.rs
      to: fava_write::EventBuilder
      via: "qualified source content and cloned tags become one bounded successor"
      pattern: "EventBuilder"
---

<objective>
Complete the README-shaped NIP-02 edit surface while preserving shared kind-3 data outside Fava's ownership.

Purpose: Make follow metadata and string inputs ergonomic without turning edits into destructive list rewrites.
Output: R7 `follow`/`unfollow`/`follow_with`, checked private encoding, complete preservation/refusal/rebase evidence.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-SPEC.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-RESEARCH.md
@docs/issues/0013-edit-author-at-taker.md
@crates/fava-nip02/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red-first fallible targets and metadata edit encoding</name>
  <read_first>
    @crates/fava-nip02/src/lib.rs
    @crates/fava-nip02/src/bounds.rs
    @crates/fava-nip02/src/tests.rs
    @crates/fava-nip02/tests/public_api.rs
  </read_first>
  <files>crates/fava-nip02/src/edit.rs, crates/fava-nip02/src/lib.rs, crates/fava-nip02/src/tests.rs, crates/fava-nip02/src/tests/edit.rs, crates/fava-nip02/tests/public_api.rs</files>
  <behavior>
    - Public-key values and supported key strings work for follow/unfollow/follow_with; invalid strings refuse before custody.
    - `follow_with` encodes optional relay and petname under exact edit/event/tag bounds.
    - `follow_with(target,None,None)` is semantically equal to `follow(target)`.
  </behavior>
  <action>Move existing edit/materializer code into `edit.rs` without changing behavior, then add the edit matrix in cohesive `src/tests/edit.rs` first for key/string conversion, invalid key/relay, absent/present-empty petname, exact metadata encoding, size/tag bounds, and opposing operations; keep `src/tests.rs` as a small module registry and every code file below 500 lines. Use upstream `PublicKey::parse`/`FromStr`; avoid a new public target wrapper. Add a distinct checked private metadata-add operation encoding; never reinterpret old bytes through a compatibility branch. Run red before implementing each new case.</action>
  <acceptance_criteria>Key and string calls compile externally; metadata round-trips exactly; invalid/over-bound input refuses before any accepted write; old edit bytes retain their one meaning.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-nip02 edit_codec &amp;&amp; cargo test -p fava-nip02 --test public_api</automated>
  </verify>
  <done>The full public edit constructor surface exists with private bounded encoding and typed refusal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove lossless existing-row treatment, idempotency, and concurrent rebase</name>
  <read_first>
    @crates/fava-nip02/src/edit.rs
    @crates/fava-nip02/src/contact_list.rs
    @crates/fava-nip02/src/tests/edit.rs
  </read_first>
  <files>crates/fava-nip02/src/edit.rs, crates/fava-nip02/src/tests/edit.rs</files>
  <behavior>
    - Follow/follow_with retain the first matching valid-key `p` entry verbatim and remove later matches; unfollow removes all matching rows.
    - Every nonmatching/foreign tag and content string is cloned byte-for-byte in relative order.
    - Applying the accepted edit to a newer qualified source preserves unrelated changes and yields deterministic bounded output.
  </behavior>
  <action>Add byte- and position-sensitive fixtures containing `['something-something']`, `['t','nostr']`, surrounding tags, extra values, malformed `p` entries, duplicate valid entries, and nonempty legacy content. Implement target-local normalization only: preserve the first matching row exactly, remove later matching rows, append one canonical row only when absent, and copy content unchanged. Reuse source qualification and caller-supplied timestamp. Add a no-secret-log proof: public errors expose typed classifications without raw tag/content values, and an architecture/source test rejects logging dependencies/macros in `fava-nip02`. Deliberately rebuild tags from understood rows and drop/reorder foreign data; require `nip02_preserves_foreign_kind3_bytes` to fail exact bytes/positions, restore exact bytes, and rerun.</action>
  <acceptance_criteria>All preservation, duplicate cleanup, repeated add/remove, existing metadata, bounds, newer-source, no-raw-input error/logging checks, and named deliberate-break assertions pass causally.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-nip02 edit &amp;&amp; cargo test -p fava-nip02 --all-targets</automated>
  </verify>
  <done>NIP-02 changes only understood matching contact entries and remains deterministic over concurrent source arrival.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public function: `follow_with`; changed public generic signatures for `follow` and `unfollow` accept public keys and supported strings fallibly.
- Existing public function retained: `materializer`; no signer, route, receipt, or observation symbol is added.
- New private functions/types: target conversion helper, metadata-add codec encode/decode, and lossless target-local apply logic in `edit.rs`.
- New tests: key/string conversion, metadata rows, opposing operations, exact foreign bytes/order, duplicate cleanup, repeated operations, bounds, newer-source rebase, and no-secret-log checks in `crates/fava-nip02/src/tests/edit.rs`.
- New files: `crates/fava-nip02/src/edit.rs`, `crates/fava-nip02/src/tests/edit.rs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Application metadata -> persisted edit | Key, relay, petname, and sizes become durable protocol-owned change bytes. |
| Shared source kind-3 -> successor | Data meaningful to other applications must survive local editing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-071-16 | Tampering | edit target/metadata codec | high | mitigate | Upstream parsing plus checked private encoding and typed bounds before custody. |
| T-071-17 | Information disclosure | foreign shared-document data | medium | mitigate | Byte/position fixtures prove exact preservation; typed errors omit raw tag/content values, and dependency/source checks reject logging dependencies/macros in `fava-nip02`. |
| T-071-18 | Tampering | lossless materializer | high | mitigate | Byte/position fixtures and named foreign-byte deliberate break. |
</threat_model>

<verification>
Run all NIP-02 targets and inspect the foreign-tag-loss mutation record with source-hash restoration.
</verification>

<success_criteria>
R7 is complete: application-friendly edits preserve every byte and relative position outside the exact owned target rows.
</success_criteria>

<output>
Create `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-06-SUMMARY.md` when done.
</output>
