---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 08
type: execute
wave: 7
depends_on: [07.1-06]
files_modified:
  - crates/fava/tests/semantic_write_store.rs
  - crates/fava/tests/semantic_write_store/author.rs
  - crates/fava/tests/semantic_write_store/current_guard.rs
  - crates/fava/tests/support/semantic_write.rs
  - crates/fava/tests/support/semantic_write_capability_protocol.rs
  - crates/fava/tests/support/semantic_write_capability_lifecycle.rs
  - crates/fava/tests/support/semantic_write_capability_signer.rs
  - crates/fava/tests/semantic_write_failures.rs
  - crates/fava/tests/semantic_write_failures/faults.rs
  - crates/fava/tests/semantic_write_failures/support.rs
autonomous: true
requirements: [R1, R2, R3, R4]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Store current guards, signer ownership, protocol lifecycle, and injected failure behavior retain exact facts after public caller migration."
    - "Fault stores continue to expose neutral `WriteIntent` only through contract-crate imports, never as application facade vocabulary."
  prohibitions: []
  artifacts:
    - path: crates/fava/tests/support/semantic_write_capability_lifecycle.rs
      provides: "Shared capability lifecycle harness consuming returned Write and settlement"
    - path: crates/fava/tests/semantic_write_failures/faults.rs
      provides: "Neutral fault provider retained behind public facade tests"
  key_links:
    - from: crates/fava/tests/support/semantic_write_capability_lifecycle.rs
      to: crates/fava/src/publication.rs
      via: "shared public capability flow receives Write and awaits attached settlement"
      pattern: "settled"
---

<objective>
Migrate semantic store, lifecycle, signer, and fault harnesses while preserving neutral provider contracts.

Purpose: Prevent the facade vocabulary change from leaking application nouns into stores/providers or erasing failure-isolation evidence.
Output: Ten migrated support/test modules with unchanged store/fault behavior.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-SPEC.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate store guards and shared protocol support</name>
  <read_first>
    @crates/fava/tests/semantic_write_store.rs
    @crates/fava/tests/semantic_write_store/author.rs
    @crates/fava/tests/semantic_write_store/current_guard.rs
    @crates/fava/tests/support/semantic_write.rs
    @crates/fava/tests/support/semantic_write_capability_protocol.rs
  </read_first>
  <files>crates/fava/tests/semantic_write_store.rs, crates/fava/tests/semantic_write_store/author.rs, crates/fava/tests/semantic_write_store/current_guard.rs, crates/fava/tests/support/semantic_write.rs, crates/fava/tests/support/semantic_write_capability_protocol.rs</files>
  <behavior>
    - Store-contract tests continue to construct neutral intents directly from `fava-write` when that contract is the subject.
    - Public capability helpers publish payloads via `Fava` scopes and return `Write` without exposing neutral intent types to the application path.
  </behavior>
  <action>Run the affected targets red for facade compile errors. Separate contract-owner imports from application calls: write-store tests may use `fava_write::WriteIntent`; public support uses `publish`/`by`/`to` and `Write`. Preserve every current-generation/store-CAS/author assertion and do not add adapter aliases.</action>
  <acceptance_criteria>Store contract tests still exercise the neutral interface, public helpers expose only application vocabulary, and all original cases remain listed/passing.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test semantic_write_store &amp;&amp; cargo test -p fava --test semantic_write_contract</automated>
  </verify>
  <done>Neutral store ownership and public capability consumption are clearly separated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate lifecycle/signer/fault harnesses with attributed errors intact</name>
  <read_first>
    @crates/fava/tests/support/semantic_write_capability_lifecycle.rs
    @crates/fava/tests/support/semantic_write_capability_signer.rs
    @crates/fava/tests/semantic_write_failures.rs
    @crates/fava/tests/semantic_write_failures/faults.rs
    @crates/fava/tests/semantic_write_failures/support.rs
  </read_first>
  <files>crates/fava/tests/support/semantic_write_capability_lifecycle.rs, crates/fava/tests/support/semantic_write_capability_signer.rs, crates/fava/tests/semantic_write_failures.rs, crates/fava/tests/semantic_write_failures/faults.rs, crates/fava/tests/semantic_write_failures/support.rs</files>
  <behavior>
    - Missing/wrong signer, storage refusal, panic, lagged receipt stream, transient reads, and route faults retain typed attribution.
    - Returned Write settlement never converts owner errors to generic strings or hangs after terminality.
  </behavior>
  <action>Run the affected target red for the removed facade contract, then replace facade-side accepted/terminal plumbing with `Write` and scope calls, leaving fault-provider `WriteStore` methods on neutral contract types. Run every failure target and preserve error variant/identity assertions, barriers, and time bounds. Do not turn missing signer behavior into implicit selection. Perform the named `provider_boundary_facade_leak_break` by routing one public harness back through a facade `WriteIntent` import, require the architecture/compile gate to fail, restore the exact file, and rerun.</action>
  <acceptance_criteria>Failure suites pass with the same exact owner/error/identity assertions, and no facade compatibility helper is introduced in support code.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test semantic_write_failures &amp;&amp; cargo test -p fava --test semantic_write_capabilities</automated>
  </verify>
  <done>Failure isolation and signer/store ownership remain exact under the new application door, with red pre-migration evidence and the restored `provider_boundary_facade_leak_break`.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public symbols/functions/fields: none.
- New tests/files: none; existing semantic store/lifecycle/signer/failure harnesses are migrated.
- Contract distinction preserved: neutral `WriteIntent` remains public in `fava-write` for providers, absent from application facade imports.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Facade -> fault provider | Injected failures must remain attributable to their exact neutral owner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-071-21 | Denial of service | receipt/signer fault harness | high | mitigate | Preserve deterministic deadlines and terminal wakeups for every injected failure. |
| T-071-22 | Repudiation | typed owner errors | medium | mitigate | Retain exact error variant and write/receipt identity assertions. |
</threat_model>

<verification>
Run semantic store, capability and failure integration targets; compare listed test names to the pre-migration corpus.
</verification>

<success_criteria>
The public-door migration does not merge facade, store, signer, or fault ownership.
</success_criteria>

<output>
Create `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-08-SUMMARY.md` when done.
</output>
