---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - features/publication-door.feature
  - tools/tests/test_publication_door_feature.py
  - crates/fava/tests/publication_door.rs
  - crates/fava/src/publication.rs
  - crates/fava/src/lib.rs
  - crates/fava/BUILD.bazel
  - docs/internals/vocabulary.toml
autonomous: true
requirements: [R1, R4, R9]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Edge 1: equivalent accepted edit publications have distinct WriteId and ReceiptId values while their semantic revision is idempotent."
    - "Edge 2: blocked downstream work exposes the complete accepted custody and local query contribution atomically, never a partial acceptance state."
    - "Applications publish unsigned events, pre-signed events, and replaceable edits through one synchronous Fava::publish payload door and receive Write."
  prohibitions:
    - descriptor: publish_returns_after_local_acceptance
      statement: "Publication cannot wait for routing, signing, transport, or acknowledgement before returning accepted local state."
  artifacts:
    - path: crates/fava/src/publication.rs
      provides: "Facade-owned PublishError, Write, sealed payload conversion, and synchronous payload acceptance"
      exports: [PublishError, Write]
    - path: crates/fava/tests/publication_door.rs
      provides: "Public three-payload and blocked-network tracer evidence"
    - path: features/publication-door.feature
      provides: "App-visible publication-door behavior and contrasts"
  key_links:
    - from: crates/fava/src/publication.rs
      to: crates/fava-publication/src/lib.rs
      via: "sealed payload conversion creates internal WriteIntent then calls Publication::accept synchronously"
      pattern: "\.accept\("
    - from: crates/fava/src/lib.rs
      to: crates/fava/src/publication.rs
      via: "Fava::publish delegates to the facade module and returns Write"
      pattern: "pub fn publish"
---

<objective>
Land the production-quality tracer for the universal synchronous publication door.

Purpose: Make the hardest cross-owner contract true first: a public payload enters durable custody, becomes query-visible, and returns a facade-owned Write before any external progress.
Output: R1/R4/R9 behavior text, red-first public evidence, and the first delivered `publish`/`Write` surface.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-SPEC.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-CONTEXT.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-RESEARCH.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-PATTERNS.md
@docs/issues/0013-edit-author-at-taker.md
@docs/issues/0014-publish-door-ergonomics.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Record and run the red public publication tracer</name>
  <read_first>
    @docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
    @crates/fava/src/lib.rs
    @crates/fava/tests/explicit_publication.rs
    @crates/fava/tests/semantic_write_publication.rs
  </read_first>
  <files>features/publication-door.feature, tools/tests/test_publication_door_feature.py, crates/fava/tests/publication_door.rs</files>
  <behavior>
    - Unsigned, pre-signed, and edit payloads compile at `fava.publish(payload)` without application `WriteIntent` construction.
    - With router and transport progress held behind deterministic gates, `publish` returns `Write` and a matching observation already contains the accepted event.
    - Two equivalent edit publications retain separate write/receipt identities.
  </behavior>
  <action>Write the R1 app-visible behavior and map each scenario to an exact integration test. Add the public integration tracer using existing deterministic provider gates and ordinary Fava assembly. Run `cargo test -p fava --test publication_door` before production edits and save the causal compiler/behavior failure in the execution summary. The fixture must create state through public operations and must not insert the expected result into stores.</action>
  <acceptance_criteria>The feature mapping resolves to real test names; the focused test is observed red for the absent `publish(payload) -> Write` behavior after its assembly preconditions succeed.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tools.tests.test_publication_door_feature &amp;&amp; ! cargo test -p fava --test publication_door</automated>
  </verify>
  <done>Behavior text and exact red executable evidence exist for all three payload forms, atomic visibility, and distinct identities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement synchronous payload acceptance and facade Write</name>
  <reversibility rating="costly">This changes the public Rust publication door used by every later capability, but the exact contract is already locked by the user and Phase 07.1 SPEC.</reversibility>
  <read_first>
    @crates/fava/src/lib.rs
    @crates/fava-publication/src/lib.rs
    @crates/fava-write/src/lib.rs
    @docs/internals/vocabulary.toml
  </read_first>
  <files>crates/fava/src/publication.rs, crates/fava/src/lib.rs, crates/fava/BUILD.bazel, docs/internals/vocabulary.toml, crates/fava/tests/publication_door.rs</files>
  <behavior>
    - `Fava::publish` accepts `UnsignedEvent`, `Event`, and `EventEdit` through a sealed conversion owned by the facade.
    - Successful acceptance returns `Write` containing stable `WriteId` and `ReceiptId` and access to the current receipt.
    - An unscoped edit with no current-account/runtime signer selection returns typed `PublishError` before custody; it never silently selects a signer.
  </behavior>
  <action>Create `publication.rs`; keep `WriteIntent`/`WritePayload` internal cross-crate values and expose no public conversion trait. Convert each payload to `WriteRouting::Automatic`, call the existing synchronous `Publication::accept`, and wrap the accepted identities plus cloneable `Publication` in `Write`. Add `PublishError` as the application-facing error while preserving attributed neutral owner errors. Register `Write` and `PublishError` with the seven-part vocabulary approval and executable falsifier. Keep accepted revision registration/visibility ordered before return. Make the red tracer green, then deliberately break the mechanism by moving the return behind a blocked router/transport gate; require `publish_returns_after_local_acceptance` to time out/fail, restore the exact source bytes, and rerun green.</action>
  <acceptance_criteria>`fava.publish` returns after durable local acceptance for all three payload forms, the local observation is current before any external progress, no protocol kind branch exists, and the named deliberate break is causal.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test publication_door &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check</automated>
  </verify>
  <done>The universal publication tracer is green through the public facade and its sync-return invariant fails under the named break.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public symbols: `fava::PublishError`, `fava::Write`, `Fava::publish`, `Write::write_id`, `Write::receipt_id`, `Write::receipt`.
- New private function family: sealed payload-to-`WriteIntent` conversion in `crates/fava/src/publication.rs`.
- New tests: `publication_door` integration cases and `test_publication_door_feature.py` mapping checks.
- New files: `features/publication-door.feature`, `tools/tests/test_publication_door_feature.py`, `crates/fava/tests/publication_door.rs`, `crates/fava/src/publication.rs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Application payload -> facade acceptance | Untrusted event/edit bytes and identity enter durable custody. |
| Write store commit -> application return | Partial or reordered acceptance could expose false durable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-071-01 | Tampering | sealed payload conversion | high | mitigate | Verify signed identity/event bounds and construct neutral intent before `Publication::accept`; prove invalid input leaves zero custody. |
| T-071-02 | Repudiation | accepted Write identity | high | mitigate | Return exact store-assigned `WriteId`/`ReceiptId` and correlate them to current local evidence in `publication_door.rs`. |
| T-071-03 | Denial of service | synchronous publish | medium | mitigate | Keep all external progress after return and gate the test with a deterministic deadline. |
</threat_model>

<verification>
Run the focused tracer, vocabulary checker and unit corpus; inspect the deliberate-break record for causal failure and byte-exact restoration.
</verification>

<success_criteria>
R1's synchronous publication outcome is observable through the public API and all Phase 07.1 work can build on a real `Write` value.
</success_criteria>

<output>
Create `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-01-SUMMARY.md` when done.
</output>
