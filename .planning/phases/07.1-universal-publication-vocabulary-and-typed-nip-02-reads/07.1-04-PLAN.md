---
phase: 07.1-universal-publication-vocabulary-and-typed-nip-02-reads
plan: 04
type: execute
wave: 4
depends_on: [07.1-03]
files_modified:
  - features/nip02-contact-lists.feature
  - tools/tests/test_nip02_contact_list_feature.py
  - crates/fava-nip02/src/contact_list.rs
  - crates/fava-nip02/src/lib.rs
  - crates/fava-nip02/src/tests.rs
  - crates/fava-nip02/src/tests/contact_list.rs
  - crates/fava-nip02/tests/public_api.rs
  - docs/internals/vocabulary.toml
autonomous: true
requirements: [R4, R5, R9]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Edge 12: missing, short, and extra `p` columns classify without shifting fields from one row into another."
    - "Edge 13: a valid kind-3 event containing no `p` rows decodes as a valid empty ContactList."
    - "Edge 14: invalid keys/relay hints become row evidence while present petname UTF-8 bytes remain exact and unnormalized."
    - "Edge 15: valid follows plus row evidence biject every source `p` index in source order."
  prohibitions:
    - descriptor: nip02_accounts_for_every_p_row
      statement: "Malformed, duplicate, or uninterpreted contact rows cannot disappear from the typed result."
  artifacts:
    - path: crates/fava-nip02/src/contact_list.rs
      provides: "Total event-level ContactList decoder with ordered valid rows and evidence"
      exports: [ContactList, Follow, ContactListRowEvidence, ContactListError]
    - path: features/nip02-contact-lists.feature
      provides: "App-visible typed read and no-silent-row-loss behavior"
    - path: crates/fava-nip02/tests/public_api.rs
      provides: "External exact public type/accessor compilation"
  key_links:
    - from: crates/fava-nip02/src/contact_list.rs
      to: fava_write::EventValue
      via: "event-level kind/id/signature/bounds validation before row parsing"
      pattern: "from_event"
---

<objective>
Decode kind-3 events into a typed `ContactList` that accounts for every contact row.

Purpose: Remove raw tag walking from applications without claiming malformed or duplicate rows never existed.
Output: R5 typed values/errors/evidence, app-visible behavior text, public accessors, and conservation tests.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-SPEC.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-RESEARCH.md
@crates/fava-nip02/README.md
@crates/fava-nip65/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify row conservation and add the red hostile-row matrix</name>
  <read_first>
    @docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
    @crates/fava-nip02/src/lib.rs
    @crates/fava-nip02/src/tests.rs
    @crates/fava-nip02/tests/public_api.rs
  </read_first>
  <files>features/nip02-contact-lists.feature, tools/tests/test_nip02_contact_list_feature.py, crates/fava-nip02/src/tests.rs, crates/fava-nip02/src/tests/contact_list.rs</files>
  <behavior>
    - Valid empty, single, multiple, duplicate, malformed-key, malformed-relay, short, extra-column, mixed, and wrong-kind cases are distinct.
    - Source indices for all `p` rows are conserved exactly once across valid follows and evidence.
    - Absent petname and present-empty petname remain distinguishable; no Unicode normalization occurs.
  </behavior>
  <action>Write the durable app behavior and exact test mapping, then add the table/property-style hostile row corpus in cohesive `src/tests/contact_list.rs` before production types; keep `src/tests.rs` as a small module registry and every code file below 500 lines. Construct finalized signed events through public builders. Include cases where an invalid relay precedes a later valid row for the same key so invalid input cannot reserve the duplicate set. Run the focused tests red for the absent decoder after proving fixtures validate.</action>
  <acceptance_criteria>Every edge row has an exact expected classification/source index/raw row; the red failure occurs at the decoder boundary after valid event setup.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tools.tests.test_nip02_contact_list_feature &amp;&amp; ! cargo test -p fava-nip02 contact_list</automated>
  </verify>
  <done>The smallest owner has executable red evidence for total row accounting and event-level refusal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement immutable ContactList values and complete row evidence</name>
  <reversibility rating="costly">These public protocol values become the README application vocabulary and must preserve the locked row-evidence distinction.</reversibility>
  <read_first>
    @crates/fava-nip65/src/lib.rs
    @crates/fava-nip02/src/bounds.rs
    @docs/internals/vocabulary.toml
  </read_first>
  <files>crates/fava-nip02/src/contact_list.rs, crates/fava-nip02/src/lib.rs, crates/fava-nip02/src/tests/contact_list.rs, crates/fava-nip02/tests/public_api.rs, docs/internals/vocabulary.toml</files>
  <behavior>
    - `ContactList::from_event` validates the event boundary once; row failures are `ContactListRowEvidence` values.
    - A target enters `seen` only after the entire first row is valid.
    - Accessors return author, ordered follows, evidence, supersession, pubkey, optional relay and optional petname without mutable/shared state.
  </behavior>
  <action>Implement the parser in its own module under existing source/event/tag bounds. Retain source index and an owned exact raw row in every evidence variant; distinguish missing target, invalid key, invalid non-empty relay, later valid duplicate, and uninterpreted extra columns. Preserve empty relay as no hint, absent versus present-empty petname, and source order. Register only the approved nouns with all vocabulary fields. Deliberately replace the total parser with `filter_map` over valid rows; require `nip02_accounts_for_every_p_row` to fail its conservation bijection, restore exact bytes, and rerun.</action>
  <acceptance_criteria>All valid and hostile fixtures pass, public accessors compile externally, each `p` row is conserved once, and the row-dropping deliberate break fails causally.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-nip02 contact_list &amp;&amp; cargo test -p fava-nip02 --test public_api &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check</automated>
  </verify>
  <done>Applications can parse any valid kind-3 document without raw tags or silent row loss.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public types: `ContactList`, `Follow`, `ContactListRowEvidence`, `ContactListError`.
- New public functions/accessors: `ContactList::from_event`, `ContactList::author`, `ContactList::follows`, `ContactList::evidence`, `ContactList::supersedes`, `Follow::pubkey`, `Follow::relay`, `Follow::petname`, and evidence source-index/raw-row accessors or enum fields.
- New tests: `contact_list` hostile-row/conservation matrix in `crates/fava-nip02/src/tests/contact_list.rs` and external public API bindings.
- New files: `features/nip02-contact-lists.feature`, `tools/tests/test_nip02_contact_list_feature.py`, `crates/fava-nip02/src/contact_list.rs`, `crates/fava-nip02/src/tests/contact_list.rs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Signed kind-3 event -> typed list | Hostile tag structure and encodings enter protocol interpretation. |
| Raw shared-document row -> duplicate accounting | Invalid rows must not poison later valid classification or vanish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-071-10 | Tampering | `ContactList::from_event` | high | mitigate | Verify event-level kind/id/signature/bounds once and conserve each p-row with raw evidence. |
| T-071-11 | Denial of service | tag parser | high | mitigate | Reuse structural/tag/event bounds and refuse event-level overflow without truncation. |
| T-071-12 | Repudiation | malformed rows | medium | mitigate | Preserve source index and raw row for every evidence item. |
</threat_model>

<verification>
Run focused decoder/public API/vocabulary suites and inspect the filter-map deliberate-break record.
</verification>

<success_criteria>
R5 is complete: valid follows are ergonomic and every non-valid contact row remains exact typed evidence.
</success_criteria>

<output>
Create `.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-04-SUMMARY.md` when done.
</output>
