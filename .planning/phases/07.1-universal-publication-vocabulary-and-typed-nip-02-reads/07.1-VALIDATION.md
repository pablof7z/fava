---
phase: 07.1
slug: universal-publication-vocabulary-and-typed-nip-02-reads
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-22
---

# Phase 07.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust unit/integration/doctest and compile-fail tests; Python vocabulary tests; Cargo-owned downstream canary; repository Bazel checks remain separate |
| **Config file** | root `Cargo.toml`, `apps/canary/Cargo.toml`, `apps/canary/scenarios.json`, `docs/internals/vocabulary.toml` |
| **Quick run command** | `cargo test -p fava-nip02 --all-targets && cargo test -p fava --all-targets` |
| **Full suite command** | `cargo test --workspace && cargo test --manifest-path apps/canary/Cargo.toml --all-targets && python3 tools/check_vocabulary.py && python3 -m unittest tools.tests.test_vocabulary_check && bazel test //...` followed by the exact two-run Cargo canary command below |
| **Estimated runtime** | ~900 seconds including controlled relay process tests |

## Sampling Rate

- **After every task commit:** Run the owning crate's focused test command and vocabulary checks for public/API changes.
- **After every plan wave:** Run `cargo test --workspace` plus all stable prohibition descriptors introduced by that wave.
- **Before `$gsd-verify-work`:** Full suite, source/dependency-negative checks, and two isolated controlled Croissant canary runs must be green.
- **Max feedback latency:** 120 seconds for focused tests; live-relay evidence is a wave/phase gate.

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 07.1-R1 | 01 | 0 | R1 | T-071-01 | Acceptance remains atomic and network-independent | integration | `cargo test -p fava --test publication_door` | ✅ | ✅ green |
| 07.1-R2 | 02 | 1 | R2 | T-071-02 | Invalid/dropped scopes open no work; routes bounded | compile + integration | `cargo test -p fava --test publication_scopes` | ✅ | ✅ green |
| 07.1-R3 | 03 | 2 | R3 | T-071-03 | Never-satisfied predicates terminate with complete evidence | unit + integration | `cargo test -p fava --test write_settlement` | ✅ | ✅ green |
| 07.1-R4 | 06 | 3 | R4 | T-071-04 | Protocol crate has no lifecycle/provider authority | architecture | `cargo test -p fava-nip02 --test architecture` | ✅ | ✅ green |
| 07.1-R5 | 04 | 3 | R5 | T-071-05 | Every hostile/malformed row remains bounded evidence | unit | `cargo test -p fava-nip02 contact_list` | ✅ | ✅ green |
| 07.1-R6 | 05 | 4 | R6 | Empty/exact axes never broaden acquisition | unit + integration | `cargo test -p fava-nip02 query` | ✅ | ✅ green |
| 07.1-R7 | 06 | 5 | R7 | Foreign shared-document bytes cannot be lost | unit + public API | `cargo test -p fava-nip02 edit` | ✅ | ✅ green |
| 07.1-R8 | 10 | 7 | R8 | Child relay is bounded, ephemeral, exact-write correlated, seed-redacted and independently repeated | Cargo process integration | `cargo test --manifest-path apps/canary/Cargo.toml croissant_nip02 && cargo test --manifest-path apps/canary/Cargo.toml croissant::` plus the two-run command below | ✅ | ✅ green |
| 07.1-R9 | 11 | 8 | R9 | No unapproved or compatibility vocabulary survives | architecture | `cargo test -p fava-nip02 --doc && cargo test -p fava-nip02 --test public_api && python3 tools/check_vocabulary.py && python3 -m unittest tools.tests.test_vocabulary_check` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

## Wave 0 Requirements

- [x] `crates/fava/tests/publication_door.rs` — synchronous acceptance, three payload forms, immediate local visibility.
- [x] `crates/fava/tests/publication_scopes.rs` plus compile fixtures — composition, signer misuse, inertness, route normalization/bounds.
- [x] `crates/fava/tests/write_settlement.rs` — threshold, zero refusal, races, terminal receipt evidence.
- [x] `crates/fava-nip02/tests/architecture.rs` — dependency/lifecycle/kind-blind checks.
- [x] NIP-02 decoder/query/edit fixture modules under `crates/fava-nip02/src/tests/` — total row evidence, exact queries, byte preservation, no raw-input logging, and every code file below 500 lines.
- [x] Stable executable checks for all eight `07.1-SPEC.md` prohibitions.
- [x] Redb ordered-route restart fixture.
- [x] Cargo-owned `apps/canary` Croissant supervisor, `croissant_nip02` tests, CLI scenario, pair verifier, and persistent `apps/canary/runs/` evidence contract.
- [x] Source/API check forbidding the old facade `WriteIntent` door and compatibility aliases.

## Manual-Only Verifications

All phase behaviors are automated through Cargo. `apps/canary` intentionally has no Bazel package/target; repository Bazel checks remain independent build-system controls rather than a false canary claim.

## Exact Two-Run Croissant Gate

The final automated verification executes both real-relay runs and the pair verifier in one shell chain. It creates a persistent unique pair root under `apps/canary/runs/`; nothing deletes that root. Raw seeds remain shell/process-memory values and are unset after use. The canary may retain only `scenario_seed_sha256`, public group coordinates, and author pubkey—never either raw seed or private key.

`mkdir -p apps/canary/runs && pair_root="$(mktemp -d apps/canary/runs/phase-07.1-pair.XXXXXX)" && seed_one="$(openssl rand -hex 32)" && seed_two="$(openssl rand -hex 32)" && test "$seed_one" != "$seed_two" && cargo run --quiet --manifest-path apps/canary/Cargo.toml -- run croissant-nip02-public-flow --relay-bin /Users/pablofernandez/Work/croissant/croissant --seed "$seed_one" --runs-dir "$pair_root" && cargo run --quiet --manifest-path apps/canary/Cargo.toml -- run croissant-nip02-public-flow --relay-bin /Users/pablofernandez/Work/croissant/croissant --seed "$seed_two" --runs-dir "$pair_root" && unset seed_one seed_two && cargo run --quiet --manifest-path apps/canary/Cargo.toml -- verify-croissant-pair --runs-dir "$pair_root"`

`verify-croissant-pair` requires exactly two completed manifests beneath that pair root and fails unless: `scenario_seed_sha256`, group, event, write and receipt identities differ; run two excludes run-one group/event data; executable/source/artifact hashes and byte/count/time bounds exist and validate; exact foreign tags/content and terminal receipt correlations hold; both PID/port teardown facts prove no live child remains; and neither manifest/artifact/log/error contains a raw seed/private-key field or sentinel value.

## Executed Phase Sign-Off

The full gate passed on 2026-08-22:

| Gate | Result |
|---|---|
| `cargo test --workspace` | green |
| `cargo test -p fava-nip02 --all-targets` | green: 24 unit, 2 architecture, 1 public-API test |
| `cargo test -p fava-nip02 --doc` | green: 3 compile-fail doctests |
| `cargo test --manifest-path apps/canary/Cargo.toml --all-targets` | green: 31 tests |
| `python3 tools/check_vocabulary.py` | green |
| `python3 -m unittest tools.tests.test_vocabulary_check` | green: 6 tests |
| `bazel test //...` | green: 42 targets |
| `bazel test //... --config=clippy` | green: 42 targets |
| `bazel test //... --config=fmt-check` | green: 42 targets |
| `cargo fmt --all -- --check` and `git diff --check` | green |

The retained sign-off root is `apps/canary/runs/phase-07.1-pair.1gstDm`. `verify-croissant-pair` passed over exactly these two manifests after all review fixes and full gates:

| Run | Seed hash | Group | Event | Write / receipt | Revisions | Teardown |
|---|---|---|---|---|---|---|
| `croissant-nip02-public-flow-53cf65a8eb03983a` | `ed36e165cb3c6ed7ee0ce7abd78bf7a8d5e49e7e9de8de60dbda5183c61d653e` | `25aa6ec3e669e74f0ede917946467388` | `97b4101e9c4515cc212e52a9c493bca434d7ea4bf9af914971e516db743b41d2` | `croissant-nip02-public-flow-53cf65a8eb03983a:3` | local 11, relay 15 | PID 42502 reaped; `127.0.0.1:61876` closed |
| `croissant-nip02-public-flow-627e7f30a12ddacc` | `2b45f87e7ee69dc24599ee0a756fa206e65c17bb9f9fa9df27ca1898bd7102c3` | `3385697b39de2b527980f1920c73c22f` | `2a2fa7a560c98a398b19c5d241785a64f89450f303443c5150bfb3f7afcb7198` | `croissant-nip02-public-flow-627e7f30a12ddacc:3` | local 11, relay 15 | PID 42583 reaped; `127.0.0.1:61896` closed |

Both manifests record `secret_scan_passed: true`, `old_data_excluded: true`, exact foreign tags/content, terminal receipt correlations, complete artifact hashes, 1 MiB log/wire limits, a 30-second operation limit, and completed teardown. Each artifact seal now signs the complete canonical manifest claim set as well as its artifact hashes; direct mutation negatives cover every identity, completion, terminal, bounds, and teardown claim consumed by pair verification. Secret scanning covers 19 exact classes: the seed plus raw, lowercase/uppercase hex, lowercase/uppercase `nsec`, and every supported lowercase/uppercase NIP-21 scheme/payload form for both derived keys. Generation-stage negatives inject secret material immediately before the scanner both before sealing and after manifest construction. The binary hash is `6dbf7e868641f1c763bdccb4b66a53f15d419940f4af862549dc57a61ba625bb`; Croissant source HEAD is `9c4c93e84852bd9aa6824060b74c56ab2ce812c2`. Its checkout retained the pre-existing untracked `docs/` only and was not modified. The final port/PID probes were closed/absent.

The two superseded sign-off roots, `phase-07.1-pair.dapxpz` and `phase-07.1-pair.nrSPUU`, are preserved recoverably beneath `apps/canary/runs/superseded/2026-08-22-review-iteration-2/`. The strengthened verifier rejects them respectively for an omitted seal and for a seal that does not cover signed manifest claims; neither is retained as current evidence.

All eight deliberate breaks failed causally and their owning sources were restored byte-exactly before their green controls:

| Prohibition break | Owning red evidence | Restoration evidence |
|---|---|---|
| publication waits for settlement | `publish_returns_after_local_acceptance` timed out | `publication.rs` restored to pre-mutation SHA `de420b0…`; control green |
| invalid scopes acquire custody | `publication_scopes_are_inert_before_valid_payload` failed | `publication.rs` restored to `de420b0…`; control green |
| terminal refusal erases receipt facts | `terminal_not_reached_preserves_every_destination_fact` failed | `publication.rs` restored to `de420b0…`; control green |
| malformed NIP-02 rows disappear | `nip02_accounts_for_every_p_row` failed | `contact_list.rs` restored to SHA `e5e797f…`; control green |
| protocol crate owns lifecycle/provider state | `nip02_remains_engine_provider_free` failed | manifest SHA `f6fb54d…` and lockfile restored; control green |
| foreign kind-3 bytes are discarded | `nip02_preserves_foreign_kind3_bytes` failed | edit source restored to SHA `893604e…`; control green |
| compatibility publication door returns | `facade_has_no_write_intent_compatibility_door` failed | facade source restored to SHA `157ecf0…`; control green |
| universal owner branches on kind 3 | `universal_publication_is_kind_blind` and kind-9007 control failed | `publication.rs` restored to `de420b0…`; both controls green |

`git worktree list` was refreshed before and after the run. The main checkout remained on `main`; the tag-filter, dirty `/Users/pablofernandez/Work/nnn-m8`, and NIP-02 README worktrees retained their original branches and were not modified.

## Validation Sign-Off

- [x] All tasks have automated verification or explicit Wave 0 dependencies.
- [x] Sampling continuity: no three consecutive tasks without automated verification.
- [x] Wave 0 covers every missing reference above.
- [x] Named deliberate breaks fail their owning evidence checks.
- [x] No watch-mode flags.
- [x] Focused feedback latency stays below 120 seconds.
- [x] The exact two-run Cargo command passes, retains its pair root under `apps/canary/runs/`, and the pair verifier proves distinct identities, old-data exclusion, hashes/bounds, seed redaction, and teardown.
- [x] `nyquist_compliant: true` and `wave_0_complete: true` set in frontmatter.

**Approval:** executable evidence complete
