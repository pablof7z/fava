---
phase: 07.1
slug: universal-publication-vocabulary-and-typed-nip-02-reads
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-22
---

# Phase 07.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust unit/integration/doctest and compile-fail tests; Python vocabulary tests; Cargo-owned downstream canary; repository Bazel checks remain separate |
| **Config file** | root `Cargo.toml`, `apps/canary/Cargo.toml`, `apps/canary/scenarios.json`, `docs/internals/vocabulary.toml` |
| **Quick run command** | `cargo test -p fava-nip02 --all-targets && cargo test -p fava --all-targets` |
| **Full suite command** | `cargo test --workspace && cargo test --manifest-path apps/canary/Cargo.toml --all-targets && python3 tools/check_vocabulary.py && python3 -m unittest tools.tests.test_vocabulary_check && bazel test //...` followed by the exact two-run Cargo canary command below |
| **Estimated runtime** | ~900 seconds including controlled relay process tests |

## Sampling Rate

- **After every task commit:** Run the owning crate's focused test command and vocabulary checks for public/API changes.
- **After every plan wave:** Run `cargo test --workspace` plus all stable prohibition descriptors introduced by that wave.
- **Before `$gsd-verify-work`:** Full suite, source/dependency-negative checks, and two isolated controlled Croissant canary runs must be green.
- **Max feedback latency:** 120 seconds for focused tests; live-relay evidence is a wave/phase gate.

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 07.1-W0-R1 | TBD | 0 | R1 | T-071-01 | Acceptance remains atomic and network-independent | integration | `cargo test -p fava --test publication_door` | ❌ W0 | ⬜ pending |
| 07.1-W0-R2 | TBD | 0 | R2 | T-071-02 | Invalid/dropped scopes open no work; routes bounded | compile + integration | `cargo test -p fava --test publication_scopes` | ❌ W0 | ⬜ pending |
| 07.1-W0-R3 | TBD | 0 | R3 | T-071-03 | Never-satisfied predicates terminate with complete evidence | unit + integration | `cargo test -p fava --test write_settlement` | ❌ W0 | ⬜ pending |
| 07.1-W0-R4 | TBD | 0 | R4 | T-071-04 | Protocol crate has no lifecycle/provider authority | architecture | `cargo test -p fava-nip02 --test architecture` | ❌ W0 | ⬜ pending |
| 07.1-W0-R5 | TBD | 0 | R5 | T-071-05 | Every hostile/malformed row remains bounded evidence | unit | `cargo test -p fava-nip02 contact_list` | ❌ W0 | ⬜ pending |
| 07.1-W0-R6 | TBD | 0 | R6 | Empty/exact axes never broaden acquisition | unit + integration | `cargo test -p fava-nip02 query` | ❌ W0 | ⬜ pending |
| 07.1-W0-R7 | TBD | 0 | R7 | Foreign shared-document bytes cannot be lost | unit + public API | `cargo test -p fava-nip02 edit` | ⚠ partial | ⬜ pending |
| 07.1-W0-R8 | TBD | 0 | R8 | Child relay is bounded, ephemeral, exact-write correlated, seed-redacted and independently repeated | Cargo process integration | `cargo test --manifest-path apps/canary/Cargo.toml croissant_nip02 && cargo test --manifest-path apps/canary/Cargo.toml croissant::` plus the two-run command below | ❌ W0 | ⬜ pending |
| 07.1-W0-R9 | TBD | 0 | R9 | No unapproved or compatibility vocabulary survives | architecture | `python3 tools/check_vocabulary.py && python3 -m unittest tools.tests.test_vocabulary_check` | ⚠ partial | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

## Wave 0 Requirements

- [ ] `crates/fava/tests/publication_door.rs` — synchronous acceptance, three payload forms, immediate local visibility.
- [ ] `crates/fava/tests/publication_scopes.rs` plus compile fixtures — composition, signer misuse, inertness, route normalization/bounds.
- [ ] `crates/fava/tests/write_settlement.rs` — threshold, zero refusal, races, terminal receipt evidence.
- [ ] `crates/fava-nip02/tests/architecture.rs` — dependency/lifecycle/kind-blind checks.
- [ ] NIP-02 decoder/query/edit fixture modules under `crates/fava-nip02/src/tests/` — total row evidence, exact queries, byte preservation, no raw-input logging, and every code file below 500 lines.
- [ ] Stable executable checks for all eight `07.1-SPEC.md` prohibitions.
- [ ] Redb ordered-route restart fixture.
- [ ] Cargo-owned `apps/canary` Croissant supervisor, `croissant_nip02` tests, CLI scenario, pair verifier, and persistent `apps/canary/runs/` evidence contract.
- [ ] Source/API check forbidding the old facade `WriteIntent` door and compatibility aliases.

## Manual-Only Verifications

All phase behaviors are automated through Cargo. `apps/canary` intentionally has no Bazel package/target; repository Bazel checks remain independent build-system controls rather than a false canary claim.

## Exact Two-Run Croissant Gate

The final automated verification executes both real-relay runs and the pair verifier in one shell chain. It creates a persistent unique pair root under `apps/canary/runs/`; nothing deletes that root. Raw seeds remain shell/process-memory values and are unset after use. The canary may retain only `scenario_seed_sha256`, public group coordinates, and author pubkey—never either raw seed or private key.

`mkdir -p apps/canary/runs && pair_root="$(mktemp -d apps/canary/runs/phase-07.1-pair.XXXXXX)" && seed_one="$(openssl rand -hex 32)" && seed_two="$(openssl rand -hex 32)" && test "$seed_one" != "$seed_two" && cargo run --manifest-path apps/canary/Cargo.toml -- run croissant-nip02-public-flow --relay-bin /Users/pablofernandez/Work/croissant/croissant --seed "$seed_one" --runs-dir "$pair_root" && cargo run --manifest-path apps/canary/Cargo.toml -- run croissant-nip02-public-flow --relay-bin /Users/pablofernandez/Work/croissant/croissant --seed "$seed_two" --runs-dir "$pair_root" && unset seed_one seed_two && cargo run --manifest-path apps/canary/Cargo.toml -- verify-croissant-pair --runs-dir "$pair_root"`

`verify-croissant-pair` requires exactly two completed manifests beneath that pair root and fails unless: `scenario_seed_sha256`, group, event, write and receipt identities differ; run two excludes run-one group/event data; executable/source/artifact hashes and byte/count/time bounds exist and validate; exact foreign tags/content and terminal receipt correlations hold; both PID/port teardown facts prove no live child remains; and neither manifest/artifact/log/error contains a raw seed/private-key field or sentinel value.

## Validation Sign-Off

- [ ] All tasks have automated verification or explicit Wave 0 dependencies.
- [ ] Sampling continuity: no three consecutive tasks without automated verification.
- [ ] Wave 0 covers every missing reference above.
- [ ] Named deliberate breaks fail their owning evidence checks.
- [ ] No watch-mode flags.
- [ ] Focused feedback latency stays below 120 seconds.
- [ ] The exact two-run Cargo command passes, retains its pair root under `apps/canary/runs/`, and the pair verifier proves distinct identities, old-data exclusion, hashes/bounds, seed redaction, and teardown.
- [ ] `nyquist_compliant: true` and `wave_0_complete: true` set in frontmatter.

**Approval:** pending execution
