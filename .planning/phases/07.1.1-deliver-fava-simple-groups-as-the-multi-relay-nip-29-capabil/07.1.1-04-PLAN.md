---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 04
type: execute
wave: 2
depends_on: [07.1.1-01, 07.1.1-02, 07.1.1-03]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/fava-simple-groups/Cargo.toml
  - crates/fava-simple-groups/BUILD.bazel
  - crates/fava-simple-groups/src/lib.rs
  - crates/fava-simple-groups/src/bounds.rs
  - crates/fava-simple-groups/src/group.rs
  - crates/fava-simple-groups/src/query.rs
  - crates/fava-simple-groups/src/tests.rs
  - crates/fava-simple-groups/src/tests/group.rs
  - crates/fava-simple-groups/src/tests/query.rs
  - crates/fava-simple-groups/tests/public_api.rs
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-07, GROUP-08, GROUP-11]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Group is the approved generalized identity: one bounded opaque id plus one bounded non-empty host set; one host is one cardinality of Group, not a separate required field."
    - "Duplicate and adjacent hosts normalize by first occurrence, exact RelayUrl value and deterministic order; empty/oversized hosts and empty/oversized ids refuse without truncation."
    - "Content queries append exact lowercase h, preserve every unrelated Query field, acquire from every host with AnyLocal result authority, and reject any pre-existing lowercase h axis including present-empty."
    - "Record queries use one ordinary multi-kind Query, exact lowercase d, OnlyRelays authority, deterministic selected-kind order, and present-empty selections match nothing."
    - "Unsigned preparation appends h only when absent and conserves tag/content/order; exact existing h is inert; missing-value, duplicate, or contradictory h refuses."
    - "Verified signed input remains byte-identical and invalid context refuses before custody or wire work."
  prohibitions:
    - status: flagged-unverified
      statement: "The capability never mutates a signed event or admits ambiguous group context."
    - status: flagged-unverified
      statement: "The capability owns no group lifecycle and its normal dependencies are exactly fava-query, fava-state, and fava-write."
  artifacts:
    - path: crates/fava-simple-groups/src/group.rs
      provides: "Bounded Group identity and pure group-context preparation"
      exports: [Group, GroupError]
    - path: crates/fava-simple-groups/src/query.rs
      provides: "Ordinary content and record Query lowering"
      exports: [GroupRecords]
    - path: crates/fava-simple-groups/tests/public_api.rs
      provides: "External one-host/multi-host public compile tracer"
  key_links:
    - from: crates/fava-simple-groups/src/group.rs
      to: crates/fava-write/src/routing.rs
      via: "construction reuses exact explicit-route normalization/bound semantics without owning routing"
      pattern: "WriteRouting::explicit"
    - from: crates/fava-simple-groups/src/query.rs
      to: crates/fava-query/src/lib.rs
      via: "exact h/d axes plus from_relays/only_from_relays"
      pattern: "from_relays|only_from_relays"
---

## Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Deliver the production-quality one-host `Group` tracer and its multi-host cardinality through pure construction, ordinary queries, and context preparation.

Purpose: Establish the public README spine before expanding parsers, projections, discovery, edits, facade execution, and canary evidence.
Output: Registered crate, approved core exports, exact Group/query/preparation tests, and external public compilation.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/issues/0019-simple-groups.md
@crates/fava-simple-groups/README.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-CONTEXT.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-PATTERNS.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-01-SUMMARY.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one Group through construction and exact ordinary queries</name>
  <reversibility rating="costly">The approved public Group identity and GroupRecords selector become the README/API contract; issue 0019 already approves these nouns.</reversibility>
  <read_first>
    @docs/spec/ARCHITECTURE.md
    @docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
    @crates/fava-nip02/Cargo.toml
    @crates/fava-nip02/BUILD.bazel
    @crates/fava-nip02/src/lib.rs
    @crates/fava-query/src/lib.rs
    @crates/fava-write/src/routing.rs
  </read_first>
  <files>Cargo.toml, Cargo.lock, crates/fava-simple-groups/Cargo.toml, crates/fava-simple-groups/BUILD.bazel, crates/fava-simple-groups/src/lib.rs</files>
  <behavior>
    - One-host and multi-host construction use one Group value.
    - Duplicate hosts retain first-occurrence order; empty and over-bound inputs refuse typed.
    - The external app compiles one content Query and one all-record Query without a facade dependency in the capability.
  </behavior>
  <action>Create the exact crate/package/Bazel targets and external `public_api` tracer first, recording RED because no crate/API exists. Register the root workspace member/dependency once. Declare normal dependencies exactly `fava-query`, `fava-state`, and `fava-write`; any additional test support is dev-only. Export only issue-0019 approved nouns, initially those implemented by this plan. Do not expose a payload trait or another host/route wrapper. GREEN only when external one-host and multi-host construction/query code compiles. Deliberately add a normal facade dependency, require the architecture/public target to fail, restore the manifest byte-exactly, and rerun.</action>
  <acceptance_criteria>The crate is a real Cargo/Bazel library with exact neutral dependencies, the generalized Group identity compiles externally, and no empty framework or compatibility surface is exported.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --test public_api</automated>
  </verify>
  <done>An external application can construct one or several hosts as the same Group value and obtain ordinary Query descriptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded Group identity and content/record lowering</name>
  <read_first>
    @crates/fava-nip02/src/bounds.rs
    @crates/fava-query/src/selection.rs
    @crates/fava-query/src/lib.rs
    @crates/fava-write/src/routing.rs
  </read_first>
  <files>crates/fava-simple-groups/src/bounds.rs, crates/fava-simple-groups/src/group.rs, crates/fava-simple-groups/src/query.rs, crates/fava-simple-groups/src/tests.rs, crates/fava-simple-groups/src/tests/group.rs</files>
  <behavior>
    - Host iterators consume at most the universal route bound plus one, normalize duplicates by first occurrence, and refuse empty/oversized input.
    - Group id is byte-preserving, non-empty, bounded, and never trimmed/normalized.
    - `events` refuses any existing lowercase h axis, including present-empty, preserves other fields, adds exact h, and uses `from_relays`.
    - `records` uses Query::kinds for 39000-39005, exact d, and `only_from_relays`.
  </behavior>
  <action>Write `group_construction_bounds_and_order`, `content_query_preserves_any_local_visibility`, and `record_query_uses_exact_relay_authority` RED fixtures before implementation. Reuse `WriteRouting::explicit` for host normalization/maximum rather than copying its private bound, map neutral errors into approved `GroupError`, and keep Group immutable. Use exact lowercase tag keys and the complete host slice. Cover adjacent duplicates, empty/present-empty axes, reverse construction, and deterministic output. After GREEN, deliberately swap content to `only_from_relays` and records to `from_relays`; require their exact owning descriptors to fail, restore checksummed bytes, and rerun.</action>
  <acceptance_criteria>Group construction and both query families satisfy every adjacency/empty/ordering probe, and the two source-policy mutations fail causally.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups group_construction_bounds_and_order &amp;&amp; cargo test -p fava-simple-groups content_query_preserves_any_local_visibility &amp;&amp; cargo test -p fava-simple-groups record_query_uses_exact_relay_authority</automated>
  </verify>
  <done>The pure Group value emits exact ordinary content/record queries for one or many hosts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prepare unsigned and signed arbitrary events before custody</name>
  <read_first>
    @crates/fava-write/src/lib.rs
    @crates/fava-write/src/builder.rs
    @crates/fava-nip02/src/contact_list.rs
    @crates/fava-simple-groups/src/group.rs
  </read_first>
  <files>crates/fava-simple-groups/src/group.rs, crates/fava-simple-groups/src/tests.rs, crates/fava-simple-groups/src/tests/group.rs, crates/fava-simple-groups/tests/public_api.rs</files>
  <behavior>
    - Unsigned absent h appends one exact row after all existing tags and preserves author/kind/time/content/order.
    - One matching h row, including extension columns, remains byte-for-byte in place.
    - Missing-value, present-empty, duplicate adjacent, and contradictory h rows refuse distinctly.
    - Signed valid input verifies and returns the exact original bytes/id/signature; every invalid context refuses before any Fava call.
  </behavior>
  <action>Add exact unsigned/signed matrix tests first, record RED at the missing pure preparation surface, then implement a private sealed input bound if one method supports both payload forms. Use existing `EventBuilder::from_parts`, event verification, and approved values; do not expose signing, routing, custody, or a new payload noun. Scan exact lowercase h before rebuilding unsigned values; never rebuild signed values. After GREEN, record the source checksum and deliberately rebuild a signed event plus admit duplicate h; require byte-identity and ambiguity descriptors to fail, restore byte-exactly, and rerun.</action>
  <acceptance_criteria>Arbitrary kinds prepare correctly, every malformed/ambiguous context is typed, signed input is exact, and both named breaks fail without wire/custody work.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups group_context_preparation &amp;&amp; cargo clippy -p fava-simple-groups --all-targets -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; git diff --check</automated>
  </verify>
  <done>Group prepares or refuses every supported payload entirely before universal custody.</done>
</task>

</tasks>

## Artifacts this phase produces

- New crate files: `Cargo.toml`, `BUILD.bazel`, `src/lib.rs`, `src/bounds.rs`, `src/group.rs`, `src/query.rs`, `src/tests.rs`, `src/tests/group.rs`, `src/tests/query.rs`, `tests/public_api.rs` under `crates/fava-simple-groups/`.
- New public symbols: `Group`, `GroupRecords`, `GroupError`, `Group::on`, `Group::hosts`, `Group::id`, `Group::events`, `Group::records`, and pure `Group::prepare` surface using existing payload values.
- New workspace registration in root `Cargo.toml`/`Cargo.lock` and Bazel targets `lib`, `unit_tests`, `public_api`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host/id iterators -> Group | Caller-controlled size, duplicates, URLs, and opaque bytes enter public state. |
| Unsigned/signed event -> group preparation | Hostile tags and cryptographic identity cross toward custody. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-09 | Denial of service | Group construction | high | mitigate | Consume bound+1, reuse explicit-route owner bound, typed refusal, no truncation. |
| T-0711-10 | Tampering | signed context preparation | high | mitigate | Existing signature verification, exact one matching h, return original event bytes. |
| T-0711-11 | Spoofing | record source policy | high | mitigate | OnlyRelays plus exact selected-host evidence; no saved URL/proposed host attribution. |
</threat_model>

<verification>
Run all crate/public tests, strict Clippy, formatting, vocabulary, line-limit, manifest dependency-equality, and diff checks; retain causal source-policy/signed-mutation evidence.
</verification>

<success_criteria>
GROUP-01/02/03/07/08 foundations compile and pass purely, with one generalized Group identity and no lifecycle/dependency violation.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-04-SUMMARY.md` when done.
</output>
