---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 04
type: execute
wave: 2
depends_on: [07.1.1-01, 07.1.1-02, 07.1.1-03]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/fava-simple-groups/Cargo.toml
  - crates/fava-simple-groups/BUILD.bazel
  - crates/fava-simple-groups/src/lib.rs
  - crates/fava-simple-groups/src/group.rs
  - crates/fava-simple-groups/src/query.rs
  - crates/fava-simple-groups/tests/public_api.rs
  - crates/fava-simple-groups/tests/architecture.rs
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-07, GROUP-11]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The primary identity is Group with a bounded non-empty host set; one host is one cardinality, not a second required field or alternate identity."
    - "The crate's normal dependencies are exactly fava-query, fava-state, and fava-write in Cargo and Bazel, proven by an executable architecture target owned by this plan."
    - "A narrow production path constructs one Group, prepares one unsigned event, and creates ordinary content and record Queries without owning publication or acquisition lifecycle."
    - "The public surface uses the approved Group, GroupError, and GroupRecords vocabulary and no unapproved nominal type."
  prohibitions:
    - status: flagged-unverified
      statement: "The capability does not own group lifecycle, observation, subscription, routing, signing, publication, storage, runtime, or transport."
    - status: flagged-unverified
      statement: "No canonical host, field merge, existence, membership, absence, or completeness claim is introduced."
  artifacts:
    - path: crates/fava-simple-groups/Cargo.toml
      provides: "Exact three-dependency capability manifest"
    - path: crates/fava-simple-groups/tests/architecture.rs
      provides: "Executable exact-dependency falsifier"
    - path: crates/fava-simple-groups/src/group.rs
      provides: "Production Group identity and pure preparation tracer"
      exports: ["Group", "GroupError"]
    - path: crates/fava-simple-groups/src/query.rs
      provides: "Ordinary content and GroupRecords Query tracer"
      exports: ["GroupRecords"]
  key_links:
    - from: crates/fava-simple-groups/src/group.rs
      to: crates/fava-write
      via: "Group::prepare returns the existing unsigned/signed event values without custody"
      pattern: "prepare"
    - from: crates/fava-simple-groups/src/query.rs
      to: crates/fava-query
      via: "ordinary Query builders only"
      pattern: "Query"
---

## Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Establish the production capability tracer and its exact dependency boundary.

Purpose: prove the approved Group identity and one real preparation/query path before edge expansion, while making the crate's forbidden dependency surface executable in the same plan.
Output: registered `fava-simple-groups`, exact Cargo/Bazel manifests, public Group/GroupError/GroupRecords tracer, and architecture evidence.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/issues/0019-simple-groups.md
@crates/fava-simple-groups/README.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-CONTEXT.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-RESEARCH.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-PATTERNS.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-01-SUMMARY.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Register the crate behind an exact executable dependency boundary</name>
  <read_first>
    @Cargo.toml
    @crates/fava-query/Cargo.toml
    @crates/fava-state/Cargo.toml
    @crates/fava-write/Cargo.toml
    @crates/fava-nip02/tests/architecture.rs
  </read_first>
  <files>Cargo.toml, Cargo.lock, crates/fava-simple-groups/Cargo.toml, crates/fava-simple-groups/BUILD.bazel, crates/fava-simple-groups/tests/architecture.rs</files>
  <behavior>
    - The workspace and Bazel graph register the capability crate.
    - Cargo normal dependency names equal exactly `fava-query`, `fava-state`, and `fava-write`; no normal dependency is missing or additional.
    - The Bazel library dependency labels equal the same three owners and tests remain test-only dependencies.
  </behavior>
  <action>Create the manifests, lockfile update, and executable `normal_dependencies_are_exact` architecture test in this plan. Parse Cargo TOML structurally and compare the complete normal-dependency key set for exact equality; inspect the Bazel library target separately for exact owner labels. First run the architecture target against an intentionally incomplete test fixture and capture its assertion RED, then make the equality test describe the real manifests and pass. Record checksums, deliberately add `fava` as a normal Cargo dependency and its Bazel label, require the exact equality assertion to fail, restore the manifest bytes and checksums, and rerun GREEN. Dependency refusal is the behavior under test; it must not depend on Plan 10's broader scans.</action>
  <acceptance_criteria>The crate is registered and `normal_dependencies_are_exact` is runnable here, proves complete Cargo/Bazel equality, fails for an added facade dependency, and returns to byte-identical GREEN.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --test architecture normal_dependencies_are_exact -- --exact &amp;&amp; cargo metadata --no-deps --format-version 1 &gt;/dev/null &amp;&amp; git diff --check</automated>
  </verify>
  <done>The capability has exactly its three approved normal dependencies with causal executable evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trace one Group through pure preparation and ordinary Queries</name>
  <read_first>
    @crates/fava-simple-groups/README.md
    @docs/issues/0019-simple-groups.md
    @crates/fava-query/src/selection.rs
    @crates/fava-write/src/lib.rs
    @crates/fava-state/src/lib.rs
  </read_first>
  <files>crates/fava-simple-groups/src/lib.rs, crates/fava-simple-groups/src/group.rs, crates/fava-simple-groups/src/query.rs, crates/fava-simple-groups/tests/public_api.rs</files>
  <behavior>
    - `Group::on` constructs a one-host instance of the generalized bounded non-empty host-set identity.
    - One valid opaque group id and one unsigned event are prepared with exactly one `h` context tag.
    - One content Query retains AnyLocal visibility and one GroupRecords Query carries the exact selected RelayUrl authority.
    - The external test imports the final approved Group, GroupError, and GroupRecords signatures.
  </behavior>
  <action>Write the external `one_host_group_traces_pure_preparation_and_queries` test first against the final approved signatures, capture compile RED, then implement the narrow production-quality happy path. Promote Group as the sole generalized identity with a non-empty host collection; `Group::on` is only its one-cardinality constructor. Use the exact opaque group id without trimming, existing RelayUrl/Query/EventBuilder/WriteRouting values, ordinary Query construction, and pure preparation only. Preserve AnyLocal for content and exact RelayUrl authority for records. Do not add lifecycle ownership or unapproved public nouns. After GREEN, checksum the sources, deliberately route the record Query through AnyLocal and drop the prepared `h` tag one at a time, require the external runtime assertions to fail, restore byte-exactly, verify checksums, and rerun GREEN.</action>
  <acceptance_criteria>The final API compiles externally and the one-host tracer proves pure preparation plus ordinary local-visible content and exact-host record Queries; both named breaks fail the intended assertions and restore exactly.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --test public_api one_host_group_traces_pure_preparation_and_queries -- --exact &amp;&amp; cargo test -p fava-simple-groups --test architecture normal_dependencies_are_exact -- --exact</automated>
  </verify>
  <done>The approved Group capability has a real vertical path ready for bounded edge expansion.</done>
</task>

</tasks>

## Artifacts this phase produces

- New crate registration and exact manifests: `Cargo.toml`, `Cargo.lock`, `crates/fava-simple-groups/Cargo.toml`, `crates/fava-simple-groups/BUILD.bazel`.
- New public symbols: `Group`, `GroupError`, and `GroupRecords`.
- New executable architecture test: `normal_dependencies_are_exact`.
- New external tracer test: `one_host_group_traces_pure_preparation_and_queries`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Application values -> Group | Host and opaque id enter a pure capability value. |
| Group -> neutral crates | Query/preparation meaning crosses into existing neutral owners. |
| Manifest -> build graph | Dependencies can silently broaden capability custody or lifecycle ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-04A | Tampering | Cargo/Bazel manifests | high | mitigate | Structural exact-set equality and an added-facade deliberate break. |
| T-0711-04B | Spoofing | RelayUrl record authority | high | mitigate | Exact-host tracer assertion; no canonical-host substitution. |
| T-0711-04C | Elevation of privilege | pure capability API | high | mitigate | Only neutral query/state/write dependencies and no lifecycle/provider owner. |
</threat_model>

<verification>
Run both exact tests, the complete crate test corpus, strict Clippy, formatting, vocabulary, line-limit, and diff gates. Bazel claims remain gated by Plan 12's fail-closed runner preflight.
</verification>

<success_criteria>
The crate exists with fewer than ten owned files in this plan, exact dependencies are causally falsified, and the approved Group API completes one pure preparation/query path.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-04-SUMMARY.md` when done.
</output>
