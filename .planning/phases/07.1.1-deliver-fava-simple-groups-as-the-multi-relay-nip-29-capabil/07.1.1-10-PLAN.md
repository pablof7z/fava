---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 10
type: execute
wave: 9
depends_on: [07.1.1-09]
files_modified:
  - crates/fava-simple-groups/README.md
  - crates/fava-simple-groups/tests/public_api.rs
  - crates/fava-simple-groups/tests/architecture.rs
  - crates/fava-simple-groups/BUILD.bazel
  - docs/issues/0019-simple-groups.md
  - docs/internals/vocabulary.toml
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-04, GROUP-05, GROUP-06, GROUP-07, GROUP-08, GROUP-09, GROUP-10, GROUP-11]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "README examples compile against delivered Group/records/projection/discovery/prepare plus fava publish/by/to/Write; stale WriteIntent/accepted/ValueSet/on_many wording is absent."
    - "Vocabulary promotes exactly issue-0019 approved crate/types and no new nominal row, role, host, lifecycle, provider, or configuration concept."
    - "Architecture tests prove normal dependencies equal fava-query/fava-state/fava-write; facade/canary edges are test/app-only."
    - "Universal owners contain no NIP-29 constants, h/group-id semantics, protocol switch, or capability production dependency."
    - "Every production/test Rust file is below 800 lines; touched/new files remain below 500 unless one exact cohesion reason is recorded."
  prohibitions:
    - status: flagged-unverified
      statement: "No canonical host/field merge, absence/completeness claim, Group lifecycle, ValueSet, universal NIP-29 branch, signed mutation, hidden bound, or compatibility API appears in public docs/code."
  artifacts:
    - path: crates/fava-simple-groups/README.md
      provides: "Truthful executable DX North Star"
    - path: crates/fava-simple-groups/tests/architecture.rs
      provides: "Dependency/lifecycle/kind-blind/vocabulary falsifiers"
    - path: docs/internals/vocabulary.toml
      provides: "Implemented approved fava-simple-groups registry"
  key_links:
    - from: crates/fava-simple-groups/README.md
      to: crates/fava-simple-groups/tests/public_api.rs
      via: "every nontrivial example is mirrored/compiled externally"
      pattern: "Group::on|\.publish\("
---

## Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Make the README North Star, architecture falsifiers, focused issue, and closed vocabulary exactly match the delivered capability.

Purpose: Public claims must compile and the engine-free/kind-blind boundary must fail under deliberate violations before live canary work.
Output: Current-only README/issue/vocabulary plus exact dependency/source/public API tests.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@crates/fava-simple-groups/README.md
@docs/issues/0019-simple-groups.md
@crates/fava-nip02/tests/architecture.rs
@crates/fava-nip02/tests/public_api.rs
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind the README North Star to external compilation</name>
  <read_first>
    @crates/fava-simple-groups/README.md
    @crates/fava-simple-groups/tests/public_api.rs
    @crates/fava-nip02/README.md
    @crates/fava-nip02/tests/public_api.rs
  </read_first>
  <files>crates/fava-simple-groups/README.md, crates/fava-simple-groups/tests/public_api.rs</files>
  <behavior>
    - Feed, records, fork visibility, discovery, saved edits, arbitrary publication, signed exactness, cancellation, and close examples compile or are mirrored exactly.
    - Publication uses only prepare plus `fava.to(...).publish(...) -> Write`; edits use by/to/publish.
    - Deferred on_many/previous/UI policy and unimplemented ValueSet are not claimed.
  </behavior>
  <action>Rewrite the README to current delivered APIs only, removing stale status/migration/deviation/compatibility narration and old facade words. Update external public API bindings to mirror every nontrivial example and approved symbol. Record RED where stale examples fail against current exports before rewriting. After GREEN, deliberately reintroduce one stale public publication call in the compiled mirror, require compilation to fail, restore byte-exactly, and rerun doctest/public API.</action>
  <acceptance_criteria>The README is executable current-model documentation, every example is evidenced, and no deferred/stale surface remains.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --doc &amp;&amp; cargo test -p fava-simple-groups --test public_api</automated>
  </verify>
  <done>The README is a truthful public DX contract using delivered vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce exact dependency, lifecycle, and universal-owner boundaries</name>
  <read_first>
    @crates/fava-nip02/tests/architecture.rs
    @crates/fava-simple-groups/Cargo.toml
    @crates/fava-simple-groups/BUILD.bazel
    @crates/fava/Cargo.toml
    @crates/fava/BUILD.bazel
  </read_first>
  <files>crates/fava-simple-groups/tests/architecture.rs, crates/fava-simple-groups/BUILD.bazel</files>
  <behavior>
    - Cargo normal dependencies are exactly the three neutral crates and Bazel library edges match.
    - Production sources contain no facade/observe/publication/signer/router/store/publisher/delivery/runtime/transport ownership/import.
    - Facade/universal owners contain no production capability edge or NIP-29 constant/h/group switch.
    - Public exports contain no ValueSet, lifecycle, receipt, observation, publication, or unapproved wrapper noun.
    - Kinds 9002 and 9010 cannot enter ReplaceableEventEdit; the architecture target owns a complete named wrong-kind break.
    - `pure_helpers_have_no_lifecycle_owner` proves repeated capability helpers remain inert and own no retained state, provider, or lifecycle.
  </behavior>
  <action>Extend the executable Cargo/Bazel/source/public-export/universal-owner falsifier target, retaining Plan 04's exact manifest-equality target. Parse manifests structurally and scope scans so generic Kind::from_u16/tag handling stays legal. Create final E29 descriptor `pure_helpers_have_no_lifecycle_owner` first and compile its explicit target with `cargo test -p fava-simple-groups --test architecture --no-run`. Before the architecture behavior is GREEN, capture checksums and introduce a checksum-guarded hidden-state violation fixture/mutation that gives a pure helper retained mutable state/provider ownership; run exact selector `cargo test -p fava-simple-groups --test architecture pure_helpers_have_no_lifecycle_owner -- --exact` and capture causal architecture RED at the intended hidden-state assertion. Remove that initial violation byte-exactly, require checksum restoration, complete the architecture check, and rerun the exact selector GREEN. Then capture a new pre-break SHA-256 and independently apply the distinct post-GREEN break `pure_helper_retains_hidden_state`, require the same exact selector RED, restore byte-exactly with equal SHA-256, rerun the same selector restored GREEN, and record E29 in `07.1.1-10-SUMMARY.md`. Also add descriptor `management_kinds_remain_author_bearing_architecture_break`, which detects 9002/9010 entering ReplaceableEventEdit while allowing ordinary EventBuilder preparation. Deliberately add a normal facade dependency, an arbitrary universal NIP-29 constant branch, and a 9002 ReplaceableEventEdit path one at a time; require each exact owning assertion RED, restore exact bytes/checksums, and rerun restored GREEN. This task solely owns the wrong-management-kind break assigned by Plan 09 Task 3.</action>
  <acceptance_criteria>All GROUP-11 ownership/dependency/replaceability boundaries are executable; dependency, universal-kind-switch, and `management_kinds_remain_author_bearing_architecture_break` violations fail causally without false positives and restore exactly.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --test architecture --no-run &amp;&amp; cargo test -p fava-simple-groups --test architecture pure_helpers_have_no_lifecycle_owner -- --exact &amp;&amp; cargo test -p fava-simple-groups --test architecture management_kinds_remain_author_bearing_architecture_break -- --exact &amp;&amp; cargo test -p fava-simple-groups --test architecture</automated>
  </verify>
  <done>The capability is provably engine-free and universal owners remain capability-blind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Promote approved vocabulary and replace the focused issue model</name>
  <reversibility rating="costly">The crate/types are already separately approved in issue 0019; promotion records implemented public vocabulary and affects future checker gates.</reversibility>
  <read_first>
    @docs/issues/0019-simple-groups.md
    @docs/internals/vocabulary.toml
    @tools/check_vocabulary.py
    @AGENTS.md
  </read_first>
  <files>docs/issues/0019-simple-groups.md, docs/internals/vocabulary.toml, crates/fava-simple-groups/tests/architecture.rs</files>
  <behavior>
    - Only approved crate/types/symbols are promoted from specified to implemented vocabulary.
    - Issue 0019 describes pure preparation followed by current facade publication and the generalized Group host-set identity.
    - No superseded WriteIntent/accepted/ValueSet/on_many/9002-edit narration remains.
    - Any genuinely forced new nominal type/provider/lifecycle blocks before implementation for separate Pablo approval.
  </behavior>
  <action>Promote the exact implemented symbol list and replace issue 0019 with the current model only. Before editing, diff actual public exports against the approved registry; if any new nominal type/provider/lifecycle beyond issue 0019 is required, stop this task before implementation and surface the vocabulary gate. Deliberately add one unregistered public export, require live vocabulary/architecture validation to fail, restore exact bytes, and rerun all Cargo/public/docs/vocabulary/line/diff gates.</action>
  <acceptance_criteria>Vocabulary and issue match actual approved exports/current ownership, the unregistered export mutation fails, and every code file satisfies line limits.</acceptance_criteria>
  <verify>
    <automated>python3 tools/check_vocabulary.py &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; cargo test -p fava-simple-groups --all-targets &amp;&amp; cargo clippy -p fava-simple-groups --all-targets -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; find crates/fava-simple-groups crates/fava-query crates/fava-state crates/fava-query-standard -name '*.rs' -type f -exec awk 'FNR==1 {file=FILENAME} END {if (FNR&gt;=800) {print file ":" FNR; exit 1}}' {} \; &amp;&amp; git diff --check</automated>
  </verify>
  <done>Documentation, vocabulary, exports, dependencies, and architecture claims are current and executable.</done>
</task>

</tasks>

## Artifacts this phase produces

- Rewritten delivered `crates/fava-simple-groups/README.md` and current-only `docs/issues/0019-simple-groups.md`.
- New `crates/fava-simple-groups/tests/architecture.rs` plus Bazel target.
- Implemented vocabulary entries for the approved crate and public types/symbols; no additional nominal vocabulary.
- Execution artifact: `07.1.1-10-SUMMARY.md` section `## Edge Evidence Records` with columns `row_id | descriptor | exact_command | causal_red | named_break | pre_break_sha256 | restored_sha256 | restored_green`, containing E29.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public docs/exports -> application expectations | Stale or aspirational APIs can become false public claims. |
| Capability manifests/source -> universal architecture | A dependency or kind switch can smuggle lifecycle/meaning into wrong owners. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-27 | Tampering | dependency/source boundary | high | mitigate | Exact manifest equality and recursive scoped universal-owner scans. |
| T-0711-28 | Repudiation | README/public exports | medium | mitigate | External compilation/doctests mirror every nontrivial claim. |
| T-0711-29 | Elevation of privilege | unapproved vocabulary | high | mitigate | Closed registry diff; stop before implementation if a new noun is forced. |
</threat_model>

<verification>
Run doctests, public API, architecture, complete crate tests, strict Clippy, formatting, live/unit vocabulary, line-limit, and diff gates with all three deliberate violations restored.
</verification>

<success_criteria>
GROUP-01 through GROUP-11 public claims and ownership boundaries are truthful, current, compiled, vocabulary-approved, and mutation-enforced before canary implementation.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-10-SUMMARY.md` when done.
</output>
