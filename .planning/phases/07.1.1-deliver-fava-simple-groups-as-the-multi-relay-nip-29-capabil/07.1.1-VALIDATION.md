---
phase: 07.1.1
slug: deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
status: blocked
nyquist_compliant: false
wave_0_complete: false
validated: 2026-08-25
blockers:
  - cryptographic vocabulary approval
  - fresh full validation matrix
---

# Phase 07.1.1 — Current validation record

This file replaces the self-authored completion record. It reports only commands
executed against the current isolated worktree. Historical plans and summaries
are revoked and supply no current evidence.

Pablo approved issue 0024 Option A. The query-owner and simple-group evidence
below was run after implementation. It establishes the focused GROUP-02
behavior but does not restore any checked requirement verdict: the full matrix
has not been rerun and vocabulary remains unsigned. No merge-readiness claim
survives.

## Current executable evidence

| Scope | Command | Result |
|---|---|---|
| Causal RED | `cargo test -p fava-query --test query_identity tag_axis_intersection_never_broadens_an_existing_axis` before production implementation | FAIL as required with `E0599`: `Query` had no `intersect_tag_values` method. |
| Simple-group crate | `cargo test -p fava-simple-groups` | PASS: 21 unit, 5 architecture, 4 public-API, 14 ordinary rustdoc, and 2 compile-fail rustdoc tests; 3 README guide snippets remain intentionally ignored. The content-query test covers absent, matching, disjoint, overlapping, and present-empty `h` axes. |
| Query owner | `cargo test -p fava-query` | PASS: 20 tests. `intersect_tag_values` covers absent, supplied-empty, matching, disjoint, overlapping, and present-empty axes; unrelated query state; the shared 4,096 cap; infinite input termination; and exact `TooManyTagValues`. |
| Write owner | `cargo test -p fava-write` | PASS: 9 tests, including termination for infinitely repeated explicit relay input and exact `WriteIntentError` failures. |
| Public facade | `cargo test -p fava --test simple_groups` | PASS: 4 tests through the `fava` facade, including overlapping and disjoint `h` composition. |
| Canary consumer | `cargo check --manifest-path apps/canary/Cargo.toml --all-targets` | PASS. |
| Bazel source/data closure | `bazel --output_user_root=/tmp/fava-simple-groups-bazel test --disk_cache=/tmp/fava-simple-groups-bazel-disk-cache //crates/fava-query:query_identity //crates/fava-simple-groups:unit_tests //crates/fava-simple-groups:public_api //crates/fava-simple-groups:architecture //crates/fava:simple_groups` | PASS: 5 of 5 targets. |
| Compiler README inventories | `python3 tools/crate_readme_api.py check fava fava-nip02 fava-observe fava-publication fava-query fava-query-standard fava-router-outbox fava-routing fava-runtime fava-simple-groups fava-state fava-subscriptions fava-write` | PASS for every modified library crate. The generated state inventory contains neither rejected relay-sequence identity; the simple-groups inventory contains the required-head signature. |
| Inventory gate regressions | `python3 -m unittest tools.tests.test_crate_readme_api` | PASS: 16 tests, including deletion-only changed-path discovery and stale deleted-re-export detection. |
| Vocabulary candidate machinery | `python3 -m unittest tools.tests.test_vocabulary_approval` | PASS: 63 tests after refreshing source-line evidence moved by the rejected-owner deletion and provisional-cap documentation. |
| Unsigned candidate rendering | Recompute every recorded SHA-256 from `tools.vocabulary_approval.canonical_markdown` and `docs/internals/vocabulary.toml` | PASS: all 17 blocks are byte-exact; this is integrity evidence, not cryptographic approval. |
| Strict Clippy | `cargo clippy -p fava-simple-groups -p fava-query -p fava-write --all-targets -- -D warnings` | PASS. |
| Formatting | `cargo fmt --all -- --check` | PASS. |
| Patch hygiene | `git diff --check` | PASS. |
| Repository vocabulary gate | `python3 tools/check_vocabulary.py` | FAIL: the repository-wide gate reports existing governance debt across crates and public symbols. Filtering the complete output for `SimpleGroup`, `RelaySequence`, or `intersect_tag_values` is empty, but the current `SimpleGroup` candidate remains unsigned; no vocabulary approval or GROUP-12 verdict is claimed. |

## Current boundaries

Pablo approved issue 0023 Option B. `SimpleGroup::from_relays` requires one
parsed `RelayUrl` and a finite owned `Vec<RelayUrl>` tail. Empty and arbitrary
iterator construction are unavailable; no replacement relay-sequence owner,
group-private numeric limit, query validation, or failure translation exists.

Query's 4,096 cap and write's 256 cap remain independently owned provisional
resource-safety shortcuts. They are neither Nostr constraints nor simple-group
domain semantics.

Pablo approved issue 0024 Option A. Query-owned
`Query::intersect_tag_values` consumes supplied values under the existing
provisional query cap, installs an absent axis, intersects a present axis, and
keeps disjoint values present-empty match-nothing. `SimpleGroup::events`
delegates to it without selection inspection, validation, failure translation,
or a group-owned error.

The focused GROUP-02 implementation and falsifiers pass. Its requirement
checkbox remains unchecked until the fresh full matrix restores independent
requirement-level validation. GROUP-12 is blocked until that matrix passes and
the final vocabulary receives cryptographic approval. No merge-ready or
phase-complete verdict is claimed.
