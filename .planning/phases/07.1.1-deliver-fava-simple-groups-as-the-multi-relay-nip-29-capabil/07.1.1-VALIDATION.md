---
phase: 07.1.1
slug: deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
status: blocked-on-19-unapproved-and-3-stale-vocabulary-approvals
nyquist_compliant: true
wave_0_complete: true
original_stack_base_revision: 13810907dcd71dee9f487ba38ee3543e3ca97822
behavior_baseline_revision: da9d322c1e1b8ed93aa56eeed24fceb4f757aac6
integration_base_revision: 331433eabd2de865649d0070e88795291d5c7e6c
validated: 2026-08-26
---

# Phase 07.1.1 — Current V2 validation

This record replaces the 2026-08-22 validation claims. Those claims described
removed `Group`, `GroupRecords`, `GroupSnapshot`, `SimpleGroups`, signed-event
preparation, domain relay caps, and retained-pair evidence. None validates the
current V2 API, and no retained pair from that record is claimed here.

The V2 series was reconstructed from `1381090` and is now integrated over
accepted main `331433eabd2de865649d0070e88795291d5c7e6c`. All 22 current
simple-group approval identities bind 113 compiler-derived public API records.
Nineteen are `unapproved`; three retain valid historical text-only approval
events and are `stale`. All 22 require current structurally bound owner
signatures.
Baseline-red and environment-unavailable results are retained as truthful
dispositions, not converted into V2 success claims.

The owner-scoped package has zero review problems and zero public-API gaps.
That is not a global governance claim: `check_vocabulary.py` remains red with
354 inherited unique repository-wide findings, while terminal-name and
external owner-signature gates remain independently red.

## Current behavior under validation

| Boundary | Current behavior and owner |
|---|---|
| Group value | `SimpleGroup::new(id, Vec<RelayUrl>)` accepts only finite owned input, rejects an empty id or relay vector through `SimpleGroupConstructionError`, preserves first relay occurrences, and owns no numeric domain cap. |
| Content query | `SimpleGroup::events(query)` delegates lowercase-`h` AND composition to `Query::intersect_tag_values`, then delegates acquisition to `Query::from_relays`. A disjoint existing axis becomes present-empty match-none; it is not widened and is not a group error. |
| State query | `SimpleGroup::meta_events(kinds)` delegates generic kind bounds to `Query::kinds`, adds exact lowercase `d`, and uses `Query::only_from_relays`. It returns ordinary event values for event-local protocol decoding. |
| Preparation and publication | `SimpleGroup::prepare` accepts only `UnsignedEvent`, preserves an existing matching `h` tag, otherwise appends one matching tag through the neutral `EventBuilder`, and returns its exact `EventBuildError`. Callers publish through the already-landed neutral `fava.to(group.relays()).publish(...)` boundary. There is no signed overload or second write path. |
| Protocol decoding | Kinds 39000–39005 decode one event at a time. Unknown, malformed optional, repeated, extended, and sibling material remains local; no signature verification, relay-authority projection, cross-event merge, or group lifecycle is introduced. |
| Saved lists | Kind 10009 query, decode, edits, and applier use ordinary `Query`, `EventEdit`, and `EditApplier` owners. Unrelated tags and unused values are conserved. |
| Explicit routing | `fava-write::routing_order` still bounds distinct relay identities, preserves first occurrences, and returns the existing exact empty/over-bound refusals. The simple-group stack does not duplicate or alter the neutral write boundary. |
| Caller/catalog closure | Facade tests and canary call only the V2 public surface; canary CLI option/result carriers remain crate-private. Cargo and Bazel catalogs name the current single facade test source. Changed library READMEs contain deterministic managed API inventories. |

## Causal evidence

| Decision or regression | Causal falsifier | Current executable evidence |
|---|---|---|
| Finite non-empty construction | Issue 0027 approves finite owned `Vec` input and exact empty-id or empty-relay refusal. Substituting an arbitrary iterator admits the documented infinite-input counterexample. | `cargo test -p fava-simple-groups --doc --locked`; `cargo test -p fava-simple-groups --all-targets --locked`; constructor refusal and first-occurrence tests. |
| Exact `h` composition | Issue 0028 records the union implementation as the named deliberate break. Union makes the disjoint and partial-overlap cases fail by broadening the selection. | `cargo test -p fava-query --test query_identity --locked`; `tag_axis_intersection_never_broadens_an_existing_axis`; `content_query_intersects_the_h_axis_and_uses_ordinary_relay_acquisition`. |
| Query-owned bounds | Replacing `intersect_tag_values`/`kinds` with group-local collection or truncation loses the query owner's exact `TooManyTagValues`/`TooManyKinds` refusals. | `generic_query_inputs_stop_at_query_owned_bounds`; `state_event_query_delegates_empty_and_non_empty_sets_to_query`. |
| Distinct explicit relays | Counting occurrences instead of distinct relay identities rejects a route containing repeated relays before its distinct bound is reached. | `cargo test -p fava-write --test routing_order --locked`; `explicit_route_bounds_distinct_relay_identities_not_occurrences`. |
| Neutral write closure | Adding signed preparation or a group publication path bypasses the one facade observation/write door and changes the accepted write boundary. | `cargo test -p fava --test simple_groups --locked`; `prepared_content_uses_the_ordinary_observation_and_write_doors`. |
| Event-local tolerant decoding | Turning malformed optional or repeated sibling material into whole-event failure erases the valid rows asserted alongside it. | `metadata_conserves_value_local_failures_and_repetitions`; `repeated_people_and_role_entries_survive_with_local_failures`; `saved_list_decodes_both_tag_families_once_and_tolerantly`. |
| Bazel source closure | Leaving deleted `tests/simple_groups/saved.rs` in `srcs` makes the `fmt-check` aspect fail analysis with a missing input. | Commit `dabcb78` removes only that stale input; `//crates/fava-simple-groups:{unit_tests,public_api,architecture}` all pass. |
| Deterministic README closure | Omitting a changed library README or retaining a stale inventory makes `check-modified` fail. | `python3 tools/crate_readme_api.py check-modified --base da9d322 --head HEAD`; all 11 changed libraries pass; 16 tool tests pass. |
| Reused relay sessions | The old pair verifier treated a connection as one exchange and rejected the production transport's valid multi-exchange session. Collapsing every fixture exchange onto one connection is the deliberate regression shape. | `pair_verifier_tracks_exchanges_when_one_relay_session_is_reused` passes only when publications are keyed by connection plus event id and queries by connection plus subscription; the wrong-connection, duplicate, and terminal-order refusal tests remain green. |

## Current package refresh and dated gate evidence

The owner-package, structure, canonical-byte, evidence-pointer, and repository
finding rows below were refreshed on 2026-08-26 against base `331433ea`.
Other retained command results are dated 2026-08-25 V2 evidence; they are not a
claim that every repository-wide gate was rerun or is green on the rebased tip.

Dispositions are exact:

- **PASS**: command completed successfully on the V2 tree.
- **BASELINE RED**: the same failure was reproduced from untouched exact
  integration base `da9d322`.
- **ENVIRONMENT UNAVAILABLE**: the required external fixture is absent on both
  local trees; that local command supplies no live-success claim.
- **EXTERNAL GOVERNANCE**: only a real owner-signed event can close the item;
  implementation and tests cannot substitute for it.

| Gate | V2 result | Baseline/environment disposition |
|---|---|---|
| `cargo test -p fava-simple-groups --all-targets --locked` | **PASS** — 24 unit, 6 architecture, 4 public-API tests. | V2 owner evidence. |
| `cargo test -p fava-simple-groups --doc --locked` | **PASS** — 15 compiled examples, 2 compile-fail tests, 3 intentionally ignored examples. | The approved finite-construction shape is compile-enforced. |
| `cargo test -p fava-query --test query_identity --locked` | **PASS** — 11 tests. | V2 query-composition evidence. |
| `cargo test -p fava --test simple_groups --locked` | **PASS** — 4 facade flows. | V2 caller/write-door evidence. |
| `cargo test -p fava-nip02 --all-targets --locked` | **PASS** — 28 unit, 2 architecture, 1 public-API tests after rebuilding a moved-worktree cache. | Exact fallible-query/API closure; no NIP-02 semantic change in this slice. |
| `cargo test --workspace --all-targets --locked` | **2026-08-25 HISTORICAL RED, SUPERSEDED FOR THE NAMED FAILURE** — the recorded failure was `memory_initial_route_idempotence_compares_complete_persisted_effect`. | Accepted main `331433ea` contains the idempotence fix and its focused memory/redb tests pass. No full-workspace green claim is made for the rebased tip. |
| `cargo clippy --workspace --all-targets --locked -- -D warnings` | **PASS**. | Current Rust tree is warning-free. |
| `cargo fmt --all -- --check` | **CURRENT 2026-08-26 PASS** on the rebased tip. | Formatting is current; this is not a global test or governance claim. |
| Focused Bazel: `//crates/fava-simple-groups:unit_tests`, `:public_api`, `:architecture`, `//crates/fava-query:query_identity`, `//crates/fava-nip02:architecture` | **PASS** — 5/5 targets. | Current V2 targets and catalogs are analyzable and executable. |
| `bazel test //...` | **BASELINE RED** at `//crates/fava-runtime:unit_tests`: unresolved `fava_query::OperationGeneration`, missing Bazel dependency. | The exact target and compiler error reproduce on `da9d322`; the resulting partial run is not promoted to full-suite success. |
| `bazel test --config=clippy //...` | **BASELINE RED** at the same `fava-runtime` missing dependency. | The configuration reaches the same owner-external target; no skipped target is claimed green. |
| `bazel test --config=fmt-check //...` | The V2-local stale `saved.rs` input is closed; traversal then reaches the same **BASELINE RED** `fava-runtime` dependency defect. | Focused simple-groups analysis and execution pass before the unrelated traversal stop. |
| `python3 tools/crate_readme_api.py check-modified --base da9d322 --head HEAD` | **PASS** for `fava`, `fava-nip02`, `fava-observe`, `fava-publication`, `fava-query`, `fava-query-standard`, `fava-router-outbox`, `fava-routing`, `fava-simple-groups`, `fava-subscriptions`, and `fava-write`. | Managed regions are generator-idempotent; no unrelated prose was rewritten. |
| `python3 tools/approve_vocabulary.py --root . --dump-candidates-json` | **PASS** — 100 validated review rows. | Exact `da9d322` fails with 23 diagnostics: 11 missing researched renamed rows, 10 stale `Group*`/`SavedGroup` rows, and stale `EventValue`/`InvalidEventValue` lines 250/322. The merge tree has no candidate diagnostic: `ContactListRowEvidence` research is present; the current simple-group terms are independently registered; all ten stale records are absent; the two evidence locations name declarations at lines 252/324. |
| Vocabulary review tests | **CURRENT 2026-08-26 PASS** — 74 non-server approval tests, 100 validated candidate rows, and direct paused-POST/current-GET assertions. | No server was started; repository-wide governance remains separately red below. |
| `python3 tools/vocabulary_structure.py check` | **PASS** — fresh compiler output is byte-identical to `docs/internals/vocabulary-structure.json`. | The committed snapshot contains 22 exact simple-group identity packets binding 113 public API records. |
| Owner-scoped approval package | **OWNER PACKAGE PASS** — 22 exact canonical markdown payloads; 19 `unapproved`, 3 `stale`, zero owner-scoped review problems and zero public-API gaps. | The stale terms are `SimpleGroupStateEventKind`, `SimpleGroupMetadata`, and `SimpleGroupAdmins`; their historical text-only events remain append-only. This does not make repository-wide governance green. |
| `python3 tools/check_vocabulary.py` | **CURRENT 2026-08-26 REPOSITORY-WIDE RED**: excluding the banner, 356 diagnostic occurrences and 354 unique exact findings; the dated `da9d322` comparison was 383 occurrences and 381 unique findings. | Occurrence counts are not mixed with de-duplicated set counts, candidate-review count, JSONL line count, or approval count. Zero owner-package problems does not alter this red gate. |
| `cargo test -p fava --test vocabulary_governance --locked` | **CURRENT 2026-08-26 EXTERNAL/INDEPENDENT RED** — 7 pass, 1 all-current-approvals test fails with missing descriptions/signatures and terminal-name findings. | The 22 pending simple-group signatures are a subset of the external owner-signature gate; the independent terminal-name backlog is not filtered or relabelled by the zero-problem owner package. |
| Imported approval verification | **PASS** for the four appended historical events: `Follow`, `SimpleGroupMetadata`, `SimpleGroupAdmins`, and `SimpleGroupStateEventKind` each pass `vocab-verify` event-id, Schnorr-signature, owner, kind, and single-name checks. | The committed 22-line history is preserved. Structural payload binding makes every text-only event stale; none closes a current structurally bound approval. |
| Canary `cargo check`, strict Clippy, and crate-local `cargo fmt --manifest-path apps/canary/Cargo.toml -- --check` | **2026-08-25 HISTORICAL PASS**. | Dated canary source evidence; root-workspace formatting is a current pass above. |
| `cargo test --manifest-path apps/canary/Cargo.toml --all-targets --locked` | **ENVIRONMENT UNAVAILABLE** locally — 69 pass, 5 fail. | Exact `da9d322` has 68 pass, 5 fail. Both use hard-coded fixture paths absent in this sandbox; the one added reused-session verifier test is green. This local disposition is not used as the live oracle. |
| Controlled two-relay wire pair | **2026-08-25 HISTORICAL PASS**: two fresh runs, two owned Croissant children per run, distinct identities, complete per-relay wire and provenance, bounded artifacts, secret scans, exact teardown, and no staging residue. | The sealed evidence binds its recorded pre-rebase revision; it was not rerun or relabelled as evidence for base `331433ea`. |
| `git diff --check` | **PASS**. | No whitespace error in the replacement series. |

## External governance gate

The implementation does not fabricate approval signatures. Merge readiness is
blocked on current structurally bound signatures for all 22 identities:

1. `SimpleGroup`
2. `SimpleGroupConstructionError`
3. `SimpleGroupStateEventKind`
4. `SimpleGroupMetadata`
5. `SimpleGroupAdmins`
6. `SimpleGroupMembers`
7. `SimpleGroupRoles`
8. `SimpleGroupLivekitParticipants`
9. `SimpleGroupPins`
10. `SimpleGroupDecodeError`
11. `SavedSimpleGroup`
12. `SavedGroupList`
13. `SavedGroupListApplier`
14. `SavedGroupListDecodeError`
15. `fava_simple_groups`
16. `save_simple_group`
17. `remove_saved_simple_group`
18. `rename_saved_simple_group`
19. `save_relay`
20. `remove_saved_relay`
21. `saved_group_list_applier`
22. `saved_group_lists`

The owner-only review surface is started with
`python3 tools/approve_vocabulary.py --root . --port 4319 --no-open` and opened
at `http://127.0.0.1:4319/?owner=fava-simple-groups&status=pending`. That scope
shows only the 22 `unapproved` or `stale` rows owned by `fava-simple-groups`;
each row still requires its own signing action.

No technical V2 blocker is hidden behind this gate. Repository-wide failures
classified above remain separate. The local Croissant fixture absence and
pinned Linux pair are dated 2026-08-25 evidence; neither is relabelled as a
current live proof for the rebased tip. The 19 unapproved and three stale
approval identities must receive current external signatures before this phase
can be marked complete.
