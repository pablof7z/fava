---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 12
type: historical
status: revoked
revoked: 2026-08-24
wave: 11
depends_on: [07.1.1-11]
files_modified:
  - apps/canary/runs/phase-07.1.1-pair.*
  - .planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-12-PAIR-ROOT.txt
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-04, GROUP-05, GROUP-06, GROUP-07, GROUP-08, GROUP-09, GROUP-10, GROUP-11, GROUP-12]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The approved primary identity is Group with one bounded non-empty host set; one-host is one cardinality and no parallel required host field exists."
    - "All 31 applicable edge probes are executable: duplicate/adjacent inputs, empty/present-empty distinctions, first-occurrence/deterministic order, signed exactness, same-id events, same-signer record conflicts, inert/idempotent helpers, concurrent lifecycle isolation, hostile parser bounds, and canary cleanup."
    - "Exactly two fresh controlled runs produce four distinct owned Croissant children, two exact teardown results per run, exact A/B wire/provenance/disagreement/handoff facts, distinct identities, and no cross-run data."
    - "Both secret scans, complete signed claims/hashes, closed PIDs/ports, absent staging, and atomic promotion pass before the retained pair exists."
    - "Cargo workspace/crate/canary, strict Clippy, fmt, vocabulary, line, diff, Bazel, Bazel Clippy, and Bazel fmt gates all pass from committed source."
    - "Independent pair verification passes after the two runs and no process outside the four exact owned children is inspected, killed, or claimed."
    - "The exact fresh pair_root is persisted in the Plan 12 owned execution artifact and the independent verifier runs against that path before and after the full 31-row matrix."
  prohibitions:
    - status: flagged-unverified
      statement: "No canonical host selection or metadata field merge is allowed."
    - status: flagged-unverified
      statement: "No absence, completeness, existence, migration, or negative-membership claim is allowed from bounded relay evidence."
    - status: flagged-unverified
      statement: "No group lifecycle, ValueSet, or universal NIP-29 branch is allowed."
    - status: flagged-unverified
      statement: "No signed event mutation or invalid-context custody/wire work is allowed."
    - status: flagged-unverified
      statement: "No hidden unbounded or silently truncated externally influenced input/output is allowed."
    - status: flagged-unverified
      statement: "No non-owned process cleanup, name-based discovery, or operation on PID 75649 is allowed."
    - status: flagged-unverified
      statement: "No unsafe, secret-bearing, incompletely sealed, staged, or incompletely cleaned canary artifact is retained."
  artifacts:
    - path: apps/canary/runs/phase-07.1.1-pair.*
      provides: "Exactly two fresh sealed controlled runs and independent pair-verifier input"
    - path: .planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-12-PAIR-ROOT.txt
      provides: "Exact owner-private fresh pair_root selected by Task 1 and consumed without discovery by Task 2"
  key_links:
    - from: apps/canary/runs/phase-07.1.1-pair.*
      to: apps/canary/src/croissant_simple_groups_evidence.rs
      via: "independent verifier recomputes seals, hashes, identities, bounds, pair exclusion, and exact cleanup"
      pattern: "manifest"
---

> **Revoked record:** This plan targets obsolete APIs and ownership. Nothing in
> this file is a current instruction, signature, approval, or acceptance claim.
> Current requirements and evidence are recorded in `.planning/REQUIREMENTS.md`
> and `07.1.1-VALIDATION.md`; the open relay-input decision is issue 0023.

## Historical Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Run the controlled two-relay proof twice, independently verify the pair, and close every Cargo/Bazel/security/architecture/line gate fail-closed.

Purpose: Convert implemented behavior into complete retained evidence for all GROUP requirements without weakening missing-tool or cleanup safety.
Output: One fresh two-run sealed pair, independent verification result, and complete phase gate results from committed sources.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-VALIDATION.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-11-SUMMARY.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-VERIFICATION.md
@apps/canary/src/croissant_simple_groups_evidence.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute exactly two fresh controlled two-relay runs</name>
  <precondition>`/Users/pablo/.local/bin/croissant` is executable, `/Users/pablo/Work/croissant/.git` exists, and neither target is modified by this task.</precondition>
  <read_first>
    @apps/canary/src/croissant_simple_groups.rs
    @apps/canary/src/croissant_simple_groups_flow.rs
    @apps/canary/src/croissant_simple_groups_evidence.rs
    @apps/canary/scenarios.json
    @.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-VALIDATION.md
  </read_first>
  <files>apps/canary/runs/phase-07.1.1-pair.*, .planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-12-PAIR-ROOT.txt</files>
  <action>Refresh `git worktree list` and preserve every sibling worktree. Create one owner-private pair root beneath `apps/canary/runs` with mode 0700, validate its resolved path stays beneath that directory, and immediately persist the exact fresh path with mode 0600 as the single line of `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-12-PAIR-ROOT.txt`; this owned execution artifact is the only Task 2 selector, so never discover an older run by glob or timestamp. Generate two distinct seeds in memory, invoke the enabled scenario twice with `cargo run --quiet`, exact Croissant path, distinct seeds, and the same persisted pair root, then unset seed variables. Each run must independently spawn exactly two new children and capture both cleanup results before propagation; never inspect/discover/kill by process name and never operate on PID 75649. Do not retain/promote either run until its two teardowns, pre/post secret scans, complete signed manifest/hashes, closed PID/ports, and staging cleanup pass. If either run refuses, retain no unsafe run and stop without claiming evidence.</action>
  <acceptance_criteria>The 0600 execution artifact contains exactly the fresh pair_root; that root contains exactly two promoted run directories/manifests, each with two distinct owned child/readiness/teardown facts, exact A/B frames/provenance/disagreement/handoffs, complete seals/bounds/scans, and no live PID/port/staging/secret residue.</acceptance_criteria>
  <verify>
    <automated>test -x /Users/pablo/.local/bin/croissant &amp;&amp; test -d /Users/pablo/Work/croissant/.git &amp;&amp; mkdir -p apps/canary/runs &amp;&amp; chmod 700 apps/canary/runs &amp;&amp; pair_root="$(mktemp -d apps/canary/runs/phase-07.1.1-pair.XXXXXX)" &amp;&amp; chmod 700 "$pair_root" &amp;&amp; case "$(realpath "$pair_root")" in "$(realpath apps/canary/runs)"/*) ;; *) exit 1 ;; esac &amp;&amp; umask 077 &amp;&amp; printf '%s\n' "$pair_root" &gt; .planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-12-PAIR-ROOT.txt &amp;&amp; chmod 600 .planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-12-PAIR-ROOT.txt &amp;&amp; seed_one="$(openssl rand -hex 32)" &amp;&amp; seed_two="$(openssl rand -hex 32)" &amp;&amp; test "$seed_one" != "$seed_two" &amp;&amp; cargo run --quiet --manifest-path apps/canary/Cargo.toml -- run croissant-simple-groups-public-flow --relay-bin /Users/pablo/.local/bin/croissant --seed "$seed_one" --runs-dir "$pair_root" &amp;&amp; cargo run --quiet --manifest-path apps/canary/Cargo.toml -- run croissant-simple-groups-public-flow --relay-bin /Users/pablo/.local/bin/croissant --seed "$seed_two" --runs-dir "$pair_root" &amp;&amp; unset seed_one seed_two &amp;&amp; test "$(find "$pair_root" -mindepth 2 -maxdepth 2 -name manifest.json -type f | wc -l | tr -d ' ')" = 2</automated>
  </verify>
  <done>Two fresh independent runs exist safely with exactly four owned/reaped Croissant children and complete sealed evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Independently verify the pair and close fail-closed full gates</name>
  <precondition>The Task 1 pair root contains exactly two promoted manifests, and `command -v bazel` or `command -v bazelisk` succeeds before any Bazel pass claim.</precondition>
  <read_first>
    @apps/canary/src/croissant_simple_groups_evidence.rs
    @crates/fava-simple-groups/tests/architecture.rs
    @docs/internals/vocabulary.toml
    @AGENTS.md
  </read_first>
  <files>apps/canary/runs/phase-07.1.1-pair.*, .planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-12-PAIR-ROOT.txt</files>
  <action>Read `07.1.1-12-PAIR-ROOT.txt` only after proving it has exactly one physical line with `wc -l == 1`, that line is non-empty, and the file contains no carriage return; reject every blank or extra physical line. Resolve that exact line beneath `apps/canary/runs` and do not discover any run by glob/name/time. Resolve the Bazel runner with an explicit branch: if `command -v bazel` succeeds, use that path; else if `command -v bazelisk` succeeds, use that path; else exit immediately. Do not feed a tolerated failure into runner comparison, and Cargo cannot substitute for Bazel. Run `verify-croissant-simple-groups-pair --runs-dir "$pair_root"` before the matrix. Audit every E01-E31 row in VALIDATION.md against `## Edge Evidence Records` in the owning Plans 01-11 SUMMARY: require exactly the stable columns `row_id | descriptor | exact_command | causal_red | named_break | pre_break_sha256 | restored_sha256 | restored_green`, the final descriptor/command pair, an intended causal runtime or architecture RED, the assigned named deliberate break, equal non-empty pre-break/restored checksums proving byte-exact restoration, and restored GREEN. If any owner SUMMARY, row, field, or evidence is absent or incomplete, fail closed with the row ID and owning plan; never create or execute missing owner evidence in Plan 12. Only after all 31 records exist, run the full Cargo/Bazel/Clippy/fmt/vocabulary/architecture/line/diff matrix, using `"$BAZEL_RUNNER"` for each of the three Bazel commands. Run the same independent pair verifier again after the entire matrix. Both verifier passes must require exactly two manifests/four distinct owned child instances, distinct seed/group/event/write/receipt identities, bidirectional old-data exclusion, exact shared/unique content provenance, same-signer record disagreement, two handoffs/acks, invalid-context zero frames, cancellation/close, complete signed hashes/bounds/scans, two teardown results per run, absent PIDs/closed ports, and no staging residue. Enforce the 800-line hard limit and record one exact cohesion reason for any touched file above 500; otherwise split it before completion.</action>
  <acceptance_criteria>The owned exact pair_root passes independent verification before and after the full matrix; E01-E31 each have owner/causal RED/named break/checksum restoration/restored GREEN evidence; every Cargo/Bazel/Clippy/fmt/vocabulary/architecture/line/diff gate passes; no unsafe artifact or non-owned process operation remains.</acceptance_criteria>
  <verify>
    <automated>set -e; pair_file=.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-12-PAIR-ROOT.txt; test "$(wc -l &lt; "$pair_file" | tr -d ' ')" = 1; ! LC_ALL=C grep -q "$(printf '\r')" "$pair_file"; pair_root="$(sed -n '1p' "$pair_file")"; test -n "$pair_root"; test "$(cat "$pair_file")" = "$pair_root"; case "$(realpath "$pair_root")" in "$(realpath apps/canary/runs)"/*) ;; *) exit 1 ;; esac; if command -v bazel &gt;/dev/null 2&gt;&amp;1; then BAZEL_RUNNER="$(command -v bazel)"; elif command -v bazelisk &gt;/dev/null 2&gt;&amp;1; then BAZEL_RUNNER="$(command -v bazelisk)"; else exit 1; fi; cargo run --quiet --manifest-path apps/canary/Cargo.toml -- verify-croissant-simple-groups-pair --runs-dir "$pair_root" &amp;&amp; cargo test --workspace &amp;&amp; cargo test -p fava-simple-groups --all-targets &amp;&amp; cargo test -p fava-simple-groups --doc &amp;&amp; cargo test --manifest-path apps/canary/Cargo.toml --all-targets &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; "$BAZEL_RUNNER" test //... &amp;&amp; "$BAZEL_RUNNER" test //... --config=clippy &amp;&amp; "$BAZEL_RUNNER" test //... --config=fmt-check &amp;&amp; find crates apps/canary/src -name '*.rs' -type f -exec awk 'FNR==1 {file=FILENAME} END {if (FNR&gt;=800) {print file ":" FNR; exit 1}}' {} \; &amp;&amp; git diff --check &amp;&amp; cargo run --quiet --manifest-path apps/canary/Cargo.toml -- verify-croissant-simple-groups-pair --runs-dir "$pair_root"</automated>
  </verify>
  <done>All GROUP-01 through GROUP-12 claims are supported by current causal, public, architecture, and independent two-relay evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

- One new retained owner-private pair root `apps/canary/runs/phase-07.1.1-pair.*` containing exactly two promoted sealed run directories.
- One new 0600 execution artifact `07.1.1-12-PAIR-ROOT.txt` containing the exact fresh pair_root used by both verifier passes.
- No source/API/vocabulary artifact; this plan executes final gates and independently validates the artifacts and owner evidence produced by Plans 01-11.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plans | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | README-shaped read/project/discover/publish across one/several hosts with relay-local authority and ordinary lifecycles | 04-11 | COVERED | Public and live evidence. |
| REQ | GROUP-01 | Generalized bounded Group identity | 04-05, 11 | COVERED | One-host cardinality recorded. |
| REQ | GROUP-02 | Exact h content Query with local visibility | 04, 05, 11 | COVERED | from_relays mutation. |
| REQ | GROUP-03 | Exact d multi-kind record Query with actual host evidence | 01, 04, 05, 11 | COVERED | Neutral prerequisite first. |
| REQ | GROUP-04 | Event-id dedup and exact serving evidence | 02, 08, 11 | COVERED | Shared plus unique events. |
| REQ | GROUP-05 | Host-local records/disagreement/attribution, no merge | 02, 06, 08, 11 | COVERED | Same-signer conflict. |
| REQ | GROUP-06 | Single-host fork choice; no global claims | 08, 10-11 | COVERED | Prohibition enforced. |
| REQ | GROUP-07 | Kind-blind exact-host publication | 04-06, 11 | COVERED | One Write/two handoffs. |
| REQ | GROUP-08 | Signed byte exactness and pre-custody refusal | 05-06, 11 | COVERED | Zero wire/custody. |
| REQ | GROUP-09 | Typed bounded records/pins/saved rows | 07-08, 11 | COVERED | Hostile parser corpus. |
| REQ | GROUP-10 | Ordinary discovery and kind-10009 semantic edits | 08, 09, 11 | COVERED | No ValueSet. |
| REQ | GROUP-11 | No lifecycle/dependency/switch | 03-11 | COVERED | Executable architecture/vocabulary. |
| REQ | GROUP-12 | Pure/public/two-relay bounds, lifecycle, handoff evidence | 05-11 | COVERED | Exactly two fresh runs. |
| RESEARCH | Q-KINDS | Multi-kind identity/local/wire through repeated approved Query::kind calls | 01, 04 | COVERED | Arbitrary kinds and fixed-six GroupRecords; no new vocabulary. |
| RESEARCH | RELAY-AUTH | Per-RelayUrl replaceable selection with AnyLocal preservation | 02 | COVERED | No protocol branch. |
| RESEARCH | VOCAB | Phase-slug/worktree false-positive regression | 03 | COVERED | Genuine mentions remain enforced. |
| RESEARCH | PARSE | Complete current NIP-29 records 39000-39005 plus saved rows/bounds | 07 | COVERED | Generic missing constants. |
| RESEARCH | EDIT | Target-local 10009 surgery; 9002/9010 ordinary | 09 | COVERED | Foreign bytes/order preserved. |
| RESEARCH | ARCH | Exact dependencies, README, vocabulary, universal falsifier | 04, 10 | COVERED | No normal facade edge. |
| RESEARCH | CANARY | Two children, staging/seal/scans/pair verification | 11-12 | COVERED | Prior pair is foundation only. |
| RESEARCH | BAZEL | Bazel/Bazelisk currently absent; no fallback | 12 | COVERED | Fail-closed precondition before claim. |
| CONTEXT | NAME | Crate/import exactly fava-simple-groups/fava_simple_groups | 04, 10 | COVERED | Approved issue 0019. |
| CONTEXT | IDENTITY | One opaque id over bounded non-empty host set; no RelayScope | 04, 11 | COVERED | Assumption delta promoted. |
| CONTEXT | MULTI | One/many hosts share value and preserve independent authority | 02, 04-05, 08, 11 | COVERED | Same-signer tracer. |
| CONTEXT | READS | Exact h/d ordinary Queries and host source policies | 01, 04-05 | COVERED | Present-empty handled. |
| CONTEXT | PROJECTION | Pure GroupSnapshot, per-host values/differ, no field merge | 08, 11 | COVERED | Whole-value comparison. |
| CONTEXT | FORK | App chooses through single-host Group | 08, 10-11 | COVERED | No canonical API. |
| CONTEXT | WRITES | Kind-blind complete Explicit route, signed exactness | 04-06, 11 | COVERED | Delivered facade words. |
| CONTEXT | DISCOVERY | Ordinary Query/pure projection; no completeness | 09 | COVERED | ValueSet excluded. |
| CONTEXT | PARSING | Typed bounded records and 10009 rows | 07 | COVERED | Apps avoid raw tags. |
| CONTEXT | EDITS | Only 10009 ReplaceableEventEdit | 09-10 | COVERED | 9002/9010 author-bearing. |
| CONTEXT | LIFECYCLE | No socket/observation/store/signer/router/publisher/delivery/runtime/transport owner | 05-06, 09-11 | COVERED | Architecture test. |
| CONTEXT | DEPENDENCY | Exactly fava-query/state/write; universal owners do not depend/branch | 04, 10 | COVERED | Cargo/Bazel equality. |
| CONTEXT | EVIDENCE | Controlled two-relay fork/publication/cancel/close proof | 11-12 | COVERED | Fresh twice. |
| CONTEXT | DEFERRED | on_many, previous references, UI/policy/UX/trust policy | — | EXCLUDED | Explicitly deferred/out of phase. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Committed source -> full validation tools | Missing tooling or partial suites can produce false completion claims. |
| Two retained runs -> independent verifier | Mutable, old, cross-run, unsigned, or incomplete evidence can counterfeit success. |
| Exact child handles -> host process table | Cleanup authority must not escape the four owned processes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-35 | Tampering | full validation claim | high | mitigate | Fail-closed Bazel preflight; Cargo/Bazel/Clippy/fmt/vocabulary/line/diff from committed source. |
| T-0711-36 | Spoofing | retained pair identity | high | mitigate | Independent verifier recomputes seals/hashes and requires distinct runs/four child identities. |
| T-0711-37 | Information disclosure | retained pair | high | mitigate | Both secret scans and atomic promotion; verifier rejects residue/unsafe manifests. |
| T-0711-38 | Denial of service | child/process cleanup | high | mitigate | Exact owned handles, both stops before propagation, bounded deadlines, never PID/name discovery. |
| T-0711-39 | Repudiation | edge/prohibition evidence | medium | mitigate | Named causal breaks, pre-break hashes, byte-exact restoration, restored GREEN. |
</threat_model>

<verification>
Run the exact Task 2 full matrix, independent pair verifier before/after it, all named deliberate breaks/restorations, exact child/staging/secret checks, and source/worktree cleanliness checks. Missing Bazel/Bazelisk blocks completion.
</verification>

<success_criteria>
Every GROUP requirement, edge probe, locked decision, research constraint, architecture gate, prohibition, and independent two-relay claim has current passing executable evidence; no source-audit item is missing.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-12-SUMMARY.md` when done.
</output>
