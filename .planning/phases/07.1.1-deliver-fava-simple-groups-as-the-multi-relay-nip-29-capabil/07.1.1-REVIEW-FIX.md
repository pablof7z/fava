---
phase: 07.1.1
fixed_at: 2026-08-22T23:27:15Z
review_path: .planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-REVIEW.md
iteration: 6
findings_in_scope: 2
fixed: 2
skipped: 0
status: all_fixed
---

# Phase 07.1.1: Code Review Fix Report

**Fixed at:** 2026-08-22T23:27:15Z
**Source review:** `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-REVIEW.md`
**Iteration:** 6

**Summary:**
- Findings in scope: 2
- Fixed: 2
- Skipped: 0

## Fixed Issues

### CR-01: Clean-build provenance is sampled before rustc consumes the source

**Files modified:** `apps/canary/build.rs`, `apps/canary/pinned-source.Dockerfile`, `apps/canary/pinned-output.Dockerfile`, `apps/canary/tools/`, private canary attestation/launcher/evidence modules, controlled-flow wire/proxy modules, live-fixture tests/environment, `apps/canary/Cargo.toml`, and `MODULE.bazel.lock`
**Commits:** immutable pipeline and adversarial repair series `2900a0e` through `34824b1`; evidence selector `50a8006`; exact selector-root correction `aac2ee4`; live-fixture isolation `a265df7`
**Applied fix:** The pinned build now applies exact Git objects into a canonical bounded source manifest, transports that snapshot through a digest-pinned owner-private loopback registry, and compiles only the exact read-only content-addressed source image with network disabled, numeric non-root compiler identity, enforceable PID/memory/CPU/log/deadline limits, bounded engine storage, and immutable subject-image extraction. The launcher and independent verifier bind the canonical source manifest, source image, build tree, base image, exact retained/executed Fava bytes, and final subject image. Atomic no-follow promotion, exact Docker resource identity/cleanup, and fail-closed source/build attestations prevent pathname, symlink, stale-volume, tag, registry, and rollback substitution.
**Causal evidence:** The post-build-script compiler wrapper mutates `apps/canary/src/main.rs`. Production read-only BuildKit execution refuses it with exact `EROFS`/86 and no promotable artifact; the named writable-source break compiles the hostile bytes, proving the guard is causal. Additional unchanged hostile oracles refuse post-sample Git-object substitution, stale/wrong image identities, mutable target paths, symlinked output leaves, pre-existing/mislabeled volumes, writable holder mounts, incomplete compiler environment/cwd replay, wrong registry/config digests, unbounded/hanging/log-flood Docker children, and non-atomic promotion. Every named break was restored before the committed build.
**Post-review hardening:** Fresh live execution exposed a producer/observer shutdown race. Commits `ab43732`, `8eeb30c`, and `34824b1` replace an arbitrary CLOSE count with exact per-relay bootstrap/content/records `REQ → EOSE → text CLOSE → socket close` binding, concurrent relay shutdown, bounded natural proxy-bridge drain, strict final newline-terminated wire parsing, and propagated capture errors. The prior live pair verifier supplied causal RED; named breaks `accept-two-query-roles` and `ignore-final-partial-tail` made unchanged asymmetric/partial-tail oracles exit 101, then restored GREEN.
**Selector correction:** The committed export-wrapper selector reproduced RED with `simple-groups pair must contain exactly two manifests`. Commit `aac2ee4` points to the exact inner two-manifest parent and adds `pair_verifier_requires_selector_to_name_exact_manifest_parent`; its named `selector_keeps_export_wrapper` break exited 101, restoration passed, and two clean selector-driven verifier invocations passed.
**Full-suite isolation:** The exact all-targets gate reproduced a NIP-02 terminal-receipt timeout twice under concurrent real Croissant tests while the failing test passed alone. Commit `a265df7` gives the four host-global live Croissant tests one test-only RAII owner across build/start/flow/teardown without changing production deadlines or process logic. `live_croissant_fixture_has_one_suite_owner` passed; named break `live_fixture_uses_per_caller_mutex` made the unchanged oracle exit 101; restoration and two default-thread all-targets runs passed 87/87 plus 1/1 CLI each.
**Status:** fixed: requires human verification

### WR-01: The Linux secret-marker regression uses an unbounded-size test executable

**Files modified:** `apps/canary/src/croissant_simple_groups_evidence_semantics.rs`
**Commit:** `883a28c`
**Applied fix:** The regression uses a deliberately small real executable, asserts it remains below the 128 MiB production bound, and preserves the exact-executable-only generic-marker exemption while derived secret needles remain scanned.
**Causal evidence:** The old debug `current_exe` fixture exceeded the Linux bound before reaching its marker assertion. The small real executable reaches that assertion, exempts only its exact retained path, and still rejects marker-bearing non-executable evidence.
**Status:** fixed

## Verification

Source gates ran against clean isolated worktree `gsd-reviewfix/0711-i6-62003`; the immutable build and both live runs consumed exact committed code `34824b1`, the exact selector correction is `aac2ee4`, and final test isolation is `a265df7`. The owner-private retained pair was copied atomically into the main checkout only after its candidate verifier passed; the verifier passed again after retention and twice from the corrected committed selector.

- Independent delta re-review was CLEAN at exact `34824b1` after checking exact wire directions, terminal ordering, pair symmetry, bounded natural drain, strict final EOF, child-error propagation, and all touched files below 500 lines.
- `cargo test --workspace` passed in the bounded Linux lane. `cargo test -p fava-simple-groups --all-targets` passed. Host-dependent `cargo test --locked --manifest-path apps/canary/Cargo.toml --all-targets` passed twice at 87/87 library tests and 1/1 CLI test on the owning macOS worktree after exact live-fixture isolation.
- Strict workspace Clippy and formatting passed in the bounded Linux lane. Strict canary Clippy and formatting passed on the owning macOS worktree.
- Bazel normal, strict-Clippy, and fmt-check modes each passed 49/49 test targets.
- Vocabulary validation passed with 10/10 unit tests. The 800-line hard gate, touched-file 500-line gate, and `git diff --check` passed.
- Fresh pinned output: `/tmp/fava-i6-pinned-final20.PN2hRm`; source manifest SHA-256 `2fb67c6d13f55c9934f4377ebc041f39f8a82ee2daa58240ca8c30d78b0f6598`; source image SHA-256 `476971fb9260adcb5ccf11fac1b8c986a8f1e301b357305cfb621922d49da8dc`; subject image SHA-256 `92620009f448714498df0ada55a1703f0f7462789cd76a03bbdf4048db8bca67`; exact retained/executed canary SHA-256 `5ce5d6d96492ac7368dd336a9f1e45c153ea107d9935f8c1252dcae3f5ccae3f`.
- Fava build revision `34824b1c8f32a7dcabf81fd8ba4779ac5b8b050f`; root build tree `caeaa5e0c650fa769d0b3f2c8f821a88d29a3efa`; relevant source-tree SHA-256 `f19cc729dbfbd77bffe61b46cfa4e3778a817021c2b918c96cd48db4e6d3a470`.
- Fresh exact pair root: `apps/canary/runs/phase-07.1.1-pair.i6.SjgbQz/pair`; run IDs `croissant-simple-groups-public-flow-c99d9a8f7dc08508` and `croissant-simple-groups-public-flow-ef604ede1af13861`; four distinct child PIDs `46`, `54`, `105`, and `113`; ports `45685`, `33853`, `34915`, and `33109`. All reached readiness, were exactly reaped, closed their ports, and removed staged executables.
- Croissant source revision `9c4c93e84852bd9aa6824060b74c56ab2ce812c2`; exact sealed/executed image SHA-256 `eb6a1ef42b2467421805c138b30dc481007d15da92382ff6bd6e7c917455027f`.
- The prior iteration-five pair and pinned output remain preserved. Failed candidates were never selected. Only exact owned build outputs were cleaned; no unrelated process, Docker resource, worktree, backup stash, or Phase 08 state was touched.

---

_Fixed: 2026-08-22T23:27:15Z_
_Fixer: the agent (gsd-code-fixer)_
_Iteration: 6_
