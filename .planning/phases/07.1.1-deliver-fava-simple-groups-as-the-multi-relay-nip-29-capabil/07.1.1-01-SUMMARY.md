---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 01
subsystem: query
tags: [rust, query-identity, nip-01, subscriptions, bazel, tdd]

requires:
  - phase: 06.1
    provides: canonical literal query identity, local evaluation, and exact wire-encoding patterns
provides:
  - repeated approved Query::kind calls form one canonical multi-kind selection
  - arbitrary multi-kind local evaluation and exact NIP-01 REQ evidence
  - explicit Bazel targets for both new integration-test owners
affects: [07.1.1-04, GroupRecords, fava-simple-groups]

actuals:
  tokens: 1660
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns: [repeated singleton calls union through an existing canonical BTreeSet]

key-files:
  created:
    - crates/fava-query-standard/tests/multi_kind.rs
    - crates/fava-subscriptions/tests/kinds.rs
  modified:
    - crates/fava-query/src/selection.rs
    - crates/fava-query/tests/query_identity.rs
    - crates/fava-query-standard/BUILD.bazel
    - crates/fava-subscriptions/BUILD.bazel

key-decisions:
  - "Keep Query::kind as the sole singleton-input surface; repeated calls union in the existing canonical BTreeSet without new query vocabulary."

patterns-established:
  - "Neutral multi-kind proof spans identity, local evaluation, and exact wire encoding with arbitrary kinds."
  - "Each causal test compiles before runtime RED and is independently falsified after GREEN."

requirements-completed: [GROUP-03]

coverage:
  - id: D1
    description: Repeated Query::kind calls retain every canonical kind through identity and local evaluation.
    requirement: GROUP-03
    verification:
      - kind: integration
        ref: "cargo test -p fava-query --test query_identity repeated_kind_identity_is_canonical -- --exact"
        status: pass
      - kind: integration
        ref: "cargo test -p fava-query-standard --test multi_kind repeated_kind_local_selection_is_complete -- --exact"
        status: pass
    human_judgment: false
  - id: D2
    description: The exact NIP-01 REQ filter encodes every selected kind once in canonical order.
    requirement: GROUP-03
    verification:
      - kind: integration
        ref: "cargo test -p fava-subscriptions --test kinds repeated_kind_req_encoding_is_complete -- --exact"
        status: pass
    human_judgment: false

duration: 5min
completed: 2026-08-22
status: revoked
revoked: 2026-08-24
---

> **Revoked record:** Results, requirement completions, hashes, and pass claims
> below describe a superseded implementation. They are not evidence for the
> current worktree. Only `07.1.1-VALIDATION.md` may record current executions;
> issue 0023 blocks the undecided `SimpleGroup` relay-input boundary.

# Phase 07.1.1 Plan 01: Neutral Multi-Kind Query Summary

**Repeated singleton `Query::kind` calls now preserve one canonical kind set through query identity, local evaluation, and exact NIP-01 encoding.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-08-22T09:07:20Z
- **Completed:** 2026-08-22T09:12:33Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Changed the existing approved `Query::kind(Kind) -> Query` method from replacement to canonical repeated-call union while preserving singleton behavior.
- Added exactly three focused arbitrary-kind descriptors for identity, local selection, and exact wire encoding.
- Registered the new query-standard and subscriptions integration tests as explicit Bazel targets without changing generic subscription production code.

## Task Commits

1. **Task 1 RED: Add compile-complete failing multi-kind evidence** - `3166a41` (`test`)
2. **Task 1 GREEN: Compose repeated kind selections** - `7670a09` (`feat`)
3. **Task 2: Register and independently falsify wire evidence** - `a58a600` (`test`)

## Edge Evidence Records

| row_id | descriptor | exact_command | causal_red | named_break | pre_break_sha256 | restored_sha256 | restored_green |
|---|---|---|---|---|---|---|---|
| E07 | `repeated_kind_identity_is_canonical` | `cargo test -p fava-query --test query_identity repeated_kind_identity_is_canonical -- --exact` | Task 1 RED exited 101 because adjacent/reordered calls retained different singleton kinds; implementation GREEN passed 1/1. | `query_kind_overwrites_previous_kind` exited 101 at the canonical identity assertion. | `908c8ccbe9d9f8ea4408d3bfb2d51d10013113d15df5e3ed3d7346a05b3cb1a0` | `908c8ccbe9d9f8ea4408d3bfb2d51d10013113d15df5e3ed3d7346a05b3cb1a0` | Exact selector passed 1/1 after byte-exact restoration. |
| E09 | `repeated_kind_req_encoding_is_complete` | `cargo test -p fava-subscriptions --test kinds repeated_kind_req_encoding_is_complete -- --exact` | Task 1 causal wire RED exited 101 with `[30002]` instead of `[30001,30002]`; implementation GREEN passed 1/1. | `query_req_emits_first_kind_only` exited 101 with `[30001]`; identity and local controls both stayed GREEN. | `28c909650decd9426c0a8a4e43c72d090a5a0cd5dda57f5ccd54a82a2db557b1` | `28c909650decd9426c0a8a4e43c72d090a5a0cd5dda57f5ccd54a82a2db557b1` | Exact selector passed 1/1 after byte-exact restoration. |

## Files Created/Modified

- `crates/fava-query/src/selection.rs` - canonical repeated-call union and exact rustdoc semantics.
- `crates/fava-query/tests/query_identity.rs` - E07 identity, hashing, duplicate/order, and singleton proof.
- `crates/fava-query-standard/tests/multi_kind.rs` - focused arbitrary-kind local evaluator proof.
- `crates/fava-query-standard/BUILD.bazel` - explicit `multi_kind` integration target.
- `crates/fava-subscriptions/tests/kinds.rs` - E09 exact NIP-01 REQ proof.
- `crates/fava-subscriptions/BUILD.bazel` - explicit `kinds` integration target.

## Decisions Made

- The existing `Query::kind` remains the only public input surface and consumes exactly one `Kind`; collection bounds and new refusal vocabulary are structurally unnecessary.
- The subscription conversion remains unchanged because it already consumes the complete canonical selection set.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `python3 tools/check_vocabulary.py` retains the known pre-existing Phase 07.1.1 slug and sibling-worktree false positives owned by a later plan; its six unit tests pass. This plan introduced no vocabulary.
- Bazel/Bazelisk is not on `PATH`; both required targets were registered, and execution remains explicitly deferred to the phase's fail-closed final preflight.

## User Setup Required

None.

## Next Phase Readiness

- `GroupRecords` can chain the approved singleton method over its fixed six-kind set.
- No neutral owner contains NIP-29 constants, group ids, collection builders, or protocol branches.

## Self-Check: PASSED

All created files and task commits exist; E07/E09 evidence rows and equal restoration hashes are present.

---
*Phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil*
*Completed: 2026-08-22*
