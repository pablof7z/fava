---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 03
type: historical
status: revoked
revoked: 2026-08-24
wave: 1
depends_on: []
files_modified:
  - tools/check_vocabulary.py
  - tools/tests/test_vocabulary_check.py
autonomous: true
requirements: [GROUP-11]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Phase frontmatter slugs and linked-worktree path segments are structurally ignored as metadata, not treated as crate declarations."
    - "A genuine crate reference in prose or code on the same line/path remains collected and rejected unless registered."
    - "The narrow regression does not suppress the approved fava-simple-groups crate mention or any unrelated unregistered crate."
  prohibitions:
    - status: flagged-unverified
      statement: "Vocabulary validation cannot hide genuine crate mentions by excluding planning prose, whole paths, or crate-like tokens globally."
  artifacts:
    - path: tools/tests/test_vocabulary_check.py
      provides: "Neutral phase-slug and linked-worktree false-positive regression matrix"
  key_links:
    - from: tools/check_vocabulary.py
      to: docs/internals/vocabulary.toml
      via: "structural candidate collection still validates genuine crate mentions"
      pattern: "collect_spec_vocabulary"
---

> **Revoked record:** This plan targets obsolete APIs and ownership. Nothing in
> this file is a current instruction, signature, approval, or acceptance claim.
> Current requirements and evidence are recorded in `.planning/REQUIREMENTS.md`
> and `07.1.1-VALIDATION.md`; the open relay-input decision is issue 0023.

## Historical Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Repair the two live vocabulary-checker false positives without weakening the closed vocabulary gate.

Purpose: Public crate validation must be trustworthy before the approved `fava-simple-groups` registry is promoted.
Output: Neutral metadata/path classification with positive and negative regression evidence.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/.continue-here.md
@tools/check_vocabulary.py
@tools/tests/test_vocabulary_check.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce phase-slug and linked-worktree false positives neutrally</name>
  <read_first>
    @tools/check_vocabulary.py
    @tools/tests/test_vocabulary_check.py
    @docs/internals/vocabulary.toml
  </read_first>
  <files>tools/tests/test_vocabulary_check.py</files>
  <behavior>
    - YAML/frontmatter `slug:` metadata containing a long crate-like phase identifier does not declare a crate.
    - A linked-worktree prefix/path segment containing the same identifier does not declare a crate.
    - A genuine approved or unregistered crate mention later on the same line remains visible to collection.
  </behavior>
  <action>Add exact tests `test_phase_slug_metadata_is_not_a_crate`, `test_linked_worktree_prefix_is_not_a_crate`, and `test_metadata_exclusion_does_not_hide_real_crate_reference`; record the first two RED against the live false positives and require the third control to pass. Fixtures must be neutral and temporary; do not special-case this phase number or repository absolute path.</action>
  <acceptance_criteria>Both real false-positive shapes reproduce, and the control proves the proposed boundary cannot suppress a genuine later crate reference.</acceptance_criteria>
  <verify>
    <automated>! python3 -m unittest tools.tests.test_vocabulary_check.VocabularyCheckTests.test_phase_slug_metadata_is_not_a_crate &amp;&amp; ! python3 -m unittest tools.tests.test_vocabulary_check.VocabularyCheckTests.test_linked_worktree_prefix_is_not_a_crate</automated>
  </verify>
  <done>The checker defects are causal and bounded by positive controls before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify metadata/path candidates without global suppression</name>
  <read_first>
    @tools/check_vocabulary.py
    @tools/tests/test_vocabulary_check.py
  </read_first>
  <files>tools/check_vocabulary.py, tools/tests/test_vocabulary_check.py</files>
  <behavior>
    - Only structural metadata values and linked-worktree prefix segments are excluded.
    - Prose/code mentions of `fava-simple-groups` are still collected.
    - An unregistered crate anywhere else, including later on the excluded line/path, still fails validation.
  </behavior>
  <action>Implement a narrow structural classifier beside the existing candidate collector, following the established evidence-path exception style. Do not exclude `.planning`, whole frontmatter blocks, whole lines, or tokens matching a broad suffix. Run GREEN, then record the source checksum and deliberately broaden the exclusion to the whole line; require `test_metadata_exclusion_does_not_hide_real_crate_reference` to fail, restore byte-exactly, verify the checksum, rerun the complete vocabulary unit suite, and run the live checker.</action>
  <acceptance_criteria>The two false positives disappear, real crate references remain enforced, and the broad-suppression deliberate break fails causally.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; git diff --check</automated>
  </verify>
  <done>Vocabulary validation is green and remains strict enough for subsequent public crate promotion.</done>
</task>

</tasks>

## Artifacts this phase produces

- New neutral regression tests in `tools/tests/test_vocabulary_check.py`; no new project vocabulary.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Documentation/path text -> architectural registry validation | Untrusted prose and filesystem metadata can create false declarations or hide real ones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-07 | Tampering | vocabulary candidate exclusion | high | mitigate | Structural, narrow classification with same-line real-crate controls. |
| T-0711-08 | Repudiation | live vocabulary result | medium | mitigate | Run both focused unit matrix and live repository checker. |
</threat_model>

<verification>
Run the complete Python vocabulary suite, live checker, formatting where applicable, and diff checks; retain causal broad-suppression mutation evidence.
</verification>

<success_criteria>
The live checker ignores only phase/path metadata false positives and continues to reject every genuine unregistered crate mention.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-03-SUMMARY.md` when done.
</output>
