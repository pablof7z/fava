---
phase: "07.1.1"
researched: 2026-08-22
revoked: 2026-08-24
status: revoked
---

# Phase 07.1.1 — Revoked research

The original research was implementation-directed and is obsolete. It claimed
an application relay-selection owner in `fava-state`; that owner is rejected.
No conclusion or signature from the original document remains authoritative.

Current source inspection establishes only these facts:

1. `SimpleGroup` needs to retain application-selected relays for later query
   and publication composition.
2. An arbitrary Rust iterator can be infinite.
3. A numeric relay bound is not owned by `fava-simple-groups` or `fava-state`.
4. `fava-query` and `fava-write` own distinct operation bounds and exact
   failures.
5. Pablo selected one required parsed relay plus a finite owned `Vec` tail,
   making empty and arbitrary-iterator construction impossible without a
   numeric domain bound.
6. Focused evidence covers the event-local decoders, saved-list edits, facade
   consumers, canary compilation, and generated API inventory; the fresh full
   matrix remains a separate completion gate.
7. Pablo approved query-owned `Query::intersect_tag_values`: an absent axis
   becomes the supplied set, an existing axis narrows by set intersection, and
   disjoint values remain present-empty match-nothing. The operation uses the
   existing query-owned bounded-input refusal.

`docs/issues/0023-simple-group-relay-input-boundary.md` contains the approved
constructor decision, forcing requirement, and executable falsifiers.
`docs/issues/0024-query-tag-axis-composition.md` contains the approved GROUP-02
query-owner decision and executable falsifiers.
