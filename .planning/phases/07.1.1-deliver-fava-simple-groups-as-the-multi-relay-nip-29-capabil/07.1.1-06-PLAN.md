---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 06
type: historical
status: revoked
revoked: 2026-08-24
wave: 5
depends_on: [07.1.1-05]
files_modified:
  - crates/fava/Cargo.toml
  - crates/fava/BUILD.bazel
  - crates/fava/tests/simple_groups.rs
  - crates/fava-simple-groups/src/group.rs
  - crates/fava-simple-groups/src/tests/query.rs
autonomous: true
requirements: [GROUP-02, GROUP-03, GROUP-07, GROUP-08, GROUP-11]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Accepted local group content remains visible before relay acknowledgement because content uses from_relays/AnyLocal."
    - "Write-store-only record events remain excluded because records use OnlyRelays and actual selected-host evidence."
    - "A prepared arbitrary-kind payload publishes through fava.to(group.hosts()).publish and returns ordinary Write with exact first-occurrence destinations."
    - "Valid pre-signed input crosses publication byte-identically; missing, empty, duplicate, or contradictory h refuses before custody, signing, routing, or publisher calls."
    - "Concurrent ordinary observations/writes retain exact operation/generation isolation and cancellation/close remain owned by existing Fava lifecycles."
  prohibitions:
    - status: flagged-unverified
      statement: "No facade production dependency or universal NIP-29 behavior switch is introduced."
    - status: flagged-unverified
      statement: "No signed event mutation or protocol-specific custody/receipt wrapper is exposed."
  artifacts:
    - path: crates/fava/tests/simple_groups.rs
      provides: "Public facade content/record/publication/pre-custody corpus"
  key_links:
    - from: crates/fava/tests/simple_groups.rs
      to: crates/fava/src/publication.rs
      via: "prepared payload -> to(hosts) -> publish -> ordinary Write"
      pattern: "\.to\(.*hosts.*\).*publish"
---

> **Revoked record:** This plan targets obsolete APIs and ownership. Nothing in
> this file is a current instruction, signature, approval, or acceptance claim.
> Current requirements and evidence are recorded in `.planning/REQUIREMENTS.md`
> and `07.1.1-VALIDATION.md`; the open relay-input decision is issue 0023.

## Historical Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Prove the Group tracer through the public Fava facade: local-visible reads, host-evidenced records, and kind-blind exact-host publication.

Purpose: Pure capability values become useful only through the already delivered ordinary lifecycles; this plan proves the wiring without changing facade production.
Output: Dev/test-only facade integration and causal pre-custody/wire evidence.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-05-SUMMARY.md
@crates/fava/tests/explicit_publication.rs
@crates/fava/tests/explicit_live.rs
@crates/fava/tests/multi_relay.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove local-visible group content and relay-evidenced records</name>
  <read_first>
    @crates/fava/Cargo.toml
    @crates/fava/BUILD.bazel
    @crates/fava/tests/local_source_merge.rs
    @crates/fava/tests/multi_relay.rs
    @crates/fava-simple-groups/src/query.rs
  </read_first>
  <files>crates/fava/Cargo.toml, crates/fava/BUILD.bazel, crates/fava/tests/simple_groups.rs, crates/fava-simple-groups/src/tests/query.rs</files>
  <behavior>
    - An accepted unpublished local matching content event appears through Group::events.
    - A write-store-only record does not appear through Group::records.
    - Actual A/B relay record evidence appears and proposed/contacted-but-not-serving hosts do not.
    - Empty snapshots are empty observations, not absence/completeness claims.
  </behavior>
  <action>Add `fava-simple-groups` only as a Cargo dev dependency and only to the new Bazel test target; keep production facade manifests/source free of it. Create exact tests `simple_group_content_preserves_local_visibility` and `simple_group_records_require_actual_host_evidence` first and record RED before integration. Use ordinary public Fava assembly and sources. After GREEN, deliberately swap the two source policies, require both tests to fail their owning assertions, restore exact checksums, and rerun.</action>
  <acceptance_criteria>Content and records reach the public observation lifecycle with their exact distinct authorities, empty evidence remains honest, and the facade has no normal capability edge.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test simple_groups simple_group_content_preserves_local_visibility -- --exact &amp;&amp; cargo test -p fava --test simple_groups simple_group_records_require_actual_host_evidence -- --exact</automated>
  </verify>
  <done>Applications can observe Group reads through Fava without a second observation owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish arbitrary prepared events to every exact host</name>
  <read_first>
    @crates/fava/src/lib.rs
    @crates/fava/src/publication.rs
    @crates/fava/tests/explicit_publication.rs
    @crates/fava-write/src/routing.rs
    @crates/fava-simple-groups/src/group.rs
  </read_first>
  <files>crates/fava/tests/simple_groups.rs, crates/fava-simple-groups/src/group.rs</files>
  <behavior>
    - Arbitrary kind 50029 gains exactly one h only when absent.
    - Exact prepared hosts become one ordered Explicit route with one desired lane/handoff per distinct host.
    - The returned value is ordinary `Write`; no intent/accepted/group receipt leaks to the app.
    - Repeating pure preparation is deterministic and inert before publish.
  </behavior>
  <action>Create the final E21 descriptor and complete scenario `simple_group_arbitrary_kind_publication_uses_complete_exact_route`, then compile its explicit target with `cargo test -p fava --test simple_groups --no-run`. Run exact selector `cargo test -p fava --test simple_groups simple_group_arbitrary_kind_publication_uses_complete_exact_route -- --exact` and capture causal runtime RED at the complete-route assertion. Only then implement publication as `fava.to(group.hosts())?.publish(group.prepare(payload)?)`, assert ordinary Write/Receipt identities, exact route order, one lane per normalized host, and local visibility, and rerun the exact selector GREEN. Do not add a group publish method that owns work or call a router. Capture pre-break SHA-256, independently apply named post-GREEN break `publication_routes_first_host_only` that actually routes only the first host, require the same exact selector RED, restore byte-exactly with equal SHA-256, rerun the same selector restored GREEN, and record E21 in `07.1.1-06-SUMMARY.md`.</action>
  <acceptance_criteria>Any event kind uses the complete exact host route and ordinary Write lifecycle, duplicate hosts never duplicate handoffs, and no automatic router work starts.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test simple_groups --no-run &amp;&amp; cargo test -p fava --test simple_groups simple_group_arbitrary_kind_publication_uses_complete_exact_route -- --exact</automated>
  </verify>
  <done>Prepared group payloads publish kind-blindly through the delivered public door.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refuse invalid signed context before custody and preserve lifecycle isolation</name>
  <read_first>
    @crates/fava/tests/explicit_publication.rs
    @crates/fava/tests/explicit_live.rs
    @crates/fava-simple-groups/src/group.rs
  </read_first>
  <files>crates/fava/tests/simple_groups.rs</files>
  <behavior>
    - Valid signed publication preserves serialized bytes, id, and signature exactly.
    - Missing, missing-value, present-empty, duplicate-adjacent, and contradictory h each refuse before store/signer/router/publisher calls and zero EVENT frames.
    - Concurrent ordinary observation/publication generations cannot deliver stale completion across operations.
    - Cancellation wakes pending pulls and close is idempotent with no group-owned resource.
  </behavior>
  <action>Add `simple_group_presigned_context_refuses_before_custody` and `simple_group_uses_ordinary_lifecycle_isolation` RED fixtures with spy providers/session generations. GREEN should require no production facade change. Assert zero custody rows, signer calls, router calls, publisher calls, and matching frames for every invalid matrix row. Record checksum, deliberately rebuild valid signed input or admit duplicate h, require byte-identity/pre-custody assertions to fail, restore byte-exactly, and rerun all controls.</action>
  <acceptance_criteria>Signed exactness, refusal timing, zero side effects, concurrent generation isolation, cancellation, and idempotent close all pass through existing public owners.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test simple_groups simple_group_presigned_context_refuses_before_custody -- --exact &amp;&amp; cargo test -p fava --test simple_groups simple_group_uses_ordinary_lifecycle_isolation -- --exact &amp;&amp; cargo clippy -p fava -p fava-simple-groups --all-targets -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; git diff --check</automated>
  </verify>
  <done>Every facade group operation remains exact, kind-blind, and owned by ordinary Fava lifecycles.</done>
</task>

</tasks>

## Artifacts this phase produces

- New facade integration test file `crates/fava/tests/simple_groups.rs` and Bazel target.
- Dev/test-only facade dependency on `fava-simple-groups`; no production facade source change.
- Exact public descriptors listed in Tasks 1-3.
- Execution artifact: `07.1.1-06-SUMMARY.md` section `## Edge Evidence Records` with stable columns `row_id | descriptor | exact_command | causal_red | named_break | pre_break_sha256 | restored_sha256 | restored_green`, containing E21.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Capability-prepared value -> facade custody | Invalid signed context must not enter durable or asynchronous work. |
| Facade -> selected relay sessions | Exact host routing and generation identity cross to provider calls/wire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-12 | Tampering | signed event custody | high | mitigate | Byte identity plus zero-custody/signer/router/publisher/frame matrix. |
| T-0711-13 | Spoofing | record provenance | high | mitigate | Require actual selected RelayEvidence rather than encoded/proposed hosts. |
| T-0711-14 | Elevation of privilege | saved/selected host execution | medium | mitigate | Only application-explicit Group hosts passed to ordinary `to`; parsing never opens work. |
| T-0711-15 | Denial of service | concurrent lifecycle | medium | mitigate | Reuse bounded existing operation/generation/cancel/close owners and prove isolation. |
</threat_model>

<verification>
Run the complete `simple_groups` facade target, crate tests, strict Clippy, formatting, architecture search, line limits, and diff checks with causal source-policy/route/signed breaks restored.
</verification>

<success_criteria>
GROUP-02/03/07/08/11 are proven through public Fava without any group lifecycle, normal facade dependency, signed mutation, or universal protocol switch.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-06-SUMMARY.md` when done.
</output>
