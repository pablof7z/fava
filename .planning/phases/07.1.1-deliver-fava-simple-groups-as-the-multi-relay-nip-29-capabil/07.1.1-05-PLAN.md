---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 05
type: execute
wave: 4
depends_on: [07.1.1-04]
files_modified:
  - crates/fava-simple-groups/src/lib.rs
  - crates/fava-simple-groups/src/bounds.rs
  - crates/fava-simple-groups/src/group.rs
  - crates/fava-simple-groups/src/query.rs
  - crates/fava-simple-groups/src/tests.rs
  - crates/fava-simple-groups/src/tests/group.rs
  - crates/fava-simple-groups/src/tests/query.rs
  - crates/fava-simple-groups/tests/public_api.rs
autonomous: true
requirements: [GROUP-01, GROUP-02, GROUP-03, GROUP-07, GROUP-08, GROUP-11]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Group host input consumes at most MAX_GROUP_HOST_INPUT_ITEMS + 1 total items, refuses zero and over-bound input, and terminates on an infinite iterator without hidden truncation."
    - "Opaque group ids refuse empty and greater-than-MAX_GROUP_ID_BYTES values without trimming or normalization."
    - "Content Query construction requires an explicit result bound no greater than MAX_GROUP_CONTENT_RESULTS and preserves AnyLocal; record Query construction preserves exact RelayUrl authority."
    - "Unsigned preparation normalizes exactly one valid h context; signed inputs are byte-identical and invalid context refuses before custody or wire work."
  prohibitions:
    - status: flagged-unverified
      statement: "No hidden unbounded or truncated host, id, Query, tag, or signed input is accepted."
    - status: flagged-unverified
      statement: "Signed event bytes are never rebuilt, normalized, reordered, or mutated."
  artifacts:
    - path: crates/fava-simple-groups/src/bounds.rs
      provides: "Private owner constants and bound+1 helpers; no new public vocabulary"
    - path: crates/fava-simple-groups/src/tests/group.rs
      provides: "Empty, oversized, infinite-host, and signed exactness evidence"
    - path: crates/fava-simple-groups/src/tests/query.rs
      provides: "Missing/oversized result-bound and source-authority evidence"
  key_links:
    - from: crates/fava-simple-groups/src/group.rs
      to: crates/fava-simple-groups/src/bounds.rs
      via: "total iterator consumption and opaque id refusal before normalization"
      pattern: "MAX_GROUP_HOST_INPUT_ITEMS|MAX_GROUP_ID_BYTES"
    - from: crates/fava-simple-groups/src/query.rs
      to: crates/fava-query/src/selection.rs
      via: "explicit result limit plus AnyLocal/exact-relay source semantics"
      pattern: "MAX_GROUP_CONTENT_RESULTS"
---

## Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Expand the production tracer across hostile construction, Query, and preparation edges with explicit private owner bounds.

Purpose: ensure generalized multi-host identity and signed-context safety are bounded and typed before facade integration.
Output: private bound owners plus causal empty/oversized/infinite and signed byte-exact evidence.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-04-SUMMARY.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-CONTEXT.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-RESEARCH.md
@crates/fava-simple-groups/src/group.rs
@crates/fava-simple-groups/src/query.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Register the internal test tree and bound Group identity</name>
  <read_first>
    @crates/fava-simple-groups/src/lib.rs
    @crates/fava-simple-groups/src/group.rs
    @crates/fava-simple-groups/src/query.rs
    @crates/fava-simple-groups/tests/public_api.rs
    @crates/fava-write/src/routing.rs
  </read_first>
  <files>crates/fava-simple-groups/src/lib.rs, crates/fava-simple-groups/src/bounds.rs, crates/fava-simple-groups/src/group.rs, crates/fava-simple-groups/src/tests.rs, crates/fava-simple-groups/src/tests/group.rs</files>
  <behavior>
    - Empty host input, the 257th total host input, and `std::iter::repeat(host)` are typed refusals after at most 257 pulls; duplicate inputs count toward the bound.
    - Duplicate/reordered hosts normalize in deterministic first-occurrence order through the same Group::on(hosts, id) constructor used for one and many.
    - Empty id and a 4097-byte id are typed refusals while a 4096-byte opaque id is preserved exactly.
    - `src/lib.rs` registers `#[cfg(test)] mod tests;`; `src/tests.rs` is the single test-module declaration owner.
    - `src/tests.rs` is the declaration owner for `group`, `query`, and future `records`, `snapshot`, and `saved`: Task 1 adds `group`, Task 2 adds `query`, and Plans 07/08/09 add each future declaration atomically with its matching file so every intermediate task compiles.
  </behavior>
  <action>Plan 04 already provides final Group signatures and one passing `Group::on([host], id)` path. Register `#[cfg(test)] mod tests;` in `src/lib.rs`. Make `src/tests.rs` the sole declaration owner and add only `mod group;` here because `src/tests/group.rs` lands in this task; Task 2 appends `mod query;`, and Plans 07, 08, and 09 append `mod records;`, `mod snapshot;`, and `mod saved;` only alongside each matching file. Add private constants `MAX_GROUP_HOST_INPUT_ITEMS = 256` and `MAX_GROUP_ID_BYTES = 4096`. Create final group descriptors, compile `cargo test -p fava-simple-groups --lib --no-run`, run exact selectors for causal runtime RED, then implement bound+1 total-input collection, first-occurrence normalization, and opaque id bounds for GREEN. Own E01 descriptor `group_construction_refuses_empty_oversized_and_infinite_hosts`, exact command `cargo test -p fava-simple-groups --lib tests::group::group_construction_refuses_empty_oversized_and_infinite_hosts -- --exact`, break `group_host_bound_counts_distinct_only`; E02 the same descriptor/command, break `group_host_input_silently_truncates`; and E03 descriptor `group_construction_preserves_first_occurrence_order`, exact command `cargo test -p fava-simple-groups --lib tests::group::group_construction_preserves_first_occurrence_order -- --exact`, break `group_hosts_sort_lexically`. For each, capture causal runtime RED, exact GREEN, pre-break SHA-256, named break exact-selector RED, byte-exact equal restored SHA-256, and restored exact-selector GREEN in `07.1.1-05-SUMMARY.md`.</action>
  <acceptance_criteria>The registered internal test tree compiles before RED; exact zero/bound/bound+1/infinite fixtures prove typed refusal and finite consumption, and all three host/id breaks restore byte-exactly.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --lib --no-run &amp;&amp; cargo test -p fava-simple-groups --lib tests::group::group_construction_refuses_empty_oversized_and_infinite_hosts -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::group::group_construction_preserves_first_occurrence_order -- --exact</automated>
  </verify>
  <done>The internal test tree is registered and host/id inputs are explicitly bounded with typed refusal and no hidden truncation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound ordinary content and record Query construction</name>
  <read_first>
    @crates/fava-simple-groups/src/query.rs
    @crates/fava-simple-groups/src/tests.rs
    @crates/fava-query/src/selection.rs
    @crates/fava-write/src/routing.rs
  </read_first>
  <files>crates/fava-simple-groups/src/bounds.rs, crates/fava-simple-groups/src/query.rs, crates/fava-simple-groups/src/tests.rs, crates/fava-simple-groups/src/tests/query.rs</files>
  <behavior>
    - Missing content-result limit and a limit of 4097 refuse; a positive limit at most 4096 preserves AnyLocal and all unrelated Query fields.
    - Record queries retain each selected exact RelayUrl with no field merge or canonical-host choice.
  </behavior>
  <action>Append `mod query;` to `src/tests.rs` in the same change that creates `src/tests/query.rs`. Add private `MAX_GROUP_CONTENT_RESULTS = 4096` and final query descriptors before implementation, compile with `cargo test -p fava-simple-groups --lib --no-run`, run their exact selectors for causal runtime RED, then implement positive content bounds, AnyLocal content policy, and exact relay record authority for GREEN. Own E05 descriptor `content_query_refuses_unbounded_and_oversized_results`, exact command `cargo test -p fava-simple-groups --lib tests::query::content_query_refuses_unbounded_and_oversized_results -- --exact`, break `content_limit_silently_clamps`; and E06 descriptor `content_query_preserves_any_local_visibility`, exact command `cargo test -p fava-simple-groups --lib tests::query::content_query_preserves_any_local_visibility -- --exact`, break `content_query_uses_only_from_relays`. For each, capture causal runtime RED, exact GREEN, pre-break SHA-256, named break exact-selector RED, byte-exact equal restored SHA-256, and restored exact-selector GREEN in `07.1.1-05-SUMMARY.md`.</action>
  <acceptance_criteria>Content queries refuse absent/oversized result bounds, preserve AnyLocal, and records retain exact RelayUrl authority.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --lib --no-run &amp;&amp; cargo test -p fava-simple-groups --lib tests::query::content_query_refuses_unbounded_and_oversized_results -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::query::content_query_preserves_any_local_visibility -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::query::record_query_uses_exact_relay_authority -- --exact</automated>
  </verify>
  <done>Ordinary content and record Query helpers are bounded and retain their distinct source authorities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Normalize unsigned context and preserve signed input exactly</name>
  <read_first>
    @crates/fava-simple-groups/src/group.rs
    @crates/fava-simple-groups/src/tests/group.rs
    @crates/fava-write/src/lib.rs
    @docs/spec/ARCHITECTURE.md
  </read_first>
  <files>crates/fava-simple-groups/src/group.rs, crates/fava-simple-groups/src/tests/group.rs, crates/fava-simple-groups/tests/public_api.rs</files>
  <behavior>
    - Unsigned absent-h input gains exactly one exact h tag; matching existing h remains once in original order.
    - Missing, present-empty, duplicate, contradictory, oversized, or over-bound signed context refuses before custody or wire work.
    - Every accepted signed input is byte-identical, including tag/content/order/signature/id bytes.
    - Repeating an already valid preparation is inert and deterministic.
  </behavior>
  <action>The final preparation signature and unsigned happy scenario already compile. Require the explicit `--lib` target first, then capture causal runtime RED at expected GroupError/byte equality. Consume contextual tags with bound+1 before custody; unsigned adds h only when absent and preserves foreign bytes/order, signed returns the original object unchanged or refuses with zero custody/wire calls. Own the following records: E04 descriptor `group_prepare_unsigned_context_is_lossless`, exact command `cargo test -p fava-simple-groups --lib tests::group::group_prepare_unsigned_context_is_lossless -- --exact`, actual break `unsigned_prepare_appends_duplicate_h`; E19 the same descriptor and exact command, actual break `unsigned_prepare_keeps_duplicate_h`; E20 descriptor `group_prepare_signed_is_byte_exact_or_refuses`, exact command `cargo test -p fava-simple-groups --lib tests::group::group_prepare_signed_is_byte_exact_or_refuses -- --exact`, actual break `signed_prepare_accepts_empty_h`; E22 the same descriptor and exact command, actual break `signed_prepare_admits_duplicate_h`; E23 descriptor `signed_invalid_context_refuses_before_custody`, exact command `cargo test -p fava-simple-groups --lib tests::group::signed_invalid_context_refuses_before_custody -- --exact`, actual break `signed_validation_runs_after_custody` that performs one spy custody call before refusal; E24 descriptor `group_prepare_signed_is_byte_exact_or_refuses`, exact command `cargo test -p fava-simple-groups --lib tests::group::group_prepare_signed_is_byte_exact_or_refuses -- --exact`, actual break `signed_prepare_rebuilds_event`. For each record, capture causal runtime RED before behavior implementation, then GREEN, pre-break SHA-256, named actual break RED, byte-exact restored SHA-256 equal to the pre-break value, and restored GREEN in `07.1.1-05-SUMMARY.md`. Restore after each mutation.</action>
  <acceptance_criteria>The complete final API compiles before runtime RED; unsigned normalization is inert/lossless, signed inputs are accepted byte-identically or refused before custody, and named mutation/admission breaks restore byte-exactly.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --lib --no-run &amp;&amp; cargo test -p fava-simple-groups --lib tests::group::group_prepare_unsigned_context_is_lossless -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::group::group_prepare_signed_is_byte_exact_or_refuses -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::group::signed_invalid_context_refuses_before_custody -- --exact</automated>
  </verify>
  <done>Preparation is pure, bounded, lossless for unsigned input, and byte-exact or fail-closed for signed input.</done>
</task>

</tasks>

## Artifacts this phase produces

- New private owner file: `crates/fava-simple-groups/src/bounds.rs` with `MAX_GROUP_HOST_INPUT_ITEMS`, `MAX_GROUP_ID_BYTES`, and `MAX_GROUP_CONTENT_RESULTS`.
- New internal test modules: `src/tests.rs`, `src/tests/group.rs`, and `src/tests/query.rs`.
- Exact edge tests for empty/oversized/infinite construction, bounded Queries, exact authority, signed byte identity, and pre-custody refusal.
- Execution artifact: `07.1.1-05-SUMMARY.md` section `## Edge Evidence Records` with columns `row_id | descriptor | exact_command | causal_red | named_break | pre_break_sha256 | restored_sha256 | restored_green`, containing E01, E02, E03, E04, E05, E06, E19, E20, E22, E23, and E24.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hostile iterators -> Group | Empty, duplicate, oversized, or infinite input can consume unbounded work. |
| Protocol values -> preparation | Malformed/oversized context can cross toward signed custody. |
| Signed event -> capability | Validation must not mutate cryptographic bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-05A | Denial of service | host/tag iterators | high | mitigate | Private bounds, bound+1 total consumption, and infinite-iterator tests. |
| T-0711-05B | Tampering | signed preparation | critical | mitigate | Original-object return, exact-byte assertions, and rebuild deliberate break. |
| T-0711-05C | Spoofing | record source | high | mitigate | Exact RelayUrl assertions for every host; no canonical selection. |
| T-0711-05D | Information disclosure | errors | medium | mitigate | Typed refusal does not echo raw hostile rows or secret-bearing signed data. |
</threat_model>

<verification>
Run every exact task target, the complete crate test corpus, strict Clippy, formatting, vocabulary, line-limit, and diff gates. Confirm no touched code file exceeds 500 lines unless one exact cohesion reason is recorded; none may exceed 800.
</verification>

<success_criteria>
All construction, Query, and preparation inputs have explicit owner bounds or typed refusal, bound+1/infinite evidence is causal, and signed input remains byte-identical.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-05-SUMMARY.md` when done.
</output>
