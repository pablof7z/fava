---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 09
type: execute
wave: 8
depends_on: [07.1.1-08]
files_modified:
  - crates/fava-simple-groups/src/bounds.rs
  - crates/fava-simple-groups/src/lib.rs
  - crates/fava-simple-groups/src/saved.rs
  - crates/fava-simple-groups/src/edit.rs
  - crates/fava-simple-groups/src/group.rs
  - crates/fava-simple-groups/src/tests.rs
  - crates/fava-simple-groups/src/tests/saved.rs
  - crates/fava-simple-groups/tests/public_api.rs
  - crates/fava/tests/simple_groups.rs
autonomous: true
requirements: [GROUP-09, GROUP-10, GROUP-11]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Saved-group, saved-relay, admin, and member discovery return ordinary Query values; no ValueSet is implemented."
    - "Duplicate/reordered authors or subjects canonicalize through Query sets, present-empty inputs match nothing, and pure snapshot projection repeats idempotently in deterministic order."
    - "Discovery consumes at most MAX_DISCOVERY_INPUT_ITEMS + 1 total author/subject inputs, refuses the 257th, and terminates on infinite iterators without truncation."
    - "groups_saved_by is a bounded pure QuerySnapshot projection and retains exact saving author plus id/host keys."
    - "Only kind 10009 changes use ReplaceableEventEdit; kinds 9002 and 9010 remain ordinary author-bearing prepared events."
    - "Save/rename/remove group rows target exact id+selected-host pairs; same-id other-host rows, foreign tags, extra columns, opaque content, and exact unrelated order survive."
    - "Multi-host save appends one row per selected host in deterministic first-occurrence host order; duplicate matching rows collapse deterministically and repeated operations are inert."
  prohibitions:
    - status: flagged-unverified
      statement: "No ValueSet, dynamic group lifecycle, or universal NIP-29 branch is introduced."
    - status: flagged-unverified
      statement: "Saved relay URLs remain inert parsed evidence and cannot automatically open acquisition or publication."
  artifacts:
    - path: crates/fava-simple-groups/src/query.rs
      provides: "Ordinary saved/admin/member discovery Queries and pure groups_saved_by projection"
      exports: [SimpleGroups]
    - path: crates/fava-simple-groups/src/edit.rs
      provides: "Private lossless kind-10009 codec/materializer and public edit constructors"
  key_links:
    - from: crates/fava-simple-groups/src/edit.rs
      to: crates/fava-write/src/materialization.rs
      via: "ReplaceableEventEdit plus selected pure materializer; universal publication owns lifecycle"
      pattern: "ReplaceableEventEdit|ReplaceableEventMaterializer"
---

## Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Deliver ordinary discovery and lossless kind-10009 saved-list changes while keeping management events author-bearing and all work in universal owners.

Purpose: GROUP-10 requires useful saved/admin/member composition without creating ValueSet or normalizing shared list documents.
Output: SimpleGroups query/projection surface, target-local saved-list edit constructors/materializer, facade rematerialization evidence, and public API compilation.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-RESEARCH.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-08-SUMMARY.md
@crates/fava-bookmarks/src/lib.rs
@crates/fava-nip02/src/edit.rs
@crates/fava-nip02/src/query.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ordinary saved/admin/member discovery and bounded pure projection</name>
  <read_first>
    @crates/fava-simple-groups/src/query.rs
    @crates/fava-simple-groups/src/saved.rs
    @crates/fava-nip02/src/query.rs
    @docs/spec/partial-spec-api-semantics.md
  </read_first>
  <files>crates/fava-simple-groups/src/bounds.rs, crates/fava-simple-groups/src/saved.rs, crates/fava-simple-groups/src/tests.rs, crates/fava-simple-groups/src/tests/saved.rs, crates/fava-simple-groups/src/lib.rs</files>
  <behavior>
    - Saved groups/relays query kind 10009 by exact author sets; admin/member discovery query kinds 39001/39002 with exact lowercase p sets.
    - Present-empty authors/subjects match nothing; duplicate/reordered inputs have canonical identity.
    - Empty, 257-item, and infinite author/subject iterators remain distinguishable and oversized/infinite inputs refuse after at most 257 pulls.
    - `groups_saved_by(snapshot, group)` returns bounded deterministic existing tuples/collections and retains every positive saving author.
    - Repeated helper/projection calls are pure and equal.
  </behavior>
  <action>Append `mod saved;` to the existing `src/tests.rs` declaration owner in the same change that creates `src/tests/saved.rs`. Add final discovery signatures, private `MAX_DISCOVERY_INPUT_ITEMS = 256`, and final scenarios before compiling explicit `--lib`; then capture intended runtime RED. Count total iterator inputs before Query canonicalization, stop/refuse at 257, and keep ordinary Query plus bounded pure projection only. Own E26 descriptor `discovery_queries_are_ordinary_canonical_queries`, exact command `cargo test -p fava-simple-groups --lib tests::saved::discovery_queries_are_ordinary_canonical_queries -- --exact`, actual break `discovery_retains_duplicate_keys` that appends a duplicate saved/projection row. Own E27 descriptor `discovery_refuses_oversized_and_infinite_inputs`, exact command `cargo test -p fava-simple-groups --lib tests::saved::discovery_refuses_oversized_and_infinite_inputs -- --exact`, actual break `discovery_bound_counts_distinct_only` that counts only distinct keys so 257 duplicate inputs and an infinite repeat evade total-input refusal. For each record, capture causal runtime RED before behavior implementation, then GREEN, pre-break SHA-256, named actual break RED, byte-exact restored SHA-256 equal to it, and restored GREEN in `07.1.1-09-SUMMARY.md`. Separately turn present-empty into absent as a supporting mutation, and restore after each mutation.</action>
  <acceptance_criteria>Final APIs compile before RED; ordinary discovery proves exact empty/bound/bound+1/infinite, duplicate/order/idempotence, and projection provenance behavior with no lifecycle or ValueSet surface.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --lib --no-run &amp;&amp; cargo test -p fava-simple-groups --lib tests::saved::discovery_queries_are_ordinary_canonical_queries -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::saved::discovery_refuses_oversized_and_infinite_inputs -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::saved::groups_saved_by_is_bounded_pure_projection -- --exact</automated>
  </verify>
  <done>Applications can discover positive group/saved evidence through ordinary queries and pure projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lossless exact-target kind-10009 edits</name>
  <read_first>
    @crates/fava-bookmarks/src/lib.rs
    @crates/fava-nip02/src/edit.rs
    @crates/fava-write/src/edit.rs
    @crates/fava-write/src/materialization.rs
    @crates/fava-simple-groups/src/saved.rs
  </read_first>
  <files>crates/fava-simple-groups/src/edit.rs, crates/fava-simple-groups/src/tests/saved.rs, crates/fava-simple-groups/src/lib.rs, crates/fava-simple-groups/tests/public_api.rs</files>
  <behavior>
    - save/remove/rename group target exact id+each selected host; save/remove relay target exact r URL.
    - Existing first matching row bytes stay authoritative for save; rename changes only selected name columns; duplicates are handled deterministically.
    - Same-id other-host rows, unrelated/malformed/extension tags, encrypted opaque content, author/kind/timestamp rules, and source order are conserved.
    - Empty source and newer qualified source rematerialize deterministically; repeated operations are idempotent.
  </behavior>
  <action>Create the final edit/materializer signatures, narrow save scenario, and final E28 descriptor `multi_host_save_is_deterministic_and_idempotent`. Compile the registered target with `cargo test -p fava-simple-groups --lib --no-run`, run exact selector `cargo test -p fava-simple-groups --lib tests::saved::multi_host_save_is_deterministic_and_idempotent -- --exact`, and capture causal runtime RED at deterministic first-occurrence/idempotence assertions. Only then implement bounded kind-10009 ReplaceableEventEdit source validation and foreign-byte/order/other-host conservation, and rerun the exact selector GREEN. Capture pre-break SHA-256, independently apply named post-GREEN break `saved_edit_sorts_or_reappends_rows` that sorts host rows and appends them again on repeat, require the same exact selector RED, restore byte-exactly with equal SHA-256, rerun the same selector restored GREEN, and record E28 in `07.1.1-09-SUMMARY.md`. Separately apply the typed-document reconstruction conservation break, require its exact owning assertion RED, and restore exactly.</action>
  <acceptance_criteria>All exact target operations, empty/newer source, duplicate/adjacent/order/idempotence cases pass and the reconstruction break fails causally.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --lib --no-run &amp;&amp; cargo test -p fava-simple-groups --lib tests::saved::saved_edit_conserves_foreign_bytes_and_other_hosts -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::saved::multi_host_save_is_deterministic_and_idempotent -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::saved::saved_edit_rebases_on_newer_qualified_source -- --exact</automated>
  </verify>
  <done>Kind-10009 saved rows change losslessly through the ordinary semantic-edit contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove semantic edits and ordinary management events through Fava</name>
  <read_first>
    @crates/fava/tests/simple_groups.rs
    @crates/fava/tests/semantic_write_publication.rs
    @crates/fava/src/publication.rs
    @crates/fava-simple-groups/src/group.rs
    @crates/fava-simple-groups/src/edit.rs
  </read_first>
  <files>crates/fava/tests/simple_groups.rs, crates/fava-simple-groups/src/group.rs, crates/fava-simple-groups/tests/public_api.rs</files>
  <behavior>
    - Kind-10009 edit publishes only through `fava.by(author).to(relays).publish(edit)` and returns ordinary Write.
    - Kinds 9002 and 9010 are ordinary author-bearing prepared events and never enter ReplaceableEventEdit.
    - Concurrent edits/publications retain stable write/receipt/generation isolation through existing owners.
    - Invalid or oversized edit refuses before custody; parsed saved relay values alone create no work.
  </behavior>
  <action>Create final pure 9002/9010 preparation signatures, author-bearing scenario, and final facade descriptors including E30 `simple_group_saved_edit_uses_ordinary_semantic_lifecycle`. Compile both explicit targets with `cargo test -p fava-simple-groups --lib --no-run` and `cargo test -p fava --test simple_groups --no-run`, run exact selector `cargo test -p fava --test simple_groups simple_group_saved_edit_uses_ordinary_semantic_lifecycle -- --exact`, and capture causal runtime RED at ordinary operation/generation isolation. Only then wire ordinary materialization/rematerialization, stable Write/Receipt, concurrency isolation, and exact routes, and rerun the exact selector GREEN. Capture pre-break SHA-256, independently apply named post-GREEN break `semantic_edits_share_generation` forcing concurrent edits to share operation/generation identity, require the same exact selector RED, restore byte-exactly with equal SHA-256, rerun the same selector restored GREEN, and record E30 in `07.1.1-09-SUMMARY.md`. Separately encode 9002 as ReplaceableEventEdit and restore; Plan 10 alone owns that architecture break.</action>
  <acceptance_criteria>Only 10009 uses semantic edits, ordinary management events are author-bearing, all invalid inputs refuse before work, and `semantic_edits_share_generation` causally proves concurrent operation/generation isolation before exact restoration.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --lib --no-run &amp;&amp; cargo test -p fava --test simple_groups --no-run &amp;&amp; cargo test -p fava --test simple_groups simple_group_saved_edit_uses_ordinary_semantic_lifecycle -- --exact &amp;&amp; cargo test -p fava --test simple_groups simple_group_management_events_are_author_bearing -- --exact &amp;&amp; cargo clippy -p fava-simple-groups -p fava --all-targets -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; git diff --check</automated>
  </verify>
  <done>Discovery and saved-list changes are complete without ValueSet or a second lifecycle.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public namespace/surface: `SimpleGroups::{saved_groups,saved_relays,groups_where_admin,groups_where_member,groups_saved_by,save_group,remove_group,rename_saved_group,save_relay,remove_relay,materializer}` with exact final spellings selected from issue 0019.
- New private `MAX_DISCOVERY_INPUT_ITEMS` in `src/bounds.rs`, new private file `src/edit.rs`, and test file `src/tests/saved.rs`; updated `src/query.rs`/`src/saved.rs`.
- Pure author-bearing Group preparation helpers for kinds 9002/9010 using existing event values; no new nominal change type.
- Execution artifact: `07.1.1-09-SUMMARY.md` section `## Edge Evidence Records` with stable columns `row_id | descriptor | exact_command | causal_red | named_break | pre_break_sha256 | restored_sha256 | restored_green`, containing E26, E27, E28, and E30.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Shared kind-10009 document -> target-local edit | Foreign/private rows and opaque content can be destroyed by normalization. |
| Network-derived saved URL -> application | Parsed data could be mistaken for admitted execution policy. |
| Hostile discovery iterators -> Query | Duplicate, oversized, or infinite authors/subjects can consume unbounded work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-23 | Tampering | saved-list materializer | high | mitigate | Exact target-local surgery plus foreign-byte/order/other-host conservation tests. |
| T-0711-24 | Denial of service | edit/source rows | high | mitigate | Bound+1 row/string/edit consumption and typed refusal before custody. |
| T-0711-25 | Elevation of privilege | saved relay projection | medium | mitigate | Values remain inert; only explicit app Query/to calls open work. |
| T-0711-26 | Repudiation | concurrent edit generations | medium | mitigate | Reuse stable Write/Receipt/materialization generation evidence and stale-completion rejection. |
| T-0711-27 | Denial of service | discovery iterators | high | mitigate | Bound+1 total consumption at 256 and exact oversized/infinite typed-refusal tests. |
</threat_model>

<verification>
Run all discovery/saved/facade/public API tests, strict Clippy, formatting, vocabulary, line, architecture, and diff gates with foreign-byte and wrong-edit-kind breaks restored byte-exactly.
</verification>

<success_criteria>
GROUP-09/10/11 discovery/edit behavior is complete through ordinary Query, pure projection, ReplaceableEventEdit only for kind 10009, and existing Fava lifecycle owners.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-09-SUMMARY.md` when done.
</output>
