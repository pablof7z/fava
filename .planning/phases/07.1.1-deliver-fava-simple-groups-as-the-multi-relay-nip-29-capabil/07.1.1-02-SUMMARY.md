---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 02
subsystem: query
tags: [rust, relay-authority, replaceable-events, provenance, tdd]

requires:
  - phase: 07.1.1-01
    provides: canonical multi-kind query composition and the shared query-standard Bazel owner
provides:
  - exact per-RelayUrl replaceable winner retention in neutral event state
  - OnlyRelays union of independently selected relay-local coordinate winners
  - public two-session proof for same-signer divergence, duplicate provenance, and ordinary close
affects: [07.1.1-04, 07.1.1-05, 07.1.1-08, GroupSnapshot]

actuals:
  tokens: 6404
  tasks: 3
  commits: 7

tech-stack:
  added: []
  patterns: [per-relay replaceable selection followed by event-id deduplication]

key-files:
  created:
    - crates/fava-query-standard/tests/relay_authority.rs
  modified:
    - crates/fava-state/src/lib.rs
    - crates/fava-state/tests/event_state.rs
    - crates/fava-query-standard/src/lib.rs
    - crates/fava-query-standard/BUILD.bazel
    - crates/fava/tests/multi_relay.rs

key-decisions:
  - "Relay-observed replaceable state retains the union of newest event ids selected independently for each exact RelayUrl, then retracts ids with no remaining relay authority."
  - "OnlyRelays selects per requested RelayUrl while preserving the established accepted-local replacement overlay; AnyLocal remains one global replaceable winner."

patterns-established:
  - "Merge exact same-id observations before recomputing the per-URL retained winner union."
  - "Apply selection, deterministic ordering, and the whole-query limit after per-relay winner union and event-id deduplication."

requirements-completed: [GROUP-04, GROUP-05, GROUP-06]

coverage:
  - id: D1
    description: Divergent same-coordinate events remain independently retained per exact relay URL, and duplicate ids merge every actual observation.
    requirement: GROUP-04
    verification:
      - kind: integration
        ref: "crates/fava-state/tests/event_state.rs#replaceable_winners_are_independent_per_relay_url"
        status: pass
      - kind: integration
        ref: "crates/fava-state/tests/event_state.rs#duplicate_id_merges_exact_relay_evidence"
        status: pass
    human_judgment: false
  - id: D2
    description: OnlyRelays returns the deduplicated union of requested-relay winners with whole-query ordering and limits, while AnyLocal remains global.
    requirement: GROUP-05
    verification:
      - kind: integration
        ref: "cargo test -p fava-query-standard --test relay_authority"
        status: pass
    human_judgment: false
  - id: D3
    description: Two public scripted sessions preserve same-signer divergence, duplicate provenance, and ordinary cancellation/close without protocol-specific engine behavior.
    requirement: GROUP-06
    verification:
      - kind: e2e
        ref: "crates/fava/tests/multi_relay.rs#multi_relay_replaceable_authority_survives_public_facade"
        status: pass
    human_judgment: false

duration: 13min
completed: 2026-08-22
status: revoked
revoked: 2026-08-24
---

> **Revoked record:** Results, requirement completions, hashes, and pass claims
> below describe a superseded implementation. They are not evidence for the
> current worktree. Only `07.1.1-VALIDATION.md` may record current executions;
> issue 0023 blocks the undecided `SimpleGroup` relay-input boundary.

# Phase 07.1.1 Plan 02: Exact Relay Replaceable Authority Summary

**Neutral state and query evaluation now preserve same-signer replaceable truth independently per exact relay URL through the public Fava observation path.**

## Performance

- **Duration:** 13 min
- **Started:** 2026-08-22T09:28:44Z
- **Completed:** 2026-08-22T09:42:02Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Retained the newest replaceable event independently for each actual serving `RelayUrl`, including evidence-change recomputation and fully superseded-id retraction.
- Added neutral `OnlyRelays` evaluation that unions requested-relay coordinate winners by event id before deterministic ordering and whole-query limits.
- Proved the rule through two public scripted relay sessions with same-signer divergence, one shared event id, exact cancellation, and idempotent close.

## Task Commits

1. **Task 1 RED: Add failing per-relay state evidence** - `e83b6c2` (`test`)
2. **Task 1 GREEN: Retain per-relay replaceable winners** - `a949e84` (`feat`)
3. **Task 2 RED: Add failing evaluator relay-authority evidence** - `64b27be` (`test`)
4. **Task 2 GREEN: Evaluate replaceables per requested relay** - `5524502` (`feat`)
5. **Task 3: Prove relay authority through the public facade** - `328a65a` (`test`)
6. **Task 3 falsifier alignment: Target the B-winner assertion** - `7cb98fb` (`test`)

## Edge Evidence Records

| row_id | descriptor | exact_command | causal_red | named_break | pre_break_sha256 | restored_sha256 | restored_green |
|---|---|---|---|---|---|---|---|
| P02-T1 | `replaceable_winners_are_independent_per_relay_url`; `duplicate_id_merges_exact_relay_evidence` | `cargo test -p fava-state --test event_state replaceable_winners_are_independent_per_relay_url -- --exact && cargo test -p fava-state --test event_state duplicate_id_merges_exact_relay_evidence -- --exact` | Both compile-complete descriptors exited 101 under the original global state collapse: the A/B union lost one winner and the shared id disappeared after A-only replacement. | `global_coordinate_collapse` made both descriptors exit 101 again. | `6fcc565e9d7350c70438443fe1e0d702e98a24b84038292605ce6da5d2d9b32c` | `6fcc565e9d7350c70438443fe1e0d702e98a24b84038292605ce6da5d2d9b32c` | Both exact selectors and all six state integration tests passed after byte-exact restoration. |
| P02-T2 | `only_relays_unions_per_relay_replaceable_winners`; `single_relay_selects_only_its_winner`; `any_local_keeps_global_replaceable_semantics` | `cargo test -p fava-query-standard --test relay_authority` | The first two compile-complete descriptors exited 101 under the original global evaluator; the AnyLocal control passed. | `global_coordinate_winner` made the first two descriptors exit 101 while AnyLocal stayed green. | `3d3567815714e5206c59ccb291540c20d329e5d82a2b6340f3ec047ade84ff62` | `3d3567815714e5206c59ccb291540c20d329e5d82a2b6340f3ec047ade84ff62` | All three exact relay-authority tests and all ten pre-existing source-merge controls passed after byte-exact restoration. |
| P02-T3 | `multi_relay_replaceable_authority_survives_public_facade` | `cargo test -p fava --test multi_relay multi_relay_replaceable_authority_survives_public_facade -- --exact` | With the pre-change state collapse restored, the compile-complete public tracer exited 101 at the missing relay-B winner assertion. | `state_global_coordinate_collapse` exited 101 at the same relay-B assertion while the shared-id evidence reached both sessions. | `6fcc565e9d7350c70438443fe1e0d702e98a24b84038292605ce6da5d2d9b32c` | `6fcc565e9d7350c70438443fe1e0d702e98a24b84038292605ce6da5d2d9b32c` | The exact tracer and all three `multi_relay` tests passed after byte-exact restoration. |

## Files Created/Modified

- `crates/fava-state/src/lib.rs` - per-URL replaceable retention, duplicate evidence recomputation, and idempotent unchanged admission.
- `crates/fava-state/tests/event_state.rs` - arbitrary-kind state RED/GREEN/falsifier corpus.
- `crates/fava-query-standard/src/lib.rs` - neutral ResultAuthority branch, requested-relay winner union, local overlay preservation, and post-union limit/order.
- `crates/fava-query-standard/tests/relay_authority.rs` - exact multi/single/empty-evidence/AnyLocal evaluator proof.
- `crates/fava-query-standard/BUILD.bazel` - explicit `relay_authority` integration target.
- `crates/fava/tests/multi_relay.rs` - public two-session facade tracer and close proof.

## Decisions Made

- Exact serving `RelayUrl`, not signer identity, relay access, host semantics, or a canonical choice, is the replacement-authority dimension for relay-observed state.
- A same-id observation merges its exact evidence before retained winners are recomputed, so adding the final superseding relay observation can retract a now-unneeded predecessor.
- Accepted local replacement state continues to shadow relay-qualified predecessors until cancellation; this preserves the existing write-store overlay without making local state satisfy `OnlyRelays` authority.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Preserved the accepted-local overlay under OnlyRelays**
- **Found during:** Task 2 full query-standard regression run.
- **Issue:** Initial per-relay selection exposed a cached predecessor while a newer accepted local replacement was active, breaking `local_replacement_without_relay_evidence_shadows_qualified_cached_predecessor`.
- **Fix:** Overlay the newest accepted local coordinate value onto each selected-relay candidate before final authority filtering.
- **Files modified:** `crates/fava-query-standard/src/lib.rs`
- **Verification:** All 10 `source_merge` tests and all 3 `relay_authority` tests pass.
- **Committed in:** `5524502`

**Total deviations:** 1 auto-fixed bug.
**Impact on plan:** Preserves an existing universal write/query promise while keeping the requested per-URL authority rule.

## Issues Encountered

- The exact full touched-crate Clippy command reaches pre-existing warnings in untouched `fava` publication/test files: `struct_field_names` in `publication.rs`, `similar_names` in `write_settlement.rs`, and `needless_pass_by_value` in `semantic_write_store.rs`. They are recorded in `deferred-items.md`. Strict Clippy passes for `fava-state`, `fava-query-standard`, and the focused `fava` `multi_relay` target; the focused target permits only the unrelated library-level `struct_field_names` lint.
- Bazel/Bazelisk remains unavailable on `PATH`; the explicit target is registered, and the fail-closed phase-wide Bazel gate remains owned by Plan 12.

## Verification

- State, relay-authority, source-merge, and public multi-relay suites pass.
- Vocabulary checker and all 10 checker unit tests pass.
- Formatting, diff checks, neutral-source protocol scans, and the 800-line hard limit pass; all touched Rust files remain below 500 lines.
- Full `fava` all-target strict Clippy is not green because of the three pre-existing warnings listed above.

## User Setup Required

None.

## Next Phase Readiness

- Pure group projection can now consume both same-signer host records without state or query evaluation erasing one side.
- Neutral owners contain no NIP-29 branch, record kind, `h` tag, canonical-host choice, or field merge.

## Known Stubs

None.

## Self-Check: PASSED

All six product files, the canonical summary, all six task commits, and all three checksum evidence rows exist.

---
*Phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil*
*Completed: 2026-08-22*
