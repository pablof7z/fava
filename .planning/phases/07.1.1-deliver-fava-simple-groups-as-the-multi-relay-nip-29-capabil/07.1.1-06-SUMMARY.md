---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 06
subsystem: protocol-capability
tags: [rust, nip-29, facade, publication, relay-evidence, lifecycle]

requires:
  - phase: 07.1.1-05
    provides: bounded Group construction, exact content/record queries, and byte-exact event preparation
provides:
  - public-facade proof of AnyLocal group content and OnlyRelays record authority
  - arbitrary-kind exact-host publication through ordinary Fava Write lifecycle
  - facade-level signed byte identity, pre-custody refusal, and operation isolation evidence
affects: [07.1.1-07, 07.1.1-08, 07.1.1-09, 07.1.1-10, 07.1.1-11, 07.1.1-12]

actuals:
  tokens: 5916
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns: [dev-only capability facade edge, ordinary to-hosts publication, zero-side-effect pre-custody matrix, shared facade spy assembly]

key-files:
  created:
    - crates/fava/tests/simple_groups.rs
  modified:
    - Cargo.lock
    - crates/fava/Cargo.toml
    - crates/fava/BUILD.bazel

key-decisions:
  - "The facade consumes fava-simple-groups only through Cargo dev-dependencies and one Bazel test edge; production facade source and dependencies remain capability-blind."
  - "Applications compose group.prepare(payload), fava.to(group.hosts()), and publish directly and receive ordinary Write; Group owns no publication method or lifecycle."
  - "Invalid signed group context is refused by pure preparation before any facade custody, signer, router, publisher, transport, or wire interaction."

patterns-established:
  - "Public capability integration tests share one ordinary Fava provider assembly and assert owner calls rather than inventing protocol-specific spies or lifecycle wrappers."
  - "Exact route order is asserted from WriteRouting while per-destination attempts prove one handoff per normalized host."

requirements-completed: [GROUP-02, GROUP-03, GROUP-07, GROUP-08, GROUP-11]

coverage:
  - id: D1
    description: Accepted local group content remains visible while records require actual selected-host relay evidence and empty snapshots remain honest.
    requirement: GROUP-02
    verification:
      - kind: integration
        ref: "crates/fava/tests/simple_groups.rs#simple_group_content_preserves_local_visibility"
        status: pass
      - kind: integration
        ref: "crates/fava/tests/simple_groups.rs#simple_group_records_require_actual_host_evidence"
        status: pass
    human_judgment: false
  - id: D2
    description: Arbitrary kind 50029 publishes to every exact first-occurrence host through ordinary Write with one attempt per destination.
    requirement: GROUP-07
    verification:
      - kind: integration
        ref: "crates/fava/tests/simple_groups.rs#simple_group_arbitrary_kind_publication_uses_complete_exact_route"
        status: pass
    human_judgment: false
  - id: D3
    description: Valid presigned bytes remain exact, invalid h contexts have zero side effects, and concurrent writes retain ordinary cancellation and close isolation.
    requirement: GROUP-08
    verification:
      - kind: integration
        ref: "crates/fava/tests/simple_groups.rs#simple_group_presigned_context_refuses_before_custody"
        status: pass
      - kind: integration
        ref: "crates/fava/tests/simple_groups.rs#simple_group_uses_ordinary_lifecycle_isolation"
        status: pass
    human_judgment: false

duration: 14min
completed: 2026-08-22
status: complete
---

# Phase 07.1.1 Plan 06: Public Facade Group Lifecycle Summary

**Multi-relay Group values now traverse ordinary Fava observation and exact-host publication with local visibility, actual relay provenance, signed byte identity, and zero protocol-owned lifecycle.**

## Performance

- **Duration:** 14 min
- **Started:** 2026-08-22T10:22:04Z
- **Completed:** 2026-08-22T10:35:39Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Proved `Group::events` retains accepted-local visibility while `Group::records` excludes write-store-only facts and exposes only actual A/B serving evidence.
- Published arbitrary kind 50029 through `fava.to(group.hosts()).publish(group.prepare(payload))`, preserving first-occurrence route order and exactly one attempt per normalized host.
- Proved valid signed event bytes, id, and signature cross unchanged; five invalid `h` shapes refuse with zero custody, signer, router, publisher, transport, or frame effects.
- Reused ordinary Write and Observation identities for concurrent operation isolation, cancellation-driven observation updates, and idempotent close.

## Task Commits

1. **Task 1: Prove local-visible content and relay-evidenced records** - `7961bba` (`test`)
2. **Task 2: Publish arbitrary prepared events to every exact host** - `978f7e0` (`test`)
3. **Task 3: Refuse invalid signed context and preserve lifecycle isolation** - `e720bfd` (`test`)

## Edge Evidence Records

| row_id | descriptor | exact_command | causal_red | named_break | pre_break_sha256 | restored_sha256 | restored_green |
|---|---|---|---|---|---|---|---|
| E21 | `simple_group_arbitrary_kind_publication_uses_complete_exact_route` | `cargo test -p fava --test simple_groups simple_group_arbitrary_kind_publication_uses_complete_exact_route -- --exact` | Exited 101 at the complete-route assertion with `Explicit([z])` instead of first-occurrence `Explicit([z,a,m])`. | `publication_routes_first_host_only` exited 101 at the same exact route assertion. | `8f0be3430e1ea5cb9dee1707b1d709811047809e351034dc999a5db84e62407d` | `8f0be3430e1ea5cb9dee1707b1d709811047809e351034dc999a5db84e62407d` | Exact selector passed after byte-exact restoration. |

## Files Created/Modified

- `crates/fava/tests/simple_groups.rs` - five public-facade descriptors plus one cohesive ordinary-provider spy assembly; 638 lines remain below the 800-line hard limit.
- `crates/fava/Cargo.toml`, `Cargo.lock` - `fava-simple-groups` is a facade dev dependency only.
- `crates/fava/BUILD.bazel` - dedicated `simple_groups` test target with the sole facade Bazel edge to the capability.

## Decisions Made

- Route order belongs to `WriteRouting::Explicit`; attempt evidence proves one exact lane/handoff per normalized host without relying on map iteration order.
- Signed context validation stays before the application invokes `to` or `publish`; the facade retains only universal payload validation and custody.
- The 638-line facade file remains cohesive because all five descriptors share one exact provider assembly and spy set; splitting would duplicate lifecycle owners across Bazel targets.

## Deviations from Plan

None - production facade and capability sources remained unchanged; all delivered work is dev/test integration.

## Issues Encountered

- The prescribed all-targets strict Clippy command reaches pre-existing warnings in untouched `crates/fava/src/publication.rs`, `tests/write_settlement.rs`, and `tests/semantic_write_store.rs`, already recorded as WINDOWS entries 3-5 and in `deferred-items.md`. The new `simple_groups` target passes strict Clippy when only the unrelated library `struct_field_names` lint is allowed, and `fava-simple-groups --all-targets` passes unmodified strict Clippy.
- Neither `bazelisk` nor `bazel` is installed, so `//crates/fava:simple_groups` could not execute. The Cargo target, Cargo architecture test, dependency-section search, and BUILD registration checks pass; WINDOWS entry 6 records the unrun Bazel verification.

## Verification

- All five `fava --test simple_groups` descriptors pass together and under their exact selectors.
- `cargo test -p fava-simple-groups --all-targets` passes 11 unit, architecture, and public API tests.
- E21 complete-route break, content/record source-policy swap, and duplicate-signed-`h` break each failed the owning assertion; source files restored to equal SHA-256 and controls reran GREEN.
- `python3 tools/check_vocabulary.py`, all 10 vocabulary checker unit tests, formatting, dependency-boundary searches, 800-line hard limits, and `git diff --check` pass.

## User Setup Required

None.

## Known Stubs

None.

## Next Phase Readiness

- Plan 07 can consume the facade-proven content/record values for bounded typed record parsing without introducing a second observation owner.
- No group-specific lifecycle, facade production dependency, signed mutation, compatibility path, or universal NIP-29 switch exists.

## Self-Check: PASSED

All four changed implementation/test files, the summary, all three task commits, E21 equal restoration hashes, and the complete runnable verification set exist and pass from committed product state.

---
*Phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil*
*Completed: 2026-08-22*
