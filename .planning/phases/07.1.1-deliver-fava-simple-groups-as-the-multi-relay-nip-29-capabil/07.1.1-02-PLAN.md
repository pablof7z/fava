---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/fava-state/src/lib.rs
  - crates/fava-state/tests/event_state.rs
  - crates/fava-query-standard/src/lib.rs
  - crates/fava-query-standard/tests/relay_authority.rs
  - crates/fava-query-standard/BUILD.bazel
  - crates/fava/tests/multi_relay.rs
autonomous: true
requirements: [GROUP-04, GROUP-05, GROUP-06]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Exact selected relay URL is the replaceable-record authority even when two hosts serve same-signer, same-coordinate divergent events."
    - "A newer event at relay A supersedes only A's prior winner; relay B's winner remains independently visible."
    - "An identical event id served by A and B remains one record with both exact observations."
    - "OnlyRelays returns the union of per-selected-relay winners after deduplication, while AnyLocal retains established global replacement behavior."
    - "Empty selected evidence produces an empty result without an absence/completeness claim; deterministic ordering and whole-query limits apply after the union."
  prohibitions:
    - status: flagged-unverified
      statement: "Neutral state/query owners never select a canonical host, field-merge records, or branch on NIP-29 meaning."
    - status: flagged-unverified
      statement: "A bounded snapshot cannot establish group existence, completeness, or negative membership."
  artifacts:
    - path: crates/fava-query-standard/tests/relay_authority.rs
      provides: "Arbitrary-kind per-relay replaceable authority corpus"
    - path: crates/fava/tests/multi_relay.rs
      provides: "Public two-session same-signer facade tracer"
  key_links:
    - from: crates/fava-state/src/lib.rs
      to: crates/fava-query-standard/src/lib.rs
      via: "actual RelayEvidence observations select per-RelayUrl coordinate winners"
      pattern: "observations"
---

## Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Preserve divergent same-coordinate replaceable truth independently per exact relay URL before the group projection consumes it.

Purpose: Current global winner selection can erase one host's record before `GroupSnapshot`; this neutral prerequisite fixes the authority dimension without protocol knowledge.
Output: State admission, evaluator, and public facade evidence using arbitrary kind 30001 and exact `d = same` fixtures.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-PATTERNS.md
@crates/fava-state/src/lib.rs
@crates/fava-query-standard/src/lib.rs
@crates/fava/tests/multi_relay.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retain independently evidenced replaceable winners in state</name>
  <read_first>
    @docs/spec/ARCHITECTURE.md
    @crates/fava-state/src/lib.rs
    @crates/fava-state/tests/event_state.rs
  </read_first>
  <files>crates/fava-state/src/lib.rs, crates/fava-state/tests/event_state.rs</files>
  <behavior>
    - Divergent same-signer/same-kind/same-d events from relay URLs A and B both survive admission.
    - A newer A event removes only A's obsolete winner.
    - One identical id observed at A and B deduplicates and retains both observations.
    - Duplicate/adjacent admissions and repeated recomputation are idempotent.
  </behavior>
  <action>Write exact arbitrary-kind tests `replaceable_winners_are_independent_per_relay_url` and `duplicate_id_merges_exact_relay_evidence` first and record the causal RED under current global collapse. Change the existing state owner so relay-observed replaceable coordinates retain the union of newest event ids selected independently for each actual `RelayUrl`, then deduplicate ids and recompute retractions when evidence changes. Preserve write-store/local behavior and existing event identity rules. Record the source checksum, deliberately restore global coordinate collapse, require both new descriptors to fail, restore byte-exactly, and rerun.</action>
  <acceptance_criteria>State retains exact per-URL winners, same ids merge actual evidence, newer A state does not retract B, repeat admissions are inert, and no NIP-29 constant/tag appears.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-state --test event_state replaceable_winners_are_independent_per_relay_url -- --exact &amp;&amp; cargo test -p fava-state --test event_state duplicate_id_merges_exact_relay_evidence -- --exact</automated>
  </verify>
  <done>Neutral state no longer destroys relay-local replaceable authority before evaluation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Select per-requested-relay winners under OnlyRelays</name>
  <read_first>
    @crates/fava-query/src/lib.rs
    @crates/fava-query-standard/src/lib.rs
    @crates/fava-query-standard/tests/source_merge.rs
    @crates/fava-query-standard/BUILD.bazel
  </read_first>
  <files>crates/fava-query-standard/src/lib.rs, crates/fava-query-standard/tests/relay_authority.rs, crates/fava-query-standard/BUILD.bazel</files>
  <behavior>
    - `only_from_relays([A,B])` returns both divergent coordinate winners.
    - Single-relay queries return only that relay's winner.
    - AnyLocal retains the established global replaceable winner, including accepted local values.
    - Ordering and limits apply to the completed deduplicated union, not independently per relay.
  </behavior>
  <action>Create the new Cargo/Bazel integration target with exact tests `only_relays_unions_per_relay_replaceable_winners`, `single_relay_selects_only_its_winner`, and `any_local_keeps_global_replaceable_semantics`; record RED before implementation. In the standard evaluator, branch only on the existing neutral `ResultAuthority`, select newest coordinate values independently for each requested relay's actual evidence, union by event id, and then apply global ordering/limit. Do not alter `EventCoordinate` or add host/group vocabulary. Deliberately use one global coordinate winner under OnlyRelays, require the first two tests to fail while AnyLocal remains green, restore exact bytes, and rerun.</action>
  <acceptance_criteria>OnlyRelays preserves independent URL authority and exact evidence; AnyLocal behavior remains unchanged; empty/present-empty requested relay sets and deterministic ordering are explicit.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-query-standard --test relay_authority</automated>
  </verify>
  <done>Neutral evaluation can supply all per-host records required by pure group projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the neutral authority rule through two public scripted sessions</name>
  <read_first>
    @crates/fava/tests/multi_relay.rs
    @crates/fava/src/lib.rs
    @crates/fava-state/src/lib.rs
  </read_first>
  <files>crates/fava/tests/multi_relay.rs</files>
  <behavior>
    - Two exact scripted sessions serving same-coordinate divergent arbitrary events remain visible through public `Fava::observe`.
    - Same event id from both sessions appears once with both session observations.
    - Cancellation and idempotent close remain ordinary lifecycle behavior under concurrent sessions.
  </behavior>
  <action>Add `multi_relay_replaceable_authority_survives_public_facade` first and record RED against the pre-change state/evaluator. Use arbitrary kind 30001 and exact `d = same`, with no NIP-29 helper. Exercise concurrent session delivery, duplicate-id evidence, cancellation, and close. For the named deliberate break, restore state global collapse only, require the facade tracer to fail its B-winner assertion, restore byte-exactly, and rerun all neutral controls.</action>
  <acceptance_criteria>The public facade sees both same-signer URL-authoritative records, exact duplicate evidence, and isolated lifecycle completion with no group-specific code.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava --test multi_relay multi_relay_replaceable_authority_survives_public_facade -- --exact &amp;&amp; cargo clippy -p fava-state -p fava-query-standard -p fava --all-targets -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; git diff --check</automated>
  </verify>
  <done>The per-relay replaceable authority prerequisite is proven through the public observation path.</done>
</task>

</tasks>

## Artifacts this phase produces

- New test file: `crates/fava-query-standard/tests/relay_authority.rs`.
- New Bazel target for `relay_authority`.
- New exact neutral tests named in Tasks 1-3; no new public nominal type or lifecycle owner.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Relay-session evidence -> state admission | Hostile or stale records can be misattributed or collapse another relay's truth. |
| State contributions -> Query result authority | Provenance determines which replaceable winner is visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-04 | Tampering | replaceable admission/selection | high | mitigate | Select per exact RelayUrl evidence and prove A updates cannot retract B. |
| T-0711-05 | Repudiation | duplicate event provenance | high | mitigate | Deduplicate by id only after merging every actual serving observation. |
| T-0711-06 | Denial of service | per-relay union | medium | mitigate | Use existing bounded relay/result sets and apply the existing whole-query limit after union. |
</threat_model>

<verification>
Run all state, relay-authority, and public tracer tests plus strict Clippy, formatting, line limits, and diff checks; search changed neutral production files for protocol-specific constants/tags.
</verification>

<success_criteria>
The exact selected relay URL remains authority for replaceable records, including same-signer hosts, without changing AnyLocal or adding protocol behavior to neutral owners.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-02-SUMMARY.md` when done.
</output>
