---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 07
type: execute
wave: 6
depends_on: [07.1.1-06]
files_modified:
  - crates/fava-simple-groups/src/lib.rs
  - crates/fava-simple-groups/src/records.rs
  - crates/fava-simple-groups/src/metadata.rs
  - crates/fava-simple-groups/src/people.rs
  - crates/fava-simple-groups/src/pins.rs
  - crates/fava-simple-groups/src/saved.rs
  - crates/fava-simple-groups/src/tests.rs
  - crates/fava-simple-groups/src/tests/records.rs
  - crates/fava-simple-groups/tests/public_api.rs
autonomous: true
requirements: [GROUP-05, GROUP-06, GROUP-09]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Kinds 39000-39005 validate signature/id, exact kind, exactly one unambiguous d, and explicit event/tag/string/row bounds before typed projection."
    - "Metadata covers name, picture, banner, about, private, restricted, hidden, closed, livekit, supported_kinds, parent, and ordered child; absent and present-empty supported_kinds differ."
    - "Admins, members, roles, participants expose only positive rows with exact order/attribution inputs; omission never becomes negative membership."
    - "Pins preserve exact source order across e/a targets; saved group/relay rows preserve same-id different-host rows, author evidence, present-empty optional values, and per-row malformed results."
    - "Hostile iterators/rows refuse at bound+1 without hidden unbounded allocation, truncation, first/last-wins ambiguity, or raw hostile data in errors."
  prohibitions:
    - status: flagged-unverified
      statement: "Typed parsing cannot turn missing or partial relay records into existence, completeness, or negative-membership claims."
    - status: flagged-unverified
      statement: "Malformed or oversized protocol inputs cannot be silently dropped, truncated, normalized, or retained unboundedly."
  artifacts:
    - path: crates/fava-simple-groups/src/metadata.rs
      provides: "Typed bounded kind-39000 parser"
      exports: [GroupMetadata]
    - path: crates/fava-simple-groups/src/people.rs
      provides: "Typed kinds 39001-39004 parsers"
      exports: [GroupAdmins, GroupMembers, GroupRoles, GroupParticipants]
    - path: crates/fava-simple-groups/src/pins.rs
      provides: "Ordered kind-39005 parser"
      exports: [GroupPins, PinnedItem]
    - path: crates/fava-simple-groups/src/saved.rs
      provides: "Bounded kind-10009 public row parsers"
      exports: [SavedGroup, SavedRelay]
  key_links:
    - from: crates/fava-simple-groups/src/records.rs
      to: crates/fava-write/src/lib.rs
      via: "common signed EventValue boundary validation precedes protocol decoding"
      pattern: "verify|finalize"
---

## Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Decode every required NIP-29 record, pin, saved-group, and saved-relay row into approved bounded public values.

Purpose: Applications must never walk raw protocol tags, while partial relay evidence remains positive and exact rather than overstated.
Output: Six record types, pins, saved rows, one GroupError boundary, hostile corpus, and external accessor compilation.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/issues/0019-simple-groups.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-RESEARCH.md
@crates/fava-nip02/src/contact_list.rs
@crates/fava-nip02/src/bounds.rs
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish the common signed record boundary and complete metadata value</name>
  <reversibility rating="costly">The approved typed record nouns/accessors become public protocol vocabulary; issue 0019 already approves the nouns and observable distinctions.</reversibility>
  <read_first>
    @crates/fava-nip02/src/contact_list.rs
    @crates/fava-simple-groups/src/bounds.rs
    @crates/fava-simple-groups/src/lib.rs
    @crates/fava-simple-groups/tests/public_api.rs
  </read_first>
  <files>crates/fava-simple-groups/src/records.rs, crates/fava-simple-groups/src/metadata.rs, crates/fava-simple-groups/src/tests.rs, crates/fava-simple-groups/src/tests/records.rs, crates/fava-simple-groups/src/lib.rs</files>
  <behavior>
    - Wrong kind, invalid signature/id, missing/empty/duplicate/contradictory d, oversized event/tag/string/count, and ambiguous scalar duplicate are distinct GroupError refusals.
    - Complete metadata fields preserve exact bytes/order; flags and parent/children remain exact.
    - `supported_kinds` absent means unspecified/all, while present-empty means explicitly none.
  </behavior>
  <action>Append `mod records;` to the existing `src/tests.rs` declaration owner in the same change that creates `src/tests/records.rs`. Create final approved GroupMetadata/private validator signatures, narrow valid scenario, and final descriptors `metadata_parser_conserves_complete_record` and E25 `record_boundary_refuses_ambiguous_or_oversized_input`. Compile the registered target with `cargo test -p fava-simple-groups --lib --no-run`, run exact selector `cargo test -p fava-simple-groups --lib tests::records::record_boundary_refuses_ambiguous_or_oversized_input -- --exact`, and capture causal runtime RED at its typed-refusal assertion. Only then implement generic bounded parsing with checked arithmetic, no hostile-row echo, and duplicate scalar refusal, and rerun the exact selector GREEN. Capture pre-break SHA-256, independently apply named post-GREEN break `record_parser_selects_first_conflict`, require the same exact selector RED, restore byte-exactly with equal SHA-256, rerun the same selector restored GREEN, and record E25 in `07.1.1-07-SUMMARY.md`. Separately collapse absent/present-empty supported kinds, require its owning assertion RED, and restore exactly.</action>
  <acceptance_criteria>Every metadata field and empty/duplicate/order edge is typed exactly, all hostile bounds refuse, and ambiguous scalar selection cannot pass.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --lib --no-run &amp;&amp; cargo test -p fava-simple-groups --lib tests::records::metadata_parser_conserves_complete_record -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::records::record_boundary_refuses_ambiguous_or_oversized_input -- --exact</automated>
  </verify>
  <done>Applications can parse a complete host-authored metadata record without raw tags or invented merge policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decode admins, members, roles, and participants as positive evidence</name>
  <read_first>
    @crates/fava-simple-groups/src/records.rs
    @docs/issues/0019-simple-groups.md
    @crates/fava-nip02/src/contact_list.rs
  </read_first>
  <files>crates/fava-simple-groups/src/people.rs, crates/fava-simple-groups/src/tests/records.rs, crates/fava-simple-groups/src/lib.rs, crates/fava-simple-groups/tests/public_api.rs</files>
  <behavior>
    - Admin/member p rows retain source order, pubkey, and roles using existing values/tuples rather than a new attribution wrapper.
    - Role names/descriptions and participant lowercase-hex keys are exact and bounded.
    - Empty record lists are valid empty positive evidence; malformed recognized rows return GroupError and do not reserve later duplicates.
    - Repeated parsing is deterministic and inert.
  </behavior>
  <action>First add the final approved admin/member/role/participant signatures and a narrow one-valid-row parser scenario, then require the explicit library target `cargo test -p fava-simple-groups --lib --no-run` to succeed. Add `people_records_preserve_positive_order_and_attribution` and `invalid_people_rows_do_not_reserve_later_valid_rows` only after that gate and capture runtime RED at order/attribution/later-valid assertions, not missing symbols or scenarios. Implement the approved values with slices/iterators/tuples of existing `PublicKey` and string values; add no row-evidence noun, `is_member`, or group existence API. Preserve valid siblings with bounded per-row `Result`; use whole-record typed refusal only where scalar ambiguity prevents a coherent record. Checksum sources, deliberately let an invalid first row reserve its key, require the intended runtime assertion to fail, restore exact bytes/checksum, and rerun restored GREEN.</action>
  <acceptance_criteria>All four record kinds parse bounded ordered positive facts, malformed adjacency remains visible, and no omission is interpreted negatively.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --lib --no-run &amp;&amp; cargo test -p fava-simple-groups --lib tests::records::people_records_preserve_positive_order_and_attribution -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::records::invalid_people_rows_do_not_reserve_later_valid_rows -- --exact</automated>
  </verify>
  <done>Applications receive exact typed positive people/role/participant evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Decode ordered pins and conserving saved rows</name>
  <read_first>
    @crates/fava-simple-groups/src/records.rs
    @crates/fava-nip02/src/contact_list.rs
    @crates/fava-simple-groups/README.md
  </read_first>
  <files>crates/fava-simple-groups/src/pins.rs, crates/fava-simple-groups/src/saved.rs, crates/fava-simple-groups/src/tests/records.rs, crates/fava-simple-groups/src/lib.rs, crates/fava-simple-groups/tests/public_api.rs</files>
  <behavior>
    - Kind 39005 preserves interleaved e/a pin source order and typed targets.
    - Kind 10009 group rows key exact id+relay and retain optional name and saving author; same id at another relay stays separate.
    - Relay-in-use rows retain exact RelayUrl and author; present-empty/missing/extra/invalid rows are distinguishable bounded failures.
    - Multi-row parsing conserves valid siblings and deterministic source order without a new public evidence type.
  </behavior>
  <action>First add the final approved PinnedItem/SavedGroup/SavedRelay signatures and one valid pin plus saved-row scenario, then require the explicit library target `cargo test -p fava-simple-groups --lib --no-run` to succeed. Add `pins_preserve_interleaved_source_order` and `saved_rows_preserve_host_and_author_evidence` afterward and capture runtime RED only at exact order/host/author assertions. Use generic kinds 39005/10009 and bounded `Vec&lt;Result&lt;_, GroupError&gt;&gt;` for row-local failures; never make parsed relay URLs open work. Checksum sources, deliberately sort pins by target type and collapse same-id rows across relays one at a time, require both intended runtime assertions to fail, restore byte-exactly/checksums, and rerun restored GREEN plus parser/public API, strict Clippy, formatting, vocabulary, line, and diff gates.</action>
  <acceptance_criteria>Pins and saved rows conserve order, host, author, empty/malformed distinctions, and explicit bounds; both normalization/collapse mutations fail.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-simple-groups --lib --no-run &amp;&amp; cargo test -p fava-simple-groups --lib tests::records::pins_preserve_interleaved_source_order -- --exact &amp;&amp; cargo test -p fava-simple-groups --lib tests::records::saved_rows_preserve_host_and_author_evidence -- --exact &amp;&amp; cargo test -p fava-simple-groups --test public_api &amp;&amp; cargo clippy -p fava-simple-groups --all-targets -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; git diff --check</automated>
  </verify>
  <done>All GROUP-09 protocol rows are typed, bounded, conserving, and inert.</done>
</task>

</tasks>

## Artifacts this phase produces

- New files: `src/records.rs`, `src/metadata.rs`, `src/people.rs`, `src/pins.rs`, `src/saved.rs`, `src/tests/records.rs`.
- New approved public types: `GroupMetadata`, `GroupAdmins`, `GroupMembers`, `GroupRoles`, `GroupParticipants`, `GroupPins`, `PinnedItem`, `SavedGroup`, `SavedRelay`.
- New public `from_event` and read-only accessor surfaces for those values; no new lifecycle/provider/row-evidence nominal type.
- Execution artifact: `07.1.1-07-SUMMARY.md` section `## Edge Evidence Records` with stable columns `row_id | descriptor | exact_command | causal_red | named_break | pre_break_sha256 | restored_sha256 | restored_green`, containing E25.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Signed relay event/tags -> typed protocol values | Malformed, oversized, ambiguous, forged, or hostile rows enter application-visible parsing. |
| Parsed relay URL -> application | Network-derived addresses must remain inert data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-16 | Spoofing | signed record parser | high | mitigate | Verify event boundary, exact kind/d, signature/id before typed construction. |
| T-0711-17 | Tampering | ambiguous/duplicate rows | high | mitigate | Typed refusal or bounded per-row Result; preserve source order and valid siblings. |
| T-0711-18 | Denial of service | parser iterators/allocations | high | mitigate | Explicit byte/count/nesting bounds, bound+1 consumption, checked arithmetic, no truncation. |
| T-0711-19 | Elevation of privilege | parsed relay URL | medium | mitigate | Parsing/projection remains inert; no query/publication is opened. |
</threat_model>

<verification>
Run the complete parser/public API corpus, strict Clippy, formatting, vocabulary, code-line limits, and diff checks; retain all named duplicate/order/bound mutations and byte-exact restorations.
</verification>

<success_criteria>
GROUP-09 and parser portions of GROUP-05/06 are complete: every required value is typed/bounded and no partial snapshot becomes a global or negative claim.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-07-SUMMARY.md` when done.
</output>
