---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 11
type: execute
wave: 10
depends_on: [07.1.1-10]
files_modified:
  - apps/canary/Cargo.toml
  - apps/canary/Cargo.lock
  - apps/canary/scenarios.json
  - apps/canary/src/lib.rs
  - apps/canary/src/main.rs
  - apps/canary/src/croissant_simple_groups.rs
  - apps/canary/src/croissant_simple_groups_flow.rs
  - apps/canary/src/croissant_simple_groups_evidence.rs
  - apps/canary/src/croissant_simple_groups_tests.rs
autonomous: true
requirements: [GROUP-04, GROUP-05, GROUP-06, GROUP-07, GROUP-08, GROUP-11, GROUP-12]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One scenario owns exactly two newly spawned CroissantProcess children, distinct data/log roots and loopback ports; both cleanup results are captured before either is propagated."
    - "The same unique group id is independently bootstrapped at A/B with divergent same-signer metadata/admin records, one shared content id, and one unique content id per host."
    - "Public Group/Fava APIs prove exact dedup/provenance, host-local disagreement/attribution, single-host choice, arbitrary-kind exact two-host publication, signed pre-custody refusal, cancellation, and idempotent close."
    - "One arbitrary-kind Write has exactly two desired destinations, two acknowledgements, and one matching EVENT handoff per owned child."
    - "Evidence is generated in owner-private staging, bounded and completely sealed; both secret scans and both child teardowns pass before atomic promotion."
    - "Pair verification requires exactly two fresh runs, four distinct owned child instances, distinct seed/group/event/write/receipt identities, no cross-run data, no staging/secret residue, and exact two-child cleanup per run."
  prohibitions:
    - status: flagged-unverified
      statement: "Canary cleanup never discovers/kills processes by name or touches PID 75649 or any non-owned process."
    - status: flagged-unverified
      statement: "Unsafe, unsealed, secret-bearing, incompletely cleaned, or staged canary artifacts are never retained/promoted."
  artifacts:
    - path: apps/canary/src/croissant_simple_groups.rs
      provides: "Two-child orchestration, cleanup aggregation, and scenario outcome"
    - path: apps/canary/src/croissant_simple_groups_flow.rs
      provides: "Public Group/Fava two-relay flow"
    - path: apps/canary/src/croissant_simple_groups_evidence.rs
      provides: "Complete signed manifest, secret scans, atomic promotion, pair verifier"
    - path: apps/canary/src/croissant_simple_groups_tests.rs
      provides: "Causal process/wire/evidence/refusal/cleanup tests"
  key_links:
    - from: apps/canary/src/croissant_simple_groups.rs
      to: apps/canary/src/croissant.rs
      via: "two exact CroissantProcess owners are stopped independently on every path"
      pattern: "\.stop\("
    - from: apps/canary/src/croissant_simple_groups_flow.rs
      to: crates/fava-simple-groups/src/lib.rs
      via: "only public Group/SimpleGroups values feed ordinary Fava observe/publish"
      pattern: "Group::on|\.prepare\("
---

## Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Build the controlled two-relay Croissant scenario and independent sealed pair verifier without adding any production capability lifecycle.

Purpose: GROUP-12 requires evidence Fava cannot self-assert: exact relay processes, frames, per-host truth, handoffs, cancellation, teardown, secret safety, and repeatability.
Output: Registered canary scenario/CLI, four cohesive Rust modules, exact two-child ownership, staged sealed evidence, and mutation-enforced pair verification.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-10-SUMMARY.md
@.planning/phases/07.1-universal-publication-vocabulary-and-typed-nip-02-reads/07.1-REVIEW-FIX.md
@apps/canary/src/croissant.rs
@apps/canary/src/artifacts.rs
@apps/canary/src/croissant_nip02_evidence.rs
@apps/canary/src/multi.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Own and reap exactly two Croissant children per run</name>
  <read_first>
    @apps/canary/src/lib.rs
    @apps/canary/src/croissant.rs
    @apps/canary/src/croissant_tests.rs
    @apps/canary/src/artifacts.rs
    @apps/canary/Cargo.toml
    @apps/canary/scenarios.json
  </read_first>
  <files>apps/canary/Cargo.toml, apps/canary/Cargo.lock, apps/canary/src/lib.rs, apps/canary/src/croissant_simple_groups.rs, apps/canary/src/croissant_simple_groups_tests.rs</files>
  <behavior>
    - `apps/canary/src/lib.rs` registers `#[cfg(test)] mod croissant_simple_groups_tests;`, making every module-qualified selector reachable from the explicit `--lib` target.
    - A/B use distinct exact child handles, owner keys, fresh data/log paths, reserved ports, readiness facts, and bounded stdout/stderr.
    - B-start failure after A-start explicitly stops A; flow failure still attempts both stops.
    - Both stop results are captured before returning either failure; exact PID/endpoint/absent/closed facts survive.
    - Drop kill is fallback only; no process scan/name discovery or unowned PID operation exists.
  </behavior>
  <action>Register `#[cfg(test)] mod croissant_simple_groups_tests;` in `apps/canary/src/lib.rs` and create the final two-child orchestration signatures, narrow successful scenario, and final module-qualified descriptors `two_croissant_children_are_always_reaped` and E31 `second_child_failure_still_reaps_first`. Compile the registered target with `cargo test --manifest-path apps/canary/Cargo.toml --lib --no-run`, then run exact selector `cargo test --manifest-path apps/canary/Cargo.toml --lib croissant_simple_groups_tests::second_child_failure_still_reaps_first -- --exact` and capture causal runtime RED at the missing second cleanup assertion. Only then compose two exact supervisors, aggregate flow failure plus both cleanup results, never discover processes, exclude PID 75649, and rerun the exact selector GREEN. Capture the pre-break SHA-256, independently apply actual post-GREEN break `canary_returns_before_second_cleanup` that returns after A cleanup while B is still owned, require the same exact selector RED, restore byte-exactly with equal SHA-256, rerun the same selector restored GREEN, and record E31 in `07.1.1-11-SUMMARY.md` before other canary work.</action>
  <acceptance_criteria>Every success/failure/startup permutation accounts for exactly two owned children, both cleanup attempts, exact teardown facts, and zero non-owned process operations.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path apps/canary/Cargo.toml --lib --no-run &amp;&amp; cargo test --manifest-path apps/canary/Cargo.toml --lib croissant_simple_groups_tests::two_croissant_children_are_always_reaped -- --exact &amp;&amp; cargo test --manifest-path apps/canary/Cargo.toml --lib croissant_simple_groups_tests::second_child_failure_still_reaps_first -- --exact</automated>
  </verify>
  <done>The canary has exact two-child lifecycle ownership and failure isolation before protocol flow is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute the complete public multi-relay Group flow</name>
  <read_first>
    @apps/canary/src/multi.rs
    @apps/canary/src/croissant_nip02.rs
    @apps/canary/src/wire.rs
    @crates/fava-simple-groups/README.md
    @crates/fava/tests/simple_groups.rs
  </read_first>
  <files>apps/canary/src/croissant_simple_groups_flow.rs, apps/canary/src/croissant_simple_groups.rs, apps/canary/src/croissant_simple_groups_tests.rs, apps/canary/src/lib.rs</files>
  <behavior>
    - Bootstrap same id independently at A/B, then publish divergent same-signer metadata/admin records through public preparation/publication.
    - Shared content id deduplicates with A+B exact observations and unique A/B content remains.
    - Per-host typed values differ exactly with positive attribution; single-host Group chooses each view without merge.
    - Arbitrary custom kind publishes as one Write to A+B with exactly one handoff/ack per child.
    - Invalid signed contexts create no custody and zero matching frames; ordinary observation cancel/close withdraws both hosts and close is idempotent.
  </behavior>
  <action>Create the final public-flow scenario signature, narrow one-host control, and final exact descriptor `croissant_simple_groups_public_flow` using public `fava` and `fava-simple-groups` APIs. Compile the registered target with `cargo test --manifest-path apps/canary/Cargo.toml --lib --no-run`, run exact selector `cargo test --manifest-path apps/canary/Cargo.toml --lib croissant_simple_groups_tests::croissant_simple_groups_public_flow -- --exact`, and capture causal runtime RED at the absent B-evidence/exact-route/fork assertion, never at a missing API/type/file/scenario. Implement the complete bounded two-host flow, correlate exact group/event/write/receipt/session/request identities without secret material, leave production capability code unchanged, and rerun the exact selector GREEN. Capture pre-break SHA-256, then independently apply named post-GREEN breaks `canary_drops_b_content_evidence`, `canary_merges_record_forks`, and `canary_routes_custom_event_to_a_only`; require the same exact selector RED at the corresponding assertion, restore byte-exactly with equal SHA-256 after each, and rerun the exact selector restored GREEN.</action>
  <acceptance_criteria>The full GROUP-04/05/06/07/08/12 public process/wire flow passes with exact causal failures for missing B evidence, field merge, and incomplete route.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path apps/canary/Cargo.toml --lib --no-run &amp;&amp; cargo test --manifest-path apps/canary/Cargo.toml --lib croissant_simple_groups_tests::croissant_simple_groups_public_flow -- --exact</automated>
  </verify>
  <done>One controlled run proves multi-host group semantics through two exact external relay processes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seal safe run evidence and verify an independent pair</name>
  <read_first>
    @apps/canary/src/artifacts.rs
    @apps/canary/src/croissant_nip02_evidence.rs
    @apps/canary/src/croissant_nip02_tests.rs
    @apps/canary/src/main.rs
    @apps/canary/scenarios.json
  </read_first>
  <files>apps/canary/src/croissant_simple_groups_evidence.rs, apps/canary/src/croissant_simple_groups_tests.rs, apps/canary/src/lib.rs, apps/canary/src/main.rs, apps/canary/scenarios.json</files>
  <behavior>
    - Owner-private staging precedes child start; pre-seal and post-manifest scans cover all supported seed/private-key encodings.
    - Complete verifier-consumed claims/artifact hashes, two readiness and two teardown facts are author-signed before atomic promotion.
    - Any refusal removes staging and retains no run, secret, unsafe manifest, or incomplete child evidence.
    - Pair verifier requires exactly two manifests/four distinct children, distinct identities, no cross-run data, exact bounds/hashes/seals/cleanup, and no staging residue.
  </behavior>
  <action>Create the final evidence/seal/promotion signatures, `verify-croissant-simple-groups-pair --runs-dir PATH` CLI/scenario, narrow valid-manifest fixture, and final descriptor `pair_verifier_rejects_unsafe_evidence` with persistent-parent secret, incomplete-cleanup, unsigned-claim, reused-identity, cross-run-data, extra/missing-manifest, and staging-residue cases. Compile the registered target with `cargo test --manifest-path apps/canary/Cargo.toml --lib --no-run`, run exact selector `cargo test --manifest-path apps/canary/Cargo.toml --lib croissant_simple_groups_tests::pair_verifier_rejects_unsafe_evidence -- --exact`, and capture causal runtime RED at its intended verifier refusal assertion, never at a missing CLI/file/type/scenario. Implement complete signed evidence, two cleanup results, both secret scans, refusal cleanup, and atomic promotion, then rerun the exact selector GREEN. Capture pre-break SHA-256, independently omit B teardown from the signed manifest as named post-GREEN break `pair_manifest_omits_b_teardown`, require the same exact selector RED at the ownership assertion, restore byte-exactly with equal SHA-256, and rerun the same selector restored GREEN. Keep every new/touched code file below 500 lines by the planned split.</action>
  <acceptance_criteria>Unsafe evidence never promotes, all pair mutation fixtures fail exactly, the happy pair verifier is deterministic, and registry/CLI expose only bounded explicit paths.</acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path apps/canary/Cargo.toml --lib --no-run &amp;&amp; cargo test --manifest-path apps/canary/Cargo.toml --lib croissant_simple_groups_tests::pair_verifier_rejects_unsafe_evidence -- --exact &amp;&amp; cargo clippy --manifest-path apps/canary/Cargo.toml --all-targets -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; find apps/canary/src -name 'croissant_simple_groups*.rs' -type f -exec awk 'FNR==1 {file=FILENAME} END {if (FNR&gt;=500) {print file ":" FNR; exit 1}}' {} \; &amp;&amp; git diff --check</automated>
  </verify>
  <done>The fresh two-relay scenario is ready to run twice and retain only independently verifiable safe evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

- New canary modules: `croissant_simple_groups.rs`, `croissant_simple_groups_flow.rs`, `croissant_simple_groups_evidence.rs`, `croissant_simple_groups_tests.rs`.
- New scenario/CLI: `croissant-simple-groups-public-flow` and `verify-croissant-simple-groups-pair --runs-dir PATH`.
- New canary symbols with exact final spellings: bounded options/outcome, single-run executor, and pair verifier; raw seed/private key remains process-memory-only.
- New evidence schema: executable/source provenance, seed digest/public keys, group/content/record/write/receipt/session identities, per-host frames/evidence/disagreement/handoffs, bounds, two readiness/teardown facts, hashes, seal, and promotion/scan facts.
- Execution artifact: `07.1.1-11-SUMMARY.md` section `## Edge Evidence Records` with stable columns `row_id | descriptor | exact_command | causal_red | named_break | pre_break_sha256 | restored_sha256 | restored_green`, containing E31.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canary process -> two external Croissant children | Executables, sockets, files, logs, exit/readiness, and teardown cross process boundaries. |
| Fava -> two WebSocket proxies/relays | Frames and exact host provenance require independent witnessing. |
| In-memory secret -> retained artifacts | Secret material must never survive scanning/refusal. |
| Run one -> run two/pair verifier | Old/cross-run data or mutable claims can counterfeit repeatability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-30 | Spoofing | Croissant executable/child identity | high | mitigate | Exact binary hash/source HEAD/PID/endpoint/readiness facts for each owned child. |
| T-0711-31 | Tampering | per-host wire/evidence/manifest | high | mitigate | Exact identity/frame correlation, complete signed claims/hashes, causal mutations. |
| T-0711-32 | Information disclosure | seed/private key artifacts | high | mitigate | Process-memory only, two complete encoding scans, owner-private staging, refusal cleanup. |
| T-0711-33 | Denial of service | waits/logs/children | high | mitigate | Explicit deadlines/bounds, kill-on-drop fallback, both exact stops captured before propagation. |
| T-0711-34 | Repudiation | pair repeatability | medium | mitigate | Exactly two fresh sealed runs, four distinct children/identities, cross-run exclusion. |
</threat_model>

<verification>
Run complete canary unit/process targets, strict Clippy, formatting, line, secret/staging, pair-mutation, and diff gates; do not retain a real final pair until Plan 12.
</verification>

<success_criteria>
The canary implementation can prove GROUP-12 twice with exact two-child ownership, independent wire/process evidence, safe promotion, and no capability lifecycle leakage.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-11-SUMMARY.md` when done.
</output>
