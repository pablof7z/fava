---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/fava-query/src/selection.rs
  - crates/fava-query/tests/query_identity.rs
  - crates/fava-query-standard/tests/source_merge.rs
  - crates/fava-subscriptions/tests/kinds.rs
  - crates/fava-subscriptions/BUILD.bazel
autonomous: true
requirements: [GROUP-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An ordinary Query selects several event kinds by composing the already approved Query::kind method repeatedly; no new public symbol, error variant, or NIP-29 branch is introduced."
    - "Duplicate and adjacent repeated kind calls canonicalize deterministically; call order does not affect query identity or exact NIP-01 encoding."
    - "Each approved Query::kind call consumes exactly one Kind, so this neutral owner accepts no hostile collection iterator; GroupRecords owns the fixed six-kind non-empty set."
    - "The neutral Query owner consumes exactly one Kind per approved method call; no collection iterator, bound constant, or new QueryError vocabulary is introduced."
    - "Arbitrary non-NIP-29 fixtures prove multi-kind local evaluation and wire planning before GroupRecords consumes the surface."
  prohibitions:
    - status: flagged-unverified
      statement: "Neutral query and subscription owners contain no NIP-29 kind, group id, or protocol branch."
  artifacts:
    - path: crates/fava-query/src/selection.rs
      provides: "Neutral multi-kind composition through the existing Query::kind symbol"
      exports: ["Query::kind"]
    - path: crates/fava-subscriptions/tests/kinds.rs
      provides: "Exact multi-kind NIP-01 REQ evidence"
  key_links:
    - from: crates/fava-query/src/selection.rs
      to: crates/fava-subscriptions/src/lib.rs
      via: "Query.selection.kinds is consumed as the complete canonical set"
      pattern: "selection.*kinds"
---

## Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Prove the first neutral prerequisite end to end: one ordinary Query carries several kinds through identity, local evaluation, and exact wire planning.

Purpose: GROUP-03 requires one record query for kinds 39000-39005; universal owners must gain only generic set construction.
Output: additive repeated-call semantics for the already approved `Query::kind`, canonical identity/local behavior, and exact NIP-01 multi-kind evidence without vocabulary expansion.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-CONTEXT.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-RESEARCH.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-PATTERNS.md
@crates/fava-query/src/selection.rs
@crates/fava-subscriptions/tests/tag_values.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace several arbitrary kinds from public Query identity through local evaluation</name>
  <read_first>
    @docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
    @crates/fava-query/src/selection.rs
    @crates/fava-query/tests/query_identity.rs
    @crates/fava-query-standard/src/lib.rs
    @crates/fava-query-standard/tests/source_merge.rs
    @crates/fava-subscriptions/src/lib.rs
    @crates/fava-subscriptions/tests/tag_values.rs
  </read_first>
  <files>crates/fava-query/src/selection.rs, crates/fava-query/tests/query_identity.rs, crates/fava-query-standard/tests/source_merge.rs, crates/fava-subscriptions/tests/kinds.rs, crates/fava-subscriptions/BUILD.bazel</files>
  <behavior>
    - `Query::events().kind(30001).kind(30002)` matches both arbitrary kinds locally.
    - Reversed, duplicate, and adjacent repeated calls have identical Query identity and hashing.
    - One `Query::kind(k)` call retains its existing singleton behavior.
    - The exact NIP-01 REQ contains every repeated-call kind once in deterministic order.
    - The method accepts one Kind, not a caller collection; empty, oversized, and infinite iterator inputs are structurally absent from this approved contract.
  </behavior>
  <action>Use only the already approved `Query::kind(Kind) -&gt; Query` public contract. Do not add `Query::kinds`, any QueryError variant, another method, or a nominal type. The final API/type/file already exists, so first add `repeated_kind_identity_is_canonical`, `repeated_kind_local_selection_is_complete`, and `repeated_kind_req_encoding_is_complete` plus its Cargo/Bazel target. Require all three test binaries to compile with `--no-run`, then run them against the current singleton-replacement implementation and capture causal runtime RED at the missing second-kind identity/local/wire assertions. GREEN changes only the existing method's internal set update so repeated calls add canonical Kind values while a single call remains identical; generic wire conversion remains unchanged. The API accepts exactly one Kind per call and therefore has no empty/oversized/infinite iterable input to bound. After GREEN, record source checksums, deliberately restore overwrite-on-each-call behavior under named break `query_kind_overwrites_previous_kind`, require all three runtime descriptors to fail while the one-kind control remains green, restore byte-exactly, verify checksums, and rerun restored GREEN.</action>
  <acceptance_criteria>The unchanged approved public signature compiles before runtime RED; repeated-call duplicate/order canonicalization, both arbitrary kinds, and one-kind compatibility are proven; no new vocabulary appears and the overwrite break restores byte-exactly.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-query --test query_identity --no-run &amp;&amp; cargo test -p fava-query-standard --test source_merge --no-run &amp;&amp; cargo test -p fava-subscriptions --test kinds --no-run &amp;&amp; cargo test -p fava-query --test query_identity repeated_kind_identity_is_canonical -- --exact &amp;&amp; cargo test -p fava-query-standard --test source_merge repeated_kind_local_selection_is_complete -- --exact &amp;&amp; cargo test -p fava-subscriptions --test kinds repeated_kind_req_encoding_is_complete -- --exact</automated>
  </verify>
  <done>One public neutral Query selects multiple arbitrary kinds through local semantics without protocol knowledge.</done>
</task>

<task type="auto">
  <name>Task 2: Prove exact multi-kind NIP-01 encoding</name>
  <read_first>
    @crates/fava-subscriptions/src/lib.rs
    @crates/fava-subscriptions/tests/tag_values.rs
    @crates/fava-subscriptions/BUILD.bazel
  </read_first>
  <files>crates/fava-subscriptions/tests/kinds.rs, crates/fava-subscriptions/BUILD.bazel</files>
  <behavior>
    - The exact REQ filter contains every canonical selected kind once in deterministic order.
    - A single repeated-call chain encodes all selected arbitrary kinds without a new public builder.
    - Unrelated tag, author, order, freshness, and limit fields remain unchanged.
  </behavior>
  <action>Using the exact wire descriptor and target created RED then GREEN in Task 1, record checksums and deliberately mutate conversion to emit only the first set element under `query_req_emits_first_kind_only`. Require the exact wire runtime descriptor to fail while identity/local controls remain green, restore byte/checksum exactly, and rerun. Preserve every unrelated Query field and keep production subscription code generic; this task adds no behavior or vocabulary.</action>
  <acceptance_criteria>The exact canonical REQ cell proves every repeated-call kind once in deterministic order, and the first-kind-only wire regression fails without a group fixture or new vocabulary.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-subscriptions --test kinds repeated_kind_req_encoding_is_complete -- --exact &amp;&amp; cargo fmt --all -- --check &amp;&amp; git diff --check</automated>
  </verify>
  <done>The neutral multi-kind Query is proven through the wire boundary consumed later by GroupRecords.</done>
</task>

</tasks>

## Artifacts this phase produces

- No new public symbol or type. Existing `Query::kind` gains additive repeated-call semantics while preserving singleton behavior.
- New test file: `crates/fava-subscriptions/tests/kinds.rs`.
- New exact tests: `repeated_kind_identity_is_canonical`, `repeated_kind_local_selection_is_complete`, and `repeated_kind_req_encoding_is_complete`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hostile caller iterator -> Query selection | Duplicate, reordered, empty, or oversized logical input enters canonical state. |
| Query -> NIP-01 filter | Selection meaning crosses to an external relay request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-01 | Tampering | repeated `Query::kind` | high | mitigate | Canonical repeated-call identity/local/wire parity tests prevent a later call from dropping earlier meaning. |
| T-0711-02 | Denial of service | kind input | low | accept | The approved method consumes one value per call and accepts no iterator; GroupRecords supplies one fixed six-kind chain. |
| T-0711-03 | Repudiation | exact REQ conversion | medium | mitigate | Assert exact serialized filter cells for complete and empty sets. |
</threat_model>

<verification>
Run the four exact tests, strict Clippy for the two affected crates, formatting, line limits, and diff checks. Bazel claims remain deferred to the fail-closed final preflight because Bazel/Bazelisk is currently absent.
</verification>

<success_criteria>
GROUP-03's generic prerequisite is complete: ordinary Query identity, local evaluation, and wire planning retain the entire canonical kind set with causal mutation evidence.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-01-SUMMARY.md` when done.
</output>
