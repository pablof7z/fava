---
phase: 07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/fava-query/src/selection.rs
  - crates/fava-query/tests/query_identity.rs
  - crates/fava-query-standard/tests/source_merge.rs
  - crates/fava-subscriptions/tests/kinds.rs
  - crates/fava-subscriptions/BUILD.bazel
autonomous: true
requirements: [GROUP-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An ordinary Query can select several event kinds without any NIP-29 branch, and Query::kind remains the one-kind cardinality of the same builder."
    - "Duplicate and adjacent kind inputs canonicalize deterministically; input order does not affect query identity or exact NIP-01 encoding."
    - "A present-empty kind set remains distinct from an absent kind axis and matches nothing locally and on the wire."
    - "Arbitrary non-NIP-29 fixtures prove multi-kind local evaluation and wire planning before GroupRecords consumes the surface."
  prohibitions:
    - status: flagged-unverified
      statement: "Neutral query and subscription owners contain no NIP-29 kind, group id, or protocol branch."
  artifacts:
    - path: crates/fava-query/src/selection.rs
      provides: "Neutral multi-kind Query construction"
      exports: ["Query::kinds"]
    - path: crates/fava-subscriptions/tests/kinds.rs
      provides: "Exact multi-kind NIP-01 REQ evidence"
  key_links:
    - from: crates/fava-query/src/selection.rs
      to: crates/fava-subscriptions/src/lib.rs
      via: "Query.selection.kinds is consumed as the complete canonical set"
      pattern: "selection.*kinds"
---

## Phase Goal

**As a** Fava application developer, **I want to** use the README-shaped `fava-simple-groups` capability across one or several host relays, **so that** I retain relay-local authority while reusing ordinary query and publication lifecycles.

<objective>
Prove the first neutral prerequisite end to end: one ordinary Query carries several kinds through identity, local evaluation, and exact wire planning.

Purpose: GROUP-03 requires one record query for kinds 39000-39005; universal owners must gain only generic set construction.
Output: `Query::kinds`, canonical identity/local behavior, and exact NIP-01 multi-kind evidence.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-CONTEXT.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-RESEARCH.md
@.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-PATTERNS.md
@crates/fava-query/src/selection.rs
@crates/fava-subscriptions/tests/tag_values.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace several arbitrary kinds from public Query identity through local evaluation</name>
  <read_first>
    @docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
    @crates/fava-query/src/selection.rs
    @crates/fava-query/tests/query_identity.rs
    @crates/fava-query-standard/src/lib.rs
    @crates/fava-query-standard/tests/source_merge.rs
  </read_first>
  <files>crates/fava-query/src/selection.rs, crates/fava-query/tests/query_identity.rs, crates/fava-query-standard/tests/source_merge.rs</files>
  <behavior>
    - `Query::kinds([30001, 30002])` matches both arbitrary kinds locally.
    - Reversed, duplicate, and adjacent inputs have identical Query identity and hashing.
    - `Query::kinds([])` is present-empty, matches nothing, and differs from no kind axis.
    - `Query::kind(k)` is behaviorally identical to `Query::kinds([k])`.
  </behavior>
  <action>Create exact tests named `multi_kind_identity_is_canonical` and `multi_kind_local_selection_is_complete` first and record their causal RED at the missing builder. Implement `Query::kinds(impl IntoIterator&lt;Item = Kind&gt;)` by collecting the complete set into the existing selection owner; make `kind` delegate to it. Do not add a union query, special record selector, or protocol constant. After GREEN, record the source checksum, deliberately retain only the first supplied kind, require both identity/local descriptors to fail while the one-kind control remains green, restore byte-exactly, verify the checksum, and rerun GREEN.</action>
  <acceptance_criteria>The public builder preserves absent versus present-empty semantics, duplicate/order canonicalization, both selected arbitrary kinds, and existing one-kind behavior; the named truncation break fails causally and restoration is byte-exact.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-query --test query_identity multi_kind_identity_is_canonical -- --exact &amp;&amp; cargo test -p fava-query-standard --test source_merge multi_kind_local_selection_is_complete -- --exact</automated>
  </verify>
  <done>One public neutral Query selects multiple arbitrary kinds through local semantics without protocol knowledge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove exact multi-kind NIP-01 encoding</name>
  <read_first>
    @crates/fava-subscriptions/src/lib.rs
    @crates/fava-subscriptions/tests/tag_values.rs
    @crates/fava-subscriptions/BUILD.bazel
  </read_first>
  <files>crates/fava-subscriptions/tests/kinds.rs, crates/fava-subscriptions/BUILD.bazel</files>
  <behavior>
    - The exact REQ filter contains every canonical selected kind once in deterministic order.
    - Present-empty kinds encode as an explicit empty kinds cell rather than an absent constraint.
    - Unrelated tag, author, order, freshness, and limit fields remain unchanged.
  </behavior>
  <action>Add the Cargo/Bazel test target and exact `multi_kind_req_encoding_is_complete` and `present_empty_kinds_remain_present` fixtures before altering any production subscription code; current conversion should go GREEN unchanged. Record a checksum, deliberately make the test-side Query contain a duplicate/reordered input and mutate the conversion to emit only one element, require the exact wire descriptor to fail, restore byte-exactly, and rerun. If production conversion needs a change, keep it generic and preserve every other Query field.</action>
  <acceptance_criteria>The exact canonical REQ cells prove complete and present-empty kind sets, and a one-kind wire regression fails without relying on a group-specific fixture.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-subscriptions --test kinds &amp;&amp; cargo fmt --all -- --check &amp;&amp; git diff --check</automated>
  </verify>
  <done>The neutral multi-kind Query is proven through the wire boundary consumed later by GroupRecords.</done>
</task>

</tasks>

## Artifacts this phase produces

- New public symbol: `Query::kinds` in `crates/fava-query/src/selection.rs`.
- New test file: `crates/fava-subscriptions/tests/kinds.rs`.
- New exact tests: `multi_kind_identity_is_canonical`, `multi_kind_local_selection_is_complete`, `multi_kind_req_encoding_is_complete`, `present_empty_kinds_remain_present`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hostile caller iterator -> Query selection | Duplicate, reordered, empty, or oversized logical input enters canonical state. |
| Query -> NIP-01 filter | Selection meaning crosses to an external relay request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-0711-01 | Tampering | `Query::kinds` | high | mitigate | Canonical set construction plus identity/local/wire parity tests prevents dropped or reordered meaning. |
| T-0711-02 | Denial of service | kind iterator | medium | mitigate | Reuse the existing bounded Query representation and present-empty typed semantics; no hidden truncation. |
| T-0711-03 | Repudiation | exact REQ conversion | medium | mitigate | Assert exact serialized filter cells for complete and empty sets. |
</threat_model>

<verification>
Run the four exact tests, strict Clippy for the two affected crates, formatting, line limits, and diff checks. Bazel claims remain deferred to the fail-closed final preflight because Bazel/Bazelisk is currently absent.
</verification>

<success_criteria>
GROUP-03's generic prerequisite is complete: ordinary Query identity, local evaluation, and wire planning retain the entire canonical kind set with causal mutation evidence.
</success_criteria>

<output>
Create `.planning/phases/07.1.1-deliver-fava-simple-groups-as-the-multi-relay-nip-29-capabil/07.1.1-01-SUMMARY.md` when done.
</output>
