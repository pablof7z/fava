---
phase: 07.8-independent-correctness-defects
plan: 05
type: execute
wave: 5
depends_on: ["07.8-04"]
files_modified:
  - crates/fava/src/query_source.rs
  - crates/fava/src/lib.rs
  - apps/canary/src/automatic_support.rs
  - apps/canary/src/automatic_publication.rs
  - crates/fava/tests/outbox_acquisition.rs
  - docs/spec/ARCHITECTURE.md
  - docs/internals/vocabulary.toml
autonomous: true
requirements: [WRITE-014, RELAY-001, QUERY-004]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`crates/fava/src/query_source.rs` no longer exists and `Fava` implements no query-source contract. The facade's last bare `tokio::spawn` is gone with it."
    - "The canary's automatic-routing scenario builds exactly one `Fava`, one transport, and one write store. No second engine exists anywhere in `apps/canary/`."
    - "The indexer REQ for a router's discovery query appears on the frame log of the same transport the application's own queries use, and the test goes red when a second transport is reintroduced."
    - "`ARCHITECTURE.md` describes one router acquisition door, not two invented services. The one load-bearing sentence in the deleted section survives verbatim."
    - "`docs/internals/vocabulary.toml` records the approved contract names, and `python3 tools/check_vocabulary.py` passes without any name registered merely to quiet it."
  artifacts:
    - "the deletion of `crates/fava/src/query_source.rs` and `apps/canary/src/automatic_support.rs::query_fava`"
    - "crates/fava/tests/outbox_acquisition.rs — the single-transport-stack falsifier"
    - "docs/spec/ARCHITECTURE.md — the router acquisition section rewritten to the shipped model"
  key_links:
    - "`OutboxRouter::new` no longer takes a source → `query_fava` has no caller → the second engine and its `WebSocketTransport` go"
    - "one transport → WRITE-014's acceptance (`GOALS:882`, 'through explicit query machinery and no separate transport stack') holds structurally rather than by inspection"
---

<objective>
Delete what the new contract made unnecessary, and bring the authoritative documents onto the
shipped model.

Purpose: `RESEARCH.md` Q5 counts one whole-workspace consumer of `impl QuerySource for Fava` and
zero tests. `apps/canary/src/automatic_support.rs:68-79` builds a second `Fava` with its own
`MemoryWriteStore` and its own `WebSocketTransport` purely to feed that impl, which is exactly
what WRITE-014 forbids. `ARCHITECTURE.md:1299-1328` specifies two injected services that are two
values of one existing enum wearing invented nouns; `07.8-CONTEXT.md` rules that the section is
deleted rather than implemented, keeping only its one load-bearing sentence.

Output: the deletions, the falsifier that makes the single-transport claim structural, and the
document edits.

## THE BINDING CONSTRAINT

Nothing in this plan touches the execution path. If a deletion turns out to require a change
below `Observer::open`, stop and report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.8-independent-correctness-defects/07.8-CONTEXT.md
@.planning/phases/07.8-independent-correctness-defects/RESEARCH.md
@.planning/phases/07.8-independent-correctness-defects/07.8-04-SUMMARY.md

@AGENTS.md
@crates/fava/src/query_source.rs
@crates/fava/src/lib.rs
@apps/canary/src/automatic_support.rs
@apps/canary/src/automatic_publication.rs
@docs/internals/vocabulary.toml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One engine, one transport — proved, then the second one deleted</name>
  <files>crates/fava/tests/outbox_acquisition.rs, crates/fava/src/query_source.rs, crates/fava/src/lib.rs, apps/canary/src/automatic_support.rs, apps/canary/src/automatic_publication.rs</files>
  <read_first>
    crates/fava/src/query_source.rs (whole file — note the fabricated empty initial snapshot at
    :21 and the bare `tokio::spawn` at :30 that `fava-runtime` exists to eliminate),
    crates/fava/src/lib.rs:1-10 (the module list),
    apps/canary/src/automatic_support.rs:68-79 (`query_fava`),
    apps/canary/src/automatic_publication.rs:93-97 (its only caller),
    crates/fava/tests/source_contract.rs:1-10 — this is the corpus for the two memory providers,
    not for `Fava`; confirm that before deleting anything.
  </read_first>
  <behavior>
    - One assembled `Fava` with one transport, an `OutboxRouter` over one indexer, and an unknown
      author. The kind-10002 REQ appears on that transport's frame log.
    - The application's own query to a different relay appears on the same transport's frame log.
      One stack serves both.
  </behavior>
  <action>
    Falsifier first: add the single-transport case to `crates/fava/tests/outbox_acquisition.rs`,
    asserting both frames land on the one transport double the assembly was given. Run it against
    the current tree and record the result — it should already pass after plan 04, because the
    router now acquires through the engine. A falsifier that is green before the change is not
    evidence of *this* change; its value is the named deliberate break below, which is what makes
    it load-bearing.

    Named deliberate break, run before the deletions and recorded in the summary: reintroduce a
    second transport into the assembly and route the router's acquisition through it. The test
    must go red. If it stays green, the assertion is on the wrong object — fix the test before
    deleting anything, because the deletions are what it is guarding.

    Then delete, in this order so each step compiles:

    1. `apps/canary/src/automatic_publication.rs`: drop the `query_fava` call and the
       `Arc<dyn QuerySource>` binding; `OutboxRouter::new` already lost that parameter in plan 04.
    2. `apps/canary/src/automatic_support.rs`: delete `query_fava` and every import that becomes
       unused with it — in particular the second `WebSocketTransport` and `MemoryWriteStore`.
    3. `crates/fava/src/query_source.rs`: delete the file and its module declaration at
       `crates/fava/src/lib.rs:5`. Remove the imports that become unused.

    Confirm nothing else consumed it: `cargo check --workspace --all-targets` is the real test,
    but also state in the summary the whole-workspace consumer count you found before deleting.

    Deleting this also removes the facade's last bare `tokio::spawn` — the one 07.5 created
    `fava-runtime` to eliminate and 07.6 did not reach. Note it; do not go looking for others in
    this plan.
  </action>
  <verify>
    <automated>test ! -f crates/fava/src/query_source.rs &amp;&amp; cargo test -p fava --test outbox_acquisition 2>&amp;1 | tail -20 &amp;&amp; cargo check --workspace --all-targets 2>&amp;1 | tail -10</automated>
  </verify>
  <done>The module file is gone, the workspace compiles, the single-transport case passes, and the deliberate break's red transcript is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Bring ARCHITECTURE.md and the vocabulary onto the shipped model</name>
  <files>docs/spec/ARCHITECTURE.md, docs/internals/vocabulary.toml</files>
  <read_first>
    docs/spec/ARCHITECTURE.md:1288-1340 (`## Route preview` at :1290 is authority and stays;
    `## Router input queries` at :1294; `### Local query service` at :1299;
    `### Explicit query service` at :1309; `### Router-owned state` at :1329 stays),
    AGENTS.md:64 ("Documentation describes the current model only. Replace superseded concepts
    completely; do not retain migration narration, aliases, or rejected-design commentary in
    authoritative docs or code."),
    docs/internals/vocabulary.toml:653-685 (the `Router` term entry).
  </read_first>
  <action>
    Replace the two invented service subsections between `### Local query service` and
    `### Router-owned state` with a description of the one door that shipped: routers acquire
    through a contract the engine implements, handed to them at open and preview time, carrying
    one capability — open a query. The contract refuses anything that is neither explicitly
    routed nor cache-only, and that refusal is the recursion guard.

    Carry these forward from the deleted text because they are load-bearing, not decoration:

    - The sentence "Router-owned acquisition is explicitly routed. This prevents automatic-routing
      recursion." — `07.8-CONTEXT.md` names it as the one load-bearing sentence in the section.
    - The list of what router acquisition reuses (wire protocol, subscription planner, transport,
      event verification, event cache, query-source observation, cancellation semantics). That
      list is the binding constraint written as a specification.
    - The local-first claim from `### Local query service`: a locally accepted relay-list update
      influences routing immediately through the merged local view, before a relay echoes it.
      Plan 04 task 1 proves it; the spec should still say it.

    Write the current model only. No note that two services were once specified, no rationale for
    the change, no aliases. `### Route preview` at :1290 and `### Router-owned state` at :1329 are
    untouched.

    Then update `docs/internals/vocabulary.toml`: add the two approved contract symbols to the
    `Router` term's `symbols` list, with the term's `meaning` and `distinction` extended to cover
    router acquisition. Register only what plan 02's checkpoint approved. If the checker demands a
    name that was not approved, stop and report it — `07.8-CONTEXT.md` is explicit that the
    vocabulary gate is intentionally red and names must not be registered to quiet it.
  </action>
  <verify>
    <automated>python3 tools/check_vocabulary.py 2>&amp;1 | tail -20 &amp;&amp; python3 -m pytest tools/tests -q 2>&amp;1 | tail -10</automated>
  </verify>
  <done>The vocabulary checker and its unit tests pass; `ARCHITECTURE.md` describes one acquisition door and retains the three load-bearing claims; no unapproved name was registered.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-wide sweep and findings ledger</name>
  <files>crates/fava/tests/outbox_acquisition.rs</files>
  <read_first>
    .planning/audit/2026-08-23/LEDGER.md:160-210 (Wave 5 and Wave 6 items this phase touches),
    AGENTS.md:70-72 (the 500-line soft and 800-line hard limits).
  </read_first>
  <action>
    Run the whole gate set and record every result verbatim in the summary:
    workspace tests, clippy with warnings denied across all targets, the vocabulary checker and
    its unit tests, and the canary's automatic-routing scenario end to end.

    Report the line count of every file this phase touched, flagging anything over 500 with its
    cohesion reason and anything over 800 as a hard failure that must be split before the phase
    closes. The files most at risk are `crates/fava-routing/src/chain.rs`,
    `crates/fava-routing/tests/failure_isolation.rs`, and
    `crates/fava-router-outbox/src/lib.rs`.

    Then write the findings ledger into the summary, one line per audit finding this phase
    touched, each with its disposition and the evidence that closes it:

    - Closed here: the chain's failure-derived settled absence; the LEDGER's WRITE-027
      cross-owner question; `router-acquisition-starts-from-fabricated-empty-state`;
      `router-source-fabricates-empty-initial` (both causes);
      `source-role-impersonation`'s live-half instance.
    - Re-specified and closed here: `outbox-does-not-coalesce-discovery`, with its named residual.
    - Confirmed already fixed on this tree, with the file and line that fixes each, not carried
      by this phase: `router-open-failure-kills-whole-query`,
      `chain-collapse-tears-down-all-relay-demand`,
      `outbox-fabricates-settled-absence-from-source-close`, `deletion-refused-at-capacity`,
      `only-from-relays-local-shadow`, `auth-required-bypasses-delivery-policy` /
      `auth-denied-collapsed-into-givenup`, `router-open-failure-abandons-write`.
    - Still open and explicitly not carried by this phase: `no-nip11-invented-planner-limits`
      and `expiry-is-never-swept`, plus the residual on `ingest-attribution-check-is-a-no-op`
      (the refusal moved up to the observe owner and is reachable there, but
      `RelayIngestError::WrongSubscription` now has no production producer).

    Leave the boundedness gap named in plan 02's threat register (nothing caps router-issued
    observations) open and recorded. `07.8-CONTEXT.md` puts it out of scope; do not close it.
  </action>
  <verify>
    <automated>cargo test --workspace 2>&amp;1 | tail -30 &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings 2>&amp;1 | tail -10</automated>
  </verify>
  <done>Every gate is green or its failure is recorded with a reason; the findings ledger is in the summary with evidence per line; no file exceeds 800 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → transport stack | After this plan there is exactly one, shared by application queries, router acquisition, and publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.8-20 | Denial of Service | duplicate transport stack | high | mitigate | A second engine means a second connection pool, second reconnect budget, and relay traffic attributable to no application demand — a reconnect storm the owner cannot see or bound. Task 1 deletes it and the deliberate break keeps it deleted. |
| T-07.8-21 | Repudiation | fabricated empty initial snapshot | high | mitigate | `query_source.rs:21` returned an empty snapshot stamped as event-cache state before any observation existed. Deleting the impl deletes the fabrication and the `SourceKind` impersonation with it. |
| T-07.8-22 | Tampering | vocabulary registration | medium | mitigate | Registering an unapproved name to make the checker pass would launder a vocabulary change through a feature change, which `AGENTS.md:62` forbids. Task 2 stops rather than registers. |
| T-07.8-23 | Denial of Service | unbounded router-issued observations | high | accept | Carried forward from plan 02 (T-07.8-08), out of scope per `07.8-CONTEXT.md`, recorded in task 3's ledger so it survives the phase. |

No package-manager installs in this plan; no new crate dependencies.
</threat_model>

<verification>
- `crates/fava/src/query_source.rs` does not exist and the workspace compiles.
- `grep -rn "Fava::builder\|Fava::builder()" apps/canary/src/automatic_support.rs` names one
  engine constructor, not two.
- `cargo test --workspace` and `cargo clippy --workspace --all-targets -- -D warnings` green.
- `python3 tools/check_vocabulary.py` and `python3 -m pytest tools/tests` green.
- The canary's automatic-routing scenario runs end to end.
- The single-transport falsifier goes red under its named deliberate break.
</verification>

<success_criteria>
One engine, one transport stack, one event cache, one registry, one admission window — and the
authoritative documents say so. Every audit finding this phase touched has a recorded disposition
with evidence, and the ones it does not carry are named rather than dropped.
</success_criteria>

<output>
Create `.planning/phases/07.8-independent-correctness-defects/07.8-05-SUMMARY.md` when done.
Record: the pre-deletion consumer count, the deliberate-break transcript, every gate result
verbatim, the line-count table, and the findings ledger.
</output>
