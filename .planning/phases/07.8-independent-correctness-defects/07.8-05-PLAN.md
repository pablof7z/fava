---
phase: 07.8-independent-correctness-defects
plan: 05
type: execute
wave: 5
depends_on: ["07.8-04"]
files_modified:
  - crates/fava-routing/src/lib.rs
  - crates/fava-routing/src/chain.rs
  - crates/fava-routing/tests/router_contract.rs
  - crates/fava-routing/tests/simultaneous_routers.rs
  - crates/fava-routing/tests/settled_absence.rs
  - crates/fava-routing/tests/failure_isolation.rs
  - crates/fava-router-app-relays/src/lib.rs
  - crates/fava-router-app-relays/Cargo.toml
  - crates/fava-router-app-relays/BUILD.bazel
  - crates/fava-router-hints/src/lib.rs
  - crates/fava-router-hints/tests/hints.rs
  - crates/fava-router-fallback-relays/src/lib.rs
  - crates/fava-router-fallback-relays/Cargo.toml
  - crates/fava-router-fallback-relays/BUILD.bazel
  - crates/fava-router-testkit/src/lib.rs
  - crates/fava-router-testkit/Cargo.toml
  - crates/fava-router-testkit/BUILD.bazel
  - crates/fava-router-outbox/src/lib.rs
  - crates/fava-router-outbox/tests/outbox.rs
  - crates/fava-router-outbox/Cargo.toml
  - crates/fava-router-outbox/BUILD.bazel
  - crates/fava-observe/src/observer.rs
  - crates/fava-observe/src/routes.rs
  - crates/fava-publication/src/run.rs
  - crates/fava-publication/src/materialization.rs
  - crates/fava-publication/tests/owner_lifecycle.rs
  - crates/fava-publication/tests/owner_lifecycle/harness.rs
  - crates/fava/src/builder.rs
  - crates/fava/src/lib.rs
  - crates/fava/src/query_source.rs
  - crates/fava/Cargo.toml
  - crates/fava/BUILD.bazel
  - crates/fava/tests/automatic_publication.rs
  - crates/fava/tests/automatic_routes.rs
  - crates/fava/tests/runtime_signers.rs
  - crates/fava/tests/multi_relay.rs
  - crates/fava/tests/write_bounds.rs
  - crates/fava/tests/simple_groups.rs
  - crates/fava/tests/simple_groups/routing.rs
  - crates/fava/tests/support/semantic_write.rs
  - crates/fava/tests/semantic_write_failures/route_revision.rs
  - crates/fava/tests/outbox_routing.rs
autonomous: false
requirements: [QUERY-004, QUERY-009, QUERY-010, WRITE-012, WRITE-013, WRITE-014, WRITE-015, WRITE-016, WRITE-027, WRITE-028, RELAY-001, ROUTER-001]
estimate:
  tokens: 59000
  raw_tokens: 59000
  tasks: 9
  confidence: low
  basis: "maximum of nine fresh-executor units; each is below the 80000-token blocker, total approximately 369000 tokens"
must_haves:
  truths:
    - "Plans 02/04 are explicitly approved and Plan 03 working evidence is verified before implementation."
    - "One required Router/RouterSession surface exists with no method defaults, old signature, dual entry, compatibility path, or hidden adapter."
    - "Outbox declares one bounded complete-author kind-10002 MaxAge Query across configured indexers and treats every ordered complete QuerySnapshot Vec as replacement truth."
    - "Identical discovery needs across queries/writes coalesce to one ordinary observation/REQ and release only after the last need closes; zero configured indexers plus no retained list is unknown, never settled absence."
    - "Every negative protocol/lifecycle cause has its own independently selectable causal test and RED transcript."
    - "AppRelayRouter plus OutboxRouter are proved through actual public Fava only for deduplication and outbox withdrawal; a separate controllable two-router owner test proves both withdrawal orders without claiming runtime app-relay withdrawal."
    - "Open automatic writes re-evaluate current router snapshots on router change, restart recovery, signer availability, reconnect, and queue drain; explicit routes remain fixed and acknowledged lanes are not resent without a corrected generation."
    - "Preview is local-only; complete closure removes every QuerySource-family dependency/representation from outbox and the facade adapter; repository-wide callers include router.preview."
    - "Nine bounded units use durable handoff artifacts and eight blocking resume checkpoints to force fresh executor contexts on one issue/branch/PLAN; every unit touches fewer than 15 physical non-handoff files and no Plan 06 is created."
    - "Every touched code file that remains after the slice is at most 800 lines; every file above 500 has a concrete cohesion reason in the durable handoff and final summary."
  artifacts:
    - "approved exact Router/RouterSession implementation and complete caller closure"
    - "crates/fava-router-outbox/src/lib.rs — stateless complete-snapshot projection"
    - "crates/fava-routing/tests/router_contract.rs — exact surface/no-default compile contract"
    - "crates/fava-routing/tests/simultaneous_routers.rs — two independently controlled withdrawal-order proofs"
    - "crates/fava/tests/outbox_routing.rs — public outbox/query/negative/coalescing/zero-indexer/preview proof"
    - "WRITE-028 public/owner tests in existing automatic publication, signer, reconnect, and owner-lifecycle homes"
    - "crates/fava/tests/simple_groups/routing.rs — cohesive extracted SpyRouter fixture and router-contract migration, keeping simple_groups.rs at or below 800 lines"
    - "deletion of crates/fava/src/query_source.rs and all outbox QuerySource-family state"
    - ".planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md — append-only nine-unit handoff with per-unit file counts and code-line gates"
  key_links:
    - "approved Router signatures -> repository-wide implementor/open/preview inventory -> exact closure"
    - "complete route targets -> one outbox Query -> engine-owned Observation -> ordered QuerySnapshot replacement -> one contribution slot"
    - "router slot ownership -> RelaySessionKey deduplication -> withdrawal only after last contributor"
    - "restart/signer/reconnect/queue-drain trigger -> current router snapshot -> new route revision without duplicate acknowledged delivery"
---

<objective>
Implement the approved engine-owned router-input contract, migrate outbox to ordinary complete QuerySnapshot replacement truth, prove exact ROUTER-001/WRITE-028 behavior, and subtract every parallel path.

Purpose: automatic routing must reuse one bounded query/engine stack while retaining independent contribution ownership, exact absence, immediate local truth, and current-route reevaluation.

Output: one complete implementation slice executed in nine fresh contexts, staged API/behavior RED evidence, complete caller closure, public/owner proofs, cohesive simple-groups extraction, automated code-line gates, and final subtraction.
</objective>

<execution_context>
@/Users/pablofernandez/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.8-independent-correctness-defects/07.8-CONTEXT.md
@.planning/phases/07.8-independent-correctness-defects/RESEARCH.md
@.planning/phases/07.8-independent-correctness-defects/07.8-PATTERNS.md
@.planning/phases/07.8-independent-correctness-defects/07.8-VALIDATION.md
@.planning/phases/07.8-independent-correctness-defects/07.8-01-SUMMARY.md
@.planning/phases/07.8-independent-correctness-defects/07.8-03-SUMMARY.md
@.planning/phases/07.8-independent-correctness-defects/07.8-04-SUMMARY.md
@docs/spec/ARCHITECTURE.md
@docs/internals/vocabulary.toml
</context>

<scope_reconciliation>
This is only the locked outbox migration/subtraction slice. Plan 01 and its stronger all-answered evidence remain immutable. Relay attribution is already verified. The production expiry sweep/cadence and RELAY-004 remain deferred; an explicit expiry mutation may test current snapshot replacement but cannot create a sweep task. QUERY-009 forbids six global claims; QUERY-010 owns exact EOSE. No Plan 06, extra architecture slice, compatibility path, or alternate PLAN is permitted.
</scope_reconciliation>

<fresh_context_protocol>
The eight `checkpoint:human-verify gate="blocking-human"` tasks below intentionally override end-of-phase human-verify mode as execution-integrity resume gates, not UAT. Each unit appends a packet to `07.8-05-EXECUTION.md` containing: unit ID; starting/ending HEAD; exact changed files and commits; approved-contract hash; normalized implementor/caller inventories; focused command/output hashes; remaining compile/behavior RED list; normalized vocabulary hash/status; Plan 01 and dirty/untracked preservation hashes; physical non-handoff file count; touched-code line-count report; required cohesion reasons; and next-unit invariants. The executor returns `## CHECKPOINT REACHED` and stops. Execute-phase discards that context and spawns a fresh gsd-executor on the same issue, branch, worktree, and Plan 05. A continuation verifies the packet before editing. No intermediate SUMMARY or PLAN is created.
</fresh_context_protocol>

<closure_inventory>
Before any production edit, extract the approved Router/RouterSession block from `07.8-04-SUMMARY.md` and hash it. Inventory repository-wide declarations, implementors, and callers, explicitly including `Router::preview` implementations and `router.preview(...)` calls:

1. `rg -n --glob '*.rs' 'trait Router|trait RouterSession|impl Router for|impl RouterSession for|fn queries\(|fn preview\(|fn open\(|fn replace\(' crates`
2. `rg -n --glob '*.rs' 'fava_routing::open\(|fava_routing::preview\(|router\.open\(|router\.preview\(|\.preview_routes\(' crates`

Normalize both by repository-relative path, symbol, and enclosing item, sort under `LC_ALL=C`, and write the complete classified result to the execution handoff. Include `crates/fava-routing/src/chain.rs` open/preview, observe route open, publication route open and automatic preview/materialization, facade preview, every router implementation, all RouterSession implementations, and every test double/direct caller. Any unclassified hit or inventory delta is an unmet precondition: revise this Plan 05 inventory before production; never omit a caller, add a default method, or retain the old surface.

Currently known production implementors: AppRelayRouter/StaticSession, HintRouter/HintSession, FallbackRelayRouter/FallbackSession, OutboxRouter/OutboxSession, DelayedRouter/DelayedSession, and OpenedChain. Known test implementors/callers include routing settled-absence/failure-isolation fixtures, publication owner-lifecycle RefusingRouter, automatic-publication RefusingRouter, route-revision QueuedRouter/QueuedSession, simple-groups SpyRouter, semantic-write CountingRouter/StaticRouterSession, hints/outbox direct tests, automatic-routes, write-bounds, and facade preview. Task R0 adds only router_contract fixtures; C2B extracts and migrates only the simple-groups routing fixture; Task B adds only named public/controller fixtures. The final compiler plus the same normalized searches must close the exact set.
</closure_inventory>

<execution_budget>
The 59,000-token estimate is the maximum fresh unit, not the sum. R0 24k; C1 40k; C2A 38k; C2B 40k; C3 59k; B 42k; G1 48k; G2 44k; S 34k. Every value is below the 80k blocker. The corresponding physical non-handoff file counts are 1, 6, 11, 8, 14, 9, 10, 6, and 8; every count is below 15. Each unit is independently resumed and bounded by its listed files/invariants. If a unit discovers closure outside its inventory, stop at its checkpoint and revise that unit within this same Plan 05; do not create another architecture slice or Plan 06.
</execution_budget>

<code_file_gate>
R0 records one exact `PLAN_START_HEAD: &lt;sha&gt;` line in `07.8-05-EXECUTION.md`. After every unit, derive the distinct remaining touched Rust files from `git diff --name-only --diff-filter=ACMR "$plan_start"..HEAD -- '*.rs'`; record each path and `wc -l` result. The automated gate fails if any count exceeds 800. For every count above 500, the same handoff must contain exactly one non-empty `COHESION_REASON &lt;path&gt;: &lt;reason&gt;` line explaining why that physical file remains cohesive. Deleted code is recorded separately and has no after-slice line count. Unit C2B must extract the SpyRouter fixture and router-specific helper code from the currently 797-line `crates/fava/tests/simple_groups.rs` into `crates/fava/tests/simple_groups/routing.rs` before or with its signature migration; both files pass the same gate, and `simple_groups.rs` must finish at or below 800 lines.
<automated>plan_start=$(sed -n 's/^PLAN_START_HEAD: //p' .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md | head -1); test -n "$plan_start" &amp;&amp; git diff --name-only --diff-filter=ACMR "$plan_start"..HEAD -- '*.rs' | LC_ALL=C sort -u &gt;/tmp/fava-07.8-05-code-files &amp;&amp; while IFS= read -r file; do lines=$(wc -l &lt; "$file"); test "$lines" -le 800 || exit 1; if test "$lines" -gt 500; then awk -v prefix="COHESION_REASON $file: " 'index($0,prefix)==1 &amp;&amp; length($0)&gt;length(prefix){count++} END{exit count==1?0:1}' .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md || exit 1; fi; done &lt; /tmp/fava-07.8-05-code-files</automated>
</code_file_gate>

<prohibitions>
- No production edit before approval/evidence/inventory preconditions.
- No QuerySource-family dependency or representation in outbox: QuerySource, OpenedQuerySource, QuerySourceClosed, QuerySourceError, SourceSnapshot, SourceChanges, FavaChanges, or any wrapper/alias/equivalent lifecycle.
- No router-owned event/freshness map, Observation/handle/opener, cumulative ingestion, missing-author narrowing, second engine/cache/transport, automatic recursive acquisition, provider-private path, or fabricated initial snapshot.
- No default trait methods, old/dual signatures, adapter, alias, deprecation, feature flag, migration narration, or compatibility behavior.
- No other-router mutation, first-contributor withdrawal, failure-derived absence, preview acquisition, process-name close, or unbounded input/result/evidence/task/queue.
- No Plan 01, attribution, production expiry-sweep, RELAY-004, or dirty/untracked approval-tooling edit.
</prohibitions>

<tasks>

<task type="tracer" tdd="true">
  <name>Unit R0: Freeze exact approved-API compile RED and repository-wide closure inventory</name>
  <precondition>07.8-02/04 summaries record Pablo approval, 07.8-03 summary is green, and the exact approved contract hash matches architecture.</precondition>
  <files>crates/fava-routing/tests/router_contract.rs, .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md</files>
  <behavior>
    - A compile fixture implements only the approved required methods/signatures and calls queries, preview, open, current, replace, and close with exact arity/ownership.
    - The current pre-change trait fails specifically on missing approved methods/changed signatures; no forbidden facade/outbox adapter participates.
    - Repository-wide implementor and caller inventory includes router.preview and every direct preview/open route.
  </behavior>
  <action>
    Capture initial HEAD as the handoff's exact `PLAN_START_HEAD`, plus Plan 01, dirty/untracked hashes, and normalized vocabulary checker bytes/status. Run and classify the two closure-inventory commands. Add one compile-conformance integration test using only fava-routing plus neutral fava-query values; it must not import fava, outbox, QuerySource, or any adapter. Run `cargo test -p fava-routing --test router_contract` against the old trait and record a non-zero compile result whose diagnostics name the exact approved methods/signatures. Syntax/import/setup failures are invalid RED. Record the contract hash, inventory, command/output hash, unchanged production hashes, one-file count, and touched-code line report in the R0 packet, then run the code-file gate.</action>
  <verify>
    <automated>! cargo test -p fava-routing --test router_contract --no-fail-fast &amp;&amp; test -s .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md</automated>
  </verify>
  <done>Exact adapter-free approved-API compile RED and complete normalized caller/implementor inventory are durable; production is unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Resume C1 in a fresh executor context</name>
  <what-built>R0 exact contract compile RED plus normalized repository-wide closure inventory.</what-built>
  <how-to-verify>Confirm the handoff contains the approved-contract hash, causal compiler diagnostics, router.preview callers, vocabulary baseline, and unchanged production/dirty hashes. Execute-phase must stop this executor and spawn a fresh C1 executor.</how-to-verify>
  <resume-signal>Reply `resume 07.8-05 C1`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Unit C1: Implement the one required routing contract and composer host</name>
  <precondition>The fresh executor verifies R0 and the normalized inventory has no unclassified hit.</precondition>
  <files>crates/fava-routing/src/lib.rs, crates/fava-routing/src/chain.rs, crates/fava-routing/tests/router_contract.rs, crates/fava-routing/tests/simultaneous_routers.rs, crates/fava-routing/tests/settled_absence.rs, crates/fava-routing/tests/failure_isolation.rs, .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md</files>
  <behavior>
    - Router/RouterSession exactly match the approved block with required no-default methods and no old next-change/dual entry.
    - Complete declarations validate before work; ordered input Vec replaces atomically under private exact generation identity.
    - Composer slots remain independent, rollback-safe, all-answered-correct, and deduplicate by session while retaining reasons.
    - Two controlled routers prove first-then-second and second-then-first withdrawal in separate test selections.
  </behavior>
  <action>
    Implement the exact Plan 04 contract in fava-routing. Add no helper trait or public wrapper. Update chain orchestration for private generation fencing, full ordered snapshot delivery, atomic contribution replacement, failure rollback, and exact close. Make router_contract compile GREEN. In simultaneous_routers, create two independently controlled generic routers; add separately selectable `shared_destination_survives_first_then_second_withdrawal` and `shared_destination_survives_second_then_first_withdrawal`. These owner tests prove both orders and make no claim about changing AppRelayRouter at runtime. Preserve all Plan 01 settled-absence cases. Run focused tests/clippy, rerun structural searches proving one trait declaration/surface and no default/old method, run the code-file gate, and append the C1 packet. Downstream implementors may remain compiler-red until C2A; list them exactly.</action>
  <verify>
    <automated>cargo test -p fava-routing --test router_contract --test simultaneous_routers --test settled_absence --test failure_isolation --no-fail-fast &amp;&amp; cargo clippy -p fava-routing --all-targets -- -D warnings</automated>
  </verify>
  <done>The sole exact routing surface and both owner withdrawal orders are green; Plan 01 stays green; every remaining downstream compiler error is inventoried.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Resume C2A in a fresh executor context</name>
  <what-built>C1 exact no-default contract, generation/composer host, and two owner withdrawal-order proofs.</what-built>
  <how-to-verify>Confirm the packet records one trait surface, absence of the old method/dual entry, both independently selected withdrawal tests, Plan 01 GREEN, and the exact remaining implementor list. Spawn a fresh C2A executor.</how-to-verify>
  <resume-signal>Reply `resume 07.8-05 C2A`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Unit C2A: Close the four non-outbox provider crates</name>
  <precondition>The fresh executor verifies C1 and the remaining compiler-error list equals the R0 inventory minus C1 files.</precondition>
  <files>crates/fava-router-app-relays/src/lib.rs, crates/fava-router-app-relays/Cargo.toml, crates/fava-router-app-relays/BUILD.bazel, crates/fava-router-hints/src/lib.rs, crates/fava-router-hints/tests/hints.rs, crates/fava-router-fallback-relays/src/lib.rs, crates/fava-router-fallback-relays/Cargo.toml, crates/fava-router-fallback-relays/BUILD.bazel, crates/fava-router-testkit/src/lib.rs, crates/fava-router-testkit/Cargo.toml, crates/fava-router-testkit/BUILD.bazel, .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md</files>
  <behavior>
    - App-relay, hints, fallback, and testkit implement only the approved value-input surface.
    - Static routers declare zero queries and interpret an empty ordered snapshot Vec; no fabricated snapshot or default is used.
    - Existing delayed replacement, fallback, hint, and settled-absence behavior remains exact.
  </behavior>
  <action>
    Migrate only the four named non-outbox provider crates. Add/remove direct fava-query metadata edges only where compiler-proven by the approved signatures; no workspace/root dependency changes. Static policies return an explicit empty Query Vec and require an empty input Vec. Testkit delayed replacement uses RouterSession::replace through the approved surface, not an async side channel. Rerun each focused crate/test, strict clippy, normalized inventory, and the code-file gate; every remaining non-outbox compiler hit must be one of the direct test callers assigned C2B. Append exact file/commit/test/output, 11-file count, line gate, and remaining-error packet.</action>
  <verify>
    <automated>cargo test -p fava-router-app-relays -p fava-router-hints -p fava-router-fallback-relays -p fava-router-testkit --no-fail-fast &amp;&amp; cargo test -p fava-routing --no-fail-fast &amp;&amp; cargo clippy -p fava-router-app-relays -p fava-router-hints -p fava-router-fallback-relays -p fava-router-testkit --all-targets -- -D warnings</automated>
  </verify>
  <done>All four provider crates use only the approved surface; no compatibility/default path exists; the exact remaining direct-test inventory is bounded to C2B and C2A's line gate passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Resume C2B in a fresh executor context</name>
  <what-built>C2A provider-crate closure with an exact remaining direct-test inventory.</what-built>
  <how-to-verify>Confirm the four provider crates are green, metadata changes are compiler-required, C2A touched 11 non-handoff files, its code-line gate passed, and the remaining non-outbox hits equal C2B's seven existing files plus the named simple-groups extraction. Spawn a fresh C2B executor.</how-to-verify>
  <resume-signal>Reply `resume 07.8-05 C2B`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Unit C2B: Close direct test callers and extract the simple-groups routing fixture</name>
  <precondition>The fresh executor verifies the C2A packet and the remaining direct-test compiler inventory exactly matches this unit.</precondition>
  <files>crates/fava-publication/tests/owner_lifecycle/harness.rs, crates/fava/tests/automatic_publication.rs, crates/fava/tests/automatic_routes.rs, crates/fava/tests/write_bounds.rs, crates/fava/tests/simple_groups.rs, crates/fava/tests/simple_groups/routing.rs, crates/fava/tests/support/semantic_write.rs, crates/fava/tests/semantic_write_failures/route_revision.rs, .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md</files>
  <behavior>
    - Every named test double implements only the approved required methods and value-input surface.
    - Existing refusal, delayed replacement, static routing, preview, and write-bound behavior remains exact.
    - The simple-groups SpyRouter fixture lives cohesively in simple_groups/routing.rs and simple_groups.rs is at most 800 lines after migration.
  </behavior>
  <action>
    Migrate only the normalized direct-test callers listed in this unit. Before or with the SpyRouter signature migration, move the cohesive SpyRouter fixture and its router-specific imports/helpers from the currently 797-line `crates/fava/tests/simple_groups.rs` into `crates/fava/tests/simple_groups/routing.rs`, included from the parent integration test without adding a second test target or compatibility wrapper. Preserve every simple-groups assertion and fixture behavior byte-for-byte except required module/import/signature movement. Run focused tests, strict clippy, normalized inventory, and the code-file gate. Require `simple_groups.rs` and the extracted file independently at or below 800, and record concrete cohesion reasons for either touched code file above 500. Append the eight-file count, extraction diff, line report, and remaining C3-only inventory.</action>
  <verify>
    <automated>test "$(wc -l &lt; crates/fava/tests/simple_groups.rs)" -le 800 &amp;&amp; test "$(wc -l &lt; crates/fava/tests/simple_groups/routing.rs)" -le 800 &amp;&amp; cargo test -p fava --test simple_groups --test automatic_publication --test automatic_routes --test write_bounds --test semantic_write_failures --no-fail-fast &amp;&amp; cargo test -p fava-publication --test owner_lifecycle --no-fail-fast &amp;&amp; cargo clippy -p fava -p fava-publication --all-targets -- -D warnings</automated>
  </verify>
  <done>Every direct test caller uses the approved surface; the simple-groups routing fixture is cohesively extracted, both physical files pass the 800-line hard limit, C2B's full line gate passes, and remaining closure is exactly C3 scope.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Resume C3 in a fresh executor context</name>
  <what-built>C2A/C2B non-outbox provider/test closure, cohesive simple-groups extraction, and normalized remaining scope.</what-built>
  <how-to-verify>Confirm focused crates are green, metadata changes are compiler-required, C2A/C2B file and line gates pass, simple_groups.rs is at most 800 lines, and the only remaining implementors/callers are outbox plus engine/publication/facade files assigned C3. Spawn a fresh C3 executor.</how-to-verify>
  <resume-signal>Reply `resume 07.8-05 C3`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Unit C3: Establish adapter-free outbox/engine/facade compile closure</name>
  <precondition>The fresh executor verifies C2A/C2B and both approved-summary preconditions; no remaining closure file lies outside C3.</precondition>
  <files>crates/fava-router-outbox/src/lib.rs, crates/fava-router-outbox/tests/outbox.rs, crates/fava-router-outbox/Cargo.toml, crates/fava-router-outbox/BUILD.bazel, crates/fava-observe/src/observer.rs, crates/fava-observe/src/routes.rs, crates/fava-publication/src/run.rs, crates/fava-publication/src/materialization.rs, crates/fava-publication/tests/owner_lifecycle.rs, crates/fava/src/builder.rs, crates/fava/src/lib.rs, crates/fava/src/query_source.rs, crates/fava/Cargo.toml, crates/fava/BUILD.bazel, .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md</files>
  <behavior>
    - Outbox declares one complete-author/all-indexer bounded MaxAge Query and interprets whole ordered snapshots without QuerySource-family state.
    - Observe/publication/facade host the same engine-owned observation path for open and preview.
    - The facade query_source file/module is deleted in this unit; compilation uses no adapter, alternate path, default, or dual signature.
    - Full workspace compiles on the approved surface before behavior RED staging.
  </behavior>
  <action>
    Establish real compile closure, not a placeholder. Outbox queries exact kind 10002 for the complete canonical requested-author set against every configured indexer with approved MaxAge and a positive bound; each complete current QuerySnapshot Vec is the sole projection input. Delete KnownLists, remember, missing-author narrowing, cumulative ingestion, and all QuerySource-family fields/methods/fixtures while changing the signature; never retain them for later subtraction.

    Implement one engine host shared by observation routing, automatic publication/rematerialization, and facade preview. It validates complete declarations, opens ordinary observations, coalesces exact Query identity through fava-observe, supplies same-order complete replacements, fences generations, and closes exactly. Preview evaluates local one-shot snapshots. Delete `crates/fava/src/query_source.rs`, its module/impl, and adapter-only Cargo/Bazel edges now. Update direct open/preview callers including router.preview; do not route through Fava-as-QuerySource.

    This unit claims compile closure and owner contract only. Run cargo check for the full workspace, focused existing non-capstone tests, normalized caller inventory, and the code-file gate. Record any behavioral failures without weakening them; Task B owns the independently causal behavior RED matrix. Append the C3 packet with 14-file count, deleted symbols/file, zero adapter paths, line report, and cohesion reasons.
  </action>
  <verify>
    <automated>cargo check --workspace --all-targets --all-features &amp;&amp; cargo test -p fava-router-outbox --test outbox --no-fail-fast &amp;&amp; test ! -e crates/fava/src/query_source.rs</automated>
  </verify>
  <done>The whole workspace compiles on exactly one approved adapter-free surface; outbox parallel truth and facade QuerySource adapter are already deleted; C3's line gate passes; behavior claims remain gated on B/G1/G2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Resume B in a fresh executor context</name>
  <what-built>C3 exact adapter-free compile closure and deletion of the parallel query path.</what-built>
  <how-to-verify>Confirm workspace compile GREEN, query_source.rs absent, outbox QuerySource-family search empty, normalized callers closed on the approved surface, and no behavior completion claimed. Spawn a fresh B executor.</how-to-verify>
  <resume-signal>Reply `resume 07.8-05 B`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Unit B: Stage independently causal behavior RED on the approved API</name>
  <precondition>The fresh executor verifies C3 compiles without the forbidden adapter and reruns the exact contract/inventory/subtraction prechecks.</precondition>
  <files>crates/fava-router-outbox/tests/outbox.rs, crates/fava-routing/tests/simultaneous_routers.rs, crates/fava/tests/outbox_routing.rs, crates/fava/tests/automatic_publication.rs, crates/fava/tests/runtime_signers.rs, crates/fava/tests/multi_relay.rs, crates/fava-publication/tests/owner_lifecycle.rs, crates/fava/Cargo.toml, crates/fava/BUILD.bazel, .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md</files>
  <behavior>
    - Warm/cold/stale and each relay/access/filter/since/until/limit mismatch have separate exact tests.
    - Proven empty and each silence/timeout/refusal/panic/authentication/CLOSED/disconnect/cancellation/observation-close cause have separate exact tests.
    - Identical needs coalesce/release correctly and zero indexers remains unknown.
    - Replacement, deletion, explicit expiry mutation, accepted-local cancellation, and cache removal reveal current truth.
    - Actual public AppRelayRouter+OutboxRouter proves deduplication then outbox withdrawal only; generic owner tests prove both withdrawal orders.
    - Router change, restart recovery, signer availability, reconnect, and queue drain each re-evaluate current snapshots in separately selectable WRITE-028 tests.
  </behavior>
  <action>
    Add behavior tests only after the exact approved API compiles and the forbidden adapter is absent. Use actual protocol/cache facts and the one assembled Fava stack. Do not seed derived outbox state. Give every row an independently selectable name and command; combined negative matrices are forbidden.

    Required negative selectors: `silence_keeps_outbox_need_unknown`, `timeout_keeps_outbox_need_unknown`, `router_refusal_keeps_outbox_need_unknown`, `router_panic_keeps_other_contributions`, `authentication_requirement_keeps_outbox_need_unknown`, `closed_keeps_outbox_need_unknown`, `disconnect_keeps_outbox_need_unknown`, `cancellation_keeps_outbox_need_unknown`, and `observation_close_keeps_outbox_need_unknown`. Each must fail at its own settlement/evidence/frame assertion, never a common compile/setup error.

    Add `identical_outbox_needs_share_one_req_until_last_close` and `zero_indexers_without_retained_list_is_unknown`. Add public `app_relay_and_outbox_deduplicate_then_outbox_withdrawal_preserves_destination`; drive an actual OutboxRouter list removal/complete replacement and assert the still-static AppRelayRouter reason remains. Do not attempt or claim runtime app-relay withdrawal. Run the two generic owner withdrawal-order tests separately.

    Add WRITE-028 selectors `router_change_recomputes_open_automatic_route`, `restart_recovery_uses_current_router_snapshot`, `signer_availability_uses_current_router_snapshot`, `reconnect_uses_current_router_snapshot`, and `queue_drain_uses_current_router_snapshot`. Each changes router knowledge while the write is open/parked, triggers exactly the named lifecycle, and asserts the new snapshot is used; explicit-route controls stay fixed and acknowledged destinations are not resent without a corrected generation.

    Run each selector independently against C3. Require causal behavior RED. If a case is already correct, prove discrimination with one narrowly scoped temporary deliberate break, record RED plus unrelated green guard, and restore before leaving B; do not weaken assertions or manufacture a shared failure. Run the code-file gate and append every command/status/assertion, nine-file count, line report/cohesion reasons, and unchanged production hashes to the B packet.
  </action>
  <verify>
    <automated>test ! -e crates/fava/src/query_source.rs &amp;&amp; ! cargo test -p fava --test outbox_routing zero_indexers_without_retained_list_is_unknown -- --exact &amp;&amp; ! cargo test -p fava --test outbox_routing silence_keeps_outbox_need_unknown -- --exact &amp;&amp; ! cargo test -p fava --test outbox_routing app_relay_and_outbox_deduplicate_then_outbox_withdrawal_preserves_destination -- --exact &amp;&amp; ! cargo test -p fava --test automatic_publication restart_recovery_uses_current_router_snapshot -- --exact &amp;&amp; ! cargo test -p fava --test runtime_signers signer_availability_uses_current_router_snapshot -- --exact</automated>
  </verify>
  <done>Every behavior row has its own approved-path causal RED or restored discriminating-break transcript; no compile/setup/adapter failure satisfies a row.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Resume G1 in a fresh executor context</name>
  <what-built>B independent behavior RED matrix on the compile-closed approved path.</what-built>
  <how-to-verify>Confirm every negative cause, coalescing, zero-indexer, public outbox-withdrawal-only proof, both owner orders, and five WRITE-028 triggers have independent command/assertion transcripts with production restored. Spawn a fresh G1 executor.</how-to-verify>
  <resume-signal>Reply `resume 07.8-05 G1`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Unit G1: Make routing, outbox, and public query behavior GREEN</name>
  <precondition>The fresh executor verifies all B rows independently and the approved contract/inventory/subtraction invariants remain intact.</precondition>
  <files>crates/fava-routing/src/lib.rs, crates/fava-routing/src/chain.rs, crates/fava-router-outbox/src/lib.rs, crates/fava-router-outbox/tests/outbox.rs, crates/fava-observe/src/observer.rs, crates/fava-observe/src/routes.rs, crates/fava/src/builder.rs, crates/fava/src/lib.rs, crates/fava/tests/outbox_routing.rs, crates/fava/tests/multi_relay.rs, .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md</files>
  <behavior>
    - Every B routing/outbox/negative/coalescing/zero-indexer selector turns GREEN without selector/assertion weakening.
    - Exact Query identity coalesces identical discovery; acquisition releases after last owner.
    - Zero indexers/no retained list is unresolved unknown; only exact proven empty settles absence.
    - Current complete snapshot replacement drives routing and public outbox withdrawal behavior.
    - Public actual AppRelayRouter+OutboxRouter proof remains limited to dedup plus outbox withdrawal; owner controller proves both orders.
  </behavior>
  <action>
    Implement only the routing/query-host/outbox fixes required by B. Outbox derives newest qualified NIP-65 per author using timestamp then event id, replaces its whole contribution, and never reserves an author on parse/failure. Engine hosting coalesces identical ordinary queries by exact Query identity and retains per-route ownership so last-close alone releases acquisition. Zero configured indexers is an unresolved need unless current local truth resolves it; it never fabricates absence. Preserve failure/panic isolation and Plan 01 all-answered semantics.

    Run every routing/outbox B selector independently GREEN, both owner withdrawal orders, focused crates, strict clippy, exact caller inventory, and the code-file gate. Leave the five WRITE-028 lifecycle selectors unchanged and causally RED for G2. Append results, 10-file count, line report/cohesion reasons, and exact remaining G2 RED list to the G1 packet. Do not perform publication/write-trigger fixes, new architecture, or deferred scope.
  </action>
  <verify>
    <automated>cargo test -p fava-routing -p fava-router-outbox -p fava-observe --no-fail-fast &amp;&amp; cargo test -p fava --test outbox_routing --test multi_relay --no-fail-fast &amp;&amp; cargo clippy -p fava-routing -p fava-router-outbox -p fava-observe -p fava --all-targets -- -D warnings</automated>
  </verify>
  <done>ROUTER-001, negative causes, replacement, public outbox withdrawal, and both owner orders are green for exact reasons; G1's line gate passes; only G2's five WRITE-028 triggers remain RED.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Resume G2 in a fresh executor context</name>
  <what-built>G1 complete routing/outbox GREEN matrix with the five WRITE-028 lifecycle selectors still causally RED.</what-built>
  <how-to-verify>Confirm all routing/outbox selectors are GREEN, G1 touched 10 non-handoff files, its code-line gate passes, public/owner withdrawal claims retain their boundaries, and the remaining RED list is exactly router-change/restart/signer/reconnect/queue-drain. Spawn a fresh G2 executor.</how-to-verify>
  <resume-signal>Reply `resume 07.8-05 G2`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Unit G2: Make every WRITE-028 reevaluation trigger GREEN</name>
  <precondition>The fresh executor verifies the G1 packet, reruns all five remaining selectors RED, and confirms routing/outbox GREEN guards.</precondition>
  <files>crates/fava-publication/src/run.rs, crates/fava-publication/src/materialization.rs, crates/fava-publication/tests/owner_lifecycle.rs, crates/fava/tests/automatic_publication.rs, crates/fava/tests/runtime_signers.rs, crates/fava/tests/multi_relay.rs, .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md</files>
  <behavior>
    - Router change, restart recovery, signer availability, reconnect, and queue drain each re-evaluate the current complete router snapshot.
    - Explicit routes remain fixed and acknowledged destinations are not resent without a corrected event generation.
    - Every G1 routing/outbox behavior remains GREEN.
  </behavior>
  <action>
    Keep automatic write route strategy live while unresolved or new lanes remain possible. At router update, recovered open obligation, signer availability, reconnect, and queue-drain resumption, read the current router snapshot before opening or retiring lanes. Explicit routes remain fixed; acknowledged destinations are not resent absent a corrected event generation. Make each named WRITE-028 selector independently GREEN without weakening its trigger or assertion, rerun G1 guards, focused publication/public tests, strict clippy, and the code-file gate. Append the six-file count, line report/cohesion reasons, five independent GREEN transcripts, and remaining S-only audit list to the G2 packet.</action>
  <verify>
    <automated>cargo test -p fava-publication --test owner_lifecycle --no-fail-fast &amp;&amp; cargo test -p fava --test automatic_publication --test runtime_signers --test multi_relay --no-fail-fast &amp;&amp; cargo test -p fava --test outbox_routing --no-fail-fast &amp;&amp; cargo clippy -p fava-publication -p fava --all-targets -- -D warnings</automated>
  </verify>
  <done>All five WRITE-028 lifecycle triggers are independently green, explicit/acknowledged controls remain exact, G1 guards stay green, and G2's line gate passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Resume S in a fresh executor context</name>
  <what-built>G1/G2 complete GREEN matrix, exact caller closure, and per-unit line gates.</what-built>
  <how-to-verify>Confirm all independent selectors are GREEN, five WRITE-028 triggers use current snapshots, G1/G2 file and line gates pass, public/owner withdrawal claims have correct boundaries, and the remaining work is only final subtraction/audit/breaks. Spawn a fresh S executor.</how-to-verify>
  <resume-signal>Reply `resume 07.8-05 S`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Unit S: Prove complete subtraction, named breaks, and final integration</name>
  <precondition>The fresh executor verifies all R0/C1/C2A/C2B/C3/B/G1/G2 packets, exact HEAD chain, dirty hashes, per-unit file/line gates, and GREEN selectors before final audit.</precondition>
  <files>crates/fava-router-outbox/src/lib.rs, crates/fava-router-outbox/tests/outbox.rs, crates/fava-router-outbox/Cargo.toml, crates/fava-router-outbox/BUILD.bazel, crates/fava/src/lib.rs, crates/fava/src/query_source.rs, crates/fava/Cargo.toml, crates/fava/BUILD.bazel, .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md</files>
  <behavior>
    - Every G1/G2 behavior stays green after repository-wide subtraction audit.
    - Outbox contains no QuerySource-family dependency/representation, retained list/revision authority, cumulative ingestion, history narrowing, or direct source lifecycle.
    - Exactly one required Router/RouterSession surface and caller path remains with no default/dual/adapter/alias/flag.
    - Normalized vocabulary before/after and approval-tooling/Plan 01 hashes are unchanged.
  </behavior>
  <action>
    Audit rather than defer deletion: C3 must already have removed all parallel state and the facade adapter. Correct any residue in its owning file scope, rerun focused GREEN, then run these repository-wide gates:

    1. `crates/fava/src/query_source.rs` absent; no facade QuerySource impl/module/FavaChanges.
    2. No QuerySource-family type or equivalent representation anywhere in outbox source/tests/manifests/Bazel: QuerySource, OpenedQuerySource, QuerySourceClosed, QuerySourceError, SourceSnapshot, SourceChanges, FavaChanges, Arc of QuerySource, or watch/closing wrapper.
    3. No KnownLists, remember, last_checked, cumulative ingestion, missing-author narrowing, retained relay-list revision/map, source-change fixture, or source-change-only dependency in outbox.
    4. Exactly one Router and one RouterSession trait declaration; exact approved methods; no old next_change, method default body, dual signature, alternate routing entry, or caller missing from the normalized inventory, including router.preview.
    5. No forbidden router-query synonym, compatibility/deprecation/alias/feature flag, alternate facade module edge, or restored canary second engine/transport.
    6. Public outbox Cargo/Bazel edges are singular; no obsolete adapter/source-change metadata remains.

    Apply and restore three named breaks independently: complete-author declaration narrows to previously missing authors; whole-snapshot projection retains a disappeared list cumulatively; composer withdraws a shared destination on first contributor. The first fails replacement/coalescing cases, the second deletion/cache-removal/cancellation, the third only both owner-order tests and the public outbox-withdrawal retention test. Record exact red/green guards and restoration hashes.

    Finish with every focused selector, workspace all-target/all-feature tests, strict clippy, formatting, normalized vocabulary before/after comparison including exit status, vocabulary unit tests, exact inventories/gates, and byte hashes for Plan 01 plus dirty/untracked approval tooling. Append S with its eight-file count and the complete cumulative touched-code report, add one concrete cohesion reason for every remaining touched code file above 500, then run the automated code-file gate. Create the final summary only after the gate proves every remaining touched code file is at most 800 lines and `simple_groups.rs` is at most 800; do not create Plan 06.
  </action>
  <verify>
    <automated>cargo test --workspace --all-targets --all-features --no-fail-fast &amp;&amp; cargo clippy --workspace --all-targets --all-features -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; test ! -e crates/fava/src/query_source.rs &amp;&amp; ! rg -n '\b(QuerySource|OpenedQuerySource|QuerySourceClosed|QuerySourceError|SourceSnapshot|SourceChanges|FavaChanges|KnownLists)\b|Arc&lt;dyn[[:space:]]+QuerySource&gt;|fn[[:space:]]+remember\(|\.remember\(' crates/fava-router-outbox &amp;&amp; ! rg -n 'impl[[:space:]]+QuerySource[[:space:]]+for[[:space:]]+Fava|mod[[:space:]]+query_source|\bFavaChanges\b' crates/fava/src crates/fava/tests &amp;&amp; ! rg -n '\b(RouterQueries|RouterObservation|OpenedQuery|RouterQueryService|RouterQueryHandle|RouterQueryOpener|RouterInputObservation|RouterInputHandle|RouterInputService)\b' crates/fava-routing crates/fava-router-* crates/fava-observe crates/fava-publication crates/fava/src docs/spec</automated>
    <automated>test "$(wc -l &lt; crates/fava/tests/simple_groups.rs)" -le 800 &amp;&amp; plan_start=$(sed -n 's/^PLAN_START_HEAD: //p' .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md | head -1); test -n "$plan_start" &amp;&amp; git diff --name-only --diff-filter=ACMR "$plan_start"..HEAD -- '*.rs' | LC_ALL=C sort -u &gt;/tmp/fava-07.8-05-code-files &amp;&amp; while IFS= read -r file; do lines=$(wc -l &lt; "$file"); test "$lines" -le 800 || exit 1; if test "$lines" -gt 500; then awk -v prefix="COHESION_REASON $file: " 'index($0,prefix)==1 &amp;&amp; length($0)&gt;length(prefix){count++} END{exit count==1?0:1}' .planning/phases/07.8-independent-correctness-defects/07.8-05-EXECUTION.md || exit 1; fi; done &lt; /tmp/fava-07.8-05-code-files</automated>
  </verify>
  <done>All final gates and behavior pass; three breaks discriminate and restore; one Router surface and zero QuerySource-family outbox/facade paths remain; every remaining touched code file passes the 800-line/cohesion gate; preservation/vocabulary hashes match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router declaration -> engine | Replaceable policy requests bounded ordinary work. |
| indexer -> query/cache | Untrusted NIP-65/completion/failure affects routes only through normal admission and exact evidence. |
| QuerySnapshot -> outbox | Complete current truth becomes one router's whole contribution. |
| contributions -> publication | Independent reasons merge into current desired lanes and durable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.8-05-01 | Spoofing | indexer completion | critical | mitigate | Plan 03 exact EOSE plus independent negative selectors. |
| T-07.8-05-02 | Tampering | cumulative outbox truth | critical | mitigate | Whole-snapshot replacement, subtraction, deliberate break. |
| T-07.8-05-03 | Denial of Service | router inputs/results/tasks | high | mitigate | Approved bounds, coalescing, all-or-nothing validation, exact close. |
| T-07.8-05-04 | Repudiation | failure-derived absence | critical | mitigate | Plan 01 all-answered plus zero-indexer/negative proofs. |
| T-07.8-05-05 | Tampering | shared withdrawal | high | mitigate | Per-router slots, two owner orders, bounded public outbox-withdrawal proof. |
| T-07.8-05-06 | Elevation of Privilege | alternate query path | high | mitigate | Adapter deletion and repository-wide subtraction gates. |
</threat_model>

<acceptance_criteria>
- Approved contract/evidence and exact normalized inventory are checked before edits.
- R0 compile RED uses only approved neutral API and no forbidden adapter; B behavior RED runs only after adapter-free compile closure and gives every cause its own selector/transcript.
- Nine units have explicit handoff packets, eight blocking resume checkpoints, fresh executor contexts, per-unit estimates below 80k, and fewer than 15 physical non-handoff files each while remaining one issue/branch/PLAN slice.
- `simple_groups.rs` is at most 800 lines after cohesive extraction to `crates/fava/tests/simple_groups/routing.rs`; the automated cumulative gate proves every remaining touched code file is at most 800 and records one concrete cohesion reason for each file above 500.
- Repository-wide inventory includes Router::preview implementations and router.preview callers; final inventory is closed.
- Exactly one required Router/RouterSession surface remains with no defaults, old/dual entry, compatibility, or alternate path.
- ROUTER-001 identical needs coalesce/release correctly and zero indexers/no retained list reports unknown.
- All five WRITE-028 router-change/restart/signer/reconnect/queue-drain tests use current snapshots; explicit/acknowledged controls remain exact.
- Each negative cause is independently selectable and only proven empty settles absence.
- Actual AppRelayRouter+OutboxRouter public proof claims dedup plus outbox withdrawal only; two controlled owner tests prove both orders separately.
- All QuerySource-family dependencies/representations and parallel state are absent from outbox/facade; no expiry sweep, RELAY-004, attribution, Plan 01, or Plan 06 work appears.
- Normalized `python3 tools/check_vocabulary.py` before/after bytes/status match and vocabulary unit tests pass; dirty/untracked hashes match.
</acceptance_criteria>

<verification>
<automated>cargo test --workspace --all-targets --all-features --no-fail-fast &amp;&amp; cargo clippy --workspace --all-targets --all-features -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; python3 tools/check_vocabulary.py &gt;/tmp/fava-07.8-05-vocabulary-after.raw 2&gt;&amp;1; vocab_after_status=$?; { printf 'status=%s\n' "$vocab_after_status"; sed -E 's#(^|[[:space:]])/Users/[^[:space:]]+#\1&lt;ABS_PATH&gt;#g; s/[[:space:]]+$//' /tmp/fava-07.8-05-vocabulary-after.raw | LC_ALL=C sort; } &gt;/tmp/fava-07.8-05-vocabulary-after.norm; cmp /tmp/fava-07.8-05-vocabulary-before.norm /tmp/fava-07.8-05-vocabulary-after.norm &amp;&amp; test ! -e crates/fava/src/query_source.rs</automated>
</verification>

<success_criteria>
Outbox routing is one ordinary bounded engine-owned query consumer: exact current truth drives independent contributions and every open-write reevaluation trigger, identical needs coalesce, zero-source remains unknown, preview is inert, and every parallel QuerySource/adapter path is gone.
</success_criteria>

<output>
Create `07.8-05-SUMMARY.md` only after S. Record approvals, exact contract/inventory hashes, R0 compile RED, every independent B RED/GREEN selector, nine unit packets, eight checkpoint resumes, actual per-unit token use and physical non-handoff file counts, ROUTER-001 coalescing/zero-indexer evidence, all five WRITE-028 triggers, public outbox-withdrawal-only and both owner-order evidence, simple-groups extraction, cumulative line-count gate plus every required cohesion reason, three break/restoration transcripts, all subtraction outputs, normalized vocabulary hashes/status, deleted paths/symbols/metadata, Plan 01/dirty preservation hashes, and commits. No Plan 06.
</output>
