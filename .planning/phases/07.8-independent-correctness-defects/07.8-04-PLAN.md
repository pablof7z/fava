---
phase: 07.8-independent-correctness-defects
plan: 04
type: execute
wave: 4
depends_on: ["07.8-03"]
files_modified:
  - docs/spec/ARCHITECTURE.md
autonomous: false
requirements: [QUERY-004, QUERY-009, QUERY-010, WRITE-012, WRITE-013, WRITE-014, WRITE-015, WRITE-016, RELAY-001, ROUTER-001]
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Plan 03's working public MaxAge and exact EOSE evidence is a checked precondition."
    - "The exact Rust-shaped Router/RouterSession contract below is stated byte-identically in architecture Task 1 and Pablo's checkpoint."
    - "Routers declare complete ordered ordinary Query values; engine orchestration validates and owns observations/generations; routers receive only complete ordered QuerySnapshot replacements."
    - "Preview uses the same query declaration and interpretation over one-shot local snapshots and owns no acquisition or retained observation."
    - "Every error, shortfall, generation swap, stale completion, and close outcome has an exact owner and no partial installation or cross-router withdrawal."
    - "The contract changes only existing Router/RouterSession public symbols and adds no public/cross-crate noun or vocabulary entry."
  artifacts:
    - "docs/spec/ARCHITECTURE.md — one exact current router contract, lifecycle, bounds, failure semantics, and falsifiers"
    - "07.8-04-SUMMARY.md — Plan 03 evidence, exact approved contract, vocabulary delta, and Pablo response"
  key_links:
    - "Router::queries order -> engine-owned observations -> same-order QuerySnapshot Vec -> RouterSession::replace"
    - "private input generation -> stale-result fence -> atomic complete contribution replacement -> old observation close"
    - "Router::preview with locally evaluated snapshots -> one contribution -> zero retained work"
---

<objective>
Replace stale router-input architecture with one exact bounded engine-owned declarative Router/RouterSession contract and stop for Pablo's approval.

Purpose: routers need ordinary query values, not observation/query-opening authority or a second source of truth.

Output: architecture-only contract, exact semantics/falsifiers, zero vocabulary delta, and non-bypassable decision.
</objective>

<execution_context>
@/Users/pablofernandez/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.8-independent-correctness-defects/07.8-CONTEXT.md
@.planning/phases/07.8-independent-correctness-defects/RESEARCH.md
@.planning/phases/07.8-independent-correctness-defects/07.8-PATTERNS.md
@.planning/phases/07.8-independent-correctness-defects/07.8-VALIDATION.md
@.planning/phases/07.8-independent-correctness-defects/07.8-03-SUMMARY.md
@docs/spec/ARCHITECTURE.md
@docs/internals/vocabulary.toml
</context>

<scope_reconciliation>
This is only the locked Plan 04 architecture slice after working Plan 03 evidence. Relay attribution and immutable Plan 01 are excluded; expiry sweeping and RELAY-004 remain deferred. QUERY-009's six prohibitions remain distinct from QUERY-010's exact EOSE evidence. No implementation or Plan 05 work occurs here.
</scope_reconciliation>

<prohibitions>
- No Rust/tests/manifests/generated/runtime changes and no vocabulary.toml edit.
- No router-owned cache, QuerySource, Observation, query handle/opener, imperative acquisition, cumulative truth, private evaluator, second engine/cache/transport, or automatic-routing recursion.
- No default trait method, dual signature, compatibility alias, provider framework, hidden fallback, feature flag, or migration narration.
- No new nominal term. If this exact contract cannot be sound without one, stop and present a separate full vocabulary packet; Plan 05 remains blocked.
- No Plan 03/01 or dirty/untracked approval-tooling edit.
</prohibitions>

<tasks>

<task type="auto">
  <name>Task 1: Author the exact bounded Router/RouterSession contract after Plan 03 evidence</name>
  <precondition>`07.8-03-SUMMARY.md` records green public close/reopen zero-REQ, mixed-source acquisition, no polling, original-time late join, QUERY-009 six-prohibition proof, exact QUERY-010 EOSE, invalidation, and all restored breaks.</precondition>
  <files>docs/spec/ARCHITECTURE.md</files>
  <read_first>
    `07.8-03-SUMMARY.md` in full for exact working interfaces/evidence;
    `07.8-CONTEXT.md` owners, required behavior, and subtraction;
    `docs/spec/ARCHITECTURE.md:1197-1379,2059-2098,2352-2393,2968-3002,3096-3118`;
    `crates/fava-routing/src/lib.rs:316-357` only to inventory the surface this architecture replaces;
    existing Query, QuerySnapshot, Observation, Router, and RouterSession vocabulary entries.
  </read_first>
  <rust_contract>
```rust
pub trait Router: Send + Sync {
    fn name(&self) -> &str;

    fn queries(
        &self,
        request: &RouteRequest,
        upstream: &RoutePlan,
    ) -> Result<Vec<Query>, RouterError>;

    fn preview(
        &self,
        request: &RouteRequest,
        upstream: &RoutePlan,
        inputs: &[QuerySnapshot],
    ) -> Result<RouteContribution, RouterError>;

    fn open(
        &self,
        request: RouteRequest,
        upstream: Arc<RoutePlan>,
        inputs: Vec<QuerySnapshot>,
    ) -> Result<Box<dyn RouterSession>, RouterError>;
}

pub trait RouterSession: Send {
    fn current(&self) -> RouteContribution;

    fn replace(
        &mut self,
        upstream: Arc<RoutePlan>,
        inputs: Vec<QuerySnapshot>,
    ) -> Result<RouteContribution, RouterError>;

    fn close(&mut self);
}
```
  </rust_contract>
  <action>
    Capture normalized repository-wide vocabulary output/exit status and the vocabulary.toml hash before editing. Replace the stale router-input and outbox-input sections completely; do not append a competing model.

    Put the `<rust_contract>` block above into the Router contract section verbatim. Method names, arity, references, owned values, and return types are fixed. `queries` and `preview` borrow request/upstream and cannot retain those borrows. `preview` borrows the complete snapshot slice. `open` owns RouteRequest, Arc<RoutePlan>, and Vec<QuerySnapshot>; `Box<dyn RouterSession>` is `'static`. `replace` owns the next Arc/Vec, returns an owned complete contribution, and retains no input borrow. No method has a default body.

    Define ordered delivery exactly: after `queries` succeeds, `inputs.len()` must equal the query count and `inputs[i]` is the complete current QuerySnapshot for `queries[i]`. The Vec is one atomic full replacement, never a delta or partial prefix. Query errors refuse before observations open. Query shortfalls/evidence remain inside their corresponding snapshots; RouterError reports declaration/interpretation/session failure; routing orchestration converts it into a bounded router-attributed RouteShortfall while preserving the last coherent contribution and every other router.

    Define engine ownership/generation exactly. The engine validates the entire query set and existing bounds before work, opens ordinary fava-observe Observations, captures a private tuple `(route-session identity, router index, input-set generation)`, and discards every completion that misses it. On a declaration change, it opens the next validated set, obtains all initial local snapshots without waiting for relay completion, calls `replace`, atomically installs the returned complete contribution plus generation, then closes the prior observation set. If open/evaluation/replace fails, it closes the candidate set and retains the old generation/contribution. The private generation is not exposed or registered as vocabulary.

    Define close exactly: close is idempotent; the engine fences the generation first, invokes RouterSession::close once, closes each exact owned Observation once, and withdraws resulting demand once. After fencing, replace is not called and late values are inert. RouterSession::close owns no observation handle itself; it releases only router-internal per-request state.

    Define preview exactly: engine calls `queries`, validates the full set, obtains one-shot EventCache and WriteStore source snapshots, evaluates the ordered complete QuerySnapshot Vec, calls `preview`, and closes local source handles before return. It emits zero relay frames, retains zero observations, does not retain SourceCoverage, and shares no hidden state with a later open.

    Keep numerical bounds explicit: at most 256 queries per router session; each query satisfies QueryBounds, explicit acquisition is non-empty and never Automatic, and positive result limit is at most 4096; aggregate configured-router-count times 256 and every result/evidence/contribution bound are checked before partial installation with existing errors/shortfalls.

    State vocabulary consequences: Router and RouterSession are existing approved public symbols whose method shapes change; Query, QuerySnapshot, RouteRequest, RoutePlan, RouteContribution, RouterError, and RouteShortfall are reused unchanged as concepts. `queries`/`replace` method names and the private generation tuple add no nominal vocabulary. Any required extra public/cross-crate type blocks Plan 05 for a separate approval packet.

    Add executable falsifiers: invalid/257th/unbounded queries produce zero partial work; ordered-snapshot permutation changes/fails the exact projection test; stale generations cannot revise; preview has zero frames/retention; close drains once; one router failure preserves another; shared destination survives either owner-level withdrawal order. Re-run normalized vocabulary comparison and unit tests; vocabulary.toml stays byte-identical.
  </action>
  <verify>
    <automated>git diff --check -- docs/spec/ARCHITECTURE.md &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check</automated>
  </verify>
  <done>Architecture contains exactly the stated Rust contract plus ordered replacement, error/shortfall, generation, preview, close, bounds, and zero-new-vocabulary semantics; no other file changes.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Pablo approves, revises, or rejects the exact Router/RouterSession contract</name>
  <decision>Approve this exact Rust-shaped public contract and its stated ownership/lifecycle semantics for Plan 05.</decision>
  <context>
```rust
pub trait Router: Send + Sync {
    fn name(&self) -> &str;

    fn queries(
        &self,
        request: &RouteRequest,
        upstream: &RoutePlan,
    ) -> Result<Vec<Query>, RouterError>;

    fn preview(
        &self,
        request: &RouteRequest,
        upstream: &RoutePlan,
        inputs: &[QuerySnapshot],
    ) -> Result<RouteContribution, RouterError>;

    fn open(
        &self,
        request: RouteRequest,
        upstream: Arc<RoutePlan>,
        inputs: Vec<QuerySnapshot>,
    ) -> Result<Box<dyn RouterSession>, RouterError>;
}

pub trait RouterSession: Send {
    fn current(&self) -> RouteContribution;

    fn replace(
        &mut self,
        upstream: Arc<RoutePlan>,
        inputs: Vec<QuerySnapshot>,
    ) -> Result<RouteContribution, RouterError>;

    fn close(&mut self);
}
```

Borrowed arguments live only for each call; open/replace inputs are owned and the returned session is `'static`. Snapshot Vec length/order must exactly match the latest successful `queries` result and replaces the whole prior input set. Query errors refuse before work; Query shortfalls/evidence remain per snapshot; RouterError becomes bounded router-attributed RouteShortfall while last coherent/other-router contributions survive. Engine-private route-session/router-index/input-generation identity fences all late completions. Candidate generation install is atomic before old observations close; failure retains old state. Close fences first and drains the session/observations/demand exactly once. Preview performs local one-shot evaluation only. No method default, alternate entry, public generation wrapper, or new vocabulary term is permitted.
  </context>
  <options>
    <option id="approve">
      <name>Approve exact contract</name>
      <pros>Plan 05 has implementation-ready method/ownership semantics with no naming decision.</pros>
      <cons>Every Router, RouterSession, direct open caller, and preview caller must migrate in the one locked closure slice.</cons>
    </option>
    <option id="revise">
      <name>Revise and re-present</name>
      <pros>Corrects exact signature or lifecycle semantics before code.</pros>
      <cons>Plan 04 remains incomplete and the full byte-identical Task/checkpoint contract must be re-presented.</cons>
    </option>
    <option id="reject">
      <name>Reject contract</name>
      <pros>Prevents an unsound boundary.</pros>
      <cons>Plan 05 remains blocked and outbox retains its current parallel truth until another approved architecture exists.</cons>
    </option>
  </options>
  <resume-signal>Reply `approve`; `revise: &lt;exact correction&gt;`; or `reject: &lt;reason&gt;`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router declarations -> engine | Replaceable policy requests bounded ordinary query work. |
| engine snapshots -> RouterSession | Complete exact query truth enters third-party interpretation. |
| contribution -> composer | One router may replace only its own slot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.8-04-01 | Elevation of Privilege | queries | critical | mitigate | Explicit/local only; Automatic refused before work. |
| T-07.8-04-02 | Denial of Service | declarations/results | critical | mitigate | 256/4096/existing bounds and all-or-nothing validation. |
| T-07.8-04-03 | Tampering | generation replacement | high | mitigate | Private exact generation, atomic install, stale discard. |
| T-07.8-04-04 | Repudiation | router failure | high | mitigate | Existing attributed errors/shortfalls preserve coherent contributions. |
</threat_model>

<acceptance_criteria>
- Plan 03 evidence is a checked precondition; no repair or implementation occurs.
- Task 1 and checkpoint contain byte-identical Rust contract blocks.
- Architecture states exact method names/arity, ownership/lifetimes, ordered full-snapshot replacement, errors/shortfalls, generation swap, failure rollback, preview, and idempotent close.
- One required Router/RouterSession surface has no defaults, dual entry, compatibility path, or public generation wrapper.
- No vocabulary.toml change; normalized repository checker output/exit status matches before/after and vocabulary unit tests pass.
- Attribution, Plan 01, expiry sweep, and RELAY-004 remain excluded.
- Plan 05 requires an approved 07.8-04 summary containing this exact contract.
</acceptance_criteria>

<verification>
<automated>git diff --check -- docs/spec/ARCHITECTURE.md &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; python3 tools/check_vocabulary.py &gt;/tmp/fava-07.8-04-vocabulary-after.raw 2&gt;&amp;1; vocab_after_status=$?; { printf 'status=%s\n' "$vocab_after_status"; sed -E 's#(^|[[:space:]])/Users/[^[:space:]]+#\1&lt;ABS_PATH&gt;#g; s/[[:space:]]+$//' /tmp/fava-07.8-04-vocabulary-after.raw | LC_ALL=C sort; } &gt;/tmp/fava-07.8-04-vocabulary-after.norm; cmp /tmp/fava-07.8-04-vocabulary-before.norm /tmp/fava-07.8-04-vocabulary-after.norm</automated>
</verification>

<success_criteria>
Pablo can approve one implementation-ready Rust contract without interpreting missing ownership, lifetime, ordering, failure, generation, close, bound, or vocabulary decisions.
</success_criteria>

<output>
Create `07.8-04-SUMMARY.md` only after `approve`. Record Plan 03 evidence hashes, the exact contract verbatim, bounds/owner map, normalized vocabulary hashes/status, changed-file scope, and Pablo's response. On revise/reject, return `## CHECKPOINT REACHED`; do not begin Plan 05.
</output>
