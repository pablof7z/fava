---
phase: 07.8-independent-correctness-defects
plan: 02
type: execute
wave: 2
depends_on: ["07.8-01"]
files_modified:
  - docs/spec/ARCHITECTURE.md
  - docs/internals/vocabulary.toml
autonomous: false
requirements: [QUERY-004, QUERY-009, QUERY-010, QUERY-013A, EVENT-005, EVENT-007]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The architecture states that `Freshness::MaxAge(Duration)` is evaluated once at ordinary `Query` open against exact source-scoped completion evidence and never creates an age-expiry polling loop."
    - "The concrete `SourceCoverage` proposal identifies the closest existing concepts, exact observable distinction, counterexample, immutable fields, sole retention owner, lifecycle, forcing requirements, insufficiency of current state, explicit bounds, and executable falsifier."
    - "Only an attributed `EoseCompleteness::Proven` result for the exact relay access, filter, interval, and limit can create reusable completion evidence; empty proven completion is valid, while silence, timeout, refusal, authentication requirement, disconnect, limited EOSE, and close are not."
    - "QUERY-009 remains the separate prohibition on all six global claims: global synced, global complete, percentage completeness, authoritative empty, end of global history, and proof that no matching event exists elsewhere; QUERY-010 alone owns exact EOSE."
    - "No Rust production or test implementation is added in this plan, and Plan 03 cannot execute until Pablo approves or renames the vocabulary packet."
  artifacts:
    - "docs/spec/ARCHITECTURE.md — proposed MaxAge open-time flow, source-coverage ownership/coherence table, bounds, failure isolation, and falsifiers"
    - "docs/internals/vocabulary.toml — concrete candidate `SourceCoverage` packet and `Query`/`Freshness` extension, with no unrelated registrations"
    - ".planning/phases/07.8-independent-correctness-defects/07.8-02-SUMMARY.md — exact vocabulary-check delta and Pablo's approve/rename/reject result"
  key_links:
    - "`Query::max_age` policy -> one observation-open timestamp -> exact `EventCache` coverage lookup -> per-source relay demand decision"
    - "attributed EOSE + `EoseCompleteness::Proven` -> retained exact completion fact -> later public close/reopen zero-REQ falsifier"
    - "event eviction/reset/restart guarantee -> coverage invalidation -> no stale completeness claim"
---

<objective>
Define the MaxAge/source-coverage architecture and present one concrete vocabulary packet for human approval. This is an architecture-only slice: it establishes ownership, bounds, invalidation, failure isolation, and falsifiers without changing Rust production or test code.

Purpose: the ordinary query model needs a durable, exact answer to whether a source/request completion is recent enough before implementation can be sound. Event time, relay observation time, and reopen time are not that answer.

Output: proposed authoritative architecture and vocabulary entries, followed by a non-bypassable Pablo approve/rename/reject checkpoint. Plan 03 remains blocked until the proposal is approved under its final name.
</objective>

<execution_context>
@/Users/pablofernandez/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.8-independent-correctness-defects/07.8-CONTEXT.md
@.planning/phases/07.8-independent-correctness-defects/RESEARCH.md
@.planning/phases/07.8-independent-correctness-defects/07.8-PATTERNS.md
@.planning/phases/07.8-independent-correctness-defects/07.8-VALIDATION.md
@.planning/phases/07.8-independent-correctness-defects/07.8-01-SUMMARY.md
@docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md
@docs/spec/ARCHITECTURE.md
@docs/spec/partial-spec-api-semantics.md
@docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
@docs/internals/vocabulary.toml
@docs/internals/README.md
</context>

<read_first>
1. `AGENTS.md` in full, especially architectural vocabulary approval and the six architecture gates.
2. `07.8-CONTEXT.md` in full; its ordinary-query decision, locked plan sequence, and subtractive constraints are non-negotiable.
3. `RESEARCH.md` sections “Architecture Responsibility Map” and “Mandatory Pablo Checkpoint”.
4. `07.8-PATTERNS.md` assignments for Plans 02 and 03.
5. `07.8-VALIDATION.md` Plan 02 gate and deliberate-break matrix.
6. `ARCHITECTURE.md` current `fava-query`, `fava-event-cache`, query-source composition, and `fava-observe` ownership/open-sequence sections.
7. `vocabulary.toml` complete `Query`, `QuerySnapshot`, `Observation`, `Router`, `CacheMutation`, and `Write` entries before editing.
</read_first>

<scope_reconciliation>
This plan implements only the locked MaxAge/source-coverage architecture slice. Relay attribution is already verified and excluded; Plan 01 and its stronger completed summary are immutable; an expiry sweep owner/cadence and RELAY-004 remain deferred. QUERY-009 is not completion evidence: it forbids all six global claims. QUERY-010 is the sole requirement used when actual attributed EOSE creates exact source-scoped completion.
</scope_reconciliation>

<prohibitions>
- No Rust production code, Rust tests, manifests, generated code, or runtime configuration changes.
- No router-owned cache, freshness timestamp map, query truth, or lifecycle.
- No use of event `created_at`, `RelayEvidence.observed_at`, cache revision, source-open time, or reopen time as completion proof.
- No filter-containment reuse: reusable coverage matches exact relay access and exact filter, including interval and limit.
- No age-expiry timer, polling cadence, background sweep, compatibility alias, alternate query path, or hidden flag.
- No additional nominal term beyond the single `SourceCoverage` candidate unless Pablo first rejects it and requests a replacement packet.
- No edits to 07.8-01 or to dirty/untracked vocabulary-approval tooling.
</prohibitions>

<tasks>

<task type="auto">
  <name>Task 1: Author the MaxAge/source-coverage architecture and exact vocabulary packet</name>
  <files>docs/spec/ARCHITECTURE.md, docs/internals/vocabulary.toml</files>
  <read_first>
    `docs/spec/partial-spec-api-semantics.md:92-123` for the already-specified freshness surface;
    `docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md:403-481,580-616,852-900,1150-1163` for QUERY-009/013A, EVENT-005/007, WRITE-014/015, and ROUTER-001;
    `docs/spec/ARCHITECTURE.md:562-728,763-849,1032-1107,2059-2098,3096-3118` for current owners and dependency rules;
    `docs/internals/vocabulary.toml:290-418` for the exact registry schema and neighboring query terms.
  </read_first>
  <action>
    Before editing, run both vocabulary commands and save stdout, stderr, exit status, and hashes under an explicit `/tmp/fava-07.8-02-vocabulary-before-*` prefix. The repository-wide checker is known red; preserve its diagnostics as the baseline rather than changing unrelated files.

    Update the current architecture, not migration commentary. Extend the ordinary `Query` section with `Freshness::MaxAge(Duration)` and `Query::max_age(Duration)`: the duration is part of query identity; one open timestamp is captured; freshness is decided independently for each exact source/request; the immediate merged local snapshot is returned before remote work; uncovered sources acquire; a MaxAge observation continues reacting to local source replacements but never starts remote work merely because wall time later crosses the maximum age.

    Extend the `EventCache` contract/ownership description with one immutable `SourceCoverage` value whose fields are exactly `session: RelaySessionKey`, `filter: nostr::Filter`, and `completed_at: Timestamp`. State that `fava-query` owns the neutral value's meaning, the selected `EventCache` provider exclusively owns retention and coherence, and `fava-observe` exclusively produces it after an attributed current request receives EOSE classified `EoseCompleteness::Proven`. Preserve existing observation-scoped evidence; this value does not replace it.

    Make the match and counterexample exact. Completion from indexer A cannot suppress indexer B, another access identity, another author set, a broader or narrower filter, another `since` or `until`, another limit, or the same request after its maximum age. An empty proven EOSE is reusable for only the exact request. Every non-completion cause remains non-completion and cannot create settled absence.

    Specify provider coherence and bounds without adding a second lifecycle: coverage count is bounded by the memory provider's existing non-zero capacity with deterministic oldest-first eviction; over-bound keys are refused before mutation through the existing cache error family; matching event eviction invalidates every retained exact filter whose answer could have changed; reset clears coverage; the volatile provider retains neither events nor coverage across restart; deletion, supersession, and expiry preserve coverage only while retained cache facts still make the completed window coherent. Retention failure creates scoped shortfall and future acquisition, never a false completeness fact.

    Add executable falsifier contracts to the owning sections: public close/reopen through `fava` yields the immediate local snapshot and zero new REQ for fresh exact coverage; relay/access/filter/interval/limit mismatches reacquire only the mismatched source; matching eviction invalidates; limited or failed completion never retains; advancing time after open creates no timer-driven work. Add a separate QUERY-009 falsifier that waits indefinitely on an empty query and inventories the complete public snapshot/evidence/diagnostic surface to prove it exposes or implies none of the six forbidden global claims. Keep this proof separate from QUERY-010's actual-EOSE cases.

    Add one complete `SourceCoverage` registry entry and extend the existing `Query` entry for the `Freshness::MaxAge` surface. The new entry must carry nearest Nostr concept, distinction, counterexample, owner, lifecycle, forcing requirements, and executable falsifier. Register the exact approved Rust path as a specified public symbol in this plan; Plan 03 must not edit `vocabulary.toml` merely to promote the symbol after implementation. Do not claim the implementation exists before Plan 03. Keep the plan summary explicit that implementation is gated on the next task.

    Re-run both vocabulary commands. Normalize before/after output by recording exit status, removing absolute-worktree prefixes and trailing whitespace, and sorting diagnostic lines under `LC_ALL=C`; compare normalized bytes and hashes. The unit suite must be green. The repository-wide diagnostics must have no unrelated delta from the captured baseline; if the proposal itself creates a diagnostic, fix only the proposal/registry mismatch. Do not touch checker code or approval tooling to make the delta disappear.
  </action>
  <verify>
    <automated>python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; git diff --check -- docs/spec/ARCHITECTURE.md docs/internals/vocabulary.toml</automated>
  </verify>
  <done>`ARCHITECTURE.md` and `vocabulary.toml` contain the complete proposal and falsifiers; no other file changed in this task; vocabulary unit tests pass; the summary reports the exact before/after repository-wide diagnostic delta.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Pablo approves, renames, or rejects the MaxAge/source-coverage packet</name>
  <decision>Approve the proposed `Freshness::MaxAge(Duration)` extension and exact `SourceCoverage` vocabulary/ownership packet before any production implementation.</decision>
  <context>
    Proposed meaning: one exact relay session actually completed one exact admitted filter at `completed_at`, and the selected event cache still retains coherent reusable proof for that result window. The immutable value lives in `fava-query`; retention, bounds, invalidation, reset, and restart guarantees belong only to `EventCache`; `fava-observe` is the sole producer after attributed proven EOSE. The public falsifier closes the first observation, reopens the same MaxAge query, and observes zero additional REQ for the covered source.

    This is a vocabulary and public-contract door. Auto-selection would defeat the repository's approval rule, so this checkpoint remains blocking in every execution mode.
  </context>
  <options>
    <option id="approve">
      <name>Approve exact packet</name>
      <pros>Plan 03 can implement the researched contract and falsifiers without another naming decision.</pros>
      <cons>Adds one public/cross-crate Fava term and an EventCache contract extension.</cons>
    </option>
    <option id="rename">
      <name>Rename and re-present</name>
      <pros>Preserves the architecture while replacing the proposed term with Pablo's preferred exact name.</pros>
      <cons>Plan 02 remains incomplete until architecture and registry use the replacement consistently and this checkpoint is presented again.</cons>
    </option>
    <option id="reject">
      <name>Reject architecture</name>
      <pros>Prevents an unwanted public concept from entering implementation.</pros>
      <cons>Plans 03-05 remain blocked; the ordinary-query requirement needs a different approved source-completion model.</cons>
    </option>
  </options>
  <resume-signal>Reply `approve`; or `rename: &lt;exact term&gt;` to keep Plan 02 open for revision and re-review; or `reject: &lt;reason&gt;` to stop the phase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay protocol evidence -> observation owner | An untrusted relay frame must be attributed to the exact accepted request before it can claim completion. |
| observation owner -> event-cache contract | A completion fact crosses into reusable retained state and can suppress later network acquisition. |
| cache retention -> later query open | Provider eviction, reset, and restart guarantees determine whether old evidence remains truthful. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.8-02-01 | Spoofing | EOSE attribution -> `SourceCoverage` | critical | mitigate | Production is allowed only after exact relay access, filter, interval, limit, current request, and `Proven` classification match; Plan 03 owns the falsifier. |
| T-07.8-02-02 | Tampering | EventCache invalidation | high | mitigate | Matching eviction/reset/restart lowers coverage; a named deliberate break in Plan 03 must make the lifecycle suite fail. |
| T-07.8-02-03 | Denial of Service | Retained coverage/filter keys | high | mitigate | Existing non-zero provider capacity bounds record count; over-bound keys refuse before mutation and create shortfall rather than partial state. |
| T-07.8-02-04 | Repudiation | Empty/failure distinction | high | mitigate | Only actual proven EOSE can support reusable empty; all failure causes remain distinct evidence and cannot become absence. |
| T-07.8-02-05 | Information Disclosure | Exact filter retained in memory provider | low | accept | The volatile cache already receives the query/filter to evaluate local events; this plan adds no new external disclosure surface and persistence remains profile-declared. |

No package installation or external dependency change occurs in this plan.
</threat_model>

<artifacts-this-phase-produces>
- A candidate authoritative MaxAge/source-coverage architecture with exact owners, bounds, coherence, and falsifiers.
- One complete candidate vocabulary entry for `SourceCoverage` plus the existing `Query` term's MaxAge extension.
- A Plan 02 summary recording vocabulary diagnostics and the explicit human decision. No production implementation.
</artifacts-this-phase-produces>

<acceptance_criteria>
- Only `docs/spec/ARCHITECTURE.md` and `docs/internals/vocabulary.toml` change before the checkpoint.
- The architecture states one open-time age evaluation, exact per-source equality, immediate local result, partial acquisition, proven-empty reuse, no polling, and exact invalidation/reset/restart behavior.
- The architecture assigns all six no-global-completeness prohibitions to QUERY-009 and exact current-request EOSE only to QUERY-010; their falsifiers are separate.
- The scope note excludes already-verified attribution, immutable Plan 01, expiry sweeping, and RELAY-004 without adding replacement work.
- The packet contains every vocabulary-approval field required by `AGENTS.md`, one explicit capacity rule, and one executable public falsifier.
- `python3 -m unittest tools.tests.test_vocabulary_check` passes.
- The repository-wide vocabulary checker result is captured before and after; unrelated diagnostics are byte-identical and are not suppressed or laundered.
- The final task is a `gate="blocking-human"` approve/rename/reject checkpoint.
- Plan 03 remains unexecutable unless `07.8-02-SUMMARY.md` records Pablo's approval under the exact final name.
</acceptance_criteria>

<verification>
<automated>python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; git diff --check -- docs/spec/ARCHITECTURE.md docs/internals/vocabulary.toml</automated>
</verification>

<success_criteria>
The architecture can be falsified before production code exists, the sole new term has a complete decision packet, and human approval—not plan ordering or auto-mode—controls whether implementation may begin.
</success_criteria>

<output>
Create `.planning/phases/07.8-independent-correctness-defects/07.8-02-SUMMARY.md` only after the checkpoint resolves `approve`. Record exact before/after vocabulary output hashes, changed-file scope, the approved spelling and fields, and the checkpoint response. On `rename` or `reject`, return `## CHECKPOINT REACHED` and do not start Plan 03.
</output>
