---
phase: 07.8-independent-correctness-defects
plan: 03
type: execute
wave: 3
depends_on: ["07.8-02"]
files_modified:
  - crates/fava-query/src/lib.rs
  - crates/fava-query/src/evidence.rs
  - crates/fava-event-cache/src/lib.rs
  - crates/fava-event-cache-memory/src/lib.rs
  - crates/fava-event-cache-memory/tests/cache_lifecycle.rs
  - crates/fava-observe/src/observer.rs
  - crates/fava-observe/src/completions.rs
  - crates/fava-observe/src/engine.rs
  - crates/fava-observe/src/slot.rs
  - crates/fava-observe/tests/open_sequence.rs
  - crates/fava-observe/tests/shared_work.rs
  - crates/fava/tests/max_age.rs
  - crates/fava/BUILD.bazel
  - crates/fava/tests/semantic_write_failures/faults.rs
autonomous: false
requirements: [QUERY-004, QUERY-009, QUERY-010, QUERY-013A, EVENT-005, EVENT-007]
estimate:
  tokens: 47000
  raw_tokens: 47000
  tasks: 3
  confidence: low
  basis: "maximum fresh-executor unit; three blocking resume segments total approximately 113000 tokens"
must_haves:
  truths:
    - "Plan 02's exact vocabulary packet is approved before Rust implementation; Plan 03 does not edit vocabulary.toml because Plan 02 registers the specified public symbol."
    - "A public MaxAge query returns its complete current local snapshot immediately and acquires only exact sources whose completion is missing, stale, or mismatched."
    - "Only actual attributed current-request EOSE classified Proven creates reusable completion, including exact empty completion; every other terminal or non-terminal cause remains distinct."
    - "QUERY-009 is proved separately: an indefinitely empty query and a source-scoped EOSE expose none of global synced, global complete, percentage completeness, authoritative empty, end of global history, or proof of no match elsewhere."
    - "Late attachment preserves the original proven EOSE Timestamp after a bounded wall-clock separation; attachment time cannot be substituted."
    - "Coverage is exact, bounded, replaceable-provider-visible, and coherent with eviction, reset, deletion, supersession, explicit expiry mutation, and volatile restart. No expiry sweep owner or cadence is introduced."
    - "Three bounded implementation units execute in fresh executor contexts through blocking GSD resume checkpoints and one durable execution handoff."
  artifacts:
    - "14 physical files maximum: focused owner homes plus new public capstone crates/fava/tests/max_age.rs and crates/fava/BUILD.bazel; no vocabulary edit"
    - "crates/fava-query — approved MaxAge/SourceCoverage value and exact public evidence"
    - "crates/fava-event-cache plus memory provider — required no-default coverage contract and bounded coherent implementation"
    - "crates/fava-observe — open-time selection and sole exact EOSE producer"
    - "crates/fava/tests/max_age.rs — public MaxAge identity, close/reopen, mismatch, no-polling, and no-global-completeness capstone"
    - "crates/fava/BUILD.bazel — public MaxAge capstone registration"
    - ".planning/phases/07.8-independent-correctness-defects/07.8-03-EXECUTION.md — append-only unit handoff"
  key_links:
    - "Query::max_age -> query identity -> one open Timestamp -> exact source coverage lookup -> per-source demand"
    - "current request identity + actual EOSE + Proven -> original completion Timestamp -> retained SourceCoverage"
    - "late join wire identity/generation -> replay original Timestamp, never attachment wall time"
    - "cache mutation -> coverage invalidation -> later exact reacquisition"
---

<objective>
Implement approved MaxAge behavior through public `fava`, neutral query/cache contracts, the default cache, and the observation owner, with causal RED evidence and exact source-scoped completion.

Purpose: suppress redundant acquisition without inventing global completeness, weakening provider replaceability, or turning age into a polling lifecycle.

Output: three fresh-context execution units, focused owner tests plus the public `max_age.rs` capstone registered in Bazel, normalized vocabulary delta proof, and named deliberate-break transcripts.
</objective>

<execution_context>
@/Users/pablofernandez/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.8-independent-correctness-defects/07.8-CONTEXT.md
@.planning/phases/07.8-independent-correctness-defects/RESEARCH.md
@.planning/phases/07.8-independent-correctness-defects/07.8-PATTERNS.md
@.planning/phases/07.8-independent-correctness-defects/07.8-VALIDATION.md
@.planning/phases/07.8-independent-correctness-defects/07.8-02-SUMMARY.md
@docs/spec/ARCHITECTURE.md
@docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
@docs/internals/vocabulary.toml
</context>

<scope_reconciliation>
Locked scope only: MaxAge/source coverage. Relay attribution is already verified; Plan 01 and its stronger completed summary remain byte-immutable; the production expiry sweep and RELAY-004 stay deferred. Explicit expiry mutation remains a cache-coherence case and must not become a sweep/cadence task. QUERY-009 owns exactly six forbidden global claims; QUERY-010 alone owns actual EOSE and fresh request identity.
</scope_reconciliation>

<fresh_context_protocol>
`workflow.human_verify_mode=end-of-phase` is intentionally overridden only for the two `gate="blocking-human"` resume checkpoints below. They are execution-integrity gates, not UAT. At each gate the current executor appends the exact unit record to `07.8-03-EXECUTION.md`, returns `## CHECKPOINT REACHED`, and stops. Execute-phase must discard that executor context and spawn a fresh gsd-executor for the next unit on the same issue, branch, worktree, and PLAN. A continuation first verifies HEAD, dirty-file hashes, previous unit files, commands, and remaining-work list from the handoff. No SUMMARY is created until Unit M3 is complete.
</fresh_context_protocol>

<prohibitions>
- No router/outbox/publication surface or Plan 04 work.
- No edit to `docs/internals/vocabulary.toml`; Plan 02 owns specified-symbol registration.
- No event timestamp, observation timestamp, open time, reopen time, or attachment time as completion time.
- No filter containment, cross-source reuse, inferred empty result, timer/polling loop, hidden flag, compatibility path, provider default method, or concrete-provider bypass.
- No production expiry sweep, cadence, RELAY-004/NIP-11 work, attribution repair, Plan 01 edit, or dirty/untracked approval-tooling edit.
</prohibitions>

<tasks>

<task type="tracer" tdd="true">
  <name>Unit M1: Freeze owner/public RED evidence in focused test homes</name>
  <precondition>`07.8-02-SUMMARY.md` records Pablo's unconditional approval of the exact MaxAge/SourceCoverage packet and specified Rust path.</precondition>
  <files>crates/fava-event-cache-memory/tests/cache_lifecycle.rs, crates/fava-observe/tests/open_sequence.rs, crates/fava-observe/tests/shared_work.rs, crates/fava/tests/max_age.rs, crates/fava/tests/semantic_write_failures/faults.rs, crates/fava/BUILD.bazel, .planning/phases/07.8-independent-correctness-defects/07.8-03-EXECUTION.md</files>
  <behavior>
    - Query identity distinguishes MaxAge durations and preserves equal-construction identity.
    - Cache contract tests cover exact positive/empty retention, every relay/access/filter/since/until/limit mismatch, over-bound refusal without mutation, eviction/reset/explicit-expiry/volatile-restart coherence, and the fault wrapper's exact delegation.
    - Observation tests cover immediate local state, mixed fresh/stale sources, every non-completion cause independently, and no work triggered by later age crossing.
    - Public tests cover positive and empty proven EOSE close/reopen with zero covered-source REQ and a six-item QUERY-009 no-global-completeness inventory.
    - Late join proves the pre-fix attachment Timestamp is strictly later than and unequal to the captured EOSE Timestamp, then preserves a durable desired assertion requiring exact equality to the original EOSE.
  </behavior>
  <action>
    Capture production hashes and a normalized vocabulary baseline before test edits. Normalization records checker exit status, removes absolute-worktree prefixes/trailing whitespace, and sorts diagnostics under `LC_ALL=C`; write the normalized bytes to `/tmp/fava-07.8-03-vocabulary-before.norm` and its hash to the handoff.

    Create `crates/fava/tests/max_age.rs` as the authoritative public MaxAge capstone and register it in `crates/fava/BUILD.bazel`. Put the new duration-sensitive Query identity case there with the public close/reopen, mismatch, no-polling, and six-prohibition cases; leave the generic `crates/fava-query/tests/query_identity.rs` and unrelated `crates/fava/tests/request_identity.rs` byte-untouched. Keep owner-only evidence in cache lifecycle, observation open/shared-work, and the cache fault wrapper. Use the approved API names directly. The cache fault wrapper must be forced to implement every required EventCache method with no default; its behavior test lives in `faults.rs`, delegates a retained positive and empty fact, preserves an exact mismatch miss/error, and remains orthogonal to its injected close behavior.

    For late join, capture the actual EOSE `Timestamp`, then use `tokio::time::timeout(Duration::from_secs(2), async { loop { if Timestamp::now() > captured_eose { break; } tokio::time::sleep(Duration::from_millis(5)).await; } })` before attachment. First run a temporary characterization assertion proving the pre-fix replay is unequal to and strictly later than `captured_eose`; record both exact values. Replace it with the durable assertion `replayed_eose == captured_eose`, which must fail before the fix with the differing attachment-time value. The final test also proves no coverage is minted for a non-identical attached filter.

    Give limited EOSE, silence, timeout, refusal, authentication requirement, CLOSED, disconnect, unreachable, cancellation, and observation close independently selectable test names. The public QUERY-009 test enumerates all six prohibited claims and proves public snapshot/evidence/diagnostic fields remain exact and source-scoped both while empty work waits and after one source EOSE; this test does not treat EOSE as QUERY-009 coverage.

    Record every focused command, causal failure, exact pre-fix late-join timestamps, unchanged production hashes, the exact 14-file physical inventory including `max_age.rs` and `BUILD.bazel`, and normalized vocabulary baseline in `07.8-03-EXECUTION.md`. Stop with tests RED; do not implement production.
  </action>
  <verify>
    <automated>test -s /tmp/fava-07.8-03-vocabulary-before.norm &amp;&amp; rg -n 'max_age' crates/fava/BUILD.bazel &amp;&amp; ! cargo test -p fava --test max_age max_age_query_identity -- --exact &amp;&amp; ! cargo test -p fava-event-cache-memory --test cache_lifecycle source_coverage_contract -- --exact &amp;&amp; ! cargo test -p fava-observe --test shared_work late_joiner_preserves_original_completion_time -- --exact &amp;&amp; ! cargo test -p fava --test max_age max_age_close_reopen_uses_exact_completion -- --exact</automated>
  </verify>
  <done>All named protections exist in focused owner homes and the Bazel-registered public `max_age.rs` capstone; late-join RED records a strictly later fabricated attachment time; production hashes are unchanged; the handoff is complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Resume M2 in a fresh executor context</name>
  <what-built>Unit M1's causal RED matrix and durable handoff at `.planning/phases/07.8-independent-correctness-defects/07.8-03-EXECUTION.md`.</what-built>
  <how-to-verify>Confirm the M1 record names the exact HEAD, seven changed/handoff files, production hashes, normalized vocabulary baseline, every RED command, and both pre-fix late-join timestamps. Execute-phase must return from the current executor and spawn a fresh executor for Unit M2; continuing in the same executor is a failed gate.</how-to-verify>
  <resume-signal>Reply `resume 07.8-03 M2` to dispatch the fresh continuation.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Unit M2: GREEN the neutral query/cache contract and default provider</name>
  <precondition>The fresh executor verifies the M1 handoff, unchanged production baseline, approved Plan 02 symbol, and causal RED selections before editing.</precondition>
  <files>crates/fava-query/src/lib.rs, crates/fava-query/src/evidence.rs, crates/fava-event-cache/src/lib.rs, crates/fava-event-cache-memory/src/lib.rs, crates/fava-event-cache-memory/tests/cache_lifecycle.rs, crates/fava/tests/semantic_write_failures/faults.rs, .planning/phases/07.8-independent-correctness-defects/07.8-03-EXECUTION.md</files>
  <behavior>
    - MaxAge is part of exact Query identity and SourceCoverage carries the approved exact immutable fields.
    - EventCache lookup/retention methods are required with no default and use existing errors; MemoryEventCache is bounded and atomically coherent.
    - Applied source coverage remains source-scoped QueryEvidence and creates none of QUERY-009's global claims.
    - Matching eviction/reset/explicit-expiry/volatile restart invalidates as approved; no sweep lifecycle is added.
  </behavior>
  <action>
    Implement only the approved Plan 02 surface. Extend the neutral EventCache contract, not the memory provider privately. Both known implementors must explicitly implement every method: MemoryEventCache owns real state; ClosingEventCache delegates exact values and errors. Use existing error/evidence/shortfall families and no default body, fallback miss, synonym, wrapper, or vocabulary edit.

    Keep coverage and events in one bounded atomic state transition. Exact equality includes session/access and the complete nostr Filter, including authors/tags, interval, and limit. Refuse an over-bound key before mutation. Deterministically evict the oldest retained completion under the approved capacity. Matching event removal invalidates coverage in the same transition; reset clears it; a new volatile provider starts empty. An explicit expiry mutation participates in coherence but this unit adds no clock owner, sweep, or cadence.

    Make query/cache/fault-wrapper tests green, run strict focused clippy, and append the unit's starting/ending HEAD, exact files, commits, commands, remaining RED targets, physical-file count, and dirty-file preservation hashes to the handoff. Do not touch observation production.
  </action>
  <verify>
    <automated>cargo test -p fava-query -p fava-event-cache -p fava-event-cache-memory --no-fail-fast &amp;&amp; cargo test -p fava --test semantic_write_failures faults::closing_event_cache_forwards_exact_source_coverage -- --exact &amp;&amp; cargo clippy -p fava-query -p fava-event-cache -p fava-event-cache-memory -p fava --all-targets -- -D warnings &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check</automated>
  </verify>
  <done>The approved replaceable cache surface and default are green with exact bounds/coherence and explicit implementors; observe/public tests remain causally RED; the M2 handoff is complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Resume M3 in a fresh executor context</name>
  <what-built>Unit M2's green neutral contract/default and updated durable handoff.</what-built>
  <how-to-verify>Confirm the M2 handoff records its focused GREEN commands, exact required trait methods with no defaults, normalized vocabulary-unit result, remaining observation/public RED list, and preserved dirty-file hashes. Execute-phase must stop this executor and spawn a fresh executor for Unit M3.</how-to-verify>
  <resume-signal>Reply `resume 07.8-03 M3` to dispatch the fresh continuation.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Unit M3: GREEN exact observation ownership, public capstones, and deliberate breaks</name>
  <precondition>The fresh executor verifies the complete M1/M2 handoff and reruns the remaining observation/public RED selectors before production edits.</precondition>
  <files>crates/fava-observe/src/observer.rs, crates/fava-observe/src/completions.rs, crates/fava-observe/src/engine.rs, crates/fava-observe/src/slot.rs, crates/fava-observe/tests/open_sequence.rs, crates/fava-observe/tests/shared_work.rs, crates/fava/tests/max_age.rs, .planning/phases/07.8-independent-correctness-defects/07.8-03-EXECUTION.md</files>
  <behavior>
    - One captured open Timestamp partitions exact covered/uncovered sources; initial local snapshot precedes remote completion.
    - Only current attributed Proven EOSE stores its actual Timestamp; non-proven/failure causes remain exact evidence/shortfalls.
    - Slot replay is wire-identity and operation-generation scoped; late attachment receives the original EOSE Timestamp.
    - Finite MaxAge demand releases after completion, never restarts on age crossing, and local replacements continue.
    - Public close/reopen emits zero REQ only for exact fresh sources, including proven empty; the six QUERY-009 prohibitions remain absent.
  </behavior>
  <action>
    Implement the approved open-time flow in fava-observe. Capture one Timestamp for the open decision, evaluate exact coverage per session/filter, publish complete local truth without waiting, and retain demand only for uncovered sources. At actual current-request EOSE, retain only Proven completion with the frame's original Timestamp. Replace boolean-only slot replay with private exact wire/generation state; clear it on generation change and discard every stale completion.

    The late-join test must use the bounded strict-greater-than wait authored in M1. Preserve the original EOSE value exactly after attachment; never call `Timestamp::now()` to synthesize replay or retain coverage for another filter. Coverage-retention refusal remains a scoped shortfall and future reopen reacquires.

    Make every independent negative-cause selector, mixed-source case, late join, no-polling/local-replacement case, public close/reopen case, and QUERY-009 six-prohibition case green. Apply and restore four breaks independently: wrong exact key, matching eviction retains proof, non-proven EOSE retains proof, and finite MaxAge retains/restarts remote demand. Record exact failing subsets, unrelated green guards, diffs, and restoration hashes.

    Finish with focused and workspace tests, strict clippy, formatting, vocabulary unit tests, and normalized before/after repository checker comparison. Append final hashes, line counts, cohesion reasons for touched code over 500 lines, and complete invariant status to the handoff, then create the plan summary.
  </action>
  <verify>
    <automated>cargo test -p fava-observe --test open_sequence --no-fail-fast &amp;&amp; cargo test -p fava-observe --test shared_work --no-fail-fast &amp;&amp; cargo test -p fava --test max_age --no-fail-fast &amp;&amp; cargo test --workspace --all-targets --all-features --no-fail-fast &amp;&amp; cargo clippy --workspace --all-targets --all-features -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check</automated>
  </verify>
  <done>All owner/public RED cases are green; late join preserves the original timestamp after a bounded separation; QUERY-009 and QUERY-010 proofs remain distinct; four breaks discriminate and restore; final handoff is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay frame -> observation | Untrusted EOSE/failure must match current exact request identity before reuse. |
| observation -> cache | Exact completion can suppress later work and must remain bounded/coherent. |
| clock -> open decision | Time selects acquisition once and must not create a lifecycle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.8-03-01 | Spoofing | EOSE producer | critical | mitigate | Exact request/generation attribution, Proven-only retention, independent negative tests. |
| T-07.8-03-02 | Tampering | cache coherence | high | mitigate | Atomic invalidation and eviction/reset/restart break. |
| T-07.8-03-03 | Repudiation | empty/global claims | critical | mitigate | Separate QUERY-009 six-prohibition proof and QUERY-010 exact EOSE proof. |
| T-07.8-03-04 | Denial of Service | retained filters/results | high | mitigate | Existing bounds, typed refusal, finite demand, no timer. |
| T-07.8-03-05 | Elevation of Privilege | access mismatch | high | mitigate | Exact RelaySessionKey and mismatch matrix. |
</threat_model>

<acceptance_criteria>
- Exactly 14 physical files are in scope (13 Rust plus `crates/fava/BUILD.bazel`), below the 15-file ceiling. New MaxAge identity/public evidence is consolidated in `crates/fava/tests/max_age.rs`; generic `query_identity.rs` and unrelated `request_identity.rs` remain untouched; Plan 03 does not edit vocabulary.toml.
- M1, M2, and M3 each stay below the 47,000-token maximum fresh-unit estimate and are separated by real blocking resume checkpoints plus the durable execution handoff.
- The no-global-completeness proof enumerates all six QUERY-009 prohibitions; exact EOSE/reopen identity is attributed only to QUERY-010.
- Late join waits boundedly until Timestamp::now() is strictly greater than captured EOSE, records pre-fix inequality, and ends with exact original-time equality.
- Every EventCache implementor supplies required methods explicitly; exact lookup/retention and atomic invalidation are provider-replaceable and bounded.
- Fresh/stale/mismatch/negative, no polling, local replacement, close/reopen, and four deliberate breaks pass through owner and public paths.
- The production expiry sweep, RELAY-004, attribution repair, Plan 01, and dirty/untracked approval tooling remain untouched.
- `python3 tools/check_vocabulary.py` normalized before/after bytes and exit status match; vocabulary unit tests pass.
</acceptance_criteria>

<verification>
<automated>cargo test -p fava --test max_age --no-fail-fast &amp;&amp; rg -n 'max_age' crates/fava/BUILD.bazel &amp;&amp; cargo test --workspace --all-targets --all-features --no-fail-fast &amp;&amp; cargo clippy --workspace --all-targets --all-features -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; python3 tools/check_vocabulary.py &gt;/tmp/fava-07.8-03-vocabulary-after.raw 2&gt;&amp;1; vocab_after_status=$?; { printf 'status=%s\n' "$vocab_after_status"; sed -E 's#(^|[[:space:]])/Users/[^[:space:]]+#\1&lt;ABS_PATH&gt;#g; s/[[:space:]]+$//' /tmp/fava-07.8-03-vocabulary-after.raw | LC_ALL=C sort; } &gt;/tmp/fava-07.8-03-vocabulary-after.norm; cmp /tmp/fava-07.8-03-vocabulary-before.norm /tmp/fava-07.8-03-vocabulary-after.norm</automated>
</verification>

<success_criteria>
Approved MaxAge behavior works through public fava with exact source-scoped completion, no global claims, no polling, original-time late replay, bounded coherent cache state, and three independently resumed fresh executor units.
</success_criteria>

<output>
Create `07.8-03-SUMMARY.md` only after M3. Record Plan 02 approval, all RED/GREEN commands, strict-greater late-join timestamps, six-prohibition inventory, independent negative cases, four break/restoration transcripts, M1-M3 checkpoint/handoff evidence, normalized vocabulary hashes/status, exact 14-file inventory, `max_age.rs` creation plus Bazel registration, untouched generic test-home hashes, line counts, dirty-file preservation hashes, and commits. Plan 04 remains blocked until this evidence is complete.
</output>
