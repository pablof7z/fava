---
phase: 06.1-literal-tag-value-query-semantics-remediation
plan: 02
subsystem: subscriptions
tags: [rust, nostr, custom-tags, subscription-grouping, attribution]

requires:
  - phase: 06.1-01
    provides: canonical case-sensitive literal tag axes in Query identity and local evaluation
provides:
  - exact Query-to-NIP-01 custom-tag translation including present-empty axes
  - one-axis case-sensitive tag grouping with exact logical attribution
  - 300-logical-demand to one-wire-filter planner evidence
affects: [06.1-03, relay-demand, subscription-grouping, controlled-relay-canary]

actuals:
  tokens: 3670
  tasks: 2
  commits: 4

tech-stack:
  added: []
  patterns:
    - upstream Filter custom-tag translation for every canonical Query axis
    - immutable merge candidates that union exactly one proven-compatible axis

key-files:
  created:
    - crates/fava-subscriptions/tests/tag_values.rs
  modified:
    - crates/fava-subscriptions/src/lib.rs
    - crates/fava-subscriptions/BUILD.bazel
    - crates/fava-subscriptions-standard/src/lib.rs
    - crates/fava-subscriptions-standard/tests/grouping.rs

key-decisions:
  - "Translate every canonical Query tag entry through nostr::Filter::custom_tags, including empty value sets."
  - "Compute an exact merged Filter without mutating a group; accept equality, one non-empty author axis, or one identical non-empty tag axis only."
  - "Require identical tag-key sets and every other Filter field to match before unioning one case-sensitive tag value set."

patterns-established:
  - "Wire translation: preserve exact key case, UTF-8 values, canonical ordering, and present-empty axes through the upstream Nostr filter."
  - "Planner grouping: return a merge candidate or refuse; never partially mutate a group while deciding compatibility."

requirements-completed: [ROUTE-10]

coverage:
  - id: D1
    description: "Exact lowercase and uppercase custom-tag keys, UTF-8 values, canonical value order, and present-empty axes survive Query-to-REQ translation."
    requirement: ROUTE-10
    verification:
      - kind: integration
        ref: "crates/fava-subscriptions/tests/tag_values.rs"
        status: pass
      - kind: other
        ref: "bazel test //crates/fava-subscriptions:tag_values"
        status: pass
    human_judgment: false
  - id: D2
    description: "One exact non-empty tag axis groups 300 logical demands into one accepted wire filter while retaining all logical IDs."
    requirement: ROUTE-10
    verification:
      - kind: integration
        ref: "crates/fava-subscriptions-standard/tests/grouping.rs#one_exact_non_empty_tag_axis_groups_with_exact_values_and_logical_ids"
        status: pass
      - kind: other
        ref: "bazel test //crates/fava-subscriptions-standard:grouping"
        status: pass
    human_judgment: false
  - id: D3
    description: "Unsafe limit, multi-axis, opposite-case, empty-axis, and unrelated-field candidates remain separate while existing author grouping and typed bounds remain exact."
    requirement: ROUTE-10
    verification:
      - kind: integration
        ref: "crates/fava-subscriptions-standard/tests/grouping.rs#unsafe_multi_axis_limit_case_and_empty_axis_candidates_remain_separate"
        status: pass
      - kind: integration
        ref: "crates/fava-subscriptions-standard/tests/grouping.rs#empty_duplicate_and_frame_bounds_return_exact_refusals"
        status: pass
    human_judgment: false

duration: 9min
completed: 2026-08-21
status: complete
---

# Phase 06.1 Plan 02: Exact Tag Relay Demand and Grouping Summary

**Exact case-sensitive literal tag axes now reach NIP-01 REQ bytes and group 300-to-1 only when one non-empty axis is provably compatible, with every logical ID retained.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-08-21T18:48:48Z
- **Completed:** 2026-08-21T18:58:39Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added exact Query-to-wire custom-tag translation through the existing `demand_for_query` seam, preserving `#e`/`#E`, UTF-8, canonical values, and present-empty arrays.
- Replaced boolean compatibility plus assumed mutation with an exact merge-candidate operation that accepts only equality, one author axis, or one case-sensitive non-empty tag axis.
- Proved 300 logical tag-value demands produce one wire message and retain all 300 logical subscription IDs, while unsafe candidates and existing typed bounds remain exact.

## Task Commits

1. **Task 1 RED: failing custom-tag wire evidence** — `85752eb` (`test`)
2. **Task 1 GREEN: exact tag axes in relay demand** — `ecac1d5` (`feat`)
3. **Task 2 RED: failing 300-query tag grouping evidence** — `2c26ac5` (`test`)
4. **Task 2 GREEN: one-axis exact tag grouping** — `b2eebbd` (`feat`)

## Files Created/Modified

- `crates/fava-subscriptions/tests/tag_values.rs` — Byte-exact Cargo/Bazel REQ evidence for case, UTF-8, canonical values, and present-empty axes.
- `crates/fava-subscriptions/src/lib.rs` — Translates every canonical Query tag axis through `Filter::custom_tags`.
- `crates/fava-subscriptions/BUILD.bazel` — Mirrors the wire-test pattern for the new contract test.
- `crates/fava-subscriptions-standard/src/lib.rs` — Computes exact immutable merge candidates for equality, authors, or one tag axis.
- `crates/fava-subscriptions-standard/tests/grouping.rs` — Proves 300-to-1 grouping, complete logical attribution, refusal cases, and typed bounds.

## Red/Green Evidence

### Task 1 Query-to-wire boundary

- RED after `85752eb`: `cargo test -p fava-subscriptions --test tag_values` exited 101; all 3 tests received `{}` filters instead of exact `#e`, `#E`, UTF-8, and empty-array axes.
- GREEN after `ecac1d5`: the same Cargo target passed 3/3 tests and `bazel test //crates/fava-subscriptions:tag_values` passed 1/1 target.

### Task 2 planner boundary

- RED after `2c26ac5`: `cargo test -p fava-subscriptions-standard --test grouping` exited 101 with 5/6 tests passing; the 300-query case returned `TooManySubscriptions { required: 300, maximum: 64 }` because tag filters did not group.
- GREEN after `b2eebbd`: the same Cargo target passed 6/6 tests and `bazel test //crates/fava-subscriptions-standard:grouping` passed 1/1 target.

## Verification

- `cargo test -p fava-subscriptions --test tag_values` — 3/3 passed.
- `cargo test -p fava-subscriptions-standard --test grouping` — 6/6 passed.
- `bazel test //crates/fava-subscriptions:tag_values //crates/fava-subscriptions-standard:grouping` — 2/2 targets passed.
- `cargo test -p fava-query -p fava-query-standard -p fava-subscriptions -p fava-subscriptions-standard -p fava-ingest -p fava --tests` — passed.
- `cargo clippy -p fava-subscriptions -p fava-subscriptions-standard --tests -- -D warnings` — passed.
- `cargo fmt --all -- --check` — passed.
- `python3 -m unittest tools/tests/test_vocabulary_check.py` — 4/4 passed.
- `git diff -- Cargo.toml Cargo.lock` — empty; no dependency or lockfile changes.
- Code-size gate — all changed code files remain below 500 lines; the largest is `grouping.rs` at 250 lines.

## Decisions Made

- Preserved `demand_for_query` as the sole Query-to-relay conversion seam and used the pinned upstream custom-tag representation directly.
- Made merge compatibility constructive: the helper returns the exact merged filter or refuses, so failed candidates cannot partially mutate grouping state.
- Preserved equality deduplication before the limit refusal; unequal limited filters remain separate while identical filters retain existing deduplication semantics.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added the direct Bazel dependency required by the public Query test**
- **Found during:** Task 1 GREEN verification
- **Issue:** The new integration test imports `fava_query::{Query, SingleLetterTag}` directly, but Rust Bazel dependencies are non-transitive and the planned `:lib` plus `fava-wire` dependencies did not expose `fava_query` to the test target.
- **Fix:** Added `//crates/fava-query:lib` to the test target without changing Cargo manifests or dependency versions.
- **Files modified:** `crates/fava-subscriptions/BUILD.bazel`
- **Verification:** Both the Cargo and Bazel tag-value targets pass.
- **Committed in:** `ecac1d5`

---

**Total deviations:** 1 auto-fixed (1 blocking issue).
**Impact on plan:** Required only for the existing first-party Bazel graph; no package, manifest, lockfile, vocabulary, or runtime behavior changed.

## Issues Encountered

- The initial linked-worktree branch did not satisfy the mandatory executor commit guard. The orchestrator rebound the same worktree to `agent-06.1-02`; the uncommitted RED evidence was preserved and no task commit landed on the original branch.
- One source edit initially resolved against the main checkout rather than the assigned worktree. The exact three-line edit was removed immediately, the main checkout returned to its prior state, and the change was reapplied and verified only in this worktree.
- `python3 tools/check_vocabulary.py` still reports the pre-existing `/tmp/fava-tag-values-06-1` planning example as an undocumented crate name. This known false positive predates Plan 02, is recorded in `deferred-items.md`, and remains assigned to Plan 03; the checker unit suite passes.

## Deferred Issues

- Plan 03 must correct the pre-existing vocabulary-path false positive before its full phase gate.

## User Setup Required

None.

## Next Phase Readiness

- Plan 03 can use exact 300-to-1 planner demand for the controlled-relay differential proof and named case-folding deliberate break.
- Query-to-wire and planner owner evidence is causally red-before-code and green through Cargo and Bazel.

## Self-Check: PASSED

- All five implementation files and this summary exist.
- All four Plan 02 task commits are present in repository history.
- The complete scoped Cargo regression suite, focused Bazel targets, strict focused Clippy, and formatting gate pass.

---
*Phase: 06.1-literal-tag-value-query-semantics-remediation*
*Completed: 2026-08-21*
