---
phase: 06.1-literal-tag-value-query-semantics-remediation
plan: 01
subsystem: query
tags: [rust, nostr, SingleLetterTag, query-identity, local-evaluation]

requires:
  - phase: 06-local-semantic-state
    provides: coherent cache and write-store query sources through the public Fava facade
provides:
  - canonical case-sensitive literal tag-value axes in Query identity
  - exact signed-cache and unsigned-write-store tag evaluation
  - upstream SingleLetterTag exposed through the thin fava facade
affects: [06.1-02, 06.1-03, relay-demand, subscription-grouping]

actuals:
  tokens: 6421
  tasks: 3
  commits: 5

tech-stack:
  added: []
  patterns:
    - canonical BTreeMap and BTreeSet query axes
    - direct exact tag cell-zero and cell-one evaluation

key-files:
  created:
    - crates/fava-query/src/selection.rs
  modified:
    - crates/fava-query/src/lib.rs
    - crates/fava-query/tests/query_identity.rs
    - crates/fava-query-standard/src/lib.rs
    - crates/fava-query-standard/tests/source_merge.rs
    - crates/fava/src/lib.rs
    - crates/fava/tests/local_source_merge.rs

key-decisions:
  - "Use nostr::filter::SingleLetterTag directly; no Fava wrapper or compatibility alias."
  - "Represent literal tag constraints as exact case-sensitive keys with canonical value sets; a present empty set matches nothing."
  - "Evaluate exact tag cells locally without delegating Fava query meaning to the upstream whole-filter matcher."

patterns-established:
  - "Literal tag identity: repeated exact keys union values while construction order and duplicates are identity-inert."
  - "Source evaluation: OR values within a key, AND every key with existing literal axes, and ignore cells after value cell one."

requirements-completed: [LOCAL-09]

coverage:
  - id: D1
    description: "Applications construct canonical case-distinct literal tag constraints for all 52 ASCII letter keys."
    requirement: LOCAL-09
    verification:
      - kind: unit
        ref: "crates/fava-query/tests/query_identity.rs#all_ascii_letter_tag_axes_are_case_sensitive"
        status: pass
      - kind: unit
        ref: "crates/fava-query/tests/query_identity.rs#literal_tag_values_have_canonical_query_identity"
        status: pass
    human_judgment: false
  - id: D2
    description: "Signed cache and unsigned write-store events obey exact UTF-8, case, OR, AND, empty-axis, and later-cell semantics."
    requirement: LOCAL-09
    verification:
      - kind: integration
        ref: "crates/fava-query-standard/tests/source_merge.rs#literal_tag_selection_matches_exact_signed_and_unsigned_cells"
        status: pass
      - kind: integration
        ref: "crates/fava/tests/local_source_merge.rs#literal_tag_selection_preserves_exact_sources_through_public_observation"
        status: pass
    human_judgment: false
  - id: D3
    description: "The thin fava facade exposes the exact upstream SingleLetterTag type used by Query."
    requirement: LOCAL-09
    verification:
      - kind: integration
        ref: "cargo test -p fava --test local_source_merge"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-08-21
status: complete
---

# Phase 06.1 Plan 01: Literal Tag-Value Query Semantics Summary

**Canonical case-sensitive Nostr tag axes now flow through Query identity, exact local evaluation, and public Fava observation with source-specific evidence.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-08-21T18:30:33Z
- **Completed:** 2026-08-21T18:44:57Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments

- Added canonical `SingleLetterTag` to exact-value-set selection with all 52 ASCII keys, case distinction, duplicate/order invariance, repeated-key union, and present-empty identity.
- Applied exact UTF-8 cell-one matching to signed cached and unsigned local event materializations with OR within a key and AND across keys and existing axes.
- Proved the behavior through real memory providers and `Fava::observe`, including exact relay/publication evidence and cache-independent local state.
- Re-exported the same upstream `SingleLetterTag` through `fava` without adding vocabulary or a second nominal type.

## Task Commits

1. **Task 1 RED: failing literal tag owner evidence** — `cc8c63b` (`test`)
2. **Task 1 GREEN: canonical selection and exact evaluator** — `2ff9b9e` (`feat`)
3. **Task 2: public Fava observation capstone** — `566f98a` (`test`)
4. **Task 3 RED: require facade import** — `8ea2447` (`test`)
5. **Task 3 GREEN: expose upstream key through facade** — `22dc7fe` (`feat`)

## Files Created/Modified

- `crates/fava-query/src/selection.rs` — Owns canonical literal selection state and builders.
- `crates/fava-query/src/lib.rs` — Re-exports selection contracts while keeping the crate root below 500 lines.
- `crates/fava-query/tests/query_identity.rs` — Proves 52-key case distinction and canonical semantic identity.
- `crates/fava-query-standard/src/lib.rs` — Adds exact tag matching as one selection conjunct.
- `crates/fava-query-standard/tests/source_merge.rs` — Proves signed/unsigned exact semantics at the evaluator owner.
- `crates/fava/src/lib.rs` — Re-exports the upstream tag-key type through the facade.
- `crates/fava/tests/local_source_merge.rs` — Proves exact results and evidence through public observation.

## Red/Green Evidence

### Task 1 owner boundary

- RED: `cargo test -p fava-query --test query_identity` exited 101 because `fava_query::SingleLetterTag` and `Query::tag_values` were unresolved.
- RED: `cargo test -p fava-query-standard --test source_merge` exited 101 for the same missing public tag query surface.
- GREEN after `cc8c63b` then `2ff9b9e`: query identity passed 5 tests; standard evaluator passed 7 tests.

### Task 3 facade boundary

- RED after `8ea2447`: `cargo test -p fava --test local_source_merge` exited 101 with `E0432`, unresolved import `fava::SingleLetterTag`.
- GREEN after `22dc7fe`: the public facade suite passed 7 tests.

## Verification

- `cargo test -p fava-query -p fava-query-standard -p fava --tests` — passed.
- `bazel test //crates/fava-query/... //crates/fava-query-standard/... //crates/fava:local_source_merge` — 3/3 tests passed.
- `cargo fmt --all -- --check` — passed.
- `python3 -m unittest tools/tests/test_vocabulary_check.py` — 4/4 tests passed.
- Code-size gate — every changed code file remains below 500 lines; `crates/fava-query/src/lib.rs` is 459 lines and no changed code file exceeds 800 lines.

## Decisions Made

- Preserved `fava-query` as the sole owner of literal tag query meaning and re-exported its upstream protocol type through the facade.
- Used exact ordinary equality for key/value matching; no normalization, parsing wrapper, compatibility path, or provider-owned semantic shortcut was added.

## Deviations from Plan

None in implementation. The pre-existing verification exception below was kept outside this plan's code scope.

## Issues Encountered

- The strict vocabulary command reports `fava-tag-values-06-1` from the pre-existing `/tmp/fava-tag-values-06-1` planning example as an undocumented crate name. The finding predates these code changes, is already recorded in `deferred-items.md`, and the checker unit suite passes. The unrelated planning artifact was not changed in Plan 01.

## Deferred Issues

- Resolve the vocabulary checker's false positive for the Plan 03 run-directory example before Plan 03's full vocabulary gate.

## User Setup Required

None.

## Next Phase Readiness

- Plan 02 can translate the canonical exact tag axes into NIP-01 relay demand and grouping.
- Plan 03 remains responsible for the 300-query controlled-relay differential proof, named deliberate break, focused issue evidence, and the deferred vocabulary-path correction.

## Self-Check: PASSED

- All seven implementation files and this summary exist.
- All five task commits are present in repository history.

---
*Phase: 06.1-literal-tag-value-query-semantics-remediation*
*Completed: 2026-08-21*
