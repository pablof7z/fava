---
phase: 06.1-literal-tag-value-query-semantics-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/fava-query/src/selection.rs
  - crates/fava-query/src/lib.rs
  - crates/fava-query/tests/query_identity.rs
  - crates/fava-query-standard/src/lib.rs
  - crates/fava-query-standard/tests/source_merge.rs
  - crates/fava/src/lib.rs
  - crates/fava/tests/local_source_merge.rs
autonomous: true
requirements:
  - LOCAL-09
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-02: Applications construct literal tag constraints with Nostr SingleLetterTag for all 52 ASCII letters, and uppercase and lowercase keys remain distinct."
    - "D-03/D-04: Query identity is independent of construction order and duplicates; repeated exact keys union values; absent and present-empty tag axes remain distinct."
    - "D-05/D-06/D-07/D-08: Local signed and unsigned event results use exact UTF-8 cell-one values, OR values within a key, AND every key with ids/authors/kinds, and ignore later tag cells."
    - "D-13/D-14/D-15: Owner and public-facade evidence covers the complete literal-tag corpus, is observed red before production changes, and is suitable for the phase mutation."
  artifacts:
    - path: "crates/fava-query/src/selection.rs"
      provides: "Canonical SingleLetterTag-to-exact-value-set selection state and Query builder implementation"
    - path: "crates/fava-query/tests/query_identity.rs"
      provides: "52-key case and canonical semantic-identity evidence"
    - path: "crates/fava-query-standard/src/lib.rs"
      provides: "Exact local tag-cell predicate in the standard semantic oracle"
    - path: "crates/fava-query-standard/tests/source_merge.rs"
      provides: "Signed/unsigned owner-level exact matching corpus"
    - path: "crates/fava/tests/local_source_merge.rs"
      provides: "Public Fava observation capstone for literal tag selection and evidence"
  key_links:
    - from: "crates/fava-query/src/lib.rs"
      to: "crates/fava-query/src/selection.rs"
      via: "private module plus public re-export and Query builder implementation"
      pattern: "mod selection|pub use selection"
    - from: "crates/fava-query-standard/src/lib.rs"
      to: "crates/fava-query/src/selection.rs"
      via: "matches_selection consumes the canonical tag map without owning query meaning"
      pattern: "matches_selection|tag_values"
    - from: "crates/fava/tests/local_source_merge.rs"
      to: "crates/fava-query-standard/src/lib.rs"
      via: "Fava::observe uses StandardQueryEvaluator over real cache and write-store sources"
      pattern: "Fava::builder|StandardQueryEvaluator|observe"
---

<objective>
Restore literal tag-value selection through the public Rust query and local-observation path for LOCAL-09.

Purpose: Make exact tag selection a canonical part of Query identity and ensure both cached signed events and independent write-store revisions obey the same semantics.
Output: Public query contract, standard local evaluator support, owner evidence, and a public Fava capstone.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-CONTEXT.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-RESEARCH.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-PATTERNS.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-VALIDATION.md
@docs/issues/0018-literal-tag-value-filters.md
@docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md
@docs/spec/ARCHITECTURE.md
@docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
@docs/spec/partial-spec-api-semantics.md
@crates/fava-query/src/lib.rs
@crates/fava-query-standard/src/lib.rs
@crates/fava-query/tests/query_identity.rs
@crates/fava-query-standard/tests/source_merge.rs
@crates/fava/tests/local_source_merge.rs

<interfaces>
Existing contracts to preserve:

- `FilterSelection` derives `Clone, Debug, Default, Eq, Hash, PartialEq`; ids/authors/kinds use optional ordered sets where absent is unconstrained and present-empty matches nothing.
- `Query::events()` constructs the declarative request; consuming builders mutate canonical selection and return `Self`; `Query` equality/hash includes selection, source policy, relay access, freshness, order, and limit.
- `fn matches_selection(selection: &FilterSelection, record: &EventRecord) -> bool` is the standard evaluator's semantic predicate.
- `Fava::builder().event_cache(...).write_store(...).query_evaluator(...).build()` and `Fava::observe(Query)` are the public capstone path.
- `nostr::filter::SingleLetterTag`, `Tag::single_letter_tag()`, and `Tag::content()` are the pinned protocol primitives; no local key parser or nominal wrapper is permitted.
</interfaces>
</context>

## Source Audit

CONTEXT decision labels below are plan-local trace labels assigned in source order because `06.1-CONTEXT.md` does not number its locked bullets.

| Source | ID | Required item | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Exact case-sensitive one-letter tag selection plus meaning-preserving relay grouping | 01-03 | COVERED |
| REQ | LOCAL-09 | Stable semantic identity for equivalent query descriptions | 01, 03 | COVERED |
| REQ | ROUTE-10 | Grouping changes wire shape without changing meaning, evidence, access isolation, or cancellation | 02, 03 | COVERED |
| RESEARCH | R-01 | Canonical ordered tag map and public builder in fava-query | 01 | COVERED |
| RESEARCH | R-02 | Direct exact local matching over tag cells zero and one | 01 | COVERED |
| RESEARCH | R-03 | Exact Query-to-NIP-01 custom-tag translation | 02 | COVERED |
| RESEARCH | R-04 | Single-axis merge candidate with exact attribution and existing refusals | 02 | COVERED |
| RESEARCH | R-05 | 300-query controlled-relay grouped/no-grouping differential proof | 03 | COVERED |
| RESEARCH | R-06 | Red-first owner/public evidence, causal mutation, full Cargo/Bazel/vocabulary gates | 01-03 | COVERED |
| RESEARCH | R-07 | No dependency installation; existing pinned nostr primitives only | 01-03 | COVERED |
| CONTEXT | D-01 | Use upstream SingleLetterTag; introduce no Fava tag-key wrapper or vocabulary | 01 | COVERED |
| CONTEXT | D-02 | Accept all 52 ASCII letter keys and keep uppercase/lowercase distinct | 01 | COVERED |
| CONTEXT | D-03 | Canonical exact key/value storage makes order and duplicates identity-inert | 01 | COVERED |
| CONTEXT | D-04 | Repeated keys union; present-empty matches nothing; absent is unconstrained | 01, 02 | COVERED |
| CONTEXT | D-05 | Values within one key are OR alternatives | 01 | COVERED |
| CONTEXT | D-06 | Different keys AND with each other and ids/authors/kinds | 01 | COVERED |
| CONTEXT | D-07 | Match only exact tag cells zero and one | 01 | COVERED |
| CONTEXT | D-08 | Preserve exact value bytes and case | 01-03 | COVERED |
| CONTEXT | D-09 | Encode exact independent NIP-01 custom keys including #e and #E | 02 | COVERED |
| CONTEXT | D-10 | Merge only one identical case-sensitive tag axis | 02 | COVERED |
| CONTEXT | D-11 | Preserve wire-to-logical attribution and refilter every logical query | 02, 03 | COVERED |
| CONTEXT | D-12 | Prove 300 compatible tag-value queries share one wire request with exact results/evidence | 02, 03 | COVERED |
| CONTEXT | D-13 | Exercise all ASCII letters and opposite-case non-matches | 01 | COVERED |
| CONTEXT | D-14 | Exercise UTF-8, OR, AND, empty-axis, and canonical identity | 01, 02 | COVERED |
| CONTEXT | D-15 | Observe evidence red before implementation and under a named deliberate break | 01-03 | COVERED |
| CONTEXT | D-16 | Add no tag-specific term cap; retain existing encoded-frame refusal | 02 | COVERED |

The CONTEXT deferred section is excluded from the plan set by authority.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove exact query identity and signed/unsigned owner semantics red, then implement them</name>
  <reversibility rating="costly">This expands the public Query contract and identity across downstream query consumers, but adds no persisted or wire format owned by Fava.</reversibility>
  <files>crates/fava-query/src/selection.rs, crates/fava-query/src/lib.rs, crates/fava-query/tests/query_identity.rs, crates/fava-query-standard/src/lib.rs, crates/fava-query-standard/tests/source_merge.rs</files>
  <behavior>
    - D-01/D-02: `Query::events()` accepts upstream `SingleLetterTag` keys, and otherwise-identical lowercase and uppercase axes are unequal and select different events.
    - D-03/D-04: construction order and duplicate values do not affect equality/hash; repeated exact keys union; an inserted empty key differs from an absent key and yields no events.
    - D-05/D-06/D-07/D-08: exact UTF-8 values use OR within a key and AND across keys plus existing literal axes; only cells zero and one participate.
    - Owner tracer: complete `SourceSnapshot` inputs prove the standard evaluator returns the same exact result for cached signed and local unsigned event values while excluding opposite-case, wrong-value, and later-cell decoys.
  </behavior>
  <action>Evidence first, with a hard ordering boundary: extend `query_identity.rs` and `source_merge.rs` before touching either production file. The owner corpus must use complete `SourceSnapshot` inputs for both `SourceEvent::Cached(EventValue::Signed)` and `SourceEvent::Local(EventValue::Unsigned)` and cover exact UTF-8/value case, same-axis OR, cross-axis AND, conjunction with ids/authors/kinds, present-empty match-nothing, all 52 keys with opposite-case non-matches, and irrelevant later cells. Run `cargo test -p fava-query --test query_identity` and `cargo test -p fava-query-standard --test source_merge` separately against the unchanged implementation; require both new evidence targets to fail for the missing literal-tag contract/evaluator behavior, and retain exact commands, exit statuses, and causal diagnostics. Do not edit `crates/fava-query-standard/src/lib.rs` until the signed and unsigned owner cases have produced that red evidence. Only then create the cohesive private `selection.rs` module, move `FilterSelection` plus Query selection builders/accessors out of the 500-line crate root, and re-export them without changing existing public paths. Add `BTreeMap<SingleLetterTag, BTreeSet<String>>` canonical state and a consuming `tag_values` builder that inserts the exact key before extending values so an empty iterator remains represented; repeated calls union by ordinary case-sensitive key equality. Re-export upstream `SingleLetterTag` from `fava-query` without defining a wrapper, per D-01. Extend `matches_selection` as one additional conjunct: every constrained key must find a tag whose `single_letter_tag()` equals the exact key and whose `content()` is in the exact set; an empty set therefore fails naturally, and later cells are ignored. Do not delegate Fava semantics to the upstream whole-filter matcher. Keep cache/write providers and ingest untouched. Re-run both formerly red targets green and preserve their red/green evidence in the task result for Task 3.</action>
  <verify>
    <automated>cargo test -p fava-query --test query_identity &amp;&amp; cargo test -p fava-query-standard --test source_merge &amp;&amp; cargo fmt --all -- --check</automated>
  </verify>
  <done>D-01 through D-08 and D-13/D-14 hold at their contract/evaluator owners; both the identity corpus and signed/unsigned evaluator corpus were observed red before their production files changed and green afterward; `fava-query/src/lib.rs` remains below the 500-line soft limit after the cohesive split.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the already-red signed and unsigned semantics through public Fava observation</name>
  <files>crates/fava/tests/local_source_merge.rs</files>
  <action>Add a public capstone for the behavior already observed red at the owner in Task 1. Through `Fava::builder`, real memory event-cache/write-store providers, `StandardQueryEvaluator`, and `Fava::observe`, prove one signed cached event and one unsigned accepted-local revision follow exact key/value case, UTF-8, OR/AND, ids/authors/kinds conjunction, present-empty, and later-cell semantics. Assert exact source evidence and verify the write-store contribution remains independent from any cached predecessor. Use only supported constructors/commits and public observations; do not insert expected results, call a private evaluator helper, require a physical index, or change provider contracts. This task adds a cross-boundary witness for behavior whose causal red/green owner proof is already recorded, not a new semantic claim.</action>
  <verify>
    <automated>cargo test -p fava --test local_source_merge &amp;&amp; cargo fmt --all -- --check</automated>
  </verify>
  <done>The already-red D-02/D-04 through D-08/D-13/D-14 owner semantics pass through public Fava for signed cache and unsigned write-store values with exact source evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete the direct Fava facade surface and record local red/green evidence</name>
  <files>crates/fava/src/lib.rs, crates/fava/tests/local_source_merge.rs</files>
  <behavior>
    - D-01: application code imports `Query` and the upstream `SingleLetterTag` through the thin `fava` facade with no duplicate nominal type.
    - D-04/D-08: public observation proves both signed cache and unsigned write-store revisions, exact source evidence, and present-empty refusal.
  </behavior>
  <action>Resolve Research Question 1 exactly: first switch Task 2's public capstone to import `SingleLetterTag` from `fava` and run it to capture the intended unresolved-import failure. Then re-export the same upstream type from `fava` via `fava_query`, keeping query meaning in `fava-query`; do not add a facade builder, wrapper, compatibility alias, provider bypass, or new crate. This is the selected self-contained ordinary-application surface; the direct-import alternative is closed for this phase. Re-run the plan's scoped Cargo tests, the relevant Bazel targets, formatting, strict vocabulary checker, and vocabulary-checker unit tests. Carry exact red/green commands and outcomes into `06.1-01-SUMMARY.md` so Plan 03 can append them to focused issue 0018.</action>
  <verify>
    <automated>cargo test -p fava-query --test query_identity &amp;&amp; cargo test -p fava-query-standard --test source_merge &amp;&amp; cargo test -p fava --test local_source_merge &amp;&amp; bazel test //crates/fava-query/... //crates/fava-query-standard/... //crates/fava:local_source_merge &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; python3 -m unittest tools/tests/test_vocabulary_check.py</automated>
  </verify>
  <done>The public facade exposes the upstream key type directly, the complete local semantic corpus passes through public contracts, vocabulary gates are green, and the summary contains exact causal red/green evidence for D-15.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| application → Query | Application-supplied keys and exact UTF-8 values enter canonical query identity. |
| cache/write-store event values → standard evaluator | Externally derived signed tags and application-authored unsigned tags are filtered before public observation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06.1-01 | Information Disclosure | `FilterSelection`, `matches_selection` | high | mitigate | Ordinary case-sensitive `SingleLetterTag` equality, explicit present-empty representation, 52-key/opposite-case tests, public capstone, and Plan 03 mutation. |
| T-06.1-02 | Tampering | local tag scan | medium | mitigate | Compare only `Tag::single_letter_tag()` and `Tag::content()` with exact UTF-8 membership; owner corpus includes later-cell decoys and signed/unsigned inputs. |
| T-06.1-SC | Tampering | dependency supply chain | high | mitigate | No package install or dependency/lockfile change is in scope; use the already audited pinned `nostr` crate and fail review on an unexpected manifest change. |
</threat_model>

<verification>
Run the three task gates in order. Confirm Task 1's new owner/public evidence was captured failing before production edits, then green. Confirm no code file exceeds 800 lines and the query crate root is below 500 after extraction. Run `cargo test -p fava-query -p fava-query-standard -p fava --tests`, relevant Bazel targets, formatting, and both vocabulary checks.
</verification>

<success_criteria>
LOCAL-09 is restored for literal tag axes: exact canonical identity, all 52 case-distinct keys, present-empty distinction, exact local signed/unsigned evaluation, and public Fava source evidence are executable and green without new vocabulary or provider ownership.
</success_criteria>

<output>
Create `.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-01-SUMMARY.md` when done.
</output>
