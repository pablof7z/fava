---
phase: 06.1-literal-tag-value-query-semantics-remediation
plan: 02
type: execute
wave: 2
depends_on:
  - 06.1-01
files_modified:
  - crates/fava-subscriptions/src/lib.rs
  - crates/fava-subscriptions/tests/tag_values.rs
  - crates/fava-subscriptions/BUILD.bazel
  - crates/fava-subscriptions-standard/src/lib.rs
  - crates/fava-subscriptions-standard/tests/grouping.rs
autonomous: true
requirements:
  - ROUTE-10
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09: Relay demand preserves exact independent #e and #E keys, exact UTF-8 values, canonical value order, and a present-empty custom-tag axis."
    - "D-10: Standard planning merges only identical filters or one proven-compatible non-empty author/tag axis and refuses every multi-axis, opposite-case, limit, and empty-axis merge candidate."
    - "D-11/D-12: A 300-value tag axis produces one wire message while SubscriptionPlan retains all 300 exact logical IDs and accepted filter attribution."
    - "D-16: Existing encoded-frame and subscription-count bounds remain exact; no tag-specific term cap is added."
  artifacts:
    - path: "crates/fava-subscriptions/src/lib.rs"
      provides: "Exact Query-to-NIP-01 custom-tag translation"
    - path: "crates/fava-subscriptions/tests/tag_values.rs"
      provides: "Encoded REQ evidence for case, UTF-8, canonical values, and present-empty axes"
    - path: "crates/fava-subscriptions-standard/src/lib.rs"
      provides: "Single-axis merge-candidate planner preserving exact attribution and bounds"
    - path: "crates/fava-subscriptions-standard/tests/grouping.rs"
      provides: "300-to-1 grouping and unsafe-merge refusal corpus"
  key_links:
    - from: "crates/fava-subscriptions/src/lib.rs"
      to: "crates/fava-query/src/selection.rs"
      via: "demand_for_query folds every canonical tag axis through Filter::custom_tags"
      pattern: "demand_for_query|custom_tags"
    - from: "crates/fava-subscriptions-standard/src/lib.rs"
      to: "crates/fava-subscriptions/src/lib.rs"
      via: "planner groups RelayDemand filters and preserves logical IDs in SubscriptionPlan::demand"
      pattern: "RelayDemand|SubscriptionPlan|logical"
    - from: "crates/fava-subscriptions-standard/src/lib.rs"
      to: "crates/fava-wire/src/lib.rs"
      via: "merged Filter is encoded and checked against the existing exact frame-size bound"
      pattern: "encode_client|maximum_frame_bytes"
---

<objective>
Carry the exact literal tag axis into NIP-01 relay demand and group only semantically safe tag-value queries for ROUTE-10.

Purpose: Allow the standard planner to reduce compatible wire demand without changing query meaning, access isolation, logical attribution, cancellation identity, or existing typed bounds.
Output: Exact relay conversion, Cargo/Bazel wire tests, one-axis grouping, and 300-logical-ID planner evidence.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-CONTEXT.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-RESEARCH.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-PATTERNS.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-VALIDATION.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-01-SUMMARY.md
@docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md
@docs/spec/ARCHITECTURE.md
@docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
@crates/fava-subscriptions/src/lib.rs
@crates/fava-subscriptions/BUILD.bazel
@crates/fava-subscriptions-standard/src/lib.rs
@crates/fava-subscriptions-standard/tests/grouping.rs

<interfaces>
Contracts established before this plan:

- Plan 01 adds `FilterSelection::tag_values` as canonical ordered `SingleLetterTag` keys to exact ordered string sets and exposes the consuming `Query::tag_values` builder.
- `pub fn demand_for_query(subscription_id: SubscriptionId, query: &Query) -> RelayDemand` is the sole public Query-to-relay-demand conversion seam.
- `RelayDemand { subscription_id, filter }` carries one logical query; `SubscriptionPlan` carries exact `messages`, accepted-filter `attribution`, and wire-ID-to-logical-demand mapping under one `RelaySessionKey`.
- `SubscriptionPlanner::plan(&RelaySessionKey, &[RelayDemand])` is invoked for one exact access/session boundary; this plan must not add cross-call grouping state.
- `nostr::Filter::custom_tags(SingleLetterTag, values)` owns exact protocol key serialization; `fava_wire::encode_client` owns REQ encoding.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one Query-to-wire custom-tag path, then preserve every canonical axis</name>
  <files>crates/fava-subscriptions/src/lib.rs, crates/fava-subscriptions/tests/tag_values.rs, crates/fava-subscriptions/BUILD.bazel</files>
  <behavior>
    - D-09: a public Query containing lowercase and uppercase forms encodes both exact JSON custom keys with no normalization and exact UTF-8 values.
    - D-04/D-09: a present-empty canonical key remains an empty JSON array rather than disappearing and widening the relay request.
    - D-03/D-08: duplicate and reordered values encode canonically without changing exact bytes.
  </behavior>
  <action>Evidence first: add `tests/tag_values.rs`, build public Queries only through the Plan 01 facade, convert only through `demand_for_query`, encode only through `fava_wire::encode_client`, and assert exact REQ JSON for independent lowercase/uppercase keys, exact UTF-8, canonical duplicate/order behavior, and a retained empty array. Run the new Cargo target and capture its expected failure before editing production. Then fold every canonical query tag entry through `Filter::custom_tags(*key, values.iter().cloned())` after ids/authors/kinds and before the existing limit; call it for empty sets as well. Do not assemble JSON, normalize keys/values, add a second conversion path, or change routing/ingest. Add the Bazel `rust_test` target by following the fava-wire test target pattern: load `rust_test`, use the crate's normal and dev dependency aliases, depend on `:lib` and `//crates/fava-wire:lib`, and retain Cargo.toml as compile data. Run Cargo and Bazel green, and retain red/green diagnostics for the plan summary.</action>
  <verify>
    <automated>cargo test -p fava-subscriptions --test tag_values &amp;&amp; bazel test //crates/fava-subscriptions:tag_values &amp;&amp; cargo fmt --all -- --check</automated>
  </verify>
  <done>D-03/D-04/D-08/D-09 are preserved from the public Query through exact encoded NIP-01 REQ JSON, the new evidence was red before production and green after it, and Cargo/Bazel execute the same test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove and implement one-axis tag grouping with exact 300-query attribution</name>
  <reversibility rating="costly">This changes the standard planner's shared coalescing policy, but the neutral SubscriptionPlanner contract and logical attribution remain unchanged.</reversibility>
  <files>crates/fava-subscriptions-standard/src/lib.rs, crates/fava-subscriptions-standard/tests/grouping.rs</files>
  <behavior>
    - D-10: identical filters still deduplicate; author-only grouping remains; filters differing only by non-empty values for one exact tag key merge.
    - D-10: limits, author-plus-tag differences, two differing tag keys, opposite-case keys, present-empty/non-empty pairs, and any other unequal field remain separate.
    - D-11/D-12: 300 logical demands for one exact tag key create one accepted wire filter containing 300 values and retain 300 exact logical subscription IDs.
    - D-16: existing duplicate-ID, empty-demand, subscription-count, encoded-frame-size, and encoding refusals remain exact.
  </behavior>
  <action>Evidence first: expand `grouping.rs` with the 300-to-1 positive case and table-driven refusal cases before changing the planner, run the test, and capture the causal failure. Preserve existing author grouping and typed-shortfall tests. Replace the separate `compatible` then `merge_filter` assumption with one private merge-candidate operation that returns an exact merged `Filter` or refuses without mutating the group. It may return an identical filter, union one non-empty author axis under the existing rule, or union non-empty value sets for one identical ordinary-case-sensitive tag key only when every other field and tag axis is equal. It must refuse the D-10 cases in the behavior block. Keep greedy grouping local to one `plan` call and one supplied `RelaySessionKey`; retain each original `subscription_id` in `group.logical`, `SubscriptionPlan::demand`, and accepted-filter attribution. Apply the existing subscription-count and encoded-frame-size checks after grouping exactly where they are; per D-16 add no tag-value-count policy. Run focused Cargo/Bazel and the query-to-wire test green, then preserve exact red/green outcomes in the summary for Plan 03.</action>
  <verify>
    <automated>cargo test -p fava-subscriptions --test tag_values &amp;&amp; cargo test -p fava-subscriptions-standard --test grouping &amp;&amp; bazel test //crates/fava-subscriptions:tag_values //crates/fava-subscriptions-standard:grouping &amp;&amp; cargo fmt --all -- --check</automated>
  </verify>
  <done>D-10 through D-12 and D-16 pass: safe exact tag demand groups 300-to-1 with all logical attribution retained, unsafe combinations remain separate, existing author grouping/bounds remain green, and the new planner evidence has causal red/green results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical Query → NIP-01 Filter | Application query meaning is projected into externally visible relay request bytes. |
| logical RelayDemand set → standard SubscriptionPlan | Multiple logical operations sharing one access/session boundary may be physically coalesced. |
| standard plan → transport frame bound | Externally influenced filter terms determine encoded request size and number of wire subscriptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06.1-01 | Information Disclosure | `demand_for_query` | high | mitigate | Use exact upstream custom-tag encoding for every represented axis, including empty axes; assert independent case keys and exact JSON bytes. |
| T-06.1-03 | Tampering | standard merge candidate | high | mitigate | Merge one proven-compatible axis only, retain every logical ID, and cover cross-product/opposite-case/empty-axis refusals with differential tests. |
| T-06.1-04 | Denial of Service | standard planner output | medium | mitigate | Preserve existing exact subscription-count and encoded-frame-size checks after grouping; 300-query test proves bounded attribution without introducing unbounded retained state. |
| T-06.1-05 | Information Disclosure | relay access isolation | high | mitigate | Keep grouping confined to one `SubscriptionPlanner::plan` call for one exact `RelaySessionKey`; add no shared cross-session grouping state. |
| T-06.1-SC | Tampering | dependency supply chain | high | mitigate | Install no package and change no manifest/lockfile; use the approved pinned `nostr` and existing Cargo/Bazel graph only. |
</threat_model>

<verification>
Run both task gates and `cargo test -p fava-query -p fava-query-standard -p fava-subscriptions -p fava-subscriptions-standard -p fava-ingest -p fava --tests`. Confirm each new target was observed red before its production change, then green. Confirm `git diff -- Cargo.toml Cargo.lock` is empty, existing planner refusal variants are unchanged, and the summary records exact red/green commands and results for focused issue 0018.
</verification>

<success_criteria>
ROUTE-10 holds before live relay execution: exact literal tag semantics survive Query-to-wire translation, a single safe tag axis groups 300-to-1 with exact logical attribution, unsafe grouping remains separate, access isolation is unchanged, and existing typed bounds remain active.
</success_criteria>

<output>
Create `.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-02-SUMMARY.md` when done.
</output>
