---
phase: 06.1
slug: literal-tag-value-query-semantics-remediation
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-21
---

# Phase 06.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in test harness through Cargo 1.90.0; Python `unittest` for vocabulary; Bazel 9.2.0 mirror targets |
| **Config file** | `Cargo.toml`, `rust-toolchain.toml`, per-crate `BUILD.bazel`, `apps/canary/Cargo.toml` |
| **Quick run command** | `cargo test -p fava-query -p fava-query-standard -p fava-subscriptions -p fava-subscriptions-standard -p fava-ingest -p fava --tests` |
| **Full suite command** | `cargo test --workspace --all-targets && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all -- --check && cargo test --manifest-path apps/canary/Cargo.toml && cargo clippy --manifest-path apps/canary/Cargo.toml --all-targets -- -D warnings && cargo fmt --manifest-path apps/canary/Cargo.toml -- --check && bazel test //... && python3 tools/check_vocabulary.py && python3 -m unittest tools/tests/test_vocabulary_check.py` |
| **Estimated runtime** | Quick: under 30 seconds; full: several minutes plus real-relay canary |

---

## Sampling Rate

- **After every task commit:** Run the owning package test and `cargo fmt --all -- --check`.
- **After every plan wave:** Run the quick suite and relevant Bazel targets.
- **Before `$gsd-verify-work`:** Full suite, vocabulary checks, and real-relay grouping canary must be green.
- **Max feedback latency:** 30 seconds for task-level tests.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.1-01-01 | 01 | 1 | LOCAL-09 | T-06.1-01, T-06.1-02 | Identity plus signed/unsigned owner semantics are red before model/evaluator implementation, then exact | owner/unit | `cargo test -p fava-query --test query_identity && cargo test -p fava-query-standard --test source_merge` | ✅ | ✅ green |
| 06.1-01-02 | 01 | 1 | LOCAL-09 | T-06.1-01, T-06.1-02 | Public observation returns exact signed/unsigned logical results and source evidence | facade/integration | `cargo test -p fava --test local_source_merge` | ✅ | ✅ green |
| 06.1-01-03 | 01 | 1 | LOCAL-09 | T-06.1-01 | The ordinary facade re-exports the exact upstream key type without duplicate vocabulary | facade/compile-integration | `cargo test -p fava --test local_source_merge && python3 tools/check_vocabulary.py` | ✅ | ✅ green |
| 06.1-02-01 | 02 | 2 | ROUTE-10 | T-06.1-01, T-06.1-02 | Exact `#e`/`#E`, UTF-8, and empty-axis wire projection | contract/unit | `cargo test -p fava-subscriptions --tests` | ✅ | ✅ green |
| 06.1-02-02 | 02 | 2 | ROUTE-10 | T-06.1-03, T-06.1-04 | Only one safe tag axis merges; attribution and bounds remain exact | planner/differential | `cargo test -p fava-subscriptions-standard --test grouping` | ✅ | ✅ green |
| 06.1-03-01 | 03 | 3 | LOCAL-09, ROUTE-10 | T-06.1-01, T-06.1-03 | 300 grouped logical queries equal no-grouping results/evidence and canary formatting is clean | real process/wire | `cargo fmt --manifest-path apps/canary/Cargo.toml -- --check && cargo run --manifest-path apps/canary/Cargo.toml -- run subscription-grouping-equivalence --relay-bin /Users/pablofernandez/.cargo/bin/nostr-rs-relay --seed tag-values-06-1-final2 --runs-dir /tmp/fava-tag-values-final2.FcLarU` | ✅ | ✅ green |
| 06.1-03-02 | 03 | 3 | LOCAL-09, ROUTE-10 | T-06.1-01, T-06.1-02 | Named case-folding break fails targeted evidence | deliberate break | Run the owning test and canary under the temporary named mutation, then restore | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] Add deterministic 52-key identity/case/empty evidence to `fava-query` tests.
- [x] Add exact signed/unsigned tag-matching evidence to `fava-query-standard` tests and a public-facade cache-only case.
- [x] Add a Cargo/Bazel `fava-subscriptions` conversion-test target.
- [x] Expand standard-planner grouping evidence to safe tag-axis merging and 300 logical IDs.
- [x] Rewrite the existing grouping canary around 300 tag-value logical queries.
- [x] Record red-before-code and named deliberate-break results in the focused issue.

---

## Manual-Only Verifications

All phase behaviors have automated verification.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify.
- [x] Wave 0 covers all MISSING references.
- [x] No watch-mode flags.
- [x] Feedback latency < 30 seconds for task-level tests.
- [x] `nyquist_compliant: true` set in frontmatter.

**Approval:** approved — full Cargo, strict Clippy, formatting, canary, Bazel, vocabulary, mutation, and controlled-relay evidence passed on 2026-08-21.
