---
phase: 06.1
slug: literal-tag-value-query-semantics-remediation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-21
---

# Phase 06.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in test harness through Cargo 1.90.0; Python `unittest` for vocabulary; Bazel 9.2.0 mirror targets |
| **Config file** | `Cargo.toml`, `rust-toolchain.toml`, per-crate `BUILD.bazel`, `apps/canary/Cargo.toml` |
| **Quick run command** | `cargo test -p fava-query -p fava-query-standard -p fava-subscriptions -p fava-subscriptions-standard -p fava-ingest -p fava --tests` |
| **Full suite command** | `cargo test --workspace --all-targets && cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --all -- --check && cargo test --manifest-path apps/canary/Cargo.toml && cargo clippy --manifest-path apps/canary/Cargo.toml --all-targets -- -D warnings && bazel test //... && python3 tools/check_vocabulary.py && python3 -m unittest tools/tests/test_vocabulary_check.py` |
| **Estimated runtime** | Quick: under 30 seconds; full: several minutes plus real-relay canary |

---

## Sampling Rate

- **After every task commit:** Run the owning package test and `cargo fmt --all -- --check`.
- **After every plan wave:** Run the quick suite and relevant Bazel targets.
- **Before `$gsd-verify-work`:** Full suite, vocabulary checks, and real-relay grouping canary must be green.
- **Max feedback latency:** 30 seconds for task-level tests.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.1-01-01 | 01 | 1 | LOCAL-09 | T-06.1-01 | Case-sensitive identity; present-empty stays distinct | owner/unit | `cargo test -p fava-query --test query_identity` | ✅ | ⬜ pending |
| 06.1-01-02 | 01 | 1 | LOCAL-09 | T-06.1-01, T-06.1-02 | Exact signed/unsigned matching without widening | provider/unit | `cargo test -p fava-query-standard --tests` | ✅ | ⬜ pending |
| 06.1-01-03 | 01 | 1 | LOCAL-09 | T-06.1-01, T-06.1-02 | Public observation returns exact logical results and evidence | facade/integration | `cargo test -p fava --test local_source_merge` | ✅ | ⬜ pending |
| 06.1-02-01 | 02 | 2 | ROUTE-10 | T-06.1-01, T-06.1-02 | Exact `#e`/`#E`, UTF-8, and empty-axis wire projection | contract/unit | `cargo test -p fava-subscriptions --tests` | ❌ W0 | ⬜ pending |
| 06.1-02-02 | 02 | 2 | ROUTE-10 | T-06.1-03, T-06.1-04 | Only one safe tag axis merges; attribution and bounds remain exact | planner/differential | `cargo test -p fava-subscriptions-standard --test grouping` | ✅ | ⬜ pending |
| 06.1-03-01 | 03 | 3 | LOCAL-09, ROUTE-10 | T-06.1-01, T-06.1-03 | 300 grouped logical queries equal no-grouping results/evidence | real process/wire | `cargo run --manifest-path apps/canary/Cargo.toml -- run subscription-grouping-equivalence --relay-bin /Users/pablofernandez/.cargo/bin/nostr-rs-relay --seed tag-values-06-1 --runs-dir /tmp/fava-tag-values-06-1` | ✅ rewrite | ⬜ pending |
| 06.1-03-02 | 03 | 3 | LOCAL-09, ROUTE-10 | T-06.1-01, T-06.1-02 | Named case-folding or empty-axis break fails targeted evidence | deliberate break | Run the owning test and canary under the temporary named mutation, then restore | ❌ evidence record | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Add deterministic 52-key identity/case/empty evidence to `fava-query` tests.
- [ ] Add exact signed/unsigned tag-matching evidence to `fava-query-standard` tests and a public-facade cache-only case.
- [ ] Add a Cargo/Bazel `fava-subscriptions` conversion-test target.
- [ ] Expand standard-planner grouping evidence to safe tag-axis merging and 300 logical IDs.
- [ ] Rewrite the existing grouping canary around 300 tag-value logical queries.
- [ ] Record red-before-code and named deliberate-break results in the focused issue.

---

## Manual-Only Verifications

All phase behaviors have automated verification.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies.
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify.
- [ ] Wave 0 covers all MISSING references.
- [ ] No watch-mode flags.
- [ ] Feedback latency < 30 seconds for task-level tests.
- [ ] `nyquist_compliant: true` set in frontmatter.

**Approval:** pending
