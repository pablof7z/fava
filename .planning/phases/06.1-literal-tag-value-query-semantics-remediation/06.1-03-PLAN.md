---
phase: 06.1-literal-tag-value-query-semantics-remediation
plan: 03
type: execute
wave: 3
depends_on:
  - 06.1-01
  - 06.1-02
files_modified:
  - apps/canary/src/grouping.rs
  - apps/canary/scenarios.json
  - crates/fava-subscriptions-standard/src/lib.rs
  - docs/issues/0018-literal-tag-value-filters.md
  - .planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-VALIDATION.md
autonomous: true
requirements:
  - LOCAL-09
  - ROUTE-10
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-11/D-12: Against a controlled third-party relay process, 300 exact tag-value logical queries use one standard REQ versus 300 no-grouping REQs and yield identical one-event-per-query IDs plus relay evidence."
    - "D-02/D-09/D-10: The canary independently checks opposite-case tag axes remain separate before executing the 300-query workload."
    - "D-15: A named temporary case-folded grouping-axis mutation makes the owning planner evidence and canary fail causally, is fully reversed, and all focused evidence returns green."
    - "The focused issue records exact red, green, mutation, real-relay artifact/hash, and complete validation outcomes without claiming unfinished behavior."
  artifacts:
    - path: "apps/canary/src/grouping.rs"
      provides: "Controlled-relay 300-query grouped/no-grouping differential capstone"
    - path: "apps/canary/scenarios.json"
      provides: "Enabled authoritative literal tag-value grouping scenario description"
    - path: "docs/issues/0018-literal-tag-value-filters.md"
      provides: "Focused causal evidence and validation record for the remediation"
    - path: ".planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-VALIDATION.md"
      provides: "Completed Nyquist task and sign-off status"
  key_links:
    - from: "apps/canary/src/grouping.rs"
      to: "crates/fava-subscriptions-standard/src/lib.rs"
      via: "same 300 RelayDemand values execute through standard and no-grouping planners"
      pattern: "StandardSubscriptionPlanner|NoGroupingSubscriptionPlanner"
    - from: "apps/canary/src/grouping.rs"
      to: "crates/fava-ingest/src/lib.rs"
      via: "every relay EVENT is admitted only through accepted wire ID/filter attribution"
      pattern: "plan.attribution|admit_subscription_event"
    - from: "apps/canary/src/grouping.rs"
      to: "crates/fava/tests/local_source_merge.rs"
      via: "public cache-only Query evaluation repeats the exact logical predicate after grouped admission"
      pattern: "Fava::builder|cache_only|observe"
    - from: "docs/issues/0018-literal-tag-value-filters.md"
      to: "apps/canary/runs"
      via: "recorded preserved run path, hashed manifest, process facts, wire frames, results, and report"
      pattern: "subscription-grouping-equivalence|manifest"
---

<objective>
Close Phase 06.1 with independent controlled-relay evidence, a causal case-folding falsifier, and the complete repository validation record.

Purpose: Prove the grouped physical plan is observably equivalent to no grouping through the public Fava path and that the evidence detects the exact semantic protection it claims.
Output: Upgraded real-relay canary, preserved evidence bundle, named mutation result, completed focused issue, and Nyquist sign-off.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-CONTEXT.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-RESEARCH.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-PATTERNS.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-VALIDATION.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-01-SUMMARY.md
@.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-02-SUMMARY.md
@docs/issues/0018-literal-tag-value-filters.md
@docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md
@docs/spec/ARCHITECTURE.md
@docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
@docs/spec/FAVA_REWRITE_IMPLEMENTATION_PLAN.md
@apps/canary/src/grouping.rs
@apps/canary/scenarios.json

<interfaces>
Existing capstone seams to preserve:

- `run_grouping_scenario(SmokeOptions) -> CanaryResult<PathBuf>` owns the `subscription-grouping-equivalence` controlled-relay run and returns the preserved evidence directory.
- `execute_plan(relay, demand, planner, cache)` sends planner output, attributes each EVENT through `SubscriptionPlan::attribution`, and admits only accepted frames.
- The current logical-result helper constructs a public `Fava` assembly and opens cache-only Queries; extend its comparison value to include exact event IDs and source evidence rather than reading provider internals.
- `RunArtifacts` records JSONL, report, process facts, wire frames, files, and a hashed manifest; keep these as the independent witness instead of internal diagnostics.
- Plan 02 guarantees one standard wire filter with 300 exact values and 300 retained logical IDs before this real-process slice starts.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Run the authoritative 300-query grouped/no-grouping path through a controlled relay</name>
  <precondition>`/Users/pablofernandez/.cargo/bin/nostr-rs-relay` exists, is executable, and reports the researched 0.8.12 controlled-relay version.</precondition>
  <files>apps/canary/src/grouping.rs, apps/canary/scenarios.json, docs/issues/0018-literal-tag-value-filters.md</files>
  <action>Replace the author-only workload in the existing `subscription-grouping-equivalence` scenario with exactly 300 public logical tag-value Queries sharing one ordinary-case-sensitive `SingleLetterTag` key and 300 unique exact values. Generate one valid signed event per value with deterministic seed-derived content/identity inputs, publish all events through the real WebSocket boundary, and preserve the existing bounded EOSE/CLOSED/process handling. Before the relay workload, add an explicit standard-planner preflight proving otherwise-identical lowercase and uppercase custom-tag axes remain two messages; record this as a distinct case-isolation fact. Execute the same 300 demands with `StandardSubscriptionPlanner` and `NoGroupingSubscriptionPlanner`, require one versus 300 total REQ messages, authorize each inbound EVENT only via the accepted plan's exact wire ID/filter attribution, then evaluate every original logical Query through a public cache-only Fava assembly. Compare per-query exact event-ID sets and exact relay source evidence; require exactly one event for each logical query in both paths. Resolve Research Question 2 exactly: first attempt all 300 no-grouping subscriptions together. If and only if the controlled relay explicitly returns a capacity refusal, preserve the exact refusal as evidence and execute the no-grouping side in deterministic bounded batches against the same seeded relay, accumulating all 300 logical results/evidence and retaining the exact total of 300 separate REQs; the standard side must still prove all 300 logical demands in one wire request. Silence, timeout, or unrelated failure must fail the scenario and cannot select batching. This concurrent-first, explicit-refusal-only rule is final for the phase. Update `scenario_passed`, `report.md`, and the hashed manifest inputs with the exact wire counts, logical count, case-isolation fact, result/evidence equivalence, execution mode, any exact capacity refusal, relay version, process facts, and preserved wire evidence. Keep the scenario ID and enabled status; update only its description to the D-12 acceptance case. Create a unique run root with `mktemp -d`, run the scenario against the installed controlled `nostr-rs-relay`, and append the actual run directory plus manifest hash and exact green command/result to issue 0018. Do not use public-relay reconnaissance or Fava diagnostics as the witness.</action>
  <verify>
    <automated>cargo fmt --manifest-path apps/canary/Cargo.toml -- --check &amp;&amp; runs_dir=$(mktemp -d /tmp/fava-tag-values-06-1.XXXXXX) &amp;&amp; cargo run --manifest-path apps/canary/Cargo.toml -- run subscription-grouping-equivalence --relay-bin /Users/pablofernandez/.cargo/bin/nostr-rs-relay --seed tag-values-06-1 --runs-dir "$runs_dir"</automated>
  </verify>
  <done>D-02/D-09 through D-12 pass through real process and wire boundaries: exact case axes stay separate, the authoritative 300 queries use 1 versus 300 REQs, every logical query receives one identical event and relay evidence, and the preserved run path/hash is recorded in issue 0018.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the case-isolation falsifier, restore it exactly, and close validation</name>
  <files>crates/fava-subscriptions-standard/src/lib.rs, docs/issues/0018-literal-tag-value-filters.md, .planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-VALIDATION.md</files>
  <action>Use a reversible local patch, never a production flag, to create the named `case-folded grouping-axis` deliberate break. Before mutation, compute the SHA-256 of `crates/fava-subscriptions-standard/src/lib.rs`. Alter only the implemented standard planner tag-key candidate comparison so opposite-case keys are treated as the same merge axis. Run the owning grouping test and the real-relay canary independently without short-circuiting; require both to exit non-zero for the intended opposite-case isolation assertion. Run the query identity, local evaluator/facade, and exact wire tests as controls and record their actual outcomes rather than implying every unrelated proof must fail. Reverse the exact mutation hunk without checkout/reset, compute the restored source SHA-256, require it to equal the pre-mutation hash, and rerun the focused matrix plus a fresh real-relay scenario green. Then run the complete root Cargo tests, strict Clippy, root format check, canary Cargo tests, canary strict Clippy, canary format check, Bazel suite, vocabulary checker, and vocabulary unit tests; every command must complete successfully before sign-off. Append to issue 0018 the exact Plan 01/02 red-before-code evidence from their SUMMARY files, scoped green results, mutation failure/restoration, latest controlled-relay artifact/hash, full validation commands/outcomes, and final status; do not claim any command not actually run. Use these exact evidence headings and scalar lines so verification measures results rather than prose: `### Red before implementation`, `### Green focused evidence`, `### Deliberate break: case-folded grouping-axis`, `Planner mutation exit: N`, `Canary mutation exit: N`, `Pre-mutation source SHA-256: HEX`, `Post-restoration source SHA-256: HEX`, `### Controlled-relay artifact`, `### Full validation`, and `Full validation exit: 0`. Mark every 06.1-VALIDATION task/sign-off item from actual evidence, set `wave_0_complete` and `nyquist_compliant` true only after all mapped commands pass, and leave it pending on any failure. Confirm the diff introduces no dependency, lockfile, compatibility path, hidden flag, new nominal vocabulary, ingest change, relay-facade change, or provider-index requirement.</action>
  <verify>
    <automated>cargo test -p fava-subscriptions-standard --test grouping &amp;&amp; cargo fmt --all -- --check &amp;&amp; issue=docs/issues/0018-literal-tag-value-filters.md &amp;&amp; validation=.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-VALIDATION.md &amp;&amp; for heading in '### Red before implementation' '### Green focused evidence' '### Deliberate break: case-folded grouping-axis' '### Controlled-relay artifact' '### Full validation'; do grep -Fx "$heading" "$issue" &gt;/dev/null || exit 1; done &amp;&amp; planner_exit=$(awk '/^Planner mutation exit: /{print $4}' "$issue" | tail -1) &amp;&amp; canary_exit=$(awk '/^Canary mutation exit: /{print $4}' "$issue" | tail -1) &amp;&amp; pre_hash=$(awk '/^Pre-mutation source SHA-256: /{print $4}' "$issue" | tail -1) &amp;&amp; post_hash=$(awk '/^Post-restoration source SHA-256: /{print $4}' "$issue" | tail -1) &amp;&amp; full_exit=$(awk '/^Full validation exit: /{print $4}' "$issue" | tail -1) &amp;&amp; test -n "$planner_exit" &amp;&amp; test "$planner_exit" -ne 0 &amp;&amp; test -n "$canary_exit" &amp;&amp; test "$canary_exit" -ne 0 &amp;&amp; test -n "$pre_hash" &amp;&amp; test "$pre_hash" = "$post_hash" &amp;&amp; test "$full_exit" -eq 0 &amp;&amp; grep -Fx 'nyquist_compliant: true' "$validation" &gt;/dev/null &amp;&amp; grep -Fx 'wave_0_complete: true' "$validation" &gt;/dev/null &amp;&amp; test "$(awk '/^## Validation Sign-Off/{flag=1} flag &amp;&amp; /- \[ \]/{count++} END{print count+0}' "$validation")" -eq 0 &amp;&amp; test "$(grep -F -c '| ⬜ pending |' "$validation")" -eq 0</automated>
  </verify>
  <done>D-15 has causal mutation evidence, the exact temporary patch is absent, every focused test and real-relay capstone is green after restoration, the complete validation set actually passed, issue 0018 contains reconstructable evidence, and 06.1-VALIDATION is truthfully signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canary → third-party relay process | Signed events and NIP-01 requests cross a real socket/process boundary outside Fava. |
| relay EVENT → ingest/cache | Untrusted frames must be attributed to the exact accepted wire filter before cache admission. |
| grouped cache state → 300 public logical Queries | A broad physical response is narrowed back to each original application query with exact evidence. |
| process/wire run → retained evidence record | Completion claims depend on bounded artifacts and hashes that must not be self-certified by Fava internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06.1-03 | Information Disclosure | grouped planner and logical refilter | high | mitigate | Opposite-case preflight, exact accepted-filter attribution, 300 independent public refilters, grouped/no-grouping differential comparison, and causal planner mutation. |
| T-06.1-06 | Spoofing | inbound relay EVENT attribution | high | mitigate | Retain exact wire subscription ID/filter lookup and existing ingest admission; never insert returned events directly into cache. |
| T-06.1-07 | Denial of Service | 300-query real-process workload | medium | mitigate | Preserve bounded EOSE deadlines, existing frame/subscription checks, finite 300-query corpus, process supervision, and shutdown failure reporting. |
| T-06.1-08 | Repudiation | evidence bundle and issue record | medium | mitigate | Preserve proxy frames, process facts, report, JSONL, manifest hashes, exact commands/exit statuses, and causal mutation excerpts under one run identity. |
| T-06.1-SC | Tampering | relay binary and dependency supply chain | high | mitigate | Use the already installed audited relay binary and pinned workspace dependencies; perform no install or dependency/lockfile change. |
</threat_model>

<verification>
After both tasks, rerun the full gate exactly: `cargo test --workspace --all-targets &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo test --manifest-path apps/canary/Cargo.toml &amp;&amp; cargo clippy --manifest-path apps/canary/Cargo.toml --all-targets -- -D warnings &amp;&amp; bazel test //... &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; python3 -m unittest tools/tests/test_vocabulary_check.py`. Run a final fresh controlled-relay grouping scenario after the mutation is removed. Verify issue 0018 cites the actual latest artifact path/hash, all five evidence layers, and exact command outcomes. Verify `git diff -- Cargo.toml Cargo.lock crates/fava-ingest/src/lib.rs crates/fava/src/relay.rs` is empty.
</verification>

<success_criteria>
The Phase 06.1 goal is achieved only when public exact local semantics and exact relay grouping are green, the 300-query controlled-relay bundle proves 1-versus-300 physical equivalence with exact per-query results/evidence, the named case-folding break fails causally and is absent afterward, all repository gates pass, and the focused issue contains reconstructable evidence.
</success_criteria>

<output>
Create `.planning/phases/06.1-literal-tag-value-query-semantics-remediation/06.1-03-SUMMARY.md` when done.
</output>
