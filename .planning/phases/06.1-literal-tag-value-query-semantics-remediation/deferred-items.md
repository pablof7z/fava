# Deferred Items

- `python3 tools/check_vocabulary.py` currently reports the pre-existing `/tmp/fava-tag-values-06-1` run-directory example in `06.1-VALIDATION.md` and `06.1-03-PLAN.md` as an undocumented crate name. This false positive is outside Plan 01's code scope and must be resolved before Plan 03's vocabulary gate.
