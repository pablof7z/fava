---
phase: 06.1-literal-tag-value-query-semantics-remediation
verified: 2026-08-21T20:07:45Z
status: passed
score: 12/12 must-haves verified
behavior_unverified: 0
overrides_applied: 0
decision_coverage:
  honored: 0
  total: 0
  not_honored: []
human_verification: []
---

# Phase 06.1: Literal Tag-Value Query Semantics Remediation Verification Report

**Phase Goal:** As a Fava application developer, I want to select events by any exact case-sensitive ASCII one-letter Nostr tag key and safely group compatible relay demand, so that wire optimization never changes logical results or evidence.
**Verified:** 2026-08-21T20:07:45Z
**Status:** passed
**Re-verification:** No — initial verification
**Verified head:** `cb1b698` (Phase 06.1 merged with completed M7)

## User Flow Coverage

User story: “As a Fava application developer, I want to select events by any exact case-sensitive ASCII one-letter Nostr tag key and safely group compatible relay demand, so that wire optimization never changes logical results or evidence.”

| Step | Expected | Evidence | Status |
|---|---|---|---|
| Construct an exact tag query | Application code imports `Query` and the upstream `SingleLetterTag` from `fava` and can use every ASCII letter in either case | `crates/fava/src/lib.rs:20-22`; `crates/fava-query/src/selection.rs:3,16-19,50-65`; `all_ascii_letter_tag_axes_are_case_sensitive` passes | ✓ |
| Express exact selection | Repeated values canonicalize, same-key values OR, distinct axes AND, and present-empty matches nothing | `selection.rs:55-65`; `fava-query-standard/src/lib.rs:119-137`; identity/evaluator/public-facade tests pass | ✓ |
| Observe exact local results | Signed cache and unsigned write-store events are selected through public `Fava::observe` with their exact source evidence | `crates/fava/tests/local_source_merge.rs:318-449`; named public-facade test passes | ✓ |
| Build and group relay demand | Exact `#e`/`#E` and UTF-8 values reach NIP-01 filters; only one safe non-empty axis is unioned | `crates/fava-subscriptions/src/lib.rs:97-117`; `crates/fava-subscriptions-standard/src/lib.rs:119-175`; wire/grouping tests pass | ✓ |
| Outcome: optimization preserves meaning | 300 logical queries use one grouped request versus 300 ungrouped requests and retain identical one-event results and relay-session evidence | `apps/canary/src/grouping.rs:42-124,241-415,417-638`; preserved controlled-relay artifact has 300 rows and zero mismatches | ✓ |

## Goal Achievement

### Observable Truths

The four ROADMAP success criteria are non-negotiable and are subsumed by the twelve more-specific PLAN truths below.

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Applications construct literal constraints for all 52 ASCII letter keys and uppercase/lowercase remain distinct | ✓ VERIFIED | Canonical `BTreeMap<SingleLetterTag, ...>` plus passing 52-key query-identity and evaluator corpora |
| 2 | Query identity ignores construction order and duplicates; repeated keys union; absent differs from present-empty | ✓ VERIFIED | `literal_tag_values_have_canonical_query_identity` and `absent_and_present_empty_tag_axes_are_distinct` pass |
| 3 | Local signed/unsigned results use exact UTF-8 cell-one values, OR within a key, AND across keys/ids/authors/kinds, ignoring later cells | ✓ VERIFIED | `literal_tag_selection_matches_exact_signed_and_unsigned_cells` passes against complete cache/write-store snapshots |
| 4 | Owner and public-facade evidence covers the literal corpus and has causal red/mutation records | ✓ VERIFIED | Owner, facade, and issue evidence exist; current named tests pass; issue records red-before-code and the named break |
| 5 | Relay demand preserves independent `#e`/`#E`, exact UTF-8, canonical ordering, and present-empty axes | ✓ VERIFIED | Exact encoded REQ tests pass; translation calls upstream `Filter::custom_tags` for every represented axis, including empty sets |
| 6 | Planning merges only identical filters or one proven-compatible non-empty author/tag axis and refuses unsafe candidates | ✓ VERIFIED | Constructive `merge_candidate`; table-driven limit, multi-axis, opposite-case, empty-axis, and unequal-field refusal test passes |
| 7 | A 300-value tag axis produces one plan message with all 300 logical IDs and accepted-filter attribution | ✓ VERIFIED | `one_exact_non_empty_tag_axis_groups_with_exact_values_and_logical_ids` passes with exact value-set and ID assertions |
| 8 | Existing subscription-count and encoded-frame bounds remain exact; no tag-specific cap exists | ✓ VERIFIED | Existing typed refusal tests pass; planner applies count/frame checks after grouping and defines no tag-term bound |
| 9 | Controlled relay execution proves one grouped versus 300 separate REQs with identical per-query IDs and relay evidence | ✓ VERIFIED | Preserved `nostr-rs-relay 0.8.12` artifact: 300 rows, zero mismatches, `grouped_reqs=1`, `separate_reqs=300` |
| 10 | Canary checks opposite-case isolation before the 300-query workload | ✓ VERIFIED | `prove_case_isolation` requires two messages/two attributions before publishing; artifact records `case_isolation=true` |
| 11 | The case-folded grouping-axis mutation fails planner and canary evidence, is reversed exactly, and current evidence is green | ✓ VERIFIED | Issue records exits 101/1 and equal pre/post SHA-256; current source and complete regression matrix pass |
| 12 | Focused issue contains reconstructable red, green, mutation, relay-artifact/hash, and validation outcomes | ✓ VERIFIED | `docs/issues/0018-literal-tag-value-filters.md:57-113`; cited final manifest hash matches the retained file |

**Score:** 12/12 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `crates/fava-query/src/selection.rs` | Canonical exact tag selection | ✓ VERIFIED | 73 substantive lines; upstream type, ordered map/set, and present-empty builder semantics |
| `crates/fava-query/tests/query_identity.rs` | 52-key and canonical identity proof | ✓ VERIFIED | 5 active value-level tests; full target passes |
| `crates/fava-query-standard/src/lib.rs` | Exact local predicate | ✓ VERIFIED | `matches_selection` performs all-axis/every-tag exact matching |
| `crates/fava-query-standard/tests/source_merge.rs` | Signed/unsigned owner proof | ✓ VERIFIED | Exact cells, all cases, empty axis, source evidence, and post-review replacement/order regressions pass |
| `crates/fava/tests/local_source_merge.rs` | Public Fava capstone | ✓ VERIFIED | Real memory providers and `Fava::observe`; exact cache/publication evidence asserted |
| `crates/fava-subscriptions/src/lib.rs` | Query-to-NIP-01 projection | ✓ VERIFIED | Every canonical tag axis flows through upstream `custom_tags` |
| `crates/fava-subscriptions/tests/tag_values.rs` | Exact encoded REQ proof | ✓ VERIFIED | 3 active byte-exact tests; Cargo and Bazel targets pass |
| `crates/fava-subscriptions-standard/src/lib.rs` | Safe grouping and typed bounds | ✓ VERIFIED | Constructive one-axis merge; count/frame refusals preserved |
| `crates/fava-subscriptions-standard/tests/grouping.rs` | 300-to-1 and refusal corpus | ✓ VERIFIED | 6 active value/behavioral tests pass |
| `apps/canary/src/grouping.rs` | Real-relay differential owner | ✓ VERIFIED | 647 lines, below 800 hard cap; documented cohesion rationale covers the soft-cap exception |
| `apps/canary/src/grouping_tests.rs` | Exact wire-witness/review regression | ✓ VERIFIED | Accepts exact filters, rejects over-broad/wrong filters, ignores close/binary frames |
| `apps/canary/scenarios.json` | Enabled acceptance scenario | ✓ VERIFIED | `subscription-grouping-equivalence` is enabled and names the 300-query contract |
| `docs/issues/0018-literal-tag-value-filters.md` and `06.1-VALIDATION.md` | Causal and validation records | ✓ VERIFIED | Required headings/scalars present; validation is fully signed off |

**Artifacts:** 13/13 verified. `verify.artifacts` also reports 13/13 PLAN-declared artifacts passing.

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `fava-query/src/lib.rs` | `selection.rs` | private module + public re-export | ✓ WIRED | `mod selection`; public `FilterSelection`/`SingleLetterTag` exports |
| `Query::tag_values` | `StandardQueryEvaluator` | canonical selection consumed by `matches_selection` | ✓ WIRED | Exact key/content scan is part of the evaluator conjunction |
| `fava/tests/local_source_merge.rs` | standard evaluator/providers | `Fava::builder` + `Fava::observe` | ✓ WIRED | Public result includes cache and write-store evidence |
| `demand_for_query` | canonical selection | iterate `query.selection().tag_values` | ✓ WIRED | Exact upstream custom tags are produced |
| standard planner | relay demand | `merge_candidate` + retained `group.logical` | ✓ WIRED | Merged filter and all logical IDs enter `SubscriptionPlan` |
| standard planner | wire bound | `encode_client` before frame-size acceptance | ✓ WIRED | Exact encoded byte length drives typed refusal |
| canary | standard/no-grouping planners | same 300 `RelayDemand` values | ✓ WIRED | Both physical plans run against the same seeded relay corpus |
| canary | ingest | exact `plan.attribution` lookup then `admit_subscription_event` | ✓ WIRED | No direct cache bypass |
| canary | public query evaluator | 300 original cache-only `Query` values through `Fava::observe` | ✓ WIRED | Each grouped event is logically refiltered |
| wire witness | post-review proxy records | client-to-relay text frames only | ✓ WIRED | Close and binary frames are ignored, exact filter multisets still checked |

**Wiring:** 10/10 connections verified. `verify.key-links` reports all 10 PLAN-declared links verified.

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `selection.rs` | exact tag axes | application `Query::tag_values` input | Canonical ordered key/value sets | ✓ FLOWING |
| `fava-query-standard` + facade | event records/evidence | real memory cache and independent write-store snapshots | Public signed/unsigned results with exact evidence | ✓ FLOWING |
| `fava-subscriptions` + planner | relay filters/attribution | canonical Query selection and logical demand | Exact NIP-01 filters and complete logical-ID mapping | ✓ FLOWING |
| grouping canary | per-query IDs and relay sessions | signed events crossing a controlled relay, attribution-gated cache admission, public refilter | 300 exact result rows in hashed artifact | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| All ASCII letter keys remain case-distinct | named `query_identity` test | 1 passed | ✓ PASS |
| Exact signed/unsigned local cells | named `source_merge` test | 1 passed | ✓ PASS |
| Public facade result/evidence | named `local_source_merge` test | 1 passed | ✓ PASS |
| Exact case/UTF-8 relay encoding | named `tag_values` test | 1 passed | ✓ PASS |
| 300-to-1 grouping and attribution | named `grouping` positive test | 1 passed | ✓ PASS |
| Unsafe merge refusal | named `grouping` table test | 1 passed | ✓ PASS |
| Review fix: close/binary wire frames | named canary wire-witness test | 1 passed | ✓ PASS |
| Final integrated regression | full Cargo/Clippy/fmt + canary + Bazel + vocabulary gate at `cb1b698` | all commands exited 0; 21 canary and 37 Bazel tests passed | ✓ PASS |

### Probe Execution

No probe scripts are declared or implied by the phase artifacts. The enabled controlled-relay canary is the phase's executable external-process witness and is covered by its preserved artifact plus current canary tests.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| LOCAL-09 | 01, 03 | Equivalent query descriptions retain stable semantic identity | ✓ SATISFIED | Canonical tag identity, exact local semantics, public facade, causal mutation, and merged-head regression pass |
| ROUTE-10 | 02, 03 | Grouping changes wire shape without changing meaning, evidence, access isolation, or cancellation | ✓ SATISFIED | One-session constructive planner, exact attribution, refusal/bounds corpus, and controlled-relay grouped/no-grouping equivalence |

**Coverage:** 2/2 requirements satisfied. No orphaned Phase 06.1 requirements were found.

### Test Quality Audit

| Test File | Linked Req | Active | Skipped | Circular | Assertion Level | Verdict |
|---|---|---:|---:|---|---|---|
| `fava-query/tests/query_identity.rs` | LOCAL-09 | 5 | 0 | No | Value | ✓ STRONG |
| `fava-query-standard/tests/source_merge.rs` | LOCAL-09 | 10 | 0 | No | Behavioral/value | ✓ STRONG |
| `fava/tests/local_source_merge.rs` | LOCAL-09 | 7 | 0 | No | Public behavioral | ✓ STRONG |
| `fava-subscriptions/tests/tag_values.rs` | ROUTE-10 | 3 | 0 | No | Byte-exact value | ✓ STRONG |
| `fava-subscriptions-standard/tests/grouping.rs` | ROUTE-10 | 6 | 0 | No | Behavioral/value | ✓ STRONG |
| `apps/canary/src/grouping_tests.rs` | ROUTE-10 | 3 | 0 | No | Independent wire multiset | ✓ STRONG |

**Disabled requirement tests:** 0. **Circular patterns:** 0. **Insufficient assertions:** 0.

### Post-Review Fix Verification

| Finding | Current evidence | Status |
|---|---|---|
| CR-01 replacement winner selection order | Replacement/tag and relay-authority regressions pass in `source_merge` | ✓ VERIFIED |
| CR-02 newest-first equal-time ID order | Ordering/limit regression passes | ✓ VERIFIED |
| WR-01 wire witness previously checked counts only | Exact grouped/separate filter multisets checked; both negative witnesses pass | ✓ VERIFIED |
| WR-02 vocabulary `/tmp` suppression scope | Occurrence-local regression and 6 vocabulary unit tests pass | ✓ VERIFIED |
| WR-03 unequal queries required unequal hashes | Invalid collision assumption is absent; equality still requires equal hashes | ✓ VERIFIED |
| CR-06 close frames parsed as JSON | Parser requires `client_to_relay` + `text`; close/binary fixture and a post-fix controlled-relay artifact with 311 close frames pass | ✓ VERIFIED |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| Phase-modified code/tests | — | No `TBD`, `FIXME`, `XXX`, TODO/HACK/placeholder, disabled test, or stub-return pattern found | ℹ️ Info | None |
| `apps/canary/src/grouping.rs` | — | 647 lines exceeds 500-line soft cap | ℹ️ Info | Below 800 hard cap; issue 0018 records the concrete single-witness cohesion rationale |

### Decision Coverage

No trackable decisions in CONTEXT.md. The automated decision-coverage gate reports `skipped: true`, `total: 0`; the unnumbered locked decisions were nevertheless checked through the twelve must-haves above.

### Disconfirmation Pass

- LOCAL-09 is broader than this remediation; the phase-specific part is fully covered, while the pre-existing access/source-authority identity tests also pass in the merged workspace.
- The 52-key identity test alone would not prove matching. That potential misleading pass is closed by the separate 52-key signed/unsigned evaluator corpus and public-facade evidence.
- No phase-critical uncovered error path was found. Unsafe grouping, exact bounds, relay capacity refusal, malformed wire-filter witnesses, and non-text proxy frames all have executable or preserved external evidence.

### Human Verification Required

N/A — infrastructure/core-library phase with no user-facing UI. Every acceptance criterion has programmatic owner, public-facade, independent wire, or controlled-process evidence; `behavior_unverified` is zero.

### Gaps Summary

**No gaps found.** All roadmap and PLAN must-haves are substantive, wired, behaviorally exercised, and green on the final merged head. No later-phase deferral is needed.

---

_Verified: 2026-08-21T20:07:45Z_
_Verifier: the agent (gsd-verifier)_
