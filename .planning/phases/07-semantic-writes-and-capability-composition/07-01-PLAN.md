---
phase: 07-semantic-writes-and-capability-composition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - features/semantic-writes.feature
  - .planning/phases/07-semantic-writes-and-capability-composition/07-PATTERNS.md
  - tools/tests/test_semantic_write_feature.py
  - crates/fava/tests/semantic_write_contract.rs
  - crates/fava/BUILD.bazel
  - crates/fava-write/src/lib.rs
  - crates/fava-write/src/edit.rs
  - crates/fava-write/src/materialization.rs
  - docs/internals/vocabulary.toml
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The complete semantic-write promise is written as observable scenarios and mapped to executable Rust evidence before production behavior changes."
    - "A bounded replaceable edit, its inverse, one exact coordinate, and one selected materializer cross the neutral write boundary without protocol meaning entering universal core code."
    - "A first materialization has no prior source; a successor materialization receives the selected qualified source and an exact injected timestamp."
    - "Structurally invalid, oversized, malformed, addressable, duplicate-field, and over-capacity edit values refuse through existing typed errors before custody or effects."
    - "No descriptor-like, registry-like, factory-like, profile-like, compatibility-like, or migration-like public abstraction is introduced."
  artifacts:
    - "features/semantic-writes.feature"
    - "crates/fava/tests/semantic_write_contract.rs"
    - "crates/fava-write/src/edit.rs"
    - "crates/fava-write/src/materialization.rs"
  key_links:
    - "Feature scenario tags map each public promise to a named Rust test target and test name."
    - "WriteIntent::Edit retains ReplaceableEventEdit until a selected ReplaceableEventMaterializer produces an ordinary unsigned event."
    - "MaterializationId identifies a concrete generated event independently of stable WriteId and ReceiptId."
---

<objective>
Define the behavior-first semantic-write contract and the closed neutral vocabulary it needs.

Purpose: Make every later store, publication, protocol, restart, and N+1 slice implement an already-falsifiable public promise.
Output: Executable feature mapping plus approved neutral edit/materialization values and contract tests.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-CONTEXT.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-RESEARCH.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-PATTERNS.md
@docs/issues/0010-m7-semantic-writes-and-capability-composition.md
@docs/issues/0011-m7-semantic-write-vocabulary.md
@docs/internals/vocabulary.toml
@crates/fava-write/src/lib.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Write the mapped semantic-write behavior before production</name>
  <files>features/semantic-writes.feature, .planning/phases/07-semantic-writes-and-capability-composition/07-PATTERNS.md, tools/tests/test_semantic_write_feature.py, crates/fava/tests/semantic_write_contract.rs, crates/fava/BUILD.bazel</files>
  <behavior>
    - Scenario `first value` maps to a public `fava` test whose semantic edit has no prior source.
    - Scenario `source-v2 rematerialization` maps to a public test that preserves unrelated successor-source fields.
    - Scenario `stable receipt and new MaterializationId` maps to a public test with one WriteId and ReceiptId across two generated events.
    - Scenario `retired completion` maps to a public test proving an older completion is attributable and inert.
    - Scenario `inverse` maps to a public follow/unfollow or bookmark/unbookmark test.
    - Scenario `external N+1` maps to the external capability falsifier.
    - Scenario `raw future kinds` maps to a public raw-event regression test.
  </behavior>
  <action>Create `features/semantic-writes.feature` first with exactly the seven observable scenarios above and stable mapping tags that name the ownership-split Rust target and eventual test function. Preserve the analog guidance in 07-PATTERNS.md as prose about existing responsibilities and dependency boundaries; illustrative snippets must not resemble new public declarations or package identifiers and must not register vocabulary. Add a Python mapping test that refuses duplicate scenario names, missing target/name mappings, malformed mapping tags, and placeholder step mappings; Plan 09 strengthens the same test to require every later target/name to exist. Add the mapped contract test target next, run it before production changes, and preserve the RED output in the plan summary. The executable mapping is the behavioral authority; the later shared capability corpus is composition evidence rather than this RED gate. Do not add production symbols in this task.</action>
  <verify>
    <automated>python3 -m unittest tools.tests.test_semantic_write_feature &amp;&amp; test "$(rg -c '^  Scenario: (first value|source-v2 rematerialization|stable receipt and new MaterializationId|retired completion|inverse|external N\+1|raw future kinds)$' features/semantic-writes.feature)" -eq 7 &amp;&amp; if cargo test -p fava --test semantic_write_contract; then echo 'expected semantic contract RED before production' &gt;&amp;2; exit 1; else exit 0; fi</automated>
  </verify>
  <done>The mapping test passes, all seven named scenarios have explicit ownership target/name destinations, and the public contract target is captured failing for missing semantic-write behavior before production code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the bounded neutral edit and materialization contract</name>
  <files>crates/fava-write/src/lib.rs, crates/fava-write/src/edit.rs, crates/fava-write/src/materialization.rs, crates/fava/tests/semantic_write_contract.rs, docs/internals/vocabulary.toml</files>
  <behavior>
    - ReplaceableEventEdit bounds its opaque change bytes, records actor and one exact EventCoordinate, round-trips durably, and produces a bounded inverse.
    - ReplaceableEventMaterializer receives the edit, optional qualified source event, and exact caller-injected Timestamp; first value receives None.
    - WriteIntent::Edit performs structural and bounded edit validation only: it accepts only a non-addressable exact coordinate and rejects a non-empty d-tag, malformed encoding, or oversize change/inverse through existing WriteIntentError values before custody.
    - MaterializationId changes with each generated event while WriteId and ReceiptId remain stable.
    - Receipt and PublicationEvidence carry bounded current/retired materialization and post-accept failure facts using only those existing value types.
  </behavior>
  <action>After the RED evidence exists, implement only the already-approved `ReplaceableEventEdit`, `ReplaceableEventMaterializer`, and `MaterializationId`. Keep edit change and inverse bytes opaque to core, cap each at the existing write-intent encoded-size bound, and use EventCoordinate as the exact key. Add `WriteIntent::Edit` constructor validation for structural and bounded edit facts only, including rejection of addressable coordinates before custody because no exact bounded d-tag selector exists. Provider/materializer availability is not known in `fava-write` and must not be checked here; Plan 03 owns empty, unsupported, duplicate, and over-capacity selection refusal. The materializer method must accept an exact Timestamp supplied by its caller and return the existing unsigned-event/result vocabulary; it must not read time, sign, route, publish, cache, or own receipt state. Extend Receipt/PublicationEvidence fields rather than adding a new failure/evidence nominal type: bound failure text with the existing receipt-text limit, identify source/event/generation with existing IDs, and bound retired evidence with the existing route/evidence cap. Update only the already-approved vocabulary registry entries; introduce no other public or cross-crate nominal type, provider contract, wrapper, synonym, descriptor, registry, profile, compatibility path, or migration path.</action>
  <verify>
    <automated>CONTRACT_LIST="$(cargo test -p fava --test semantic_write_contract -- --list)" &amp;&amp; test "$(printf '%s\n' "$CONTRACT_LIST" | rg -c '(^|::)(edit_contract_is_bounded_and_round_trips|first_value_receives_no_prior_and_exact_timestamp|addressable_edit_refuses_before_custody|materialization_identity_changes_but_receipt_identity_does_not): test$')" -eq 4 &amp;&amp; cargo test -p fava --test semantic_write_contract &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check</automated>
  </verify>
  <done>Four required contract tests are listed and pass; malformed, addressable, oversized, duplicate-field, and arithmetic-overflow edit structures are typed refusals; no provider-availability check exists in `fava-write`; first value and exact timestamp behavior are proven; vocabulary gates pass with only the four approved M7 entries.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

Apply OWASP ASVS L1 proportional controls at every boundary below; every listed threat has an explicit severity and disposition.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application to WriteIntent | Untrusted opaque edit bytes, inverse, coordinate, actor, and routing enter durable write validation. |
| publication to materializer | Selected source state and exact timestamp cross into protocol-owned meaning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | edit decode and inverse | high | mitigate | Bound and validate encoded edit/inverse values before custody; round-trip and malformed-input tests use existing typed errors. |
| T-07-02 | Denial of Service | edit and retired evidence bounds | high | mitigate | Reuse write size and route/evidence caps; refuse overflow before allocation or custody. |
| T-07-03 | Elevation of Privilege | protocol materializer contract | high | mitigate | Keep the contract pure and unable to sign, route, publish, mutate caches, or own receipts. |
| T-07-04 | Repudiation | generation attribution | medium | mitigate | Preserve stable operation IDs and exact MaterializationId/event/source identity in existing receipt evidence. |
| T-07-SC | Tampering | cargo installs | low | accept | This plan adds no third-party package or dependency. |
</threat_model>

<verification>
Run the feature mapping test, the positively enumerated contract target, vocabulary checker, vocabulary unit tests, and `git diff --check`.
</verification>

<success_criteria>
The entire M7 semantic-write promise is mapped before implementation, the neutral contract is bounded and persistable, and no unapproved architectural vocabulary exists.
</success_criteria>

<output>
Create `.planning/phases/07-semantic-writes-and-capability-composition/07-01-SUMMARY.md` when done.
</output>
