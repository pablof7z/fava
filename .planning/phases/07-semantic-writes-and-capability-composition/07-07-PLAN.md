---
phase: 07-semantic-writes-and-capability-composition
plan: 07
type: execute
wave: 5
depends_on: [07-04]
files_modified:
  - falsifiers/external-semantic-capability/Cargo.toml
  - falsifiers/external-semantic-capability/Cargo.lock
  - falsifiers/external-semantic-capability/src/lib.rs
  - falsifiers/external-semantic-capability/tests/public_capability.rs
autonomous: true
requirements: [CAP-08, CAP-09]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An out-of-workspace crate defines an unrelated non-addressable replaceable capability and selects it through public fava without editing universal core behavior."
    - "The external capability proves first value, inverse, source successor preservation, deterministic empty/adjacent/duplicate composition, bounds, preview/live parity, stable receipt, and retired completion inertness."
    - "Raw arbitrary and future event kinds remain constructible, routable, publishable, and observable through public fava without a selected semantic applier."
    - "The falsifier's capability code uses public fava functions/approved values only; integration tests instantiate actors through explicit existing provider/testkit dev-dependencies without private publication access or facade re-export expansion."
    - "No descriptor-like, registry-like, profile-like, compatibility-like, or migration-like universal extension point is added."
  artifacts:
    - "falsifiers/external-semantic-capability/src/lib.rs"
    - "falsifiers/external-semantic-capability/tests/public_capability.rs"
  key_links:
    - "The external crate's sole normal dependency is public `fava`; explicit existing provider/testkit crates and Tokio are dev-dependencies only, and the crate is absent from root workspace members and selected product manifests."
    - "Its private applier is selected by the same FavaBuilder function used for NIP-02/bookmarks."
---

<objective>
Falsify core coupling with an external N+1 capability and preserve raw future-event publication.

Purpose: Prove the architecture composes new protocol meaning without any universal-core behavior edit or closed kind switch.
Output: Independent external crate and public end-to-end behavioral falsifier.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-04-SUMMARY.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-RESEARCH.md
@falsifiers/external-null-cache/Cargo.toml
@falsifiers/external-null-cache/src/lib.rs
@crates/fava/src/lib.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Build an out-of-workspace capability using only public fava</name>
  <files>falsifiers/external-semantic-capability/Cargo.toml, falsifiers/external-semantic-capability/Cargo.lock, falsifiers/external-semantic-capability/src/lib.rs</files>
  <behavior>
    - The crate has `fava` as its sole normal path dependency, plus explicit existing provider/testkit and runtime dev-dependencies sufficient to instantiate public integration actors; it is not a root workspace member or dependency of a selected product assembly.
    - A private applier for an unrelated standard-replaceable future kind consumes public EventEdit/Event values and returns an ordinary unsigned event.
    - Its public helper functions prove empty source, inverse, deterministic adjacent/duplicate edits, unrelated field preservation, and typed bounds/refusal without exporting a new nominal protocol value.
    - The external crate compiles after core is held fixed.
  </behavior>
  <action>Create the independent manifest and local behavior tests first, run its whole lib target RED, then implement a deliberately unrelated standard-replaceable kind using only public `fava` exports. Keep `fava` as the sole normal dependency. Declare the existing `fava-event-cache-memory`, `fava-write-store-memory`, `fava-query-standard`, `fava-signer-local`, `fava-router-testkit`, `fava-transport-testkit`, `fava-delivery-standard`, and `fava-publisher-nip01` crates plus Tokio only under dev-dependencies for integration actor construction. Keep the external decoded state and concrete applier private; expose functions returning approved/existing values. The coordinate must be non-addressable so exact bounded selection is available. Exercise empty, inverse, adjacent insertion/removal, duplicate idempotency, stable ordering, unrelated tag/content preservation, malformed source, and bounds. Do not expand `fava` facade re-exports, add this crate to the root workspace/selected product/vocabulary registry, edit universal-core manifests/source, or modify `crates/` in this plan.</action>
  <verify>
    <automated>EXTERNAL_LIB_LIST="$(cargo test --manifest-path falsifiers/external-semantic-capability/Cargo.toml --lib -- --list)" &amp;&amp; test "$(printf '%s\n' "$EXTERNAL_LIB_LIST" | rg -c '(^|::)(external_first_value_inverse_and_preservation|external_duplicate_adjacent_and_ordering_are_deterministic|external_bounds_and_malformed_source_refuse): test$')" -eq 3 &amp;&amp; cargo test --manifest-path falsifiers/external-semantic-capability/Cargo.toml --lib &amp;&amp; if rg -n 'external-semantic-capability' Cargo.toml crates apps; then exit 1; else exit 0; fi</automated>
  </verify>
  <done>Three required external library tests pass; the crate is independent, public-API-only, absent from product/core graphs, and implements unrelated meaning without a core behavior edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove external public publication and raw future-event preservation</name>
  <files>falsifiers/external-semantic-capability/tests/public_capability.rs</files>
  <behavior>
    - Public Fava first-value publication creates one receipt and event for the external kind; a newer qualified source creates a new RevisionId while preserving unrelated fields.
    - Preview and live initial routing agree with preview producing zero custody/effects.
    - A retired completion is attributable/inert, duplicate invocation has one effect, and applier failure preserves current state.
    - EventBuilder and WriteIntent::event still publish a different arbitrary future kind unchanged without any matching applier.
  </behavior>
  <action>Add the public integration target first and capture RED. Instantiate memory cache/store, evaluator, signer, router, publisher, delivery, and transport actors from the manifest's explicit dev-dependencies, then assemble them and the external applier only through public `FavaBuilder` methods. Do not access publication/store internals or request new `fava` re-exports. Drive fixed-timestamp first value, successor, preview, duplicate/concurrent invocation, retired completion, and failure cases through public methods and receipts. In the same target, construct and publish a distinct arbitrary future kind through the raw event intent with an empty/irrelevant applier selection and assert exact content/tags/kind survive. Use barriers and explicit injected timestamps, never wall-clock sleeps. Confirm only this external test file changes during the task.</action>
  <verify>
    <automated>EXTERNAL_PUBLIC_LIST="$(cargo test --manifest-path falsifiers/external-semantic-capability/Cargo.toml --test public_capability -- --list)" &amp;&amp; test "$(printf '%s\n' "$EXTERNAL_PUBLIC_LIST" | rg -c '(^|::)(external_capability_composes_through_public_fava|external_retired_completion_and_failure_preserve_current|raw_future_event_kind_publishes_unchanged): test$')" -eq 3 &amp;&amp; cargo test --manifest-path falsifiers/external-semantic-capability/Cargo.toml --test public_capability</automated>
  </verify>
  <done>Three guarded external public tests pass; N+1 composes end to end without core edits, and raw arbitrary/future events remain unchanged and publishable.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

Apply OWASP ASVS L1 proportional controls at every boundary below; every listed threat has an explicit severity and disposition.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external crate to public fava | Third-party capability code supplies edits and applier output through the public contract. |
| raw future event to universal publication | Unknown kind/content/tags cross core without protocol interpretation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-25 | Elevation of Privilege | external applier | high | mitigate | Public-contract-only compilation plus output validation and no private/runtime dependencies. |
| T-07-26 | Tampering | unknown future events | high | mitigate | Exact raw-event round-trip/publication assertion; no kind switch or semantic rewrite in core. |
| T-07-27 | Denial of Service | external edit/output | high | mitigate | Existing edit/source/output/admission/evidence bounds and typed refusal. |
| T-07-28 | Repudiation | external late completion | medium | mitigate | Stable receipt and exact RevisionId assertions through public evidence. |
| T-07-SC | Tampering | cargo installs | low | accept | The falsifier uses existing locked workspace dependencies through the public fava path only. |
</threat_model>

<verification>
Run both positively guarded external targets, confirm root/product absence, inspect `cargo metadata --manifest-path` for exactly one normal dependency (`fava`) and the explicit provider/testkit/runtime dev-dependencies, and run `git diff --check`.
</verification>

<success_criteria>
An unrelated external capability composes through public Fava with full lifecycle evidence while universal core stays unchanged and raw future kinds still publish unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-semantic-writes-and-capability-composition/07-07-SUMMARY.md` when done.
</output>
