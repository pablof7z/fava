---
phase: 07-semantic-writes-and-capability-composition
plan: 05
type: execute
wave: 5
depends_on: [07-04]
files_modified:
  - crates/fava-write-store-redb/Cargo.toml
  - crates/fava-write-store-redb/BUILD.bazel
  - crates/fava-write-store-redb/src/lib.rs
  - crates/fava-write-store-redb/src/ops.rs
  - crates/fava-write-store-redb/tests/semantic_write_store.rs
  - crates/fava-write-store-redb/tests/process_kill.rs
autonomous: true
requirements: [CAP-03, CAP-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "redb has behavior parity with memory for one-coordinate admission, atomic generation replacement, bounded retired/failure evidence, retry, settlement, and stale-completion isolation."
    - "The redb semantic schema is hard-versioned; unknown or older schema refuses with no compatibility fallback or partial open."
    - "After process SIGKILL and reopen, a selected applier is established before recovery, the stable receipt/current generation survives, and exactly one eligible successor or failed-source retry resumes."
    - "A killed retired completion, terminal completion, or cancelled edit cannot revive or mutate current state."
    - "No descriptor-like, registry-like, migration-like, compatibility-like, or profile-like persistence abstraction exists."
  artifacts:
    - "crates/fava-write-store-redb/tests/semantic_write_store.rs"
    - "crates/fava-write-store-redb/tests/process_kill.rs"
    - "crates/fava-write-store-redb/src/ops.rs"
  key_links:
    - "Every redb semantic mutation compares current IDs and writes receipt/edit/current/retired/failure facts in one transaction before notification."
    - "FavaBuilder validates selected appliers before Publication::recover reads durable live edits."
---

<objective>
Give the semantic-write state machine durable redb parity and crash recovery.

Purpose: Prove restart cannot lose custody, duplicate effects, retry terminal work, or let a retired completion win.
Output: Hard-versioned redb schema/operations, parity corpus, and real SIGKILL/reopen evidence.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-04-SUMMARY.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-RESEARCH.md
@crates/fava-write-store-redb/src/lib.rs
@crates/fava-write-store-redb/src/ops.rs
@crates/fava-write-store-redb/tests/process_kill.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Implement transactional redb parity with a hard schema version</name>
  <files>crates/fava-write-store-redb/Cargo.toml, crates/fava-write-store-redb/BUILD.bazel, crates/fava-write-store-redb/src/lib.rs, crates/fava-write-store-redb/src/ops.rs, crates/fava-write-store-redb/tests/semantic_write_store.rs</files>
  <behavior>
    - Duplicate and simultaneous exact-coordinate acceptance commit one live edit and one observer effect.
    - Generation replacement, retired evidence, failure set/clear, retry, cancellation, and settlement match the memory corpus and remain within active/text/destination/evidence caps.
    - A stale expected RevisionId/source transaction commits nothing and cannot notify.
    - Schema version mismatch returns the existing redb/store refusal before reading semantic rows; no fallback, migration, alias, or partial compatibility path runs.
    - Reopen returns byte-equivalent current receipt/edit/failure facts.
  </behavior>
  <action>Write the redb-local parity target first and capture RED. Encode the approved edit, current RevisionId/source, bounded retired PublicationEvidence, and bounded failure facts directly in the existing hard-versioned receipt schema; bump the one schema version and refuse every mismatch. Do not add legacy decoders, migrations, optional-table fallbacks, or dual writes. Perform coordinate admission and generation/failure/retired/current receipt changes in one immediate transaction with exact compare-and-set predicates; notify only after commit. Reuse active, terminal, and receipt-text bounds, and consume the fava-write-store destination/evidence capacity function introduced in Plan 02 rather than duplicating its numeric limit. Return existing WriteStoreError values on overflow with no partial row. Keep parity behavior identical to memory, including idempotent duplicate calls and deterministic simultaneous ordering.</action>
  <verify>
    <automated>REDB_LIST="$(cargo test -p fava-write-store-redb --test semantic_write_store -- --list)" &amp;&amp; test "$(printf '%s\n' "$REDB_LIST" | rg -c '(^|::)(redb_coordinate_admission_is_single_owner|redb_generation_and_failure_state_match_memory|redb_stale_and_overflow_mutations_are_atomic_noops|redb_schema_mismatch_refuses_without_fallback): test$')" -eq 4 &amp;&amp; cargo test -p fava-write-store-redb --test semantic_write_store &amp;&amp; cargo test -p fava-write-store-redb --lib</automated>
  </verify>
  <done>Four guarded redb parity tests and redb lib tests pass; transactions match memory behavior, schema mismatch hard-refuses, and no compatibility code exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove SIGKILL recovery, retry, and retired-completion inertness</name>
  <files>crates/fava-write-store-redb/src/lib.rs, crates/fava-write-store-redb/src/ops.rs, crates/fava-write-store-redb/tests/process_kill.rs</files>
  <behavior>
    - A child killed after first revision reopens with the same WriteId, ReceiptId, current RevisionId/event, and one live exact-coordinate owner.
    - A child killed after a newer source is durable resumes exactly one successor generation after the applier selection is validated.
    - A child killed after bounded failure evidence retries that source once and clears failure only on successful atomic install.
    - A retired or terminal completion surviving process death is attributable but cannot replace/revive current or produce a duplicate effect.
  </behavior>
  <action>Extend the existing real subprocess/SIGKILL harness first and capture RED. Add exact kill barriers after edit custody, first install, newer-source durability, failure-evidence commit, retirement, and terminal settlement. On reopen, assemble and validate the selected applier set before invoking Publication recovery; unsupported durable edits must fail assembly through existing BuildError/PublicationError rather than be accepted without a live owner. Recover one task/observation per live receipt up to active capacity, retry the persisted failed source once, consume any newer source once, and preserve terminal/cancelled/retired inertness. Assert durable IDs and public receipt/query evidence across each kill without sleeps or timing guesses.</action>
  <verify>
    <automated>KILL_LIST="$(cargo test -p fava-write-store-redb --test process_kill -- --list)" &amp;&amp; test "$(printf '%s\n' "$KILL_LIST" | rg -c '(^|::)(semantic_first_generation_survives_sigkill|semantic_successor_and_failed_source_resume_once|semantic_retired_and_terminal_work_stays_inert_after_sigkill): test$')" -eq 3 &amp;&amp; cargo test -p fava-write-store-redb --test process_kill</automated>
  </verify>
  <done>Three guarded SIGKILL scenarios pass repeatedly; applier selection precedes recovery, live work resumes once, failure retries once, and stale/terminal work remains inert.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

Apply OWASP ASVS L1 proportional controls at every boundary below; every listed threat has an explicit severity and disposition.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process memory to redb | Live semantic facts cross a crash boundary into durable transactional state. |
| redb file to recovery | Potentially stale, malformed, or wrong-version bytes enter provider reconstruction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-17 | Tampering | redb generation transaction | critical | mitigate | One immediate exact-ID transaction and post-commit notification; stale compare failure is an atomic no-op. |
| T-07-18 | Denial of Service | durable rows/evidence | high | mitigate | Existing active/terminal/text/destination/evidence caps checked before commit. |
| T-07-19 | Repudiation | crash recovery | high | mitigate | Persist stable operation/current/source/retired/failure attribution and assert it after real SIGKILL. |
| T-07-20 | Elevation of Privilege | schema decoding | high | mitigate | Exact hard version, strict decode, no fallback/migration/alias behavior. |
| T-07-SC | Tampering | cargo installs | low | accept | redb is already locked and audited; this plan installs no new package. |
</threat_model>

<verification>
Run both guarded redb targets, the redb library suite, `cargo test -p fava-write-store-redb`, `cargo check --workspace`, and `git diff --check`.
</verification>

<success_criteria>
redb matches memory semantics and real process death cannot lose, duplicate, revive, or misattribute semantic-write work.
</success_criteria>

<output>
Create `.planning/phases/07-semantic-writes-and-capability-composition/07-05-SUMMARY.md` when done.
</output>
