---
phase: 07-semantic-writes-and-capability-composition
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - crates/fava-publication/src/lib.rs
  - crates/fava-publication/src/materialization.rs
  - crates/fava-publication/src/run.rs
  - crates/fava-write-store-memory/src/semantic.rs
  - crates/fava/tests/semantic_write_publication.rs
  - crates/fava/tests/semantic_write_failures.rs
  - crates/fava/BUILD.bazel
autonomous: true
requirements: [CAP-02, CAP-03, CAP-05]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every signing, route, relay-session, attempt, publication, cancellation, panic, and failure completion carries exact operation and MaterializationId correlation."
    - "A completion for a retired generation remains bounded and attributable but cannot change the current generation, route, receipt outcome, or query-visible event."
    - "Materializer error, panic, malformed output, over-size output, timestamp overflow, evidence overflow, and task exhaustion preserve the prior current generation and publish a bounded attributed failure through existing Receipt/PublicationEvidence."
    - "Active semantic tasks cannot exceed write-store admission capacity; semantic completion capacity equals that capacity and per-receipt delivery completion consumes the store-owned destination/evidence capacity primitive."
    - "Cancellation closes observation and live work; panic/failure/cancellation at one receipt cannot terminate or mutate another."
    - "No descriptor-like, registry-like, failure-like, profile-like, compatibility-like, or migration-like new public nominal type exists."
  artifacts:
    - "crates/fava-publication/src/run.rs"
    - "crates/fava/tests/semantic_write_publication.rs"
    - "crates/fava/tests/semantic_write_failures.rs"
  key_links:
    - "Every async completion calls a store compare-and-set mutation with stable receipt plus exact MaterializationId."
    - "Publication catches materializer panic/error/output refusal and commits bounded failure evidence before retry policy proceeds."
    - "Bounded channels and permits derive from `WriteStore::active_capacity()` and the fava-write-store destination/evidence capacity function rather than a duplicated literal or new configuration owner."
---

<objective>
Complete exact generation correlation and isolate every semantic-write failure path.

Purpose: Ensure rematerialization cannot let late or hostile work corrupt current durable/public state.
Output: Bounded completion orchestration, stale-completion evidence, cancellation isolation, and attributed failure/retry behavior.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-03-SUMMARY.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-RESEARCH.md
@crates/fava-publication/src/run.rs
@crates/fava-write-store-memory/src/lib.rs
@crates/fava-publisher/src/lib.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Correlate and retire every asynchronous generation completion</name>
  <files>crates/fava-publication/src/run.rs, crates/fava-publication/src/materialization.rs, crates/fava-write-store-memory/src/semantic.rs, crates/fava/tests/semantic_write_publication.rs</files>
  <behavior>
    - After a source successor installs a new MaterializationId, signer, route, session, attempt, and publisher completions for the retired ID update only its bounded retired evidence.
    - Simultaneous source-successor and completion orderings converge to the same current generation and one effect; duplicate completion is idempotent.
    - Cancelling before, during, or after materialization closes exactly that receipt observation/tasks and late completion is inert; a separate receipt continues.
    - One active semantic runner exists per admitted edit, total runners never exceed store active capacity, and queue exhaustion refuses before custody.
    - The existing unbounded lane-completion channel is replaced by a bounded channel sized from the existing destination capacity; semantic completion capacity equals store active admission capacity.
  </behavior>
  <action>Write the exact interleaving and cancellation tests first and capture target RED. Thread WriteId, ReceiptId, current MaterializationId, generated event ID, source event ID, route revision, RelaySessionKey, and attempt ID through every asynchronous completion. Store mutations must compare exact current identity; a retired completion may update only its already-retired bounded PublicationEvidence and cannot select routes, schedule retry, settle current outcome, or alter query-visible current state. Replace the existing unbounded lane completion queue with `tokio::sync::mpsc::channel` sized by the primitive destination/evidence-capacity function exported by fava-write-store in Plan 02, and await sends with cancellation selection. Publication must not repeat or privately redefine the underlying numeric limit. Size the semantic completion queue and permits exactly to `WriteStore::active_capacity()`, reserve recovery permits before new acceptance, and allow one semantic runner/observation per admitted receipt. Queue/permit exhaustion uses an existing PublicationError before custody. Do not add a channel, capacity, completion, or task configuration nominal type.</action>
  <verify>
    <automated>PUBLICATION_LIST="$(cargo test -p fava --test semantic_write_publication -- --list)" &amp;&amp; test "$(printf '%s\n' "$PUBLICATION_LIST" | rg -c '(^|::)(retired_completion_is_attributable_and_inert|simultaneous_source_and_completion_converge_once|semantic_cancellation_is_scoped_and_late_work_is_inert|semantic_task_and_completion_bounds_refuse_cleanly): test$')" -eq 4 &amp;&amp; cargo test -p fava --test semantic_write_publication &amp;&amp; if rg -n 'mpsc::unbounded_channel' crates/fava-publication/src; then echo 'unbounded publication completion channel remains' &gt;&amp;2; exit 1; else exit 0; fi</automated>
  </verify>
  <done>Four guarded tests pass, the unbounded channel search is empty, active work and completions are concretely bounded, and all retired/cancelled/duplicate completion orderings leave current state unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attribute materializer failure, panic, malformed output, and exhaustion</name>
  <files>crates/fava-publication/src/lib.rs, crates/fava-publication/src/materialization.rs, crates/fava-publication/src/run.rs, crates/fava/tests/semantic_write_failures.rs, crates/fava/BUILD.bazel</files>
  <behavior>
    - Materializer returned error, panic, wrong actor/kind/coordinate, malformed encoding, over-size output, timestamp overflow, retained-evidence overflow, and task/queue exhaustion never replace the prior current generation.
    - Each post-accept failure is visible from public receipt and ordinary query EventRecord publication evidence with bounded source/generation attribution using existing values.
    - A changed qualified source retries immediately; the same failed source does not spin within one run and retries once after recovery.
    - A successful retry atomically installs the successor and clears the failure; repeated failure/success calls have no duplicate notification or delivery effect.
    - Failure or panic for one receipt cannot stop another receipt's observation, signing, routing, delivery, or receipt stream.
  </behavior>
  <action>Add all failure cases first and capture the whole named target RED. Wrap synchronous materializer invocation with panic containment, validate every returned unsigned event before durable installation, and map all provider-controlled text to bounded existing failure evidence. A post-accept failure commits through the write store against the current MaterializationId/source, preserving the prior event/generation; the public Receipt and EventRecord publication field expose that attribution without a new error/evidence wrapper. Suppress an immediate same-source retry to prevent spinning, retry a changed qualified source at once, and leave the failed source eligible for one recovery retry. On successful compare-and-set replacement, clear the failure in that same commit. Exercise malformed output, over-size, checked-timestamp overflow, exhaustion of the store-owned destination/evidence capacity primitive, and active/completion exhaustion. Isolate every spawned unit and cancellation token by receipt; do not let a panic close shared observation or receipt-change delivery.</action>
  <verify>
    <automated>FAILURE_LIST="$(cargo test -p fava --test semantic_write_failures -- --list)" &amp;&amp; test "$(printf '%s\n' "$FAILURE_LIST" | rg -c '(^|::)(materializer_error_is_bounded_and_preserves_current|materializer_panic_is_scoped_and_attributed|malformed_and_oversize_outputs_preserve_current|timestamp_and_evidence_overflow_preserve_current|recovery_retries_failed_source_once|successful_retry_clears_failure_without_duplicate_effect): test$')" -eq 6 &amp;&amp; cargo test -p fava --test semantic_write_failures &amp;&amp; cargo test -p fava-publication</automated>
  </verify>
  <done>Six guarded failure tests and the publication suite pass; every error/panic/malformed/oversize/overflow/exhaustion path is bounded and publicly attributable, preserves current state, and cannot harm another receipt.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

Apply OWASP ASVS L1 proportional controls at every boundary below; every listed threat has an explicit severity and disposition.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spawned task to durable store | Delayed, duplicated, cancelled, or panicking work attempts current-state mutation. |
| provider output to publication | Materializer/signer/router/publisher controlled data and failures enter receipt evidence. |
| bounded queues to runtime | Backpressure and cancellation determine whether work remains attributable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Tampering | late completion | critical | mitigate | Exact operation/generation/source/session/attempt compare-and-set; retired evidence cannot mutate current fields. |
| T-07-14 | Denial of Service | tasks, queues, failure text | high | mitigate | Store-derived admission/queue bounds, destination-cap completion channel, bounded evidence, and no same-source spin. |
| T-07-15 | Repudiation | provider failure/panic | high | mitigate | Catch and persist bounded source/generation attribution in existing public receipt evidence. |
| T-07-16 | Elevation of Privilege | malformed materialized event | high | mitigate | Validate actor, coordinate, encoding, timestamp, and size before store mutation/signing. |
| T-07-SC | Tampering | cargo installs | low | accept | This plan adds no third-party package or dependency. |
</threat_model>

<verification>
Run both guarded public targets, the publication crate suite, the negative unbounded-channel search, `cargo check --workspace`, and `git diff --check`.
</verification>

<success_criteria>
Exact identity makes every stale completion inert; all materializer/runtime failure modes are bounded, durable, publicly attributable, restart-safe, and receipt-scoped.
</success_criteria>

<output>
Create `.planning/phases/07-semantic-writes-and-capability-composition/07-04-SUMMARY.md` when done.
</output>
