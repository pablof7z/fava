---
phase: 07-semantic-writes-and-capability-composition
plan: 09
type: execute
wave: 7
depends_on: [07-08]
files_modified:
  - crates/fava-publication/src/run.rs
  - tools/tests/test_semantic_write_feature.py
  - docs/issues/0010-m7-semantic-writes-and-capability-composition.md
  - .planning/phases/07-semantic-writes-and-capability-composition/07-VALIDATION.md
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03, CAP-04, CAP-05, CAP-06, CAP-07, CAP-08, CAP-09]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A named deliberate break of current-generation correlation makes the mapped retired-completion evidence fail, and restoring the predicate makes it pass."
    - "Every feature scenario resolves to an existing test target and exact listed test name; positive guards prevent a missing/empty target from greening."
    - "Memory, redb, restart/SIGKILL, public publication, preview, failures, NIP-02, bookmarks, external N+1, raw future kinds, dependency negatives, vocabulary, Cargo, and Bazel gates pass together."
    - "Every Rust code file is at most 800 lines; each 501-800 line file has a concrete cohesion reason recorded in the issue and validation evidence."
    - "Public/cross-crate vocabulary contains only approved/existing nouns and no descriptor-like, registry-like, factory-like, profile-like, compatibility-like, migration-like, ContactList, or BookmarkList symbol."
  artifacts:
    - "docs/issues/0010-m7-semantic-writes-and-capability-composition.md"
    - ".planning/phases/07-semantic-writes-and-capability-composition/07-VALIDATION.md"
    - "features/semantic-writes.feature"
  key_links:
    - "The deliberate-break record names the exact source predicate, exact mapped test, observed failure, restoration, and passing rerun."
    - "Final feature mapping verifies every scenario target/name before the full suite can pass."
    - "The line audit enumerates files with rg rather than a shell glob and exits nonzero on any hard-limit breach."
---

<objective>
Close M7 with a deliberate-break proof and one comprehensive falsifiable gate.

Purpose: Demonstrate the invariant tests can actually catch the most dangerous regression and that all CAP-01..09 evidence composes under repository gates.
Output: Deliberate-break record, completed validation matrix, exact test mapping, and passing full validation.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-08-SUMMARY.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-VALIDATION.md
@features/semantic-writes.feature
@docs/issues/0010-m7-semantic-writes-and-capability-composition.md
@crates/fava-publication/src/run.rs
</context>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Replaceable-event edit lifecycle and independently composable protocol crates through public `fava` | 07-01 through 07-09; public tracer in 07-03, protocols/external proof in 07-06/07, composed canaries in 07-08 |
| REQ | CAP-01 bounded persistable edit | 07-01, 07-03, 07-08, 07-09 |
| REQ | CAP-02 first/current source materialization | 07-01 through 07-04, 07-08, 07-09 |
| REQ | CAP-03 stable receipt and exact generations | 07-01 through 07-05, 07-08, 07-09 |
| REQ | CAP-04 source-successor rematerialization | 07-03, 07-08, 07-09 |
| REQ | CAP-05 stale/failure/restart isolation | 07-02 through 07-05, 07-08, 07-09 |
| REQ | CAP-06 NIP-02 follow/unfollow | 07-06, 07-08, 07-09 |
| REQ | CAP-07 public bookmark/unbookmark | 07-06, 07-08, 07-09 |
| REQ | CAP-08 shared contract/composition | 07-06 through 07-09 |
| REQ | CAP-09 external N+1 and raw future kinds | 07-07 through 07-09 |
| RESEARCH | Neutral contract, store ownership, publication observation/preview/tasks, memory/redb parity, protocol private decoding, external falsifier, bounds/failures | 07-01 through 07-07 with composition/final proof in 07-08/09 |
| CONTEXT | Behavior-first RED, first value, inverse, source change, deterministic composition, stable IDs, retired completion, public corpus, dependency negatives, crash recovery, canaries | 07-01 feature/RED; 07-02 through 07-08 implementation/evidence; 07-09 deliberate break/full gate |

Deferred native projections, profiles, authentication, hostile-boundary expansion, and release packaging remain excluded exactly as listed in 07-CONTEXT.md.

<tasks>

<task type="tracer">
  <name>Task 1: Prove the named retired-completion deliberate break fails</name>
  <files>crates/fava-publication/src/run.rs, docs/issues/0010-m7-semantic-writes-and-capability-composition.md</files>
  <behavior>
    - Removing only the current MaterializationId equality predicate from the stale-completion store update makes `retired_completion_is_attributable_and_inert` fail for a current-state mutation.
    - The failure is not compilation-only and the unaffected first-value public tracer remains able to execute.
    - Restoring the exact predicate makes the guarded publication target pass again with no residual source diff from the temporary break.
  </behavior>
  <action>Name the experiment `DELIBERATE_BREAK_M7_STALE_COMPLETION`. Record the pre-break source checksum, temporarily remove only the current MaterializationId equality check at the store-mutation call in publication completion handling, and run the whole `semantic_write_publication` target after positively confirming the named test is listed. Require that the named retired-completion test fails because retired work changed current state; a compile failure or unrelated failure does not count. Restore the predicate with a scoped edit, confirm the source checksum matches the pre-break value, rerun the full target green, and append exact command/result/test/failure/restoration evidence to issue 0010. Do not commit the broken state and do not restore through a destructive Git command.</action>
  <verify>
    <automated>BREAK_LIST="$(cargo test -p fava --test semantic_write_publication -- --list)" &amp;&amp; test "$(printf '%s\n' "$BREAK_LIST" | rg -c '(^|::)retired_completion_is_attributable_and_inert: test$')" -eq 1 &amp;&amp; cargo test -p fava --test semantic_write_publication &amp;&amp; rg -n '^DELIBERATE_BREAK_M7_STALE_COMPLETION: PASS ' docs/issues/0010-m7-semantic-writes-and-capability-composition.md</automated>
  </verify>
  <done>The issue records a behavior-level failure under the exact removed predicate and a green full-target rerun after byte-exact restoration; the working tree contains no deliberate-break source change.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete CAP, dependency, vocabulary, line, Cargo, and Bazel gate</name>
  <files>tools/tests/test_semantic_write_feature.py, docs/issues/0010-m7-semantic-writes-and-capability-composition.md, .planning/phases/07-semantic-writes-and-capability-composition/07-VALIDATION.md</files>
  <action>Strengthen the early feature-mapping test so every scenario now resolves to an existing target file and exact test name reported by that target's `--list` output; preserve the earlier RED record and label the shared corpus as later composition evidence. Run every ownership-split semantic target as a whole named target with a required-name/count guard, then the complete workspace, external falsifier, canary, Bazel, vocabulary, formatting, lint, dependency-negative, and public-vocabulary gates. Enumerate Rust files with `rg --files -g '*.rs'`: make the automated audit exit nonzero for any file above 800 lines; enumerate every 501-800 line file and require its exact path plus a concrete cohesion reason in issue 0010 or 07-VALIDATION. Split files rather than waiving the hard limit. Record each command, positive test count, result, deliberate break, restart/SIGKILL evidence, canary artifact path, dependency falsifier, ASVS L1 threat disposition, and CAP-01..09 mapping in validation/issue evidence. Keep ownership-split tests under 500 lines where feasible and always under 800. Public-declaration checks must reject the prohibited contact/bookmark decoded nouns and adjective-qualified descriptor/registry/factory/profile/compatibility/migration nouns without matching comments.</action>
  <verify>
    <automated>python3 -m unittest tools.tests.test_semantic_write_feature &amp;&amp; CONTRACT_LIST="$(cargo test -p fava --test semantic_write_contract -- --list)" &amp;&amp; test "$(printf '%s\n' "$CONTRACT_LIST" | rg -c ': test$')" -ge 4 &amp;&amp; STORE_LIST="$(cargo test -p fava --test semantic_write_store -- --list)" &amp;&amp; test "$(printf '%s\n' "$STORE_LIST" | rg -c ': test$')" -ge 8 &amp;&amp; PUBLICATION_LIST="$(cargo test -p fava --test semantic_write_publication -- --list)" &amp;&amp; test "$(printf '%s\n' "$PUBLICATION_LIST" | rg -c ': test$')" -ge 11 &amp;&amp; FAILURE_LIST="$(cargo test -p fava --test semantic_write_failures -- --list)" &amp;&amp; test "$(printf '%s\n' "$FAILURE_LIST" | rg -c ': test$')" -ge 6 &amp;&amp; CORPUS_LIST="$(cargo test -p fava --test semantic_write_capabilities -- --list)" &amp;&amp; test "$(printf '%s\n' "$CORPUS_LIST" | rg -c ': test$')" -ge 4 &amp;&amp; cargo test -p fava --test semantic_write_contract &amp;&amp; cargo test -p fava --test semantic_write_store &amp;&amp; cargo test -p fava --test semantic_write_publication &amp;&amp; cargo test -p fava --test semantic_write_failures &amp;&amp; cargo test -p fava --test semantic_write_capabilities &amp;&amp; cargo test --workspace --all-targets &amp;&amp; cargo test --manifest-path falsifiers/external-semantic-capability/Cargo.toml --all-targets &amp;&amp; cargo test --manifest-path apps/canary/Cargo.toml --all-targets &amp;&amp; bazel test //... &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; rg --files -g '*.rs' -0 | xargs -0 wc -l | awk '$2 != "total" &amp;&amp; $1 &gt; 800 { print; bad=1 } END { exit bad }' &amp;&amp; rg --files -g '*.rs' -0 | xargs -0 wc -l | awk '$2 != "total" &amp;&amp; $1 &gt; 500 &amp;&amp; $1 &lt;= 800 { print $2 }' | while IFS= read -r file; do rg -F -- "$file" docs/issues/0010-m7-semantic-writes-and-capability-composition.md .planning/phases/07-semantic-writes-and-capability-composition/07-VALIDATION.md &gt;/dev/null || { echo "missing cohesion reason for $file" &gt;&amp;2; exit 1; }; done &amp;&amp; if rg -n '^[[:space:]]*pub[[:space:]]+(struct|enum|trait|type)[[:space:]]+(ContactList|BookmarkList|[A-Za-z0-9_]*(Descriptor|Registry|Factory|Profile|Compatibility|Migration)[A-Za-z0-9_]*)' crates; then exit 1; else exit 0; fi &amp;&amp; git diff --check</automated>
  </verify>
  <done>Every positively guarded semantic target, full build/test/lint/vocabulary/dependency/canary/Bazel gate, exact feature mapping, hard/soft line audit, ASVS L1 evidence, and CAP-01..09 validation entry passes with no unapproved public vocabulary.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

Apply OWASP ASVS L1 proportional controls at every boundary below; every listed threat has an explicit severity and disposition.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deliberate break to test evidence | A controlled invariant violation must fail behavior and be fully restored. |
| repository artifacts to release claim | Test discovery, build graphs, line limits, vocabulary, and validation records determine whether M7 may be claimed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-33 | Repudiation | deliberate-break evidence | high | mitigate | Named predicate/test, pre/post checksum, observed behavior failure, full-target restoration rerun, and issue record. |
| T-07-34 | Tampering | zero-test/partial gate | high | mitigate | Exact target/name mapping and positive counts before every ownership target/full gate. |
| T-07-35 | Denial of Service | Rust file growth | medium | mitigate | rg-based all-file audit exits nonzero above 800 and requires recorded cohesion at 501-800. |
| T-07-36 | Elevation of Privilege | public vocabulary/dependencies | high | mitigate | Vocabulary checker, declaration-scoped negative symbol gate, Cargo/Bazel dependency falsifiers, external N+1 proof. |
| T-07-SC | Tampering | supply chain | low | accept | No new third-party package is introduced; locked dependency and full build graph gates run. |
</threat_model>

<verification>
The task-2 command is the authoritative phase gate; validation additionally records the four CLI canary runs from Plan 08 and three redb SIGKILL tests from Plan 05.
</verification>

<success_criteria>
The deliberate break proves the critical invariant test is sensitive, every mapped behavior and CAP requirement has executable evidence, all architecture/security/bounds/restart/dependency gates pass, and no Rust file violates repository limits.
</success_criteria>

<output>
Create `.planning/phases/07-semantic-writes-and-capability-composition/07-09-SUMMARY.md` when done.
</output>
