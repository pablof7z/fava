---
phase: 07-semantic-writes-and-capability-composition
plan: 09
type: execute
wave: 7
depends_on: [07-08]
files_modified:
  - crates/fava-write-store/src/lib.rs
  - tools/tests/test_semantic_write_feature.py
  - features/semantic-writes.feature
  - docs/issues/0010-m7-semantic-writes-and-capability-composition.md
  - .planning/phases/07-semantic-writes-and-capability-composition/07-VALIDATION.md
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03, CAP-04, CAP-05, CAP-06, CAP-07, CAP-08, CAP-09]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Removing only the current MaterializationId predicate from write-store validation makes the mapped retired-completion test mutate current state and fail behaviorally; byte-exact restoration makes the whole target pass."
    - "Every feature scenario resolves to an existing target and exact listed test name, including module-qualified names; missing, empty, and zero-test targets fail closed."
    - "Memory, redb, restart/SIGKILL, publication, failure, protocol, shared-corpus, four-CLI-canary, external N+1, raw-future-kind, dependency-negative, vocabulary, Cargo, and Bazel evidence pass together."
    - "A checksum-restored temporary signer reference in a protocol crate fails compilation for the intended undeclared dependency."
    - "Every Rust code file is at most 800 lines; every 501-800 line file has a concrete cohesion reason in the issue or validation record."
    - "Protocol public surfaces contain only the exact approved functions and existing public values; declaration and re-export checks reject unapproved nominal vocabulary while allowing established Nostr Kind::ContactList."
  artifacts:
    - "docs/issues/0010-m7-semantic-writes-and-capability-composition.md"
    - ".planning/phases/07-semantic-writes-and-capability-composition/07-VALIDATION.md"
    - "features/semantic-writes.feature"
    - "tools/tests/test_semantic_write_feature.py"
  key_links:
    - "The stale-completion break targets fava-write-store::validate_current_materialization, not Publication lane bookkeeping."
    - "The final feature mapper validates target existence and exact Cargo --list output before feature evidence passes."
    - "The authoritative gate proves Cargo and Bazel dependency direction and independently validates both standalone workspaces."
---

<objective>
Close M7 with two sensitivity experiments and one comprehensive falsifiable gate.

Purpose: Prove the most dangerous generation and dependency regressions are detected, then show every CAP-01..09 behavior composes under architecture, boundedness, security, and build gates.
Output: Byte-restored deliberate-break records, exact feature mapping, a current validation matrix, and passing full validation.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-08-SUMMARY.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-VALIDATION.md
@features/semantic-writes.feature
@tools/tests/test_semantic_write_feature.py
@docs/issues/0010-m7-semantic-writes-and-capability-composition.md
@crates/fava-write-store/src/lib.rs
@crates/fava/tests/semantic_write_publication/interleavings.rs
@docs/spec/FAVA_REWRITE_IMPLEMENTATION_PLAN.md
</context>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Protocol-owned replaceable-event edits reuse one public Fava write lifecycle and survive changing source state | 07-01 through 07-09; public lifecycle 07-03/04, persistence 07-05, composition 07-06 through 07-08, final sensitivity/gates 07-09 |
| REQ | CAP-01 pure protocol edits and inverses | 07-01, 07-06, 07-08, 07-09 |
| REQ | CAP-02 actor identity exists before materialization and becomes event pubkey | 07-01, 07-03, 07-08, 07-09 |
| REQ | CAP-03 first value materializes without a prior source | 07-01 through 07-03, 07-08, 07-09 |
| REQ | CAP-04 qualified source successor rematerializes and preserves unrelated state | 07-03 through 07-05, 07-08, 07-09 |
| REQ | CAP-05 stable write and receipt across generations/restart | 07-01 through 07-05, 07-08, 07-09 |
| REQ | CAP-06 retired signer/route/delivery completions remain attributable and inert | 07-04, 07-05, 07-08, 07-09 deliberate break |
| REQ | CAP-07 two unrelated protocol crates share the neutral contract | 07-06, 07-08, 07-09 |
| REQ | CAP-08 external N+1 composes without universal-owner edits | 07-07 through 07-09 |
| REQ | CAP-09 raw future kinds cross public Fava without a core kind switch | 07-07 through 07-09 |
| SPEC | Detailed M7 section includes four canaries, including `replaceable-edit-inverse`; global roster omits only that inverse row | 07-08 implements all four; 07-09 records the discrepancy and follows the detailed M7 requirement |
| CONTEXT | Behavior-first RED, exact generations, stable IDs, deterministic composition, dependency negatives, crash recovery, and public canaries | 07-01 through 07-08 implementation/evidence; 07-09 sensitivity and comprehensive gate |

Deferred native projections, profiles, authentication, hostile-boundary expansion, and release packaging remain excluded as listed in 07-CONTEXT.md.

<tasks>

<task type="tracer">
  <name>Task 1: Prove the exact current-generation write-store guard is sensitive</name>
  <files>crates/fava-write-store/src/lib.rs, docs/issues/0010-m7-semantic-writes-and-capability-composition.md</files>
  <behavior>
    - Removing only `receipt.current.publication.materialization_id != materialization_id` from `validate_current_materialization` makes `interleavings::retired_completion_is_attributable_and_inert` fail because generation-one completion changes successor current state.
    - The broken source still compiles and the unaffected first-value public tracer executes; compilation failure or an unrelated failure does not count.
    - Restoring the exact predicate restores the pre-break checksum and makes the complete `semantic_write_publication` target pass.
  </behavior>
  <action>Name the experiment `DELIBERATE_BREAK_M7_STALE_COMPLETION`. Require exactly one source occurrence of the predicate, record the SHA-256 checksum of `crates/fava-write-store/src/lib.rs`, positively list the exact module-qualified test, and run the complete publication target green. Temporarily remove only that predicate from `validate_current_materialization`; do not alter `fava-publication::Publication`, because the test exercises authoritative write-store mutation directly. While broken, run the first-value tracer successfully, then run the exact retired-completion test and require a compiled behavioral assertion failure showing current-state mutation. Restore with a scoped edit, not Git reset/checkout; require the original checksum and zero source diff; rerun the whole target green; append commands, checksums, failure text, counterexample, restoration, and final result to issue 0010. Never commit the broken source.</action>
  <verify>
    <automated>test "$(rg -c 'receipt\.current\.publication\.materialization_id != materialization_id' crates/fava-write-store/src/lib.rs)" -eq 1 &amp;&amp; PUBLICATION_LIST="$(cargo test -p fava --test semantic_write_publication -- --list)" &amp;&amp; test "$(printf '%s\n' "$PUBLICATION_LIST" | rg -c '^interleavings::retired_completion_is_attributable_and_inert: test$')" -eq 1 &amp;&amp; test "$(printf '%s\n' "$PUBLICATION_LIST" | rg -c '^first_value_edit_publishes_through_public_fava: test$')" -eq 1 &amp;&amp; cargo test -p fava --test semantic_write_publication &amp;&amp; git diff --exit-code -- crates/fava-write-store/src/lib.rs &amp;&amp; rg -n '^DELIBERATE_BREAK_M7_STALE_COMPLETION: PASS ' docs/issues/0010-m7-semantic-writes-and-capability-composition.md</automated>
  </verify>
  <done>Issue 0010 contains behavior-level failure evidence for the exact MaterializationId guard and a green full-target rerun after checksum-identical restoration; no deliberate-break source diff remains.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite current validation and run the complete M7 gate</name>
  <files>tools/tests/test_semantic_write_feature.py, features/semantic-writes.feature, docs/issues/0010-m7-semantic-writes-and-capability-composition.md, .planning/phases/07-semantic-writes-and-capability-composition/07-VALIDATION.md</files>
  <behavior>
    - `source-v2 rematerialization` maps to `fava/semantic_write_publication#newer_source_rematerializes_once_and_preserves_unrelated_fields`.
    - `retired completion` maps to `fava/semantic_write_publication#interleavings::retired_completion_is_attributable_and_inert`; the parser accepts one or more `::` components and rejects malformed names.
    - Every mapping resolves its package/target to a real test target and exactly one listed test; nonexistent, empty, duplicate, or zero-test targets fail.
    - All ownership, redb, process-kill, protocol, corpus, external, and canary targets are positively counted; four CLI canaries each produce one fresh bounded JSON evidence bundle.
    - A temporary forbidden signer reference fails for the intended undeclared dependency, then restores the protocol source checksum exactly.
    - `07-VALIDATION.md` contains only current paths, commands/counts, measured results, threats, CAP-01..09 mappings, and final Nyquist state.
  </behavior>
  <action>Strengthen the feature-mapping parser/tests first. Extend only the test-name grammar for module qualification, correct the two stale mappings above, and resolve every package/target through Cargo metadata plus exact `--list` output. Add negative fixtures for nonexistent targets, empty/zero-test output, duplicate names, and malformed `::`; source-file existence alone is insufficient.</action>
  <action>Positively guard and run 4 contract, 8 memory-store, 12 publication, 6 failure, 10 redb recovery, 6 process-kill, 7+1 NIP-02, 9+1 bookmarks, 3+3 external, 4 shared-corpus, and 4 canary-library tests. Require all three semantic SIGKILL, four corpus, and four canary names. Run each exact CLI scenario in its own fresh evidence root; require the printed path, one parseable `manifest.json`, exact scenario ID, Plan 07-08's fixed shape, bounded counts/sizes, and no raw user seed. Record all four paths.</action>
  <action>Name `DELIBERATE_BREAK_M7_PROTOCOL_DEPENDENCY`. Record the checksum of `crates/fava-nip02/src/lib.rs`, temporarily add only `use fava_signer as _deliberate_break_m7_forbidden_dependency;`, and require `cargo check -p fava-nip02 --lib` to fail with the intended unresolved/unlinked `fava_signer` diagnostic. Syntax or unrelated failure does not count. Restore by scoped edit, require the original checksum and zero diff, rerun NIP-02 green, and record commands/output/restoration in issue 0010. Never add the dependency or commit broken source.</action>
  <action>Assert Cargo metadata gives each protocol exactly normal dependencies `fava-state,fava-write`, canary normal dependencies include both protocols, and external normal dependencies equal only `fava`. Reject NIP-02/bookmark kind or crate switches in universal-owner production Rust. Run Cargo-tree and Bazel path negatives for publication, transport, store implementations, signer, router, publisher, delivery, and cache. Independently run build/test/format/strict-Clippy for root, canary, and external workspaces.</action>
  <action>Generate rustdoc JSON and compare every public item, including re-exports, to exact allowlists: NIP-02 root module plus `follow`, `unfollow`, `materializer`; bookmarks root module plus `bookmark_event`, `unbookmark_event`, `bookmark_coordinate`, `unbookmark_coordinate`, `materializer`. Scan public nominal declarations/re-exports for `ContactList`, `BookmarkList`, decoded-list synonyms, and descriptor/registry/factory/profile/compatibility/migration variants. This forbids protocol-owned nominal types, not established Nostr `Kind::ContactList`. Run vocabulary checks.</action>
  <action>Replace stale `07-VALIDATION.md` content with current target paths, exact counts, measured results, both breaks, restart/SIGKILL evidence, canary paths, dependency proofs, ASVS L1 dispositions, and corrected CAP mapping from this plan. Set complete status, `nyquist_compliant: true`, and `wave_0_complete: true` only after all gates pass. Record `SPEC_DISCREPANCY_M7_CANARY_ROSTER: RECORDED` in issue 0010: detailed M7 requires `replaceable-edit-inverse`, global roster omits it, and all four follow the detailed section. Audit all Rust files; fail above 800 and require path/cohesion evidence for 501-800. Check phase range `6fe21f745297b4af414e52269c3ae1c813cbf28f`, current changes, and no temporary/unexpected residue.</action>
  <verify>
    <automated>
set -euo pipefail
python3 -m unittest tools.tests.test_semantic_write_feature

CONTRACT_LIST="$(cargo test -p fava --test semantic_write_contract -- --list)"
STORE_LIST="$(cargo test -p fava --test semantic_write_store -- --list)"
PUBLICATION_LIST="$(cargo test -p fava --test semantic_write_publication -- --list)"
FAILURE_LIST="$(cargo test -p fava --test semantic_write_failures -- --list)"
REDB_LIST="$(cargo test -p fava-write-store-redb --test semantic_write_store -- --list)"
KILL_LIST="$(cargo test -p fava-write-store-redb --test process_kill -- --list)"
NIP02_LIST="$(cargo test -p fava-nip02 --lib -- --list)"
NIP02_API_LIST="$(cargo test -p fava-nip02 --test public_api -- --list)"
BOOKMARK_LIST="$(cargo test -p fava-bookmarks --lib -- --list)"
BOOKMARK_API_LIST="$(cargo test -p fava-bookmarks --test public_api -- --list)"
EXTERNAL_LIB_LIST="$(cargo test --manifest-path falsifiers/external-semantic-capability/Cargo.toml --lib -- --list)"
EXTERNAL_PUBLIC_LIST="$(cargo test --manifest-path falsifiers/external-semantic-capability/Cargo.toml --test public_capability -- --list)"
CORPUS_LIST="$(cargo test -p fava --test semantic_write_capabilities -- --list)"
CANARY_LIST="$(cargo test --manifest-path apps/canary/Cargo.toml --lib -- --list)"

test "$(printf '%s\n' "$CONTRACT_LIST" | rg -c ': test$')" -eq 4
test "$(printf '%s\n' "$STORE_LIST" | rg -c ': test$')" -eq 8
test "$(printf '%s\n' "$PUBLICATION_LIST" | rg -c ': test$')" -eq 12
test "$(printf '%s\n' "$FAILURE_LIST" | rg -c ': test$')" -eq 6
test "$(printf '%s\n' "$REDB_LIST" | rg -c ': test$')" -eq 10
test "$(printf '%s\n' "$KILL_LIST" | rg -c ': test$')" -eq 6
test "$(printf '%s\n' "$KILL_LIST" | rg -c '^semantic::(semantic_first_generation_survives_sigkill|semantic_successor_and_failed_source_resume_once|semantic_retired_and_terminal_work_stays_inert_after_sigkill): test$')" -eq 3
test "$(printf '%s\n' "$NIP02_LIST" | rg -c ': test$')" -eq 7
test "$(printf '%s\n' "$NIP02_API_LIST" | rg -c '^external_surface_uses_only_approved_functions_and_types: test$')" -eq 1
test "$(printf '%s\n' "$BOOKMARK_LIST" | rg -c ': test$')" -eq 9
test "$(printf '%s\n' "$BOOKMARK_API_LIST" | rg -c '^external_surface_uses_only_approved_functions_and_types: test$')" -eq 1
test "$(printf '%s\n' "$EXTERNAL_LIB_LIST" | rg -c ': test$')" -eq 3
test "$(printf '%s\n' "$EXTERNAL_PUBLIC_LIST" | rg -c ': test$')" -eq 3
test "$(printf '%s\n' "$CORPUS_LIST" | rg -c '(^|::)(nip02_passes_public_semantic_write_corpus|bookmarks_pass_public_semantic_write_corpus|capabilities_share_preview_bounds_and_failure_behavior|capabilities_share_concurrency_and_retired_completion_behavior): test$')" -eq 4
test "$(printf '%s\n' "$CANARY_LIST" | rg -c '(^|::)(replaceable_edit_first_value_records_materialization|replaceable_edit_rematerialization_records_retired_inertness|replaceable_edit_inverse_covers_both_capabilities|protocol_crate_n_plus_one_records_external_and_raw_proofs): test$')" -eq 4

cargo test -p fava --test semantic_write_contract
cargo test -p fava --test semantic_write_store
cargo test -p fava --test semantic_write_publication
cargo test -p fava --test semantic_write_failures
cargo test -p fava-write-store-redb --test semantic_write_store
cargo test -p fava-write-store-redb --test process_kill
cargo test -p fava-nip02 --all-targets
cargo test -p fava-bookmarks --all-targets
cargo test -p fava --test semantic_write_capabilities
cargo test --manifest-path falsifiers/external-semantic-capability/Cargo.toml --all-targets
cargo test --manifest-path apps/canary/Cargo.toml --all-targets

CANARY_IDS="$(cargo run --manifest-path apps/canary/Cargo.toml -- list)"
test "$(printf '%s\n' "$CANARY_IDS" | rg -c '^(replaceable-edit-first-value|replaceable-edit-rematerialization|replaceable-edit-inverse|protocol-crate-n-plus-one)[[:space:]]+M7[[:space:]]+enabled$')" -eq 4
M7_EVIDENCE="$(mktemp -d)"
for scenario in replaceable-edit-first-value replaceable-edit-rematerialization replaceable-edit-inverse protocol-crate-n-plus-one; do
  output="$(cargo run --manifest-path apps/canary/Cargo.toml -- run "$scenario" --seed "m7-$scenario" --runs-dir "$M7_EVIDENCE/$scenario")"
  evidence="$(printf '%s\n' "$output" | sed -n 's/^evidence: //p')"
  test -n "$evidence"
  test -f "$evidence/manifest.json"
  jq -e --arg scenario "$scenario" '.scenario == $scenario' "$evidence/manifest.json" &gt;/dev/null
done
test "$(find "$M7_EVIDENCE" -name manifest.json -type f | wc -l | tr -d ' ')" -eq 4

ROOT_METADATA="$(cargo metadata --no-deps --format-version 1)"
test "$(printf '%s' "$ROOT_METADATA" | jq -r '[.packages[] | select(.name == "fava-nip02") | .dependencies[] | select(.kind == null) | .name] | sort | join(",")')" = 'fava-state,fava-write'
test "$(printf '%s' "$ROOT_METADATA" | jq -r '[.packages[] | select(.name == "fava-bookmarks") | .dependencies[] | select(.kind == null) | .name] | sort | join(",")')" = 'fava-state,fava-write'
CANARY_METADATA="$(cargo metadata --manifest-path apps/canary/Cargo.toml --no-deps --format-version 1)"
test "$(printf '%s' "$CANARY_METADATA" | jq -r '[.packages[] | select(.name == "canary") | .dependencies[] | select(.kind == null and (.name == "fava-nip02" or .name == "fava-bookmarks")) | .name] | sort | join(",")')" = 'fava-bookmarks,fava-nip02'
EXTERNAL_METADATA="$(cargo metadata --manifest-path falsifiers/external-semantic-capability/Cargo.toml --no-deps --format-version 1)"
test "$(printf '%s' "$EXTERNAL_METADATA" | jq -r '[.packages[] | select(.name == "fava-external-semantic-capability-proof") | .dependencies[] | select(.kind == null) | .name] | sort | join(",")')" = 'fava'
if rg -n 'Kind::(ContactList|BookmarkList)|fava[_-](nip02|bookmarks)' crates -g '*.rs' -g '!fava-nip02/**' -g '!fava-bookmarks/**' -g '!**/tests/**'; then exit 1; fi
if cargo tree -p fava-nip02 -e normal | rg 'fava-(publication|transport|write-store-memory|write-store-redb|event-cache-memory|signer|router|routing|publisher|delivery)'; then exit 1; fi
if cargo tree -p fava-bookmarks -e normal | rg 'fava-(publication|transport|write-store-memory|write-store-redb|event-cache-memory|signer|router|routing|publisher|delivery)'; then exit 1; fi
FORBIDDEN_BAZEL='set(//crates/fava-publication:lib //crates/fava-transport:lib //crates/fava-write-store-memory:lib //crates/fava-write-store-redb:lib //crates/fava-event-cache-memory:lib //crates/fava-signer-local:lib //crates/fava-router-app-relays:lib //crates/fava-router-fallback-relays:lib //crates/fava-router-hints:lib //crates/fava-router-outbox:lib //crates/fava-router-testkit:lib //crates/fava-publisher-nip01:lib //crates/fava-delivery-standard:lib)'
test -z "$(bazel query "somepath(//crates/fava-nip02:lib, $FORBIDDEN_BAZEL)")"
test -z "$(bazel query "somepath(//crates/fava-bookmarks:lib, $FORBIDDEN_BAZEL)")"

RUSTC_BOOTSTRAP=1 cargo rustdoc -p fava-nip02 --lib -- -Z unstable-options --output-format json
RUSTC_BOOTSTRAP=1 cargo rustdoc -p fava-bookmarks --lib -- -Z unstable-options --output-format json
NIP02_PUBLIC="$(jq -r '.index | to_entries[] | select(.value.visibility == "public") | "\(.value.name):\(.value.inner | keys[0])"' target/doc/fava_nip02.json | sort | paste -sd, -)"
BOOKMARK_PUBLIC="$(jq -r '.index | to_entries[] | select(.value.visibility == "public") | "\(.value.name):\(.value.inner | keys[0])"' target/doc/fava_bookmarks.json | sort | paste -sd, -)"
test "$NIP02_PUBLIC" = 'fava_nip02:module,follow:function,materializer:function,unfollow:function'
test "$BOOKMARK_PUBLIC" = 'bookmark_coordinate:function,bookmark_event:function,fava_bookmarks:module,materializer:function,unbookmark_coordinate:function,unbookmark_event:function'
if rg -n '^[[:space:]]*pub(\([^)]*\))?[[:space:]]+(struct|enum|trait|type)[[:space:]]+(ContactList|BookmarkList|[A-Za-z0-9_]*(Descriptor|Registry|Factory|Profile|Compatibility|Migration)[A-Za-z0-9_]*)|^[[:space:]]*pub(\([^)]*\))?[[:space:]]+use[^;]*(ContactList|BookmarkList|Descriptor|Registry|Factory|Profile|Compatibility|Migration)' crates/fava-nip02/src crates/fava-bookmarks/src; then exit 1; fi

python3 tools/check_vocabulary.py
python3 -m unittest tools.tests.test_vocabulary_check
cargo build --workspace
cargo check --workspace --all-targets
cargo test --workspace --all-targets
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo fmt --manifest-path apps/canary/Cargo.toml --all -- --check
cargo clippy --manifest-path apps/canary/Cargo.toml --all-targets -- -D warnings
cargo fmt --manifest-path falsifiers/external-semantic-capability/Cargo.toml --all -- --check
cargo clippy --manifest-path falsifiers/external-semantic-capability/Cargo.toml --all-targets -- -D warnings
bazel test //...

rg --files -g '*.rs' -0 | xargs -0 wc -l | awk '$2 != "total" &amp;&amp; $1 &gt; 800 { print; bad=1 } END { exit bad }'
rg --files -g '*.rs' -0 | xargs -0 wc -l | awk '$2 != "total" &amp;&amp; $1 &gt; 500 &amp;&amp; $1 &lt;= 800 { print $2 }' | while IFS= read -r file; do rg -F -- "$file" docs/issues/0010-m7-semantic-writes-and-capability-composition.md .planning/phases/07-semantic-writes-and-capability-composition/07-VALIDATION.md &gt;/dev/null || { echo "missing cohesion reason for $file" &gt;&amp;2; exit 1; }; done
git diff --exit-code -- crates/fava-write-store/src/lib.rs crates/fava-nip02/src/lib.rs
git diff --check 6fe21f745297b4af414e52269c3ae1c813cbf28f..HEAD
git diff --check 6fe21f745297b4af414e52269c3ae1c813cbf28f
rg -n '^DELIBERATE_BREAK_M7_STALE_COMPLETION: PASS ' docs/issues/0010-m7-semantic-writes-and-capability-composition.md
rg -n '^DELIBERATE_BREAK_M7_PROTOCOL_DEPENDENCY: PASS ' docs/issues/0010-m7-semantic-writes-and-capability-composition.md
rg -n '^SPEC_DISCREPANCY_M7_CANARY_ROSTER: RECORDED ' docs/issues/0010-m7-semantic-writes-and-capability-composition.md
test "$(rg -c '^nyquist_compliant: true$|^wave_0_complete: true$' .planning/phases/07-semantic-writes-and-capability-composition/07-VALIDATION.md)" -eq 2
    </automated>
  </verify>
  <done>All mapped and guarded M7 behavior, crash, protocol, corpus, CLI, external, dependency, vocabulary, standalone-workspace, Cargo, Bazel, line, and phase-range gates pass; both temporary mutations are checksum-restored; validation and issue records are complete.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

Apply OWASP ASVS L1 proportional controls at every boundary below.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deliberate break to test evidence | Controlled generation and dependency violations must fail for the intended reason and restore byte-exactly. |
| feature prose to executable evidence | Each scenario must map to one real Cargo target and exact listed test. |
| protocol/product manifests to universal owners | Selected capabilities compose while protocol crates cannot reach lifecycle/provider owners. |
| canary CLI to evidence filesystem | Four executions produce separate bounded, attributable, parseable artifacts. |
| repository artifacts to milestone claim | Discovery, graphs, limits, vocabulary, phase range, and validation determine whether M7 may be claimed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-33 | Repudiation | stale-completion break | high | mitigate | Exact predicate/test, checksum, state counterexample, restoration rerun, issue record. |
| T-07-34 | Tampering | zero-test/stale mapping | high | mitigate | Target resolution, exact module-qualified discovery, negative fixtures, positive counts. |
| T-07-35 | Denial of Service | source/evidence growth | medium | mitigate | All-file limits, cohesion evidence, fixed bounded canary artifacts. |
| T-07-36 | Elevation of Privilege | protocol dependencies | high | mitigate | Compile-negative signer reference, metadata, Cargo tree, Bazel path negatives. |
| T-07-37 | Elevation of Privilege | public vocabulary | high | mitigate | Exact rustdoc allowlists including re-exports, declaration scans, vocabulary checker. |
| T-07-38 | Repudiation | milestone record | medium | mitigate | Correct CAP map, spec discrepancy, ASVS dispositions, phase-range and clean-state checks. |
| T-07-SC | Tampering | supply chain | low | accept | No new third-party package; locked root/canary/external graphs pass independent quality gates. |
</threat_model>

<verification>
Task 2 is the authoritative phase gate. After its commit and before the summary, rerun `git diff --check 6fe21f745297b4af414e52269c3ae1c813cbf28f..HEAD` and require `git status --porcelain --untracked-files=all` empty except orchestrator-owned `.gsd/` and `.planning/milestone.lock`. Validation records four fresh CLI evidence paths, all six process-kill tests including three semantic SIGKILL cases, both restored breaks, and the exact phase-range HEAD.
</verification>

<success_criteria>
Both exact falsifiers fail for intended reasons and restore byte-exactly; every CAP-01..09 behavior has current executable evidence; four canaries and both standalone workspaces pass; architecture, vocabulary, bounds, security, build, and clean-range gates pass.
</success_criteria>

<output>
Create `.planning/phases/07-semantic-writes-and-capability-composition/07-09-SUMMARY.md` when done.
</output>
