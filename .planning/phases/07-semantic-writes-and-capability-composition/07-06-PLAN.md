---
phase: 07-semantic-writes-and-capability-composition
plan: 06
type: execute
wave: 5
depends_on: [07-04]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/fava-nip02/Cargo.toml
  - crates/fava-nip02/BUILD.bazel
  - crates/fava-nip02/src/lib.rs
  - crates/fava-bookmarks/Cargo.toml
  - crates/fava-bookmarks/BUILD.bazel
  - crates/fava-bookmarks/src/lib.rs
  - docs/internals/vocabulary.toml
autonomous: true
requirements: [CAP-06, CAP-07, CAP-08]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "fava-nip02 provides public follow/unfollow functions and a selected materializer through approved/existing values only; source decoding stays private."
    - "fava-bookmarks provides public public-bookmark/unbookmark functions and a selected materializer through approved/existing values only; private encryption is absent."
    - "Both crates handle empty source, inverse, duplicate invocation, adjacent insertion/removal, deterministic ordering, bounded input/output, unrelated tag/content preservation, and qualified-source refusal."
    - "Neither protocol crate signs, routes, publishes, delivers, owns receipts/runtime/store/cache, or depends on concrete provider crates."
    - "No public ContactList, BookmarkList, decoded-list, descriptor-like, registry-like, factory-like, profile-like, compatibility-like, or migration-like nominal symbol exists."
  artifacts:
    - "crates/fava-nip02/src/lib.rs"
    - "crates/fava-bookmarks/src/lib.rs"
    - "docs/internals/vocabulary.toml"
  key_links:
    - "Each public helper produces ReplaceableEventEdit and each public materializer function returns the approved trait object for FavaBuilder selection."
    - "Both Cargo and Bazel graphs point only toward domain values/neutral contracts and away from publication/runtime/provider implementations."
---

<objective>
Implement the two unrelated protocol capabilities against the same neutral semantic-write contract.

Purpose: Challenge the contract with real NIP-02 and public NIP-51 meaning without leaking protocol structure into core.
Output: `fava-nip02` and `fava-bookmarks` crates with local behavior evidence and dependency gates.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-04-SUMMARY.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-RESEARCH.md
@docs/issues/0011-m7-semantic-write-vocabulary.md
@docs/internals/vocabulary.toml
@crates/fava-nip65/src/lib.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Implement NIP-02 follow and unfollow as private decoding plus public functions</name>
  <files>Cargo.toml, Cargo.lock, crates/fava-nip02/Cargo.toml, crates/fava-nip02/BUILD.bazel, crates/fava-nip02/src/lib.rs</files>
  <behavior>
    - follow on empty source creates a kind-3 event for the actor with exactly the requested p-tag; unfollow is its bounded inverse.
    - follow/unfollow preserves content and unrelated/unknown tags, is idempotent under duplicate invocation, and handles adjacent first/middle/last tags and empty result.
    - Output ordering is deterministic for equivalent input, including duplicate/malformed relevant tags and equal source ordering.
    - Wrong actor/kind, malformed source, edit decode failure, input/output over-size, inverse overflow, and timestamp overflow return existing WriteIntentError values.
    - Public exports are functions plus approved/existing values; decoded contact-list representation and concrete materializer implementation remain private.
  </behavior>
  <action>Create the crate metadata and protocol-local tests first, then run the whole lib target RED before implementation. Publicly expose only narrow `follow`, `unfollow`, and materializer-selection functions whose arguments/results are existing PublicKey, ReplaceableEventEdit, ReplaceableEventMaterializer trait object, and existing error values. Keep the decoded kind-3 list and concrete materializer struct private; specifically do not export `ContactList`, any alternate decoded-list noun, or a protocol descriptor/factory. Decode bounded source tags, preserve content and every unrelated/unknown tag byte-for-byte, canonicalize only the affected p-tag ordering deterministically, make duplicate calls idempotent, and support empty/adjacent removal. Validate actor/kind/coordinate and exact injected timestamp, return an unsigned event, and never sign, route, publish, access stores/caches, or own lifecycle.</action>
  <verify>
    <automated>NIP02_LIST="$(cargo test -p fava-nip02 --lib -- --list)" &amp;&amp; test "$(printf '%s\n' "$NIP02_LIST" | rg -c '(^|::)(follow_empty_and_unfollow_inverse|follow_preserves_unrelated_state_and_orders_deterministically|follow_duplicate_and_adjacent_edits_are_idempotent|follow_bounds_and_invalid_sources_are_typed_refusals): test$')" -eq 4 &amp;&amp; cargo test -p fava-nip02 --lib</automated>
  </verify>
  <done>Four required NIP-02 tests are listed and pass; public functions yield bounded edits/materializer selection, private decoding preserves unrelated state, and inverse/idempotency/ordering/refusal cases are proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement public NIP-51 bookmark and unbookmark on the same contract</name>
  <files>Cargo.toml, Cargo.lock, crates/fava-bookmarks/Cargo.toml, crates/fava-bookmarks/BUILD.bazel, crates/fava-bookmarks/src/lib.rs</files>
  <behavior>
    - Bookmark on empty source creates the public standard-replaceable bookmark event for the actor; unbookmark is its bounded inverse.
    - Event-id and address-coordinate bookmark functions preserve content and unrelated/unknown tags, are duplicate-idempotent, and handle adjacent first/middle/last removal and empty result.
    - Equivalent sets produce deterministic tag ordering without discarding unknown future tags.
    - Private encrypted bookmark content is neither read nor written; wrong actor/kind, malformed source, bounds, output, inverse, and timestamp overflow use existing typed refusals.
    - Public exports are functions plus approved/existing values; decoded bookmark-list representation and concrete materializer implementation remain private.
  </behavior>
  <action>Create crate metadata and public-bookmark local tests first, then run the whole lib target RED. Publicly expose only functions for bookmark/unbookmark of existing EventId and EventCoordinate values plus materializer selection; return approved ReplaceableEventEdit/ReplaceableEventMaterializer and existing errors. Keep the decoded list and concrete implementation private; specifically do not export `BookmarkList`, encryption state, decoded-list wrappers, descriptors, or factories. Implement public NIP-51 tags only, preserve content and unrelated/unknown/future tags, deterministically order the affected public bookmark tags, make duplicates idempotent, support empty/adjacent removal, and refuse malformed/bounds/overflow cases. Do not implement or silently pass through private encrypted bookmark ownership in this phase.</action>
  <verify>
    <automated>BOOKMARK_LIST="$(cargo test -p fava-bookmarks --lib -- --list)" &amp;&amp; test "$(printf '%s\n' "$BOOKMARK_LIST" | rg -c '(^|::)(bookmark_empty_and_unbookmark_inverse|bookmark_preserves_unrelated_state_and_orders_deterministically|bookmark_duplicate_and_adjacent_edits_are_idempotent|bookmark_bounds_private_and_invalid_sources_are_typed_refusals): test$')" -eq 4 &amp;&amp; cargo test -p fava-bookmarks --lib</automated>
  </verify>
  <done>Four required public-bookmark tests are listed and pass; public functions use the same neutral contract, private encryption is absent, and inverse/idempotency/ordering/preservation/refusal cases are proven.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce closed public vocabulary and protocol dependency direction</name>
  <files>docs/internals/vocabulary.toml, crates/fava-nip02/src/lib.rs, crates/fava-bookmarks/src/lib.rs</files>
  <action>Register only the two already-approved crate names under their existing Nostr concepts. Keep concrete decoded structs and materializer implementations private and public API limited to functions plus approved/existing values. Add compile-time/private-surface assertions or rustdoc/public-item checks that prohibit public `ContactList`, `BookmarkList`, decoded list, descriptor, registry, factory, profile, compatibility, and migration nominal symbols. Use Cargo tree and Bazel query to prove both protocol crates depend on neutral/domain crates only and have no path to fava-publication, fava-runtime, fava-transport, fava-write-store-memory, fava-write-store-redb, signer, router, publisher, delivery, or cache implementations.</action>
  <verify>
    <automated>python3 tools/check_vocabulary.py &amp;&amp; python3 -m unittest tools.tests.test_vocabulary_check &amp;&amp; cargo test -p fava-nip02 --lib &amp;&amp; cargo test -p fava-bookmarks --lib &amp;&amp; if cargo tree -p fava-nip02 -e normal | rg 'fava-(publication|runtime|transport|write-store-memory|write-store-redb|event-cache-memory|signer|routing|publisher|delivery)'; then exit 1; else exit 0; fi &amp;&amp; if cargo tree -p fava-bookmarks -e normal | rg 'fava-(publication|runtime|transport|write-store-memory|write-store-redb|event-cache-memory|signer|routing|publisher|delivery)'; then exit 1; else exit 0; fi &amp;&amp; test -z "$(bazel query 'somepath(//crates/fava-nip02:lib, set(//crates/fava-publication:lib //crates/fava-write-store-redb:lib))')" &amp;&amp; test -z "$(bazel query 'somepath(//crates/fava-bookmarks:lib, set(//crates/fava-publication:lib //crates/fava-write-store-redb:lib))')"</automated>
  </verify>
  <done>Vocabulary/unit gates and both local suites pass; prohibited public nominal symbols are absent and both dependency graphs point only to permitted neutral/domain crates.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

Apply OWASP ASVS L1 proportional controls at every boundary below; every listed threat has an explicit severity and disposition.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source event to protocol decoder | Arbitrary tags/content and future protocol fields enter capability-specific parsing. |
| public helper to neutral edit | Application-selected actors/targets become persistable opaque edit bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-21 | Tampering | NIP-02/NIP-51 decode and rewrite | high | mitigate | Strict actor/kind validation, bounded decode, deterministic affected-tag edit, byte-preserving unrelated fields. |
| T-07-22 | Denial of Service | tag collections/edit output | high | mitigate | Existing input/edit/output bounds and typed refusal for overflow before allocation growth. |
| T-07-23 | Elevation of Privilege | protocol dependencies | high | mitigate | Negative Cargo/Bazel dependency proofs and private concrete implementations prevent lifecycle access. |
| T-07-24 | Information Disclosure | private bookmarks | high | mitigate | Public-only implementation; no encrypted content parsing, mutation, key access, or claim. |
| T-07-SC | Tampering | cargo installs | low | accept | Crates use existing locked workspace dependencies only; no third-party install is added. |
</threat_model>

<verification>
Run both positively guarded protocol suites, vocabulary checks, Cargo dependency negatives, Bazel query negatives, `cargo check --workspace`, and `git diff --check`.
</verification>

<success_criteria>
NIP-02 and public bookmarks independently implement the same bounded contract with private decoding, deterministic composition, correct inverses, closed vocabulary, and no lifecycle dependencies.
</success_criteria>

<output>
Create `.planning/phases/07-semantic-writes-and-capability-composition/07-06-SUMMARY.md` when done.
</output>
