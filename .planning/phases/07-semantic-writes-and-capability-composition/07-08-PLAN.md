---
phase: 07-semantic-writes-and-capability-composition
plan: 08
type: execute
wave: 6
depends_on: [07-05, 07-06, 07-07]
files_modified:
  - crates/fava/Cargo.toml
  - crates/fava/BUILD.bazel
  - crates/fava/tests/semantic_write_capabilities.rs
  - apps/canary/Cargo.toml
  - apps/canary/Cargo.lock
  - apps/canary/scenarios.json
  - apps/canary/src/lib.rs
  - apps/canary/src/semantic_writes.rs
  - apps/canary/src/main.rs
  - apps/canary/README.md
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03, CAP-04, CAP-05, CAP-06, CAP-07, CAP-08, CAP-09]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One shared public corpus proves NIP-02 and public bookmarks satisfy identical first-value, inverse, source-change, composition, bounds, preview, stable-receipt, and stale-completion behavior."
    - "The shared corpus is composition evidence over already-RED/green contract, store, publication, failure, protocol, and external targets; it is not substituted for their earlier RED proof."
    - "Four enabled M7 canaries run through public fava: replaceable-edit-first-value, replaceable-edit-reapplication, replaceable-edit-inverse, and protocol-crate-n-plus-one."
    - "Canary evidence records exact events, RevisionIds, stable receipt IDs, source IDs, route/publication facts, bounds/refusals, and no duplicate effects without sleep-based timing."
    - "No descriptor-like, registry-like, profile-like, compatibility-like, or migration-like public abstraction is introduced."
  artifacts:
    - "crates/fava/tests/semantic_write_capabilities.rs"
    - "apps/canary/src/semantic_writes.rs"
    - "apps/canary/scenarios.json"
  key_links:
    - "The same corpus function accepts each public protocol helper/applier pair without core kind branches."
    - "Canary CLI scenario IDs dispatch to public-facade executions and write bounded evidence directories."
---

<objective>
Compose both capabilities under one public corpus and expose the four M7 canary proofs.

Purpose: Turn component evidence into externally runnable milestone behavior without hiding protocol-specific branches in core.
Output: Shared capability corpus, enabled canary implementations, CLI wiring, and durable evidence artifacts.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-05-SUMMARY.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-06-SUMMARY.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-07-SUMMARY.md
@features/semantic-writes.feature
@apps/canary/src/lib.rs
@apps/canary/scenarios.json
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Run NIP-02 and bookmarks through one public conformance corpus</name>
  <files>crates/fava/Cargo.toml, crates/fava/BUILD.bazel, crates/fava/tests/semantic_write_capabilities.rs</files>
  <behavior>
    - Both capability rows pass first value, inverse, empty collection, adjacent edits, duplicate invocation, deterministic ordering, source successor, removal, unrelated preservation, bounds, preview/live parity, and stable receipt/new RevisionId.
    - Both rows prove simultaneous source/completion ordering and retired completion inertness with one observable effect.
    - Unsupported/addressable edit, empty selection, duplicate/overflow selection, malformed/oversize output, and evidence exhaustion use existing typed refusals.
    - Protocol selection is data supplied by each row; core contains no NIP-02/bookmark kind switch.
  </behavior>
  <action>Add one public parameterized composition target after the earlier behavior-first targets are green. This corpus consolidates evidence; do not treat its initial failure as the phase's RED record or weaken the earlier target-specific proofs. Feed each row public helper functions and approved EditApplier trait objects, then exercise the complete identical lifecycle through public Fava with fixed timestamps and explicit barriers. Include empty/adjacent/duplicate/equal-ordering cases, bounds and existing typed refusals, one-effect concurrency, source removal, unrelated field preservation, preview/live route parity, stable IDs, current/retired attribution, and failure evidence. Assert the universal core does not branch on either protocol kind.</action>
  <verify>
    <automated>CORPUS_LIST="$(cargo test -p fava --test semantic_write_capabilities -- --list)" &amp;&amp; test "$(printf '%s\n' "$CORPUS_LIST" | rg -c '(^|::)(nip02_passes_public_semantic_write_corpus|bookmarks_pass_public_semantic_write_corpus|capabilities_share_preview_bounds_and_failure_behavior|capabilities_share_concurrency_and_retired_completion_behavior): test$')" -eq 4 &amp;&amp; cargo test -p fava --test semantic_write_capabilities</automated>
  </verify>
  <done>Four guarded corpus tests pass for both capabilities across the complete shared lifecycle, with no core kind branch and without replacing earlier RED evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement four deterministic M7 canary executions and evidence</name>
  <files>apps/canary/Cargo.toml, apps/canary/Cargo.lock, apps/canary/scenarios.json, apps/canary/src/lib.rs, apps/canary/src/semantic_writes.rs</files>
  <behavior>
    - `replaceable-edit-first-value` records no prior, one applied/published event, stable receipt, exact route, and public query evidence.
    - `replaceable-edit-reapplication` records qualified source successor, new RevisionId, preserved unrelated fields, retired completion inertness, and one effect.
    - `replaceable-edit-inverse` records follow/unfollow and bookmark/unbookmark returning to the expected public state, including empty/adjacent cases.
    - `protocol-crate-n-plus-one` invokes the external falsifier and raw future-kind proof without a core/product dependency.
    - Every canary uses exact injected timestamps/barriers, bounded evidence, and fails rather than silently skipping an unmet deterministic prerequisite.
  </behavior>
  <action>Add the four enabled M7 entries and library tests first, run the canary lib target RED, then implement each scenario through public fava. Reuse the existing scripted signer/router/publisher/transport and evidence-directory conventions, but add exact revision/source/current/retired/failure facts required to distinguish a real lifecycle from compilation. Use fixed timestamps, explicit source injection, and synchronization barriers; no wall-clock sleeps. Cover first value, source successor, stable receipt/new RevisionId, retired completion, inverse for both capabilities, duplicate/ordering/empty bounds, external N+1, and raw future kinds. Keep scenario evidence bounded with existing canary artifact limits and fail on missing prerequisites.</action>
  <verify>
    <automated>CANARY_LIST="$(cargo test --manifest-path apps/canary/Cargo.toml --lib -- --list)" &amp;&amp; test "$(printf '%s\n' "$CANARY_LIST" | rg -c '(^|::)(replaceable_edit_first_value_records_revision|replaceable_edit_reapplication_records_retired_inertness|replaceable_edit_inverse_covers_both_capabilities|protocol_crate_n_plus_one_records_external_and_raw_proofs): test$')" -eq 4 &amp;&amp; cargo test --manifest-path apps/canary/Cargo.toml --lib</automated>
  </verify>
  <done>Four guarded canary library tests pass; each enabled scenario produces bounded behavioral evidence through public fava with no sleeps or silent skips.</done>
</task>

<task type="auto">
  <name>Task 3: Wire and execute the four canaries from the ordinary CLI</name>
  <files>apps/canary/src/main.rs, apps/canary/README.md</files>
  <action>Dispatch the four exact scenario IDs from the existing `run` command to the tested library functions and document their deterministic invocation/evidence fields. Execute `list` and require all four IDs to report enabled, then run each through the built CLI with a unique fixed seed and a fresh temporary evidence directory. Treat any unknown scenario, missing evidence field, duplicate effect, or skipped prerequisite as a nonzero failure.</action>
  <verify>
    <automated>CANARY_IDS="$(cargo run --manifest-path apps/canary/Cargo.toml -- list)" &amp;&amp; test "$(printf '%s\n' "$CANARY_IDS" | rg -c '^(replaceable-edit-first-value|replaceable-edit-reapplication|replaceable-edit-inverse|protocol-crate-n-plus-one)[[:space:]]+M7[[:space:]]+enabled$')" -eq 4 &amp;&amp; M7_EVIDENCE="$(mktemp -d)" &amp;&amp; cargo run --manifest-path apps/canary/Cargo.toml -- run replaceable-edit-first-value --seed m7-first --runs-dir "$M7_EVIDENCE" &amp;&amp; cargo run --manifest-path apps/canary/Cargo.toml -- run replaceable-edit-reapplication --seed m7-reapply --runs-dir "$M7_EVIDENCE" &amp;&amp; cargo run --manifest-path apps/canary/Cargo.toml -- run replaceable-edit-inverse --seed m7-inverse --runs-dir "$M7_EVIDENCE" &amp;&amp; cargo run --manifest-path apps/canary/Cargo.toml -- run protocol-crate-n-plus-one --seed m7-n-plus-one --runs-dir "$M7_EVIDENCE"</automated>
  </verify>
  <done>CLI listing exposes exactly four enabled M7 canaries and all four run successfully with distinct bounded evidence artifacts.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

Apply OWASP ASVS L1 proportional controls at every boundary below; every listed threat has an explicit severity and disposition.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capability row to shared corpus | Protocol functions/appliers must satisfy one neutral lifecycle without hidden special cases. |
| canary CLI to evidence filesystem | Scenario inputs and observed lifecycle facts become bounded durable evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-29 | Tampering | shared corpus dispatch | high | mitigate | Same parameterized assertions for both capabilities; explicit negative core-kind-switch gate. |
| T-07-30 | Repudiation | canary evidence | high | mitigate | Record exact stable/current/retired/source/route/publication IDs and require scenario-specific fields. |
| T-07-31 | Denial of Service | canary artifacts | medium | mitigate | Existing bounded artifact conventions, unique temporary run directories, and no sleeps/retry loops. |
| T-07-32 | Elevation of Privilege | N+1 invocation | high | mitigate | Run independent public-only manifest; keep it outside selected product/root graph. |
| T-07-SC | Tampering | cargo installs | low | accept | Canary uses locked existing dependencies and the two approved local protocol crates; no third-party package is added. |
</threat_model>

<verification>
Run the guarded shared target, guarded canary library suite, CLI list/count gate, all four deterministic canaries, and `git diff --check`.
</verification>

<success_criteria>
Both capabilities pass one public corpus and the four enabled M7 canaries produce concrete first-value, reapplication, inverse, external N+1, and raw-future evidence.
</success_criteria>

<output>
Create `.planning/phases/07-semantic-writes-and-capability-composition/07-08-SUMMARY.md` when done.
</output>
