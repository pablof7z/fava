---
phase: 07-semantic-writes-and-capability-composition
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - crates/fava-publication/src/lib.rs
  - crates/fava-publication/src/revision.rs
  - crates/fava-publication/src/run.rs
  - crates/fava/src/lib.rs
  - crates/fava/tests/semantic_write_publication.rs
  - crates/fava/BUILD.bazel
autonomous: true
requirements: [CAP-01, CAP-02, CAP-03, CAP-04, CAP-05]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Through public fava, a first-value edit is accepted, materially becomes an event, routes and publishes, and is observable under one stable receipt."
    - "A selected newer independent qualified source triggers a new RevisionId while preserving unrelated source changes; the same receipt/current RevisionId local contribution and equal, older, unqualified, duplicate, or reordered observations are inert."
    - "Preview and initial custody use the same selected applier, exact bounded source path, injected timestamp policy, and initial routing result; preview has zero durable custody, tasks, notifications, or transport effects."
    - "At most 64 appliers are selected deterministically; duplicates, overflow, empty-selection edits, unsupported kinds, addressable edits, and exhausted publication admission refuse before custody through existing errors."
    - "No descriptor-like, registry-like, factory-like, profile-like, compatibility-like, or migration-like public abstraction is introduced."
  artifacts:
    - "crates/fava-publication/src/revision.rs"
    - "crates/fava-publication/src/run.rs"
    - "crates/fava/tests/semantic_write_publication.rs"
  key_links:
    - "FavaBuilder passes selected EditApplier trait objects plus existing QuerySource/QueryEvaluator providers into Publication with no private default bypass."
    - "Publication opens the canonical event-cache plus write-store observation before applying, then stores the generated event before signing/routing."
    - "Winner selection uses existing publication provenance to exclude the observing receipt's current local revision while permitting qualified independent cache and unrelated-local sources."
    - "Fava::preview_write_routes delegates semantic preview to the same publication selection/source/revision helper used by accept."
---

<objective>
Run the first public semantic-write tracer and establish live source/preview composition.

Purpose: Prove the neutral/store skeleton reaches real public publication before adding exhaustive completion and failure isolation.
Output: Public Fava assembly, bounded applier selection, first publication, live reapplication observation, and effect-free preview.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-02-SUMMARY.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-RESEARCH.md
@crates/fava/src/lib.rs
@crates/fava-publication/src/lib.rs
@crates/fava-publication/src/run.rs
@crates/fava-observe/src/lib.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Publish one first-value edit end to end through public fava</name>
  <files>crates/fava/src/lib.rs, crates/fava/BUILD.bazel, crates/fava-publication/src/lib.rs, crates/fava-publication/src/revision.rs, crates/fava/tests/semantic_write_publication.rs</files>
  <behavior>
    - An application selects one applier on FavaBuilder, publishes a supported first-value WriteIntent::Edit, observes an ordinary generated event reach the scripted publisher, and reads one stable receipt with RevisionId.
    - The applier receives None as source and one exact publication-injected Timestamp; the generated event has the requested actor and exact non-addressable coordinate.
    - Empty selection, unsupported kind, duplicate kind selection, selection 65, and admission exhaustion refuse before store acceptance, routing, signing, task creation, or transport calls.
    - Selection order is deterministic and no protocol implementation has a private/default bypass.
  </behavior>
  <action>Add the public tests first and capture the named target RED. Extend FavaBuilder with direct selection of approved EditApplier trait objects, capped at 64. Validate duplicates and overflow during build through existing BuildError/PublicationError variants and strings; do not introduce a registry, descriptor, entry, factory, or configuration nominal type. Pass the selected appliers and existing event-cache/write-store query sources plus evaluator to Publication. Before custody, select exactly one applier by its precise non-addressable coordinate kind, acquire one permit from the existing write-store active admission capacity, and refuse empty/unsupported/addressable/exhausted cases through existing errors. For the first tracer, select the current source from the exact bounded author+kind query (empty means None), compute one checked timestamp strictly newer than any source, inject it, validate the returned unsigned event against actor/coordinate/size, install it in the write store, then use the ordinary signer/router/publisher path. Production reads time once in publication; appliers do not read it, and tests inject fixed timestamps without sleeps. Never copy the unpublished event into event cache.</action>
  <verify>
    <automated>PUBLICATION_LIST="$(cargo test -p fava --test semantic_write_publication -- --list)" &amp;&amp; test "$(printf '%s\n' "$PUBLICATION_LIST" | rg -c '(^|::)(first_value_edit_publishes_through_public_fava|applier_selection_bounds_refuse_before_custody|first_value_receives_exact_injected_timestamp): test$')" -eq 3 &amp;&amp; cargo test -p fava --test semantic_write_publication</automated>
  </verify>
  <done>The three required public tests are listed and pass; one real first-value edit crosses public fava to an ordinary publication and stable receipt, while every unsupported selection/configuration refuses with zero custody or effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Observe exact source successors and make semantic preview effect-free</name>
  <files>crates/fava-publication/src/lib.rs, crates/fava-publication/src/revision.rs, crates/fava-publication/src/run.rs, crates/fava/src/lib.rs, crates/fava/tests/semantic_write_publication.rs</files>
  <behavior>
    - One live edit owns one active exact-coordinate observation; a strictly newer qualified source reapplies once and preserves unrelated source content/tags.
    - Equal timestamp, older timestamp, wrong actor, wrong kind, duplicate snapshot, and deterministic equal-timestamp loser produce no generation, receipt update, signing, routing, or publication effect.
    - Source removal reapplies from the next qualified winner or None exactly once; simultaneous source/completion orderings converge on one current generation.
    - The first local publication does not create a second generation; only a qualified independent cache or unrelated-local source change can do so.
    - Semantic preview uses the same applier, current exact source winner, timestamp rule, validation, and router input as initial acceptance, but leaves store, receipt stream, task counters, signers, routers, publishers, and transport unchanged.
    - Unsupported and addressable semantic previews return existing typed refusals with no effects.
  </behavior>
  <action>Add successor, self-contribution, ordering, removal, and preview tests first; run the entire target RED. Generalize the tracer into one live canonical observation per admitted semantic receipt using the existing Query/QuerySource/QueryEvaluator boundaries. Before winner selection, inspect existing EventRecord/PublicationEvidence provenance and exclude the local contribution whose ReceiptId and current RevisionId match the observing semantic write. Do not exclude independent cache events or qualified local contributions from unrelated receipts. Filter by exact actor and kind; non-empty d-tag was already refused, so never approximate exactness with an unbounded scan. Track the selected independent winner by timestamp then stable event-id tie resolution, consume each winner once, and ignore self, equal/older/unqualified/duplicate snapshots. On a qualified independent change or removal, invoke the selected applier with the store-current edit/source and a caller-computed checked timestamp, then request compare-and-set installation. Implement semantic `preview_write_routes` by calling the same selection, exact bounded source, revision validation, and routing helper as the initial live path while explicitly bypassing acceptance, permits, observations, spawned work, signing, and delivery. Assert preview/current initial route parity when the source is unchanged. Keep all selection helpers private and add no new public nominal API beyond direct functions/methods and the already-approved values.</action>
  <verify>
    <automated>PUBLICATION_LIST="$(cargo test -p fava --test semantic_write_publication -- --list)" &amp;&amp; test "$(printf '%s\n' "$PUBLICATION_LIST" | rg -c '(^|::)(newer_source_reapplies_once_and_preserves_unrelated_fields|own_local_revision_does_not_create_a_second_generation|equal_older_unqualified_and_duplicate_sources_are_inert|source_removal_selects_next_or_empty_once|semantic_preview_matches_initial_route_with_zero_effects): test$')" -eq 5 &amp;&amp; cargo test -p fava --test semantic_write_publication &amp;&amp; cargo test -p fava-publication</automated>
  </verify>
  <done>Five required expansion tests and the publication suite pass; the write's own local revision cannot self-rebase, independent qualified sources drive deterministic successor generations, and semantic preview is route-equivalent but entirely effect-free.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

Apply OWASP ASVS L1 proportional controls at every boundary below; every listed threat has an explicit severity and disposition.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FavaBuilder to Publication | Application-selected appliers and existing providers become live capabilities. |
| query sources to applier | Potentially hostile cached/remote state becomes protocol input. |
| preview to live publication | Prospective evaluation must not cross the custody/effect boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Spoofing | source selection | high | mitigate | Exact author+kind query, canonical winner rule, returned-event actor/coordinate validation, and addressable refusal. |
| T-07-10 | Denial of Service | applier/task selection | high | mitigate | Cap selection at 64, one observation per live edit, and pre-custody permit from store active capacity. |
| T-07-11 | Tampering | preview/live parity | high | mitigate | One private apply-and-route helper drives both; zero-effect instrumentation tests guard preview. |
| T-07-12 | Elevation of Privilege | applier output | high | mitigate | Validate actor, coordinate, encoding, and size before store installation/signing; protocol code receives no runtime owners. |
| T-07-SC | Tampering | cargo installs | low | accept | This plan adds no third-party package or dependency. |
</threat_model>

<verification>
Run both positively guarded publication groups, the publication crate suite, `cargo check --workspace`, and `git diff --check`.
</verification>

<success_criteria>
Public Fava publishes a first semantic event, deterministically reapplies on exact qualified source changes, and previews the same route with no custody or side effect.
</success_criteria>

<output>
Create `.planning/phases/07-semantic-writes-and-capability-composition/07-03-SUMMARY.md` when done.
</output>
