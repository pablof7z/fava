---
phase: 07-semantic-writes-and-capability-composition
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - crates/fava-write-store/src/lib.rs
  - crates/fava-write-store-memory/src/lib.rs
  - crates/fava-write-store-memory/src/semantic.rs
  - crates/fava/tests/semantic_write_store.rs
  - crates/fava/BUILD.bazel
autonomous: true
requirements: [CAP-02, CAP-03, CAP-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The write store is the sole owner of durable edit custody, one live exact-coordinate owner, current generation, stable receipt, retired evidence, and bounded post-accept revision failure facts."
    - "Memory admission and generation replacement are atomic: duplicate and simultaneous operations produce one effect, and stale expected generations cannot mutate the current generation."
    - "A successful successor clears its prior failure fact atomically; a failed successor preserves the previous current event and generation."
    - "Recovery returns every live edit with enough existing receipt/edit/source identity to resume exactly once."
    - "The store contract exposes its existing destination/evidence capacity as a primitive usize-returning function; consumers never duplicate the numeric limit."
    - "No descriptor-like, registry-like, profile-like, compatibility-like, or migration-like store abstraction exists."
  artifacts:
    - "crates/fava-write-store/src/lib.rs"
    - "crates/fava-write-store-memory/src/semantic.rs"
    - "crates/fava/tests/semantic_write_store.rs"
  key_links:
    - "WriteStore acceptance serializes the exact coordinate check with stable receipt creation."
    - "Generation replacement compares WriteId, current RevisionId, and selected source before swapping store-owned current state."
    - "Receipt subscription and ordinary query-source observation see only committed store state."
    - "Memory retention and later publication channel sizing consume the same fava-write-store capacity function."
---

<objective>
Implement the memory semantic-write state machine behind the neutral store contract.

Purpose: Establish one durable authority for coordinate admission, generation replacement, failure evidence, and recovery before publication orchestration begins.
Output: Store contract extensions, memory implementation, and concurrency/recovery evidence.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-01-SUMMARY.md
@.planning/phases/07-semantic-writes-and-capability-composition/07-RESEARCH.md
@crates/fava-write-store/src/lib.rs
@crates/fava-write-store-memory/src/lib.rs
@crates/fava-write/src/lib.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Admit one live edit per coordinate and atomically install generations</name>
  <files>crates/fava-write-store/src/lib.rs, crates/fava-write-store-memory/src/lib.rs, crates/fava-write-store-memory/src/semantic.rs, crates/fava/tests/semantic_write_store.rs, crates/fava/BUILD.bazel</files>
  <behavior>
    - First acceptance stores the edit and stable receipt but no fabricated prior event.
    - A duplicate invocation and simultaneous acceptance at the same exact coordinate yield one live owner and no duplicate effect.
    - Acceptance beyond active capacity or for unsupported configuration refuses with existing WriteStoreError/PublicationError values before custody.
    - Installing a generated event succeeds only for the current expected generation/source and changes RevisionId without changing WriteId or ReceiptId.
    - An equal, older, unqualified, or already-consumed source observation is inert.
  </behavior>
  <action>Add the listed memory-store tests first and run the whole named target to capture RED before changing production. Extend the existing WriteStore trait with methods, primitive return values, and existing approved values only; add no new provider contract or nominal state object. Expose the existing active admission limit through a non-nominal method so publication can bound its tasks. In memory, serialize acceptance and exact-coordinate ownership under the existing mutex, compare-and-set the current RevisionId/source for every replacement, and commit before notifying. Keep one active edit per exact coordinate, one current generation per receipt, and one bounded retired-evidence entry per replaced generation up to the existing route/evidence cap. A duplicate request, concurrent request, stale expected generation, equal/older/unqualified source, and capacity exhaustion must cause no second mutation or observer effect. Never insert an unpublished generated event into event cache.</action>
  <verify>
    <automated>STORE_LIST="$(cargo test -p fava --test semantic_write_store -- --list)" &amp;&amp; test "$(printf '%s\n' "$STORE_LIST" | rg -c '(^|::)(memory_first_edit_has_no_prior|memory_simultaneous_coordinate_admission_has_one_owner|memory_generation_swap_is_compare_and_set|memory_unqualified_source_is_inert): test$')" -eq 4 &amp;&amp; cargo test -p fava --test semantic_write_store</automated>
  </verify>
  <done>The four required tests are positively enumerated and pass; memory owns one live exact-coordinate edit, atomically swaps only current generations, preserves stable IDs, and emits no duplicate effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist bounded failure, retry, settlement, and recovery state</name>
  <files>crates/fava-write-store/src/lib.rs, crates/fava-write-store-memory/src/lib.rs, crates/fava-write-store-memory/src/semantic.rs, crates/fava/tests/semantic_write_store.rs</files>
  <behavior>
    - A post-accept applier failure is recorded in the current Receipt/PublicationEvidence with its existing source and revision IDs, bounded text, and no current-generation replacement.
    - A successful retry clears the failure and installs the successor atomically; repeating either call is idempotent.
    - Restart recovery returns the live edit with current generation, failed source attribution, and retry eligibility, but terminal/cancelled edits do not recover.
    - Settlement removes the live coordinate owner; retired completions remain bounded, attributable, and unable to change current state.
    - Retired evidence and correction destinations use existing route/evidence caps and overflow refuses without partial mutation.
  </behavior>
  <action>Write the failure/recovery cases before production and capture RED. Add store mutations that set and clear failure facts only through existing Receipt and PublicationEvidence fields: preserve WriteId, ReceiptId, current event, current RevisionId, selected source identity, destinations, and attempts. Bound external failure text with the existing receipt-text limit; if provider text is unusable, store a fixed bounded attribution. Clear only in the same atomic commit that installs a successful successor. Recovery must retain a failed live source for one retry after restart, whereas cancel/terminal state releases coordinate ownership and cannot retry. Make fava-write-store expose its already-owned destination/evidence cap through a primitive `usize`-returning free function using existing vocabulary; memory retention must call that function rather than own another numeric literal. Retired entries, correction destinations, and later publication channels all consume this one primitive. Overflow/exhaustion returns an existing error and leaves the prior receipt byte-for-byte unchanged. Do not add a public failure, retry, recovery, capacity, or evidence nominal type.</action>
  <verify>
    <automated>STORE_LIST="$(cargo test -p fava --test semantic_write_store -- --list)" &amp;&amp; test "$(printf '%s\n' "$STORE_LIST" | rg -c '(^|::)(memory_failure_preserves_current_and_is_attributed|memory_successful_retry_clears_failure_atomically|memory_live_edit_recovers_once_and_terminal_is_inert|memory_evidence_exhaustion_has_no_partial_effect): test$')" -eq 4 &amp;&amp; cargo test -p fava --test semantic_write_store &amp;&amp; cargo test -p fava-write-store-memory</automated>
  </verify>
  <done>Four required failure/recovery tests and the memory crate suite pass; bounded evidence survives recovery, retries and clears correctly, prior current state survives all failures, and terminal/stale work is inert.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

Apply OWASP ASVS L1 proportional controls at every boundary below; every listed threat has an explicit severity and disposition.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publication to WriteStore | Concurrent admissions, expected generations, source identities, and provider failures request durable mutation. |
| WriteStore to observers | Committed receipt and query-source state becomes externally observable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | generation compare-and-set | high | mitigate | Require exact operation, current RevisionId, and source identity under one store lock/transaction before mutation. |
| T-07-06 | Denial of Service | active edits and retained evidence | high | mitigate | Reuse active admission, receipt-text, destination, and evidence caps; typed refusal leaves no custody or partial mutation. |
| T-07-07 | Repudiation | retired and failed work | medium | mitigate | Retain bounded exact generation/source/event attribution in existing receipt evidence. |
| T-07-08 | Elevation of Privilege | observer visibility | high | mitigate | Notify only after commit; expose unpublished revisions only from the independent write-store query source. |
| T-07-SC | Tampering | cargo installs | low | accept | This plan adds no third-party package or dependency. |
</threat_model>

<verification>
Run both positively guarded store test groups, the full memory-store crate suite, `cargo check --workspace`, and `git diff --check`.
</verification>

<success_criteria>
Memory behavior proves serialized one-coordinate custody, exact generation correlation, bounded failure evidence, retry/recovery, and no stale or duplicate mutation.
</success_criteria>

<output>
Create `.planning/phases/07-semantic-writes-and-capability-composition/07-02-SUMMARY.md` when done.
</output>
