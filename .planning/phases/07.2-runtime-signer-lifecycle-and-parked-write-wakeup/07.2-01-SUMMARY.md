---
phase: 07.2-runtime-signer-lifecycle-and-parked-write-wakeup
plan: 01
subsystem: identity
tags: [rust, signer, session, publication, tokio-watch]
requires:
  - phase: 07.1-universal-publication-vocabulary
    provides: durable accepted writes, stable Write/Receipt identity, and exact author signing
provides:
  - bounded fava-session runtime signer attachment owner
  - runtime Fava add/replace/remove signer facade
  - exact-pubkey parked-write wakeup under unchanged custody identity
affects: [07.2-02, publication, auth, runtime-identity]
actuals:
  tokens: 67806
  tasks: 2
  commits: 2
tech-stack:
  added: [fava-session]
  patterns: [generation-keyed runtime attachment, coalescible wake with exact-state reload]
key-files:
  created:
    - crates/fava-session/src/lib.rs
    - crates/fava-publication/src/sign.rs
    - crates/fava/tests/runtime_signers.rs
  modified:
    - crates/fava-publication/src/run.rs
    - crates/fava/src/lib.rs
    - crates/fava/src/session.rs
key-decisions:
  - "Session is the sole mutable signer attachment owner; Publication retains only per-write signing operations."
  - "Session revisions are wake signals only; each publication reloads its event's exact pubkey and compares attachment generation."
patterns-established:
  - "Provider calls occur after Session lookup clones the signer and releases the owner lock."
  - "Builder signers seed the same Session mutated by the runtime facade."
requirements-completed: [SESSION-01, SESSION-02, SESSION-07]
coverage:
  - id: D1
    description: "A signer added after acceptance wakes the original unsigned write without changing WriteId or ReceiptId."
    requirement: SESSION-01
    verification:
      - kind: integration
        ref: "crates/fava/tests/runtime_signers.rs#signer_added_after_acceptance_wakes_same_write"
        status: pass
      - kind: other
        ref: "//crates/fava:runtime_signers"
        status: pass
    human_judgment: false
  - id: D2
    description: "Builder and runtime signers share one bounded exact-key Session owner."
    requirement: SESSION-02
    verification:
      - kind: unit
        ref: "crates/fava-session/tests/session.rs"
        status: pass
      - kind: other
        ref: "//crates/fava-session:session"
        status: pass
    human_judgment: false
  - id: D3
    description: "Adding Bob neither invokes Bob nor cancels or restarts Alice's active signer operation."
    requirement: SESSION-07
    verification:
      - kind: integration
        ref: "crates/fava/tests/runtime_signers.rs#adding_bob_does_not_wake_alice"
        status: pass
    human_judgment: false
duration: 9min
completed: 2026-08-23
status: complete
---

# Phase 07.2 Plan 01: Runtime Signer Tracer Summary

**A bounded runtime Session now wakes exact-author parked writes on the same Fava, WriteId, and ReceiptId.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-08-23T13:17:01Z
- **Completed:** 2026-08-23T13:26:10Z
- **Tasks:** 2
- **Files modified:** 19

## Accomplishments

- Added the approved `fava-session` crate as the sole bounded pubkey-to-signer attachment owner.
- Replaced Publication's immutable signer map with Session generation observation and exact-pubkey wakeup.
- Exposed runtime add, explicit replace, and remove through `Fava` while preserving durable write and receipt identity.

## Task Commits

1. **Task 1: Prove add-after-acceptance and exact-pubkey wakeup through Fava** - `00c5d35` (test RED)
2. **Task 2: Implement Session ownership and wire the public tracer** - `c3222c1` (feature GREEN)

## Files Created/Modified

- `crates/fava-session/src/lib.rs` - bounded signer attachments, generations, exact lookup, mutation, and wake subscription.
- `crates/fava-publication/src/sign.rs` - exact attachment selection and stale-generation completion admission.
- `crates/fava-publication/src/run.rs` - subscribes before signer lookup and reacts only to changed exact-key generations.
- `crates/fava/src/session.rs` - runtime signer mutation facade.
- `crates/fava/tests/runtime_signers.rs` - public add-after-acceptance and Alice/Bob isolation evidence.
- Cargo/Bazel manifests and lock metadata - register the new internal crate and dependency edges.

## Decisions Made

- Session owns attachment generations; its global revision never authorizes work by itself.
- An attached but currently unavailable signer still counts as the observed generation, preventing unrelated signer mutations from spuriously retrying it.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Split new facade and publication logic into cohesive modules**
- **Found during:** Task 2
- **Issue:** Adding the runtime methods inline pushed existing code files over the repository's 500-line soft limit.
- **Fix:** Added `fava/src/session.rs` and `fava-publication/src/sign.rs` along the architecture's existing owner boundaries.
- **Files modified:** `crates/fava/src/lib.rs`, `crates/fava/src/session.rs`, `crates/fava-publication/src/lib.rs`, `crates/fava-publication/src/run.rs`, `crates/fava-publication/src/sign.rs`
- **Verification:** All affected code files are below 500 lines; Cargo tests, Clippy, and Bazel targets pass.
- **Committed in:** `c3222c1`

**2. [Rule 3 - Blocking] Updated direct Publication test assembly**
- **Found during:** Task 2
- **Issue:** The shared semantic-write fixture constructed Publication directly with the removed signer iterator.
- **Fix:** Constructed a Session explicitly and added its Bazel edges to every target compiling that fixture.
- **Files modified:** `crates/fava/tests/support/semantic_write.rs`, `crates/fava/BUILD.bazel`
- **Verification:** Complete `cargo test -p fava` and affected Bazel targets pass.
- **Committed in:** `c3222c1`

**3. [Rule 3 - Blocking] Registered implemented vocabulary in Plan 01**
- **Found during:** Task 2
- **Issue:** The checker correctly treated the newly implemented crate and symbols as undocumented while they remained specification-only entries.
- **Fix:** Moved `fava-session`, `fava_session::Session`, and `fava_session::SessionError` into the implemented fields of the already-approved Session term.
- **Files modified:** `docs/internals/vocabulary.toml`
- **Verification:** The branch adds no vocabulary findings relative to `main`; vocabulary checker unit tests pass.
- **Committed in:** `c3222c1`

---

**Total deviations:** 3 auto-fixed (1 missing critical, 2 blocking).
**Impact on plan:** Required cohesion, test assembly, and approved-vocabulary registration only; no behavioral scope expansion.

## Issues Encountered

- The vocabulary checker remains non-clean on findings already present on `main`; branch/main output comparison confirms this plan adds none, and all 14 checker unit tests pass.
- The `bazel` executable is not on `PATH`; the installed `/Users/pablo/.local/bin/bazelisk` wrapper ran all three affected targets successfully.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Runtime add and exact-key wakeup are complete.
- Plan 02 can harden replacement, removal/re-add, fixed-capacity refusal, and stale old-signer completions.

---
*Phase: 07.2-runtime-signer-lifecycle-and-parked-write-wakeup*
*Completed: 2026-08-23*
