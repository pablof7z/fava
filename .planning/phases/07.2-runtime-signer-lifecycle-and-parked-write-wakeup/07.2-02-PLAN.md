---
phase: 07.2-runtime-signer-lifecycle-and-parked-write-wakeup
plan: 02
type: execute
wave: 2
depends_on: ["07.2-01"]
files_modified:
  - crates/fava-session/src/lib.rs
  - crates/fava-session/tests/session.rs
  - crates/fava-publication/src/run.rs
  - crates/fava/tests/runtime_signers.rs
  - crates/fava/BUILD.bazel
  - docs/internals/vocabulary.toml
  - docs/spec/ARCHITECTURE.md
  - docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md
  - docs/issues/0020-runtime-signer-lifecycle.md
autonomous: true
requirements: [SESSION-03, SESSION-04, SESSION-05, SESSION-06]
must_haves:
  truths:
    - "Duplicate add refuses without mutation; explicit replacement installs one fresh generation; repeated missing replace/remove refuses exactly."
    - "Removal preserves accepted writes and receipts, cancels the removed generation, and re-add wakes the same current materialization."
    - "A signer completion from a replaced or removed attachment generation cannot install signed state, record a fresh-generation refusal, or start delivery."
    - "The 64th distinct signer succeeds, the 65th refuses atomically, replacement at capacity succeeds, and concurrent final-slot growth cannot exceed 64."
    - "Duplicate public keys collide, empty and one-item sessions are valid, and insertion order does not affect exact lookup."
    - "Repeated stale completions remain inert, and replacement/removal races resolve by exact attachment plus materialization generation."
  prohibitions:
    - statement: "MUST NOT select a sole, current, fallback, or different-pubkey signer when the exact event pubkey has no attachment."
      status: resolved
      verification: test
    - statement: "MUST NOT move accepted-write, receipt, routing, retry, delivery, or persistence ownership into fava-session."
      status: resolved
      verification: judgment
    - statement: "MUST NOT rebuild Fava, replace its write store, or change accepted write/receipt identity to apply a signer mutation."
      status: resolved
      verification: test
  artifacts:
    - path: crates/fava-session/tests/session.rs
      provides: "Mutation, capacity, and concurrency owner evidence"
    - path: crates/fava/tests/runtime_signers.rs
      provides: "Replacement, removal/re-add, stale-completion, and exact isolation facade evidence"
  key_links:
    - from: crates/fava-publication/src/run.rs
      to: crates/fava-session/src/lib.rs
      via: "completion installation re-checks exact attachment generation"
      pattern: "is_current|materialization_id"
---

<objective>
Harden the runtime tracer into the complete lifecycle: explicit replacement, removal and
re-add, capacity/concurrency refusal, stale same-pubkey completion isolation, and truthful
authoritative documentation.

Decision coverage: D-03, D-04, D-05, D-10, D-13.
</objective>

<execution_context>
@/Users/pablo/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07.2-runtime-signer-lifecycle-and-parked-write-wakeup/07.2-SPEC.md
@.planning/phases/07.2-runtime-signer-lifecycle-and-parked-write-wakeup/07.2-CONTEXT.md
@.planning/phases/07.2-runtime-signer-lifecycle-and-parked-write-wakeup/07.2-01-SUMMARY.md
@crates/fava-session/src/lib.rs
@crates/fava-publication/src/run.rs
@crates/fava/tests/runtime_signers.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete bounded mutation and exact stale-completion isolation</name>
  <read_first>
    @docs/spec/FAVA_TDD_BDD_TESTING_GUIDE.md
    @crates/fava-session/src/lib.rs
    @crates/fava-session/tests/session.rs
    @crates/fava-publication/src/run.rs
    @crates/fava/tests/runtime_signers.rs
    @crates/fava/tests/semantic_write_publication/author.rs
  </read_first>
  <files>crates/fava-session/src/lib.rs, crates/fava-session/tests/session.rs, crates/fava-publication/src/run.rs, crates/fava/tests/runtime_signers.rs, crates/fava/BUILD.bazel</files>
  <behavior>
    - Add/replace/remove have exact duplicate/missing/capacity outcomes and one committed revision per success.
    - Concurrent attempts for the 64th slot permit one growth mutation; no partial mutation survives refusal.
    - Removing a blocked signer cancels it; re-add starts the same write with a fresh generation.
    - Releasing a replaced/removed same-pubkey signer completion is inert even when its event is otherwise valid.
  </behavior>
  <action>Write final owner/facade descriptors before changing production behavior and capture causal RED for each missing semantic. Complete Session's explicit replace/remove and capacity/concurrency semantics with checked monotonic generations. Extend publication signing completion correlation to re-check the captured session attachment generation immediately before install or refusal recording; keep existing materialization/event checks. Use a controllable signer that can return a valid event after cancellation to prove generation—not cooperative cancellation—is the safety boundary. Add exact Alice/Bob tests proving unrelated mutations do not cancel or restart operations. Capture the named post-GREEN deliberate break `ignore_session_attachment_generation`, require the stale-completion selector RED, restore byte-exactly, and rerun GREEN.</action>
  <acceptance_criteria>Every mutation edge and stale-completion race is typed, bounded, exact-key isolated, and proven under deliberate break.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-session &amp;&amp; cargo test -p fava --test runtime_signers</automated>
  </verify>
  <done>SESSION-03 through SESSION-06 pass owner and facade evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Close architecture, documentation, and validation gates</name>
  <read_first>
    @docs/internals/vocabulary.toml
    @docs/spec/ARCHITECTURE.md
    @docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md
    @docs/issues/0020-runtime-signer-lifecycle.md
    @tools/check_vocabulary.py
    @Cargo.toml
    @MODULE.bazel
  </read_first>
  <files>docs/internals/vocabulary.toml, docs/spec/ARCHITECTURE.md, docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md, docs/issues/0020-runtime-signer-lifecycle.md</files>
  <action>Replace specified Session symbols/crate with implemented qualified symbols/crate in vocabulary.toml. Update authoritative docs to current delivered behavior only: runtime add/replace/remove, one exact signer per pubkey, 64 bound, exact wakeup, generation rejection, and no Fava rebuild. Remove any static-publication signer ownership narration. Run dependency inspection proving `fava-publication -> fava-session -> fava-signer/fava-write`, no reverse edge, and no publication-owned signer map. Demonstrate and record the existing unrelated `fava-canary` vocabulary finding separately if it still exists; do not silently claim a clean vocabulary gate. Run full targeted and workspace validation plus Bazel affected/all tests. Confirm code files remain below 500 lines or document a concrete cohesion reason below the 800-line hard cap.</action>
  <acceptance_criteria>Documentation and vocabulary describe the implemented owner without migration narration; all new behavior and build graphs pass, with any pre-existing unrelated gate failure isolated by evidence.</acceptance_criteria>
  <verify>
    <automated>python3 tools/check_vocabulary.py; python3 -m unittest tools.tests.test_vocabulary_check; cargo test --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check; bazel test //crates/fava-session:all //crates/fava-publication:all //crates/fava:all; git diff --check</automated>
  </verify>
  <done>The complete runtime signer lifecycle is truthful, build-system complete, and validation-backed.</done>
</task>

</tasks>

## Artifacts this phase produces

- Completed `SessionError` variants: `DuplicateSigner`, `MissingSigner`, `SignerCapacityExceeded`, and `GenerationExhausted`.
- Public facade methods: `Fava::replace_signer` and `Fava::remove_signer` in addition to Plan 01's `add_signer`.
- Exact tests for duplicate add, missing replace/remove, replacement, removal/re-add, 0/1/64/65 bounds, final-slot concurrency, Alice/Bob isolation, and stale valid completion.
- Updated implemented vocabulary entries for `fava_session::Session`, `fava_session::SessionError`, and `fava-session`.
- Execution artifact: `07.2-02-SUMMARY.md` with causal RED/GREEN and deliberate-break restoration evidence.

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| Concurrent application mutations -> Session | over-capacity or lost update | one owner lock, checked generation, atomic refusal, final-slot race test |
| Removed/replaced provider -> write store | stale valid signature installation | exact attachment-generation re-check plus existing materialization/event checks |
| Documentation/build graph -> downstream consumers | claimed API without usable artifacts | Cargo/Bazel parity, vocabulary gate, facade tests |

Security enforcement: ASVS level 1; high severity blocks completion.
</threat_model>

<verification>
All exact owner/facade tests, deliberate breaks, affected regressions, workspace Cargo tests/Clippy/fmt, Bazel affected/all targets, dependency checks, vocabulary checks, line limits, and diff checks must be recorded in the summary.
</verification>

<success_criteria>
SESSION-03 through SESSION-06 and the remaining edge/prohibition matrix are complete without expanding into account management or `by(signer)`.
</success_criteria>

<output>
Create `.planning/phases/07.2-runtime-signer-lifecycle-and-parked-write-wakeup/07.2-02-SUMMARY.md` when done.
</output>
