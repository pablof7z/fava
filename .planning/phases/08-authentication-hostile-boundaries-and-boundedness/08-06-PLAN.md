---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 06
type: execute
wave: 4
depends_on: [08-05]
files_modified:
  - crates/fava/src/lib.rs
  - crates/fava/src/live.rs
  - crates/fava/src/routes.rs
  - crates/fava-subscriptions-standard/src/lib.rs
  - crates/fava-subscriptions-standard/tests/relay_limits.rs
  - crates/fava-write-store-memory/src/bounds.rs
  - crates/fava-write-store-redb/src/bounds.rs
  - crates/fava/tests/write_bounds.rs
  - crates/fava/tests/observation_bounds.rs
  - crates/fava/tests/resource_bounds.rs
  - crates/fava/BUILD.bazel
  - docs/issues/0022-m8-ops004-bound-closure.md
autonomous: true
requirements: [HARD-08]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every non-runtime ledger row resolved in Plan 05 has its exact owner-local bound, typed refusal/backpressure/shortfall, high-water/loss fact, and exceed-limit test."
    - "At max the operation succeeds exactly; max+1 refuses before custody/effect; empty input is exact; adjacent owners remain usable."
    - "Concurrent session/subscription/write/observation pressure has deterministic ordering, bounded retained state, and exact loss/shortfall evidence."
    - "This slice adds no public/cross-crate noun, provider contract, persisted entity, configuration concept, or package; runtime vocabulary is isolated to Plan 07."
  artifacts:
    - "crates/fava/tests/resource_bounds.rs"
    - "owner-local active relay session and wire-subscription bound enforcement"
    - "Memory/Redb active/terminal receipt bound parity"
    - "docs/issues/0022-m8-ops004-bound-closure.md"
  key_links:
    - "08-RESOURCE-LEDGER approved row -> existing owner implementation -> exact owner test -> public resource_bounds corpus -> envelope field"
    - "Fava explicit/automatic open -> bounded session admission -> scoped ObserveError/shortfall -> unrelated observation progress"
  prohibitions:
    - statement: "Crossing one resource maximum must not partially acquire custody, silently omit work, or corrupt an adjacent owner's capacity."
      status: unverified
      flagged: true
      verification: unverified
---

<objective>
Close every implementation gap identified by the approved OPS-004 ledger.

Purpose: Make session, subscription, cache/write, observation, diagnostic, service, bridge, and retained-evidence pressure explicit through existing owners and public Fava.
Output: Owner-local bounds, provider parity, one public exceed-limit corpus, Bazel registration, and causal bound-removal evidence.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-05-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md
@crates/fava/tests/write_bounds.rs
@crates/fava/tests/observation_bounds.rs
</context>

## Artifacts this phase produces

- `crates/fava/tests/resource_bounds.rs`: public parameterized corpus covering every ledger row's boundary, empty, adjacency, ordering/concurrency, and precision predicate.
- Private existing-owner constants/state for resolved active-session and other non-runtime limits; exact identifiers/values are copied from the sealed ledger into issue 0022 before implementation.
- No new crate/package/public provider contract. Any forced runtime nominal change belongs only to Plan 07's blocking architecture decision.

<slice_contract>
Issue: `docs/issues/0022-m8-ops004-bound-closure.md`  
Branch: `m8/ops004-bound-closure`  
Validation: ledger-driven owner/public corpus, provider parity, bound-removal breaks, Cargo/Bazel/Clippy/vocabulary/line/diff  
Commit series: public ledger corpus RED -> owner-local bounds GREEN -> one-break-per-new-bound and full ledger closure
</slice_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Bound one public observation from relay-session admission through exact shortfall</name>
  <read_first>.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md, crates/fava/src/lib.rs, crates/fava/src/live.rs, crates/fava/src/routes.rs, crates/fava-subscriptions-standard/src/lib.rs</read_first>
  <files>crates/fava/src/lib.rs, crates/fava/src/live.rs, crates/fava/src/routes.rs, crates/fava-subscriptions-standard/src/lib.rs, crates/fava-subscriptions-standard/tests/relay_limits.rs</files>
  <behavior>
    - One public explicit/automatic query at the approved session/wire limit opens all exact work.
    - One additional session/subscription produces exact typed ObserveError/route shortfall before opening wire work.
    - Closing/cancelling releases capacity exactly; healthy existing observations continue.
  </behavior>
  <action>Write the public tracer RED using exact controlled transports and session counters. Implement the approved owner-local session/subscription admission in the existing Fava/live/routes/planner owners; keep acquisition scope and query meaning unchanged. Reserve before spawning/opening, release on every failure/cancel/close path, and attribute refusal to the exact query/route/session. Do not add a global runtime owner, public configuration noun, or compatibility bypass.</action>
  <acceptance_criteria>One public query path proves capacity admission, no-wire overflow, exact release, and adjacent observation progress end-to-end.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-subscriptions-standard --test relay_limits &amp;&amp; cargo test -p fava --test resource_bounds session_subscription</automated>
  </verify>
  <done>Active relay sessions and wire subscriptions have exact public bounded behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill remaining owner/provider rows and public exceed-limit corpus</name>
  <read_first>.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md, crates/fava-write-store-memory/src/bounds.rs, crates/fava-write-store-redb/src/bounds.rs, crates/fava/tests/write_bounds.rs, crates/fava/tests/observation_bounds.rs</read_first>
  <files>crates/fava-write-store-memory/src/bounds.rs, crates/fava-write-store-redb/src/bounds.rs, crates/fava/tests/write_bounds.rs, crates/fava/tests/observation_bounds.rs, crates/fava/tests/resource_bounds.rs</files>
  <behavior>
    - Every approved missing row passes max/max+1, empty, adjacent, concurrent ordering, and exact precision assertions.
    - Memory and Redb expose identical active/terminal receipt refusal and retention behavior.
    - Latest-state observation coalescing reports exact loss while causal/durable facts remain loss-honest.
    - Every refused operation leaves current/durable state and adjacent capacity unchanged.
  </behavior>
  <action>Build a parameterized public corpus from the ledger and make it RED for every missing row before changing owners. Modify only the ledger-named owners. Reuse existing typed error/shortfall values and per-owner constants; do not create a common bounds bucket. Add Memory/Redb parity and restart where a retained/persisted row is involved. Include the committed auth/NIP-11/frame/query/router/cache/write bounds in the corpus as characterization so the ledger cannot regress to partial coverage.</action>
  <acceptance_criteria>The ledger checker and public corpus have a one-to-one passing row mapping, including provider parity and no partial mutation.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tools.tests.test_m8_resource_ledger &amp;&amp; cargo test -p fava --test write_bounds &amp;&amp; cargo test -p fava --test observation_bounds &amp;&amp; cargo test -p fava --test resource_bounds &amp;&amp; cargo test -p fava-write-store-memory -p fava-write-store-redb</automated>
  </verify>
  <done>All non-provider-execution OPS-004 rows have exact owner/public behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Kill each newly closed bound and register the public corpus</name>
  <read_first>crates/fava/BUILD.bazel, docs/issues/0022-m8-ops004-bound-closure.md, docs/internals/vocabulary.toml</read_first>
  <files>crates/fava/BUILD.bazel, docs/issues/0022-m8-ops004-bound-closure.md</files>
  <action>Register `resource_bounds` in Bazel. For each bound added by this plan, checksum the exact owner file, temporarily raise/remove only that bound, run its exact max+1 public test and require compiled behavioral failure, restore by scoped edit, and require the original checksum plus green focused target. Aggregate the records under `DELIBERATE_BREAK_M8_OPS004_BOUNDS`. Run ledger, affected owner/public, Cargo/Bazel, strict Clippy, vocabulary, all-file line, diff, and stash-identity gates. A compile failure or unrelated assertion is not a killed mutant.</action>
  <acceptance_criteria>Every newly implemented bound is causally necessary to one exact test and the full OPS-004 ledger has no missing/unreachable row.</acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
python3 -m unittest tools.tests.test_m8_resource_ledger
cargo test -p fava --test resource_bounds
cargo test -p fava --test write_bounds
cargo test -p fava --test observation_bounds
cargo clippy -p fava -p fava-subscriptions-standard -p fava-write-store-memory -p fava-write-store-redb --all-targets -- -D warnings
bazel test //crates/fava:resource_bounds //crates/fava:write_bounds //crates/fava:observation_bounds //crates/fava-subscriptions-standard:all
python3 tools/check_vocabulary.py
python3 -m unittest tools.tests.test_vocabulary_check
rg -n '^DELIBERATE_BREAK_M8_OPS004_BOUNDS: PASS ' docs/issues/0022-m8-ops004-bound-closure.md
rg --files -g '*.rs' -0 | xargs -0 wc -l | awk '$2 != "total" &amp;&amp; $1 &gt; 800 { print; bad=1 } END { exit bad }'
test "$(git rev-parse 'stash@{0}')" = 5faecf42c0ec903507e3faeb04962f4680a9cb44
git diff --check</automated>
  </verify>
  <done>All OPS-004 bounds outside provider execution are implemented, sensitive, and build-registered.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

## Trust Boundaries

| Boundary | Description |
|---|---|
| query/route input -> session/subscription owners | External fan-out can open tasks, sockets, and wire subscriptions. |
| write/query activity -> stores/observations | External work consumes retained and active capacity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-19 | Denial of Service | session/subscription pools | high | mitigate | Exact reservation/release, max+1 no-wire refusal, concurrent healthy witness. |
| T-08-20 | Tampering | partial overflow mutation | high | mitigate | Refuse before custody/effect and assert unchanged state/provider parity. |
| T-08-21 | Repudiation | coalescing/loss | medium | mitigate | Exact coalesced/loss counters and latest-state versus causal-fact separation. |
| T-08-27 | Tampering | dependencies | low | accept | No new package; existing locked workspace only. |
</threat_model>

<verification>
Run ledger, every owner/public bound target, provider parity/restart, one-break-per-bound, Cargo/Bazel/Clippy/vocabulary/line/diff, and stash-identity gates.
</verification>

<success_criteria>
Every OPS-004 row except Plans 07-09's runtime/provider-execution rows has exact bounded owner/public behavior and causal evidence.
</success_criteria>

<output>
Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-06-SUMMARY.md` when done.
</output>
