---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 15
type: execute
wave: 10
depends_on: [08-06, 08-14]
files_modified:
  - apps/canary/scenarios.json
  - apps/canary/src/lib.rs
  - apps/canary/src/lib_tests.rs
  - apps/canary/src/main.rs
  - apps/canary/src/hostile.rs
  - apps/canary/src/m8.rs
  - apps/canary/src/m8_child.rs
  - apps/canary/src/m8_failure.rs
  - features/hostile-boundaries.feature
  - tools/tests/test_m8_feature_mapping.py
  - docs/issues/0024-m8-hostile-process-canary.md
autonomous: true
requirements: [HARD-03, HARD-08, HARD-10]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "hostile-relay-ingress runs every hostile schedule in a separate process over real sockets after runtime audit."
    - "Independent transcript proves exact frames/order; public Fava proves absence and healthy progress."
    - "Every success/failure bundle is bounded and all processes/tasks/sockets are reaped."
  artifacts: ["CLI hostile-relay-ingress", "m8-child hostile-relay", "hostile feature mapping"]
  key_links: ["registry -> CLI -> separate PID/socket -> transcript -> public absence -> bounded manifest"]
  prohibitions:
    - statement: "In-process adversary, diagnostic-only oracle, sleep ordering, or unbounded artifact cannot satisfy HARD-10."
      status: unverified
      flagged: true
      verification: process/evidence tests and break
---

<objective>
Build the hostile real-process/socket canary after explicit runtime migration and audit.

Purpose: Close hostile external-effect proof.
Output: One exact CLI scenario, bounded process harness, mapping, causal break.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-03-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-14-SUMMARY.md
@apps/canary/src/publication_child.rs
</context>

## Artifacts this phase produces

- Exact CLI ID `hostile-relay-ingress`, child command, bounded evidence validator, feature row.
- `DELIBERATE_BREAK_M8_HOSTILE_PROCESS_ADMISSION`.

<slice_contract>
Issue: `docs/issues/0024-m8-hostile-process-canary.md`
Branch: `m8/hostile-process-canary`
Validation: real PID/socket/transcript/bundle/mapping, break, canary Cargo/Clippy
Commit series: forged-event tracer -> hostile matrix -> mapping/break
</slice_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Send forged EVENT from child process to public absence</name>
  <read_first>apps/canary/src/hostile.rs, apps/canary/src/publication_child.rs, apps/canary/src/proxy.rs, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md</read_first>
  <files>apps/canary/src/hostile.rs, apps/canary/src/m8_child.rs, apps/canary/src/m8_failure.rs, apps/canary/src/main.rs, docs/issues/0024-m8-hostile-process-canary.md</files>
  <behavior>
    - Separate child sends forged EVENT over socket; transcript sees it; public Fava excludes it; healthy relay completes; child is reaped.
  </behavior>
  <action>Write canary RED then implement one process/socket path with explicit barriers and bounded bundle. Use committed/runtime-audited public Fava; diagnostics are not the independent oracle.</action>
  <acceptance_criteria>One hostile external path is proven across process, wire, owner, public, and evidence boundaries.</acceptance_criteria>
  <verify><automated>cargo test --manifest-path apps/canary/Cargo.toml --lib hostile_relay_ingress_forged_event</automated></verify>
  <done>The hostile process tracer is real and bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand every authoritative hostile schedule</name>
  <read_first>docs/spec/FAVA_REWRITE_IMPLEMENTATION_PLAN.md, apps/canary/src/m8.rs, apps/canary/src/m8_child.rs, features/hostile-boundaries.feature</read_first>
  <files>apps/canary/src/m8.rs, apps/canary/src/m8_child.rs, apps/canary/src/m8_failure.rs, apps/canary/src/lib_tests.rs, features/hostile-boundaries.feature</files>
  <behavior>
    - Invalid id/signature, off-filter, malformed, oversized, stale, post-CLOSED, never-EOSE, truncation, silent-limit, NOTICE, disconnect are exact and scoped.
  </behavior>
  <action>Write each schedule RED with barriers/proxy gates; expand executor and bounded success/failure/resource envelopes. Assert adjacent/empty/order/precision/concurrency explicitly.</action>
  <acceptance_criteria>Full hostile corpus passes with exact independent witnesses and no generic error inference.</acceptance_criteria>
  <verify><automated>cargo test --manifest-path apps/canary/Cargo.toml --lib hostile_relay_ingress</automated></verify>
  <done>The complete hostile process corpus is executable.</done>
</task>

<task type="auto">
  <name>Task 3: Register exact mapping and kill process/admission bypass</name>
  <read_first>apps/canary/scenarios.json, apps/canary/src/lib.rs, apps/canary/src/main.rs, tools/tests/test_m8_feature_mapping.py, docs/issues/0024-m8-hostile-process-canary.md</read_first>
  <files>apps/canary/scenarios.json, apps/canary/src/lib.rs, apps/canary/src/main.rs, tools/tests/test_m8_feature_mapping.py, docs/issues/0024-m8-hostile-process-canary.md</files>
  <action>Register one enabled exact ID/executor/CLI/test/feature/evidence mapping. Run `DELIBERATE_BREAK_M8_HOSTILE_PROCESS_ADMISSION` by replacing child with in-process work or bypassing admission; require CLI/mapper failure, restore, record PASS.</action>
  <acceptance_criteria>Exact mapping fails closed and the causal process/admission break is killed.</acceptance_criteria>
  <verify><automated>set -euo pipefail
python3 -m unittest tools.tests.test_m8_feature_mapping
cargo test --manifest-path apps/canary/Cargo.toml --all-targets
rg -n '^DELIBERATE_BREAK_M8_HOSTILE_PROCESS_ADMISSION: PASS ' docs/issues/0024-m8-hostile-process-canary.md
git diff --check</automated></verify>
  <done>Hostile process canary is registered, mapped, and causal.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-63 | Tampering | hostile frames | high | mitigate | Complete real-socket matrix. |
| T-08-64 | Repudiation | process evidence | high | mitigate | PID/transcript/manifest hashes. |
| T-08-65 | Denial of Service | hostile process/artifacts | high | mitigate | Deadlines, byte caps, reap accounting. |
| T-08-66 | Tampering | canary dependencies | high | mitigate | Locked Cargo and audited runtime graph. |
</threat_model>

<verification>Exact CLI/process/socket/bundle/mapping and break pass after runtime audit.</verification>
<success_criteria>Hostile ingress external effects are proven through real sockets and separate processes.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-15-SUMMARY.md` when done.</output>
