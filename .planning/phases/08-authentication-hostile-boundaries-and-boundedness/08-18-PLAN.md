---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 18
type: execute
wave: 13
depends_on: [08-17]
files_modified:
  - apps/canary/scenarios.json
  - apps/canary/src/lib.rs
  - apps/canary/src/main.rs
  - tools/tests/test_m8_feature_mapping.py
  - docs/issues/0025-m8-delivery-provider-canaries-and-exit.md
  - .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-BREAK-MANIFEST.tsv
  - .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-THREAT-MANIFEST.tsv
  - .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-LINE-GATE.tsv
  - .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-VALIDATION.md
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-05, HARD-06, HARD-07, HARD-08, HARD-09, HARD-10]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "All seven IDs are unique, enabled, dispatched, mapped, executable, bounded, and independently witnessed."
    - "Every exact deliberate break and unique threat has one valid manifest row."
    - "Every modified code file appears once in the line manifest; none exceeds 800 and every 500+ file has one path-specific reason."
    - "The post-task committed tree passes the full milestone gate and is clean while stash identity remains exact."
  artifacts: ["08-BREAK-MANIFEST.tsv", "08-THREAT-MANIFEST.tsv", "08-LINE-GATE.tsv", "complete 08-VALIDATION.md"]
  key_links: ["18-plan implementation graph -> seven scenarios -> exact manifests -> committed clean milestone"]
  prohibitions:
    - statement: "Wildcard checks, missing dispatch, sampled line scans, mixed dirty state, or in-process-only witnesses cannot complete M8."
      status: unverified
      flagged: true
      verification: seven-ID break, exact manifests, exhaustive line checker, full post-commit gate
---

<objective>
Seal Phase 08 from completed delivery/provider canaries and one committed clean tree.

Purpose: Make every behavior, threat, break, line constraint, and external scenario independently checkable.
Output: Seven-ID causal seal, exact manifests, complete validation, issue record, and clean-tree evidence.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-14-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-15-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-16-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-17-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-VALIDATION.md
</context>

## Artifacts this phase produces

- `DELIBERATE_BREAK_M8_SEVEN_ID_PROVIDER_SEAL` and exact seven-scenario composition.
- Exact 24-row break manifest and T-08-01..T-08-76 threat manifest.
- Exhaustive modified-code `08-LINE-GATE.tsv`.
- Complete 18-plan/57-task validation and final issue evidence.

<slice_contract>
Issue: `docs/issues/0025-m8-delivery-provider-canaries-and-exit.md`
Branch: `m8/final-milestone-seal`
Validation: seven-ID break -> exact manifests -> line/validation seal -> full Cargo/Bazel/Go/scenario/diff/clean gate
Commit series: seven-ID causal seal -> break/threat manifests -> line/validation/final issue seal
</slice_contract>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Compose all seven IDs and kill dispatch/provider seal</name>
  <read_first>apps/canary/scenarios.json, apps/canary/src/lib.rs, apps/canary/src/main.rs, features/hostile-boundaries.feature, tools/tests/test_m8_feature_mapping.py</read_first>
  <files>apps/canary/scenarios.json, apps/canary/src/lib.rs, apps/canary/src/main.rs, tools/tests/test_m8_feature_mapping.py, docs/issues/0025-m8-delivery-provider-canaries-and-exit.md</files>
  <behavior>
    - Seven exact IDs occur once through registry, executor, CLI, test, feature, and evidence.
    - Removing provider dispatch or duplicating one ID fails the exact oracle while code compiles.
  </behavior>
  <action>Extend mapping to exact seven-ID composition without changing Plan 17 executor behavior. Run `DELIBERATE_BREAK_M8_SEVEN_ID_PROVIDER_SEAL` by removing only provider dispatch or duplicating one registry ID; require the mapper or exact CLI count to fail for that cause, restore checksums, rerun GREEN, and record PASS.</action>
  <acceptance_criteria>All seven scenarios are causally sealed with no unreachable, duplicate, disabled, or manifest-only ID.</acceptance_criteria>
  <verify><automated>set -euo pipefail
python3 -m unittest tools.tests.test_m8_feature_mapping
cargo test --manifest-path apps/canary/Cargo.toml --all-targets
M8_LIST="$(cargo run --manifest-path apps/canary/Cargo.toml -- list)"
test "$(printf '%s\n' "$M8_LIST" | rg -c '^(nip42-write-and-reconnect|auth-account-isolation|hostile-relay-ingress|relay-limit-shortfall|ambiguous-handoff|attempt-ceiling|provider-failure-isolation)[[:space:]]+M8[[:space:]]+enabled$')" -eq 7
rg -n '^DELIBERATE_BREAK_M8_SEVEN_ID_PROVIDER_SEAL: PASS ' docs/issues/0025-m8-delivery-provider-canaries-and-exit.md</automated></verify>
  <done>Seven-ID/provider dispatch is causal and complete.</done>
</task>

<task type="auto">
  <name>Task 2: Seal every exact break and threat row</name>
  <read_first>tools/tests/test_m8_feature_mapping.py, docs/issues/0025-m8-delivery-provider-canaries-and-exit.md, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-VALIDATION.md</read_first>
  <files>tools/tests/test_m8_feature_mapping.py, docs/issues/0025-m8-delivery-provider-canaries-and-exit.md, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-BREAK-MANIFEST.tsv, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-THREAT-MANIFEST.tsv</files>
  <action>Create one headerless row for each of these 24 breaks: delivery identity, hostile admission, store parity, diagnostics envelope in issue 0021, OPS bounds, Runtime boundary, standard order, Runtime build graph, publication materializer, publication run, auth execution, routing execution, NIP-11 timeout, query observation, relay session, route fan-out, publisher future, delivery/signer, Runtime audit, hostile process, auth generation, limit no-wire, delivery/provider process, and seven-ID seal. Create exact T-08-01 through T-08-76 threat rows, one disposition each; fail duplicates, missing, extra, or non-mitigated high threats. Validate every literal marker individually.</action>
  <acceptance_criteria>All 24 breaks and 76 threats have one valid row and every high threat is mitigated.</acceptance_criteria>
  <verify><automated>set -euo pipefail
python3 -m unittest discover -s tools/tests
while IFS=$'\t' read -r issue marker; do test -n "$issue" &amp;&amp; test -n "$marker" &amp;&amp; rg -n "^${marker}: PASS " "$issue" &gt;/dev/null; done &lt; .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-BREAK-MANIFEST.tsv
test "$(wc -l &lt; .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-BREAK-MANIFEST.tsv | tr -d ' ')" -eq 24
test "$(wc -l &lt; .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-THREAT-MANIFEST.tsv | tr -d ' ')" -eq 76
test "$(cut -f1 .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-THREAT-MANIFEST.tsv | sort | uniq -d | wc -l | tr -d ' ')" -eq 0
diff -u &lt;(seq -w 1 76 | sed 's/^/T-08-/') &lt;(cut -f1 .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-THREAT-MANIFEST.tsv | sort)
git diff --check</automated></verify>
  <done>Exact break and threat manifests are committed.</done>
</task>

<task type="auto">
  <name>Task 3: Seal exhaustive line/validation records and run final milestone gate</name>
  <read_first>tools/tests/test_m8_line_gate.py, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-VALIDATION.md, docs/issues/0025-m8-delivery-provider-canaries-and-exit.md</read_first>
  <files>.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-LINE-GATE.tsv, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-VALIDATION.md, docs/issues/0025-m8-delivery-provider-canaries-and-exit.md</files>
  <action>Reconcile validation to all 18 plans and 57 tasks. Generate the line manifest from the exact union of code paths in all plan frontmatters, one row per path with exact line count and `-` only below 500; every 500-800 row needs one unique concrete cohesion reason and over 800 is forbidden. Run the live checker, record the complete final gate, and commit Task 3. Only after that commit run plan-level seven-scenario/full/clean verification; clean status never runs inside this task.</action>
  <acceptance_criteria>The line set is exhaustive, validation has no stale row, evidence is committed, and the plan-level gate starts from that commit.</acceptance_criteria>
  <verify><automated>set -euo pipefail
python3 -m unittest tools.tests.test_m8_line_gate
git diff --check</automated></verify>
  <done>Line, validation, and final issue records are committed.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Boundary | Description |
|---|---|
| completed implementation -> milestone evidence | Scenario, threat, break, line, and git-state facts must resist omission or false success. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-74 | Tampering | exact manifests | high | mitigate | Set equality and literal row validation. |
| T-08-75 | Repudiation | clean seal | high | mitigate | Post-task committed full gate and identity checks. |
| T-08-76 | Information Disclosure | failure bundle | high | mitigate | Redaction and text/file/byte bounds. |
</threat_model>

<verification>
<automated>set -euo pipefail
cargo build --workspace
cargo check --workspace --all-targets
cargo test --workspace --all-targets
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --manifest-path apps/canary/Cargo.toml --all-targets
bazel test //...
python3 tools/check_vocabulary.py
python3 -m unittest discover -s tools/tests
(cd apps/canary/relays/khatru &amp;&amp; go version | rg 'go1\.25\.' &amp;&amp; GOTOOLCHAIN=local go mod verify &amp;&amp; GOTOOLCHAIN=local go test ./... &amp;&amp; GOTOOLCHAIN=local go build ./... &amp;&amp; ./probe.sh)
M8_FINAL_RUNS="$(mktemp -d)"
for scenario in nip42-write-and-reconnect auth-account-isolation hostile-relay-ingress relay-limit-shortfall ambiguous-handoff attempt-ceiling provider-failure-isolation; do cargo run --manifest-path apps/canary/Cargo.toml -- run "$scenario" --seed "m8-final-$scenario" --runs-dir "$M8_FINAL_RUNS/$scenario"; done
python3 -m unittest tools.tests.test_m8_line_gate
git diff --check caeee9e73f2b3919934bcb70043491d33c200daa..HEAD
git diff --check
test "$(git rev-parse 'stash@{0}')" = 5faecf42c0ec903507e3faeb04962f4680a9cb44
test -z "$(git status --porcelain --untracked-files=all)"</automated>
All seven scenarios, exact manifests, full gates, and clean/stash checks pass after Task 3 commit.
</verification>

<success_criteria>All HARD-01..10 outcomes pass from one clean committed tree with exhaustive Runtime ownership and exact external evidence.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-18-SUMMARY.md` when done.</output>
