---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/canary/relays/khatru/probe.sh
  - docs/issues/0018-m8-go25-khatru-readiness.md
autonomous: false
requirements: [HARD-04, HARD-10]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The active Go toolchain reports Go 1.25.x; the checked-in Khatru go.mod remains pinned at 1.25.0."
    - "The checked-in Khatru module passes go mod verify, go test ./..., build, and a bounded real-process loopback readiness probe."
    - "stash@{0} still resolves to 5faecf42c0ec903507e3faeb04962f4680a9cb44 and no source WIP byte is changed by environment setup."
  artifacts:
    - "docs/issues/0018-m8-go25-khatru-readiness.md"
    - "verified CLI boundary: apps/canary/relays/khatru with Go 1.25.x"
  key_links:
    - "Go 1.25.x -> go.mod 1.25.0 -> verified Khatru process -> relay-limit-shortfall and second-relay exit evidence"
  edge_dispositions:
    - statement: "HARD-04 unclassified edge: real advertised limits and the independent no-wire witness are the sufficient remaining closure beyond committed 94e04cd."
      status: resolved
      source: spec-less probe
    - statement: "HARD-10 unclassified coverage is resolved only by the exact adjacency, empty, ordering, and concurrency predicates carried by canary closure Plans 15-18."
      status: resolved
      source: spec-less probe
  prohibitions:
    - statement: "The pinned Khatru module must not be weakened or rewritten to accommodate Go 1.23.3."
      status: unverified
      flagged: true
      verification: unverified
---

<objective>
Provision and prove the exact toolchain required by the checked-in second-relay fixture.

Purpose: Remove the only known environment blocker without changing protocol source, module pins, dirty WIP, or the preserved stash.
Output: A focused readiness issue containing automated Go/Khatru module and real-process evidence.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESEARCH.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-PATTERNS.md
@apps/canary/relays/khatru/go.mod
@apps/canary/relays/khatru/main.go
</context>

## Artifacts this phase produces

- `docs/issues/0018-m8-go25-khatru-readiness.md` with the exact toolchain path/version, commands, process PID/port/readiness evidence, source/stash preflight and postflight hashes, and bounded teardown result.
- No new Rust/Go symbol, crate, package, or canary ID. This plan qualifies the existing `github.com/fava/canary-khatru-relay` fixture.

<slice_contract>
Issue: `docs/issues/0018-m8-go25-khatru-readiness.md`  
Branch: `m8/go25-khatru-readiness`  
Validation: Go version/module/test/build plus real-process readiness and bounded reap  
Commit series: one evidence-only commit after every automated gate passes
</slice_contract>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Provision Go 1.25.x outside the repository</name>
  <read_first>apps/canary/relays/khatru/go.mod, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESEARCH.md</read_first>
  <action>Install or select a Go 1.25.x toolchain using the host's normal toolchain manager. This is a host-level prerequisite and is the one intentionally manual action. Do not edit `go.mod`, `go.sum`, any source file, the dirty patch, or `stash@{0}`. Resume only after `go version` selects 1.25.x in a fresh shell.</action>
  <instructions>Provision Go 1.25.x outside the repository, open a fresh shell, and report `done`; do not edit the checked-in module to fit the current Go 1.23.3 host.</instructions>
  <verification>The executor will run `go version`, verify the stash object and source diff, then execute module/test/build/process gates in Task 2.</verification>
  <resume-signal>Type `done` after `go version` reports Go 1.25.x, or provide the exact provisioning blocker.</resume-signal>
  <acceptance_criteria>The selected host toolchain is Go 1.25.x and the repository, dirty source inventory, and stash object are byte-identical to pre-checkpoint state.</acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go version | rg 'go1\.25\.'
test "$(git diff -- crates ':!*.md' | shasum -a 256 | awk '{print $1}')" = e7710b21f0fb81300ae136ecb31d062c5aeff2e3b08b449b96ccdf4bb5e8b19c
test "$(git hash-object crates/fava/tests/hostile_ingress.rs)" = 7b9270a3c255a00a8a42e5d1d90294bd662e82ae
test "$(git rev-parse 'stash@{0}')" = 5faecf42c0ec903507e3faeb04962f4680a9cb44
git diff --check</automated>
  </verify>
  <done>Go 1.25.x is active without any repository or stash mutation.</done>
</task>

<task type="tracer">
  <name>Task 2: Prove the checked-in Khatru module through one real process</name>
  <read_first>apps/canary/relays/khatru/go.mod, apps/canary/relays/khatru/main.go</read_first>
  <files>apps/canary/relays/khatru/probe.sh, docs/issues/0018-m8-go25-khatru-readiness.md</files>
  <behavior>
    - Go 1.25.x loads the unchanged module and verifies every downloaded module checksum.
    - All Go tests and build targets pass with `GOTOOLCHAIN=local`.
    - One loopback Khatru process advertises configured NIP-11 limits, becomes ready within 10 seconds, and is terminated and reaped within 5 seconds.
  </behavior>
  <action>Create the focused issue and executable `probe.sh`. The probe runs `go mod verify`, `go test ./...`, and `go build ./...` with `GOTOOLCHAIN=local`, starts the checked-in relay on an OS-selected loopback port with strict nonzero limit flags, waits for a successful live NIP-11 HTTP response/readiness within 10 seconds, records PID/port/document/logs in a temporary directory, sends termination, and proves `wait` reaped the exact PID within 5 seconds. It must fail on early exit, readiness timeout, invalid document, failed termination, or surviving PID. Do not check in binaries/generated module changes or accept a marker without the probe command succeeding.</action>
  <acceptance_criteria>The issue proves unchanged pinned source runs as a second real relay process and provides the exact automated prerequisite consumed by canary closure Plans 15-18.</acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
go version | rg 'go1\.25\.'
(cd apps/canary/relays/khatru &amp;&amp; GOTOOLCHAIN=local go mod verify &amp;&amp; GOTOOLCHAIN=local go test ./... &amp;&amp; GOTOOLCHAIN=local go build ./...)
(cd apps/canary/relays/khatru &amp;&amp; ./probe.sh)
rg -n '^GO25_KHATRU_READINESS: PASS ' docs/issues/0018-m8-go25-khatru-readiness.md
test "$(git diff -- crates ':!*.md' | shasum -a 256 | awk '{print $1}')" = e7710b21f0fb81300ae136ecb31d062c5aeff2e3b08b449b96ccdf4bb5e8b19c
test "$(git hash-object crates/fava/tests/hostile_ingress.rs)" = 7b9270a3c255a00a8a42e5d1d90294bd662e82ae
test "$(git rev-parse 'stash@{0}')" = 5faecf42c0ec903507e3faeb04962f4680a9cb44
git diff --check</automated>
  </verify>
  <done>Go module, test, build, process readiness, NIP-11 document, and teardown evidence are recorded as PASS with no generated residue.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

## Trust Boundaries

| Boundary | Description |
|---|---|
| host toolchain -> checked-in Go module | A host executable evaluates pinned third-party dependency metadata. |
| Khatru process -> loopback client | Real HTTP/WebSocket process output becomes later interoperability evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01 | Tampering | go.mod/go.sum | high | mitigate | Require Go 1.25.x and unchanged pins; run `go mod verify`; reject generated diffs. |
| T-08-02 | Repudiation | process readiness | medium | mitigate | Record PID, port, stdout, NIP-11 response, exit status, and bounded reap. |
| T-08-03 | Denial of Service | child lifecycle | high | mitigate | Ten-second readiness and five-second stop/reap ceilings; fail on leaked process. |
| T-08-22 | Tampering | Go dependencies | high | mitigate | Existing checked-in go.mod/go.sum only; package legitimacy was established by the committed fixture and `go mod verify`; no install task changes dependencies. |
</threat_model>

<verification>
Go 1.25.x, unchanged pinned module, module verification, all Go tests/builds, one real-process readiness/NIP-11/teardown proof, stash identity, and diff hygiene pass.
</verification>

<success_criteria>
The checked-in Khatru fixture is executable with its required toolchain and can be safely consumed by later M8 process canaries.
</success_criteria>

<output>
Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-01-SUMMARY.md` when done.
</output>
