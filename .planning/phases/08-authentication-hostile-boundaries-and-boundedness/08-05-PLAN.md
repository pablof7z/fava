---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 05
type: execute
wave: 3
depends_on: [08-03, 08-04]
files_modified:
  - .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md
  - tools/tests/test_m8_resource_ledger.py
  - crates/fava-diagnostics/src/lib.rs
  - crates/fava-diagnostics/tests/relay_facts.rs
  - apps/canary/src/artifacts.rs
  - apps/canary/src/m8_evidence.rs
  - docs/issues/0021-m8-resource-ledger-envelope.md
autonomous: true
requirements: [HARD-08, HARD-10]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every OPS-004 category has exactly one ledger row naming owner, limit source/value, max behavior, typed refusal/backpressure/shortfall, high-water/loss evidence, owner/public tests, and process-envelope field."
    - "For every bounded category, max is accepted and max+1 yields the exact refusal/backpressure/shortfall without mutating adjacent owner state."
    - "An empty externally influenced collection produces no work, no false success, and an exact zero envelope; bounded concurrent pressure has deterministic ordering and exact loss accounting."
    - "Diagnostics eviction reports per-category loss rather than silently dropping facts."
    - "Every success or failure bundle has bounded file count/bytes, sequenced evidence, resource maxima, exact failure ledger, and independent witness provenance."
  artifacts:
    - ".planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md"
    - "tools/tests/test_m8_resource_ledger.py"
    - "DiagnosticsSnapshot per-category loss/high-water fields using existing diagnostics vocabulary"
    - "apps/canary/src/m8_evidence.rs private evidence-envelope validation"
    - "docs/issues/0021-m8-resource-ledger-envelope.md"
  key_links:
    - "OPS-004 category -> owner limit -> exceed-limit test -> public behavior -> canary envelope field"
    - "Diagnostics::push_bounded -> exact loss count -> public snapshot -> resource envelope"
    - "RunArtifacts writes -> bounded manifest/artifact set -> independent process sampler -> final HARD-10 bundle"
  prohibitions:
    - statement: "A resource or evidence limit must not silently discard work or facts while the public result or phase artifact claims success."
      status: unverified
      flagged: true
      verification: unverified
---

<objective>
Create the exhaustive OPS-004 ownership ledger and the evidence machinery that makes every remaining bound measurable.

Purpose: Replace scattered bounds and RSS-only artifacts with a fail-closed owner-to-public-to-process contract before adding missing bounds.
Output: Validated ledger, diagnostics loss accounting, bounded resource/failure envelope, and resolved exact values for Plans 06-07.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESEARCH.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-PATTERNS.md
@crates/fava-diagnostics/src/lib.rs
@apps/canary/src/artifacts.rs
</context>

## Artifacts this phase produces

- `08-RESOURCE-LEDGER.md` with one exact row for query structure/derived values, router contributions/fan-out, active relay sessions, wire subscriptions, frame/message sizes, event-cache memory, write active/retained receipts, provider operations, observation delivery, diagnostics, fetched service entries, platform bridge queues, and bounded phase artifacts.
- `tools/tests/test_m8_resource_ledger.py` validating row completeness, uniqueness, exact test/envelope references, and no unresolved category.
- Private canary functions/types in `apps/canary/src/m8_evidence.rs` for envelope collection/validation; if a cross-crate/public nominal surface is forced, stop at Task 3 for architecture approval.
- Existing `DiagnosticsSnapshot` extended only with owner-local loss/high-water facts using approved vocabulary.

<slice_contract>
Issue: `docs/issues/0021-m8-resource-ledger-envelope.md`  
Branch: `m8/resource-ledger-envelope`  
Validation: ledger schema/coverage, diagnostics max/max+1/loss, artifact max/max+1/resource sampling, deliberate breaks  
Commit series: ledger/test RED -> diagnostics/envelope GREEN -> bound/vocabulary decision record
</slice_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Trace every OPS-004 category from owner to public/process evidence</name>
  <read_first>docs/spec/FULL_FAVA_REWRITE_SPEC_GOALS_AND_OBJECTIVES.md, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESEARCH.md, crates/fava/tests/write_bounds.rs, crates/fava/tests/observation_bounds.rs</read_first>
  <files>.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md, tools/tests/test_m8_resource_ledger.py, docs/issues/0021-m8-resource-ledger-envelope.md</files>
  <behavior>
    - Each authoritative OPS-004 bullet maps to one or more non-overlapping owner rows and every row reaches a public or process proof.
    - Existing bounds cite their actual source constant/configuration and current tests; absent bounds are marked blocking for Plans 05/06 rather than assigned invented values.
    - Every row defines max, max+1, empty, adjacent-owner, ordering/concurrency, precision, and resource-envelope evidence where applicable.
  </behavior>
  <action>Write the checker first and require it RED against the absent ledger. Inventory source owners, constants, queue types, retained sets/histories, and public reachability. Author the ledger with exact paths/symbols/current values; distinguish implemented/tested from missing. Include bounded phase artifacts as an externally influenced resource even though OPS-004's source list ends at platform bridge queues. Require one public action/API call to reach each owner, or mark the row unreachable and block. Do not choose values for missing session/provider/shutdown/artifact bounds in this task.</action>
  <acceptance_criteria>The ledger checker proves complete category coverage and every missing bound has an exact owner, forcing workload, and downstream plan rather than an empty/TBD row.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tools.tests.test_m8_resource_ledger</automated>
  </verify>
  <done>One validated ledger traces all OPS-004 resources to existing or explicitly planned behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add exact diagnostics loss and bounded evidence envelopes</name>
  <read_first>crates/fava-diagnostics/src/lib.rs, crates/fava-diagnostics/tests/relay_facts.rs, apps/canary/src/artifacts.rs, apps/canary/src/semantic_failure.rs</read_first>
  <files>crates/fava-diagnostics/src/lib.rs, crates/fava-diagnostics/tests/relay_facts.rs, apps/canary/src/artifacts.rs, apps/canary/src/m8_evidence.rs</files>
  <behavior>
    - Diagnostics retains at most the configured capacity per category and reports exact category loss/high-water after max+1 and burst pressure.
    - Max facts retain deterministic order; duplicate replacement does not fabricate loss.
    - Every evidence bundle bounds total files, total bytes, per-record text, sample count, and sampling deadline.
    - Resource envelope records peak RSS, file descriptors, tasks/threads, queues, relay sessions, wire subscriptions, diagnostics, artifact counts/bytes, and exact unsupported measurements.
    - A failed run still produces one valid bounded failure/replay/envelope/manifest bundle and reaps every child.
  </behavior>
  <action>Write max/max+1/duplicate/order diagnostics tests and artifact overflow/failure tests RED. Extend the existing diagnostics owner; do not create a second diagnostic store. Extend `RunArtifacts` and a private M8 evidence helper to sample independent OS/process facts at a declared cadence/deadline, collect owner-exported queue/session/subscription/diagnostic maxima, validate fixed bundle count/byte limits before hashing, and emit typed failure on overflow. Name `DELIBERATE_BREAK_M8_DIAGNOSTIC_LOSS_ENVELOPE`: checksum the four owned files, temporarily suppress diagnostics loss/high-water reporting or bypass the bundle byte/count refusal while code still compiles, require the exact max+1/overflow test to fail causally, restore checksums, and record PASS in issue 0021. Preserve redaction of raw seeds/secret material. Keep private evidence utilities below 500 lines by splitting private harness helpers if needed.</action>
  <acceptance_criteria>Diagnostics and artifacts expose exact boundedness/loss/high-water truth, and both successful-empty and failed runs have schema-valid bounded envelopes.</acceptance_criteria>
  <verify>
    <automated>cargo test -p fava-diagnostics --test relay_facts &amp;&amp; cargo test --manifest-path apps/canary/Cargo.toml --lib m8_evidence &amp;&amp; rg -n '^DELIBERATE_BREAK_M8_DIAGNOSTIC_LOSS_ENVELOPE: PASS ' docs/issues/0021-m8-resource-ledger-envelope.md</automated>
  </verify>
  <done>The phase has reusable, test-driven envelope machinery and no silent diagnostic/artifact loss.</done>
</task>

<task type="auto">
  <name>Task 3: Seal the resolved owner-local bound table and vocabulary boundary</name>
  <read_first>.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md, docs/issues/0021-m8-resource-ledger-envelope.md, docs/internals/vocabulary.toml</read_first>
  <files>.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md, docs/issues/0021-m8-resource-ledger-envelope.md</files>
  <context>Research resolves the private reversible values: 256 active sessions, existing 256 diagnostics facts/category, 64 runtime operations/channels, 5-second operation and shutdown deadlines, existing 1,048,576-byte process output, 1,048,576 bytes/evidence stream, and 8,388,608 bytes/run. Only the separate runtime contract/implementation vocabulary requires Plan 07's architecture checkpoint.</context>
  <action>Copy the resolved table and evidence sources into the ledger and issue 0021, classify each row as existing source-owned or Phase-08 owner-private, and require max/max+1 or deadline tests for each. Record `M8_RESOURCE_BOUND_DECISION: OWNER_LOCAL_RESOLVED`; do not add a public configuration noun, persisted entity, or substitute value. Route runtime contract/crate vocabulary exclusively to Plan 07.</action>
  <acceptance_criteria>Every previously missing ledger row has the exact resolved value/source/test and no unapproved vocabulary expansion.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tools.tests.test_m8_resource_ledger &amp;&amp; python3 tools/check_vocabulary.py &amp;&amp; rg -n '^M8_RESOURCE_BOUND_DECISION: OWNER_LOCAL_RESOLVED ' docs/issues/0021-m8-resource-ledger-envelope.md</automated>
  </verify>
  <done>The owner-local table is deterministic and all vocabulary expansion remains outside this slice.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

## Trust Boundaries

| Boundary | Description |
|---|---|
| external work -> owner resource | Untrusted volume can consume queues, sets, sessions, histories, or memory. |
| owner facts -> diagnostics | Bounded current facts can be evicted or misattributed. |
| process/OS -> evidence bundle | Independent measurements and failure text become milestone proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-15 | Denial of Service | OPS-004 resources | high | mitigate | Exhaustive ledger, exact maxima, max+1 tests, owner/public reachability. |
| T-08-16 | Repudiation | diagnostics loss | high | mitigate | Per-category exact loss/high-water and deterministic retention tests. |
| T-08-17 | Tampering | resource envelope | high | mitigate | Independent sampler provenance, sequenced records, bounded hashed manifest. |
| T-08-18 | Information Disclosure | failure artifacts | high | mitigate | Existing seed redaction, bounded text, explicit sensitive-field negative fixtures. |
| T-08-26 | Tampering | dependencies | low | accept | No package install or dependency change; existing locked crates only. |
</threat_model>

<verification>
Ledger schema/coverage, diagnostics and artifact max/max+1/empty/order/concurrency/precision, canary library, vocabulary, line, diff, and recorded decision gates pass.
</verification>

<success_criteria>
Every external resource has a checked owner/evidence path and later plans can implement only measured, approved gaps.
</success_criteria>

<output>
Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-05-SUMMARY.md` when done.
</output>
- `docs/issues/0021-m8-resource-ledger-envelope.md` with `DELIBERATE_BREAK_M8_DIAGNOSTIC_LOSS_ENVELOPE`.
