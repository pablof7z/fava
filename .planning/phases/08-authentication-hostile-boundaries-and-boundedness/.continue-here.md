---
context: phase
phase: 08-authentication-hostile-boundaries-and-boundedness
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-08-22T05:48:41.633Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual execution failures. Explain each in your own words and name its structural prevention before continuing.

- [ ] **Agent branch namespace:** A sequential GSD executor in this linked checkout refused to commit from `milestone/m8-auth-hostile-limits`. Resume on a fresh `worktree-agent-*` branch created from the remote handoff branch; integrate to the milestone branch only after the plan completes.
- [ ] **Dirty-WIP isolation:** Plans 08-03 and 08-04 began from uncommitted authoritative bytes. Do not create an isolated executor from an older clean base or run their executors concurrently. Continue Plan 08-03 first, then Plan 08-04, staging only each plan's declared files.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Commit from the milestone branch in a linked executor worktree | Plan 08-01 passed every gate but the mandatory branch guard rejected its commit. | blocking | Create/use a `worktree-agent-*` continuation branch before executor dispatch, then fast-forward the milestone branch after plan completion. |
| Reconstruct or isolate the preserved dirty WIP | Worktree isolation does not contain uncommitted source bytes and parallel agents can stage another plan's changes. | blocking | Resume from the published handoff snapshot; execute Plans 08-03 and 08-04 sequentially with explicit file ownership. |
| Treat snapshot commit `f3ce04d` as Plan 08-04 completion | The commit exists only because Git cannot publish an uncommitted working tree. | advisory | Keep Plan 08-04 incomplete until its RED, GREEN, deliberate break, full gates, and SUMMARY all pass. |
| Reset to the original planning hash | Plan 08-02 legitimately changed the earlier dirty-patch fingerprint. | advisory | Use the published handoff branch and recorded per-file hashes; never reset/restore/apply the old `e7710b...` witness. |

<current_state>

Phase 08 is executing. Plans 08-01 and 08-02 are complete. Plan 08-03 is paused after Task 1 at its mandatory post-tracer human verification checkpoint.

- Milestone baseline: `e928c633f59917dec02b8f67fb80695b6944ad15` (`milestone/m8-auth-hostile-limits`).
- Plan 08-03 Task 1: `ffeb622472041f21884f6066fe4ce7ce33afcc9f`.
- Remote handoff snapshot: branch `handoff/m8-phase8-plan03-checkpoint`.
- Plan 08-04 non-completion WIP snapshot: `f3ce04d84d59aa46df67f6ed4b8183268074726a`.
- Pre-rebase stash backup: `5faecf42c0ec903507e3faeb04962f4680a9cb44`, published as `backup/m8-pre-rebase-autostash`.
- No server, watcher, test, or async external job is running.

The Plan 08-03 tracer verified:

- exact 1024-byte frame admitted intact;
- exact 1025-byte frame refused as scoped `InvalidFrame`;
- public hostile ingress 2/2 passing;
- websocket transport conformance 6/6 passing;
- all extracted code files below 500 lines.

</current_state>

<completed_work>

- Plan 08-01 complete: Go 1.25.14 Khatru fixture, pinned-module verification, live NIP-11 readiness, bounded teardown.
- Plan 08-02 complete: public delivery contract closure, attempt/spent budget split, Bazel registration.
- Plan 08-03 Task 1 complete in `ffeb622`: exact bounded websocket ingress, private relay admission extraction, hostile-case support extraction.
- Plan 08-04's four pre-existing store edits are preserved remotely in `f3ce04d`; they are not validated or complete.

</completed_work>

<remaining_work>

- Plan 08-03 Task 2: after tracer approval, add the complete hostile-class precision matrix RED, implement exact scoped outcomes, and keep healthy concurrent work live.
- Plan 08-03 Task 3: add Bazel registration, prove `DELIBERATE_BREAK_M8_HOSTILE_ADMISSION`, run Cargo/Bazel/Clippy/vocabulary/line/diff gates, and create `08-03-SUMMARY.md`.
- Integrate completed Plan 08-03 to `milestone/m8-auth-hostile-limits`.
- Execute Plan 08-04 sequentially from the preserved store snapshot; do not infer completion from `f3ce04d`.
- Continue Phase 08 Plans 05-18 through `$gsd-execute-phase 8` after the WIP adoption plans close.

</remaining_work>

<decisions_made>

- Go is test-fixture infrastructure only; Fava remains Rust.
- Plans 08-03 and 08-04 are serialized because their initial source of truth was dirty working-tree state.
- `stash@{0}` was never applied or dropped; its exact object is published as a remote backup ref.
- `.gsd/` and `.planning/milestone.lock` are machine-local execution artifacts and are intentionally excluded from the remote snapshot.

</decisions_made>

<blockers>

- Human verification: approve the Plan 08-03 tracer before Tasks 2-3.
- Structural resume prerequisite: use a `worktree-agent-*` branch, not the milestone or `handoff/*` branch, for the continuation executor's commits.

</blockers>

## Required Reading (in order)

1. `AGENTS.md` — repository authority, workflow, and architecture gates.
2. This `.continue-here.md` and `.planning/HANDOFF.json` — exact checkpoint and remote refs.
3. `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-03-PLAN.md` — current task contract.
4. `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESEARCH.md` and `08-PATTERNS.md` — Phase 08 design constraints.
5. `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-02-SUMMARY.md` — delivery contract already closed.
6. `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-04-PLAN.md` — ownership of snapshot commit `f3ce04d`.
7. `.planning/METHODOLOGY.md`, if present — project analytical lenses.

## Infrastructure State

- Repository: `https://github.com/pablof7z/fava.git`.
- Go: 1.25.14 was verified locally for the Khatru fixture; re-check on the new computer before Plan 08-16.
- Background processes: none.
- Async external jobs: none.
- Remote backup ref: `refs/heads/backup/m8-pre-rebase-autostash`.

<context>

The correct next move is continuation, not replanning or reapplying the old stash. The handoff branch already contains all committed Phase 08 work through Plan 08-03 Task 1 plus the exact four-file Plan 08-04 WIP snapshot. Preserve the causal RED→GREEN sequence from Task 2 onward and do not let the WIP snapshot satisfy any plan gate by existence alone.

</context>

<next_action>

Clone/fetch `origin/handoff/m8-phase8-plan03-checkpoint`, create a local `worktree-agent-m8-p03-resume` branch from it, run `$gsd-resume-work`, explicitly acknowledge the two blocking constraints, approve the tracer, and continue Plan 08-03 Tasks 2-3.

</next_action>
