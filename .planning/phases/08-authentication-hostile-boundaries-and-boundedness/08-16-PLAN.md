---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 16
type: execute
wave: 11
depends_on: [08-01, 08-09, 08-14, 08-15]
files_modified:
  - apps/canary/scenarios.json
  - apps/canary/src/lib.rs
  - apps/canary/src/lib_tests.rs
  - apps/canary/src/main.rs
  - apps/canary/src/relay.rs
  - apps/canary/src/proxy.rs
  - apps/canary/src/m8.rs
  - features/relay-authentication.feature
  - features/relay-limits.feature
  - tools/tests/test_m8_feature_mapping.py
  - docs/issues/0010-authentication-hostile-limits-and-boundedness.md
  - docs/issues/0028-m8-auth-limit-canaries.md
autonomous: true
requirements: [HARD-01, HARD-02, HARD-04, HARD-08, HARD-10]
estimate: {tokens: 34000, raw_tokens: 34000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "NIP-42 reconnect/persistence uses nostr-rs-relay; account isolation uses Runtime-backed auth."
    - "Khatru real NIP-11 drives exact shortfall and proxy proves zero invalid wire; core read/publish works."
    - "Four IDs through Plans 15-16 are exact, enabled, mapped, bounded, and independently witnessed."
  artifacts: ["nip42-write-and-reconnect", "auth-account-isolation", "relay-limit-shortfall"]
  key_links: ["real relay challenge/AUTH/restart/db -> public receipt", "Khatru NIP-11 -> exact plan/shortfall -> proxy zero-wire"]
  prohibitions:
    - statement: "Auth identity may not derive from authorship/current account, and invalid shortfall work may not cross wire."
      status: unverified
      flagged: true
      verification: process canaries and named breaks
---

<objective>
Add persistent auth, account isolation, and NIP-11 no-wire real-process canaries.

Purpose: Close HARD-01/02/04 after runtime migration/audit and Go readiness.
Output: Three exact IDs, two relay implementations, feature mappings, breaks.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-01-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-09-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-14-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-15-SUMMARY.md
</context>

## Artifacts this phase produces

- Exact CLI IDs `nip42-write-and-reconnect`, `auth-account-isolation`, `relay-limit-shortfall`.
- Persistent `nostr-rs-relay` profile, Khatru core/NIP-11 profile, auth-generation/limit-no-wire breaks.

<slice_contract>
Issue: `docs/issues/0028-m8-auth-limit-canaries.md`
Branch: `m8/auth-limit-canaries`
Validation: real relay/Khatru processes, persistence/no-wire, exact mappings/breaks
Commit series: persistent auth tracer -> account/limit expansion -> mapping/break seal
</slice_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Authenticate, publish, restart, rechallenge, prove persistence</name>
  <read_first>apps/canary/src/relay.rs, apps/canary/src/proxy.rs, features/relay-authentication.feature, docs/spec/FAVA_REWRITE_IMPLEMENTATION_PLAN.md</read_first>
  <files>apps/canary/src/relay.rs, apps/canary/src/proxy.rs, apps/canary/src/m8.rs, features/relay-authentication.feature, docs/issues/0010-authentication-hostile-limits-and-boundedness.md</files>
  <behavior>
    - nostr-rs-relay challenges, accepts AUTH/write, persists, restarts same directory, issues fresh challenge, and public/db/log facts agree.
  </behavior>
  <action>Write RED then implement real persistent relay scenario through public Fava and exact proxy barriers. Map the existing authentication feature and update issue 0010 only with this externally witnessed closure. Do not cite Khatru memory for persistence or add auth vocabulary.</action>
  <acceptance_criteria>Fresh-generation authentication and durable event survive real process restart.</acceptance_criteria>
  <verify><automated>cargo test --manifest-path apps/canary/Cargo.toml --lib nip42_write_and_reconnect</automated></verify>
  <done>Persistent NIP-42 capstone is executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add account isolation and Khatru limit/no-wire</name>
  <read_first>apps/canary/relays/khatru/probe.sh, apps/canary/src/m8.rs, apps/canary/src/proxy.rs, features/relay-limits.feature</read_first>
  <files>apps/canary/src/m8.rs, apps/canary/src/relay.rs, apps/canary/src/proxy.rs, apps/canary/src/lib_tests.rs, features/relay-limits.feature</files>
  <behavior>
    - Denied/stalled access cannot delay another; Khatru core works; exact advertised shortfall sends zero invalid REQ/EVENT bytes.
  </behavior>
  <action>Write RED then implement both through public Fava, Plan 09 Runtime auth/NIP-11, Plan 15 process harness, and real Khatru probe under Go 1.25. Use barriers and proxy zero-wire evidence.</action>
  <acceptance_criteria>Both canaries pass exact independent process/wire/resource evidence.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test --manifest-path apps/canary/Cargo.toml --lib auth_account_isolation
cargo test --manifest-path apps/canary/Cargo.toml --lib relay_limit_shortfall
(cd apps/canary/relays/khatru &amp;&amp; go version | rg 'go1\.25\.' &amp;&amp; GOTOOLCHAIN=local go mod verify &amp;&amp; GOTOOLCHAIN=local go test ./... &amp;&amp; ./probe.sh)</automated></verify>
  <done>Account isolation and Khatru limit proof are executable.</done>
</task>

<task type="auto">
  <name>Task 3: Map four IDs and kill auth-generation/limit-wire bypasses</name>
  <read_first>apps/canary/scenarios.json, apps/canary/src/lib.rs, apps/canary/src/main.rs, tools/tests/test_m8_feature_mapping.py, docs/issues/0028-m8-auth-limit-canaries.md</read_first>
  <files>apps/canary/scenarios.json, apps/canary/src/lib.rs, apps/canary/src/main.rs, tools/tests/test_m8_feature_mapping.py, docs/issues/0028-m8-auth-limit-canaries.md</files>
  <action>Add exact mappings for all four IDs through Plans 15-16. Run `DELIBERATE_BREAK_M8_AUTH_GENERATION` and `DELIBERATE_BREAK_M8_LIMIT_NO_WIRE`, require precise CLI/mapper failures, restore, record PASS.</action>
  <acceptance_criteria>Four exact IDs are mapped/causal and no stale-auth or invalid-wire path survives.</acceptance_criteria>
  <verify><automated>set -euo pipefail
python3 -m unittest tools.tests.test_m8_feature_mapping
cargo test --manifest-path apps/canary/Cargo.toml --all-targets
rg -n '^DELIBERATE_BREAK_M8_AUTH_GENERATION: PASS ' docs/issues/0028-m8-auth-limit-canaries.md
rg -n '^DELIBERATE_BREAK_M8_LIMIT_NO_WIRE: PASS ' docs/issues/0028-m8-auth-limit-canaries.md
git diff --check</automated></verify>
  <done>Auth/limit canaries are registered, bounded, and causal.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-67 | Spoofing | stale AUTH | high | mitigate | Fresh challenge/generation break. |
| T-08-68 | Elevation of Privilege | cross-account auth | high | mitigate | Distinct RelayAccess/concurrency witness. |
| T-08-69 | Tampering | NIP-11 omission | high | mitigate | Exact shortfall/proxy zero-wire. |
| T-08-70 | Repudiation | persistence | high | mitigate | Database/log/public agreement. |
| T-08-71 | Tampering | Go fixture | high | mitigate | Go 1.25 module verify/test/probe. |
</threat_model>

<verification>Three exact canaries, two relays, mappings, breaks, resource evidence pass.</verification>
<success_criteria>Real auth/account/NIP-11 behavior is independently proven after runtime closure.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-16-SUMMARY.md` when done.</output>
