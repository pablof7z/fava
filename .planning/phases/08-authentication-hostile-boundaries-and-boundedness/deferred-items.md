# Deferred Items

## Plan 08-02

- `python3 tools/check_vocabulary.py` reports the pre-existing specified crate
  `fava-runtime-tokio` as undocumented. Plan 08-07 owns the blocking vocabulary
  decision and registry change for that crate, so Plan 08-02 did not approve or
  modify architectural vocabulary. All Plan 08-02 public symbols and crates are
  already registered, and its Cargo, Clippy, Bazel, line, whitespace, and
  deliberate-break gates pass.
