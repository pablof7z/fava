---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 13
type: execute
wave: 8
depends_on: [08-02, 08-09]
files_modified:
  - crates/fava-publisher-nip01/src/lib.rs
  - crates/fava-publisher-nip01/tests/runtime_ownership.rs
  - crates/fava-publication/src/delivery.rs
  - crates/fava-publication/src/runtime_delivery.rs
  - crates/fava-publication/src/runtime_signer.rs
  - crates/fava/tests/runtime_delivery.rs
  - docs/issues/0032-m8-runtime-delivery-signer.md
autonomous: true
requirements: [HARD-05, HARD-06, HARD-07, HARD-09]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Publisher futures, signer work, delivery retries, timers, cancellation, joins, and shutdown deadlines execute through Runtime."
    - "Publisher and delivery/signer each first fail an exact compiled public test against still-unmigrated production."
    - "Delivery policy/receipt authority remains in universal owners and accepts only exact correlated typed completions."
  artifacts:
    - "runtime_publisher_future_uses_runtime"
    - "runtime_delivery_signer_uses_runtime"
    - "fava_publication private runtime_delivery and runtime_signer adapters"
    - "docs/issues/0032-m8-runtime-delivery-signer.md"
  key_links:
    - "write authorization -> Runtime publisher/signer work -> exact handoff/completion -> delivery/receipt owner"
  prohibitions:
    - statement: "Publisher, signer, or delivery owners must not regain local execution-resource authority."
      status: unverified
      flagged: true
      verification: two compiled RED/GREEN/break cycles and Plan 14 audit
---

<objective>
Migrate publisher, signer, and delivery execution after the adopted delivery contract and complete pre-migration Runtime graph.

Purpose: Preserve exact attempt/ambiguity semantics while centralizing execution resources in the authoritative provider boundary.
Output: Publisher and delivery/signer RED commits, private adapters, public/owner tests, and causal breaks.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-02-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-09-SUMMARY.md
@crates/fava-publication/src/delivery.rs
@crates/fava-publisher-nip01/src/lib.rs
</context>

## Artifacts this phase produces

- Private `fava_publication::runtime_delivery` and `runtime_signer` adapters; no parallel helper contract.
- Tests `runtime_publisher_future_uses_runtime` and `runtime_delivery_signer_uses_runtime`.
- Markers `DELIBERATE_BREAK_M8_RUNTIME_PUBLISHER_FUTURE` and `DELIBERATE_BREAK_M8_RUNTIME_DELIVERY_SIGNER`.

<slice_contract>
Issue: `docs/issues/0032-m8-runtime-delivery-signer.md`
Branch: `m8/runtime-delivery-signer`
Validation: exact compiled RED per owner -> GREEN -> same-test type-correct break -> restoration -> public/provider matrix
Commit series: publisher RED/GREEN/break -> delivery/signer RED/GREEN/break -> five-mode seal
</slice_contract>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Compile publisher RED against direct future execution, then migrate it</name>
  <read_first>crates/fava-publisher-nip01/src/lib.rs, crates/fava-publisher-nip01/tests/runtime_ownership.rs, crates/fava-publication/src/runtime_delivery.rs, crates/fava/tests/runtime_delivery.rs, docs/issues/0032-m8-runtime-delivery-signer.md</read_first>
  <files>crates/fava-publisher-nip01/src/lib.rs, crates/fava-publisher-nip01/tests/runtime_ownership.rs, crates/fava-publication/src/runtime_delivery.rs, crates/fava/tests/runtime_delivery.rs, docs/issues/0032-m8-runtime-delivery-signer.md</files>
  <behavior>
    - Recording Runtime observes publisher future with exact delivery operation/generation.
    - Handoff remains provider-produced typed evidence.
    - Pre-migration production fails with `publisher future bypassed Runtime`.
  </behavior>
  <action>Replace or extend Plan 09's public delivery and publisher-provider scaffolds with exact test `runtime_publisher_future_uses_runtime`; remove smoke markers only after real behavioral setup exists. Capture its single Cargo filter against still-unmigrated publisher production. Require nonzero plus exact `publisher future bypassed Runtime`; reject compiler/import/syntax/unrelated failures, then commit RED. Migrate future execution/panic isolation/cancellation through the private adapter while preserving the neutral outcome and handoff fact. Pass tests. Run type-correct `DELIBERATE_BREAK_M8_RUNTIME_PUBLISHER_FUTURE` by directly executing the tested future; require same assertion, restore, record PASS. Edit no metadata.</action>
  <acceptance_criteria>The RED proves direct publisher execution is the cause; GREEN/break prove Runtime execution without changing delivery truth.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava --test runtime_delivery runtime_publisher_future_uses_runtime -- --exact --nocapture
cargo test -p fava-publisher-nip01 --test runtime_ownership
rg -n '^DELIBERATE_BREAK_M8_RUNTIME_PUBLISHER_FUTURE: PASS ' docs/issues/0032-m8-runtime-delivery-signer.md
git diff --check</automated></verify>
  <done>Publisher futures execute through Runtime with exact handoff correlation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile delivery/signer RED, then migrate every remaining execution resource</name>
  <read_first>crates/fava-publication/src/delivery.rs, crates/fava-publication/src/runtime_delivery.rs, crates/fava-publication/src/runtime_signer.rs, crates/fava/tests/runtime_delivery.rs, docs/issues/0032-m8-runtime-delivery-signer.md</read_first>
  <files>crates/fava-publication/src/delivery.rs, crates/fava-publication/src/runtime_delivery.rs, crates/fava-publication/src/runtime_signer.rs, crates/fava/tests/runtime_delivery.rs, docs/issues/0032-m8-runtime-delivery-signer.md</files>
  <behavior>
    - Signer work, retry timers, cancellation, joins, and shutdown deadlines use selected Runtime.
    - Exact operation/generation gates every completion.
    - Pre-migration production fails with `delivery/signer execution bypassed Runtime`.
  </behavior>
  <action>Add exact test `runtime_delivery_signer_uses_runtime`; capture its Cargo filter against still-unmigrated delivery/signer production. Require nonzero plus exact `delivery/signer execution bypassed Runtime`; reject compiler/import/syntax/unrelated failures, commit RED. Migrate signer work, delivery tasks, retry timers, cancellation, joins, and shutdown deadlines through Runtime; keep attempt accounting, ambiguity, revalidation, and receipt mutation in existing owners. Pass test. Run type-correct `DELIBERATE_BREAK_M8_RUNTIME_DELIVERY_SIGNER` by bypassing one signer/retry resource; require same assertion, restore, record PASS. Edit no metadata.</action>
  <acceptance_criteria>The RED identifies only delivery/signer bypass; GREEN and same-test break cover every assigned resource.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava --test runtime_delivery runtime_delivery_signer_uses_runtime -- --exact --nocapture
rg -n '^DELIBERATE_BREAK_M8_RUNTIME_DELIVERY_SIGNER: PASS ' docs/issues/0032-m8-runtime-delivery-signer.md
git diff --check</automated></verify>
  <done>Signer and delivery execution resources are Runtime-owned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seal public/provider failure isolation and correlated completion</name>
  <read_first>crates/fava-publisher-nip01/tests/runtime_ownership.rs, crates/fava/tests/runtime_delivery.rs, docs/issues/0032-m8-runtime-delivery-signer.md</read_first>
  <files>crates/fava-publisher-nip01/tests/runtime_ownership.rs, crates/fava/tests/runtime_delivery.rs, docs/issues/0032-m8-runtime-delivery-signer.md</files>
  <behavior>
    - Panic, block, late, malformed, and cancellation-ignore modes are exact-operation scoped.
    - Adjacent delivery reaches an independent outcome and shutdown reaps within deadline.
    - Both RED commit hashes and restored break checksums are recorded.
  </behavior>
  <action>Expand only owner/provider and public tests across the five required hostile modes with controlled barriers and recording Runtime. Assert exact operation/generation, unrelated progress, bounded evidence, and bounded shutdown/reap. Record both causal RED commit hashes and restored break checksums. Use source-shape scans only as backstop; add no executor, timer, or provider abstraction.</action>
  <acceptance_criteria>The matrix proves exact correlation, adjacent progress, bounded shutdown, and traceable RED/break provenance.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava-publisher-nip01 --test runtime_ownership
cargo test -p fava --test runtime_delivery
rg -n '^COMPILED_RED_(PUBLISHER|DELIVERY_SIGNER)_COMMIT: [0-9a-f]{7,40}$' docs/issues/0032-m8-runtime-delivery-signer.md
git diff --check</automated></verify>
  <done>Publisher/delivery/signer isolation is behavior-complete.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Boundary | Description |
|---|---|
| delivery/signing owner -> Runtime/provider | Authorized sensitive work returns untrusted timing, panic, handoff, and completion evidence. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-55 | Spoofing | signer/delivery completion | high | mitigate | Exact operation/generation correlation. |
| T-08-56 | Tampering | handoff/attempt truth | high | mitigate | Typed outcome and owner-only mutation. |
| T-08-57 | Denial of Service | blocked/cancel-ignore work | high | mitigate | Runtime capacity/deadline/cancel/join. |
| T-08-58 | Repudiation | ambiguity/failure evidence | high | mitigate | Public/provider five-mode matrix. |
</threat_model>

<verification>Both owner groups have compiled RED commits before migration, GREEN tests, same-test compiling breaks, and five-mode isolation evidence.</verification>
<success_criteria>Publisher, signer, and delivery use Runtime while write/delivery owners retain semantic authority.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-13-SUMMARY.md` when done.</output>
