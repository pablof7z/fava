---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 11
type: execute
wave: 7
depends_on: [08-09]
files_modified:
  - crates/fava-auth/src/lib.rs
  - crates/fava-auth/src/runtime.rs
  - crates/fava-auth/tests/authentication.rs
  - crates/fava-routing/src/chain.rs
  - crates/fava-routing/src/runtime.rs
  - crates/fava-routing/tests/failure_isolation.rs
  - crates/fava-nip11-http/src/lib.rs
  - crates/fava-nip11-http/src/runtime.rs
  - crates/fava-nip11-http/tests/runtime_ownership.rs
  - crates/fava/tests/runtime_auth_routing.rs
  - docs/issues/0027-m8-runtime-auth-routing-nip11.md
autonomous: true
requirements: [HARD-01, HARD-02, HARD-04, HARD-09]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Authentication, routing, and NIP-11 owners authorize work through Runtime and consume only exact operation/generation-correlated completions."
    - "Each still-unmigrated owner first fails one exact compiled behavioral test for the intended bypass, then passes after migration."
    - "Panic, late completion, cancellation-ignore, and deadline failure remain scoped while adjacent owner work completes."
  artifacts:
    - "runtime_auth_provider_uses_runtime"
    - "runtime_routing_tasks_use_runtime"
    - "runtime_nip11_deadline_uses_runtime"
    - "docs/issues/0027-m8-runtime-auth-routing-nip11.md"
  key_links:
    - "FavaBuilder-selected Runtime -> auth/routing/NIP-11 authorization -> correlated typed completion -> universal owner mutation"
  prohibitions:
    - statement: "Auth, routing, or NIP-11 providers must not retain owner-local execution-resource authority."
      status: unverified
      flagged: true
      verification: three compiled RED/GREEN cycles, three compiling breaks, and Plan 14 audit
---

<objective>
Migrate auth, routing, and the confirmed NIP-11 deadline bypass only after the complete pre-migration Runtime build graph compiles.

Purpose: Prove causally that each universal owner delegates execution while retaining semantic authorization.
Output: Three exact compiled RED/GREEN migrations, owner/public tests, private adapters, and deliberate-break records.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-09-SUMMARY.md
@docs/spec/ARCHITECTURE.md
@crates/fava-auth/src/lib.rs
@crates/fava-routing/src/chain.rs
@crates/fava-nip11-http/src/lib.rs
</context>

## Artifacts this phase produces

- Private `fava-auth::runtime`, `fava-routing::runtime`, and `fava-nip11-http::runtime` adapters; no new public/cross-crate noun.
- Public tests `runtime_auth_provider_uses_runtime`, `runtime_routing_tasks_use_runtime`, and `runtime_nip11_deadline_uses_runtime`.
- Markers `DELIBERATE_BREAK_M8_RUNTIME_AUTH_EXECUTION`, `DELIBERATE_BREAK_M8_RUNTIME_ROUTING_EXECUTION`, and `DELIBERATE_BREAK_M8_NIP11_RUNTIME_TIMEOUT`.

<slice_contract>
Issue: `docs/issues/0027-m8-runtime-auth-routing-nip11.md`
Branch: `m8/runtime-auth-routing-nip11`
Validation: exact compiled RED commit per owner -> GREEN -> same-test type-correct break -> restoration -> owner/public Cargo and Bazel
Commit series: auth RED/GREEN/break -> routing RED/GREEN/break -> NIP-11 RED/GREEN/break
</slice_contract>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Compile auth RED against the owner-local provider path, then migrate it</name>
  <read_first>crates/fava-auth/src/lib.rs, crates/fava-auth/tests/authentication.rs, crates/fava/tests/runtime_auth_routing.rs, crates/fava-runtime/src/lib.rs, docs/issues/0027-m8-runtime-auth-routing-nip11.md</read_first>
  <files>crates/fava-auth/src/lib.rs, crates/fava-auth/src/runtime.rs, crates/fava-auth/tests/authentication.rs, crates/fava/tests/runtime_auth_routing.rs, docs/issues/0027-m8-runtime-auth-routing-nip11.md</files>
  <behavior>
    - Recording Runtime observes auth-provider work with exact access/generation identity.
    - Provider panic or late completion changes only that authentication operation.
    - Pre-migration production fails with exact assertion `authentication provider bypassed Runtime`.
  </behavior>
  <action>Replace or extend Plan 09's `runtime_auth_routing.rs` compile scaffold with exact public test `runtime_auth_provider_uses_runtime`, removing the scaffold marker only after real behavioral setup exists. Capture the single filtered Cargo command's status/output against still-unmigrated auth; require nonzero plus exact `authentication provider bypassed Runtime`, and reject `error[E`, `could not compile`, import, syntax, or unrelated assertion output. Commit causal RED alone. Implement the private adapter so auth authorizes work and accepts only exact correlated completion; pass public/owner tests. Run type-correct `DELIBERATE_BREAK_M8_RUNTIME_AUTH_EXECUTION` by routing work around the recording Runtime, require the same test/assertion, restore checksums, record PASS. Edit no metadata.</action>
  <acceptance_criteria>The RED fails only because unmigrated auth bypasses Runtime; GREEN and the compiling break prove exact auth execution ownership.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava --test runtime_auth_routing runtime_auth_provider_uses_runtime -- --exact --nocapture
cargo test -p fava-auth --test authentication
rg -n '^DELIBERATE_BREAK_M8_RUNTIME_AUTH_EXECUTION: PASS ' docs/issues/0027-m8-runtime-auth-routing-nip11.md
git diff --check</automated></verify>
  <done>Authentication provider execution is Runtime-owned with a preserved causal RED commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile routing RED against local task execution, then migrate it</name>
  <read_first>crates/fava-routing/src/chain.rs, crates/fava-routing/tests/failure_isolation.rs, crates/fava/tests/runtime_auth_routing.rs, crates/fava-runtime/src/lib.rs, docs/issues/0027-m8-runtime-auth-routing-nip11.md</read_first>
  <files>crates/fava-routing/src/chain.rs, crates/fava-routing/src/runtime.rs, crates/fava-routing/tests/failure_isolation.rs, crates/fava/tests/runtime_auth_routing.rs, docs/issues/0027-m8-runtime-auth-routing-nip11.md</files>
  <behavior>
    - Recording Runtime observes each routed operation with exact correlation.
    - Panic, cancellation-ignore, and stale completion cannot mutate another operation.
    - Pre-migration production fails with `routing execution bypassed Runtime`.
  </behavior>
  <action>Replace or extend Plan 09's routing provider scaffold and extend the public behavioral file with exact test `runtime_routing_tasks_use_runtime`; no smoke marker may count as behavior. Capture its single Cargo filter against still-unmigrated routing production. Require nonzero plus exact `routing execution bypassed Runtime`; reject compiler/import/syntax failures and every other assertion, then commit RED. Migrate task/timer/cancel/join use to Runtime without moving routing policy. Pass public/owner tests. Run type-correct `DELIBERATE_BREAK_M8_RUNTIME_ROUTING_EXECUTION` using one local task; require the same compiled assertion, restore, record PASS. Edit no metadata.</action>
  <acceptance_criteria>The RED identifies only the routing bypass, and GREEN/break prove Runtime execution plus owner-held route authority.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava --test runtime_auth_routing runtime_routing_tasks_use_runtime -- --exact --nocapture
cargo test -p fava-routing --test failure_isolation
rg -n '^DELIBERATE_BREAK_M8_RUNTIME_ROUTING_EXECUTION: PASS ' docs/issues/0027-m8-runtime-auth-routing-nip11.md
git diff --check</automated></verify>
  <done>Routing execution is Runtime-owned and correlation remains owner-authoritative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compile NIP-11 RED against the confirmed timeout, then migrate it</name>
  <read_first>crates/fava-nip11-http/src/lib.rs, crates/fava-nip11-http/tests/runtime_ownership.rs, crates/fava/tests/runtime_auth_routing.rs, crates/fava-runtime/src/lib.rs, docs/issues/0027-m8-runtime-auth-routing-nip11.md</read_first>
  <files>crates/fava-nip11-http/src/lib.rs, crates/fava-nip11-http/src/runtime.rs, crates/fava-nip11-http/tests/runtime_ownership.rs, crates/fava/tests/runtime_auth_routing.rs, docs/issues/0027-m8-runtime-auth-routing-nip11.md</files>
  <behavior>
    - NIP-11 deadline/cancellation are selected Runtime resources.
    - Completion is correlated to exact relay/session generation.
    - Confirmed pre-migration timeout fails with `NIP-11 acquisition bypassed Runtime`.
  </behavior>
  <action>Replace or extend Plan 09's NIP-11 provider scaffold and extend the public behavioral file with exact test `runtime_nip11_deadline_uses_runtime`; remove scaffold markers only after real setup exists. Capture its single Cargo filter against the live provider timeout before production edits. Require nonzero plus exact `NIP-11 acquisition bypassed Runtime`; reject compiler/import/syntax/unrelated failures and commit RED. Replace the timeout with Runtime timer/cancellation/completion use while the owner retains parsing/projection authority. Pass tests. Run type-correct `DELIBERATE_BREAK_M8_NIP11_RUNTIME_TIMEOUT` by restoring a local timeout only at that seam; require the same compiled assertion, restore, record PASS. Edit no metadata; the negative source scan is only a backstop.</action>
  <acceptance_criteria>The live timeout is removed only after its compiled RED commit, and the break proves the deadline cannot bypass Runtime.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava --test runtime_auth_routing runtime_nip11_deadline_uses_runtime -- --exact --nocapture
cargo test -p fava-nip11-http --test runtime_ownership
rg -n '^DELIBERATE_BREAK_M8_NIP11_RUNTIME_TIMEOUT: PASS ' docs/issues/0027-m8-runtime-auth-routing-nip11.md
rg -n 'tokio::time::timeout' crates/fava-nip11-http/src/lib.rs &amp;&amp; exit 1 || true
git diff --check</automated></verify>
  <done>NIP-11 deadlines are Runtime-owned with causal compiled proof.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Boundary | Description |
|---|---|
| auth/routing/NIP-11 owner -> Runtime provider | Semantic authorization crosses into replaceable execution and returns untrusted completion facts. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-45 | Spoofing | auth completion identity | high | mitigate | Exact access/generation correlation and stale-completion test. |
| T-08-46 | Elevation of Privilege | auth execution | high | mitigate | Runtime-only execution and causal bypass break. |
| T-08-47 | Tampering | routing completion | high | mitigate | Owner authorization plus operation/generation match. |
| T-08-48 | Denial of Service | NIP-11 deadline/cancel | high | mitigate | Runtime deadline and bounded cancel/join test. |
| T-08-49 | Repudiation | provider failure | medium | mitigate | Exact typed completion and public/owner evidence. |
</threat_model>

<verification>Each owner has a committed exact compiled RED against unmigrated production, GREEN owner/public tests, a same-test compiling break, and no metadata mutation.</verification>
<success_criteria>Auth, routing, and NIP-11 use only selected Runtime resources with exact scoped completions.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-11-SUMMARY.md` when done.</output>
