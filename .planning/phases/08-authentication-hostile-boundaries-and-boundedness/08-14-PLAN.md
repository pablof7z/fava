---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 14
type: execute
wave: 9
depends_on: [08-10, 08-11, 08-12, 08-13]
files_modified:
  - .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md
  - tools/runtime_ownership_allowlist.toml
  - tools/tests/test_runtime_ownership.py
  - tools/tests/test_m8_line_gate.py
  - docs/issues/0033-m8-runtime-ownership-audit.md
autonomous: true
requirements: [HARD-03, HARD-08, HARD-09]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "The audit verifies all modifying migrations, including the three confirmed live bypass files; it substitutes for none."
    - "No spawn/timer/join/cancel/panic bypass remains outside runtime/provider implementation and exact approved harness paths."
    - "Final OPS-004 ledger includes all runtime resources and transport frame/message bounds."
  artifacts: ["runtime_ownership_allowlist.toml", "test_runtime_ownership.py", "test_m8_line_gate.py", "final runtime ledger rows"]
  key_links: ["explicit migrated source -> exact allowlist audit -> final resource ledger"]
  prohibitions:
    - statement: "Broad allowlists, renamed wrappers, transitive concrete dependencies, and audit-only closure are forbidden."
      status: unverified
      flagged: true
      verification: exact-path source/build falsifier and confirmed-file assertions
---

<objective>
Audit the completed explicit migrations and reconcile final Runtime/OPS-004 ownership.

Purpose: Backstop modifying tasks against hidden or renamed bypasses.
Output: Exact allowlist/audit, final ledger, causal injected bypass.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/spec/ARCHITECTURE.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-10-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-11-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-12-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-13-SUMMARY.md
</context>

## Artifacts this phase produces

- Exact-path runtime allowlist/audit with mandatory checks for query_source.rs, relay.rs, and fava-nip11-http/lib.rs.
- Final runtime/transport OPS-004 rows and `DELIBERATE_BREAK_M8_RUNTIME_OWNERSHIP_AUDIT`.
- Self-tested exhaustive Phase-08 modified-code line-gate checker consumed by Plan 17.

<slice_contract>
Issue: `docs/issues/0033-m8-runtime-ownership-audit.md`
Branch: `m8/runtime-ownership-audit`
Validation: exact source/build allowlist, three confirmed-file assertions, ledger, injected bypass
Commit series: audit tracer -> ledger/bypass seal
</slice_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Reject one injected owner-local executor and assert three migrations</name>
  <read_first>crates/fava/src/query_source.rs, crates/fava/src/relay.rs, crates/fava-nip11-http/src/lib.rs, Cargo.toml, MODULE.bazel</read_first>
  <files>tools/runtime_ownership_allowlist.toml, tools/tests/test_runtime_ownership.py, docs/issues/0033-m8-runtime-ownership-audit.md</files>
  <behavior>
    - Exact scanner rejects task/thread spawn, timers, joins, cancellation registries, catch_unwind, concrete Runtime deps, and broad allowlists.
    - It separately asserts all three confirmed original bypass expressions are absent.
  </behavior>
  <action>Write RED with a compiling injected bypass; implement exact source/manifest/BUILD scan rejecting task/thread spawn, timers, current-runtime handle lookup, joins, cancellation registries, and panic isolation outside runtime/provider implementation and enumerated tests/canary harness files. Add mandatory file-specific assertions; do not use audit PASS to waive a missing modifying migration.</action>
  <acceptance_criteria>Injected bypass fails precisely and all three modified files are independently checked.</acceptance_criteria>
  <verify><automated>python3 -m unittest tools.tests.test_runtime_ownership</automated></verify>
  <done>Runtime ownership is a repository invariant and explicit migrations remain required.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile final ledger and kill audit bypass</name>
  <read_first>.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md, tools/tests/test_runtime_ownership.py, crates/fava-transport-websocket/src/lib.rs, docs/issues/0033-m8-runtime-ownership-audit.md</read_first>
  <files>.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md, docs/issues/0033-m8-runtime-ownership-audit.md</files>
  <behavior>
    - Every owner/resource has numeric bound, refusal/backpressure, high-water, exceed-limit test, and public/process reachability.
  </behavior>
  <action>Refresh final frame/message/runtime rows. Name `DELIBERATE_BREAK_M8_RUNTIME_OWNERSHIP_AUDIT`: inject a compiling local timer/join/panic helper, require audit failure although Cargo target builds, restore checksums, record PASS.</action>
  <acceptance_criteria>Ledger is exhaustive and audit rejects a buildable bypass.</acceptance_criteria>
  <verify><automated>set -euo pipefail
python3 -m unittest tools.tests.test_runtime_ownership tools.tests.test_m8_resource_ledger
cargo metadata --locked --format-version 1 &gt;/dev/null
rg -n '^DELIBERATE_BREAK_M8_RUNTIME_OWNERSHIP_AUDIT: PASS ' docs/issues/0033-m8-runtime-ownership-audit.md
git diff --check</automated></verify>
  <done>Explicit migrations, audit, and ledger are sealed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the exhaustive modified-code 500/800 line-gate checker</name>
  <read_first>AGENTS.md, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-02-PLAN.md, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-13-PLAN.md</read_first>
  <files>tools/tests/test_m8_line_gate.py</files>
  <behavior>
    - The checker derives the exact set of modified code files from all 18 Phase-08 plan frontmatters rather than a hand-picked glob.
    - It rejects any code file over 800 lines.
    - It rejects every file at or over 500 lines unless one exact path has one nonempty concrete cohesion reason in the line-gate manifest.
    - It rejects duplicate, missing, extra, wildcard, directory, blank-reason, stale-count, or blanket-waiver rows.
  </behavior>
  <action>Write unit-test fixtures RED for missing path, extra path, stale count, 500-line blank reason, 801-line file, wildcard path, and valid exact rows. Implement a parser/checker that loads every `files_modified` entry from `08-01-PLAN.md` through `08-18-PLAN.md`, selects code files by exact extension, compares that set one-for-one with Plan 18's headerless `08-LINE-GATE.tsv`, computes live line counts, and enforces hard/soft rules. The manifest does not exist yet, so unit tests use isolated fixtures in this task; Plan 18 supplies and runs the live manifest. Do not encode path-specific waivers in the checker.</action>
  <acceptance_criteria>The checker is independently unit-tested and can prove an exhaustive exact manifest, not a sampled line scan.</acceptance_criteria>
  <verify><automated>python3 -m unittest tools.tests.test_m8_line_gate</automated></verify>
  <done>Plan 18 has a self-tested exhaustive 500/800 enforcement tool.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-59 | Elevation of Privilege | hidden owner-local executor | high | mitigate | Exact source/build falsifier. |
| T-08-60 | Tampering | broad allowlist | high | mitigate | Exact paths/no wildcards. |
| T-08-61 | Denial of Service | omitted resource | high | mitigate | Exhaustive final ledger. |
| T-08-62 | Repudiation | audit-only migration claim | high | mitigate | Mandatory checks for three modifying tasks. |
</threat_model>

<verification>Audit, confirmed-file checks, ledger, compiled injected bypass, and graph identity pass.</verification>
<success_criteria>No runtime bypass remains and the audit proves—not replaces—explicit migration.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-14-SUMMARY.md` when done.</output>
