---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 08
type: execute
wave: 5
depends_on: [08-06, 08-07]
files_modified:
  - crates/fava/src/runtime.rs
  - crates/fava/src/lib.rs
  - crates/fava-standard/src/lib.rs
  - crates/fava-standard/tests/assembly.rs
  - docs/issues/0026-m8-runtime-facade-standard-surface.md
autonomous: true
requirements: [HARD-08, HARD-09]
estimate: {tokens: 20000, raw_tokens: 20000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "FavaBuilder exposes the approved contract-facing Runtime selection surface before standard assembly source is authored."
    - "fava_standard::standard_builder selects TokioRuntime only through the already-landed FavaBuilder surface."
    - "Neither interface source implements an owner execution migration; Plan 09 first compiles the pre-migration graph."
  artifacts: ["FavaBuilder Runtime selection", "fava_standard::standard_builder", "DELIBERATE_BREAK_M8_STANDARD_RUNTIME_ORDER: SPECIFIED"]
  key_links: ["approved Runtime contract -> FavaBuilder selection -> standard_builder(TokioRuntime) -> Plan 09 graph compile"]
  prohibitions:
    - statement: "The facade cannot depend on TokioRuntime, and standard assembly cannot invent or precede its facade API."
      status: unverified
      flagged: true
      verification: source ordering here; compiled replaceability break in Plan 09
---

<objective>
Land the contract-facing facade selection surface, then author standard assembly source without migrating owner execution.

Purpose: Make every future owner RED test compile through one public Runtime selection path while preserving facade/provider dependency direction.
Output: FavaBuilder Runtime selection, standard_builder source/test, exact order-break specification.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/spec/ARCHITECTURE.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-07-SUMMARY.md
</context>

## Artifacts this phase produces

- Approved contract-facing `FavaBuilder` Runtime selection and internal handle threading surface, with no concrete provider dependency.
- Approved `fava_standard::standard_builder` source around `TokioRuntime` and a competing fake Runtime assembly test.
- `docs/issues/0026-m8-runtime-facade-standard-surface.md` with `FACADE_RUNTIME_SURFACE: SOURCE_READY` and `DELIBERATE_BREAK_M8_STANDARD_RUNTIME_ORDER: SPECIFIED`.

<slice_contract>
Issue: `docs/issues/0026-m8-runtime-facade-standard-surface.md`
Branch: `m8/runtime-facade-standard-surface`
Validation: source/API order now; first Cargo/Bazel compilation and causal break in Plan 09
Commit series: facade selection source -> standard assembly source/break specification
</slice_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Land FavaBuilder Runtime selection before any owner migration</name>
  <read_first>docs/spec/ARCHITECTURE.md, crates/fava/src/lib.rs, crates/fava-runtime/src/lib.rs, docs/issues/0023-m8-runtime-contract.md</read_first>
  <files>crates/fava/src/runtime.rs, crates/fava/src/lib.rs, docs/issues/0026-m8-runtime-facade-standard-surface.md</files>
  <behavior>
    - A caller supplies any approved Runtime implementation through FavaBuilder; fava source never selects TokioRuntime or a fallback executor.
  </behavior>
  <action>Author the approved builder selection and internal handle threading surface only. Record `FACADE_RUNTIME_SURFACE: SOURCE_READY`. Do not migrate query, relay, publication, auth, routing, observation, delivery, publisher, signer, timer, join, or shutdown execution here. Do not edit build metadata or claim compilation before Plan 09.</action>
  <acceptance_criteria>The facade API exists in source first, is fake-implementable, and has no standard/concrete dependency.</acceptance_criteria>
  <verify><automated>set -euo pipefail
rg -n 'Runtime' crates/fava/src/runtime.rs crates/fava/src/lib.rs
if rg -n 'TokioRuntime|fava_standard' crates/fava/src; then exit 1; fi
rg -n '^FACADE_RUNTIME_SURFACE: SOURCE_READY ' docs/issues/0026-m8-runtime-facade-standard-surface.md
git diff --check</automated></verify>
  <done>The contract-facing selection surface is committed before standard source or owner migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author standard_builder against the committed facade surface</name>
  <read_first>crates/fava/src/runtime.rs, crates/fava/src/lib.rs, crates/fava-standard/src/lib.rs, crates/fava-standard/tests/assembly.rs, crates/fava-runtime-tokio/src/lib.rs, docs/spec/ARCHITECTURE.md</read_first>
  <files>crates/fava-standard/src/lib.rs, crates/fava-standard/tests/assembly.rs, docs/issues/0026-m8-runtime-facade-standard-surface.md</files>
  <behavior>
    - standard_builder creates TokioRuntime and passes it through the existing FavaBuilder API; the same API accepts a fake Runtime.
  </behavior>
  <action>Start only after Task 1 is committed. Author standard assembly source and the competing-runtime test against that exact API. Specify `DELIBERATE_BREAK_M8_STANDARD_RUNTIME_ORDER` as concrete-only construction that must fail the compiled competing-runtime test in Plan 09. Do not edit manifests/BUILD/MODULE/lock or claim GREEN before Plan 09.</action>
  <acceptance_criteria>Standard source consumes, never invents, the facade API; its exact compiled order falsifier is specified.</acceptance_criteria>
  <verify><automated>set -euo pipefail
rg -n 'standard_builder|TokioRuntime|Runtime' crates/fava-standard/src/lib.rs crates/fava-standard/tests/assembly.rs
rg -n '^DELIBERATE_BREAK_M8_STANDARD_RUNTIME_ORDER: SPECIFIED ' docs/issues/0026-m8-runtime-facade-standard-surface.md
git diff --check</automated></verify>
  <done>Standard assembly source is ready for the single pre-migration graph compile.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-33 | Elevation of Privilege | concrete runtime dependency | high | mitigate | Facade source gate and Plan 09 dependency test. |
| T-08-34 | Tampering | hidden fallback executor | high | mitigate | Competing-runtime assembly and source audit. |
| T-08-35 | Repudiation | inverted API order | high | mitigate | Task/commit ordering plus named compiled break. |
| T-08-36 | Tampering | uncompiled interface source | high | mitigate | Plan 09 compiles Cargo/Bazel before migrations. |
</threat_model>

<verification>Facade-before-standard source ordering passes here; Plan 09 is the sole first compilation and break owner.</verification>
<success_criteria>Future owner tests can compile through one contract-facing Runtime selection path without dependency inversion.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-08-SUMMARY.md` when done.</output>
