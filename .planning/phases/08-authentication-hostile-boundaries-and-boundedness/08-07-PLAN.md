---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 07
type: execute
wave: 4
depends_on: [08-05]
files_modified:
  - docs/issues/0023-m8-runtime-contract.md
  - docs/internals/vocabulary.toml
  - crates/fava-runtime/src/lib.rs
  - crates/fava-runtime/tests/contract.rs
  - crates/fava-runtime-tokio/src/lib.rs
  - crates/fava-runtime-tokio/tests/conformance.rs
autonomous: false
requirements: [HARD-09]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Runtime exclusively owns task execution, provider isolation, timers, bounded channels, cancellation, joins, and shutdown deadlines."
    - "TokioRuntime is the first real implementation and exports no Tokio primitive through the neutral contract."
    - "Universal owners authorize opaque work and consume exact typed operation/generation completions."
  artifacts:
    - "fava_runtime::{Runtime,RuntimeTask,RuntimeHandle,RuntimeTaskOutcome,RuntimeLimits}"
    - "fava_runtime_tokio::TokioRuntime"
    - "docs/issues/0023-m8-runtime-contract.md"
  key_links:
    - "owner authorization -> opaque RuntimeTask -> bounded execution fact and owner-private correlated completion"
  prohibitions:
    - statement: "No owner-local executor, timer, join registry, cancellation owner, or panic boundary may compete with Runtime."
      status: unverified
      flagged: true
      verification: source contract gates now; compiled conformance in Plan 09 and repository audit after owner migrations
---

<objective>
Define the authoritative runtime contract/provider source before consumer migration.

Purpose: Establish the one execution owner required by architecture.
Output: Approved vocabulary, neutral runtime/Tokio source and RED tests, and causal-break specification.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/spec/ARCHITECTURE.md
@docs/internals/vocabulary.toml
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md
</context>

## Artifacts this phase produces

- Approved symbols `Runtime`, `RuntimeTask`, `RuntimeHandle`, `RuntimeTaskOutcome`, `RuntimeLimits`, `TokioRuntime`, and later `fava_standard::standard_builder`.
- Runtime/provider source and RED tests; Plan 09 alone creates manifests/build targets and compiles them before owner migrations.
- `DELIBERATE_BREAK_M8_RUNTIME_BOUNDARY: SPECIFIED` for Plan 09 to execute.

<slice_contract>
Issue: `docs/issues/0023-m8-runtime-contract.md`
Branch: `m8/runtime-contract-source`
Validation: blocking approval and source RED now; compiled conformance deferred explicitly to centralized Plan 09
Commit series: architecture approval -> contract source/RED -> Tokio provider source/RED
</slice_contract>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Approve runtime/provider/standard-entry vocabulary</name>
  <read_first>AGENTS.md, docs/spec/ARCHITECTURE.md, docs/internals/vocabulary.toml, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESOURCE-LEDGER.md</read_first>
  <files>docs/issues/0023-m8-runtime-contract.md</files>
  <decision>Approve Runtime contract symbols, `fava-runtime-tokio`/`TokioRuntime`, and `fava_standard::standard_builder`; rejection blocks Tasks 2-3 and Plans 08-17.</decision>
  <action>Record closest concepts, distinction, owner/lifecycle, forcing requirement, dependency direction, counterexample, and falsifier. Write `M8_RUNTIME_ARCHITECTURE: APPROVED` only on approval. Rejection stops and never satisfies completion.</action>
  <options>
    <option id="approve">Approve the exact contract/provider/standard-entry vocabulary.</option>
    <option id="reject-stop">Reject and stop all dependent runtime work for a revised architecture proposal.</option>
  </options>
  <resume-signal>Select `approve` to continue or `reject-stop` to halt without completing the plan.</resume-signal>
  <acceptance_criteria>The exact approval marker exists; no dead rejection-success branch exists.</acceptance_criteria>
  <verify><automated>rg -n '^M8_RUNTIME_ARCHITECTURE: APPROVED ' docs/issues/0023-m8-runtime-contract.md</automated></verify>
  <done>Runtime/provider/standard-entry vocabulary is approved.</done>
</task>

<task type="tracer">
  <name>Task 2: Specify owner-authorized work to bounded correlated completion</name>
  <read_first>docs/spec/ARCHITECTURE.md, docs/issues/0023-m8-runtime-contract.md, docs/internals/vocabulary.toml</read_first>
  <files>docs/internals/vocabulary.toml, crates/fava-runtime/src/lib.rs, crates/fava-runtime/tests/contract.rs</files>
  <behavior>
    - Opaque authorized work gets exact complete/panic/timeout/cancel/capacity outcomes under 64/64/five-second bounds.
    - Owner-private completion retains operation/generation identity and Runtime gains no domain authority.
  </behavior>
  <action>Write neutral contract RED and source after approval. Register approved vocabulary. Do not create manifests/BUILD, touch Cargo.lock, import Tokio, or claim compilation; Plan 09 owns build integration. Use source-shape assertions now.</action>
  <acceptance_criteria>The source expresses a fake-implementable, execution-only contract.</acceptance_criteria>
  <verify><automated>set -euo pipefail
rg -n 'pub trait Runtime|pub struct RuntimeLimits|RuntimeTaskOutcome' crates/fava-runtime/src/lib.rs
rg -n '64|Duration::from_secs\(5\)|panicked|timed_out|cancel' crates/fava-runtime/tests/contract.rs
python3 tools/check_vocabulary.py</automated></verify>
  <done>The neutral source contract and causal RED await only central compilation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Tokio task/timer/cancel/join source and break</name>
  <read_first>crates/fava-runtime/src/lib.rs, crates/fava-runtime/tests/contract.rs, docs/issues/0023-m8-runtime-contract.md</read_first>
  <files>crates/fava-runtime-tokio/src/lib.rs, crates/fava-runtime-tokio/tests/conformance.rs, docs/issues/0023-m8-runtime-contract.md</files>
  <behavior>
    - TokioRuntime privately owns tasks, timers, bounded queues, panic conversion, cancellation, joins, and five-second shutdown.
    - Ignored cancellation becomes a bounded join outcome; late output has no active owner channel.
  </behavior>
  <action>Write conformance RED and provider source. Specify `DELIBERATE_BREAK_M8_RUNTIME_BOUNDARY` by omitting capacity or join accounting while type-correct; Plan 09 runs, kills, restores, and records compiled PASS.</action>
  <acceptance_criteria>Provider source covers every runtime resource without domain authority.</acceptance_criteria>
  <verify><automated>set -euo pipefail
rg -n 'catch_unwind|timeout|cancel|join|shutdown' crates/fava-runtime-tokio/src/lib.rs crates/fava-runtime-tokio/tests/conformance.rs
rg -n '^DELIBERATE_BREAK_M8_RUNTIME_BOUNDARY: SPECIFIED ' docs/issues/0023-m8-runtime-contract.md
git diff --check</automated></verify>
  <done>Runtime source is complete; Plan 09 is its sole build-integration prerequisite.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-28 | Denial of Service | runtime capacity/deadline | high | mitigate | Exact bounds and compiled conformance in Plan 09. |
| T-08-29 | Tampering | late completion | high | mitigate | Owner-private correlation and retirement. |
| T-08-30 | Elevation of Privilege | runtime domain authority | high | mitigate | Execution-only source/dependency gate. |
| T-08-31 | Repudiation | cancel/join outcome | high | mitigate | Exact outcomes and shutdown accounting. |
| T-08-32 | Tampering | internal package identity | high | mitigate | Blocking approval and centralized manifests. |
</threat_model>

<verification>Approval/source RED pass now; Plan 09 compiles/runs tests and break before every owner migration.</verification>
<success_criteria>One authoritative runtime source contract/provider exists with no manifest or lock mutation.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-07-SUMMARY.md` when done.</output>
