---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 10
type: execute
wave: 7
depends_on: [08-09]
files_modified:
  - crates/fava-publication/src/materialization.rs
  - crates/fava-publication/src/lib.rs
  - crates/fava-publication/src/run.rs
  - crates/fava-publication/src/runtime.rs
  - crates/fava-publication/tests/provider_runtime.rs
  - crates/fava/tests/provider_failure_isolation.rs
  - docs/issues/0034-m8-runtime-materialization-run.md
autonomous: true
requirements: [HARD-08, HARD-09]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Materializer and publication-run behavioral tests compile against the pre-migration graph and fail because current production bypasses the selected Runtime."
    - "After each RED commit, Runtime owns provider panic isolation, task execution, retry timers, cancellation, joins, and shutdown deadlines while publication retains domain authority."
    - "Each migration passes GREEN, then its named compiling bypass fails the same exact test and restores byte-for-byte."
  artifacts: ["MaterializationCompletion", "publication runtime adapter", "two compiled RED/GREEN/break records"]
  key_links: ["FavaBuilder selected Runtime -> publication authorization -> exact receipt/materialization/generation completion -> public result"]
  prohibitions:
    - statement: "Source-shape absence cannot substitute for the exact compiled failing assertion before production migration."
      status: unverified
      flagged: true
      verification: captured RED output and named deliberate-break records
---

<objective>
Migrate materializer and publication-run execution through Runtime with causal compiled RED first.

Purpose: Prove the graph is usable and every publication execution ownership change is behaviorally necessary.
Output: Runtime adapters, owner/public matrices, two killed deliberate breaks.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/spec/ARCHITECTURE.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-09-SUMMARY.md
</context>

## Artifacts this phase produces

- Private publication Runtime adapter and exact correlated `MaterializationCompletion`.
- Compiled tests `runtime_materializer_scopes_panics` and `runtime_publication_run_uses_runtime`.
- `DELIBERATE_BREAK_M8_PUBLICATION_MATERIALIZER_RUNTIME` and `DELIBERATE_BREAK_M8_PUBLICATION_RUN_RUNTIME` PASS records.

<slice_contract>
Issue: `docs/issues/0034-m8-runtime-materialization-run.md`
Branch: `m8/runtime-materialization-run`
Validation: exact compiled RED output -> RED commit -> production GREEN -> same-test compiling break -> checksum restore
Commit series: materializer RED/GREEN/break -> run-coordinator RED/GREEN/break -> public matrix seal
</slice_contract>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove and migrate materializer execution ownership</name>
  <read_first>crates/fava-publication/src/materialization.rs, crates/fava/tests/semantic_write_failures.rs, crates/fava-runtime/src/lib.rs, docs/spec/ARCHITECTURE.md</read_first>
  <files>crates/fava-publication/src/materialization.rs, crates/fava-publication/src/runtime.rs, crates/fava-publication/tests/provider_runtime.rs, crates/fava/tests/provider_failure_isolation.rs, docs/issues/0034-m8-runtime-materialization-run.md</files>
  <behavior>
    - `runtime_materializer_scopes_panics` compiles with a selected recording Runtime and fails pre-migration with `materializer work bypassed Runtime`.
    - GREEN scopes panic/block/late/malformed/cancel-ignore by exact write/receipt/materialization/generation while adjacent work progresses.
  </behavior>
  <action>Replace or extend Plan 09's `PLAN09_COMPILE_SCAFFOLD` in both public and provider test files: remove the scaffold marker only when the exact behavioral tests exist, and retain no smoke test as substitute evidence. Add the exact public/owner test first. Run `cargo test -p fava --test provider_failure_isolation runtime_materializer_scopes_panics -- --exact`, require nonzero status, the literal assertion `materializer work bypassed Runtime`, and `test result: FAILED`; reject compile errors or any other failure, then commit RED. Implement Runtime authorization/private completion and remove owner-local panic isolation. Require the same exact test GREEN. Run `DELIBERATE_BREAK_M8_PUBLICATION_MATERIALIZER_RUNTIME` by compiling a scoped reintroduction of local provider execution, require the same assertion failure, restore exact checksums without Git restore/reset/stash, rerun GREEN, and record PASS.</action>
  <acceptance_criteria>One committed compiled behavioral RED precedes production; GREEN and the named compiling break use the same exact test and assertion.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava --test provider_failure_isolation runtime_materializer_scopes_panics -- --exact
cargo test -p fava-publication --test provider_runtime runtime_materializer_scopes_panics -- --exact
rg -n '^DELIBERATE_BREAK_M8_PUBLICATION_MATERIALIZER_RUNTIME: PASS ' docs/issues/0034-m8-runtime-materialization-run.md
git diff --check</automated></verify>
  <done>Materializer execution ownership is causally migrated and restored after its compiling break.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove and migrate publication run tasks, timers, cancellation, and joins</name>
  <read_first>crates/fava-publication/src/lib.rs, crates/fava-publication/src/run.rs, crates/fava-publication/src/runtime.rs, crates/fava/tests/provider_failure_isolation.rs</read_first>
  <files>crates/fava-publication/src/lib.rs, crates/fava-publication/src/run.rs, crates/fava-publication/src/runtime.rs, crates/fava/tests/provider_failure_isolation.rs, docs/issues/0034-m8-runtime-materialization-run.md</files>
  <behavior>
    - `runtime_publication_run_uses_runtime` compiles pre-migration and fails with `publication run task bypassed Runtime` while the healthy write completes.
    - GREEN routes coordinator tasks, retry timers, cancellation, joins, and bounded shutdown through Runtime with exact receipt/generation completion.
  </behavior>
  <action>Extend the now-behavioral public scaffold replacement from Task 1 with exact test `runtime_publication_run_uses_runtime`; capture `cargo test -p fava --test provider_failure_isolation runtime_publication_run_uses_runtime -- --exact --nocapture` against still-unmigrated production. Require nonzero, exact `publication run task bypassed Runtime`, and `test result: FAILED`; reject compiler/import/syntax/unrelated failures, then commit RED. Replace ambient runtime lookup, task creation, retry timing, cancellation, and join ownership with Runtime/private completion; run the same test GREEN. Run `DELIBERATE_BREAK_M8_PUBLICATION_RUN_RUNTIME` by restoring one compiling local task/timer path, require the same exact failure, restore checksums, rerun GREEN, and record PASS. Treat the negative source scan in verification only as a backstop to compiled behavior.</action>
  <acceptance_criteria>The coordinator migration has a committed compiled RED and one same-test killed compiling break.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava --test provider_failure_isolation runtime_publication_run_uses_runtime -- --exact --nocapture
rg -n '^DELIBERATE_BREAK_M8_PUBLICATION_RUN_RUNTIME: PASS ' docs/issues/0034-m8-runtime-materialization-run.md
if rg -n 'catch_unwind|tokio::spawn|tokio::time::(timeout|sleep)|tokio::runtime::Handle' crates/fava-publication/src/materialization.rs crates/fava-publication/src/lib.rs crates/fava-publication/src/run.rs crates/fava-publication/src/runtime.rs; then exit 1; fi
git diff --check</automated></verify>
  <done>Publication-run execution resources are causally migrated and break-restored.</done>
</task>

<task type="auto">
  <name>Task 3: Seal owner/public five-mode isolation and source backstop</name>
  <read_first>crates/fava-publication/tests/provider_runtime.rs, crates/fava/tests/provider_failure_isolation.rs, docs/issues/0034-m8-runtime-materialization-run.md</read_first>
  <files>crates/fava-publication/tests/provider_runtime.rs, crates/fava/tests/provider_failure_isolation.rs, docs/issues/0034-m8-runtime-materialization-run.md</files>
  <action>Complete the panic/block/late/malformed/cancellation-ignore matrix at owner and public Fava layers. Require exact operation/generation attribution, unrelated progress, and bounded close after both migration commits; record the RED commit hashes and captured assertion lines for Tasks 1-2.</action>
  <acceptance_criteria>Both compiled RED histories are auditable and every hostile mode has owner plus public evidence.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava-publication --test provider_runtime
cargo test -p fava --test provider_failure_isolation
rg -n 'RED_COMMIT_MATERIALIZER|RED_COMMIT_PUBLICATION_RUN|materializer work bypassed Runtime|publication run task bypassed Runtime' docs/issues/0034-m8-runtime-materialization-run.md
git diff --check</automated></verify>
  <done>Publication Runtime migration is externally behavior-complete and causally recorded.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-42 | Denial of Service | hostile materializer/run task | high | mitigate | Compiled RED, Runtime deadline/capacity, adjacent progress. |
| T-08-43 | Tampering | stale/malformed completion | high | mitigate | Exact receipt/materialization/generation validation. |
| T-08-44 | Repudiation | false causal RED | high | mitigate | Captured exact assertion/status and rejected compile errors. |
</threat_model>

<verification>Both exact compiled RED histories, GREEN tests, named compiling breaks, owner/public matrices, source backstop, and diff checks pass.</verification>
<success_criteria>Publication execution is Runtime-owned with causal compiled proof before every production migration.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-10-SUMMARY.md` when done.</output>
