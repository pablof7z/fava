---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/fava-delivery-standard/src/lib.rs
  - crates/fava-publisher-nip01/src/lib.rs
  - crates/fava-publisher/src/lib.rs
  - crates/fava-write-store/src/receipt.rs
  - crates/fava-write/src/lib.rs
  - crates/fava-write/src/delivery.rs
  - crates/fava-delivery/src/lib.rs
  - crates/fava-publication/src/delivery.rs
  - crates/fava/tests/delivery_bounds.rs
  - crates/fava/BUILD.bazel
  - docs/issues/0019-m8-delivery-contract-public-closure.md
autonomous: true
requirements: [HARD-05, HARD-06, HARD-07]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "The five existing dirty neutral/publisher/policy files are adopted exactly once and make commit 197c278's public delivery behavior self-contained before store parity work."
    - "Unreachable time spends zero attempts, real retryable handoffs spend exactly one, and post-handoff loss remains ambiguous."
    - "Generation identity remains monotonic and distinct from spent-attempt policy input through public Fava."
  artifacts:
    - "RelayDeliveryOutcome::Unreachable"
    - "PublishOutcome::Unreachable"
    - "Receipt::spent and Receipt::spent_attempts"
    - "docs/issues/0019-m8-delivery-contract-public-closure.md"
  key_links:
    - "publisher connection/handoff fact -> neutral outcome -> publication decision -> public receipt"
    - "receipt generation -> exact operation identity; spent_attempts -> delivery ceiling"
  prohibitions:
    - statement: "Offline elapsed time must not become a spent attempt, and ambiguous handoff must not become a definite relay outcome."
      status: unverified
      flagged: true
      verification: public delivery target plus named break
---

<objective>
Adopt the dirty delivery contract/public half as a focused self-contained vertical slice.

Purpose: Preserve the resolved unreachable retry while separating neutral/public closure from provider persistence parity.
Output: Five adopted WIP files, public causal evidence, build registration, and focused issue/commit series.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/debug/resolved/m8-unreachable-delivery-retry.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESEARCH.md
@crates/fava/tests/delivery_bounds.rs
</context>

## Artifacts this phase produces

- Adopted specified symbols: `RelayDeliveryOutcome::Unreachable`, `PublishOutcome::Unreachable`, `Receipt::spent`, and `Receipt::spent_attempts`.
- Exact public tests for non-spending unreachable time, finite real attempts, and ambiguous full handoff.
- `docs/issues/0019-m8-delivery-contract-public-closure.md` with `DELIBERATE_BREAK_M8_DELIVERY_IDENTITY_BUDGET`.

<slice_contract>
Issue: `docs/issues/0019-m8-delivery-contract-public-closure.md`  
Branch: `m8/delivery-contract-public-closure`  
Validation: public/policy/publisher Cargo, Bazel, break, Clippy, vocabulary, line, diff, stash identity  
Commit series: causal public RED -> exact WIP adoption GREEN -> break/restoration and graph closure
</slice_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Adopt the five dirty delivery contract files through public Fava</name>
  <read_first>.planning/debug/resolved/m8-unreachable-delivery-retry.md, crates/fava-delivery-standard/src/lib.rs, crates/fava-publisher-nip01/src/lib.rs, crates/fava-publisher/src/lib.rs, crates/fava-write-store/src/receipt.rs, crates/fava-write/src/lib.rs, crates/fava/tests/delivery_bounds.rs</read_first>
  <files>crates/fava-delivery-standard/src/lib.rs, crates/fava-publisher-nip01/src/lib.rs, crates/fava-publisher/src/lib.rs, crates/fava-write-store/src/receipt.rs, crates/fava-write/src/lib.rs</files>
  <behavior>
    - Unreachable establishment returns the explicit non-spending outcome and remains retryable.
    - A real pre-outcome attempt spends one and reaches the configured ceiling.
    - Full EVENT handoff without relay OK remains durable ambiguity.
    - Operation generation and spent budget are independently observable.
  </behavior>
  <action>Fingerprint all five current dirty files into issue 0019, run the existing public target, and add its causal RED assertions before adopting these exact bytes. Never recreate, reset, checkout, or source them from `stash@{0}`. Preserve the specified contract/store vocabulary and make only recorded deltas required by the RED evidence. Commit this task before Plan 04 consumes the contract.</action>
  <acceptance_criteria>Exactly one task adopts each of the five dirty paths, and the public test exercises a complete unreachable/retry/ambiguity path without store-provider assumptions.</acceptance_criteria>
  <verify><automated>cargo test -p fava --test delivery_bounds &amp;&amp; cargo test -p fava-delivery-standard -p fava-publisher -p fava-publisher-nip01 &amp;&amp; git diff --check</automated></verify>
  <done>The delivery contract/public WIP is committed and independently consumable by provider parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Kill the identity-budget regression and close public build graphs</name>
  <read_first>crates/fava-delivery/src/lib.rs, crates/fava-publication/src/delivery.rs, crates/fava/tests/delivery_bounds.rs, crates/fava/BUILD.bazel, docs/internals/vocabulary.toml</read_first>
  <files>crates/fava-delivery/src/lib.rs, crates/fava-publication/src/delivery.rs, crates/fava/tests/delivery_bounds.rs, crates/fava/BUILD.bazel, docs/issues/0019-m8-delivery-contract-public-closure.md</files>
  <behavior>
    - The public test fails if WaitFor stops authorizing a delayed revalidated attempt.
    - The public test fails if spent attempts are reused as operation generation.
    - Bazel and Cargo execute the same exact target.
  </behavior>
  <action>Add only public assertions and build registration after Task 1's adoption commit. Run `DELIBERATE_BREAK_M8_DELIVERY_IDENTITY_BUDGET` by checksum-scoped removal of the two contract/public seams, require compiled causal failure, then restore exact checksums without Git restore/reset/checkout/stash. Store-provider transitions remain Plan 04 ownership.</action>
  <acceptance_criteria>The named public break is killed, restoration is exact, and all contract/public graphs pass without touching dirty store implementation paths.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava-delivery -p fava-delivery-standard -p fava-publication -p fava-publisher -p fava-publisher-nip01 -p fava
cargo clippy -p fava-delivery -p fava-delivery-standard -p fava-publication -p fava-publisher -p fava-publisher-nip01 -p fava --all-targets -- -D warnings
bazel test //crates/fava-delivery-standard:all //crates/fava-publication:all //crates/fava:delivery_bounds
python3 tools/check_vocabulary.py
rg -n '^DELIBERATE_BREAK_M8_DELIVERY_IDENTITY_BUDGET: PASS ' docs/issues/0019-m8-delivery-contract-public-closure.md
test "$(git rev-parse 'stash@{0}')" = 5faecf42c0ec903507e3faeb04962f4680a9cb44
git diff --check</automated></verify>
  <done>Public delivery closure is causal, build-registered, and ready for provider parity.</done>
</task>

<task type="auto">
  <name>Task 3: Extract the committed delivery state machine below the soft line limit</name>
  <read_first>crates/fava-write/src/lib.rs, AGENTS.md, docs/spec/ARCHITECTURE.md</read_first>
  <files>crates/fava-write/src/lib.rs, crates/fava-write/src/delivery.rs</files>
  <action>Start only after Tasks 1 and 2 commit the existing bytes and causal public closure. Move the cohesive private delivery attempt, generation, retry, and receipt-transition implementation from `lib.rs` into private module `delivery.rs`; preserve every public re-export, serialization shape, authority boundary, and behavior. This extraction is the preferred resolution for the current 557-line soft-limit crossing: both resulting code files must be below 500 lines, with no waiver. Run the same public delivery test before and after the move so the extraction is behavior-neutral.</action>
  <acceptance_criteria>The committed implementation is split by cohesion, both modified Rust files are below 500 lines, and no public API or delivery result changes.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava-write -p fava --test delivery_bounds
for file in crates/fava-write/src/lib.rs crates/fava-write/src/delivery.rs; do lines="$(wc -l &lt; "$file" | tr -d ' ')"; test "$lines" -lt 500; done
git diff --check</automated></verify>
  <done>The delivery owner is below the 500-line soft limit through a private cohesive extraction.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

## Trust Boundaries

| Boundary | Description |
|---|---|
| publisher/transport -> publication/store contract | Untrusted connection/handoff facts become durable delivery truth. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-04 | Tampering | attempt generation | high | mitigate | Exact generation assertions and public causal break. |
| T-08-05 | Repudiation | spent budget | high | mitigate | Separate spent counter and real-handoff criterion. |
| T-08-06 | Repudiation | ambiguity | high | mitigate | Exact ambiguous outcome; no inferred ACK/rejection. |
| T-08-07 | Denial of Service | retry loop | high | mitigate | Revalidated delayed retry and finite real-attempt ceiling. |
| T-08-23 | Tampering | dependencies | low | accept | No package install or graph expansion. |
</threat_model>

<verification>Run exact public/policy/publisher Cargo and Bazel targets, named break, Clippy, vocabulary, line, diff, and stash identity.</verification>
<success_criteria>Commit 197c278 plus Task 1's adopted contract bytes compile and prove public behavior without uncommitted store definitions.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-02-SUMMARY.md` when done.</output>
