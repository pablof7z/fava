---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - crates/fava-transport-websocket/src/lib.rs
  - crates/fava-transport-websocket/tests/conformance.rs
  - crates/fava/src/relay.rs
  - crates/fava/src/relay/ingress.rs
  - crates/fava/tests/hostile_ingress.rs
  - crates/fava/tests/hostile_ingress/cases.rs
  - crates/fava/BUILD.bazel
  - docs/issues/0020-m8-hostile-ingress-closure.md
autonomous: true
requirements: [HARD-03, HARD-08]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The existing transport, relay-owner, and untracked hostile-ingress bytes are adopted rather than recreated or replaced from stash."
    - "A frame exactly at the configured boundary is admitted to parsing; one byte over is a scoped transport/session refusal before owner mutation."
    - "Invalid id/signature, off-filter, malformed, oversized, unattributed, stale-generation, post-CLOSED, never-EOSE, NOTICE, truncation, silent-limit, and disconnect cases remain exact-session/request attributable while healthy work continues."
    - "No hostile input enters the event cache or current query state, and the admission-bypass deliberate break makes the public target fail."
  artifacts:
    - "crates/fava/tests/hostile_ingress.rs"
    - "WebSocketTransport inbound message/frame bound enforcement"
    - "OpenedRelay exact-generation terminal subscription tracking"
    - "docs/issues/0020-m8-hostile-ingress-closure.md"
  key_links:
    - "WebSocketConfig frame/message bounds -> RelaySession::next_message -> exact generation attribution -> admit_subscription_event -> cache"
    - "CLOSED terminal set -> post-CLOSED inertness -> healthy concurrent relay/query witness"
  prohibitions:
    - statement: "Malformed, oversized, stale, off-filter, or post-terminal relay input must not bypass admission or acquire cache/current-state authority."
      status: unverified
      flagged: true
      verification: unverified
---

<objective>
Close the existing hostile-ingress WIP as one bounded public-Fava admission slice.

Purpose: Make all currently scripted hostile classes exact, scoped, attributable, and causally guarded before process harness work begins.
Output: Adopted transport/relay/public-test bytes, complete scripted corpus, Bazel target, line evidence, and admission-bypass sensitivity record.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-RESEARCH.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-PATTERNS.md
@crates/fava-transport-websocket/src/lib.rs
@crates/fava/src/relay.rs
@crates/fava/tests/hostile_ingress.rs
</context>

## Artifacts this phase produces

- Exact public tests in `crates/fava/tests/hostile_ingress.rs` for the complete scripted hostile class matrix and concurrent healthy witness.
- Exact transport conformance boundary tests in `crates/fava-transport-websocket/tests/conformance.rs`.
- Existing private owner behavior in `OpenedRelay::handle_relay_message`; no new public/cross-crate noun.
- `docs/issues/0020-m8-hostile-ingress-closure.md` with `DELIBERATE_BREAK_M8_HOSTILE_ADMISSION`.

<slice_contract>
Issue: `docs/issues/0020-m8-hostile-ingress-closure.md`  
Branch: `m8/hostile-ingress-closure`  
Validation: exact transport/public corpus, healthy concurrency, bypass break, Cargo/Bazel/Clippy/line/vocabulary/diff  
Commit series: RED hostile classes -> adopted WIP GREEN -> break/restoration and focused validation
</slice_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Adopt the exact inbound boundary from real WebSocket bytes to public state</name>
  <read_first>crates/fava-transport-websocket/src/lib.rs, crates/fava-transport-websocket/tests/conformance.rs, crates/fava/src/relay.rs, crates/fava/tests/hostile_ingress.rs</read_first>
  <files>crates/fava-transport-websocket/src/lib.rs, crates/fava-transport-websocket/tests/conformance.rs, crates/fava/src/relay.rs, crates/fava/src/relay/ingress.rs, crates/fava/tests/hostile_ingress.rs, crates/fava/tests/hostile_ingress/cases.rs</files>
  <behavior>
    - Exactly-at-limit text crosses transport intact; max+1 never reaches relay parsing/cache mutation and yields a scoped failure.
    - The same public observation remains live on a healthy relay while the hostile relay fails.
    - Terminal subscription state is exact-generation state and is cleared only on deliberate same-generation demand restore or fresh session generation.
  </behavior>
  <action>Fingerprint and record all four existing dirty/untracked inputs before any edit. Adopt those exact bytes as input, not as files to regenerate. This task alone owns the dirty `crates/fava/src/relay.rs` and untracked `crates/fava/tests/hostile_ingress.rs`: preserve their behavior while extracting admission/terminal logic into private `relay/ingress.rs` and the case corpus into private `hostile_ingress/cases.rs`, then commit all six files together. Bring the current 566-line `relay.rs` and every extracted Rust file below 500 lines; no waiver is permitted for this cohesive private extraction. Add boundary-first public evidence: a real WebSocket session sends max and max+1 cases, and public Fava plus an independent server counter proves only the permitted frame reaches semantic parsing. Keep Tungstenite complete-message and individual-frame limits plus owner text defense. Retain exact `RelaySessionKey` and generation at every diagnostic/admission step.</action>
  <acceptance_criteria>One end-to-end inbound frame path is bounded before parsing, attributed by exact generation, and either safely admitted or scoped without affecting healthy public state.</acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
cargo test -p fava-transport-websocket --test conformance
cargo test -p fava --test hostile_ingress
for file in crates/fava/src/relay.rs crates/fava/src/relay/ingress.rs crates/fava/tests/hostile_ingress.rs crates/fava/tests/hostile_ingress/cases.rs; do lines="$(wc -l &lt; "$file" | tr -d ' ')"; test "$lines" -lt 500; done</automated>
  </verify>
  <done>The dirty transport/owner/public path is adopted and exact boundary behavior is executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the scripted hostile class and precision matrix</name>
  <read_first>docs/spec/FAVA_REWRITE_IMPLEMENTATION_PLAN.md, .planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-03-SUMMARY.md, crates/fava/tests/hostile_ingress/cases.rs, crates/fava/src/relay/ingress.rs</read_first>
  <files>crates/fava/tests/hostile_ingress/cases.rs, crates/fava/src/relay/ingress.rs</files>
  <behavior>
    - Invalid signature/id, off-filter, malformed JSON, oversized frame, stale generation, unattributed id, post-CLOSED EVENT/EOSE, NOTICE, truncation, silent limit, never-EOSE, and disconnect each have one exact oracle.
    - Every failure names only the hostile session/generation/request; never-EOSE remains silence rather than EOSE/failure.
    - Adjacent healthy relay/query work completes before the hostile lane is released or closed.
  </behavior>
  <action>Start only after Task 1 has committed the adopted/extracted WIP. Consume that commit and edit only the two extracted private modules. Write the missing exact cases RED with controlled frames/barriers, then complete admission/terminal handling and the scenario matrix there. Preserve existing Nostr vocabulary and `admit_subscription_event` authority. Keep every code file below 500 lines unless a concrete cohesion reason is recorded; never reopen the formerly dirty entry files in this task.</action>
  <acceptance_criteria>Every HARD-03 class has an exact public oracle and a healthy-concurrent isolation witness; no class is inferred from a generic error.</acceptance_criteria>
  <verify>
    <automated>HOSTILE_LIST="$(cargo test -p fava --test hostile_ingress -- --list)" &amp;&amp; test "$(printf '%s\n' "$HOSTILE_LIST" | rg -c ': test$')" -ge 4 &amp;&amp; cargo test -p fava --test hostile_ingress</automated>
  </verify>
  <done>The scripted corpus covers the full authoritative hostile set with precise scoped outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Prove admission sensitivity and register the focused build targets</name>
  <read_first>crates/fava/BUILD.bazel, docs/internals/vocabulary.toml, AGENTS.md</read_first>
  <files>crates/fava/BUILD.bazel, docs/issues/0020-m8-hostile-ingress-closure.md</files>
  <action>Add exact Bazel targets for `hostile_ingress` and any split helper target. Name `DELIBERATE_BREAK_M8_HOSTILE_ADMISSION`: checksum owned files, temporarily route the malformed/forged hostile EVENT around `admit_subscription_event` into cache mutation at the narrowest owner seam, require the exact public hostile target to compile and fail by exposing hostile state while the healthy control still runs, then restore by scoped edit and require original checksums. Record the counterexample and restoration. Run affected Cargo/Bazel/strict-Clippy/vocabulary/line/diff gates and assert the preserved stash identity; never use Git restore/reset/checkout or stash operations.</action>
  <acceptance_criteria>The precise admission bypass is behaviorally detected, all source restores exactly, Bazel knows the public target, and soft/hard line rules have explicit evidence.</acceptance_criteria>
  <verify>
    <automated>set -euo pipefail
cargo test -p fava-transport-websocket --all-targets
cargo test -p fava --test hostile_ingress
cargo clippy -p fava-transport-websocket -p fava --all-targets -- -D warnings
bazel test //crates/fava-transport-websocket:all //crates/fava:hostile_ingress
python3 tools/check_vocabulary.py
python3 -m unittest tools.tests.test_vocabulary_check
rg -n '^DELIBERATE_BREAK_M8_HOSTILE_ADMISSION: PASS ' docs/issues/0020-m8-hostile-ingress-closure.md
rg --files crates/fava crates/fava-transport-websocket -g '*.rs' -0 | xargs -0 wc -l | awk '$2 != "total" &amp;&amp; $1 &gt; 800 { print; bad=1 } END { exit bad }'
test "$(git rev-parse 'stash@{0}')" = 5faecf42c0ec903507e3faeb04962f4680a9cb44
git diff --check</automated>
  </verify>
  <done>Hostile owner/public closure is causal, build-registered, line-compliant, and committed without losing WIP.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

## Trust Boundaries

| Boundary | Description |
|---|---|
| relay bytes -> WebSocket transport | Untrusted frames can allocate memory before protocol parsing. |
| parsed relay message -> relay owner | Untrusted protocol values request diagnostic or cache mutation authority. |
| relay owner -> event cache/current query | Only exact admitted event evidence may cross. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-08 | Tampering | hostile event admission | high | mitigate | Signature/id/filter/session/generation/terminal checks before cache mutation plus bypass falsifier. |
| T-08-09 | Spoofing | stale/unattributed subscription | high | mitigate | Exact subscription and generation attribution; terminal set per opened session. |
| T-08-10 | Denial of Service | inbound frame buffering | high | mitigate | Tungstenite frame/message limits plus owner defense and max/max+1 tests. |
| T-08-11 | Repudiation | scoped diagnostics | medium | mitigate | Exact relay/session/generation/request reasons and healthy concurrent witness. |
| T-08-24 | Tampering | Cargo dependencies | low | accept | No package install or dependency change; locked existing workspace only. |
</threat_model>

<verification>
Run complete scripted/real-WebSocket hostile targets, healthy concurrency, admission break/restoration, Cargo/Bazel/Clippy/vocabulary/line/diff, and stash-identity gates.
</verification>

<success_criteria>
The existing hostile WIP becomes a committed, complete owner/public boundary whose dangerous bypass is causally detected.
</success_criteria>

<output>
Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-03-SUMMARY.md` when done.
</output>
