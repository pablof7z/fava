---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 04
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - crates/fava-write-store-memory/src/lib.rs
  - crates/fava-write-store-memory/src/lifecycle.rs
  - crates/fava-write-store-memory/src/semantic.rs
  - crates/fava-write-store-memory/src/semantic/delivery.rs
  - crates/fava-write-store-memory/src/bounds.rs
  - crates/fava-write-store-memory/tests/delivery_lifecycle.rs
  - crates/fava-write-store-memory/BUILD.bazel
  - crates/fava-write-store-redb/src/semantic.rs
  - crates/fava-write-store-redb/src/bounds.rs
  - crates/fava-write-store-redb/tests/delivery_lifecycle.rs
  - crates/fava-write-store-redb/tests/process_kill.rs
  - crates/fava-write-store-redb/BUILD.bazel
  - docs/issues/0029-m8-delivery-store-parity.md
autonomous: true
requirements: [HARD-05, HARD-06, HARD-07]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "The three dirty Memory paths and one dirty Redb path are each adopted by exactly one task after the neutral/public contract is committed."
    - "Memory and Redb preserve independent operation-generation and spent-attempt facts across reopen."
    - "Unreachable is retryable without spending; real outcomes spend exactly one; stale or ambiguous facts remain exact."
  artifacts:
    - "Memory and Redb delivery_lifecycle provider corpora"
    - "Redb process-kill/reopen generation-budget proof"
    - "docs/issues/0029-m8-delivery-store-parity.md"
  key_links:
    - "Plan 02 receipt contract -> Memory/Redb transition CAS -> reopened public receipt truth"
  prohibitions:
    - statement: "A store must not repair, infer, or collapse generation, spent budget, or ambiguous outcome during reopen."
      status: unverified
      flagged: true
      verification: provider parity plus process-kill tests
---

<objective>
Adopt the remaining dirty store providers and close durable parity separately from delivery contract/public ownership.

Purpose: Make Plan 02 self-contained across Memory and Redb without oversized mixed ownership.
Output: Four adopted store paths, shared lifecycle corpus, restart proof, Bazel registration, and causal break.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-02-SUMMARY.md
@.planning/debug/resolved/m8-unreachable-delivery-retry.md
@crates/fava-write-store-redb/src/ops.rs
</context>

## Artifacts this phase produces

- Exact Memory/Redb `delivery_lifecycle` provider tests for generation, spent attempts, unreachable, ambiguity, and stale refusal.
- Redb child-process reopen evidence preserving the same receipt facts.
- `docs/issues/0029-m8-delivery-store-parity.md` with `DELIBERATE_BREAK_M8_STORE_GENERATION_PARITY`.

<slice_contract>
Issue: `docs/issues/0029-m8-delivery-store-parity.md`  
Branch: `m8/delivery-store-parity`  
Validation: Memory/Redb exact lifecycle/restart, break, Cargo/Bazel/Clippy/vocabulary/line/diff/stash  
Commit series: Memory tracer adoption -> Redb/restart parity -> break and graph closure
</slice_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Adopt dirty Memory state through the committed public receipt contract</name>
  <read_first>.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-02-SUMMARY.md, crates/fava-write-store-memory/src/lib.rs, crates/fava-write-store-memory/src/lifecycle.rs, crates/fava-write-store-memory/src/semantic.rs, crates/fava-write-store-redb/src/ops.rs</read_first>
  <files>crates/fava-write-store-memory/src/lib.rs, crates/fava-write-store-memory/src/lifecycle.rs, crates/fava-write-store-memory/src/semantic.rs, crates/fava-write-store-memory/src/bounds.rs, crates/fava-write-store-memory/tests/delivery_lifecycle.rs</files>
  <behavior>
    - Memory initializes generation and spent attempts independently for raw and semantic receipts.
    - Unreachable permits only the next exact generation and spends zero.
    - Attempting completion spends one; stale generation/outcome is refused.
  </behavior>
  <action>Fingerprint and adopt the three existing dirty Memory files exactly once after Plan 02 is committed. Write the shared lifecycle cases RED, preserve validate-update-notify ordering, and extract the later OPS-004 admission surface into private `bounds.rs` while the dirty `lib.rs` adoption is owned here; Plan 06 edits only that extracted module. Do not edit the neutral receipt contract or create another counter/persisted entity.</action>
  <acceptance_criteria>The Memory provider executes a complete accepted-write-to-retry lifecycle against the committed contract and all three WIP paths have one adoption owner.</acceptance_criteria>
  <verify><automated>cargo test -p fava-write-store-memory --test delivery_lifecycle &amp;&amp; cargo test -p fava-write-store-memory &amp;&amp; bazel test //crates/fava-write-store-memory:all</automated></verify>
  <done>Memory delivery truth is committed and provider-complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adopt dirty Redb semantic state and prove durable parity/reopen</name>
  <read_first>crates/fava-write-store-redb/src/semantic.rs, crates/fava-write-store-redb/src/ops.rs, crates/fava-write-store-redb/tests/delivery_lifecycle.rs, crates/fava-write-store-redb/tests/process_kill.rs, docs/internals/vocabulary.toml</read_first>
  <files>crates/fava-write-store-redb/src/semantic.rs, crates/fava-write-store-redb/src/bounds.rs, crates/fava-write-store-redb/tests/delivery_lifecycle.rs, crates/fava-write-store-redb/tests/process_kill.rs, docs/issues/0029-m8-delivery-store-parity.md</files>
  <behavior>
    - Redb matches every Memory transition and semantic initialization case.
    - Process reopen reconstructs exact generation, spent count, destination outcome, and ambiguity.
    - Removing Unreachable from the valid source set fails the exact retry/reopen tests.
  </behavior>
  <action>Fingerprint and adopt the one dirty Redb semantic path, port the Task 1 corpus, extend the existing crash/reopen harness, and create private `bounds.rs` as the only store file Plan 06 later edits. Run `DELIBERATE_BREAK_M8_STORE_GENERATION_PARITY` by temporarily removing only the Redb Unreachable-to-next-generation seam, require causal failure, then restore exact checksums. Preserve `#[serde(default)]` and existing schema; do not add compatibility paths.</action>
  <acceptance_criteria>Memory and Redb return identical lifecycle facts and Redb restart preserves them without repair or inference.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava-write-store-memory -p fava-write-store-redb
cargo test -p fava-write-store-redb --test delivery_lifecycle
cargo test -p fava-write-store-redb --test process_kill
cargo clippy -p fava-write-store-memory -p fava-write-store-redb --all-targets -- -D warnings
bazel test //crates/fava-write-store-memory:all //crates/fava-write-store-redb:all
python3 tools/check_vocabulary.py
rg -n '^DELIBERATE_BREAK_M8_STORE_GENERATION_PARITY: PASS ' docs/issues/0029-m8-delivery-store-parity.md
test "$(git rev-parse 'stash@{0}')" = 5faecf42c0ec903507e3faeb04962f4680a9cb44
git diff --check</automated></verify>
  <done>Store parity is durable, causal, and ready for Task 3 build registration.</done>
</task>

<task type="auto">
  <name>Task 3: Extract Memory semantic delivery logic and register parity graphs</name>
  <read_first>crates/fava-write-store-memory/src/semantic.rs, crates/fava-write-store-memory/BUILD.bazel, crates/fava-write-store-redb/BUILD.bazel, crates/fava-write-store-memory/tests/delivery_lifecycle.rs, crates/fava-write-store-redb/tests/process_kill.rs</read_first>
  <files>crates/fava-write-store-memory/src/semantic.rs, crates/fava-write-store-memory/src/semantic/delivery.rs, crates/fava-write-store-memory/BUILD.bazel, crates/fava-write-store-redb/BUILD.bazel</files>
  <action>Start after Task 1 commits the existing Memory semantic bytes. Move the cohesive private delivery receipt initialization/transition implementation from the current 502-line `semantic.rs` into private `semantic/delivery.rs`, preserving transaction ordering and the public store contract; require both files below 500 lines with no waiver. Register the exact lifecycle and process-kill tests after both providers and break restoration are committed. Keep Cargo/Bazel target coverage symmetric and add no runtime dependency or package.</action>
  <acceptance_criteria>The committed Memory semantic owner is split below 500 lines and both provider BUILD files expose the exact lifecycle/restart tests without changing dependency direction.</acceptance_criteria>
  <verify><automated>set -euo pipefail
bazel test //crates/fava-write-store-memory:all //crates/fava-write-store-redb:all
cargo test -p fava-write-store-memory -p fava-write-store-redb
for file in crates/fava-write-store-memory/src/semantic.rs crates/fava-write-store-memory/src/semantic/delivery.rs; do lines="$(wc -l &lt; "$file" | tr -d ' ')"; test "$lines" -lt 500; done
git diff --check</automated></verify>
  <done>Memory semantic code is cohesive and below the soft limit; Memory/Redb parity is registered in both build systems.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

## Trust Boundaries

| Boundary | Description |
|---|---|
| neutral receipt -> Memory/Redb -> process reopen | Contract facts cross replaceable persistence and process lifetime. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-12 | Tampering | store generation CAS | high | mitigate | Shared exact lifecycle corpus and stale refusal. |
| T-08-13 | Repudiation | durable spent budget | high | mitigate | Independent persisted fact plus process reopen. |
| T-08-14 | Repudiation | ambiguous recovery | high | mitigate | Exact outcome survives restart without inference. |
| T-08-25 | Tampering | dependencies | low | accept | Existing locked providers only. |
</threat_model>

<verification>Run Memory/Redb lifecycle and process-kill targets, named break, Cargo/Bazel/Clippy/vocabulary/line/diff/stash gates.</verification>
<success_criteria>All store WIP is committed once and both providers satisfy the same public delivery contract across restart.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-04-SUMMARY.md` when done.</output>
