---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 17
type: execute
wave: 12
depends_on: [08-04, 08-11, 08-12, 08-13, 08-14, 08-16]
files_modified:
  - apps/canary/scenarios.json
  - apps/canary/src/lib.rs
  - apps/canary/src/main.rs
  - apps/canary/src/m8.rs
  - apps/canary/src/m8_child.rs
  - apps/canary/src/m8_failure.rs
  - apps/canary/src/proxy.rs
  - apps/canary/src/relay.rs
  - features/hostile-boundaries.feature
  - tools/tests/test_m8_feature_mapping.py
  - docs/issues/0035-m8-delivery-provider-process-canaries.md
autonomous: true
requirements: [HARD-05, HARD-06, HARD-07, HARD-08, HARD-09, HARD-10]
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Attempt ceiling and ambiguity persist exactly across real process restart."
    - "Provider-failure-isolation uses fully Runtime-migrated owners and proves bounded unrelated progress, shutdown, and reap."
    - "All three final scenario executors are public/CLI reachable and produce bounded independently witnessed evidence."
  artifacts: ["ambiguous-handoff executor", "attempt-ceiling executor", "provider-failure-isolation executor", "docs/issues/0035-m8-delivery-provider-process-canaries.md"]
  key_links: ["owner/Runtime behavior -> real process/proxy/restart -> bounded evidence -> public CLI dispatch"]
  prohibitions:
    - statement: "In-process substitutes, inferred handoff truth, unbounded retry/provider work, or unwitnessed success cannot satisfy delivery/provider closure."
      status: unverified
      flagged: true
      verification: process/proxy/provider RED, named process break, public CLI behavior
---

<objective>
Implement the three remaining delivery/provider process canaries through public Fava and the canary CLI.

Purpose: Complete physical/restart/provider behavior before the final manifest and clean-tree seal.
Output: Three enabled executors, bounded evidence, public/CLI tests, and one causal process break.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-11-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-12-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-13-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-14-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-16-SUMMARY.md
</context>

## Artifacts this phase produces

- Exact executors `ambiguous-handoff`, `attempt-ceiling`, and `provider-failure-isolation`.
- Transparent proxy/restart evidence for durable ambiguity and exact attempt budget.
- Five-mode provider process evidence with exact operation/generation, adjacent progress, bounded shutdown, and reap.
- `DELIBERATE_BREAK_M8_DELIVERY_PROVIDER_PROCESS`.

<slice_contract>
Issue: `docs/issues/0035-m8-delivery-provider-process-canaries.md`
Branch: `m8/delivery-provider-process-canaries`
Validation: process/proxy RED -> exact delivery behavior -> provider RED -> causal process break -> public CLI/mapping
Commit series: delivery process tracer -> provider process GREEN/break -> registry/CLI behavior
</slice_contract>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove attempt budget and ambiguity across real process restart</name>
  <read_first>apps/canary/src/publication.rs, apps/canary/src/proxy.rs, crates/fava/tests/delivery_bounds.rs, .planning/debug/resolved/m8-unreachable-delivery-retry.md, docs/issues/0035-m8-delivery-provider-process-canaries.md</read_first>
  <files>apps/canary/src/m8.rs, apps/canary/src/m8_child.rs, apps/canary/src/proxy.rs, apps/canary/src/relay.rs, docs/issues/0035-m8-delivery-provider-process-canaries.md</files>
  <behavior>
    - Offline intervals spend zero; real retryable failures spend exact attempts to GivenUp; restart preserves identity/counters.
    - Proxy witnesses full EVENT handoff then cut-before-OK; restart preserves ambiguity without duplicate effect.
  </behavior>
  <action>Write exact failing process tests first, using controlled readiness/barriers rather than sleeps for order. Implement both public/process/proxy/restart scenarios with bounded evidence; the proxy alone owns handoff truth. Record the causal RED assertions and process identities in issue 0035.</action>
  <acceptance_criteria>Both delivery scenarios cross physical and durable boundaries with exact outcomes and independent witnesses.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test --manifest-path apps/canary/Cargo.toml --lib attempt_ceiling
cargo test --manifest-path apps/canary/Cargo.toml --lib ambiguous_handoff
git diff --check</automated></verify>
  <done>Delivery retry/ambiguity process capstones are executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement provider process isolation and kill the process bypass</name>
  <read_first>crates/fava/tests/provider_failure_isolation.rs, apps/canary/src/m8.rs, apps/canary/src/m8_failure.rs, apps/canary/src/relay.rs, docs/issues/0035-m8-delivery-provider-process-canaries.md</read_first>
  <files>apps/canary/src/m8.rs, apps/canary/src/m8_failure.rs, apps/canary/src/relay.rs, apps/canary/src/lib.rs, docs/issues/0035-m8-delivery-provider-process-canaries.md</files>
  <behavior>
    - Panic, block, late, malformed, and cancellation-ignore providers are exact-operation/generation scoped.
    - Adjacent work completes; shutdown and child reap remain within declared Runtime/resource bounds.
  </behavior>
  <action>Write provider process RED, then implement the real child/socket executor using Plans 07-14 Runtime closure and exact bounded evidence. Run `DELIBERATE_BREAK_M8_DELIVERY_PROVIDER_PROCESS` by replacing the child/provider boundary with in-process or uncorrelated completion while keeping the test compilable; require the exact provider/process assertion to fail, restore checksums, rerun GREEN, and record PASS in issue 0035.</action>
  <acceptance_criteria>Provider failure isolation crosses a real process boundary, preserves adjacent progress, reaps resources, and kills the named bypass.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test --manifest-path apps/canary/Cargo.toml --lib provider_failure_isolation
rg -n '^DELIBERATE_BREAK_M8_DELIVERY_PROVIDER_PROCESS: PASS ' docs/issues/0035-m8-delivery-provider-process-canaries.md
git diff --check</automated></verify>
  <done>Provider process isolation is behavior-complete and causal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose all three executors through public CLI behavior</name>
  <read_first>apps/canary/scenarios.json, apps/canary/src/lib.rs, apps/canary/src/main.rs, features/hostile-boundaries.feature, tools/tests/test_m8_feature_mapping.py</read_first>
  <files>apps/canary/scenarios.json, apps/canary/src/lib.rs, apps/canary/src/main.rs, features/hostile-boundaries.feature, tools/tests/test_m8_feature_mapping.py</files>
  <behavior>
    - Each final ID occurs once, is enabled, dispatches to its exact executor, and maps to executable feature/evidence.
    - CLI list and individual run behavior expose no manifest-only or unreachable scenario.
  </behavior>
  <action>Add the three exact registry, dispatch, CLI, feature, and focused mapping rows. Test each executor independently and assert the three exact enabled list lines. Do not create final break/threat/line manifests or run the milestone clean seal; Plan 18 owns those.</action>
  <acceptance_criteria>The three final scenarios are independently public/CLI reachable and ready for the final seven-ID composition.</acceptance_criteria>
  <verify><automated>set -euo pipefail
python3 -m unittest tools.tests.test_m8_feature_mapping
cargo test --manifest-path apps/canary/Cargo.toml --all-targets
M8_FINAL_THREE="$(cargo run --manifest-path apps/canary/Cargo.toml -- list)"
test "$(printf '%s
' "$M8_FINAL_THREE" | rg -c '^(ambiguous-handoff|attempt-ceiling|provider-failure-isolation)[[:space:]]+M8[[:space:]]+enabled$')" -eq 3
git diff --check</automated></verify>
  <done>Delivery/provider canaries are public and CLI behavior, not only source implementations.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Boundary | Description |
|---|---|
| public Fava -> canary child/proxy/provider process | Real transport, handoff, restart, hostile completion, and OS lifecycle cross independently witnessed boundaries. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-72 | Repudiation | ambiguous handoff/restart | high | mitigate | Proxy cut-before-OK plus durable reopen evidence. |
| T-08-73 | Denial of Service | retry/provider child | high | mitigate | Exact attempt/deadline/resource bounds, adjacent progress, reap. |
</threat_model>

<verification>All three process executors, public tests, focused CLI lines, bounded evidence, and the process-boundary deliberate break pass.</verification>
<success_criteria>Delivery, ambiguity, attempt ceiling, and provider isolation are externally executable before final milestone sealing.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-17-SUMMARY.md` when done.</output>

