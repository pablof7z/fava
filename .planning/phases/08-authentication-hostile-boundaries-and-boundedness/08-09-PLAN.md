---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 09
type: execute
wave: 6
depends_on: [08-07, 08-08]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - MODULE.bazel
  - crates/fava-runtime/Cargo.toml
  - crates/fava-runtime/BUILD.bazel
  - crates/fava-runtime-tokio/Cargo.toml
  - crates/fava-runtime-tokio/BUILD.bazel
  - crates/fava-standard/Cargo.toml
  - crates/fava-standard/BUILD.bazel
  - crates/fava/Cargo.toml
  - crates/fava/BUILD.bazel
  - crates/fava-publication/Cargo.toml
  - crates/fava-publication/BUILD.bazel
  - crates/fava-auth/Cargo.toml
  - crates/fava-auth/BUILD.bazel
  - crates/fava-routing/Cargo.toml
  - crates/fava-routing/BUILD.bazel
  - crates/fava-observe/Cargo.toml
  - crates/fava-observe/BUILD.bazel
  - crates/fava-nip11-http/Cargo.toml
  - crates/fava-nip11-http/BUILD.bazel
  - crates/fava-publisher-nip01/Cargo.toml
  - crates/fava-publisher-nip01/BUILD.bazel
  - crates/fava/tests/provider_failure_isolation.rs
  - crates/fava/tests/runtime_auth_routing.rs
  - crates/fava/tests/runtime_sessions.rs
  - crates/fava/tests/runtime_delivery.rs
  - crates/fava-publication/tests/provider_runtime.rs
  - crates/fava-routing/tests/failure_isolation.rs
  - crates/fava-observe/tests/runtime_ownership.rs
  - crates/fava-nip11-http/tests/runtime_ownership.rs
  - crates/fava-publisher-nip01/tests/runtime_ownership.rs
  - tools/tests/test_runtime_build_graph.py
  - docs/issues/0023-m8-runtime-contract.md
  - docs/issues/0026-m8-runtime-facade-standard-surface.md
  - docs/issues/0030-m8-central-runtime-build-graph.md
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-05, HARD-06, HARD-07, HARD-08, HARD-09]
estimate: {tokens: 54000, raw_tokens: 54000, tasks: 8, confidence: low}
must_haves:
  truths:
    - "The Runtime contract, Tokio first implementation, FavaBuilder surface, and standard_builder compile before any owner execution migration."
    - "Every future consumer already has its unused normal fava-runtime Cargo/Bazel edge while retaining pre-migration dependencies, so later owner tasks edit source/tests only."
    - "One plan owns root workspace, all runtime/standard/consumer metadata, MODULE metadata, and the phase's single Cargo.lock regeneration."
    - "The graph-test RED succeeds only for one exact missing-edge assertion; unrelated import/syntax/test errors cannot satisfy it."
    - "Every predeclared test target has an honest compile/smoke source before Bazel :all or Cargo --all-targets runs; no scaffold claims unimplemented Runtime behavior."
  artifacts: ["complete pre-migration Cargo/Bazel Runtime graph", "single Cargo.lock", "test_runtime_build_graph.py", "compiled TokioRuntime", "nine private wave0_compile_smoke test sources"]
  key_links: ["Runtime source -> contract/provider targets -> all predeclared consumer edges -> locked compile -> later compiled owner RED tests"]
  prohibitions:
    - statement: "No later plan may edit a Cargo manifest, BUILD dependency edge, MODULE metadata, or Cargo.lock for Runtime migration."
      status: unverified
      flagged: true
      verification: exact graph test, plan ownership scan, final git history
---

<objective>
Land and compile the complete Runtime build graph in pre-migration production state.

Purpose: Give every later owner task a real compiled contract/provider and dependency edge so behavioral RED can fail for the intended assertion rather than missing infrastructure.
Output: Central Cargo/Bazel/MODULE graph, single lock regeneration, pre-migration compile, three causal graph/interface breaks.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@Cargo.toml
@Cargo.lock
@MODULE.bazel
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-07-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-08-SUMMARY.md
</context>

## Artifacts this phase produces

- Root workspace and Bazel/MODULE registration for `fava-runtime`, `fava-runtime-tokio`, and `fava-standard`.
- Unused normal contract edges for fava, publication, auth, routing, observe, nip11-http, and publisher-nip01 before their source migrations.
- One final regenerated `Cargo.lock`, exact graph checker, and pre-migration Cargo/Bazel compile evidence.
- Nine minimal private test-source scaffolds whose only assertion is that the pre-migration crate/API graph compiles and a smoke harness starts; Plans 10-13 must replace/extend them before behavioral RED.
- Compiled PASS for `DELIBERATE_BREAK_M8_RUNTIME_BOUNDARY`, `DELIBERATE_BREAK_M8_STANDARD_RUNTIME_ORDER`, and `DELIBERATE_BREAK_M8_RUNTIME_BUILD_GRAPH`.

<slice_contract>
Issue: `docs/issues/0030-m8-central-runtime-build-graph.md`
Branch: `m8/pre-migration-runtime-build-graph`
Validation: exact graph RED/output -> all metadata -> one lock -> pre-migration Cargo/Bazel compile -> three causal breaks
Commit series: graph-test/runtime targets -> root/standard -> consumer Cargo -> consumer Bazel -> public scaffolds -> provider scaffolds -> final providers/lock/first GREEN -> compile/break seal
</slice_contract>

<tasks>

<task type="tracer">
  <name>Task 1: Compile graph-test RED against one exact missing root edge</name>
  <read_first>crates/fava-runtime/src/lib.rs, crates/fava-runtime-tokio/src/lib.rs, Cargo.toml, MODULE.bazel</read_first>
  <files>crates/fava-runtime/Cargo.toml, crates/fava-runtime/BUILD.bazel, crates/fava-runtime-tokio/Cargo.toml, crates/fava-runtime-tokio/BUILD.bazel, tools/tests/test_runtime_build_graph.py</files>
  <behavior>
    - The graph checker loads successfully, inspects the two real crate targets, and fails only with `Runtime build graph missing required edge: root workspace member fava-runtime`.
  </behavior>
  <action>Create exact runtime contract/provider manifests and BUILD targets plus a graph test whose first unresolved assertion is the root workspace member edge. The checker must also parse every affected BUILD target's literal source list and fail if any referenced file is absent by Task 8; this source-existence branch remains dormant behind the intentional first missing-edge RED. Capture unittest status/output: require nonzero, the exact stable missing-edge message, and `FAILED (failures=1)`; fail the task on zero status, `ERROR`, import/syntax failure, a different assertion, or multiple failures. Commit this exact RED before adding the root edge. Do not run Cargo or mutate the lock yet.</action>
  <acceptance_criteria>The graph RED proves the intended missing edge exactly and both real runtime packages have complete source-bearing targets.</acceptance_criteria>
  <verify><automated>set -euo pipefail
set +e
GRAPH_RED_OUTPUT="$(python3 -m unittest tools.tests.test_runtime_build_graph 2&gt;&amp;1)"
GRAPH_RED_STATUS=$?
set -e
test "$GRAPH_RED_STATUS" -ne 0
printf '%s\n' "$GRAPH_RED_OUTPUT" | rg -F 'Runtime build graph missing required edge: root workspace member fava-runtime'
printf '%s\n' "$GRAPH_RED_OUTPUT" | rg -F 'FAILED (failures=1)'
if printf '%s\n' "$GRAPH_RED_OUTPUT" | rg -n '(^|[[:space:]])ERROR([[:space:]]|:)|ImportError|ModuleNotFoundError|SyntaxError|FAILED \(errors='; then exit 1; fi
test -s crates/fava-runtime/BUILD.bazel
test -s crates/fava-runtime-tokio/BUILD.bazel</automated></verify>
  <done>One exact captured graph failure is committed; unrelated failures cannot masquerade as RED.</done>
</task>

<task type="auto">
  <name>Task 2: Register root, MODULE, and standard profile metadata</name>
  <read_first>Cargo.toml, MODULE.bazel, crates/fava-standard/src/lib.rs, crates/fava-standard/tests/assembly.rs</read_first>
  <files>Cargo.toml, MODULE.bazel, crates/fava-standard/Cargo.toml, crates/fava-standard/BUILD.bazel, docs/issues/0030-m8-central-runtime-build-graph.md</files>
  <action>Add exact root members/workspace dependencies for all three crates and complete standard Cargo/BUILD targets against the already-authored source. Keep MODULE root manifest/lock inputs exact. Do not regenerate the lock yet; update the graph test so the next stable missing edge is the first universal consumer.</action>
  <acceptance_criteria>Runtime/standard packages are root-registered in both build systems and the graph checker advances past the original RED.</acceptance_criteria>
  <verify><automated>set -euo pipefail
rg -n 'fava-runtime|fava-runtime-tokio|fava-standard' Cargo.toml
rg -n 'Cargo.toml|Cargo.lock' MODULE.bazel
rg -n 'rust_library|rust_test|fava_runtime_tokio' crates/fava-standard/BUILD.bazel
git diff --check</automated></verify>
  <done>Root and standard metadata are centrally declared without lock mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Predeclare every universal consumer Cargo edge</name>
  <read_first>crates/fava/Cargo.toml, crates/fava-publication/Cargo.toml, crates/fava-auth/Cargo.toml, crates/fava-routing/Cargo.toml, crates/fava-observe/Cargo.toml</read_first>
  <files>crates/fava/Cargo.toml, crates/fava-publication/Cargo.toml, crates/fava-auth/Cargo.toml, crates/fava-routing/Cargo.toml, crates/fava-observe/Cargo.toml</files>
  <action>Add unused normal fava-runtime dependencies required by Plans 10-13 while preserving every dependency needed by current pre-migration production. Forbid the concrete provider crate from universal manifests. Do not edit source, run Cargo, or mutate the lock.</action>
  <acceptance_criteria>Five universal consumers can compile their later RED tests without any manifest change.</acceptance_criteria>
  <verify><automated>set -euo pipefail
for manifest in crates/fava/Cargo.toml crates/fava-publication/Cargo.toml crates/fava-auth/Cargo.toml crates/fava-routing/Cargo.toml crates/fava-observe/Cargo.toml; do rg -n 'fava-runtime' "$manifest"; if rg -n 'fava-runtime-tokio' "$manifest"; then exit 1; fi; done
git diff --check</automated></verify>
  <done>Universal Cargo edges are final before migration.</done>
</task>

<task type="auto">
  <name>Task 4: Predeclare every universal consumer Bazel edge</name>
  <read_first>crates/fava/BUILD.bazel, crates/fava-publication/BUILD.bazel, crates/fava-auth/BUILD.bazel, crates/fava-routing/BUILD.bazel, crates/fava-observe/BUILD.bazel</read_first>
  <files>crates/fava/BUILD.bazel, crates/fava-publication/BUILD.bazel, crates/fava-auth/BUILD.bazel, crates/fava-routing/BUILD.bazel, crates/fava-observe/BUILD.bazel</files>
  <action>Add contract dependencies and predeclare every later runtime migration test target while preserving existing pre-migration targets. Forbid concrete-runtime labels. Do not edit source or run Bazel before the final lock/graph task.</action>
  <acceptance_criteria>Five universal Bazel packages have final Runtime/test edges before source migration.</acceptance_criteria>
  <verify><automated>set -euo pipefail
for build in crates/fava/BUILD.bazel crates/fava-publication/BUILD.bazel crates/fava-auth/BUILD.bazel crates/fava-routing/BUILD.bazel crates/fava-observe/BUILD.bazel; do rg -n 'fava-runtime' "$build"; if rg -n 'fava-runtime-tokio' "$build"; then exit 1; fi; done
git diff --check</automated></verify>
  <done>Universal Bazel edges are final before migration.</done>
</task>

<task type="auto">
  <name>Task 5: Create honest public and publication compile/smoke scaffolds</name>
  <read_first>crates/fava/src/lib.rs, crates/fava-publication/src/lib.rs, crates/fava/BUILD.bazel, crates/fava-publication/BUILD.bazel</read_first>
  <files>crates/fava/tests/provider_failure_isolation.rs, crates/fava/tests/runtime_auth_routing.rs, crates/fava/tests/runtime_sessions.rs, crates/fava/tests/runtime_delivery.rs, crates/fava-publication/tests/provider_runtime.rs</files>
  <action>Create each currently absent source referenced by the predeclared Cargo/Bazel targets. Each file contains only a private `wave0_compile_smoke` test that imports the already-compiled public surface and constructs or type-checks the smallest inert harness; it must not name, skip, ignore, or pass an assertion for any unimplemented migration behavior. Do not add the later exact test names or their expected failure strings. Mark the file header `PLAN09_COMPILE_SCAFFOLD: owning Plan 10/11/12/13 must replace or extend before behavioral RED`. Update no build metadata in this task.</action>
  <acceptance_criteria>All four public target sources and publication provider source exist, compile honestly, and cannot be mistaken for passing Runtime behavior.</acceptance_criteria>
  <verify><automated>set -euo pipefail
for file in crates/fava/tests/provider_failure_isolation.rs crates/fava/tests/runtime_auth_routing.rs crates/fava/tests/runtime_sessions.rs crates/fava/tests/runtime_delivery.rs crates/fava-publication/tests/provider_runtime.rs; do test -s "$file"; rg -n '^// PLAN09_COMPILE_SCAFFOLD:' "$file"; rg -n 'wave0_compile_smoke' "$file"; if rg -n 'runtime_(materializer_scopes_panics|publication_run_uses_runtime|auth_provider_uses_runtime|routing_tasks_use_runtime|nip11_deadline_uses_runtime|query_observation_uses_runtime|relay_session_uses_runtime|route_fanout_uses_runtime|publisher_future_uses_runtime|delivery_signer_uses_runtime)|bypassed Runtime|#\[ignore\]' "$file"; then exit 1; fi; done
git diff --check</automated></verify>
  <done>Every public/publication target has a source-bearing honest pre-migration smoke harness.</done>
</task>

<task type="auto">
  <name>Task 6: Create honest provider compile/smoke scaffolds</name>
  <read_first>crates/fava-routing/src/lib.rs, crates/fava-observe/src/lib.rs, crates/fava-nip11-http/src/lib.rs, crates/fava-publisher-nip01/src/lib.rs</read_first>
  <files>crates/fava-routing/tests/failure_isolation.rs, crates/fava-observe/tests/runtime_ownership.rs, crates/fava-nip11-http/tests/runtime_ownership.rs, crates/fava-publisher-nip01/tests/runtime_ownership.rs</files>
  <action>Create each absent provider test source referenced by the predeclared targets. Apply the same exact `PLAN09_COMPILE_SCAFFOLD` and private `wave0_compile_smoke` contract as Task 5: compile/type-check an inert pre-migration harness only, with no ignored/passing placeholder for future behavior and none of the exact RED names/assertion strings. Plans 11-13 explicitly replace or extend these sources before running their causal RED. Update no metadata or lock.</action>
  <acceptance_criteria>Routing, observe, NIP-11, and publisher provider targets all have honest source files before any `:all` invocation.</acceptance_criteria>
  <verify><automated>set -euo pipefail
for file in crates/fava-routing/tests/failure_isolation.rs crates/fava-observe/tests/runtime_ownership.rs crates/fava-nip11-http/tests/runtime_ownership.rs crates/fava-publisher-nip01/tests/runtime_ownership.rs; do test -s "$file"; rg -n '^// PLAN09_COMPILE_SCAFFOLD:' "$file"; rg -n 'wave0_compile_smoke' "$file"; if rg -n 'runtime_(routing_tasks_use_runtime|query_observation_uses_runtime|nip11_deadline_uses_runtime|publisher_future_uses_runtime)|bypassed Runtime|#\[ignore\]' "$file"; then exit 1; fi; done
git diff --check</automated></verify>
  <done>Every provider target has an honest pre-migration compile/smoke source.</done>
</task>

<task type="auto">
  <name>Task 7: Add final provider edges, regenerate the single lock, and make the graph GREEN</name>
  <read_first>crates/fava-nip11-http/Cargo.toml, crates/fava-nip11-http/BUILD.bazel, crates/fava-publisher-nip01/Cargo.toml, crates/fava-publisher-nip01/BUILD.bazel, Cargo.lock</read_first>
  <files>crates/fava-nip11-http/Cargo.toml, crates/fava-nip11-http/BUILD.bazel, crates/fava-publisher-nip01/Cargo.toml, crates/fava-publisher-nip01/BUILD.bazel, Cargo.lock</files>
  <action>Start only after Tasks 5-6 have created all nine registered scaffold sources. Add unused normal contract/test edges for NIP-11 and publisher-nip01 while retaining current pre-migration dependencies. Regenerate Cargo.lock exactly once after every manifest is final. Run the first graph-checker GREEN/source-existence gate now that every registered BUILD source exists; no later plan may edit these files.</action>
  <acceptance_criteria>The full consumer graph is final, every registered source exists before the first GREEN, and one locked resolution covers pre- and post-migration source.</acceptance_criteria>
  <verify><automated>set -euo pipefail
for file in crates/fava/tests/provider_failure_isolation.rs crates/fava/tests/runtime_auth_routing.rs crates/fava/tests/runtime_sessions.rs crates/fava/tests/runtime_delivery.rs crates/fava-publication/tests/provider_runtime.rs crates/fava-routing/tests/failure_isolation.rs crates/fava-observe/tests/runtime_ownership.rs crates/fava-nip11-http/tests/runtime_ownership.rs crates/fava-publisher-nip01/tests/runtime_ownership.rs; do test -s "$file"; done
cargo generate-lockfile
cargo metadata --locked --format-version 1 &gt;/dev/null
python3 -m unittest tools.tests.test_runtime_build_graph
git diff --check</automated></verify>
  <done>All scaffold sources precede the first graph GREEN, and the single final lock is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Compile every source-bearing pre-migration target and kill graph/interface breaks</name>
  <read_first>tools/tests/test_runtime_build_graph.py, docs/issues/0023-m8-runtime-contract.md, docs/issues/0026-m8-runtime-facade-standard-surface.md, docs/issues/0030-m8-central-runtime-build-graph.md</read_first>
  <files>docs/issues/0023-m8-runtime-contract.md, docs/issues/0026-m8-runtime-facade-standard-surface.md, docs/issues/0030-m8-central-runtime-build-graph.md</files>
  <behavior>
    - Runtime contract/Tokio implementation, standard assembly, and every affected current owner compile before source migration.
    - Removing runtime capacity/join accounting, inverting standard construction, or deleting one required graph edge fails its exact compiled oracle.
  </behavior>
  <action>First assert every source path referenced by every predeclared test target exists and carries either its real pre-existing test or the exact Plan 09 smoke marker. Compile/test all affected crates in their still-unmigrated production state under Cargo and Bazel; no `:all` target may refer to an absent file. Execute `DELIBERATE_BREAK_M8_RUNTIME_BOUNDARY`, `DELIBERATE_BREAK_M8_STANDARD_RUNTIME_ORDER`, and `DELIBERATE_BREAK_M8_RUNTIME_BUILD_GRAPH` one at a time; require the exact conformance/assembly/graph assertion, reject compile/unrelated failures, restore checksums without reset/checkout/stash, rerun GREEN, and record PASS. Preserve current owner-local execution until Plans 10-13 replace/extend their scaffolds and create compiled behavioral RED.</action>
  <acceptance_criteria>All infrastructure compiles before migration and each of three named compiling breaks is causally killed.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo metadata --locked --format-version 1 &gt;/dev/null
for file in crates/fava-runtime/tests/contract.rs crates/fava-runtime-tokio/tests/conformance.rs crates/fava-standard/tests/assembly.rs crates/fava-auth/tests/authentication.rs crates/fava/tests/provider_failure_isolation.rs crates/fava/tests/runtime_auth_routing.rs crates/fava/tests/runtime_sessions.rs crates/fava/tests/runtime_delivery.rs crates/fava-publication/tests/provider_runtime.rs crates/fava-routing/tests/failure_isolation.rs crates/fava-observe/tests/runtime_ownership.rs crates/fava-nip11-http/tests/runtime_ownership.rs crates/fava-publisher-nip01/tests/runtime_ownership.rs; do test -s "$file"; done
cargo test -p fava-runtime -p fava-runtime-tokio -p fava-standard -p fava -p fava-publication -p fava-auth -p fava-routing -p fava-observe -p fava-nip11-http -p fava-publisher-nip01
cargo clippy -p fava-runtime -p fava-runtime-tokio -p fava-standard -p fava -p fava-publication -p fava-auth -p fava-routing -p fava-observe -p fava-nip11-http -p fava-publisher-nip01 --all-targets -- -D warnings
bazel test //crates/fava-runtime:all //crates/fava-runtime-tokio:all //crates/fava-standard:all //crates/fava:all //crates/fava-publication:all //crates/fava-auth:all //crates/fava-routing:all //crates/fava-observe:all //crates/fava-nip11-http:all //crates/fava-publisher-nip01:all
python3 -m unittest tools.tests.test_runtime_build_graph
rg -n '^DELIBERATE_BREAK_M8_RUNTIME_BOUNDARY: PASS ' docs/issues/0023-m8-runtime-contract.md
rg -n '^DELIBERATE_BREAK_M8_STANDARD_RUNTIME_ORDER: PASS ' docs/issues/0026-m8-runtime-facade-standard-surface.md
rg -n '^DELIBERATE_BREAK_M8_RUNTIME_BUILD_GRAPH: PASS ' docs/issues/0030-m8-central-runtime-build-graph.md
git diff --check</automated></verify>
  <done>The complete build/runtime/standard graph is compiled and causally sealed before any owner migration.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-37 | Tampering | false graph RED | high | mitigate | Exact captured message/status and error rejection. |
| T-08-38 | Elevation of Privilege | concrete dependency inversion | high | mitigate | Exact Cargo/Bazel graph checker. |
| T-08-39 | Tampering | parallel manifest/lock mutation | high | mitigate | One owner and one regeneration. |
| T-08-40 | Repudiation | uncompiled infrastructure | high | mitigate | Pre-migration Cargo/Bazel compile. |
| T-08-41 | Tampering | Cargo/Bazel/MODULE mismatch | high | mitigate | Locked metadata plus dual-build graph test. |
</threat_model>

<verification>Exact graph RED, final graph GREEN, single lock, all referenced test sources present, pre-migration Cargo/Bazel compile, and three compiled breaks pass.</verification>
<success_criteria>Every later owner task can start with a compiled behavioral RED without adding build metadata or changing the lock.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-09-SUMMARY.md` when done.</output>
