---
phase: 08-authentication-hostile-boundaries-and-boundedness
plan: 12
type: execute
wave: 8
depends_on: [08-03, 08-09]
files_modified:
  - crates/fava/src/query_source.rs
  - crates/fava-observe/src/lib.rs
  - crates/fava-observe/src/runtime.rs
  - crates/fava-observe/tests/runtime_ownership.rs
  - crates/fava/src/relay.rs
  - crates/fava/src/relay/runtime.rs
  - crates/fava/src/live.rs
  - crates/fava/src/routes.rs
  - crates/fava/tests/runtime_sessions.rs
  - docs/issues/0031-m8-runtime-observe-facade.md
autonomous: true
requirements: [HARD-03, HARD-08, HARD-09]
estimate: {tokens: 30000, raw_tokens: 30000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Query-source polling, observation, relay sessions, and route fan-out use the selected Runtime."
    - "The live query_source spawn and post-adoption relay timeout each first fail an exact compiled public test for their intended bypass."
    - "Hostile session failure and late completion remain exact-session/generation scoped while adjacent public work completes."
  artifacts:
    - "runtime_query_observation_uses_runtime"
    - "runtime_relay_session_uses_runtime"
    - "runtime_route_fanout_uses_runtime"
    - "docs/issues/0031-m8-runtime-observe-facade.md"
  key_links:
    - "FavaBuilder Runtime -> source observation/relay/route execution -> correlated completion -> owner state"
  prohibitions:
    - statement: "Facade and observation owners must not retain local execution-resource bypasses."
      status: unverified
      flagged: true
      verification: exact compiled RED/GREEN/break cycles plus Plan 14 audit
---

<objective>
Migrate source observation and facade relay/route execution after hostile-WIP adoption and the complete pre-migration Runtime graph.

Purpose: Remove confirmed live bypasses through causal compiled behavior while preserving relay authority and exact session identity.
Output: Three owner migrations, three compiled RED commits, private adapters, and public session evidence.
</objective>

<execution_context>
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/workflows/execute-plan.md
@/Users/pablofernandez/Work/nnn/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-03-SUMMARY.md
@.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-09-SUMMARY.md
@crates/fava/src/query_source.rs
@crates/fava/src/relay.rs
</context>

## Artifacts this phase produces

- Private `fava_observe::runtime` and `fava::relay::runtime` adapters; no owner-local executor contract.
- Tests `runtime_query_observation_uses_runtime`, `runtime_relay_session_uses_runtime`, and `runtime_route_fanout_uses_runtime`.
- Markers `DELIBERATE_BREAK_M8_RUNTIME_QUERY_OBSERVE`, `DELIBERATE_BREAK_M8_RUNTIME_RELAY_SESSION`, and `DELIBERATE_BREAK_M8_RUNTIME_ROUTE_FANOUT`.

<slice_contract>
Issue: `docs/issues/0031-m8-runtime-observe-facade.md`
Branch: `m8/runtime-observe-facade`
Validation: compiled RED commit per live owner -> GREEN -> same-test type-correct break -> restoration -> Cargo/Bazel
Commit series: query/observe RED/GREEN/break -> relay RED/GREEN/break -> route RED/GREEN/break
</slice_contract>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Compile observation RED against query_source spawn, then migrate it</name>
  <read_first>crates/fava/src/query_source.rs, crates/fava-observe/src/lib.rs, crates/fava/tests/runtime_sessions.rs, crates/fava-runtime/src/lib.rs, docs/issues/0031-m8-runtime-observe-facade.md</read_first>
  <files>crates/fava/src/query_source.rs, crates/fava-observe/src/lib.rs, crates/fava-observe/src/runtime.rs, crates/fava-observe/tests/runtime_ownership.rs, crates/fava/tests/runtime_sessions.rs</files>
  <behavior>
    - Recording Runtime observes source polling with exact generation identity.
    - One blocked or panicking source does not block another observation.
    - Pre-migration production fails with `observation polling bypassed Runtime`.
  </behavior>
  <action>Replace or extend Plan 09's public sessions and observe-provider scaffolds with exact test `runtime_query_observation_uses_runtime`; remove smoke markers only after real behavioral setup exists. Capture its single Cargo filter against still-unmigrated `query_source.rs`. Require nonzero and exact `observation polling bypassed Runtime`; reject compiler/import/syntax/unrelated assertion output, then commit RED. Replace the confirmed spawn plus polling timer/cancel/join with Runtime operations while the owner retains demand/result authority. Pass public/owner tests. Run type-correct `DELIBERATE_BREAK_M8_RUNTIME_QUERY_OBSERVE` by locally spawning the tested poll; require the same compiled assertion, restore, and retain evidence for Task 3 to record. Edit no metadata; the negative source scan is only a backstop.</action>
  <acceptance_criteria>The exact live spawn is preceded by a causal compiled RED and cannot return without detection.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava --test runtime_sessions runtime_query_observation_uses_runtime -- --exact --nocapture
cargo test -p fava-observe --all-targets
rg -n 'tokio::spawn' crates/fava/src/query_source.rs &amp;&amp; exit 1 || true
git diff --check</automated></verify>
  <done>Source polling and observation execution are Runtime-owned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile relay RED against the Plan 03 timeout, then migrate sessions</name>
  <read_first>.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-03-SUMMARY.md, crates/fava/src/relay.rs, crates/fava/src/live.rs, crates/fava/tests/runtime_sessions.rs, docs/issues/0031-m8-runtime-observe-facade.md</read_first>
  <files>crates/fava/src/relay.rs, crates/fava/src/relay/runtime.rs, crates/fava/src/live.rs, crates/fava/tests/runtime_sessions.rs, docs/issues/0031-m8-runtime-observe-facade.md</files>
  <behavior>
    - Recording Runtime owns session tasks, timer, cancellation, join, and shutdown deadline.
    - Completion mutates only exact relay/session generation.
    - Plan 03-committed pre-migration timeout fails with `relay session execution bypassed Runtime`.
  </behavior>
  <action>Start after Plan 03's relay WIP commit and Plan 09's graph compile. Extend the now-behavioral `runtime_sessions.rs` with exact test `runtime_relay_session_uses_runtime`; capture its Cargo filter against the still-unmigrated Plan 03 timeout. Require nonzero plus exact `relay session execution bypassed Runtime`; reject compiler/import/syntax/unrelated failures, commit RED. Migrate relay/live execution resources to Runtime while `OpenedRelay` retains protocol/admission authority. Pass the same test. Run type-correct `DELIBERATE_BREAK_M8_RUNTIME_RELAY_SESSION` by reinstating one local timeout; require the same assertion, restore, record PASS. Do not reopen hostile cases or edit metadata; the negative source scan is only a backstop.</action>
  <acceptance_criteria>The RED proves the Plan 03-committed timeout bypass specifically; GREEN/break preserve exact session isolation.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava --test runtime_sessions runtime_relay_session_uses_runtime -- --exact --nocapture
rg -n '^DELIBERATE_BREAK_M8_RUNTIME_RELAY_SESSION: PASS ' docs/issues/0031-m8-runtime-observe-facade.md
rg -n 'tokio::time::timeout' crates/fava/src/relay.rs &amp;&amp; exit 1 || true
for file in crates/fava/src/relay.rs crates/fava/src/relay/runtime.rs; do lines="$(wc -l &lt; "$file" | tr -d ' ')"; test "$lines" -lt 500; done
git diff --check</automated></verify>
  <done>Facade relay sessions use Runtime without changing semantic ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compile route-fanout RED, then migrate its execution resources</name>
  <read_first>crates/fava/src/routes.rs, crates/fava/tests/runtime_sessions.rs, crates/fava-runtime/src/lib.rs, docs/issues/0031-m8-runtime-observe-facade.md</read_first>
  <files>crates/fava/src/routes.rs, crates/fava/tests/runtime_sessions.rs, docs/issues/0031-m8-runtime-observe-facade.md</files>
  <behavior>
    - Route fan-out work and joins are observed through selected Runtime.
    - Exact route operation/generation gates completion.
    - Pre-migration production fails with `route fan-out bypassed Runtime`.
  </behavior>
  <action>Extend the behavioral replacement of Plan 09's sessions scaffold with exact test `runtime_route_fanout_uses_runtime`; capture its Cargo filter against still-unmigrated routes production. Require nonzero plus exact `route fan-out bypassed Runtime`; reject compiler/import/syntax/unrelated failures, commit RED. Migrate task/join/cancellation resources to Runtime without moving route selection authority. Pass exact test. Run type-correct `DELIBERATE_BREAK_M8_RUNTIME_ROUTE_FANOUT` with one local fan-out task; require same assertion, restore, record PASS. Record Task 1's query-observation marker evidence here. Source-shape audit is only a backstop.</action>
  <acceptance_criteria>The route bypass has a causal compiled RED and same-test compiling break after GREEN.</acceptance_criteria>
  <verify><automated>set -euo pipefail
cargo test -p fava --test runtime_sessions runtime_route_fanout_uses_runtime -- --exact --nocapture
rg -n '^DELIBERATE_BREAK_M8_RUNTIME_ROUTE_FANOUT: PASS ' docs/issues/0031-m8-runtime-observe-facade.md
rg -n '^DELIBERATE_BREAK_M8_RUNTIME_QUERY_OBSERVE: PASS ' docs/issues/0031-m8-runtime-observe-facade.md
git diff --check</automated></verify>
  <done>Route fan-out uses Runtime and exact owner correlation.</done>
</task>

</tasks>

<threat_model>
## ASVS L1 Scope

| Boundary | Description |
|---|---|
| relay/source sessions -> facade Runtime | Untrusted blocking, panic, timing, and stale completions cross into universal owners. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-50 | Denial of Service | observation poll | high | mitigate | Runtime capacity/deadline and adjacent source witness. |
| T-08-51 | Spoofing | relay completion | high | mitigate | Exact relay/session/generation correlation. |
| T-08-52 | Tampering | route fan-out | high | mitigate | Owner authorization and operation match. |
| T-08-53 | Elevation of Privilege | local execution | high | mitigate | Three causal bypass tests and audit. |
| T-08-54 | Repudiation | shutdown/reap | medium | mitigate | Runtime join/deadline evidence. |
</threat_model>

<verification>Each live facade/observe owner has a committed compiled RED before migration, exact GREEN, same-test compiling break, and no metadata mutation.</verification>
<success_criteria>Observation, relay sessions, and route fan-out use only selected Runtime execution resources.</success_criteria>
<output>Create `.planning/phases/08-authentication-hostile-boundaries-and-boundedness/08-12-SUMMARY.md` when done.</output>
